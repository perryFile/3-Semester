var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/main.js
var main_exports = {};
__export(main_exports, {
  default: () => main_default
});
module.exports = __toCommonJS(main_exports);

// src/notedraw-plugin.js
var import_obsidian = require("obsidian");

// extras/code-1.jpg
var code_1_default = "data:image/jpeg;base64,";

// extras/code-2.png
var code_2_default = "data:image/png;base64,";

// src/canvas-sizing.mjs
var DEFAULT_MIN_WINDOW_HEIGHT = 1024;
var DEFAULT_MAX_WINDOW_HEIGHT = 4096;
function finitePositive(value, fallback) {
  const number = Number(value);
  return Number.isFinite(number) && number > 0 ? number : fallback;
}
function clamp(value, min, max) {
  return Math.min(max, Math.max(min, value));
}
function measureWithoutFloatingMarkdown(root, measure) {
  const elements = Array.from(root?.querySelectorAll?.(".notedraw-md-block.is-floating") || []);
  if (!elements.length) {
    return measure();
  }
  const states = elements.map((element) => ({
    element,
    value: element.style?.getPropertyValue?.("display") || "",
    priority: element.style?.getPropertyPriority?.("display") || ""
  }));
  try {
    for (const { element } of states) {
      element.style?.setProperty?.("display", "none", "important");
    }
    return measure();
  } finally {
    for (const { element, value, priority } of states) {
      if (value) {
        element.style?.setProperty?.("display", value, priority);
      } else {
        element.style?.removeProperty?.("display");
      }
    }
  }
}
function measureCanvasExtent(previewEl, measureEl = null, visualScale = 1) {
  const previewRect = previewEl.getBoundingClientRect();
  const measureRect = measureEl?.getBoundingClientRect?.();
  const measureIsPreview = !measureEl || measureEl === previewEl;
  const scale = Math.max(0.01, Number(visualScale) || 1);
  const scrollLeft = Math.max(0, Number(previewEl.scrollLeft) || 0);
  const scrollTop = Math.max(0, Number(previewEl.scrollTop) || 0);
  const relativeRight = measureRect ? (measureRect.right - previewRect.left + scrollLeft) / scale : 0;
  const relativeBottom = measureRect ? (measureRect.bottom - previewRect.top + scrollTop) / scale : 0;
  const horizontalOverflow = measureWithoutFloatingMarkdown(measureEl || previewEl, () => Math.max(
    measureIsPreview ? (previewEl.scrollWidth || 0) / scale : 0,
    measureEl?.scrollWidth || 0
  ));
  const width = Math.max(
    horizontalOverflow,
    previewEl.clientWidth || 0,
    measureEl?.offsetWidth || 0,
    relativeRight,
    previewRect.width || 0,
    (measureRect?.width || 0) / scale
  );
  const height = Math.max(
    measureIsPreview ? (previewEl.scrollHeight || 0) / scale : 0,
    previewEl.clientHeight || 0,
    measureEl?.scrollHeight || 0,
    measureEl?.offsetHeight || 0,
    relativeBottom,
    measureIsPreview ? previewEl.offsetHeight || 0 : 0,
    previewRect.height || 0,
    (measureRect?.height || 0) / scale
  );
  return {
    width: Math.max(1, width),
    height: Math.max(1, height),
    visibleWidth: Math.max(1, previewRect.width || width)
  };
}
function calculateCanvasWindow({
  documentHeight,
  viewportTop = 0,
  viewportHeight,
  previousTop = null,
  previousHeight = 0,
  overscanScreens = 1,
  minWindowHeight = DEFAULT_MIN_WINDOW_HEIGHT,
  maxWindowHeight = DEFAULT_MAX_WINDOW_HEIGHT
}) {
  const height = Math.max(1, finitePositive(documentHeight, 1));
  const visibleHeight = Math.min(height, finitePositive(viewportHeight, height));
  const maxTop = Math.max(0, height - visibleHeight);
  const visibleTop = clamp(Number(viewportTop) || 0, 0, maxTop);
  const overscan = Math.max(0, Number(overscanScreens) || 0);
  const maximum = Math.max(visibleHeight, finitePositive(maxWindowHeight, DEFAULT_MAX_WINDOW_HEIGHT));
  const minimum = Math.min(maximum, Math.max(visibleHeight, finitePositive(minWindowHeight, DEFAULT_MIN_WINDOW_HEIGHT)));
  const targetHeight = Math.min(height, Math.max(minimum, Math.min(maximum, visibleHeight * (1 + overscan * 2))));
  const priorTop = Number(previousTop);
  const priorHeight = Number(previousHeight);
  const canReusePrevious = Number.isFinite(priorTop) && priorTop >= 0 && Number.isFinite(priorHeight) && Math.abs(priorHeight - targetHeight) < 1;
  if (canReusePrevious) {
    const priorBottom = priorTop + priorHeight;
    const guard = Math.max(0, Math.min(visibleHeight * 0.35, (priorHeight - visibleHeight) / 2));
    const safeTop = priorTop <= 0 ? 0 : priorTop + guard;
    const safeBottom = priorBottom >= height ? height : priorBottom - guard;
    if (visibleTop >= safeTop && visibleTop + visibleHeight <= safeBottom) {
      return { top: priorTop, height: priorHeight, changed: false };
    }
  }
  const centeredTop = visibleTop - (targetHeight - visibleHeight) / 2;
  const top = clamp(Math.round(centeredTop), 0, Math.max(0, height - targetHeight));
  return {
    top,
    height: targetHeight,
    changed: !canReusePrevious || Math.abs(top - priorTop) >= 1
  };
}
function calculateQualityWindowLimit({
  cssWidth,
  viewportHeight,
  devicePixelRatio = 1,
  maxDevicePixelRatio = 4,
  maxPixels = 6 * 1024 * 1024,
  maxWindowHeight = DEFAULT_MAX_WINDOW_HEIGHT
}) {
  const width = Math.max(1, finitePositive(cssWidth, 1));
  const visibleHeight = Math.max(1, finitePositive(viewportHeight, 1));
  const scale = Math.min(
    finitePositive(devicePixelRatio, 1),
    finitePositive(maxDevicePixelRatio, 4)
  );
  const pixelLimit = finitePositive(maxPixels, 6 * 1024 * 1024);
  const heightAtNativeScale = Math.floor(pixelLimit / (width * scale * scale));
  return Math.max(
    visibleHeight,
    Math.min(finitePositive(maxWindowHeight, DEFAULT_MAX_WINDOW_HEIGHT), heightAtNativeScale)
  );
}
function calculateZoomAwareWindowFloor({
  visualScale = 1,
  baseWindowHeight = DEFAULT_MIN_WINDOW_HEIGHT,
  minimumWindowHeight = 32
} = {}) {
  const scale = Math.max(1, finitePositive(visualScale, 1));
  const base = finitePositive(baseWindowHeight, DEFAULT_MIN_WINDOW_HEIGHT);
  const minimum = finitePositive(minimumWindowHeight, 32);
  return Math.max(minimum, base / scale);
}
function shouldClearStaleReadingVirtualMinHeight({
  inlineMinHeight,
  sectionHeight,
  viewportHeight
} = {}) {
  const inline = Math.max(0, Number(inlineMinHeight) || 0);
  const sections = Math.max(1, Number(sectionHeight) || 1);
  const viewport = Math.max(1, Number(viewportHeight) || 1);
  return inline > Math.max(sections * 1.75, sections + viewport * 2);
}
function calculateCanvasBackingStore({
  cssWidth,
  cssHeight,
  devicePixelRatio = 1,
  maxDevicePixelRatio = 2,
  maxDimension = 8192,
  maxPixels = 8 * 1024 * 1024
}) {
  const width = Math.max(1, finitePositive(cssWidth, 1));
  const height = Math.max(1, finitePositive(cssHeight, 1));
  const deviceScale = finitePositive(devicePixelRatio, 1);
  const requestedScale = Math.min(
    deviceScale,
    finitePositive(maxDevicePixelRatio, 2)
  );
  const dimensionLimit = finitePositive(maxDimension, 8192);
  const pixelLimit = finitePositive(maxPixels, 8 * 1024 * 1024);
  const scale = Math.max(0.01, Math.min(
    requestedScale,
    dimensionLimit / width,
    dimensionLimit / height,
    Math.sqrt(pixelLimit / (width * height))
  ));
  const backingWidth = Math.max(1, Math.floor(width * scale));
  const backingHeight = Math.max(1, Math.floor(height * scale));
  const effectiveScale = Math.min(backingWidth / width, backingHeight / height);
  return {
    width: backingWidth,
    height: backingHeight,
    scale: effectiveScale,
    limited: effectiveScale + 1e-6 < deviceScale
  };
}

// src/drawing-persistence.mjs
function recordId(record) {
  if (typeof record?.id === "string") {
    return record.id;
  }
  return typeof record?.elementId === "string" ? record.elementId : "";
}
function mergeRetainedRecords(latestRecords, incomingRecords) {
  const latest = Array.isArray(latestRecords) ? latestRecords : [];
  const incoming = Array.isArray(incomingRecords) ? incomingRecords : [];
  const incomingById = new Map(incoming.map((record) => [recordId(record), record]).filter(([id]) => id));
  const latestIds = new Set(latest.map(recordId).filter(Boolean));
  const merged = [];
  for (const record of latest) {
    const id = recordId(record);
    if (!id) {
      continue;
    }
    merged.push(incomingById.get(id) || record);
  }
  for (const record of incoming) {
    const id = recordId(record);
    if (!id || !latestIds.has(id)) {
      merged.push(record);
    }
  }
  return merged;
}
function mergeControllerDrawingSnapshot(latest, incoming) {
  if (!latest || !incoming) {
    return incoming;
  }
  const strokes = mergeRetainedRecords(latest.strokes, incoming.strokes);
  const markdownBlocks = mergeRetainedRecords(latest.markdownBlocks, incoming.markdownBlocks);
  const referencedGroupIds = new Set([
    ...strokes,
    ...markdownBlocks
  ].map((record) => record?.groupId).filter(Boolean));
  const incomingGroupIds = new Set((Array.isArray(incoming.elementGroups) ? incoming.elementGroups : []).map(recordId).filter(Boolean));
  const elementGroups = mergeRetainedRecords(latest.elementGroups, incoming.elementGroups).filter((group) => {
    const id = recordId(group);
    return incomingGroupIds.has(id) || referencedGroupIds.has(id);
  });
  return {
    ...incoming,
    strokes,
    markdownBlocks,
    elementGroups
  };
}
function coalesceDrawingSaveRequest(previous, next) {
  const priorEntries = Array.isArray(previous?.entries) ? previous.entries : [];
  const entry = {
    data: next?.data,
    excludeData: next?.excludeData || next?.data,
    replace: next?.replace === true
  };
  const entries = entry.replace ? [] : priorEntries.slice();
  const priorIndex = entries.findIndex((candidate) => candidate?.data === entry.data);
  if (priorIndex >= 0) {
    entry.replace = entry.replace || entries[priorIndex]?.replace === true;
    entries.splice(priorIndex, 1);
  }
  entries.push(entry);
  return {
    file: next?.file || previous?.file || null,
    generation: Math.max(0, Number(previous?.generation) || 0) + 1,
    entries
  };
}
function materializeDrawingSaveRequest(latest, request, normalize) {
  let canonical = latest || null;
  for (const entry of Array.isArray(request?.entries) ? request.entries : []) {
    const incoming = normalize(entry?.data, request?.file);
    canonical = entry?.replace === true ? incoming : mergeControllerDrawingSnapshot(canonical, incoming);
  }
  return canonical;
}

// src/layout-coordinates.mjs
var RESPONSIVE_POINT_BASIS = "note-content-v1";
function finite(value, fallback = 0) {
  const number = Number(value);
  return Number.isFinite(number) ? number : fallback;
}
function clamp2(value, min, max) {
  return Math.min(max, Math.max(min, value));
}
function normalizeLinePosition(value) {
  if (value === null || value === void 0 || value === "") {
    return null;
  }
  const line = Number(value);
  return Number.isFinite(line) && line >= 0 ? line : null;
}
function normalizeLineConfidence(value) {
  if (value === null || value === void 0 || value === "") {
    return null;
  }
  const confidence = Number(value);
  return Number.isFinite(confidence) ? clamp2(confidence, 0, 1) : null;
}
function isEndClampedLine(line) {
  if (!Number.isFinite(line)) {
    return false;
  }
  return line - Math.floor(line) >= 0.985;
}
function canTrustLineAnchor(anchor) {
  if (anchor.line === null) {
    return false;
  }
  if (anchor.lineConfidence !== null) {
    return anchor.lineConfidence >= 0.75;
  }
  return !isEndClampedLine(anchor.line);
}
function normalizeContentFrame({ surfaceWidth, contentLeft = 0, contentWidth } = {}) {
  const width = Math.max(1, finite(surfaceWidth, 1));
  const left = clamp2(finite(contentLeft, 0), -width, width * 2);
  const available = Math.max(1, width - Math.max(0, left));
  const frameWidth = clamp2(finite(contentWidth, available), 1, width * 2);
  return { left, width: frameWidth, surfaceWidth: width };
}
function mapClientPointToCanvas({ clientX, clientY } = {}, geometry = {}) {
  const rect = geometry.rect || geometry;
  const rectLeft = finite(rect?.left, 0);
  const rectTop = finite(rect?.top, 0);
  const rectWidth = finite(rect?.width, 0);
  const rectHeight = finite(rect?.height, 0);
  const canvasWidth = Math.max(1, finite(geometry.canvasWidth, Math.max(1, rectWidth)));
  const canvasHeight = Math.max(1, finite(geometry.canvasHeight, Math.max(1, rectHeight)));
  const canvasRenderHeight = Math.max(1, finite(geometry.canvasRenderHeight, Math.max(1, rectHeight)));
  const canvasWindowTop = finite(geometry.canvasWindowTop, 0);
  const xScale = rectWidth > 0 ? canvasWidth / rectWidth : 1;
  const yScale = rectHeight > 0 ? canvasRenderHeight / rectHeight : 1;
  return {
    canvasX: (finite(clientX, rectLeft) - rectLeft) * xScale,
    canvasY: (finite(clientY, rectTop) - rectTop) * yScale + canvasWindowTop,
    canvasWidth,
    canvasHeight
  };
}
function mapResizeClientDeltaToPoint({ clientX, clientY } = {}, {
  startClient,
  corner,
  geometry = {},
  clientBounds
} = {}) {
  const rect = geometry.rect || geometry;
  const rectLeft = finite(rect?.left, 0);
  const rectTop = finite(rect?.top, 0);
  const rectWidth = finite(rect?.width, 0);
  const rectHeight = finite(rect?.height, 0);
  const startX = Number(startClient?.x);
  const startY = Number(startClient?.y);
  const cornerX = Number(corner?.x);
  const cornerY = Number(corner?.y);
  if (rectWidth <= 0 || rectHeight <= 0 || ![startX, startY, cornerX, cornerY].every(Number.isFinite)) {
    return null;
  }
  const clientLeft = Math.max(rectLeft, finite(clientBounds?.left, rectLeft));
  const clientRight = Math.min(rectLeft + rectWidth, finite(clientBounds?.right, rectLeft + rectWidth));
  const clientTop = Math.max(rectTop, finite(clientBounds?.top, rectTop));
  const clientBottom = Math.min(rectTop + rectHeight, finite(clientBounds?.bottom, rectTop + rectHeight));
  const boundedClientX = clamp2(finite(clientX, startX), Math.min(clientLeft, startX), Math.max(clientRight, startX));
  const boundedClientY = clamp2(finite(clientY, startY), Math.min(clientTop, startY), Math.max(clientBottom, startY));
  const canvasHeight = Math.max(1, finite(geometry.canvasHeight, rectHeight));
  const canvasRenderHeight = Math.max(1, finite(geometry.canvasRenderHeight, rectHeight));
  return {
    x: cornerX + (boundedClientX - startX) / rectWidth,
    y: cornerY + (boundedClientY - startY) * canvasRenderHeight / rectHeight / canvasHeight
  };
}
function constrainWideContentFrame(frameInput, {
  isMobile = false,
  minSurfaceWidth = 900,
  minLaneWidth = 720,
  maxLaneWidth = 860,
  filledRatio = 0.78
} = {}) {
  const frame = normalizeContentFrame(frameInput);
  if (isMobile || frame.surfaceWidth < minSurfaceWidth || frame.width / frame.surfaceWidth < filledRatio) {
    return frame;
  }
  const laneLimit = clamp2(frame.surfaceWidth * 0.72, minLaneWidth, maxLaneWidth);
  const available = Math.max(1, frame.surfaceWidth - Math.max(0, frame.left));
  return {
    ...frame,
    width: Math.min(frame.width, laneLimit, available)
  };
}
function normalizeResponsiveAnchor(anchor) {
  if (!anchor || anchor.basis !== RESPONSIVE_POINT_BASIS) {
    return null;
  }
  return {
    v: 1,
    basis: RESPONSIVE_POINT_BASIS,
    x: clamp2(finite(anchor.x, 0), -1, 2),
    y: clamp2(finite(anchor.y, 0), 0, 1),
    path: typeof anchor.path === "string" ? anchor.path : "",
    line: normalizeLinePosition(anchor.line),
    lineConfidence: normalizeLineConfidence(anchor.lineConfidence),
    offsetY: Number.isFinite(Number(anchor.offsetY)) ? clamp2(Number(anchor.offsetY), -1e5, 1e5) : 0
  };
}
function createResponsivePoint({
  canvasX,
  canvasY,
  canvasWidth,
  canvasHeight,
  frame,
  sourcePath = "",
  linePosition = null,
  lineConfidence = null,
  lineOffsetY = 0,
  time = Date.now()
}) {
  const width = Math.max(1, finite(canvasWidth, 1));
  const height = Math.max(1, finite(canvasHeight, 1));
  const normalizedFrame = normalizeContentFrame({
    surfaceWidth: width,
    contentLeft: frame?.left,
    contentWidth: frame?.width
  });
  const x = clamp2(finite(canvasX, 0) / width, 0, 1);
  const y = clamp2(finite(canvasY, 0) / height, 0, 1);
  return {
    x,
    y,
    t: Number.isFinite(Number(time)) ? Number(time) : Date.now(),
    anchor: {
      v: 1,
      basis: RESPONSIVE_POINT_BASIS,
      x: clamp2((finite(canvasX, 0) - normalizedFrame.left) / normalizedFrame.width, -1, 2),
      y,
      path: typeof sourcePath === "string" ? sourcePath : "",
      line: normalizeLinePosition(linePosition),
      lineConfidence: normalizeLineConfidence(lineConfidence),
      offsetY: Number.isFinite(Number(lineOffsetY)) ? clamp2(Number(lineOffsetY), -1e5, 1e5) : 0
    }
  };
}
function projectResponsivePoint(point, {
  canvasWidth,
  canvasHeight,
  frame,
  lineToCanvasY
} = {}) {
  const width = Math.max(1, finite(canvasWidth, 1));
  const height = Math.max(1, finite(canvasHeight, 1));
  const anchor = normalizeResponsiveAnchor(point?.anchor);
  if (!anchor) {
    return {
      ...point,
      x: clamp2(finite(point?.x, 0), 0, 1),
      y: clamp2(finite(point?.y, 0), 0, 1)
    };
  }
  const normalizedFrame = normalizeContentFrame({
    surfaceWidth: width,
    contentLeft: frame?.left,
    contentWidth: frame?.width
  });
  const anchoredY = anchor.line !== null && typeof lineToCanvasY === "function" ? Number(lineToCanvasY(anchor.path, anchor.line)) : NaN;
  const canvasX = normalizedFrame.left + anchor.x * normalizedFrame.width;
  const fallbackCanvasY = anchor.y * height;
  const firstLineIsPlausible = anchor.line === null || anchor.line >= 1 || anchor.y <= 0.15;
  const lineAnchorIsReliable = canTrustLineAnchor(anchor);
  const lineShiftIsPlausible = anchor.line !== null && anchor.line >= 1 ? true : Math.abs(anchoredY - fallbackCanvasY) <= Math.max(96, height * 0.18);
  const canUseLineAnchor = Number.isFinite(anchoredY) && firstLineIsPlausible && lineAnchorIsReliable && lineShiftIsPlausible;
  const canvasY = canUseLineAnchor ? anchoredY + anchor.offsetY : fallbackCanvasY;
  return {
    ...point,
    x: clamp2(canvasX / width, 0, 1),
    y: clamp2(canvasY / height, 0, 1),
    anchor
  };
}

// src/element-layout.mjs
var ELEMENT_LAYOUT_BASIS = "note-element-frame-v1";
var ELEMENT_LAYOUT_VERSION = 1;
var CORNER_NAMES = ["topLeft", "topRight", "bottomRight", "bottomLeft"];
function finite2(value, fallback = 0) {
  const number = Number(value);
  return Number.isFinite(number) ? number : fallback;
}
function clamp3(value, min, max) {
  return Math.min(max, Math.max(min, value));
}
function normalizeLine(value) {
  if (value === null || value === void 0 || value === "") {
    return null;
  }
  const number = Number(value);
  return Number.isFinite(number) && number >= 0 ? number : null;
}
function normalizeLineConfidence2(value) {
  if (value === null || value === void 0 || value === "") {
    return null;
  }
  const number = Number(value);
  return Number.isFinite(number) ? clamp3(number, 0, 1) : null;
}
function isEndClampedLine2(line) {
  if (!Number.isFinite(line)) {
    return false;
  }
  return line - Math.floor(line) >= 0.985;
}
function canTrustCornerLine(corner) {
  if (!corner || corner.line === null) {
    return false;
  }
  if (corner.lineConfidence !== null) {
    return corner.lineConfidence >= 0.75;
  }
  return !isEndClampedLine2(corner.line);
}
function normalizeFrame(frame = {}) {
  const surfaceWidth = Math.max(1, finite2(frame.surfaceWidth, frame.width || 1));
  const contentLeft = clamp3(finite2(frame.contentLeft, frame.left || 0), -surfaceWidth, surfaceWidth * 2);
  const contentWidth = clamp3(finite2(frame.contentWidth, frame.width || surfaceWidth), 1, surfaceWidth * 2);
  const viewportHeight = Math.max(1, finite2(frame.viewportHeight, frame.documentHeight || 1));
  const documentHeight = Math.max(1, finite2(frame.documentHeight, viewportHeight));
  return {
    surfaceWidth,
    contentLeft,
    contentWidth,
    viewportHeight,
    documentHeight,
    aspectRatio: contentWidth / viewportHeight
  };
}
function normalizeCorner(corner) {
  if (!corner) {
    return null;
  }
  return {
    x: clamp3(finite2(corner.x, 0), -2, 3),
    y: clamp3(finite2(corner.y, 0), 0, 1),
    path: typeof corner.path === "string" ? corner.path : "",
    line: normalizeLine(corner.line),
    lineConfidence: normalizeLineConfidence2(corner.lineConfidence)
  };
}
function normalizeBox(box = {}) {
  return {
    x: finite2(box.x, 0),
    y: finite2(box.y, 0),
    width: Math.max(0.01, finite2(box.width, 0.01)),
    height: Math.max(0.01, finite2(box.height, 0.01))
  };
}
function normalizeMetrics(metrics = {}) {
  const result = {};
  for (const key of ["width", "fontSize", "textWidth", "previewWidth", "previewHeight"]) {
    const value = Number(metrics[key]);
    if (Number.isFinite(value) && value > 0) {
      result[key] = value;
    }
  }
  return result;
}
function normalizeRelation(relation) {
  if (!relation || typeof relation.targetId !== "string" || !relation.targetId) {
    return null;
  }
  const kind = ["intersection", "overlap", "near"].includes(relation.kind) ? relation.kind : "near";
  const sourceU = Number(relation.sourceU);
  const sourceV = Number(relation.sourceV);
  const targetU = Number(relation.targetU);
  const targetV = Number(relation.targetV);
  return {
    targetId: relation.targetId,
    kind,
    sourceCorner: CORNER_NAMES.includes(relation.sourceCorner) ? relation.sourceCorner : "topLeft",
    targetCorner: CORNER_NAMES.includes(relation.targetCorner) ? relation.targetCorner : "topLeft",
    ...Number.isFinite(sourceU) ? { sourceU: clamp3(sourceU, 0, 1) } : {},
    ...Number.isFinite(sourceV) ? { sourceV: clamp3(sourceV, 0, 1) } : {},
    ...Number.isFinite(targetU) ? { targetU: clamp3(targetU, 0, 1) } : {},
    ...Number.isFinite(targetV) ? { targetV: clamp3(targetV, 0, 1) } : {},
    dx: finite2(relation.dx, 0),
    dy: finite2(relation.dy, 0),
    weight: clamp3(finite2(relation.weight, kind === "near" ? 0.14 : 0.26), 0.05, 0.4)
  };
}
function normalizeElementLayout(layout) {
  if (!layout || layout.basis !== ELEMENT_LAYOUT_BASIS || !layout.sourceFrame || !layout.box) {
    return null;
  }
  const corners = {};
  for (const name of CORNER_NAMES) {
    corners[name] = normalizeCorner(layout.corners?.[name]);
  }
  if (!corners.topLeft) {
    return null;
  }
  return {
    v: ELEMENT_LAYOUT_VERSION,
    id: typeof layout.id === "string" && layout.id ? layout.id : "",
    basis: ELEMENT_LAYOUT_BASIS,
    primary: "topLeft",
    corners,
    sourceFrame: normalizeFrame(layout.sourceFrame),
    box: normalizeBox(layout.box),
    metrics: normalizeMetrics(layout.metrics),
    relations: (Array.isArray(layout.relations) ? layout.relations : []).map(normalizeRelation).filter(Boolean).slice(0, 4)
  };
}
function createElementLayout({
  id,
  bounds,
  canvasWidth,
  canvasHeight,
  frame,
  viewportHeight,
  sourcePath = "",
  cornerLocations = {},
  metrics = {},
  relations = []
} = {}) {
  const surfaceWidth = Math.max(1, finite2(canvasWidth, 1));
  const documentHeight = Math.max(1, finite2(canvasHeight, 1));
  const sourceFrame = normalizeFrame({
    surfaceWidth,
    contentLeft: frame?.left,
    contentWidth: frame?.width,
    viewportHeight: viewportHeight || documentHeight,
    documentHeight
  });
  const box = normalizeBox({
    x: bounds?.minX,
    y: bounds?.minY,
    width: finite2(bounds?.maxX, 0) - finite2(bounds?.minX, 0),
    height: finite2(bounds?.maxY, 0) - finite2(bounds?.minY, 0)
  });
  const positions = {
    topLeft: { x: box.x, y: box.y },
    topRight: { x: box.x + box.width, y: box.y },
    bottomRight: { x: box.x + box.width, y: box.y + box.height },
    bottomLeft: { x: box.x, y: box.y + box.height }
  };
  const corners = {};
  for (const name of CORNER_NAMES) {
    const location = cornerLocations[name] || {};
    corners[name] = {
      x: clamp3((positions[name].x - sourceFrame.contentLeft) / sourceFrame.contentWidth, -2, 3),
      y: clamp3(positions[name].y / documentHeight, 0, 1),
      path: typeof location.path === "string" && location.path ? location.path : sourcePath,
      line: normalizeLine(location.line),
      lineConfidence: normalizeLineConfidence2(location.lineConfidence ?? location.confidence)
    };
  }
  return normalizeElementLayout({
    v: ELEMENT_LAYOUT_VERSION,
    id,
    basis: ELEMENT_LAYOUT_BASIS,
    primary: "topLeft",
    corners,
    sourceFrame,
    box,
    metrics,
    relations
  });
}
function estimateElementLayoutExtent(layouts, {
  canvasWidth,
  frame,
  minHeight = 1,
  padding = 48,
  maxHeight = 2e6
} = {}) {
  const target = normalizeFrame({
    surfaceWidth: canvasWidth,
    contentLeft: frame?.left,
    contentWidth: frame?.width,
    viewportHeight: minHeight,
    documentHeight: minHeight
  });
  let extent = Math.max(1, finite2(minHeight, 1));
  for (const input of Array.isArray(layouts) ? layouts : []) {
    const layout = normalizeElementLayout(input);
    if (!layout) {
      continue;
    }
    const widthScale = clamp3(target.contentWidth / layout.sourceFrame.contentWidth, 0.2, 5);
    const sameContentLane = widthScale >= 0.82 && widthScale <= 1.2;
    const positionScale = sameContentLane ? 1 : clamp3(1 / widthScale, 0.48, 2.2);
    const conservativeHeightScale = sameContentLane ? widthScale : clamp3(Math.max(positionScale, widthScale), 0.42, 2.8);
    extent = Math.max(extent, layout.box.y * positionScale + layout.box.height * conservativeHeightScale + padding);
  }
  return Math.ceil(clamp3(extent, Math.max(1, finite2(minHeight, 1)), Math.max(1, finite2(maxHeight, 2e6))));
}
function estimateStableElementLayoutExtent(layouts, options = {}) {
  const normalized = (Array.isArray(layouts) ? layouts : []).map(normalizeElementLayout).filter(Boolean);
  const rawExtent = estimateElementLayoutExtent(normalized, options);
  if (!normalized.length) {
    return rawExtent;
  }
  const minHeight = Math.max(1, finite2(options.minHeight, 1));
  const extents = normalized.map((layout) => estimateElementLayoutExtent([layout], options)).sort((a, b) => a - b);
  const extraRoom = Math.max(192, Math.min(640, minHeight * 0.35));
  const capAfter = (plausible2) => Math.min(
    rawExtent,
    Math.ceil(Math.max(minHeight, plausible2.length ? plausible2[plausible2.length - 1] : minHeight) + extraRoom)
  );
  const hardThreshold = Math.max(minHeight * 24, minHeight + 5e4);
  const hardPlausible = extents.filter((extent) => extent <= hardThreshold);
  if (hardPlausible.length !== extents.length) {
    return capAfter(hardPlausible);
  }
  if (extents.length < 8) {
    return rawExtent;
  }
  const baseline = extents[Math.floor((extents.length - 1) * 0.9)];
  const relativeThreshold = Math.max(minHeight + 12e3, baseline + 12e3, baseline * 8);
  const plausible = extents.filter((extent) => extent <= relativeThreshold);
  const outlierCount = extents.length - plausible.length;
  return outlierCount > 0 && outlierCount <= Math.max(4, Math.ceil(extents.length * 0.12)) ? capAfter(plausible) : rawExtent;
}
function elementLayoutExceedsTarget(layoutInput, targetDocumentHeight, {
  factor = 8,
  minExcess = 12e3
} = {}) {
  const layout = normalizeElementLayout(layoutInput);
  if (!layout) {
    return false;
  }
  const targetHeight = Math.max(1, finite2(targetDocumentHeight, 1));
  const threshold = Math.max(targetHeight * factor, targetHeight + minExcess);
  return layout.sourceFrame.documentHeight > threshold && layout.box.y + layout.box.height > threshold;
}
function projectCorner(corner, target, lineToCanvasY, sourceInput = null, preferDocumentFlow = null) {
  if (!corner) {
    return null;
  }
  const frame = normalizeFrame(target);
  let fallbackY = corner.y * frame.documentHeight;
  if (sourceInput && typeof preferDocumentFlow === "boolean") {
    const source = normalizeFrame(sourceInput);
    const sourceY = corner.y * source.documentHeight;
    const widthScale = clamp3(frame.contentWidth / source.contentWidth, 0.2, 5);
    const sameContentLane = widthScale >= 0.82 && widthScale <= 1.2;
    const fallbackScale = sameContentLane ? 1 : clamp3(1 / widthScale, 0.48, 2.2);
    fallbackY = clamp3(sourceY * fallbackScale, 0, frame.documentHeight);
  }
  const lineY = corner.line !== null && typeof lineToCanvasY === "function" ? Number(lineToCanvasY(corner.path, corner.line)) : NaN;
  const firstLineIsPlausible = corner.line === null || corner.line >= 1 || corner.y <= 0.15;
  const lineIsReliable = canTrustCornerLine(corner);
  const maxLineShift = Math.max(96, Math.min(frame.documentHeight * 0.18, frame.viewportHeight * 0.45));
  const lineShiftIsPlausible = corner.line !== null && corner.line >= 1 ? true : Math.abs(lineY - fallbackY) <= maxLineShift;
  const canUseLine = Number.isFinite(lineY) && firstLineIsPlausible && lineIsReliable && lineShiftIsPlausible;
  return {
    x: frame.contentLeft + corner.x * frame.contentWidth,
    y: canUseLine ? lineY : fallbackY,
    fallbackY,
    lineAnchored: canUseLine
  };
}
function elementLayoutNeedsRepair(layoutInput) {
  const layout = normalizeElementLayout(layoutInput);
  if (!layout) {
    return true;
  }
  const frame = layout.sourceFrame;
  const stableWideLane = frame.surfaceWidth >= 900 && frame.contentWidth >= 720;
  if (frame.surfaceWidth < 180 || frame.contentWidth < 140 || frame.contentWidth / frame.surfaceWidth < 0.42 && !stableWideLane) {
    return true;
  }
  const verticalSpan = layout.box.height / Math.max(1, frame.documentHeight);
  const corners = Object.values(layout.corners).filter(Boolean);
  if (corners.some((corner) => corner.line !== null && corner.lineConfidence === null && isEndClampedLine2(corner.line) && corner.y > 0.08)) {
    return true;
  }
  const topLine = layout.corners.topLeft?.line;
  const bottomLine = layout.corners.bottomLeft?.line;
  if (Number.isFinite(topLine) && Number.isFinite(bottomLine) && Math.floor(topLine) === Math.floor(bottomLine) && verticalSpan > 0.08) {
    return true;
  }
  return false;
}
function clampAxisRatio(scales, { maxXOverY = 1.65, maxYOverX = 1.9 } = {}) {
  let xScale = finite2(scales.xScale, finite2(scales.scale, 1));
  let yScale = finite2(scales.yScale, finite2(scales.scale, 1));
  if (xScale > yScale * maxXOverY) {
    xScale = yScale * maxXOverY;
  }
  if (yScale > xScale * maxYOverX) {
    yScale = xScale * maxYOverX;
  }
  const scale = calculateVisualScale(xScale, yScale, scales.scale);
  return {
    xScale,
    yScale,
    scale
  };
}
function blendScale(base, candidate, weight) {
  if (!Number.isFinite(candidate) || candidate <= 0) {
    return base;
  }
  return Math.exp(Math.log(Math.max(1e-3, base)) * (1 - weight) + Math.log(Math.max(1e-3, candidate)) * weight);
}
function calculateVisualScale(xScaleInput, yScaleInput, baseScaleInput = 1) {
  const xScale = Math.max(1e-3, finite2(xScaleInput, 1));
  const yScale = Math.max(1e-3, finite2(yScaleInput, 1));
  const geometric = Math.sqrt(xScale * yScale);
  const baseScale = clamp3(finite2(baseScaleInput, geometric), 0.42, 2.4);
  const widthProtected = blendScale(xScale, geometric, xScale < geometric ? 0.46 : 0.22);
  const baseProtected = blendScale(widthProtected, baseScale, 0.34);
  const sameWidthFloor = xScale >= 0.82 ? Math.min(xScale, baseScale) * 0.94 : 0.42;
  return clamp3(Math.max(geometric, baseProtected, sameWidthFloor), 0.42, 2.4);
}
function calculateElementScales(sourceFrameInput, targetFrameInput, boxInput = {}) {
  const source = normalizeFrame(sourceFrameInput);
  const target = normalizeFrame(targetFrameInput);
  const box = normalizeBox(boxInput);
  const widthScale = clamp3(target.contentWidth / source.contentWidth, 0.2, 5);
  const documentScale = clamp3(target.documentHeight / source.documentHeight, 0.28, 3.6);
  const viewportScale = clamp3(target.viewportHeight / source.viewportHeight, 0.25, 4);
  const aspectChange = target.aspectRatio / Math.max(0.01, source.aspectRatio);
  const widthWeight = aspectChange < 0.9 ? 0.8 : aspectChange > 1.35 ? 0.62 : 0.7;
  let scale = Math.exp(
    Math.log(widthScale) * widthWeight + Math.log(viewportScale) * (1 - widthWeight)
  );
  scale = clamp3(scale, Math.max(0.42, widthScale * 0.58), Math.min(2.4, widthScale * 1.55));
  const fitScale = target.contentWidth * 0.98 / Math.max(1, box.width);
  scale = clamp3(Math.min(scale, fitScale), 0.42, 2.4);
  const portraitTarget = target.aspectRatio < 0.62 || target.contentWidth < 430 && target.viewportHeight > target.contentWidth * 1.2;
  const wideTarget = target.aspectRatio > 1.05;
  const sameContentLane = widthScale >= 0.82 && widthScale <= 1.2;
  let xScale = blendScale(widthScale, scale, portraitTarget ? 0.12 : 0.24);
  let yScale = Math.exp(
    Math.log(documentScale) * (portraitTarget ? 0.58 : wideTarget ? 0.38 : 0.46) + Math.log(viewportScale) * (portraitTarget ? 0.24 : wideTarget ? 0.34 : 0.3) + Math.log(widthScale) * (portraitTarget ? 0.18 : wideTarget ? 0.28 : 0.24)
  );
  yScale = blendScale(yScale, scale, portraitTarget ? 0.1 : 0.22);
  xScale = clamp3(Math.min(xScale, fitScale), 0.34, 2.8);
  yScale = clamp3(yScale, 0.34, 2.8);
  if (sameContentLane) {
    xScale = widthScale;
    yScale = widthScale;
    scale = widthScale;
  }
  if (portraitTarget) {
    return clampAxisRatio({ xScale, yScale, scale }, { maxXOverY: 1.25, maxYOverX: 2.15 });
  }
  if (wideTarget) {
    return clampAxisRatio({ xScale, yScale, scale }, { maxXOverY: 1.75, maxYOverX: 1.45 });
  }
  return clampAxisRatio({ xScale, yScale, scale }, { maxXOverY: 1.55, maxYOverX: 1.65 });
}
function projectElementLayout(layoutInput, {
  canvasWidth,
  canvasHeight,
  frame,
  viewportHeight,
  lineToCanvasY,
  preferDocumentFlow = null
} = {}) {
  const layout = normalizeElementLayout(layoutInput);
  if (!layout) {
    return null;
  }
  const targetFrame = normalizeFrame({
    surfaceWidth: canvasWidth,
    contentLeft: frame?.left,
    contentWidth: frame?.width,
    viewportHeight: viewportHeight || canvasHeight,
    documentHeight: canvasHeight
  });
  const primary = projectCorner(layout.corners.topLeft, targetFrame, lineToCanvasY, layout.sourceFrame, preferDocumentFlow);
  if (!primary) {
    return null;
  }
  let { xScale, yScale, scale } = calculateElementScales(layout.sourceFrame, targetFrame, layout.box);
  const projectedRight = projectCorner(layout.corners.topRight, targetFrame, lineToCanvasY, layout.sourceFrame, preferDocumentFlow);
  const projectedBottom = projectCorner(layout.corners.bottomLeft, targetFrame, lineToCanvasY, layout.sourceFrame, preferDocumentFlow);
  const projectedBottomRight = projectCorner(layout.corners.bottomRight, targetFrame, lineToCanvasY, layout.sourceFrame, preferDocumentFlow);
  const cornerXScales = [];
  const cornerYScales = [];
  if (projectedRight && layout.box.width > 0.01) {
    cornerXScales.push(Math.abs(projectedRight.x - primary.x) / layout.box.width);
  }
  if (projectedBottomRight && projectedBottom && layout.box.width > 0.01) {
    cornerXScales.push(Math.abs(projectedBottomRight.x - projectedBottom.x) / layout.box.width);
  }
  if (projectedBottom && layout.box.height > 0.01) {
    cornerYScales.push(Math.abs(projectedBottom.y - primary.y) / layout.box.height);
  }
  if (projectedBottomRight && projectedRight && layout.box.height > 0.01) {
    cornerYScales.push(Math.abs(projectedBottomRight.y - projectedRight.y) / layout.box.height);
  }
  const reliableXScales = cornerXScales.filter((value) => Number.isFinite(value) && value >= xScale * 0.42 && value <= xScale * 2.35);
  const reliableYScales = cornerYScales.filter((value) => Number.isFinite(value) && value >= yScale * 0.36 && value <= yScale * 2.6);
  if (reliableXScales.length) {
    const cornerScale = reliableXScales.reduce((sum, value) => sum + value, 0) / reliableXScales.length;
    xScale = clamp3(blendScale(xScale, cornerScale, 0.46), xScale * 0.62, xScale * 1.42);
  }
  if (reliableYScales.length) {
    const cornerScale = reliableYScales.reduce((sum, value) => sum + value, 0) / reliableYScales.length;
    yScale = clamp3(blendScale(yScale, cornerScale, 0.5), yScale * 0.56, yScale * 1.55);
  }
  const targetIsPortrait = targetFrame.aspectRatio < 0.62 || targetFrame.contentWidth < 430 && targetFrame.viewportHeight > targetFrame.contentWidth * 1.2;
  const targetIsWide = targetFrame.aspectRatio > 1.05;
  const contentWidthScale = targetFrame.contentWidth / layout.sourceFrame.contentWidth;
  const sameContentLane = contentWidthScale >= 0.82 && contentWidthScale <= 1.2;
  ({ xScale, yScale, scale } = clampAxisRatio({ xScale, yScale, scale }, targetIsPortrait ? { maxXOverY: 1.25, maxYOverX: 2.15 } : targetIsWide ? { maxXOverY: 1.75, maxYOverX: 1.45 } : { maxXOverY: 1.55, maxYOverX: 1.65 }));
  if (sameContentLane) {
    xScale = contentWidthScale;
    yScale = contentWidthScale;
    scale = contentWidthScale;
  }
  const fitXScale = targetFrame.contentWidth * 0.98 / Math.max(1, layout.box.width);
  if (xScale > fitXScale) {
    xScale = fitXScale;
    scale = calculateVisualScale(xScale, yScale, scale);
  }
  let x = primary.x;
  if (projectedBottom && Number.isFinite(projectedBottom.x) && Math.abs(projectedBottom.x - primary.x) <= Math.max(24, layout.box.width * scale * 0.25)) {
    x = (primary.x + projectedBottom.x) / 2;
  }
  const y = primary.y;
  const width = Math.max(0.01, layout.box.width * xScale);
  const height = Math.max(0.01, layout.box.height * yScale);
  const maxX = Math.max(0, targetFrame.surfaceWidth - width);
  const maxY = Math.max(0, targetFrame.documentHeight - height);
  const clampedX = clamp3(x, 0, maxX);
  const clampedY = clamp3(y, 0, maxY);
  const fallbackY = clamp3(primary.fallbackY, 0, maxY);
  const anchorStrength = primary.lineAnchored ? 1 : sameContentLane ? 0.94 : 0.68;
  return {
    id: layout.id,
    x: clampedX,
    y: clampedY,
    width,
    height,
    scale,
    xScale,
    yScale,
    anchorX: clampedX,
    anchorY: clampedY,
    fallbackY,
    anchorStrength,
    sameContentLane,
    primaryAnchoredToLine: primary.lineAnchored
  };
}
function stabilizeProjectedElementBox(projectedInput, previousInput, {
  positionThreshold = 0.65,
  sizeThreshold = 0.65
} = {}) {
  if (!projectedInput || !previousInput) {
    return projectedInput;
  }
  const previous = {
    x: finite2(previousInput.x, previousInput.minX),
    y: finite2(previousInput.y, previousInput.minY),
    width: Math.max(0.01, finite2(previousInput.width, finite2(previousInput.maxX) - finite2(previousInput.minX))),
    height: Math.max(0.01, finite2(previousInput.height, finite2(previousInput.maxY) - finite2(previousInput.minY)))
  };
  const projected = {
    x: finite2(projectedInput.x),
    y: finite2(projectedInput.y),
    width: Math.max(0.01, finite2(projectedInput.width, 0.01)),
    height: Math.max(0.01, finite2(projectedInput.height, 0.01))
  };
  const positionLimit = Math.max(0, finite2(positionThreshold, 0.65));
  const sizeLimit = Math.max(0, finite2(sizeThreshold, 0.65));
  const x = Math.abs(projected.x - previous.x) < positionLimit ? previous.x : projected.x;
  const y = Math.abs(projected.y - previous.y) < positionLimit ? previous.y : projected.y;
  const width = Math.abs(projected.width - previous.width) < sizeLimit ? previous.width : projected.width;
  const height = Math.abs(projected.height - previous.height) < sizeLimit ? previous.height : projected.height;
  const widthRatio = width / projected.width;
  const heightRatio = height / projected.height;
  return {
    ...projectedInput,
    x,
    y,
    width,
    height,
    xScale: finite2(projectedInput.xScale, projectedInput.scale) * widthRatio,
    yScale: finite2(projectedInput.yScale, projectedInput.scale) * heightRatio,
    scale: finite2(projectedInput.scale, 1) * Math.sqrt(widthRatio * heightRatio),
    anchorX: Number.isFinite(Number(projectedInput.anchorX)) ? Number(projectedInput.anchorX) + x - projected.x : projectedInput.anchorX,
    anchorY: Number.isFinite(Number(projectedInput.anchorY)) ? Number(projectedInput.anchorY) + y - projected.y : projectedInput.anchorY
  };
}
function transitionBox(input) {
  if (!input) {
    return null;
  }
  const x = finite2(input.x, input.minX);
  const y = finite2(input.y, input.minY);
  const width = Math.max(0.01, finite2(input.width, finite2(input.maxX) - finite2(input.minX)));
  const height = Math.max(0.01, finite2(input.height, finite2(input.maxY) - finite2(input.minY)));
  return { x, y, width, height };
}
function projectedTransitionSignature(projectedItems, quantum) {
  const step = Math.max(0.5, finite2(quantum, 3));
  return projectedItems.filter((item) => item?.id).map((item) => {
    const box = transitionBox(item);
    return box ? [
      String(item.id),
      Math.round(box.x / step),
      Math.round(box.y / step),
      Math.round(box.width / step),
      Math.round(box.height / step)
    ].join(":") : "";
  }).filter(Boolean).sort().join("|");
}
function settleProjectedElementTransition(projectedItems, previousById, pendingInput = null, {
  now = Date.now(),
  settleMs = 180,
  positionThreshold = 24,
  sizeRatioThreshold = 0.14,
  signatureQuantum = 3
} = {}) {
  const projected = Array.isArray(projectedItems) ? projectedItems.filter((item) => item?.id) : [];
  if (!projected.length) {
    return { commit: true, pending: null, abrupt: false };
  }
  const previous = previousById instanceof Map ? previousById : /* @__PURE__ */ new Map();
  let abrupt = false;
  for (const item of projected) {
    const before = transitionBox(previous.get(item.id));
    const after = transitionBox(item);
    if (!before || !after) {
      continue;
    }
    const positionLimit = Math.max(
      Math.max(0, finite2(positionThreshold, 24)),
      Math.min(56, Math.max(before.width, before.height) * 0.18)
    );
    const positionShift = Math.hypot(after.x - before.x, after.y - before.y);
    const sizeRatio = Math.max(
      Math.abs(after.width - before.width) / Math.max(1, before.width),
      Math.abs(after.height - before.height) / Math.max(1, before.height)
    );
    if (positionShift > positionLimit || sizeRatio > Math.max(0, finite2(sizeRatioThreshold, 0.14))) {
      abrupt = true;
      break;
    }
  }
  if (!abrupt) {
    return { commit: true, pending: null, abrupt: false };
  }
  const signature = projectedTransitionSignature(projected, signatureQuantum);
  const timestamp = finite2(now, Date.now());
  const pending = pendingInput?.signature === signature ? {
    signature,
    firstSeen: finite2(pendingInput.firstSeen, timestamp),
    observations: Math.max(1, Math.floor(finite2(pendingInput.observations, 1))) + 1
  } : {
    signature,
    firstSeen: timestamp,
    observations: 1
  };
  const commit = pending.observations >= 2 && timestamp - pending.firstSeen >= Math.max(0, finite2(settleMs, 180));
  return {
    commit,
    pending: commit ? null : pending,
    abrupt: true
  };
}
function rectGap(a, b) {
  const dx = Math.max(0, a.x - (b.x + b.width), b.x - (a.x + a.width));
  const dy = Math.max(0, a.y - (b.y + b.height), b.y - (a.y + a.height));
  return Math.hypot(dx, dy);
}
function overlapArea(a, b) {
  const width = Math.max(0, Math.min(a.x + a.width, b.x + b.width) - Math.max(a.x, b.x));
  const height = Math.max(0, Math.min(a.y + a.height, b.y + b.height) - Math.max(a.y, b.y));
  return width * height;
}
function overlapCenter(a, b) {
  const left = Math.max(a.x, b.x);
  const right = Math.min(a.x + a.width, b.x + b.width);
  const top = Math.max(a.y, b.y);
  const bottom = Math.min(a.y + a.height, b.y + b.height);
  if (right <= left || bottom <= top) {
    return null;
  }
  return {
    x: (left + right) / 2,
    y: (top + bottom) / 2
  };
}
function boxCorner(box, name) {
  return {
    x: box.x + (name === "topRight" || name === "bottomRight" ? box.width : 0),
    y: box.y + (name === "bottomLeft" || name === "bottomRight" ? box.height : 0)
  };
}
function nearestCornerPair(source, target) {
  let best = { sourceCorner: "topLeft", targetCorner: "topLeft", distance: Number.POSITIVE_INFINITY };
  for (const sourceCorner of CORNER_NAMES) {
    const sourcePoint = boxCorner(source, sourceCorner);
    for (const targetCorner of CORNER_NAMES) {
      const targetPoint = boxCorner(target, targetCorner);
      const distance = Math.hypot(targetPoint.x - sourcePoint.x, targetPoint.y - sourcePoint.y);
      if (distance < best.distance) {
        best = { sourceCorner, targetCorner, distance };
      }
    }
  }
  return best;
}
function normalizeBoxAnchor(box, point) {
  return {
    u: clamp3((point.x - box.x) / Math.max(0.01, box.width), 0, 1),
    v: clamp3((point.y - box.y) / Math.max(0.01, box.height), 0, 1)
  };
}
function captureElementRelations(items, { nearDistance = 80, maxRelations = 3 } = {}) {
  const normalized = (Array.isArray(items) ? items : []).map((item) => ({
    id: typeof item?.id === "string" ? item.id : "",
    scale: clamp3(finite2(item?.scale, 1), 0.05, 20),
    xScale: clamp3(finite2(item?.xScale, item?.scale ?? 1), 0.05, 20),
    yScale: clamp3(finite2(item?.yScale, item?.scale ?? 1), 0.05, 20),
    bounds: normalizeBox({
      x: item?.bounds?.minX ?? item?.bounds?.x,
      y: item?.bounds?.minY ?? item?.bounds?.y,
      width: item?.bounds?.width ?? finite2(item?.bounds?.maxX, 0) - finite2(item?.bounds?.minX, 0),
      height: item?.bounds?.height ?? finite2(item?.bounds?.maxY, 0) - finite2(item?.bounds?.minY, 0)
    })
  })).filter((item) => item.id);
  const cellSize = Math.max(16, finite2(nearDistance, 80));
  const buckets = /* @__PURE__ */ new Map();
  const cellRange = (bounds, padding = 0) => ({
    minX: Math.floor((bounds.x - padding) / cellSize),
    maxX: Math.floor((bounds.x + bounds.width + padding) / cellSize),
    minY: Math.floor((bounds.y - padding) / cellSize),
    maxY: Math.floor((bounds.y + bounds.height + padding) / cellSize)
  });
  for (const item of normalized) {
    const range = cellRange(item.bounds);
    for (let x = range.minX; x <= range.maxX; x += 1) {
      for (let y = range.minY; y <= range.maxY; y += 1) {
        const key = `${x}:${y}`;
        const bucket = buckets.get(key) || [];
        bucket.push(item);
        buckets.set(key, bucket);
      }
    }
  }
  const relations = /* @__PURE__ */ new Map();
  for (const source of normalized) {
    const candidates = [];
    const nearby = /* @__PURE__ */ new Set();
    const range = cellRange(source.bounds, nearDistance);
    for (let x = range.minX; x <= range.maxX; x += 1) {
      for (let y = range.minY; y <= range.maxY; y += 1) {
        for (const item of buckets.get(`${x}:${y}`) || []) {
          nearby.add(item);
        }
      }
    }
    for (const target of nearby) {
      if (target === source) {
        continue;
      }
      const overlap = overlapArea(source.bounds, target.bounds);
      const gap = rectGap(source.bounds, target.bounds);
      if (overlap <= 0 && gap > nearDistance) {
        continue;
      }
      const kind = overlap > 0 ? "intersection" : "near";
      const relationScaleX = Math.max(0.05, (source.xScale + target.xScale) / 2);
      const relationScaleY = Math.max(0.05, (source.yScale + target.yScale) / 2);
      const cornerPair = nearestCornerPair(source.bounds, target.bounds);
      const intersectionCenter = kind === "intersection" ? overlapCenter(source.bounds, target.bounds) : null;
      const sourceAnchor = intersectionCenter ? normalizeBoxAnchor(source.bounds, intersectionCenter) : null;
      const targetAnchor = intersectionCenter ? normalizeBoxAnchor(target.bounds, intersectionCenter) : null;
      const sourceCornerPoint = intersectionCenter || boxCorner(source.bounds, cornerPair.sourceCorner);
      const targetCornerPoint = intersectionCenter || boxCorner(target.bounds, cornerPair.targetCorner);
      candidates.push({
        targetId: target.id,
        kind,
        sourceCorner: cornerPair.sourceCorner,
        targetCorner: cornerPair.targetCorner,
        ...sourceAnchor ? { sourceU: sourceAnchor.u, sourceV: sourceAnchor.v } : {},
        ...targetAnchor ? { targetU: targetAnchor.u, targetV: targetAnchor.v } : {},
        dx: (targetCornerPoint.x - sourceCornerPoint.x) / relationScaleX,
        dy: (targetCornerPoint.y - sourceCornerPoint.y) / relationScaleY,
        weight: kind === "intersection" ? 0.32 : 0.14,
        score: kind === "intersection" ? -overlap - 1 : gap
      });
    }
    candidates.sort((a, b) => a.score - b.score || a.targetId.localeCompare(b.targetId));
    relations.set(source.id, candidates.slice(0, maxRelations).map(({ score, ...relation }) => relation));
  }
  return relations;
}
function projectElementPoints(points, layoutInput, projectedBox, { canvasWidth, canvasHeight, preserveAspectRatio = false } = {}) {
  const layout = normalizeElementLayout(layoutInput);
  if (!layout || !projectedBox) {
    return Array.isArray(points) ? points.map((point) => ({ ...point })) : [];
  }
  const targetWidth = Math.max(1, finite2(canvasWidth, 1));
  const targetHeight = Math.max(1, finite2(canvasHeight, 1));
  const source = layout.sourceFrame;
  const sourceWidth = Math.max(0.01, layout.box.width);
  const sourceHeight = Math.max(0.01, layout.box.height);
  const projectedWidth = Math.max(0.01, finite2(projectedBox.width, sourceWidth));
  const projectedHeight = Math.max(0.01, finite2(projectedBox.height, sourceHeight));
  const uniformScale = Math.min(projectedWidth / sourceWidth, projectedHeight / sourceHeight);
  return (Array.isArray(points) ? points : []).map((point) => {
    const anchor = point?.anchor;
    const sourceX = anchor && Number.isFinite(Number(anchor.x)) ? source.contentLeft + Number(anchor.x) * source.contentWidth : clamp3(finite2(point?.x, 0), 0, 1) * source.surfaceWidth;
    const sourceY = anchor && Number.isFinite(Number(anchor.y)) ? Number(anchor.y) * source.documentHeight : clamp3(finite2(point?.y, 0), 0, 1) * source.documentHeight;
    const localX = (sourceX - layout.box.x) / layout.box.width;
    const localY = (sourceY - layout.box.y) / layout.box.height;
    const projectedLocalX = preserveAspectRatio ? (sourceX - layout.box.x) * uniformScale : localX * projectedBox.width;
    const projectedLocalY = preserveAspectRatio ? (sourceY - layout.box.y) * uniformScale : localY * projectedBox.height;
    return {
      ...point,
      x: clamp3((projectedBox.x + projectedLocalX) / targetWidth, 0, 1),
      y: clamp3((projectedBox.y + projectedLocalY) / targetHeight, 0, 1)
    };
  });
}
function scaleElementMetrics(metricsInput, scaleInput) {
  const metrics = normalizeMetrics(metricsInput);
  const scaleBox = typeof scaleInput === "object" && scaleInput ? scaleInput : null;
  const scale = clamp3(finite2(scaleBox?.scale, finite2(scaleInput, 1)), 0.42, 2.4);
  const xScale = clamp3(finite2(scaleBox?.xScale, scale), 0.34, 2.8);
  const yScale = clamp3(finite2(scaleBox?.yScale, scale), 0.34, 2.8);
  return {
    width: metrics.width ? clamp3(metrics.width * scale, 0.5, 80) : void 0,
    fontSize: metrics.fontSize ? clamp3(metrics.fontSize * scale, 10, 72) : void 0,
    textWidth: metrics.textWidth ? clamp3(metrics.textWidth * xScale, 24, 900) : void 0,
    previewWidth: metrics.previewWidth ? clamp3(metrics.previewWidth * xScale, 80, 900) : void 0,
    previewHeight: metrics.previewHeight ? clamp3(metrics.previewHeight * yScale, 40, 700) : void 0
  };
}

// src/markdown-anchors.mjs
function normalizeRenderedText(value) {
  return String(value || "").replace(/\u00a0/g, " ").replace(/\r\n/g, "\n").split("\n").map((line) => line.trim()).filter(Boolean).join("\n").trim();
}
function normalizeMarkdownText(value) {
  let text = String(value || "");
  text = text.replace(/<br\s*\/?>/gi, "\n").replace(/<\/(p|div|li|h[1-6])>/gi, "\n").replace(/<pre[^>]*><code[^>]*>([\s\S]*?)<\/code><\/pre>/gi, "$1").replace(/^\s*(?:```|~~~)[^\n]*$/gm, "").replace(/\$\$([\s\S]*?)\$\$/g, "$1").replace(/^\s*\$\$\s*$/gm, "").replace(/^\s*:::[^\n]*$/gm, "").replace(/<\/?(span|u|mark|kbd|sup|sub|small|strong|b|em|i|code)[^>]*>/gi, "").replace(/<[^>]+>/g, "").replace(/!\[([^\]]*)\]\([^)]+\)/g, "$1").replace(/!\[\[([^\]|]+)\|([^\]]+)\]\]/g, "$2").replace(/!\[\[([^\]]+)\]\]/g, "$1").replace(/^\s*[-*+]\s+\[[ xX]\]\s+/gm, "").replace(/^#{1,6}\s+/gm, "").replace(/^\s{0,3}>\s?/gm, "").replace(/^\s*[-*+]\s+/gm, "").replace(/^\s*\d+[.)]\s+/gm, "").replace(/\*\*([^*]+)\*\*/g, "$1").replace(/\*([^*]+)\*/g, "$1").replace(/__([^_]+)__/g, "$1").replace(/_([^_]+)_/g, "$1").replace(/~~([^~]+)~~/g, "$1").replace(/==([^=]+)==/g, "$1").replace(/`([^`]+)`/g, "$1").replace(/\[\[([^\]|]+)\|([^\]]+)\]\]/g, "$2").replace(/\[\[([^\]]+)\]\]/g, "$1").replace(/\[([^\]]+)\]\([^)]+\)/g, "$1").split("\n").map((line) => {
    if (!line.includes("|")) {
      return line;
    }
    const cells = line.trim().replace(/^\||\|$/g, "").split("|").map((cell) => cell.trim());
    if (cells.length > 1 && cells.every((cell) => /^:?-{3,}:?$/.test(cell))) {
      return "";
    }
    return cells.length > 1 ? cells.join("	") : line;
  }).join("\n");
  return normalizeRenderedText(text);
}
function calloutRenderedAliases(sourceText) {
  const lines = String(sourceText || "").split(/\r?\n/).map((line) => line.replace(/^\s{0,3}>\s?/, ""));
  const marker = String(lines[0] || "").trim().match(/^\[!([^\]]+)\][+-]?(?:\s+(.+?))?\s*$/);
  if (!marker) {
    return [];
  }
  const type = normalizeRenderedText(marker[1]);
  const customTitle = normalizeMarkdownText(marker[2] || "");
  const defaultTitle = type ? `${type.charAt(0).toUpperCase()}${type.slice(1)}` : "";
  const body = normalizeMarkdownText(lines.slice(1).join("\n"));
  const title = customTitle || defaultTitle;
  return [
    normalizeRenderedText([title, body].filter(Boolean).join("\n")),
    normalizeRenderedText([type, body].filter(Boolean).join("\n")),
    body
  ].filter(Boolean);
}
function footnoteRenderedAliases(sourceText) {
  const source = String(sourceText || "");
  if (!/\[\^[^\]]+\](?:\([^)]+\))?/.test(source)) {
    return [];
  }
  const keepLabel = source.replace(/\[\^([^\]]+)\]\([^)]+\)/g, "$1").replace(/\[\^([^\]]+)\]/g, "$1");
  const omitLabel = source.replace(/\[\^[^\]]+\]\([^)]+\)/g, "").replace(/\[\^[^\]]+\]/g, "");
  return [normalizeMarkdownText(keepLabel), normalizeMarkdownText(omitLabel)].filter(Boolean);
}
function candidateRenderedAliases(candidate) {
  return Array.from(new Set([
    normalizeRenderedText(candidate?.text),
    candidate?.kind === "thematic-break" ? "---" : "",
    ...calloutRenderedAliases(candidate?.sourceText),
    ...footnoteRenderedAliases(candidate?.sourceText)
  ].filter(Boolean)));
}
function isMarkdownTableDelimiter(rawLine) {
  const cells = String(rawLine || "").trim().replace(/^\||\|$/g, "").split("|").map((cell) => cell.trim());
  return cells.length > 1 && cells.every((cell) => /^:?-{3,}:?$/.test(cell));
}
function markdownLineKind(rawLine) {
  const raw = String(rawLine || "");
  const trimmed = raw.trim();
  if (!trimmed) return "blank";
  if (/^\s{0,3}(?:```|~~~)/.test(raw)) return "fence";
  if (/^\s{0,3}#{1,6}(?:\s+|$)/.test(raw)) return "heading";
  if (/^\s{0,3}(?:(?:\*\s*){3,}|(?:-\s*){3,}|(?:_\s*){3,})$/.test(raw)) return "thematic-break";
  if (/^\s*(?:[-+*]|\d+[.)])\s+/.test(raw)) return "list-item";
  if (/^\s{0,3}>/.test(raw)) return "blockquote";
  if (/^\s*(?:!\[[^\]]*\]\([^)]+\)|!\[\[[^\]]+\]\])\s*$/.test(raw)) return "media";
  if (/^\s*\|/.test(raw) || isMarkdownTableDelimiter(raw)) return "table";
  if (/^\s{0,3}(?:\$\$|:::)/.test(raw)) return "container";
  return "paragraph";
}
function collectSemanticBlocks(input, rawLines) {
  const blocks = [];
  let active = null;
  let fenceMarker = "";
  let containerMarker = "";
  const appendBlock = (lineStart, lineEnd, kind) => {
    if (lineStart < 0 || lineEnd < lineStart || !rawLines[lineStart] || !rawLines[lineEnd]) {
      return;
    }
    const start = rawLines[lineStart].start;
    const end = rawLines[lineEnd].end;
    const sourceText = input.slice(start, end);
    const identitySource = kind === "heading" && lineEnd > lineStart ? rawLines[lineStart].raw : sourceText;
    const text = normalizeMarkdownText(identitySource);
    if (!text && !["thematic-break", "media"].includes(kind)) {
      return;
    }
    blocks.push({ lineStart, lineEnd, start, end, sourceText, text, kind });
  };
  const flush = () => {
    if (active) {
      appendBlock(active.start, active.end, active.kind);
      active = null;
    }
  };
  const begin = (line, kind) => {
    active = { start: line, end: line, kind };
  };
  for (let line = 0; line < rawLines.length; line += 1) {
    const raw = rawLines[line].raw;
    const nextRaw = rawLines[line + 1]?.raw || "";
    const kind = raw.includes("|") && isMarkdownTableDelimiter(nextRaw) ? "table" : markdownLineKind(raw);
    if (active?.kind === "fence") {
      active.end = line;
      if (line > active.start && raw.trim().startsWith(fenceMarker)) {
        flush();
        fenceMarker = "";
      }
      continue;
    }
    if (active?.kind === "container") {
      active.end = line;
      if (line > active.start && raw.trim().startsWith(containerMarker)) {
        flush();
        containerMarker = "";
      }
      continue;
    }
    if (active?.kind === "paragraph" && active.start === active.end && /^\s*(?:=+|-+)\s*$/.test(raw)) {
      active.end = line;
      active.kind = "heading";
      flush();
      continue;
    }
    if (kind === "blank") {
      flush();
      continue;
    }
    if (kind === "fence") {
      flush();
      fenceMarker = raw.trim().startsWith("~~~") ? "~~~" : "```";
      begin(line, "fence");
      continue;
    }
    if (["heading", "thematic-break", "media"].includes(kind)) {
      flush();
      appendBlock(line, line, kind);
      continue;
    }
    if (active?.kind === "list-item" && /^\s{2,}\S/.test(raw)) {
      active.end = line;
      continue;
    }
    if (active?.kind === "list-item" && kind === "paragraph") {
      active.end = line;
      continue;
    }
    if (kind === "list-item") {
      flush();
      begin(line, kind);
      continue;
    }
    if (active?.kind === "table" && raw.includes("|")) {
      active.end = line;
      continue;
    }
    if (["blockquote", "table", "container"].includes(kind)) {
      if (kind === "container") {
        flush();
        const trimmed = raw.trim();
        containerMarker = trimmed.startsWith("$$") ? "$$" : ":::";
        if (trimmed.length > containerMarker.length && trimmed.endsWith(containerMarker)) {
          appendBlock(line, line, kind);
          containerMarker = "";
          continue;
        }
        begin(line, kind);
      } else if (active?.kind === kind) {
        active.end = line;
      } else {
        flush();
        begin(line, kind);
      }
      continue;
    }
    if (active?.kind === "paragraph") {
      active.end = line;
    } else {
      flush();
      begin(line, "paragraph");
    }
  }
  flush();
  return blocks;
}
function collectCandidates(source) {
  const input = String(source || "");
  const parts = input.split(/(\r?\n)/);
  const rawLines = [];
  let offset = 0;
  for (let index = 0; index < parts.length; index += 2) {
    const raw = parts[index] || "";
    const newline = parts[index + 1] || "";
    rawLines.push({ raw, start: offset, end: offset + raw.length });
    offset += raw.length + newline.length;
  }
  const candidates = [];
  const semanticBlocks = collectSemanticBlocks(input, rawLines);
  candidates.push(...semanticBlocks);
  for (let line = 0; line < rawLines.length; line += 1) {
    const text = normalizeMarkdownText(rawLines[line].raw);
    if (text) {
      candidates.push({
        lineStart: line,
        lineEnd: line,
        start: rawLines[line].start,
        end: rawLines[line].end,
        sourceText: input.slice(rawLines[line].start, rawLines[line].end),
        text,
        kind: "line"
      });
    }
  }
  return candidates;
}
function createMarkdownSourceIndex(source) {
  const input = String(source || "");
  const candidates = collectCandidates(input);
  const exact = /* @__PURE__ */ new Map();
  const compact = /* @__PURE__ */ new Map();
  const append = (map, key, candidate) => {
    const values = map.get(key) || [];
    values.push(candidate);
    map.set(key, values);
  };
  for (const candidate of candidates) {
    for (const alias of candidateRenderedAliases(candidate)) {
      append(exact, alias, candidate);
      append(compact, alias.replace(/\s+/g, ""), candidate);
    }
  }
  return {
    source: input,
    candidates,
    exact,
    compact
  };
}
function indexedCandidates(source, sourceIndex) {
  const input = String(source || "");
  return sourceIndex?.source === input && Array.isArray(sourceIndex.candidates) ? sourceIndex.candidates : collectCandidates(input);
}
function indexedExactCandidates(source, rendered, compactRendered, sourceIndex) {
  const input = String(source || "");
  if (sourceIndex?.source === input && sourceIndex.exact instanceof Map && sourceIndex.compact instanceof Map) {
    return uniqueSourceCandidates([
      ...sourceIndex.exact.get(rendered) || [],
      ...sourceIndex.compact.get(compactRendered) || []
    ]);
  }
  return uniqueSourceCandidates(indexedCandidates(input, sourceIndex).filter((candidate) => {
    return candidateRenderedAliases(candidate).some((alias) => alias === rendered || alias.replace(/\s+/g, "") === compactRendered);
  }));
}
function candidateDistanceFromRange(candidate, lineStart, lineEnd) {
  if (!Number.isFinite(lineStart)) {
    return Number.POSITIVE_INFINITY;
  }
  const start = Math.min(lineStart, Number.isFinite(lineEnd) ? lineEnd : lineStart);
  const end = Math.max(lineStart, Number.isFinite(lineEnd) ? lineEnd : lineStart);
  if (candidate.lineEnd >= start && candidate.lineStart <= end) {
    return 0;
  }
  return candidate.lineStart > end ? candidate.lineStart - end : start - candidate.lineEnd;
}
function uniqueSourceCandidates(candidates) {
  const seen = /* @__PURE__ */ new Set();
  return candidates.filter((candidate) => {
    const key = `${candidate.start}:${candidate.end}`;
    if (seen.has(key)) {
      return false;
    }
    seen.add(key);
    return true;
  });
}
function normalizeEmbedDestination(value) {
  let target = String(value || "").trim().replace(/^!/, "");
  const wiki = target.match(/^\[\[([\s\S]+)\]\]$/);
  if (wiki) {
    target = wiki[1];
  }
  target = target.split("|")[0].trim().replace(/^<|>$/g, "");
  try {
    target = decodeURIComponent(target);
  } catch {
  }
  return target.replace(/\\/g, "/").replace(/^\.\//, "").replace(/\.md(?=#|$)/i, "").toLowerCase();
}
function embedDestinations(sourceText) {
  const text = String(sourceText || "");
  const destinations = [];
  for (const match of text.matchAll(/!\[\[([^\]]+)\]\]/g)) {
    destinations.push(normalizeEmbedDestination(match[1]));
  }
  for (const match of text.matchAll(/!\[[^\]]*\]\(\s*(?:<([^>]+)>|([^\s)]+))(?:\s+["'][^"']*["'])?\s*\)/g)) {
    destinations.push(normalizeEmbedDestination(match[1] || match[2]));
  }
  return destinations.filter(Boolean);
}
function resolveMarkdownEmbedSourceTarget(source, embedDestination, sourceInfo = {}, sourceIndex = null) {
  const expected = normalizeEmbedDestination(embedDestination);
  if (!expected) {
    return null;
  }
  const expectedPath = expected.split("#")[0];
  const candidates = uniqueSourceCandidates(indexedCandidates(source, sourceIndex).filter((candidate) => {
    return embedDestinations(candidate.sourceText).some((destination) => {
      return destination === expected || destination.split("#")[0] === expectedPath;
    });
  }));
  if (!candidates.length) {
    return null;
  }
  const exact = candidates.filter((candidate) => embedDestinations(candidate.sourceText).includes(expected));
  const matches = exact.length ? exact : candidates;
  const lineStart = sourceInfo?.lineStart === null || sourceInfo?.lineStart === void 0 || sourceInfo?.lineStart === "" ? Number.NaN : Number(sourceInfo.lineStart);
  const lineEnd = sourceInfo?.lineEnd === null || sourceInfo?.lineEnd === void 0 || sourceInfo?.lineEnd === "" ? Number.NaN : Number(sourceInfo.lineEnd);
  const ranked = matches.map((candidate) => ({
    candidate,
    distance: candidateDistanceFromRange(
      candidate,
      Number.isFinite(lineStart) ? lineStart : Number.NaN,
      Number.isFinite(lineEnd) ? lineEnd : lineStart
    )
  })).sort((a, b) => a.distance - b.distance || a.candidate.lineStart - b.candidate.lineStart);
  if (ranked.length > 1 && ranked[0].distance === ranked[1].distance) {
    return null;
  }
  if (matches.length > 1 && !Number.isFinite(lineStart)) {
    return null;
  }
  return sourceTarget(ranked[0].candidate, ranked[0].candidate.sourceText);
}
function sourceTarget(candidate, renderedText) {
  return candidate ? {
    start: candidate.start,
    end: candidate.end,
    line: candidate.lineStart,
    endLine: candidate.lineEnd,
    text: candidate.sourceText,
    kind: candidate.kind,
    normalizedText: normalizeRenderedText(renderedText),
    normalizedMarkdown: candidate.text
  } : null;
}
function findRenderedMarkdownSourceTargets(source, renderedText, sourceIndex = null) {
  const rendered = normalizeRenderedText(renderedText);
  if (!rendered) {
    return [];
  }
  const compactRendered = rendered.replace(/\s+/g, "");
  const candidates = indexedCandidates(source, sourceIndex);
  const exact = indexedExactCandidates(source, rendered, compactRendered, sourceIndex);
  if (exact.length) {
    return exact.map((candidate) => sourceTarget(candidate, renderedText));
  }
  return uniqueSourceCandidates(candidates.map((candidate) => {
    const contains = candidate.text.includes(rendered) || rendered.includes(candidate.text);
    const overlap = contains ? Math.min(candidate.text.length, rendered.length) / Math.max(candidate.text.length, rendered.length) : 0;
    return { candidate, overlap };
  }).filter(({ overlap }) => overlap >= 0.75).map(({ candidate }) => candidate)).map((candidate) => sourceTarget(candidate, renderedText));
}
function resolveRenderedMarkdownSourceTarget(source, renderedText, sourceInfo = {}, sourceIndex = null) {
  const rendered = normalizeRenderedText(renderedText);
  if (!rendered) {
    return null;
  }
  const compactRendered = rendered.replace(/\s+/g, "");
  const candidates = indexedCandidates(source, sourceIndex);
  const exact = indexedExactCandidates(source, rendered, compactRendered, sourceIndex);
  const lineStart = sourceInfo?.lineStart === null || sourceInfo?.lineStart === void 0 || sourceInfo?.lineStart === "" ? Number.NaN : Number(sourceInfo.lineStart);
  const lineEnd = sourceInfo?.lineEnd === null || sourceInfo?.lineEnd === void 0 || sourceInfo?.lineEnd === "" ? Number.NaN : Number(sourceInfo.lineEnd);
  const choose = (matches, { allowUnique = false, maxDistance = 2 } = {}) => {
    if (allowUnique && matches.length === 1) {
      return matches[0];
    }
    if (!Number.isFinite(lineStart)) {
      return null;
    }
    const ranked = matches.map((candidate) => ({
      candidate,
      distance: candidateDistanceFromRange(candidate, lineStart, lineEnd)
    })).sort((a, b) => {
      const distance = a.distance - b.distance;
      if (distance) return distance;
      const aSpan = a.candidate.lineEnd - a.candidate.lineStart;
      const bSpan = b.candidate.lineEnd - b.candidate.lineStart;
      const aSemantic = a.candidate.kind === "line" ? 1 : 0;
      const bSemantic = b.candidate.kind === "line" ? 1 : 0;
      return aSemantic - bSemantic || bSpan - aSpan || a.candidate.lineStart - b.candidate.lineStart;
    });
    const first = ranked[0];
    const second = ranked[1];
    const equallyRanked = first && second && first.distance === second.distance && first.candidate.kind === "line" === (second.candidate.kind === "line") && first.candidate.lineEnd - first.candidate.lineStart === second.candidate.lineEnd - second.candidate.lineStart;
    if (!first || first.distance > maxDistance || equallyRanked) {
      return null;
    }
    return first.candidate;
  };
  const exactMatch = choose(exact, { allowUnique: true });
  if (exactMatch) {
    return sourceTarget(exactMatch, renderedText);
  }
  if (exact.length > 1) {
    return null;
  }
  const partial = uniqueSourceCandidates(candidates.map((candidate) => {
    const contains = candidate.text.includes(rendered) || rendered.includes(candidate.text);
    const overlap = contains ? Math.min(candidate.text.length, rendered.length) / Math.max(candidate.text.length, rendered.length) : 0;
    return { candidate, overlap };
  }).filter(({ overlap }) => overlap >= 0.75).map(({ candidate }) => candidate));
  return sourceTarget(choose(partial, { maxDistance: 1 }), renderedText);
}
function matchRenderedTextToMarkdown(source, renderedText, sourceIndex = null) {
  const rendered = normalizeRenderedText(renderedText);
  if (!rendered) {
    return null;
  }
  const candidates = indexedCandidates(source, sourceIndex);
  const indexedExact = sourceIndex?.source === String(source || "") && sourceIndex.exact instanceof Map ? sourceIndex.exact.get(rendered) || [] : candidates.filter((candidate) => candidate.text === rendered);
  const exact = indexedExact.slice().sort((a, b) => (a.kind === "line" ? 1 : 0) - (b.kind === "line" ? 1 : 0) || b.lineEnd - b.lineStart - (a.lineEnd - a.lineStart))[0];
  if (exact) {
    return { lineStart: exact.lineStart, lineEnd: exact.lineEnd, confidence: 1 };
  }
  const compactRendered = rendered.replace(/\s+/g, "");
  const indexedCompact = sourceIndex?.source === String(source || "") && sourceIndex.compact instanceof Map ? sourceIndex.compact.get(compactRendered) || [] : candidates.filter((candidate) => candidate.text.replace(/\s+/g, "") === compactRendered);
  const whitespaceEquivalent = indexedCompact.slice().sort((a, b) => (a.kind === "line" ? 1 : 0) - (b.kind === "line" ? 1 : 0) || b.lineEnd - b.lineStart - (a.lineEnd - a.lineStart))[0];
  if (whitespaceEquivalent) {
    return { lineStart: whitespaceEquivalent.lineStart, lineEnd: whitespaceEquivalent.lineEnd, confidence: 0.98 };
  }
  const partial = candidates.map((candidate) => {
    const contains = candidate.text.includes(rendered) || rendered.includes(candidate.text);
    const overlap = contains ? Math.min(candidate.text.length, rendered.length) / Math.max(candidate.text.length, rendered.length) : 0;
    return { candidate, overlap };
  }).filter(({ overlap }) => overlap >= 0.55).sort((a, b) => b.overlap - a.overlap || a.candidate.lineEnd - a.candidate.lineStart - (b.candidate.lineEnd - b.candidate.lineStart))[0];
  if (!partial) {
    return null;
  }
  return {
    lineStart: partial.candidate.lineStart,
    lineEnd: partial.candidate.lineEnd,
    confidence: Math.min(0.92, Math.max(0.75, partial.overlap))
  };
}

// src/markdown-reorder.mjs
function normalizedSource(value) {
  return String(value ?? "").replace(/\r\n?/g, "\n");
}
function preservesAllMovedMarkdownBlocks(source, movedBlocks) {
  let remaining = normalizedSource(source);
  for (const block of movedBlocks || []) {
    const needle = normalizedSource(block).trim();
    if (!needle) {
      return false;
    }
    const index = remaining.indexOf(needle);
    if (index < 0) {
      return false;
    }
    remaining = `${remaining.slice(0, index)}${remaining.slice(index + needle.length)}`;
  }
  return true;
}

// src/markdown-inline-row.mjs
function uniqueIds(values) {
  const seen = /* @__PURE__ */ new Set();
  return (values || []).map((value) => String(value || "")).filter((value) => {
    if (!value || seen.has(value)) {
      return false;
    }
    seen.add(value);
    return true;
  });
}
function distributeInlineRowSpans(itemCount, columns = 12) {
  const count = Math.max(0, Math.floor(Number(itemCount) || 0));
  const totalColumns = Math.max(1, Math.floor(Number(columns) || 12));
  if (!count || count > totalColumns) {
    return [];
  }
  const base = Math.floor(totalColumns / count);
  const remainder = totalColumns % count;
  return Array.from({ length: count }, (_, index) => base + (index < remainder ? 1 : 0));
}
function fitPreferredSpans(preferred, columns) {
  if (!preferred.length || preferred.length > columns) {
    return [];
  }
  const available = columns - preferred.length;
  const weights = preferred.map((span) => Math.max(0, span - 1));
  const totalWeight = weights.reduce((sum, weight) => sum + weight, 0);
  if (!totalWeight || available <= 0) {
    return preferred.map(() => 1);
  }
  const exactExtras = weights.map((weight) => weight / totalWeight * available);
  const extras = exactExtras.map(Math.floor);
  let remainder = available - extras.reduce((sum, value) => sum + value, 0);
  const order = exactExtras.map((value, index) => ({ index, fraction: value - extras[index] })).sort((left, right) => right.fraction - left.fraction || left.index - right.index);
  for (const item of order) {
    if (remainder <= 0) {
      break;
    }
    extras[item.index] += 1;
    remainder -= 1;
  }
  return extras.map((extra) => extra + 1);
}
function allocateInlineRow({
  existingIds = [],
  movingIds = [],
  targetId = "",
  side = "right",
  extraMovingCount = 0,
  columns = 12,
  preferredSpanById = null
} = {}) {
  const target = String(targetId || "__notedraw-inline-target__");
  const moving = uniqueIds(movingIds);
  const movingSet = new Set(moving);
  const existing = uniqueIds(existingIds).filter((id) => !movingSet.has(id));
  if (!existing.includes(target)) {
    existing.push(target);
  }
  const extraCount = Math.max(0, Math.floor(Number(extraMovingCount) || 0));
  for (let index = 0; index < extraCount; index += 1) {
    moving.push(`__notedraw-inline-extra-${index}__`);
  }
  const targetIndex = Math.max(0, existing.indexOf(target));
  const insertionIndex = side === "left" ? targetIndex : targetIndex + 1;
  const orderedIds = existing.slice();
  orderedIds.splice(insertionIndex, 0, ...moving);
  const totalColumns = Math.max(1, Math.floor(Number(columns) || 12));
  const preferredSpan = (id) => {
    const raw = preferredSpanById instanceof Map ? preferredSpanById.get(id) : preferredSpanById?.[id];
    return Math.max(1, Math.min(totalColumns, Math.round(Number(raw) || totalColumns)));
  };
  let spans = [];
  if (preferredSpanById && orderedIds.length <= totalColumns) {
    const preferred = orderedIds.map(preferredSpan);
    spans = preferred.reduce((sum, span) => sum + span, 0) <= totalColumns ? preferred : fitPreferredSpans(preferred, totalColumns);
  } else {
    spans = distributeInlineRowSpans(orderedIds.length, totalColumns);
  }
  const spanById = new Map(orderedIds.map((id, index) => [id, spans[index] || Number(columns) || 12]));
  return {
    canFit: spans.length === orderedIds.length,
    orderedIds,
    spans,
    spanById,
    totalCount: orderedIds.length
  };
}

// src/markdown-block-records.mjs
function finiteInteger(value) {
  const number = Number(value);
  return Number.isFinite(number) ? Math.round(number) : null;
}
function semanticMarkdownBlockKey(block) {
  const path = String(block?.path || "");
  const lineStart = finiteInteger(block?.lineStart);
  const lineEnd = finiteInteger(block?.lineEnd);
  if (!path || lineStart === null || lineEnd === null || lineStart < 0 || lineEnd < lineStart) {
    return "";
  }
  return `${path}\0${lineStart}\0${lineEnd}\0${String(block?.explicitLineGroup || "")}`;
}
function shortestUsefulHint(...values) {
  return values.map((value) => String(value || "").trim()).filter(Boolean).sort((left, right) => {
    const leftLines = left.split(/\r?\n/).length;
    const rightLines = right.split(/\r?\n/).length;
    return leftLines - rightLines || left.length - right.length;
  })[0] || "";
}
function firstValue(records, property, predicate = Boolean) {
  return records.map((record) => record?.[property]).find(predicate);
}
function hasExplicitParallelMarkdownLayout(record) {
  if (!record || record.floating) {
    return false;
  }
  const span = Number(record.span);
  const widthScale = Number(record.widthScale);
  return Number.isFinite(span) && span > 0 && span < 12 || Number.isFinite(widthScale) && widthScale > 0 && widthScale < 0.999;
}
function markdownInlineWidthMode(record) {
  if (record?.inlineWidthMode === "fixed" || record?.inlineWidthMode === "fit") {
    return record.inlineWidthMode;
  }
  const span = Number(record?.span);
  const widthScale = Number(record?.widthScale);
  return span < 12 || widthScale < 0.999 ? "fixed" : "fit";
}
function mergeSemanticMarkdownBlocks(current, duplicate) {
  const records = [current, duplicate];
  const layoutSource = hasExplicitParallelMarkdownLayout(current) ? current : hasExplicitParallelMarkdownLayout(duplicate) ? duplicate : null;
  const explicitFloating = records.find((record) => record?.floating && record?.floatingExplicit && record?.floatBox);
  const implicitFloating = records.every((record) => record?.floating && record?.floatBox) ? records.find((record) => record?.floatBox) : null;
  const floatingSource = explicitFloating || implicitFloating;
  return {
    ...current,
    textHint: shortestUsefulHint(current.textHint, duplicate.textHint),
    span: layoutSource ? Number(layoutSource.span) || 1 : Math.max(Number(current.span) || 1, Number(duplicate.span) || 1),
    noteFlowAutoSpan: Boolean(current.noteFlowAutoSpan && duplicate.noteFlowAutoSpan),
    widthScale: layoutSource ? Number(layoutSource.widthScale) || 1 : Math.max(Number(current.widthScale) || 0, Number(duplicate.widthScale) || 0),
    inlineWidthMode: layoutSource ? markdownInlineWidthMode(layoutSource) : markdownInlineWidthMode(current) === "fixed" || markdownInlineWidthMode(duplicate) === "fixed" ? "fixed" : "fit",
    minHeight: Math.max(Number(current.minHeight) || 0, Number(duplicate.minHeight) || 0),
    borderColor: firstValue(records, "borderColor") || "",
    backgroundColor: firstValue(records, "backgroundColor") || "",
    contentColor: firstValue(records, "contentColor") || "",
    contentOpacity: Math.max(Number(current.contentOpacity) || 0, Number(duplicate.contentOpacity) || 0),
    contentScale: Math.max(Number(current.contentScale) || 0, Number(duplicate.contentScale) || 0),
    floating: Boolean(floatingSource),
    floatingExplicit: Boolean(explicitFloating),
    floatBox: floatingSource?.floatBox || null,
    locked: Boolean(current.locked || duplicate.locked),
    groupId: firstValue(records, "groupId") || "",
    commandId: firstValue(records, "commandId") || "",
    commandName: firstValue(records, "commandName") || ""
  };
}
function repeatedSingleLineHint(value) {
  const lines = String(value || "").split(/\r?\n/).map((line) => line.trim()).filter(Boolean);
  return lines.length > 1 && lines.every((line) => line === lines[0]) ? lines[0] : "";
}
function dedupeMarkdownBlockRecords(value) {
  const records = Array.isArray(value) ? value.filter(Boolean) : [];
  const merged = [];
  const indexes = /* @__PURE__ */ new Map();
  for (const record of records) {
    const key = semanticMarkdownBlockKey(record);
    if (!key || !indexes.has(key)) {
      if (key) {
        indexes.set(key, merged.length);
      }
      merged.push({ ...record });
      continue;
    }
    const index = indexes.get(key);
    merged[index] = mergeSemanticMarkdownBlocks(merged[index], record);
  }
  const reliableHints = new Set(merged.filter((record) => Number(record.lineStart) > 0).map((record) => String(record.textHint || "").trim()).filter(Boolean));
  return merged.filter((record) => {
    if (Number(record.lineStart) !== 0 || Number(record.lineEnd) !== 0 || record.explicitLineGroup) {
      return true;
    }
    const repeated = repeatedSingleLineHint(record.textHint);
    return !repeated || !reliableHints.has(repeated);
  });
}

// src/markdown-block-layout.mjs
function clamp4(value, min, max) {
  return Math.min(max, Math.max(min, value));
}
function resolveSelectionResizeScales({
  scaleX = 1,
  scaleY = 1
} = {}) {
  const x = Number(scaleX);
  const y = Number(scaleY);
  if (!Number.isFinite(x) || !Number.isFinite(y)) {
    return { scaleX: 1, scaleY: 1, axis: null };
  }
  return { scaleX: x, scaleY: y, axis: null };
}
function normalizeMarkdownBlockMinHeight(value, maxHeight = 2400) {
  const height = Number(value);
  if (!Number.isFinite(height) || height <= 0) {
    return 0;
  }
  return Math.round(clamp4(height, 0, Math.max(0, Number(maxHeight) || 2400)));
}
function resizeMarkdownBlockMinHeight({
  currentHeight,
  naturalHeight,
  scaleY,
  maxHeight = 2400
} = {}) {
  const natural = Math.max(1, Number(naturalHeight) || 1);
  const current = Math.max(natural, Number(currentHeight) || natural);
  const desired = normalizeMarkdownBlockMinHeight(current * Math.max(0.12, Math.abs(Number(scaleY) || 1)), maxHeight);
  return desired >= natural + 1 ? desired : 0;
}
function markdownBlockPresentationMinHeight(block) {
  return block?.floating ? 0 : normalizeMarkdownBlockMinHeight(block?.minHeight);
}
function trimMarkdownClientRect(rect, {
  insetTop = 0,
  insetBottom = 0,
  scale = 1
} = {}) {
  const left = Number(rect?.left);
  const right = Number(rect?.right);
  const top = Number(rect?.top);
  const bottom = Number(rect?.bottom);
  if (![left, right, top, bottom].every(Number.isFinite) || right <= left || bottom <= top) {
    return null;
  }
  const visualScale = Math.max(0, Number(scale) || 0);
  const visibleTop = top + Math.max(0, Number(insetTop) || 0) * visualScale;
  const visibleBottom = bottom - Math.max(0, Number(insetBottom) || 0) * visualScale;
  if (visibleBottom <= visibleTop) {
    return null;
  }
  return {
    left,
    right,
    top: visibleTop,
    bottom: visibleBottom,
    width: right - left,
    height: visibleBottom - visibleTop
  };
}
function clientPointInRect(rect, clientPoint = {}) {
  const x = Number(clientPoint?.x);
  const y = Number(clientPoint?.y);
  return Boolean(rect) && Number.isFinite(x) && Number.isFinite(y) && x >= rect.left && x <= rect.right && y >= rect.top && y <= rect.bottom;
}
function markdownClientRectsOverlap(first, second, minimumOverlap = 4) {
  const values = [
    first?.left,
    first?.right,
    first?.top,
    first?.bottom,
    second?.left,
    second?.right,
    second?.top,
    second?.bottom
  ].map(Number);
  if (!values.every(Number.isFinite)) {
    return false;
  }
  const overlapX = Math.min(values[1], values[5]) - Math.max(values[0], values[4]);
  const overlapY = Math.min(values[3], values[7]) - Math.max(values[2], values[6]);
  const threshold = Math.max(0, Number(minimumOverlap) || 0);
  return overlapX > threshold && overlapY > threshold;
}
function resolveDragDropHorizontalIntent({
  clientX,
  targetLeft,
  targetRight,
  laneLeft = targetLeft,
  laneRight = targetRight,
  draggedLeft,
  leftContactTolerance = 8,
  rightIntentRatio = 0.5,
  horizontalRoom = true,
  requireRightIntent = false,
  rightTargetRatio = 0.55
} = {}) {
  const x = Number(clientX);
  const left = Number(targetLeft);
  const right = Number(targetRight);
  const surfaceLeft = Number(laneLeft);
  const surfaceRight = Number(laneRight);
  const movingLeft = draggedLeft === null || draggedLeft === void 0 || draggedLeft === "" ? Number.NaN : Number(draggedLeft);
  if (![x, left, right, surfaceLeft, surfaceRight].every(Number.isFinite) || right <= left || surfaceRight <= surfaceLeft) {
    return "vertical";
  }
  const laneWidth = surfaceRight - surfaceLeft;
  const contactTolerance = clamp4(Number(leftContactTolerance) || 0, 0, 24);
  if (Number.isFinite(movingLeft) && movingLeft <= surfaceLeft + contactTolerance) {
    return "line-start";
  }
  if (requireRightIntent) {
    if (!horizontalRoom) {
      return "vertical";
    }
    const targetRightThreshold = left + (right - left) * clamp4(Number(rightTargetRatio) || 0.55, 0.5, 0.9);
    return x >= targetRightThreshold ? "inline-right" : "vertical";
  }
  if (horizontalRoom && x < left + (right - left) / 2) {
    return "inline-left";
  }
  const rightThreshold = surfaceLeft + laneWidth * clamp4(Number(rightIntentRatio) || 0.5, 0.4, 0.92);
  return horizontalRoom && x >= rightThreshold ? "inline-right" : "vertical";
}
function resolveVerticalMarkdownDropTarget({
  clientX,
  clientY,
  laneRect,
  candidates = [],
  laneMargin = 48,
  minimumEdgeDistance = 96
} = {}) {
  const x = Number(clientX);
  const y = Number(clientY);
  const laneLeft = Number(laneRect?.left);
  const laneRight = Number(laneRect?.right);
  if (![x, y, laneLeft, laneRight].every(Number.isFinite) || laneRight <= laneLeft) {
    return null;
  }
  const entries = candidates.filter((candidate) => {
    const rect = candidate?.rect;
    return candidate?.element && [rect?.left, rect?.right, rect?.top, rect?.bottom].every((value) => Number.isFinite(Number(value))) && Number(rect.right) > Number(rect.left) && Number(rect.bottom) > Number(rect.top);
  }).sort((a, b) => a.rect.top - b.rect.top || a.rect.left - b.rect.left);
  if (!entries.length || x < laneLeft - laneMargin || x > laneRight + laneMargin) {
    return null;
  }
  const first = entries[0];
  const last = entries[entries.length - 1];
  const firstHeight = first.rect.height || first.rect.bottom - first.rect.top;
  const lastHeight = last.rect.height || last.rect.bottom - last.rect.top;
  const topDistance = Math.max(minimumEdgeDistance, firstHeight * 1.5);
  const bottomDistance = Math.max(minimumEdgeDistance, lastHeight * 1.5);
  if (y <= first.rect.top && first.rect.top - y <= topDistance) {
    return { element: first.element, side: "before" };
  }
  if (y >= last.rect.bottom && y - last.rect.bottom <= bottomDistance) {
    return { element: last.element, side: "after" };
  }
  for (let index = 1; index < entries.length; index += 1) {
    const previous = entries[index - 1];
    const next = entries[index];
    if (y < previous.rect.bottom || y > next.rect.top) {
      continue;
    }
    const boundary = (previous.rect.bottom + next.rect.top) / 2;
    return y <= boundary ? { element: previous.element, side: "after" } : { element: next.element, side: "before" };
  }
  return null;
}
function normalizeMarkdownFloatBox(value) {
  if (!value || !Number.isFinite(Number(value.x)) || !Number.isFinite(Number(value.y))) {
    return null;
  }
  const width = clamp4(Number(value.width) || 0.5, 0.08, 1);
  const height = clamp4(Number(value.height) || 0.1, 0.02, 1);
  return {
    x: clamp4(Number(value.x), 0, 1),
    y: clamp4(Number(value.y), 0, 1),
    width,
    height
  };
}

// src/markdown-section-anchors.mjs
function finite3(value, fallback = 0) {
  const number = Number(value);
  return Number.isFinite(number) ? number : fallback;
}
function buildVirtualMarkdownSectionAnchors(sections, {
  baseTop = 0,
  left = 0,
  right = 1,
  path = "",
  confidence = 0.96
} = {}) {
  const normalized = (Array.isArray(sections) ? sections : []).map((section) => ({
    start: Number(section?.startLine),
    end: Number(section?.endLine),
    height: Math.max(1, finite3(section?.height, 1)),
    measuredTop: Number.isFinite(Number(section?.measuredTop)) ? Number(section.measuredTop) : null,
    excluded: Boolean(section?.excluded)
  }));
  let precedingHeight = 0;
  let inferredBase = finite3(baseTop, 0);
  for (const section of normalized) {
    if (section.measuredTop !== null) {
      inferredBase = finite3(baseTop, 0) + section.measuredTop - precedingHeight;
      break;
    }
    precedingHeight += section.height;
  }
  const anchors = [];
  let top = inferredBase;
  for (const section of normalized) {
    const sectionTop = section.measuredTop === null ? top : finite3(baseTop, 0) + section.measuredTop;
    if (!section.excluded && Number.isInteger(section.start) && Number.isInteger(section.end) && section.start >= 0 && section.end >= 0) {
      anchors.push({
        path,
        start: Math.min(section.start, section.end),
        end: Math.max(section.start, section.end),
        left,
        right,
        top: sectionTop,
        bottom: sectionTop + section.height,
        height: section.height,
        area: Math.max(1, right - left) * section.height,
        confidence,
        virtual: true
      });
    }
    top = sectionTop + section.height;
  }
  return anchors;
}

// src/selection-draw-gesture.mjs
var SELECTED_DRAW_GESTURE_DRAW_OR_DESELECT = "draw-or-deselect";
var SELECTED_DRAW_GESTURE_MANIPULATE = "manipulate";
function fitSelectionFrameToCanvas(frame, {
  canvasWidth,
  canvasHeight,
  inset = 0
} = {}) {
  if (!frame) {
    return null;
  }
  const width = Math.max(0, Number(canvasWidth) || 0);
  const height = Math.max(0, Number(canvasHeight) || 0);
  const x1 = Math.min(Number(frame.x) || 0, (Number(frame.x) || 0) + (Number(frame.width) || 0));
  const x2 = Math.max(Number(frame.x) || 0, (Number(frame.x) || 0) + (Number(frame.width) || 0));
  const y1 = Math.min(Number(frame.y) || 0, (Number(frame.y) || 0) + (Number(frame.height) || 0));
  const y2 = Math.max(Number(frame.y) || 0, (Number(frame.y) || 0) + (Number(frame.height) || 0));
  if (!width || !height) {
    return { x: x1, y: y1, width: x2 - x1, height: y2 - y1 };
  }
  const safeInset = Math.max(0, Math.min(Number(inset) || 0, width / 2, height / 2));
  const minX = safeInset;
  const maxX = Math.max(minX, width - safeInset);
  const minY = safeInset;
  const maxY = Math.max(minY, height - safeInset);
  const clampTo = (value, min, max) => Math.min(max, Math.max(min, value));
  const left = clampTo(x1, minX, maxX);
  const right = clampTo(x2, minX, maxX);
  const top = clampTo(y1, minY, maxY);
  const bottom = clampTo(y2, minY, maxY);
  return {
    x: left,
    y: top,
    width: Math.max(0, right - left),
    height: Math.max(0, bottom - top)
  };
}
function resolveSelectedDrawGesture({
  toolMode,
  hasSelection,
  hitStrokeIndex = -1,
  insideSelectionFrame = false
} = {}) {
  if (toolMode !== "draw" || !hasSelection) {
    return null;
  }
  if (hitStrokeIndex >= 0 || insideSelectionFrame) {
    return SELECTED_DRAW_GESTURE_MANIPULATE;
  }
  return SELECTED_DRAW_GESTURE_DRAW_OR_DESELECT;
}

// src/text-layout.mjs
function finite4(value, fallback = 0) {
  const number = Number(value);
  return Number.isFinite(number) ? number : fallback;
}
function clamp5(value, min, max) {
  return Math.min(max, Math.max(min, value));
}
function placeFloatingTextEditor({
  anchorX,
  anchorY,
  width,
  height,
  viewportWidth,
  viewportHeight,
  viewportOffsetLeft = 0,
  viewportOffsetTop = 0,
  contentInsetX = 0,
  contentInsetY = 0,
  margin = 8,
  anchorVisible = true
} = {}) {
  const leftEdge = finite4(viewportOffsetLeft) + Math.max(0, finite4(margin, 8));
  const topEdge = finite4(viewportOffsetTop) + Math.max(0, finite4(margin, 8));
  const rightEdge = finite4(viewportOffsetLeft) + Math.max(1, finite4(viewportWidth, 1)) - Math.max(0, finite4(margin, 8));
  const bottomEdge = finite4(viewportOffsetTop) + Math.max(1, finite4(viewportHeight, 1)) - Math.max(0, finite4(margin, 8));
  const editorWidth = Math.min(Math.max(1, finite4(width, 1)), Math.max(1, rightEdge - leftEdge));
  const editorHeight = Math.min(Math.max(1, finite4(height, 1)), Math.max(1, bottomEdge - topEdge));
  const centered = !anchorVisible;
  const desiredLeft = centered ? leftEdge + (rightEdge - leftEdge - editorWidth) / 2 : finite4(anchorX) - Math.max(0, finite4(contentInsetX));
  const desiredTop = centered ? topEdge + (bottomEdge - topEdge - editorHeight) / 2 : finite4(anchorY) - Math.max(0, finite4(contentInsetY));
  return {
    left: clamp5(desiredLeft, leftEdge, Math.max(leftEdge, rightEdge - editorWidth)),
    top: clamp5(desiredTop, topEdge, Math.max(topEdge, bottomEdge - editorHeight)),
    width: editorWidth,
    height: editorHeight,
    centered
  };
}
function splitOverlongToken(token, maxWidth, measureText) {
  const chunks = [];
  let current = "";
  for (const character of Array.from(token)) {
    const candidate = current + character;
    if (current && measureText(candidate) > maxWidth) {
      chunks.push(current);
      current = character;
    } else {
      current = candidate;
    }
  }
  if (current || !chunks.length) {
    chunks.push(current);
  }
  return chunks;
}
function wrapTextLines(text, maxWidth, measureText) {
  const width = Math.max(1, finite4(maxWidth, 1));
  const measure = typeof measureText === "function" ? measureText : (value) => String(value || "").length;
  const output = [];
  for (const paragraph of String(text ?? "").replace(/\r\n?/g, "\n").split("\n")) {
    if (!paragraph) {
      output.push("");
      continue;
    }
    if (measure(paragraph) <= width) {
      output.push(paragraph);
      continue;
    }
    const tokens = paragraph.match(/\s+|[^\s]+/gu) || [paragraph];
    let line = "";
    for (const token of tokens) {
      const candidate = line + token;
      if (line && measure(candidate) > width) {
        output.push(line.trimEnd());
        line = token.trimStart();
      } else {
        line = candidate;
      }
      if (measure(line) <= width) {
        continue;
      }
      const chunks = splitOverlongToken(line, width, measure);
      output.push(...chunks.slice(0, -1));
      line = chunks[chunks.length - 1];
    }
    output.push(line.trimEnd());
  }
  return output.length ? output : [""];
}
function computeTextLayout({
  text,
  fontSize = 18,
  textWidth = null,
  maxWidth = Infinity,
  padded = false,
  measureText
} = {}) {
  const size = clamp5(finite4(fontSize, 18), 10, 72);
  const measure = typeof measureText === "function" ? (value) => Math.max(0, finite4(measureText(String(value ?? "")), 0)) : (value) => Array.from(String(value ?? "")).length * size * 0.62;
  const paragraphs = String(text ?? "").replace(/\r\n?/g, "\n").split("\n");
  const naturalWidth = Math.max(size, ...paragraphs.map((line) => measure(line)));
  const availableWidth = Math.max(size, finite4(maxWidth, naturalWidth));
  const requested = Number(textWidth);
  const hasRequestedWidth = Number.isFinite(requested) && requested > 0;
  const wrapWidth = clamp5(hasRequestedWidth ? requested : naturalWidth, size, availableWidth);
  const lines = wrapTextLines(text, wrapWidth, measure);
  const measuredWidth = Math.max(size, ...lines.map((line) => measure(line)));
  const contentWidth = hasRequestedWidth ? wrapWidth : Math.min(wrapWidth, measuredWidth);
  const lineHeight = Math.max(size + 2, size * 1.28);
  const contentHeight = Math.max(lineHeight, lines.length * lineHeight);
  const paddingX = padded ? Math.max(8, size * 0.45) : 0;
  const paddingY = padded ? Math.max(4, size * 0.26) : 0;
  return {
    lines,
    fontSize: size,
    lineHeight,
    contentWidth,
    contentHeight,
    paddingX,
    paddingY,
    width: contentWidth + paddingX * 2,
    height: contentHeight + paddingY * 2
  };
}

// src/viewport-gesture.mjs
function finite5(value, fallback = 0) {
  const number = Number(value);
  return Number.isFinite(number) ? number : fallback;
}
function clamp6(value, min, max) {
  return Math.min(Math.max(value, min), max);
}
function calculatePinchPanScroll({
  scrollLeft = 0,
  scrollTop = 0,
  previousCenter = null,
  nextCenter = null,
  zoomRatio = 1,
  originX = 0,
  originY = 0,
  movementEpsilon = 0.35,
  maxScrollLeft = Number.POSITIVE_INFINITY,
  maxScrollTop = Number.POSITIVE_INFINITY
} = {}) {
  const previous = previousCenter || nextCenter || { x: 0, y: 0 };
  const next = nextCenter || previousCenter || { x: 0, y: 0 };
  const ratio = Math.max(0.01, finite5(zoomRatio, 1));
  const epsilon = Math.max(0, finite5(movementEpsilon, 0.35));
  const previousX = finite5(previous.x);
  const previousY = finite5(previous.y);
  const nextX = Math.abs(finite5(next.x) - previousX) < epsilon ? previousX : finite5(next.x);
  const nextY = Math.abs(finite5(next.y) - previousY) < epsilon ? previousY : finite5(next.y);
  const stableOriginX = finite5(originX);
  const stableOriginY = finite5(originY);
  const maxLeft = Math.max(0, finite5(maxScrollLeft, Number.MAX_SAFE_INTEGER));
  const maxTop = Math.max(0, finite5(maxScrollTop, Number.MAX_SAFE_INTEGER));
  return {
    left: clamp6((finite5(scrollLeft) + previousX) * ratio + (1 - ratio) * stableOriginX - nextX, 0, maxLeft),
    top: clamp6((finite5(scrollTop) + previousY) * ratio + (1 - ratio) * stableOriginY - nextY, 0, maxTop)
  };
}
function resolveMultiTouchGestureMode({
  mode = "undecided",
  initialDistance = 0,
  distance = 0,
  minimumDistanceChange = 8,
  minimumScaleChange = 0.04,
  centerMovement = 0,
  minimumPinchDominance = 1.25
} = {}) {
  if (mode === "pinch") {
    return "pinch";
  }
  const initial = Math.max(0, finite5(initialDistance));
  const current = Math.max(0, finite5(distance));
  if (!(initial > 0) || !(current > 0)) {
    return "pan";
  }
  const threshold = Math.max(
    Math.max(0, finite5(minimumDistanceChange, 8)),
    initial * Math.max(0, finite5(minimumScaleChange, 0.04))
  );
  const distanceChange = Math.abs(current - initial);
  if (distanceChange < threshold) {
    return "pan";
  }
  const translation = Math.max(0, finite5(centerMovement));
  const dominance = Math.max(1, finite5(minimumPinchDominance, 1.25));
  return translation > 0 && translation >= distanceChange * dominance ? "pan" : "pinch";
}
function calculateReadingZoomMargin(baseMargin, targetHeight, zoom) {
  const base = Number.isFinite(Number(baseMargin)) ? Number(baseMargin) : 0;
  const height = Math.max(1, Number.isFinite(Number(targetHeight)) ? Number(targetHeight) : 1);
  const scale = Math.max(0.01, Number.isFinite(Number(zoom)) ? Number(zoom) : 1);
  return scale < 1 ? base - height * (1 - scale) : base;
}
function calculateVisualZoomLogicalWindow({
  scrollTop = 0,
  viewportHeight = 0,
  zoom = 1,
  origin = 0
} = {}) {
  const scale = Math.max(0.01, finite5(zoom, 1));
  const visualTop = Math.max(0, finite5(scrollTop));
  const logicalTop = Math.max(0, (visualTop - finite5(origin) * (scale - 1)) / scale);
  const logicalHeight = Math.max(0, finite5(viewportHeight)) / scale;
  return {
    top: logicalTop,
    bottom: logicalTop + logicalHeight,
    height: logicalHeight
  };
}

// src/reading-layout.mjs
function finite6(value, fallback = 0) {
  const number = Number(value);
  return Number.isFinite(number) ? number : fallback;
}
function captureInitialReadingLayout(previewEl, sizer, renderer = null) {
  const previewRect = previewEl?.getBoundingClientRect?.();
  const sizerRect = sizer?.getBoundingClientRect?.();
  const sections = Array.isArray(renderer?.sections) ? renderer.sections : [];
  return {
    previewWidth: Math.max(0, finite6(previewEl?.clientWidth, previewRect?.width)),
    sizerHeight: Math.max(
      0,
      finite6(sizer?.scrollHeight),
      finite6(sizer?.offsetHeight),
      finite6(sizerRect?.height)
    ),
    sectionCount: sections.length,
    renderedSectionCount: sections.filter((section) => section?.shown !== false && section?.el?.isConnected).length,
    measuredSectionCount: sections.filter((section) => finite6(section?.height) > 0).length
  };
}
function initialReadingLayoutSignature(metrics = {}) {
  return [
    Math.round(finite6(metrics.previewWidth)),
    Math.round(finite6(metrics.sizerHeight)),
    Math.max(0, Math.round(finite6(metrics.sectionCount))),
    Math.max(0, Math.round(finite6(metrics.renderedSectionCount))),
    Math.max(0, Math.round(finite6(metrics.measuredSectionCount)))
  ].join(":");
}
function isInitialReadingLayoutUsable(metrics = {}) {
  const sectionCount = Math.max(0, Math.round(finite6(metrics.sectionCount)));
  return finite6(metrics.previewWidth) > 8 && finite6(metrics.sizerHeight) > 0 && (sectionCount === 0 || finite6(metrics.renderedSectionCount) > 0);
}
async function waitForStableReadingLayout(readMetrics, {
  requestFrame,
  shouldAbort = () => false,
  stableFrames = 3,
  maxFrames = 24
} = {}) {
  if (typeof readMetrics !== "function" || typeof requestFrame !== "function") {
    return false;
  }
  const requiredStableFrames = Math.max(1, Math.round(finite6(stableFrames, 3)));
  const frameLimit = Math.max(requiredStableFrames, Math.round(finite6(maxFrames, 24)));
  let previousSignature = "";
  let stableCount = 0;
  let lastUsable = false;
  for (let frame = 0; frame < frameLimit; frame += 1) {
    await requestFrame();
    if (shouldAbort()) {
      return false;
    }
    const metrics = readMetrics();
    lastUsable = isInitialReadingLayoutUsable(metrics);
    if (!lastUsable) {
      previousSignature = "";
      stableCount = 0;
      continue;
    }
    const signature = initialReadingLayoutSignature(metrics);
    stableCount = signature === previousSignature ? stableCount + 1 : 1;
    previousSignature = signature;
    if (stableCount >= requiredStableFrames) {
      return true;
    }
  }
  return lastUsable;
}

// src/reading-touch-guard.mjs
function finite7(value, fallback = 0) {
  const number = Number(value);
  return Number.isFinite(number) ? number : fallback;
}
function createReadingTouchGuardState() {
  return {
    active: null,
    lastTouchAt: 0,
    suppressUntil: 0
  };
}
function beginReadingTouch(state, { id, x, y, now } = {}) {
  state.active = {
    id,
    x: finite7(x),
    y: finite7(y),
    moved: false
  };
  state.lastTouchAt = finite7(now);
}
function moveReadingTouch(state, { id, x, y, now } = {}, threshold = 10) {
  const active = state?.active;
  if (!active || active.id !== id) {
    return false;
  }
  const distance = Math.hypot(finite7(x) - active.x, finite7(y) - active.y);
  if (distance >= Math.max(1, finite7(threshold, 10))) {
    active.moved = true;
  }
  state.lastTouchAt = finite7(now);
  return active.moved;
}
function finishReadingTouch(state, { id, now } = {}, suppressMs = 380) {
  const active = state?.active;
  if (!active || active.id !== id) {
    return false;
  }
  const moved = Boolean(active.moved);
  const timestamp = finite7(now);
  state.active = null;
  state.lastTouchAt = timestamp;
  if (moved) {
    state.suppressUntil = Math.max(state.suppressUntil, timestamp + Math.max(0, finite7(suppressMs, 380)));
  }
  return moved;
}
function recordReadingTouchScroll(state, { now } = {}, {
  recentTouchMs = 900,
  suppressMs = 380
} = {}) {
  const timestamp = finite7(now);
  const touchIsRecent = Boolean(state?.active) || state?.lastTouchAt > 0 && timestamp - state.lastTouchAt <= Math.max(0, finite7(recentTouchMs, 900));
  if (!touchIsRecent) {
    return false;
  }
  if (state.active) {
    state.active.moved = true;
  }
  state.lastTouchAt = timestamp;
  state.suppressUntil = Math.max(state.suppressUntil, timestamp + Math.max(0, finite7(suppressMs, 380)));
  return true;
}
function shouldSuppressReadingClick(state, now) {
  return finite7(now) < finite7(state?.suppressUntil);
}

// src/note-flow-layout.mjs
function finite8(value, fallback = 0) {
  const number = Number(value);
  return Number.isFinite(number) ? number : fallback;
}
function clamp7(value, min, max) {
  return Math.min(Math.max(value, min), max);
}
function normalizeBounds(bounds) {
  const minY = finite8(bounds?.minY, 0);
  const maxY = Math.max(minY, finite8(bounds?.maxY, minY));
  return { ...bounds, minY, maxY };
}
function stabilizeNoteFlowBounds({
  bounds,
  layout,
  contentWidth,
  viewportHeight,
  preferCurrent = false
} = {}) {
  const current = normalizeBounds(bounds);
  const box = layout?.box;
  const sourceFrame = layout?.sourceFrame;
  const sourceContentWidth = finite8(sourceFrame?.contentWidth, 0);
  const boxY = finite8(box?.y, Number.NaN);
  const boxHeight = finite8(box?.height, Number.NaN);
  if (!(sourceContentWidth > 0) || !Number.isFinite(boxY) || !(boxHeight >= 0)) {
    return {
      bounds: current,
      runaway: false,
      referenceHeight: Math.max(1, finite8(viewportHeight, 1))
    };
  }
  const widthScale = clamp7(finite8(contentWidth, sourceContentWidth) / sourceContentWidth, 0.2, 5);
  const sameContentLane = widthScale >= 0.82 && widthScale <= 1.2;
  const positionScale = sameContentLane ? 1 : clamp7(1 / widthScale, 0.48, 2.2);
  const heightScale = sameContentLane ? widthScale : clamp7(Math.max(positionScale, widthScale), 0.42, 2.8);
  const stableMinY = Math.max(0, boxY * positionScale);
  const stableMaxY = Math.max(stableMinY, stableMinY + boxHeight * heightScale);
  const sourceDocumentHeight = Math.max(1, finite8(sourceFrame?.documentHeight, 1) * positionScale);
  const referenceHeight = Math.max(1, finite8(viewportHeight, 1), sourceDocumentHeight, stableMaxY);
  const viewport = Math.max(1, finite8(viewportHeight, 1));
  const runawayThreshold = Math.max(viewport * 4, stableMaxY * 4, stableMaxY + viewport * 2);
  const runaway = current.minY > runawayThreshold || current.maxY > runawayThreshold;
  return {
    bounds: preferCurrent && !runaway ? current : { ...current, minY: stableMinY, maxY: stableMaxY },
    runaway,
    referenceHeight
  };
}
function noteFlowSurfaceRepairLimits(referenceHeight, viewportHeight) {
  const viewport = Math.max(1, finite8(viewportHeight, 1));
  const stableHeight = Math.ceil(clamp7(Math.max(viewport, finite8(referenceHeight, viewport) * 1.5), viewport, 2e5));
  return {
    stableHeight,
    runawayThreshold: Math.max(8e3, stableHeight * 4)
  };
}
function selectNoteFlowInsertionPlacement(candidates, {
  strokeTop,
  strokeBottom = strokeTop,
  tolerance = 4
} = {}) {
  const top = finite8(strokeTop, Number.NaN);
  const bottom = Math.max(top, finite8(strokeBottom, top));
  if (!Number.isFinite(top) || !Number.isFinite(bottom)) {
    return null;
  }
  const ordered = (Array.isArray(candidates) ? candidates : []).filter((candidate) => {
    return candidate && Number.isFinite(Number(candidate.top)) && Number.isFinite(Number(candidate.bottom)) && Number.isFinite(Number(candidate.start));
  }).map((candidate, index) => ({
    ...candidate,
    top: finite8(candidate.top),
    bottom: Math.max(finite8(candidate.top), finite8(candidate.bottom)),
    start: finite8(candidate.start),
    end: Math.max(finite8(candidate.start), finite8(candidate.end, candidate.start)),
    order: Number.isFinite(Number(candidate.order)) ? Number(candidate.order) : index
  })).sort((a, b) => a.top - b.top || a.bottom - b.bottom || a.order - b.order);
  if (!ordered.length) {
    return null;
  }
  const threshold = Math.max(0, finite8(tolerance, 4));
  const preciseOverlap = ordered.filter((candidate) => {
    const precise = Boolean(candidate.lineSpacer) || candidate.start === candidate.end || Math.abs(candidate.top - top) <= threshold;
    return precise && candidate.bottom >= top - threshold && candidate.top <= bottom + threshold;
  }).sort((a, b) => {
    const aLine = a.lineSpacer || a.start === a.end ? 0 : 1;
    const bLine = b.lineSpacer || b.start === b.end ? 0 : 1;
    return aLine - bLine || a.top - b.top || a.bottom - a.top - (b.bottom - b.top) || a.order - b.order;
  })[0];
  if (preciseOverlap) {
    return { candidate: preciseOverlap, side: "before", line: preciseOverlap.start };
  }
  const below = ordered.find((candidate) => candidate.top >= bottom - threshold);
  if (below) {
    return { candidate: below, side: "before", line: below.start };
  }
  const last = ordered[ordered.length - 1];
  return { candidate: last, side: "after", line: last.end };
}
function createNoteFlowDropIndex(candidates) {
  const ordered = (Array.isArray(candidates) ? candidates : []).filter((candidate) => {
    return candidate && Number.isFinite(Number(candidate.top)) && Number.isFinite(Number(candidate.bottom)) && Number.isFinite(Number(candidate.start));
  }).map((candidate, index) => ({
    ...candidate,
    top: finite8(candidate.top),
    bottom: Math.max(finite8(candidate.top), finite8(candidate.bottom)),
    start: finite8(candidate.start),
    end: Math.max(finite8(candidate.start), finite8(candidate.end, candidate.start)),
    order: Number.isFinite(Number(candidate.order)) ? Number(candidate.order) : index
  }));
  return ordered.flatMap((candidate) => {
    const height = candidate.bottom - candidate.top;
    const precise = candidate.lineSpacer || candidate.start === candidate.end ? 0 : 1;
    return [
      {
        candidate,
        side: "before",
        line: candidate.start,
        boundary: candidate.top,
        precise,
        height
      },
      {
        candidate,
        side: "after",
        line: candidate.end,
        boundary: candidate.bottom,
        precise,
        height
      }
    ];
  });
}
function selectNoteFlowDropPlacementFromIndex(index, { dropY } = {}) {
  const y = finite8(dropY, Number.NaN);
  if (!Number.isFinite(y)) {
    return null;
  }
  let placement = null;
  let placementDistance = Number.POSITIVE_INFINITY;
  for (const boundary of Array.isArray(index) ? index : []) {
    const distance = Math.abs(y - boundary.boundary);
    const better = distance < placementDistance || distance === placementDistance && (!placement || boundary.precise < placement.precise || boundary.precise === placement.precise && boundary.height < placement.height || boundary.precise === placement.precise && boundary.height === placement.height && boundary.candidate.order < placement.candidate.order || boundary.precise === placement.precise && boundary.height === placement.height && boundary.candidate.order === placement.candidate.order && boundary.side === "before" && placement.side !== "before");
    if (better) {
      placement = boundary;
      placementDistance = distance;
    }
  }
  return placement ? {
    candidate: placement.candidate,
    side: placement.side,
    line: placement.line,
    boundary: placement.boundary,
    flowOrder: Number.isFinite(Number(placement.flowOrder)) ? Number(placement.flowOrder) : null,
    noteFlowBoundary: placement.noteFlowBoundary === true
  } : null;
}
function selectNoteFlowPositionAnchor(candidates, {
  strokeTop,
  tolerance = 4,
  maxOrderExclusive = Number.POSITIVE_INFINITY
} = {}) {
  const top = finite8(strokeTop, Number.NaN);
  if (!Number.isFinite(top)) {
    return null;
  }
  const orderLimit = Number.isFinite(Number(maxOrderExclusive)) ? Number(maxOrderExclusive) : Number.POSITIVE_INFINITY;
  const above = (Array.isArray(candidates) ? candidates : []).filter((candidate) => {
    return candidate && Number.isFinite(Number(candidate.bottom)) && Number.isFinite(Number(candidate.end));
  }).map((candidate, index) => ({
    ...candidate,
    bottom: finite8(candidate.bottom),
    end: finite8(candidate.end),
    order: Number.isFinite(Number(candidate.order)) ? Number(candidate.order) : index
  })).filter((candidate) => {
    return candidate.bottom <= top + finite8(tolerance, 4) && candidate.order < orderLimit;
  }).sort((a, b) => b.bottom - a.bottom || b.end - a.end || b.order - a.order);
  if (!above.length) {
    return null;
  }
  return { candidate: above[0], line: above[0].end };
}
function selectExactNoteFlowPositionAnchor(candidates, {
  candidate,
  side,
  line
} = {}) {
  if (!candidate || !["before", "after"].includes(side)) {
    return null;
  }
  if (side === "after") {
    const targetLine = Number.isFinite(Number(line)) ? Number(line) : Number(candidate.end);
    return Number.isFinite(targetLine) ? { candidate, line: targetLine } : null;
  }
  const ordered = Array.isArray(candidates) ? candidates : [];
  const candidateOrder = Number.isFinite(Number(candidate.order)) ? Number(candidate.order) : ordered.indexOf(candidate);
  return selectNoteFlowPositionAnchor(ordered, {
    strokeTop: finite8(candidate.top, Number.NaN) - 1e-3,
    maxOrderExclusive: candidateOrder >= 0 ? candidateOrder : Number.POSITIVE_INFINITY
  });
}
function selectStoredNoteFlowAnchorCandidate(candidates, {
  side,
  strokeTop,
  tolerance = 4
} = {}) {
  const ordered = (Array.isArray(candidates) ? candidates : []).filter((candidate) => {
    return candidate && Number.isFinite(Number(candidate.top)) && Number.isFinite(Number(candidate.bottom));
  }).map((candidate, order) => ({
    ...candidate,
    top: finite8(candidate.top),
    bottom: Math.max(finite8(candidate.top), finite8(candidate.bottom)),
    order: Number.isFinite(Number(candidate.order)) ? Number(candidate.order) : order
  }));
  if (ordered.length <= 1 || !Number.isFinite(Number(strokeTop))) {
    return ordered.sort((a, b) => a.bottom - a.top - (b.bottom - b.top) || a.order - b.order)[0] || null;
  }
  const top = finite8(strokeTop);
  const threshold = Math.max(0, finite8(tolerance, 4));
  if (side === "after") {
    const above = ordered.filter((candidate) => candidate.bottom <= top + threshold).sort((a, b) => b.bottom - a.bottom || a.order - b.order);
    if (above.length) {
      return above[0];
    }
  } else {
    const below = ordered.filter((candidate) => candidate.top >= top - threshold).sort((a, b) => a.top - b.top || a.bottom - a.top - (b.bottom - b.top) || a.order - b.order);
    if (below.length) {
      return below[0];
    }
  }
  return ordered.sort((a, b) => {
    const aDistance = Math.min(Math.abs(a.top - top), Math.abs(a.bottom - top));
    const bDistance = Math.min(Math.abs(b.top - top), Math.abs(b.bottom - top));
    return aDistance - bDistance || a.order - b.order;
  })[0] || null;
}
function selectNoteFlowAvoidanceCandidate(candidates, {
  strokeTop,
  strokeBottom,
  tolerance = 4
} = {}) {
  const top = finite8(strokeTop, Number.NaN);
  const bottom = finite8(strokeBottom, Number.NaN);
  if (!Number.isFinite(top) || !Number.isFinite(bottom)) {
    return null;
  }
  const threshold = Math.max(0, finite8(tolerance, 4));
  const ordered = (Array.isArray(candidates) ? candidates : []).filter((candidate) => {
    return candidate && Number.isFinite(Number(candidate.top)) && Number.isFinite(Number(candidate.bottom));
  }).map((candidate, order) => ({
    ...candidate,
    top: finite8(candidate.top),
    bottom: Math.max(finite8(candidate.top), finite8(candidate.bottom)),
    order: Number.isFinite(Number(candidate.order)) ? Number(candidate.order) : order
  }));
  const preciseOverlap = ordered.filter((candidate) => {
    const start = Number(candidate?.start);
    const end = Number(candidate?.end);
    const precise = Boolean(candidate?.lineSpacer) || Number.isFinite(start) && Number.isFinite(end) && start === end || Math.abs(Number(candidate?.top) - top) <= threshold;
    return candidate && precise && Number.isFinite(Number(candidate.top)) && Number.isFinite(Number(candidate.bottom)) && Number(candidate.bottom) >= top + threshold && Number(candidate.top) <= bottom - threshold;
  }).sort((a, b) => {
    const aLine = a.lineSpacer || Number(a.start) === Number(a.end) ? 0 : 1;
    const bLine = b.lineSpacer || Number(b.start) === Number(b.end) ? 0 : 1;
    return aLine - bLine || a.top - b.top || a.bottom - a.top - (b.bottom - b.top) || a.order - b.order;
  })[0] || null;
  if (preciseOverlap) {
    return preciseOverlap;
  }
  const fullyCoveredBlock = ordered.filter((candidate) => {
    const multiLine = Number(candidate.start) !== Number(candidate.end) && !candidate.lineSpacer;
    return multiLine && top <= candidate.top + threshold && bottom >= candidate.bottom - threshold;
  }).sort((a, b) => {
    return a.bottom - a.top - (b.bottom - b.top) || a.order - b.order;
  })[0];
  return fullyCoveredBlock || null;
}
function pointBounds(points, canvasWidth, canvasHeight) {
  const width = Math.max(1, finite8(canvasWidth, 1));
  const height = Math.max(1, finite8(canvasHeight, 1));
  const coordinates = (Array.isArray(points) ? points : []).map((point) => ({
    x: finite8(point?.x, Number.NaN) * width,
    y: finite8(point?.y, Number.NaN) * height
  })).filter((point) => Number.isFinite(point.x) && Number.isFinite(point.y));
  if (!coordinates.length) {
    return null;
  }
  return {
    minX: Math.min(...coordinates.map((point) => point.x)),
    minY: Math.min(...coordinates.map((point) => point.y))
  };
}
function stabilizeNoteFlowPointProjection(previousPoints, projectedPoints, {
  canvasWidth,
  canvasHeight,
  threshold = 0.65
} = {}) {
  const width = Math.max(1, finite8(canvasWidth, 1));
  const height = Math.max(1, finite8(canvasHeight, 1));
  const previous = pointBounds(previousPoints, width, height);
  const projected = pointBounds(projectedPoints, width, height);
  if (!previous || !projected) {
    return Array.isArray(projectedPoints) ? projectedPoints.map((point) => ({ ...point })) : [];
  }
  const limit = Math.max(0, finite8(threshold, 0.65));
  const deltaX = Math.abs(projected.minX - previous.minX) < limit ? previous.minX - projected.minX : 0;
  const deltaY = Math.abs(projected.minY - previous.minY) < limit ? previous.minY - projected.minY : 0;
  if (!deltaX && !deltaY) {
    return projectedPoints;
  }
  return projectedPoints.map((point) => ({
    ...point,
    x: clamp7(finite8(point?.x, 0) + deltaX / width, 0, 1),
    y: clamp7(finite8(point?.y, 0) + deltaY / height, 0, 1)
  }));
}
function projectNoteFlowDocumentPoint(sourcePoint, projectedPoint, {
  canvasHeight
} = {}) {
  const absoluteY = Number(sourcePoint?.anchor?.offsetY);
  if (!Number.isFinite(absoluteY)) {
    return projectedPoint;
  }
  return {
    ...projectedPoint,
    y: clamp7(absoluteY / Math.max(1, finite8(canvasHeight, 1)), 0, 1)
  };
}
function preserveAbsoluteNoteFlowPoints(points, {
  previousWidth,
  previousHeight,
  nextWidth,
  nextHeight
} = {}) {
  const oldWidth = Math.max(1, finite8(previousWidth, 1));
  const oldHeight = Math.max(1, finite8(previousHeight, 1));
  const width = Math.max(1, finite8(nextWidth, oldWidth));
  const height = Math.max(1, finite8(nextHeight, oldHeight));
  return (Array.isArray(points) ? points : []).map((point) => ({
    ...point,
    x: clamp7(finite8(point?.x, 0) * oldWidth / width, 0, 1),
    y: clamp7(finite8(point?.y, 0) * oldHeight / height, 0, 1)
  }));
}
function hasStableNoteFlowAnchor(noteFlow) {
  const line = noteFlow?.line;
  return line !== null && line !== void 0 && line !== "" && Number.isFinite(Number(line)) && ["before", "after"].includes(noteFlow?.side) && Boolean(noteFlow?.positionBasis) && Number(noteFlow?.positionVersion) >= 1;
}
function normalizeNoteFlowPath(path) {
  return String(path || "").replace(/\\/g, "/").replace(/^\/+|\/+$/g, "");
}
function noteFlowBlockKey(noteFlow, fallbackPath = "") {
  const path = normalizeNoteFlowPath(noteFlow?.path || fallbackPath);
  const line = Number(noteFlow?.line);
  const startValue = Number(noteFlow?.blockStart);
  const endValue = Number(noteFlow?.blockEnd);
  const start = Number.isFinite(startValue) && startValue >= 0 ? Math.floor(startValue) : Number.isFinite(line) && line >= 0 ? Math.floor(line) : null;
  const end = Number.isFinite(endValue) && endValue >= 0 ? Math.floor(endValue) : start;
  if (!path || start === null || end === null) {
    return "";
  }
  return `${path}\0${Math.min(start, end)}\0${Math.max(start, end)}`;
}
function noteFlowPlacementRowKey(noteFlow, fallbackPath = "") {
  const side = noteFlow?.side;
  const blockKey = noteFlowBlockKey(noteFlow, fallbackPath);
  const placementMode = noteFlow?.placementMode === "inline" ? "inline" : "row";
  return blockKey && ["before", "after"].includes(side) ? `${blockKey}\0${side}${placementMode === "inline" ? "\0inline" : ""}` : "";
}
function noteFlowPlacementGapKey(noteFlow, fallbackPath = "") {
  const side = noteFlow?.side;
  const blockKey = noteFlowBlockKey(noteFlow, fallbackPath);
  return blockKey && ["before", "after"].includes(side) ? `${blockKey}\0${side}` : "";
}
function canonicalNoteFlowGapPlacement(candidates, { anchor, side } = {}) {
  if (!anchor || !["before", "after"].includes(side)) {
    return null;
  }
  const path = normalizeNoteFlowPath(anchor.path || "");
  const blockKey = anchor.blockKey || noteFlowBlockKey(anchor, path);
  const ordered = [];
  const seen = /* @__PURE__ */ new Set();
  for (const candidate of (Array.isArray(candidates) ? candidates : []).filter((item) => normalizeNoteFlowPath(item?.path || "") === path).sort((a, b) => finite8(a?.order, Number.POSITIVE_INFINITY) - finite8(b?.order, Number.POSITIVE_INFINITY) || finite8(a?.top) - finite8(b?.top))) {
    const key = candidate.blockKey || noteFlowBlockKey(candidate, path);
    if (!key || seen.has(key)) {
      continue;
    }
    seen.add(key);
    ordered.push(candidate);
  }
  const anchorIndex = ordered.findIndex((candidate) => candidate === anchor || (candidate.blockKey || noteFlowBlockKey(candidate, path)) === blockKey);
  const canonicalAnchor = side === "before" && anchorIndex > 0 ? ordered[anchorIndex - 1] : anchor;
  const canonicalSide = canonicalAnchor === anchor ? side : "after";
  const line = canonicalSide === "after" ? finite8(canonicalAnchor.end, canonicalAnchor.start) : finite8(canonicalAnchor.start, canonicalAnchor.end);
  return Number.isFinite(line) ? {
    anchor: canonicalAnchor,
    path: normalizeNoteFlowPath(canonicalAnchor.path || path),
    line,
    side: canonicalSide,
    blockStart: finite8(canonicalAnchor.blockStart, canonicalAnchor.start),
    blockEnd: finite8(canonicalAnchor.blockEnd, canonicalAnchor.end),
    blockKey: canonicalAnchor.blockKey || noteFlowBlockKey(canonicalAnchor, path)
  } : null;
}
function hasExactNoteFlowPlacement(noteFlow) {
  const version = noteFlow?.placementVersion;
  return hasStableNoteFlowAnchor(noteFlow) && (Number(version) >= 1 || version === null || version === void 0 || version === "");
}
function noteFlowAvoidanceReference(noteFlow, fallbackPath = "") {
  if (hasExactNoteFlowPlacement(noteFlow)) {
    return null;
  }
  const rawLine = noteFlow?.avoidanceLine;
  if (rawLine === null || rawLine === void 0 || rawLine === "") {
    return null;
  }
  const line = Number(rawLine);
  if (!Number.isFinite(line) || line < 0) {
    return null;
  }
  const path = String(noteFlow?.avoidancePath || noteFlow?.path || fallbackPath || "").replace(/\\/g, "/").replace(/^\/+|\/+$/g, "");
  return path ? { path, line } : null;
}
function noteFlowNeedsActivationRepair(strokes, frozenLayout) {
  const flows = (Array.isArray(strokes) ? strokes : []).filter((stroke) => stroke?.noteFlow?.enabled);
  if (!flows.length) {
    return false;
  }
  const frozenOffsets = Array.isArray(frozenLayout?.offsets) ? frozenLayout.offsets : [];
  if (!frozenOffsets.length) {
    return true;
  }
  const frozenKeys = new Set(frozenOffsets.map((item) => {
    const path = String(item?.path || "").replace(/\\/g, "/").replace(/^\/+|\/+$/g, "");
    const line = Number(item?.line);
    return path && Number.isFinite(line) ? `${path}\0${Math.floor(line)}` : "";
  }).filter(Boolean));
  return flows.some((stroke) => {
    if (hasExactNoteFlowPlacement(stroke.noteFlow)) {
      const path = String(stroke.noteFlow?.path || "").replace(/\\/g, "/").replace(/^\/+|\/+$/g, "");
      const line = Number(stroke.noteFlow?.line);
      return !path || !Number.isFinite(line) || !frozenKeys.has(`${path}\0${Math.floor(line)}`);
    }
    const avoidance = noteFlowAvoidanceReference(stroke.noteFlow);
    return !avoidance || !frozenKeys.has(`${avoidance.path}\0${Math.floor(avoidance.line)}`);
  });
}
function noteFlowRowReservation({ rowOffset = 0, boxHeight = 0, gap = 12 } = {}) {
  return Math.max(0, finite8(rowOffset)) + Math.max(0, finite8(boxHeight)) + Math.max(0, finite8(gap, 12));
}
function noteFlowSettledRowExtent({ rowOffset = 0, boxHeight = 0, displacement = 0 } = {}) {
  return Math.max(0, finite8(rowOffset)) + Math.max(0, finite8(displacement)) + Math.max(0, finite8(boxHeight));
}
function resizeNoteFlowGeometry(noteFlow, {
  originalBounds,
  resizedBounds,
  contentLeft = 0,
  contentWidth
} = {}) {
  const laneWidth = finite8(contentWidth);
  const originalTop = finite8(originalBounds?.minY, Number.NaN);
  const minX = finite8(resizedBounds?.minX, Number.NaN);
  const minY = finite8(resizedBounds?.minY, Number.NaN);
  const maxX = finite8(resizedBounds?.maxX, Number.NaN);
  const maxY = finite8(resizedBounds?.maxY, Number.NaN);
  if (laneWidth < 24 || ![originalTop, minX, minY, maxX, maxY].every(Number.isFinite)) {
    return null;
  }
  const width = Math.max(2, maxX - minX);
  const height = Math.max(2, maxY - minY);
  return {
    rowOffset: clamp7(Math.max(0, finite8(noteFlow?.rowOffset) + minY - originalTop), 0, 2e5),
    boxLeftRatio: clamp7((minX - finite8(contentLeft)) / laneWidth, -2, 3),
    boxWidthRatio: clamp7(width / laneWidth, 2 / laneWidth, 5),
    boxHeightRatio: clamp7(height / laneWidth, 2 / laneWidth, 5)
  };
}
function noteFlowReservedRowTop({ side, anchorTop, anchorBottom, applied = 0, scale = 1 } = {}) {
  const visualApplied = Math.max(0, finite8(applied)) * Math.max(0.01, finite8(scale, 1));
  return side === "after" ? finite8(anchorBottom) - visualApplied : finite8(anchorTop);
}
function projectStableNoteFlowBox({
  boxLeftRatio,
  boxWidthRatio,
  boxHeightRatio,
  contentLeft = 0,
  contentWidth,
  canvasWidth,
  fallbackWidth = 0,
  fallbackHeight = 0,
  minWidth = 24,
  y = 0
} = {}) {
  const safeCanvasWidth = Math.max(2, finite8(canvasWidth, 2));
  const measuredContentWidth = finite8(contentWidth, 0);
  const minimumStableWidth = clamp7(Math.max(2, finite8(minWidth, 24)), 2, safeCanvasWidth);
  const laneWidth = measuredContentWidth >= minimumStableWidth ? measuredContentWidth : safeCanvasWidth;
  const surfaceWidth = Math.max(2, finite8(canvasWidth, laneWidth));
  const width = clamp7(Math.max(
    minimumStableWidth,
    finite8(boxWidthRatio) * laneWidth,
    finite8(fallbackWidth)
  ), minimumStableWidth, surfaceWidth);
  const height = Math.max(2, finite8(boxHeightRatio) * laneWidth, finite8(fallbackHeight));
  return {
    x: clamp7(finite8(contentLeft) + finite8(boxLeftRatio) * laneWidth, 0, Math.max(0, surfaceWidth - width)),
    y: Math.max(0, finite8(y)),
    width,
    height
  };
}
function projectNoteFlowPointsToBox(points, sourceBounds, targetBox, { canvasWidth, canvasHeight } = {}) {
  const width = Math.max(1, finite8(canvasWidth, 1));
  const height = Math.max(1, finite8(canvasHeight, 1));
  const sourceLeft = finite8(sourceBounds?.minX);
  const sourceTop = finite8(sourceBounds?.minY);
  const sourceWidth = Math.max(1e-3, finite8(sourceBounds?.maxX) - sourceLeft);
  const sourceHeight = Math.max(1e-3, finite8(sourceBounds?.maxY) - sourceTop);
  const targetLeft = finite8(targetBox?.x);
  const targetTop = finite8(targetBox?.y);
  const targetWidth = Math.max(2, finite8(targetBox?.width, 2));
  const targetHeight = Math.max(2, finite8(targetBox?.height, 2));
  return (Array.isArray(points) ? points : []).map((point) => {
    const x = clamp7(finite8(point?.x) * width, 0, width);
    const y = clamp7(finite8(point?.y) * height, 0, height);
    return {
      ...point,
      x: clamp7((targetLeft + (x - sourceLeft) / sourceWidth * targetWidth) / width, 0, 1),
      y: clamp7((targetTop + (y - sourceTop) / sourceHeight * targetHeight) / height, 0, 1)
    };
  });
}
function translateNoteFlowPointsToRow(points, sourceBounds, rowY, { canvasWidth, canvasHeight } = {}) {
  const width = Math.max(1, finite8(canvasWidth, 1));
  const height = Math.max(1, finite8(canvasHeight, 1));
  const sourceTop = finite8(sourceBounds?.minY, Number.NaN);
  const targetTop = finite8(rowY, Number.NaN);
  if (!Number.isFinite(sourceTop) || !Number.isFinite(targetTop)) {
    return Array.isArray(points) ? points.map((point) => ({ ...point })) : [];
  }
  const dy = targetTop - sourceTop;
  return (Array.isArray(points) ? points : []).map((point) => ({
    ...point,
    x: clamp7(finite8(point?.x) * width, 0, width) / width,
    y: clamp7(finite8(point?.y) * height + dy, 0, height) / height
  }));
}
function selectOwnedBlankSpaceCandidate(candidates, { clientX, clientY } = {}) {
  const x = finite8(clientX, Number.NaN);
  const y = finite8(clientY, Number.NaN);
  if (!Number.isFinite(x) || !Number.isFinite(y)) {
    return null;
  }
  return (Array.isArray(candidates) ? candidates : []).map((candidate, order) => {
    const rect = candidate?.rect;
    const ownerRect = candidate?.ownerRect;
    const ownerLeft = finite8(ownerRect?.left, Number.NEGATIVE_INFINITY);
    const ownerRight = finite8(ownerRect?.right, Number.POSITIVE_INFINITY);
    const left = Math.max(finite8(rect?.left, Number.NaN), ownerLeft);
    const right = Math.min(finite8(rect?.right, Number.NaN), ownerRight);
    const top = finite8(rect?.top, Number.NaN);
    const bottom = finite8(rect?.bottom, Number.NaN);
    const applied = Math.max(0, finite8(candidate?.applied));
    const scale = Math.max(0.01, finite8(candidate?.scale, 1));
    if (![left, right, top, bottom].every(Number.isFinite) || right <= left || bottom <= top || applied <= 0) {
      return null;
    }
    const externalBefore = candidate?.styleProperty === "margin-top";
    const externalAfter = candidate?.styleProperty === "margin-bottom";
    const visualApplied = externalBefore || externalAfter ? applied * scale : Math.min(bottom - top, applied * scale);
    const heightOwned = candidate?.styleProperty === "height" || candidate?.property === "height";
    const ownedTop = externalBefore ? top - visualApplied : externalAfter ? bottom : heightOwned || candidate?.property === "padding-top" ? top : bottom - visualApplied;
    const ownedBottom = externalBefore ? top : externalAfter ? bottom + visualApplied : heightOwned || candidate?.property === "padding-bottom" ? bottom : top + visualApplied;
    if (x < left || x > right || y < ownedTop || y > ownedBottom) {
      return null;
    }
    return { ...candidate, ownedTop, ownedBottom, visualApplied, order };
  }).filter(Boolean).sort((a, b) => b.visualApplied - a.visualApplied || b.ownedBottom - a.ownedBottom || b.order - a.order)[0] || null;
}
function normalizeFrozenNoteFlowLayout(value) {
  const records = /* @__PURE__ */ new Map();
  for (const item of Array.isArray(value?.offsets) ? value.offsets : []) {
    const path = String(item?.path || "").replace(/\\/g, "/").replace(/^\/+|\/+$/g, "");
    const line = Number(item?.line);
    const property = item?.property === "padding-bottom" || item?.side === "after" ? "padding-bottom" : "padding-top";
    const side = property === "padding-bottom" ? "after" : "before";
    const offset = clamp7(finite8(item?.offset), 0, 2e5);
    if (!path || !Number.isFinite(line) || line < 0 || offset <= 0) {
      continue;
    }
    const blockStart = Number(item?.blockStart);
    const blockEnd = Number(item?.blockEnd);
    const normalized = {
      path,
      line: Math.floor(line),
      blockStart: Number.isFinite(blockStart) && blockStart >= 0 ? Math.floor(blockStart) : Math.floor(line),
      blockEnd: Number.isFinite(blockEnd) && blockEnd >= 0 ? Math.floor(blockEnd) : Math.floor(line),
      side,
      property,
      offset: Math.round(offset * 1e3) / 1e3
    };
    normalized.blockKey = noteFlowBlockKey(normalized);
    if (typeof item?.ownerId === "string" && item.ownerId) {
      normalized.ownerId = item.ownerId.slice(0, 160);
    }
    const key = `${normalized.blockKey}\0${normalized.property}`;
    const previous = records.get(key);
    if (!previous || normalized.offset > previous.offset) {
      records.set(key, normalized);
    }
  }
  return {
    version: 1,
    offsets: Array.from(records.values()).sort((a, b) => a.path.localeCompare(b.path) || a.blockStart - b.blockStart || a.blockEnd - b.blockEnd || a.property.localeCompare(b.property))
  };
}
function frozenNoteFlowLayoutSignature(value) {
  return normalizeFrozenNoteFlowLayout(value).offsets.map((item) => `${item.blockKey}:${item.property}:${item.offset}${item.ownerId ? `:${item.ownerId}` : ""}`).join("|");
}
function shouldRenderStrokeOnSurface(stroke, surfaceType) {
  return !(surfaceType === "source" && stroke?.noteFlow?.enabled);
}
function shouldPlaceStrokeBelowMarkdown(stroke) {
  return Boolean(stroke?.belowMarkdown || stroke?.noteFlow?.enabled);
}
function reflowNoteFlowRectangles(items, { gap = 6 } = {}) {
  const defaultGap = Math.max(0, finite8(gap, 6));
  const normalized = (Array.isArray(items) ? items : []).map((item, order) => {
    const minX = finite8(item?.minX, Number.NaN);
    const maxX = finite8(item?.maxX, Number.NaN);
    const minY = finite8(item?.minY, Number.NaN);
    const maxY = finite8(item?.maxY, Number.NaN);
    if (![minX, maxX, minY, maxY].every(Number.isFinite) || maxX < minX || maxY < minY) {
      return null;
    }
    return {
      ...item,
      order: Number.isFinite(Number(item?.order)) ? Number(item.order) : order,
      minX,
      maxX,
      minY,
      maxY,
      baseMinY: finite8(item?.baseMinY, minY),
      originalMinY: finite8(item?.originalMinY, minY),
      height: Math.max(0, maxY - minY),
      gap: Math.max(0, finite8(item?.gap, defaultGap)),
      moved: Boolean(item?.moved),
      rowKey: typeof item?.rowKey === "string" ? item.rowKey : "",
      overlapGroup: typeof item?.overlapGroup === "string" ? item.overlapGroup : "",
      align: item?.align === "bottom" ? "bottom" : "top"
    };
  }).filter(Boolean);
  const layoutItems = [];
  const overlapGroups = /* @__PURE__ */ new Map();
  for (const item of normalized) {
    if (!item.overlapGroup) {
      layoutItems.push({ ...item, members: [item] });
      continue;
    }
    let group = overlapGroups.get(item.overlapGroup);
    if (!group) {
      group = { ...item, members: [], originalMinY: item.minY };
      overlapGroups.set(item.overlapGroup, group);
      layoutItems.push(group);
    }
    group.members.push(item);
    group.minX = Math.min(group.minX, item.minX);
    group.maxX = Math.max(group.maxX, item.maxX);
    group.minY = Math.min(group.minY, item.minY);
    group.maxY = Math.max(group.maxY, item.maxY);
    group.baseMinY = Math.min(group.baseMinY, item.baseMinY);
    group.originalMinY = Math.min(group.originalMinY, item.minY);
    group.gap = Math.max(group.gap, item.gap);
    group.moved = group.moved || item.moved;
    group.order = Math.min(group.order, item.order);
    group.height = group.maxY - group.minY;
  }
  const horizontallyOverlaps = (first, second) => Math.min(first.maxX, second.maxX) - Math.max(first.minX, second.minX) > 0.5;
  const rows = [];
  const ordered = layoutItems.slice().sort((a, b) => a.baseMinY - b.baseMinY || a.order - b.order || Number(b.moved) - Number(a.moved));
  for (const item of ordered) {
    const row = rows.find((candidate) => {
      const sameLogicalRow = item.rowKey ? candidate.rowKey === item.rowKey : !candidate.rowKey && Math.abs(candidate.baseMinY - item.baseMinY) <= 0.5;
      return sameLogicalRow && !candidate.items.some((other) => horizontallyOverlaps(item, other));
    });
    if (row) {
      row.items.push(item);
      row.height = Math.max(row.height, item.height);
      row.gap = Math.max(row.gap, item.gap);
      row.moved = row.moved || item.moved;
      continue;
    }
    rows.push({
      rowKey: item.rowKey,
      baseMinY: item.baseMinY,
      height: item.height,
      gap: item.gap,
      moved: item.moved,
      order: item.order,
      items: [item]
    });
  }
  rows.sort((a, b) => a.baseMinY - b.baseMinY || a.order - b.order || Number(b.moved) - Number(a.moved));
  const settledRows = [];
  for (const row of rows) {
    let minY = row.baseMinY;
    let changed = true;
    while (changed) {
      changed = false;
      for (const blocker of settledRows) {
        const differentLogicalGaps = row.rowKey && blocker.rowKey && row.rowKey !== blocker.rowKey;
        if (differentLogicalGaps) {
          continue;
        }
        const sameLogicalGap = row.rowKey && blocker.rowKey && row.rowKey === blocker.rowKey;
        const overlapsLane = sameLogicalGap || row.items.some((item) => blocker.items.some((other) => horizontallyOverlaps(item, other)));
        if (!overlapsLane) {
          continue;
        }
        const clearance = Math.max(defaultGap, row.gap, blocker.gap);
        if (minY >= blocker.maxY + clearance || minY + row.height <= blocker.minY) {
          continue;
        }
        minY = blocker.maxY + clearance;
        changed = true;
      }
    }
    row.minY = minY;
    row.maxY = minY + row.height;
    for (const item of row.items) {
      item.minY = item.align === "bottom" ? row.maxY - item.height : row.minY;
      item.maxY = item.minY + item.height;
      const deltaY = item.minY - item.originalMinY;
      for (const member of item.members) {
        member.minY += deltaY;
        member.maxY += deltaY;
      }
    }
    settledRows.push(row);
  }
  return normalized.map((item) => ({
    id: item.id,
    index: item.index,
    minX: item.minX,
    maxX: item.maxX,
    minY: item.minY,
    maxY: item.maxY,
    rowKey: item.rowKey,
    deltaY: item.minY - item.originalMinY
  }));
}
function packNoteFlowInlineRectangles(items, {
  anchor,
  blockers = [],
  laneLeft = 0,
  laneRight,
  gap = 6,
  minItemWidth = 24
} = {}) {
  const left = finite8(laneLeft, 0);
  const right = Math.max(left, finite8(laneRight, left));
  const clearance = Math.max(0, finite8(gap, 6));
  const minimumWidth = Math.max(1, finite8(minItemWidth, 24));
  const normalizeRect2 = (rect, index = -1) => {
    const minX = finite8(rect?.minX, Number.NaN);
    const maxX = finite8(rect?.maxX, Number.NaN);
    const minY = finite8(rect?.minY, Number.NaN);
    const maxY = finite8(rect?.maxY, Number.NaN);
    return [minX, maxX, minY, maxY].every(Number.isFinite) && maxX >= minX && maxY >= minY ? { ...rect, index, minX, maxX, minY, maxY } : null;
  };
  const fixedAnchor = normalizeRect2(anchor);
  if (!fixedAnchor) {
    return [];
  }
  const fixed = [
    fixedAnchor,
    ...(Array.isArray(blockers) ? blockers : []).map((rect, index) => normalizeRect2(rect, index)).filter(Boolean)
  ];
  const normalized = (Array.isArray(items) ? items : []).map((item, order) => {
    const rect = normalizeRect2(item, Number.isFinite(Number(item?.index)) ? Number(item.index) : order);
    return rect ? {
      ...rect,
      id: item?.id,
      order: Number.isFinite(Number(item?.order)) ? Number(item.order) : order,
      width: Math.max(0, rect.maxX - rect.minX),
      height: Math.max(0, rect.maxY - rect.minY),
      shrinkToFit: Boolean(item?.shrinkToFit),
      overlapGroup: typeof item?.overlapGroup === "string" ? item.overlapGroup : "",
      originalMinX: rect.minX,
      originalMinY: rect.minY
    } : null;
  }).filter(Boolean).sort((a, b) => a.order - b.order || a.originalMinX - b.originalMinX || a.index - b.index);
  const packingItems = [];
  const overlapGroups = /* @__PURE__ */ new Map();
  for (const item of normalized) {
    if (!item.overlapGroup) {
      packingItems.push({ ...item, members: [item] });
      continue;
    }
    let group = overlapGroups.get(item.overlapGroup);
    if (!group) {
      group = { ...item, members: [], shrinkToFit: Boolean(item.shrinkToFit) };
      overlapGroups.set(item.overlapGroup, group);
      packingItems.push(group);
    }
    group.members.push(item);
    group.minX = Math.min(group.minX, item.minX);
    group.maxX = Math.max(group.maxX, item.maxX);
    group.minY = Math.min(group.minY, item.minY);
    group.maxY = Math.max(group.maxY, item.maxY);
    group.originalMinX = Math.min(group.originalMinX, item.originalMinX);
    group.originalMinY = Math.min(group.originalMinY, item.originalMinY);
    group.width = group.maxX - group.minX;
    group.height = group.maxY - group.minY;
    group.shrinkToFit = group.shrinkToFit || Boolean(item.shrinkToFit);
    group.order = Math.min(group.order, item.order);
  }
  const placed = [];
  const verticalOverlap = (rect, y, height) => Math.min(rect.maxY, y + height) - Math.max(rect.minY, y) > 0.5;
  const firstRowLeft = Math.min(right, Math.max(left, fixedAnchor.maxX + clearance));
  for (const item of packingItems) {
    let y = fixedAnchor.minY;
    let firstRow = true;
    let placement = null;
    const limit = fixed.length + placed.length + 8;
    for (let attempt = 0; attempt < limit; attempt += 1) {
      const occupied = [...fixed, ...placed].filter((rect) => verticalOverlap(rect, y, item.height)).sort((a, b) => a.minX - b.minX || a.maxX - b.maxX);
      let x = firstRow ? firstRowLeft : left;
      for (const rect of occupied) {
        if (x + item.width <= rect.minX - clearance) {
          break;
        }
        if (x < rect.maxX + clearance && x + item.width > rect.minX - clearance) {
          x = rect.maxX + clearance;
        }
      }
      if (x + item.width <= right + 0.5 || !occupied.length) {
        placement = { ...item, minX: x, maxX: x + item.width, minY: y, maxY: y + item.height };
        break;
      }
      if (firstRow && item.shrinkToFit) {
        const gaps = [];
        let gapLeft = firstRowLeft;
        for (const rect of occupied) {
          if (rect.maxX <= gapLeft) {
            continue;
          }
          if (rect.minX > gapLeft) {
            gaps.push({ left: gapLeft, right: Math.min(right, rect.minX - clearance) });
          }
          gapLeft = Math.max(gapLeft, rect.maxX + clearance);
        }
        if (gapLeft < right) {
          gaps.push({ left: gapLeft, right });
        }
        const availableGap = gaps.sort((a, b) => b.right - b.left - (a.right - a.left))[0];
        const availableWidth = availableGap ? availableGap.right - availableGap.left : 0;
        if (availableWidth >= minimumWidth) {
          const width = Math.min(item.width, availableWidth);
          placement = {
            ...item,
            minX: availableGap.left,
            maxX: availableGap.left + width,
            minY: y,
            maxY: y + item.height
          };
          break;
        }
      }
      const nextY = Math.min(...occupied.map((rect) => rect.maxY + clearance).filter((value) => value > y + 0.5));
      y = Number.isFinite(nextY) ? nextY : y + Math.max(item.height, clearance);
      firstRow = false;
    }
    placement || (placement = {
      ...item,
      minX: left,
      maxX: left + item.width,
      minY: y,
      maxY: y + item.height
    });
    placed.push(placement);
  }
  return placed.flatMap((item) => {
    const deltaX = item.minX - item.originalMinX;
    const deltaY = item.minY - item.originalMinY;
    const scaleX = (item.maxX - item.minX) / Math.max(1e-3, item.width);
    return item.members.map((member) => ({
      id: member.id,
      index: member.index,
      minX: item.overlapGroup ? item.minX + (member.minX - item.originalMinX) * scaleX : item.minX,
      maxX: item.overlapGroup ? item.minX + (member.maxX - item.originalMinX) * scaleX : item.maxX,
      minY: item.overlapGroup ? member.minY + deltaY : item.minY,
      maxY: item.overlapGroup ? member.maxY + deltaY : item.maxY,
      deltaX,
      deltaY,
      scaleX
    }));
  });
}

// src/preview-lifecycle.mjs
function shouldMountRootPreview({
  sourceMode,
  visible,
  hasSurface,
  sourceHasContent,
  renderedContent
} = {}) {
  return Boolean(
    hasSurface && visible && !sourceMode && (!sourceHasContent || renderedContent)
  );
}
function shouldResetDormantRootPreview({
  sourceMode,
  visible,
  sourceHasContent,
  renderedContent
} = {}) {
  return Boolean(sourceHasContent && !renderedContent && sourceMode && !visible);
}
function shouldRecoverEmptyRootPreview({
  sourceMode,
  visible,
  hasSurface,
  sourceHasContent,
  renderedContent,
  rendererMatches
} = {}) {
  return Boolean(
    hasSurface && visible && !sourceMode && sourceHasContent && !renderedContent && rendererMatches
  );
}
function pickRootPreview(previews = [], rendererPreview = null, isVisible = () => false, isLaidOut = () => false) {
  const candidates = Array.from(new Set(previews)).filter(Boolean);
  return candidates.find((preview) => isVisible(preview)) || candidates.find((preview) => isLaidOut(preview)) || candidates.find((preview) => preview === rendererPreview) || candidates[0] || null;
}

// src/connector-binding.mjs
function finite9(value, fallback = 0) {
  const parsed = Number(value);
  return Number.isFinite(parsed) ? parsed : fallback;
}
function clamp01(value) {
  return Math.min(1, Math.max(0, finite9(value)));
}
function boundsCenter(bounds) {
  return {
    x: (finite9(bounds?.minX) + finite9(bounds?.maxX)) / 2,
    y: (finite9(bounds?.minY) + finite9(bounds?.maxY)) / 2
  };
}
function canvasPoint(point, width, height) {
  return {
    x: clamp01(point?.x) * width,
    y: clamp01(point?.y) * height
  };
}
function attachmentPoint(bounds, toward) {
  const center = boundsCenter(bounds);
  const dx = finite9(toward?.x, center.x) - center.x;
  const dy = finite9(toward?.y, center.y) - center.y;
  const horizontal = Math.abs(dx) >= Math.abs(dy) * 0.62;
  return horizontal ? {
    x: dx >= 0 ? finite9(bounds?.maxX, center.x) : finite9(bounds?.minX, center.x),
    y: center.y
  } : {
    x: center.x,
    y: dy >= 0 ? finite9(bounds?.maxY, center.y) : finite9(bounds?.minY, center.y)
  };
}
function normalizePoint(point, width, height, timestamp) {
  return {
    x: clamp01(finite9(point?.x) / width),
    y: clamp01(finite9(point?.y) / height),
    t: timestamp,
    anchor: null
  };
}
function connectorSnapThreshold(hitPaddingPx = 0) {
  return Math.max(28, Math.max(0, finite9(hitPaddingPx)) * 1.75);
}
function buildSnappedConnectorPoints({
  fromBounds = null,
  toBounds = null,
  fromPoint = null,
  toPoint = null,
  canvasWidth = 1,
  canvasHeight = 1,
  timestamp = Date.now()
} = {}) {
  const width = Math.max(1, finite9(canvasWidth, 1));
  const height = Math.max(1, finite9(canvasHeight, 1));
  const freeFrom = canvasPoint(fromPoint, width, height);
  const freeTo = canvasPoint(toPoint, width, height);
  const fromTarget = toBounds ? boundsCenter(toBounds) : freeTo;
  const toTarget = fromBounds ? boundsCenter(fromBounds) : freeFrom;
  const from = fromBounds ? attachmentPoint(fromBounds, fromTarget) : freeFrom;
  const to = toBounds ? attachmentPoint(toBounds, toTarget) : freeTo;
  const horizontal = Math.abs(to.x - from.x) >= Math.abs(to.y - from.y) * 0.62;
  const control = horizontal ? { x: (from.x + to.x) / 2, y: from.y } : { x: from.x, y: (from.y + to.y) / 2 };
  return [from, control, to].map((point) => normalizePoint(point, width, height, timestamp));
}

// src/stroke-dynamics.mjs
function finite10(value, fallback = 0) {
  const number = Number(value);
  return Number.isFinite(number) ? number : fallback;
}
function clamp8(value, min, max) {
  return Math.min(max, Math.max(min, value));
}
function mix(from, to, amount) {
  return from + (to - from) * amount;
}
function buildFountainPenSegments(points, {
  canvasWidth = 1,
  canvasHeight = 1,
  baseWidth = 3,
  baseOpacity = 1
} = {}) {
  if (!Array.isArray(points) || points.length < 2) {
    return [];
  }
  const width = Math.max(1, finite10(canvasWidth, 1));
  const height = Math.max(1, finite10(canvasHeight, 1));
  const strokeWidth = Math.max(0.25, finite10(baseWidth, 3));
  const strokeOpacity = clamp8(finite10(baseOpacity, 1), 0, 1);
  const minimumVisibleWidth = Math.min(strokeWidth, Math.max(0.8, strokeWidth * 0.7));
  const samples = [];
  for (let index = 1; index < points.length; index += 1) {
    const from = points[index - 1];
    const to = points[index];
    const dx = (finite10(to?.x) - finite10(from?.x)) * width;
    const dy = (finite10(to?.y) - finite10(from?.y)) * height;
    const distance = Math.hypot(dx, dy);
    if (distance <= 0.01) {
      continue;
    }
    let sampleStart = index - 1;
    let sampleDistance = distance;
    let sampleElapsed = finite10(to?.t, index * 16) - finite10(from?.t, (index - 1) * 16);
    while (sampleStart > 0 && sampleElapsed < 22) {
      const previous = points[sampleStart - 1];
      const current = points[sampleStart];
      sampleDistance += Math.hypot(
        (finite10(current?.x) - finite10(previous?.x)) * width,
        (finite10(current?.y) - finite10(previous?.y)) * height
      );
      sampleStart -= 1;
      sampleElapsed = finite10(to?.t, index * 16) - finite10(previous?.t, sampleStart * 16);
    }
    const elapsed = clamp8(sampleElapsed > 0 ? sampleElapsed : 4, 1, 250);
    samples.push({
      from,
      to,
      speed: sampleDistance / elapsed
    });
  }
  if (!samples.length) {
    return [];
  }
  const forwardSpeeds = [];
  let filteredSpeed = samples[0].speed;
  for (const sample of samples) {
    filteredSpeed = mix(filteredSpeed, sample.speed, 0.18);
    forwardSpeeds.push(filteredSpeed);
  }
  const smoothedSpeeds = new Array(samples.length);
  let reverseSpeed = forwardSpeeds.at(-1);
  for (let index = samples.length - 1; index >= 0; index -= 1) {
    reverseSpeed = mix(reverseSpeed, forwardSpeeds[index], 0.24);
    smoothedSpeeds[index] = mix(forwardSpeeds[index], reverseSpeed, 0.35);
  }
  const segments = [];
  let previousWidth = null;
  const maximumWidthStep = Math.max(0.12, strokeWidth * 0.09);
  for (let index = 0; index < samples.length; index += 1) {
    const sample = samples[index];
    const speedRatio = clamp8((smoothedSpeeds[index] - 0.12) / 1.8, 0, 1);
    const easedSpeed = speedRatio * speedRatio * (3 - 2 * speedRatio);
    const targetWidth = strokeWidth * mix(1.36, 0.76, easedSpeed);
    const blendedWidth = previousWidth === null ? targetWidth : mix(previousWidth, targetWidth, 0.24);
    const width2 = previousWidth === null ? blendedWidth : clamp8(blendedWidth, previousWidth - maximumWidthStep, previousWidth + maximumWidthStep);
    const stableWidth = clamp8(width2, minimumVisibleWidth, strokeWidth * 1.42);
    previousWidth = stableWidth;
    segments.push({
      from: sample.from,
      to: sample.to,
      speed: smoothedSpeeds[index],
      width: stableWidth,
      opacity: strokeOpacity
    });
  }
  const terminalSamples = segments.slice(-Math.min(3, segments.length));
  const terminalSpeed = terminalSamples.length ? terminalSamples.reduce((sum, segment) => sum + segment.speed, 0) / terminalSamples.length : 0;
  if (terminalSpeed > 0.72 && segments.length >= 2) {
    const tailCount = Math.min(3, segments.length);
    const start = segments.length - tailCount;
    for (let index = start; index < segments.length; index += 1) {
      const progress = (index - start + 1) / tailCount;
      const easedProgress = progress * progress * (3 - 2 * progress);
      const taper = mix(1, 0.28, easedProgress);
      segments[index].width = Math.max(0.4, segments[index].width * taper);
      segments[index].tailTaper = progress;
    }
  }
  for (let index = 0; index < segments.length; index += 1) {
    const previous = segments[index - 1];
    const current = segments[index];
    const next = segments[index + 1];
    current.fromWidth = previous ? (previous.width + current.width) / 2 : current.width;
    current.toWidth = next ? (current.width + next.width) / 2 : current.tailTaper ? Math.max(0.08, current.width * 0.05) : current.width;
  }
  return segments;
}
function straightenWatercolorPoints(points, {
  canvasWidth = 1,
  canvasHeight = 1,
  angleTolerance = 12,
  minDistance = 18
} = {}) {
  if (!Array.isArray(points) || points.length < 2) {
    return { axis: null, points: Array.isArray(points) ? points.slice() : [] };
  }
  const width = Math.max(1, finite10(canvasWidth, 1));
  const height = Math.max(1, finite10(canvasHeight, 1));
  const start = points[0];
  const end = points[points.length - 1];
  const dx = (finite10(end?.x) - finite10(start?.x)) * width;
  const dy = (finite10(end?.y) - finite10(start?.y)) * height;
  const distance = Math.hypot(dx, dy);
  if (distance < Math.max(0, finite10(minDistance, 18))) {
    return { axis: null, points: points.slice() };
  }
  const tolerance = Math.sin(clamp8(finite10(angleTolerance, 12), 1, 45) * Math.PI / 180);
  const horizontal = Math.abs(dy) / distance <= tolerance;
  const vertical = Math.abs(dx) / distance <= tolerance;
  if (!horizontal && !vertical) {
    return { axis: null, points: points.slice() };
  }
  const axis = horizontal ? "horizontal" : "vertical";
  const centerX = (finite10(start?.x) + finite10(end?.x)) / 2;
  const centerY = (finite10(start?.y) + finite10(end?.y)) / 2;
  return {
    axis,
    points: points.map((point) => ({
      ...point,
      x: axis === "vertical" ? centerX : finite10(point?.x),
      y: axis === "horizontal" ? centerY : finite10(point?.y)
    }))
  };
}

// src/text-highlight.mjs
function finite11(value, fallback = 0) {
  const number = Number(value);
  return Number.isFinite(number) ? number : fallback;
}
function normalizeRect(rect) {
  const left = finite11(rect?.left);
  const top = finite11(rect?.top);
  const right = finite11(rect?.right, left + finite11(rect?.width));
  const bottom = finite11(rect?.bottom, top + finite11(rect?.height));
  if (right <= left || bottom <= top) {
    return null;
  }
  return {
    ...rect,
    left,
    top,
    right,
    bottom,
    width: right - left,
    height: bottom - top
  };
}
function intervalGap(startA, endA, startB, endB) {
  return Math.max(0, startA - endB, startB - endA);
}
function median(values) {
  const sorted = [...values].sort((a, b) => a - b);
  const middle = Math.floor(sorted.length / 2);
  return sorted.length % 2 ? sorted[middle] : (sorted[middle - 1] + sorted[middle]) / 2;
}
function pickTextHighlightLine(rectInputs, pointInputs, {
  maxVerticalDistance = 36,
  maxHorizontalDistance = 140
} = {}) {
  const rects = (Array.isArray(rectInputs) ? rectInputs : []).map(normalizeRect).filter(Boolean);
  const points = (Array.isArray(pointInputs) ? pointInputs : []).map((point) => ({
    x: finite11(point?.x, NaN),
    y: finite11(point?.y, NaN)
  })).filter((point) => Number.isFinite(point.x) && Number.isFinite(point.y));
  if (!rects.length || !points.length) {
    return null;
  }
  const pointLeft = Math.min(...points.map((point) => point.x));
  const pointRight = Math.max(...points.map((point) => point.x));
  const pointCenterY = median(points.map((point) => point.y));
  let best = null;
  for (const rect of rects) {
    const verticalDistances = points.map((point) => intervalGap(point.y, point.y, rect.top, rect.bottom));
    const horizontalGap = intervalGap(pointLeft, pointRight, rect.left, rect.right);
    const nearestVertical = Math.min(...verticalDistances);
    const medianVertical = median(verticalDistances);
    const verticalLimit = Math.max(finite11(maxVerticalDistance, 36), rect.height * 1.25);
    if (nearestVertical > verticalLimit || horizontalGap > Math.max(0, finite11(maxHorizontalDistance, 140))) {
      continue;
    }
    const supportedPoints = points.filter((point) => point.y >= rect.top - rect.height * 0.72 && point.y <= rect.bottom + rect.height * 0.72 && point.x >= rect.left - maxHorizontalDistance && point.x <= rect.right + maxHorizontalDistance).length;
    const centerDistance = Math.abs(pointCenterY - (rect.top + rect.height * 0.58));
    const overlap = Math.max(0, Math.min(pointRight, rect.right) - Math.max(pointLeft, rect.left));
    const span = Math.max(1, pointRight - pointLeft);
    const overlapRatio = Math.min(1, overlap / span);
    const supportRatio = supportedPoints / points.length;
    const score = medianVertical * 8 + nearestVertical * 3 + centerDistance * 0.8 + horizontalGap * 0.7 - supportRatio * 48 - overlapRatio * 24;
    if (!best || score < best.score) {
      best = { rect, score };
    }
  }
  return best?.rect || null;
}
function pickAnchoredTextHighlightLine(rectInputs, {
  lineIndex = null,
  preferredY = Number.NaN
} = {}) {
  const rects = (Array.isArray(rectInputs) ? rectInputs : []).map(normalizeRect).filter(Boolean);
  if (!rects.length) {
    return null;
  }
  const ordered = [...rects].sort((left, right) => finite11(left.top) - finite11(right.top) || finite11(left.left) - finite11(right.left));
  if (Number.isInteger(lineIndex) && lineIndex >= 0) {
    const indexed = ordered.filter((rect) => Number.isInteger(rect.lineIndex));
    if (indexed.length) {
      const exact = indexed.find((rect) => rect.lineIndex === lineIndex);
      if (exact) {
        return exact;
      }
    }
  }
  if (Number.isFinite(preferredY)) {
    return ordered.reduce((best, rect) => {
      const distance = Math.abs(preferredY - (rect.top + rect.height * 0.58));
      return !best || distance < best.distance ? { rect, distance } : best;
    }, null)?.rect || null;
  }
  return ordered[0] || null;
}

// src/mind-map.mjs
var DEFAULT_MAX_NODES = 240;
function clamp9(value, min, max) {
  return Math.min(max, Math.max(min, value));
}
function stripInlineMarkdown(value) {
  return String(value || "").replace(/!\[([^\]]*)\]\([^)]*\)/g, (_, alt) => alt || "Image").replace(/\[([^\]]+)\]\([^)]*\)/g, "$1").replace(/!?\[\[([^\]|#]+)(?:#[^\]|]+)?(?:\|([^\]]+))?\]\]/g, (_, target, alias) => alias || target).replace(/<https?:\/\/[^>]+>/g, (match) => match.slice(1, -1)).replace(/<[^>]+>/g, " ").replace(/(^|\s)([*_~`]{1,3})(?=\S)/g, "$1").replace(/(\S)([*_~`]{1,3})(?=\s|$)/g, "$1").replace(/\\([#*_[\]()`>+.!~-])/g, "$1").replace(/\s+/g, " ").trim();
}
function preserveInlineMarkdown(value) {
  return String(value || "").replace(/\r\n?/g, "\n").split("\n").map((line) => line.trim()).filter(Boolean).join("\n").trim();
}
function leadingIndent(value) {
  let count = 0;
  for (const character of String(value || "")) {
    if (character === " ") {
      count += 1;
    } else if (character === "	") {
      count += 4;
    } else {
      break;
    }
  }
  return count;
}
function parseMarkdownMindMap(source, {
  title = "Mind map",
  maxNodes = DEFAULT_MAX_NODES
} = {}) {
  const nodes = [{
    id: "root",
    parentId: null,
    text: stripInlineMarkdown(title) || "Mind map",
    markdown: preserveInlineMarkdown(title) || "Mind map",
    type: "root",
    sourceLine: -1
  }];
  const headingStack = [];
  const listStack = [];
  let currentSectionId = "root";
  let paragraphLines = [];
  let paragraphSourceLines = [];
  let paragraphStart = -1;
  let codeFence = "";
  let codeLines = [];
  let codeStart = -1;
  let codeEnd = -1;
  let frontmatter = false;
  let truncated = false;
  let sequence = 0;
  const canAdd = () => nodes.length < Math.max(2, Number(maxNodes) || DEFAULT_MAX_NODES);
  const addNode = (text, parentId, type, sourceLine, sourceEndLine = sourceLine, sourceText = "", markdownText = text) => {
    const normalized = stripInlineMarkdown(text);
    if (!normalized || !canAdd()) {
      truncated = truncated || Boolean(normalized);
      return null;
    }
    const node = {
      id: `node-${(++sequence).toString(36)}`,
      parentId: parentId || "root",
      text: normalized,
      markdown: preserveInlineMarkdown(markdownText) || normalized,
      type,
      sourceLine,
      sourceEndLine,
      sourceText: String(sourceText || "")
    };
    nodes.push(node);
    return node;
  };
  const flushParagraph = () => {
    if (!paragraphLines.length) {
      return;
    }
    addNode(
      paragraphLines.join(" "),
      currentSectionId,
      "paragraph",
      paragraphStart,
      paragraphStart + paragraphSourceLines.length - 1,
      paragraphSourceLines.join("\n"),
      paragraphLines.join("\n")
    );
    paragraphLines = [];
    paragraphSourceLines = [];
    paragraphStart = -1;
  };
  const flushCode = () => {
    if (!codeLines.length && !codeFence) {
      return;
    }
    const language = codeFence.replace(/^`{3,}|^~{3,}/, "").trim();
    const body = codeLines.join(" ").replace(/\s+/g, " ").trim();
    const sourceText = lines.slice(codeStart, (codeEnd >= codeStart ? codeEnd : codeStart + codeLines.length) + 1).join("\n");
    addNode(
      [language ? `Code (${language})` : "Code", body].filter(Boolean).join(": "),
      currentSectionId,
      "code",
      codeStart,
      codeEnd >= codeStart ? codeEnd : codeStart + codeLines.length,
      sourceText,
      sourceText
    );
    codeLines = [];
    codeFence = "";
    codeStart = -1;
    codeEnd = -1;
  };
  const lines = String(source || "").replace(/\r\n?/g, "\n").split("\n");
  for (let index = 0; index < lines.length; index += 1) {
    const line = lines[index];
    const trimmed = line.trim();
    if (index === 0 && trimmed === "---") {
      frontmatter = true;
      continue;
    }
    if (frontmatter) {
      if (trimmed === "---") {
        frontmatter = false;
      }
      continue;
    }
    const fence = trimmed.match(/^(`{3,}|~{3,})(.*)$/);
    if (codeFence) {
      if (fence && fence[1][0] === codeFence[0]) {
        codeEnd = index;
        flushCode();
      } else if (trimmed) {
        codeLines.push(trimmed);
      }
      continue;
    }
    if (fence) {
      flushParagraph();
      codeFence = `${fence[1]}${fence[2] || ""}`;
      codeStart = index;
      continue;
    }
    const heading = line.match(/^\s{0,3}(#{1,6})\s+(.+?)\s*#*\s*$/);
    if (heading) {
      flushParagraph();
      listStack.length = 0;
      const level = heading[1].length;
      while (headingStack.length && headingStack[headingStack.length - 1].level >= level) {
        headingStack.pop();
      }
      const parentId = headingStack[headingStack.length - 1]?.id || "root";
      const node = addNode(heading[2], parentId, "heading", index, index, line, heading[2]);
      if (node) {
        headingStack.push({ level, id: node.id });
        currentSectionId = node.id;
      }
      continue;
    }
    const list = line.match(/^(\s*)(?:[-+*]|\d+[.)])\s+(.*)$/);
    if (list) {
      flushParagraph();
      const indent = leadingIndent(list[1]);
      while (listStack.length && listStack[listStack.length - 1].indent >= indent) {
        listStack.pop();
      }
      const parentId = listStack[listStack.length - 1]?.id || currentSectionId;
      const task = list[2].match(/^\[([ xX])\]\s*(.*)$/);
      const text = task ? `[${task[1].toLowerCase() === "x" ? "x" : " "}] ${task[2]}` : list[2];
      const markdownText = task ? `- [${task[1].toLowerCase() === "x" ? "x" : " "}] ${task[2]}` : list[2];
      const node = addNode(text, parentId, task ? "task" : "list", index, index, line, markdownText);
      if (node) {
        listStack.push({ indent, id: node.id });
      }
      continue;
    }
    if (!trimmed) {
      flushParagraph();
      listStack.length = 0;
      continue;
    }
    if (/^\|?\s*:?-{3,}/.test(trimmed)) {
      continue;
    }
    const quote = trimmed.match(/^>\s?(.*)$/);
    const table = trimmed.includes("|") && trimmed.replace(/\|/g, "").trim() !== trimmed;
    if (quote || table) {
      flushParagraph();
      listStack.length = 0;
      addNode(
        quote ? quote[1] : trimmed.replace(/^\||\|$/g, "").split("|").map((cell) => cell.trim()).filter(Boolean).join(" | "),
        currentSectionId,
        quote ? "quote" : "table",
        index,
        index,
        line,
        quote ? quote[1] : trimmed.replace(/^\||\|$/g, "").split("|").map((cell) => cell.trim()).filter(Boolean).join(" | ")
      );
      continue;
    }
    if (paragraphStart < 0) {
      paragraphStart = index;
    }
    paragraphLines.push(trimmed);
    paragraphSourceLines.push(line);
    listStack.length = 0;
  }
  if (codeFence && codeStart >= 0) {
    codeEnd = lines.length - 1;
  }
  flushParagraph();
  flushCode();
  return { nodes, truncated };
}
function replaceMarkdownMindMapNodeText(source, node, nextText) {
  const text = String(nextText || "").trim();
  const type = String(node?.type || "");
  if (!text || type === "root") {
    return { source: String(source || ""), changed: false, reason: type === "root" ? "root-node" : "empty-text" };
  }
  const input = String(source || "");
  const newline = input.includes("\r\n") ? "\r\n" : "\n";
  const lines = input.replace(/\r\n?/g, "\n").split("\n");
  let startLine = Number(node?.sourceLine);
  let endLine = Number(node?.sourceEndLine);
  if (!Number.isInteger(startLine) || startLine < 0 || startLine >= lines.length) {
    const raw = String(node?.sourceText || "").replace(/\r\n?/g, "\n");
    const offset = raw ? lines.join("\n").indexOf(raw) : -1;
    if (offset < 0) {
      return { source: input, changed: false, reason: "source-range-not-found" };
    }
    startLine = lines.join("\n").slice(0, offset).split("\n").length - 1;
    endLine = startLine + raw.split("\n").length - 1;
  }
  if (!Number.isInteger(endLine) || endLine < startLine) {
    endLine = startLine;
  }
  endLine = Math.min(lines.length - 1, endLine);
  const original = lines.slice(startLine, endLine + 1);
  let replacement = [text];
  if (type === "heading") {
    const match = original[0]?.match(/^(\s{0,3}#{1,6}\s+)(.*?)(\s*#*\s*)$/);
    replacement = [match ? `${match[1]}${text}${match[3] || ""}` : text];
  } else if (type === "list" || type === "task") {
    const match = original[0]?.match(/^(\s*(?:[-+*]|\d+[.)])\s+)(?:\[([ xX])\]\s*)?(.*)$/);
    if (match) {
      const requestedTask = text.match(/^\[([ xX])\]\s*(.*)$/);
      const marker = requestedTask ? requestedTask[1] : match[2];
      const body = requestedTask ? requestedTask[2] : text;
      replacement = [`${match[1]}${marker !== void 0 ? `[${String(marker).toLowerCase() === "x" ? "x" : " "}] ` : ""}${body}`];
    }
  } else if (type === "quote") {
    const prefix = original[0]?.match(/^(\s*>\s?)/)?.[1] || "> ";
    replacement = [`${prefix}${text}`];
  } else if (type === "table") {
    const leading = /^\s*\|/.test(original[0] || "");
    const trailing = /\|\s*$/.test(original[0] || "");
    replacement = [`${leading ? "| " : ""}${text}${trailing ? " |" : ""}`];
  } else if (type === "code") {
    const first = original[0] || "```";
    const last = original.length > 1 && /^\s*(`{3,}|~{3,})/.test(original.at(-1)) ? original.at(-1) : null;
    const body = text.replace(/^Code(?:\s*\([^)]*\))?\s*:\s*/i, "");
    replacement = [first, body, ...last ? [last] : []];
  }
  const before = lines.slice();
  lines.splice(startLine, endLine - startLine + 1, ...replacement);
  const output = lines.join(newline);
  return {
    source: output,
    changed: output !== input,
    startLine,
    endLine: startLine + replacement.length - 1,
    lineDelta: replacement.length - original.length,
    sourceText: replacement.join(newline),
    beforeText: before.slice(startLine, endLine + 1).join(newline)
  };
}
function estimateNodeHeight(text, width, fontSize, markdown = "") {
  const charactersPerLine = Math.max(8, Math.floor(width / Math.max(6, fontSize * 0.58)));
  const lines = Math.max(1, Math.ceil(String(text || "").length / charactersPerLine));
  const mediaHeight = /!\[\[|!\[[^\]]*\]\(/.test(String(markdown || "")) ? 112 : 0;
  return Math.max(34, mediaHeight, Math.min(168, lines * fontSize * 1.28 + 16));
}
function layoutMindMap(model, {
  originX = 24,
  originY = 24,
  canvasWidth = 900,
  canvasHeight = 1200,
  nodeWidth = 176,
  columnGap = 72,
  rowGap = 18
} = {}) {
  const sourceNodes = Array.isArray(model?.nodes) ? model.nodes : [];
  if (!sourceNodes.length) {
    return { nodes: [], edges: [], width: 0, height: 0 };
  }
  const byId = new Map(sourceNodes.map((node) => [node.id, { ...node, children: [] }]));
  const root = byId.get("root") || byId.values().next().value;
  for (const node of byId.values()) {
    if (node === root) {
      continue;
    }
    (byId.get(node.parentId) || root).children.push(node);
  }
  const assignDepth = (node, depth = 0) => {
    node.depth = depth;
    for (const child of node.children) {
      assignDepth(child, depth + 1);
    }
  };
  assignDepth(root);
  const maxDepth = Math.max(...Array.from(byId.values(), (node) => node.depth));
  const availableWidth = Math.max(240, Number(canvasWidth) - Number(originX) - 20);
  const naturalWidth = nodeWidth + maxDepth * (nodeWidth + columnGap);
  const horizontalScale = Math.min(1, availableWidth / Math.max(1, naturalWidth));
  const effectiveWidth = clamp9(nodeWidth * horizontalScale, 104, nodeWidth);
  const columnStep = maxDepth > 0 ? Math.max(effectiveWidth + 24, (availableWidth - effectiveWidth) / maxDepth) : effectiveWidth;
  for (const node of byId.values()) {
    node.fontSize = node.type === "root" ? 18 : node.type === "heading" ? 15 : 13;
    node.width = node.type === "root" ? Math.min(effectiveWidth + 20, availableWidth) : effectiveWidth;
    node.height = estimateNodeHeight(node.text, node.width, node.fontSize, node.markdown);
  }
  let cursorY = Number(originY) || 0;
  const place = (node) => {
    if (!node.children.length) {
      node.y = cursorY;
      cursorY += node.height + rowGap;
    } else {
      for (const child of node.children) {
        place(child);
      }
      const first = node.children[0];
      const last = node.children[node.children.length - 1];
      const center = (first.y + first.height / 2 + (last.y + last.height / 2)) / 2;
      node.y = center - node.height / 2;
    }
    node.x = Number(originX) + node.depth * columnStep;
  };
  place(root);
  const levels = /* @__PURE__ */ new Map();
  for (const node of byId.values()) {
    const level = levels.get(node.depth) || [];
    level.push(node);
    levels.set(node.depth, level);
  }
  for (const level of levels.values()) {
    level.sort((a, b) => a.y - b.y);
    let bottom = -Infinity;
    for (const node of level) {
      node.y = Math.max(node.y, bottom + rowGap);
      bottom = node.y + node.height;
    }
  }
  const nodes = Array.from(byId.values()).map(({ children, ...node }) => node);
  const minY = Math.min(...nodes.map((node) => node.y));
  const maxY = Math.max(...nodes.map((node) => node.y + node.height));
  const wantedHeight = maxY - minY;
  const availableHeight = Math.max(100, Number(canvasHeight) - 20);
  let shiftY = minY < 8 ? 8 - minY : 0;
  if (maxY + shiftY > availableHeight && wantedHeight <= availableHeight - 8) {
    shiftY -= maxY + shiftY - availableHeight;
  }
  if (shiftY) {
    for (const node of nodes) {
      node.y += shiftY;
    }
  }
  const edges = nodes.filter((node) => node.parentId && byId.has(node.parentId)).map((node) => ({
    fromId: node.parentId,
    toId: node.id,
    depth: node.depth
  }));
  return {
    nodes,
    edges,
    width: Math.max(...nodes.map((node) => node.x + node.width)) - Number(originX),
    height: Math.max(...nodes.map((node) => node.y + node.height)) - Math.min(...nodes.map((node) => node.y))
  };
}

// src/text-edit-utils.mjs
function createAsyncCommitBarrier(onError = null) {
  let tail = Promise.resolve(true);
  const track = (task) => {
    const current = Promise.resolve(task).then(
      (value) => value !== false,
      (error) => {
        onError?.(error);
        return false;
      }
    );
    const combined = Promise.all([tail, current]).then((results) => results.every(Boolean));
    tail = combined;
    return combined;
  };
  const wait = async () => {
    let observed;
    do {
      observed = tail;
      await observed;
    } while (observed !== tail);
    return true;
  };
  return { track, wait };
}
function hoistPlainTextMarker(marker, editor, shouldUnwrap) {
  if (!marker || !editor || typeof shouldUnwrap !== "function") {
    return marker;
  }
  while (marker.parentNode && marker.parentNode !== editor && shouldUnwrap(marker.parentNode)) {
    const wrapper = marker.parentNode;
    const parent = wrapper.parentNode;
    if (!parent) {
      break;
    }
    const before = wrapper.cloneNode(false);
    const after = wrapper.cloneNode(false);
    let child = wrapper.firstChild;
    while (child && child !== marker) {
      const next = child.nextSibling;
      before.appendChild(child);
      child = next;
    }
    wrapper.removeChild(marker);
    while (wrapper.firstChild) {
      after.appendChild(wrapper.firstChild);
    }
    if (before.childNodes.length) {
      parent.insertBefore(before, wrapper);
    }
    parent.insertBefore(marker, wrapper);
    if (after.childNodes.length) {
      parent.insertBefore(after, wrapper);
    }
    parent.removeChild(wrapper);
  }
  return marker;
}

// src/portable-notedraw.mjs
var DRAWING_STORAGE_CONFIG = "config";
var DRAWING_STORAGE_NOTE_SUBFOLDER = "note-subfolder";
var DRAWING_STORAGE_NOTE_FOLDER = "note-folder";
var DRAWING_STORAGE_EMBEDDED = "note-file";
var DRAWING_STORAGE_MODES = [
  DRAWING_STORAGE_CONFIG,
  DRAWING_STORAGE_NOTE_SUBFOLDER,
  DRAWING_STORAGE_NOTE_FOLDER,
  DRAWING_STORAGE_EMBEDDED
];
var NOTEDRAW_DATA_BEGIN = "NOTEDRAW_DATA_BEGIN";
var NOTEDRAW_DATA_END = "NOTEDRAW_DATA_END";
var DATA_BLOCK_PATTERN = /<!--\s*NOTEDRAW_DATA_BEGIN\s+([a-z0-9-]+)\s*\r?\n([A-Za-z0-9+/=\r\n]+?)\r?\nNOTEDRAW_DATA_END\s*-->/gi;
function normalizeDrawingStorageMode(value) {
  return DRAWING_STORAGE_MODES.includes(value) ? value : DRAWING_STORAGE_CONFIG;
}
function resolveDrawingStoragePath({
  filePath,
  configDir = ".obsidian",
  pluginId = "notedraw",
  encodedName = "",
  mode = DRAWING_STORAGE_CONFIG
} = {}) {
  const normalizedPath = normalizePath(filePath);
  const normalizedMode = normalizeDrawingStorageMode(mode);
  const fallbackEncoded = `${normalizedPath.replace(/[^a-zA-Z0-9._/-]/g, "_").replace(/\//g, "__")}.json`;
  if (normalizedMode === DRAWING_STORAGE_CONFIG) {
    return `${normalizePath(configDir)}/plugins/${normalizePath(pluginId)}/drawings/${encodedName || fallbackEncoded}`;
  }
  if (normalizedMode === DRAWING_STORAGE_EMBEDDED) {
    return `${normalizedPath}#${NOTEDRAW_DATA_BEGIN}`;
  }
  const slash = normalizedPath.lastIndexOf("/");
  const parent = slash >= 0 ? normalizedPath.slice(0, slash) : "";
  const name = slash >= 0 ? normalizedPath.slice(slash + 1) : normalizedPath;
  const dot = name.lastIndexOf(".");
  const basename = (dot > 0 ? name.slice(0, dot) : name) || "Untitled";
  const dataName = `${basename}.notedraw.json`;
  const folder = normalizedMode === DRAWING_STORAGE_NOTE_SUBFOLDER ? joinPath(parent, "notedraw") : parent;
  return joinPath(folder, dataName);
}
async function encodeNotedrawDataBlock(bundle, options = {}) {
  const source = new TextEncoder().encode(JSON.stringify(bundle));
  const compressed = options.compress !== false ? await gzipBytes(source) : null;
  const bytes = compressed || source;
  const codec = compressed ? "gzip-base64" : "base64";
  const payload = bytesToBase64(bytes);
  const lines = payload.match(/.{1,8192}/g) || [""];
  return `<!-- ${NOTEDRAW_DATA_BEGIN} ${codec}
${lines.join("\n")}
${NOTEDRAW_DATA_END} -->`;
}
async function decodeNotedrawDataBlock(markdown) {
  const block = findNotedrawDataBlock(markdown);
  if (!block) {
    return null;
  }
  try {
    const encoded = block.payload.replace(/\s+/g, "");
    const bytes = base64ToBytes(encoded);
    const decoded = block.codec === "gzip-base64" ? await gunzipBytes(bytes) : bytes;
    return JSON.parse(new TextDecoder().decode(decoded));
  } catch {
    return null;
  }
}
function findNotedrawDataBlock(markdown) {
  const source = String(markdown || "");
  const matches = Array.from(source.matchAll(DATA_BLOCK_PATTERN));
  const match = matches[matches.length - 1];
  return match ? {
    codec: String(match[1] || "").toLowerCase(),
    payload: match[2] || "",
    start: match.index,
    end: match.index + match[0].length
  } : null;
}
function stripNotedrawDataBlocks(markdown) {
  return String(markdown || "").replace(DATA_BLOCK_PATTERN, "").replace(/[ \t]+$/gm, "").trimEnd();
}
async function appendNotedrawDataBlock(markdown, bundle, options = {}) {
  const block = await encodeNotedrawDataBlock(bundle, options);
  return appendEncodedNotedrawDataBlock(markdown, block);
}
function appendEncodedNotedrawDataBlock(markdown, block) {
  const body = stripNotedrawDataBlocks(markdown);
  return `${body}${body ? "\n\n" : ""}${block}
`;
}
function normalizePath(value) {
  return String(value || "").replace(/\\/g, "/").replace(/^\/+/, "").replace(/\/+$/, "");
}
function joinPath(...parts) {
  return parts.map(normalizePath).filter(Boolean).join("/");
}
async function gzipBytes(bytes) {
  if (typeof CompressionStream !== "function") {
    return null;
  }
  try {
    const stream = new Blob([bytes]).stream().pipeThrough(new CompressionStream("gzip"));
    return new Uint8Array(await new Response(stream).arrayBuffer());
  } catch {
    return null;
  }
}
async function gunzipBytes(bytes) {
  if (typeof DecompressionStream !== "function") {
    throw new Error("gzip decoding is unavailable");
  }
  const stream = new Blob([bytes]).stream().pipeThrough(new DecompressionStream("gzip"));
  return new Uint8Array(await new Response(stream).arrayBuffer());
}
function bytesToBase64(bytes) {
  const chunkSize = 32768;
  let binary = "";
  for (let index = 0; index < bytes.length; index += chunkSize) {
    binary += String.fromCharCode(...bytes.subarray(index, index + chunkSize));
  }
  return btoa(binary);
}
function base64ToBytes(value) {
  const binary = atob(value);
  const bytes = new Uint8Array(binary.length);
  for (let index = 0; index < binary.length; index += 1) {
    bytes[index] = binary.charCodeAt(index);
  }
  return bytes;
}

// src/notedraw-plugin.js
var activeDocument = window.activeWindow?.document || window.document;
var PLUGIN_ID = "notedraw";
var ASSET_DIR = `${PLUGIN_ID}/assets`;
var PORTABLE_RESOURCE_CACHE_LIMIT = 48;
var PORTABLE_RESOURCE_PREFIX = "notedraw-portable";
var WEBVIEW_DRAWING_PREFIX = "webviews";
var WORKSPACE_DRAWING_PREFIX = "workspaces";
var REGISTERED_SURFACE_PREFIX = "registered-surfaces";
var LEGACY_PLUGIN_ID = "note-doodle-preview";
var LEGACY_DRAWING_DIR = `${LEGACY_PLUGIN_ID}/doodles`;
var DEBUG_LOG_FILE = "debug-log.jsonl";
var DEBUG_LOG_LIMIT = 150;
var TEXT_SAVE_DELAY_MS = 160;
var SETTINGS_SAVE_DELAY_MS = 260;
var LONG_PRESS_MS = 550;
var SELECT_TAP_DISTANCE = 6;
var SELECT_STROKE_PADDING = 8;
var SELECTED_STROKE_ALPHA = 0.38;
var SELECT_RESIZE_HANDLE_SIZE = 9;
var SELECT_RESIZE_HANDLE_HIT_RADIUS = 22;
var SELECTION_FRAME_COLOR = "rgba(124, 58, 237, 0.96)";
var SELECTION_FRAME_LINE_WIDTH = 1.5;
var MIN_FRAME_CORNER_RADIUS = 0;
var MAX_FRAME_CORNER_RADIUS = 24;
var SELECTION_FRAME_DRAG_FILL = "rgba(124, 58, 237, 0.10)";
var SNAP_GRID_PX = 8;
var SNAP_THRESHOLD_PX = 7;
var DRAWING_INTERPOLATION_STEP_PX = 3;
var DRAWING_MIN_POINT_DISTANCE_PX = 0.55;
var DRAWING_COMPACT_DISTANCE_PX = 1.1;
var MAX_PEN_COUNT = 5;
var MIN_BRUSH_WIDTH = 0.25;
var MAX_BRUSH_WIDTH = 96;
var DEFAULT_PEN_OPACITY = 1;
var MIN_LONG_PRESS_MS = 250;
var MAX_LONG_PRESS_MS = 1200;
var MIN_SELECT_TAP_DISTANCE = 3;
var MAX_SELECT_TAP_DISTANCE = 18;
var MIN_SELECT_STROKE_PADDING = 2;
var MAX_SELECT_STROKE_PADDING = 28;
var MIN_SELECTED_STROKE_ALPHA = 0.12;
var MAX_SELECTED_STROKE_ALPHA = 1;
var MIN_DRAWING_INTERPOLATION_STEP_PX = 1;
var MAX_DRAWING_INTERPOLATION_STEP_PX = 8;
var MIN_DRAWING_MIN_POINT_DISTANCE_PX = 0.05;
var MAX_DRAWING_MIN_POINT_DISTANCE_PX = 3;
var MIN_DRAWING_COMPACT_DISTANCE_PX = 0.1;
var MAX_DRAWING_COMPACT_DISTANCE_PX = 6;
var MIN_AUTO_SAVE_DELAY_MS = 120;
var MAX_AUTO_SAVE_DELAY_MS = 2500;
var TOOL_DRAW = "draw";
var TOOL_SELECT = "select";
var TOOL_EDIT_MD = "edit-md";
var TOOL_TEXT = "text";
var TOOL_EMBED = "embed";
var BRUSH_PEN = "pen";
var BRUSH_WATERCOLOR = "watercolor";
var BRUSH_VARIANT_DEFAULT = "default";
var PEN_VARIANT_FOUNTAIN = "fountain";
var PEN_VARIANT_NOTE = "note-flow";
var PEN_VARIANT_LASER = "laser";
var WATERCOLOR_VARIANT_TEXT = "text-highlight";
var WATERCOLOR_VARIANT_STRAIGHT = "straight";
var MIN_READING_ZOOM = 0.6;
var MAX_READING_ZOOM = 100;
var READING_ZOOM_NORMALIZATION_EPSILON = 0.03;
var DRAG_PEER_ANIMATION_MS = 150;
var DRAG_STROKE_ANIMATION_MS = 160;
var MOUSE_SELECTED_DRAG_ACTIVATION_PX = 3;
var TOUCH_SELECTED_DRAG_ACTIVATION_PX = 5;
var MIN_INLINE_NOTE_FLOW_ITEM_WIDTH_PX = 18;
var INLINE_NOTE_FLOW_GAP_PX = 6;
var INLINE_NOTE_FLOW_LIST_GAP_PX = 10;
var INLINE_SELECTION_BOUNDARY_BIAS_PX = 4;
var TEXT_RENDER_PLAIN = "plain";
var TEXT_RENDER_MARKDOWN = "markdown";
var TEXT_RENDER_HTML = "html";
var TEXT_RENDER_NOTE = "note";
var EMBED_IMAGE = "image";
var EMBED_VIDEO = "video";
var EMBED_FILE = "file";
var COMMON_COLORS = [
  "#e53935",
  "#fdd835",
  "#43a047",
  "#1e88e5",
  "#111827"
];
var NO_COLOR = "transparent";
var SETTINGS_EXTRA_CODE_ASSETS = [
  { path: "extras/code-1.jpg", dataUrl: code_1_default, labelKey: "supportCodeAlipay" },
  { path: "extras/code-2.png", dataUrl: code_2_default, labelKey: "supportCodeBinance" }
];
var LANGUAGE_AUTO = "auto";
var LANGUAGE_OPTIONS = [
  { value: LANGUAGE_AUTO, label: "Auto" },
  { value: "zh", label: "\u7B80\u4F53\u4E2D\u6587" },
  { value: "zh-TW", label: "\u7E41\u9AD4\u4E2D\u6587" },
  { value: "en", label: "English" },
  { value: "ug", label: "\u0626\u06C7\u064A\u063A\u06C7\u0631\u0686\u06D5" },
  { value: "ru", label: "\u0420\u0443\u0441\u0441\u043A\u0438\u0439" },
  { value: "ar", label: "\u0627\u0644\u0639\u0631\u0628\u064A\u0629" },
  { value: "es", label: "Espa\xF1ol" },
  { value: "fr", label: "Fran\xE7ais" },
  { value: "de", label: "Deutsch" },
  { value: "ja", label: "\u65E5\u672C\u8A9E" },
  { value: "ko", label: "\uD55C\uAD6D\uC5B4" },
  { value: "tr", label: "T\xFCrk\xE7e" }
];
var I18N = {
  en: {
    toggleCommand: "Toggle preview edit and drawing mode",
    openNoteOrWebviewFirst: "Open a note or webview first.",
    failedSaveDrawing: "Failed to save drawing data.",
    failedImportFile: "Failed to import file.",
    editTextDraw: "Edit text / draw",
    editTextDrawHidden: "Edit text / draw (drawings hidden)",
    editWebviewDraw: "Edit webview / draw",
    selectDrawings: "Select drawings",
    editMarkdownTool: "Edit MD",
    pen: "Pen",
    standardPen: "Standard pen",
    fountainPen: "Fountain pen",
    laserPen: "Laser pointer",
    notePen: "Note pen",
    watercolorBrush: "Watercolor brush",
    standardWatercolor: "Standard watercolor",
    textWatercolor: "Text watercolor",
    straightWatercolor: "Straight watercolor",
    floatingText: "Floating text",
    undoLastDrawing: "Undo last drawing",
    redoDrawing: "Redo drawing",
    deleteSelectedDrawing: "Delete selected drawing",
    penSettings: "Pen settings",
    advancedColor: "Advanced color",
    noColor: "No color",
    blockBackground: "Box background",
    clearBlockBackground: "Clear box background",
    moveMarkdownBlock: "Move or arrange Markdown block",
    failedMoveMarkdownBlock: "Could not move the Markdown block.",
    boxElements: "Put selection in a box",
    fillBoxElements: "Fill element box",
    unboxElements: "Remove box",
    floatMarkdownBlock: "Make Markdown block floating",
    penWidth: "Pen width",
    penOpacity: "Pen opacity",
    elementSize: "Size",
    elementOpacity: "Opacity",
    textGroup: "Text and links",
    buttonGroup: "Buttons and connectors",
    textPlain: "Text",
    title: "Title",
    code: "Code",
    button: "Button",
    primaryButton: "Primary",
    outlineButton: "Rectangle",
    pillButton: "Solid rectangle",
    arrowUp: "Up",
    arrowDown: "Down",
    arrowLeft: "Left",
    arrowRight: "Right",
    arrow: "Arrow",
    fileTag: "File tag",
    importGroup: "Import",
    image: "Image",
    video: "Video",
    file: "File",
    previewGroup: "Preview",
    markdown: "MD",
    html: "HTML",
    note: "Note",
    mindMap: "Mind map from file",
    chooseMindMapFile: "Choose a note for the mind map",
    chooseButtonCommand: "Choose an Obsidian command for this button",
    buttonCommand: "Button command",
    commandAssigned: "Button command assigned: {name}",
    commandUnavailable: "Button command is unavailable: {name}",
    affectSourceNote: "Affect source note",
    openSourceNote: "Open source note",
    sourceNoteUpdated: "Source note updated",
    sourceNoteUpdateFailed: "Could not update the source note",
    placeMindMap: "Tap the reading view to place the mind map.",
    emptyMindMap: "No usable content was found in that note.",
    mindMapTruncated: "The mind map was limited to 240 nodes for performance.",
    bold: "Bold",
    italic: "Italic",
    underline: "Underline",
    alignLeft: "Align left",
    alignCenter: "Center",
    alignRight: "Align right",
    alignJustify: "Justify",
    inlineCode: "Inline code",
    keyboardTag: "Keyboard tag",
    superscript: "Superscript",
    subscript: "Subscript",
    codeBlock: "Code block",
    highlight: "Highlight",
    insertBreak: "Insert line break",
    clearFormat: "Clear formatting",
    textColor: "Text color",
    highlightColor: "Highlight color",
    textSize: "Text size",
    size: "Size",
    movePanel: "Move panel",
    useColor: "Use color {color}",
    settingsSectionInterface: "Interface",
    settingsSectionPen: "Pen",
    settingsSectionWatercolor: "Watercolor",
    settingsSectionInteraction: "Interaction",
    settingsSectionPerformance: "Performance",
    settingsSectionLayout: "Layout",
    settingsSectionStorage: "Data storage and sharing",
    settingsSectionDiagnostics: "Diagnostics",
    settingsSectionSupport: "Support",
    settingsLanguage: "Language",
    settingsLanguageDesc: "Plugin UI language. Auto follows Obsidian when possible.",
    defaultSourceEditMarkdown: "Default to Edit MD in editing view",
    defaultSourceEditMarkdownDesc: "Open NoteDraw in Edit MD mode when switching from reading view to editing view.",
    languageAuto: "Auto",
    drawingStorageMode: "NoteDraw data location",
    drawingStorageModeDesc: "Where each note's NoteDraw JSON is stored. The plugin config folder remains the default.",
    drawingStorageConfig: "Plugin config folder (default)",
    drawingStorageNoteSubfolder: "Current folder / notedraw",
    drawingStorageNoteFolder: "Current folder",
    drawingStorageEmbedded: "Current Markdown file (hidden)",
    shareNoteDrawFile: "Share NoteDraw file",
    sharePreparing: "Packaging this note and its linked resources...",
    shareReady: "NoteDraw file is ready with {count} embedded resource(s).",
    shareDownloaded: "Native sharing is unavailable. The NoteDraw Markdown file was downloaded.",
    shareFailed: "Could not package the NoteDraw file.",
    defaultPenColor: "Default pen color",
    defaultPenColorDesc: "Initial color for new pen strokes.",
    defaultPenWidth: "Default pen width",
    defaultPenWidthDesc: "Initial pen width.",
    defaultPenOpacity: "Default pen opacity",
    defaultPenOpacityDesc: "Initial pen opacity.",
    defaultWatercolorColor: "Default watercolor color",
    defaultWatercolorColorDesc: "Initial color for watercolor strokes.",
    defaultWatercolorWidth: "Default watercolor width",
    defaultWatercolorWidthDesc: "Initial watercolor width.",
    defaultWatercolorOpacity: "Default watercolor opacity",
    defaultWatercolorOpacityDesc: "Initial watercolor opacity.",
    toolbarTopOffset: "Toolbar top offset",
    toolbarTopOffsetDesc: "Extra pixels below the Obsidian header.",
    frameCornerRadius: "Frame corner radius",
    frameCornerRadiusDesc: "Corner radius for element, selection, and group frames. Zero uses square corners.",
    longPressMs: "Long press delay",
    longPressMsDesc: "Delay before long-press actions open secondary controls.",
    selectTapDistance: "Tap tolerance",
    selectTapDistanceDesc: "Movement allowed before a tap becomes a drag or stroke.",
    selectStrokePadding: "Selection hit padding",
    selectStrokePaddingDesc: "Extra hit area around drawing elements for easier selection.",
    selectedStrokeAlpha: "Selected element opacity",
    selectedStrokeAlphaDesc: "Opacity used while selected elements are previewed for editing.",
    drawingInterpolationStep: "Stroke smoothing",
    drawingInterpolationStepDesc: "Lower values add more points between samples for smoother lines.",
    drawingMinPointDistance: "Input sample spacing",
    drawingMinPointDistanceDesc: "Minimum point distance while drawing. Lower values capture more detail.",
    drawingCompactDistance: "Save compaction",
    drawingCompactDistanceDesc: "Point reduction applied during auto-save. Lower values keep more detail.",
    autoSaveDelayMs: "Auto-save delay",
    autoSaveDelayMsDesc: "Delay before drawing changes are written to the plugin data folder.",
    resetBrushDefaults: "Reset brush defaults",
    resetBrushDefaultsDesc: "Restore pen and watercolor defaults to the bundled values.",
    resetLayoutDefaults: "Reset layout and interaction",
    resetLayoutDefaultsDesc: "Restore toolbar, selection, smoothing, and auto-save defaults.",
    reset: "Reset",
    debugLog: "Debug log",
    debugLogDesc: "Write text-save diagnostics to the plugin folder only while troubleshooting.",
    supportTitle: "Support NoteDraw",
    supportSubtitle: "Scan with Alipay or Binance to support ongoing maintenance.",
    supportCodeAlipay: "Alipay",
    supportCodeBinance: "Binance",
    bringToFront: "Bring to front",
    sendToBack: "Send to back",
    selectRelatedElements: "Select related",
    moveForward: "Move up",
    moveBackward: "Move down",
    lockElement: "Lock",
    unlockElement: "Unlock",
    copyElement: "Copy element/link",
    copyElementLink: "Copy element link",
    pasteElement: "Paste",
    insertIntoNote: "Insert into note flow",
    removeFromNote: "Remove from note flow",
    convertToNoteFlow: "Convert to NoteFlow",
    convertToFloating: "Convert to floating",
    toggleFlowMode: "Switch NoteFlow / floating",
    copiedElements: "Copied {count} element(s).",
    copiedElementLink: "Copied NoteDraw element link.",
    elementLinkLabel: "NoteDraw element",
    elementLinkTargetNotFound: "The linked NoteDraw element was not found.",
    pastedElements: "Pasted {count} element(s).",
    nothingToPaste: "No NoteDraw elements have been copied."
  },
  zh: {
    toggleCommand: "\u5207\u6362\u9605\u8BFB\u7F16\u8F91\u548C\u6D82\u9E26\u6A21\u5F0F",
    openNoteOrWebviewFirst: "\u8BF7\u5148\u6253\u5F00\u7B14\u8BB0\u6216\u7F51\u9875\u89C6\u56FE\u3002",
    failedSaveDrawing: "\u6D82\u9E26\u6570\u636E\u4FDD\u5B58\u5931\u8D25\u3002",
    failedImportFile: "\u5BFC\u5165\u6587\u4EF6\u5931\u8D25\u3002",
    editTextDraw: "\u7F16\u8F91\u6587\u5B57 / \u6D82\u9E26",
    editTextDrawHidden: "\u7F16\u8F91\u6587\u5B57 / \u6D82\u9E26\uFF08\u6D82\u9E26\u5DF2\u9690\u85CF\uFF09",
    editWebviewDraw: "\u7F16\u8F91\u7F51\u9875 / \u6D82\u9E26",
    selectDrawings: "\u9009\u62E9\u5143\u7D20",
    editMarkdownTool: "\u7F16\u8F91 MD",
    pen: "\u7B14",
    standardPen: "\u666E\u901A\u7B14",
    fountainPen: "\u94A2\u7B14",
    laserPen: "\u6FC0\u5149\u7B14",
    notePen: "\u7B14\u8BB0\u7B14",
    watercolorBrush: "\u6C34\u5F69\u7B14",
    standardWatercolor: "\u666E\u901A\u6C34\u5F69\u7B14",
    textWatercolor: "\u6587\u5B57\u6C34\u5F69\u7B14",
    straightWatercolor: "\u81EA\u52A8\u62C9\u76F4\u6C34\u5F69\u7B14",
    floatingText: "\u60AC\u6D6E\u6587\u5B57",
    undoLastDrawing: "\u64A4\u9500\u4E0A\u4E00\u6B65\u6D82\u9E26",
    redoDrawing: "\u91CD\u505A\u6D82\u9E26",
    deleteSelectedDrawing: "\u5220\u9664\u9009\u4E2D\u5143\u7D20",
    penSettings: "\u753B\u7B14\u8BBE\u7F6E",
    advancedColor: "\u9AD8\u7EA7\u989C\u8272",
    noColor: "\u65E0\u8272",
    blockBackground: "\u76D2\u5B50\u5E95\u8272",
    clearBlockBackground: "\u6E05\u9664\u76D2\u5B50\u5E95\u8272",
    moveMarkdownBlock: "\u79FB\u52A8\u6216\u6392\u5217 Markdown \u5757",
    failedMoveMarkdownBlock: "Markdown \u5757\u79FB\u52A8\u5931\u8D25\u3002",
    boxElements: "\u6846\u6210\u76D2\u5B50\u7EC4",
    fillBoxElements: "\u5B9E\u5FC3\u6846",
    unboxElements: "\u53D6\u6D88\u76D2\u5B50\u6846",
    floatMarkdownBlock: "\u8BBE\u4E3A\u60AC\u6D6E Markdown \u5757",
    penWidth: "\u7B14\u5BBD",
    penOpacity: "\u7B14\u900F\u660E\u5EA6",
    elementSize: "\u5927\u5C0F",
    elementOpacity: "\u900F\u660E\u5EA6",
    textGroup: "\u6587\u5B57\u94FE\u63A5",
    buttonGroup: "\u6309\u94AE/\u8FDE\u63A5",
    textPlain: "\u666E\u901A\u6587\u5B57",
    title: "\u6807\u9898",
    code: "\u4EE3\u7801",
    button: "\u6309\u94AE",
    primaryButton: "\u4E3B\u6309\u94AE",
    outlineButton: "\u77E9\u5F62",
    pillButton: "\u5B9E\u5FC3\u77E9\u5F62",
    arrowUp: "\u4E0A",
    arrowDown: "\u4E0B",
    arrowLeft: "\u5DE6",
    arrowRight: "\u53F3",
    arrow: "\u7BAD\u5934",
    fileTag: "\u6587\u4EF6\u6807\u7B7E",
    importGroup: "\u5BFC\u5165",
    image: "\u56FE\u7247",
    video: "\u89C6\u9891",
    file: "\u6587\u4EF6",
    previewGroup: "\u9884\u89C8",
    markdown: "MD",
    html: "HTML",
    note: "\u7B14\u8BB0",
    mindMap: "\u4ECE\u6587\u4EF6\u63D0\u53D6\u601D\u7EF4\u5BFC\u56FE",
    chooseMindMapFile: "\u9009\u62E9\u8981\u63D0\u53D6\u601D\u7EF4\u5BFC\u56FE\u7684\u7B14\u8BB0",
    chooseButtonCommand: "\u9009\u62E9\u8FD9\u4E2A\u6309\u94AE\u8981\u6267\u884C\u7684 Obsidian \u547D\u4EE4",
    buttonCommand: "\u6309\u94AE\u547D\u4EE4",
    commandAssigned: "\u5DF2\u7ED1\u5B9A\u6309\u94AE\u547D\u4EE4\uFF1A{name}",
    commandUnavailable: "\u6309\u94AE\u547D\u4EE4\u4E0D\u53EF\u7528\uFF1A{name}",
    affectSourceNote: "\u5F71\u54CD\u7B14\u8BB0",
    openSourceNote: "\u8DF3\u8F6C\u6E90\u7B14\u8BB0",
    sourceNoteUpdated: "\u6E90\u7B14\u8BB0\u5DF2\u540C\u6B65",
    sourceNoteUpdateFailed: "\u6E90\u7B14\u8BB0\u540C\u6B65\u5931\u8D25",
    placeMindMap: "\u8BF7\u5728\u9605\u8BFB\u89C6\u56FE\u4E2D\u70B9\u51FB\u601D\u7EF4\u5BFC\u56FE\u7684\u653E\u7F6E\u4F4D\u7F6E\u3002",
    emptyMindMap: "\u8BE5\u7B14\u8BB0\u4E2D\u6CA1\u6709\u53EF\u63D0\u53D6\u7684\u6709\u6548\u5185\u5BB9\u3002",
    mindMapTruncated: "\u4E3A\u4FDD\u8BC1\u6027\u80FD\uFF0C\u601D\u7EF4\u5BFC\u56FE\u6700\u591A\u63D0\u53D6 240 \u4E2A\u8282\u70B9\u3002",
    bold: "\u52A0\u7C97",
    italic: "\u503E\u659C",
    underline: "\u4E0B\u5212\u7EBF",
    alignLeft: "\u5DE6\u5BF9\u9F50",
    alignCenter: "\u5C45\u4E2D",
    alignRight: "\u53F3\u5BF9\u9F50",
    alignJustify: "\u4E24\u7AEF\u5BF9\u9F50",
    inlineCode: "\u884C\u5185\u4EE3\u7801",
    keyboardTag: "\u952E\u76D8\u6807\u7B7E",
    superscript: "\u4E0A\u6807",
    subscript: "\u4E0B\u6807",
    codeBlock: "\u4EE3\u7801\u5757",
    highlight: "\u9AD8\u4EAE",
    insertBreak: "\u6362\u884C",
    clearFormat: "\u6E05\u9664\u683C\u5F0F",
    textColor: "\u6587\u5B57\u989C\u8272",
    highlightColor: "\u9AD8\u4EAE\u989C\u8272",
    textSize: "\u5B57\u53F7",
    size: "\u5B57\u53F7",
    movePanel: "\u79FB\u52A8\u9762\u677F",
    useColor: "\u4F7F\u7528\u989C\u8272 {color}",
    settingsSectionInterface: "\u754C\u9762",
    settingsSectionPen: "\u7B14",
    settingsSectionWatercolor: "\u6C34\u5F69\u7B14",
    settingsSectionInteraction: "\u4EA4\u4E92",
    settingsSectionPerformance: "\u6027\u80FD",
    settingsSectionLayout: "\u5E03\u5C40",
    settingsSectionStorage: "\u6570\u636E\u5B58\u50A8\u4E0E\u5206\u4EAB",
    settingsSectionDiagnostics: "\u8BCA\u65AD",
    settingsSectionSupport: "\u652F\u6301\u4F5C\u8005",
    settingsLanguage: "\u8BED\u8A00",
    settingsLanguageDesc: "\u63D2\u4EF6\u754C\u9762\u8BED\u8A00\u3002\u81EA\u52A8\u6A21\u5F0F\u4F1A\u5C3D\u91CF\u8DDF\u968F Obsidian\u3002",
    defaultSourceEditMarkdown: "\u7F16\u8F91\u89C6\u56FE\u9ED8\u8BA4\u8FDB\u5165\u7F16\u8F91 MD",
    defaultSourceEditMarkdownDesc: "\u4ECE\u9605\u8BFB\u89C6\u56FE\u5207\u6362\u5230\u7F16\u8F91\u89C6\u56FE\u65F6\uFF0CNoteDraw \u9ED8\u8BA4\u8FDB\u5165\u7F16\u8F91 MD \u5DE5\u5177\u3002",
    languageAuto: "\u81EA\u52A8",
    drawingStorageMode: "NoteDraw \u6570\u636E\u4F4D\u7F6E",
    drawingStorageModeDesc: "\u8BBE\u7F6E\u6BCF\u7BC7\u7B14\u8BB0\u7684 NoteDraw \u6570\u636E\u6587\u4EF6\u4F4D\u7F6E\u3002\u9ED8\u8BA4\u4ECD\u4E3A\u63D2\u4EF6\u914D\u7F6E\u6587\u4EF6\u5939\u3002",
    drawingStorageConfig: "\u63D2\u4EF6\u914D\u7F6E\u6587\u4EF6\u5939\uFF08\u9ED8\u8BA4\uFF09",
    drawingStorageNoteSubfolder: "\u5F53\u524D\u6587\u4EF6\u5939 / notedraw",
    drawingStorageNoteFolder: "\u5F53\u524D\u6587\u4EF6\u5939",
    drawingStorageEmbedded: "\u5F53\u524D Markdown \u6587\u4EF6\uFF08\u9690\u85CF\uFF09",
    shareNoteDrawFile: "\u5206\u4EAB NoteDraw \u6587\u4EF6",
    sharePreparing: "\u6B63\u5728\u6253\u5305\u7B14\u8BB0\u3001NoteDraw \u6570\u636E\u548C\u94FE\u63A5\u8D44\u6E90\u2026\u2026",
    shareReady: "NoteDraw \u6587\u4EF6\u5DF2\u5C31\u7EEA\uFF0C\u5305\u542B {count} \u4E2A\u8D44\u6E90\u3002",
    shareDownloaded: "\u7CFB\u7EDF\u5206\u4EAB\u4E0D\u53EF\u7528\uFF0C\u5DF2\u4E0B\u8F7D NoteDraw Markdown \u6587\u4EF6\u3002",
    shareFailed: "NoteDraw \u6587\u4EF6\u6253\u5305\u5931\u8D25\u3002",
    defaultPenColor: "\u9ED8\u8BA4\u7B14\u989C\u8272",
    defaultPenColorDesc: "\u65B0\u7B14\u753B\u7684\u521D\u59CB\u989C\u8272\u3002",
    defaultPenWidth: "\u9ED8\u8BA4\u7B14\u5BBD",
    defaultPenWidthDesc: "\u521D\u59CB\u7B14\u5BBD\u3002",
    defaultPenOpacity: "\u9ED8\u8BA4\u7B14\u900F\u660E\u5EA6",
    defaultPenOpacityDesc: "\u521D\u59CB\u7B14\u900F\u660E\u5EA6\u3002",
    defaultWatercolorColor: "\u9ED8\u8BA4\u6C34\u5F69\u989C\u8272",
    defaultWatercolorColorDesc: "\u65B0\u6C34\u5F69\u7B14\u753B\u7684\u521D\u59CB\u989C\u8272\u3002",
    defaultWatercolorWidth: "\u9ED8\u8BA4\u6C34\u5F69\u7B14\u5BBD",
    defaultWatercolorWidthDesc: "\u521D\u59CB\u6C34\u5F69\u7B14\u5BBD\u3002",
    defaultWatercolorOpacity: "\u9ED8\u8BA4\u6C34\u5F69\u900F\u660E\u5EA6",
    defaultWatercolorOpacityDesc: "\u521D\u59CB\u6C34\u5F69\u900F\u660E\u5EA6\u3002",
    toolbarTopOffset: "\u5DE5\u5177\u680F\u9876\u90E8\u504F\u79FB",
    toolbarTopOffsetDesc: "\u8DDD\u79BB Obsidian \u9876\u90E8\u680F\u7684\u989D\u5916\u50CF\u7D20\u3002",
    frameCornerRadius: "\u6846\u5706\u89D2",
    frameCornerRadiusDesc: "\u5143\u7D20\u6846\u3001\u9009\u62E9\u6846\u548C\u9501\u5B9A\u96C6\u6846\u7684\u5706\u89D2\u5927\u5C0F\u3002\u8BBE\u4E3A 0 \u65F6\u4F7F\u7528\u76F4\u89D2\u3002",
    longPressMs: "\u957F\u6309\u5EF6\u8FDF",
    longPressMsDesc: "\u957F\u6309\u6253\u5F00\u4E8C\u7EA7\u63A7\u5236\u7684\u7B49\u5F85\u65F6\u95F4\u3002",
    selectTapDistance: "\u70B9\u51FB\u5BB9\u5DEE",
    selectTapDistanceDesc: "\u79FB\u52A8\u591A\u5C11\u50CF\u7D20\u540E\u4ECE\u70B9\u51FB\u53D8\u4E3A\u62D6\u52A8\u6216\u6D82\u9E26\u3002",
    selectStrokePadding: "\u9009\u62E9\u547D\u4E2D\u8303\u56F4",
    selectStrokePaddingDesc: "\u5143\u7D20\u5468\u56F4\u989D\u5916\u53EF\u70B9\u8303\u56F4\uFF0C\u8D8A\u5927\u8D8A\u5BB9\u6613\u9009\u4E2D\u3002",
    selectedStrokeAlpha: "\u9009\u4E2D\u5143\u7D20\u900F\u660E\u5EA6",
    selectedStrokeAlphaDesc: "\u5143\u7D20\u88AB\u9009\u4E2D\u7F16\u8F91\u65F6\u7684\u9884\u89C8\u900F\u660E\u5EA6\u3002",
    drawingInterpolationStep: "\u7B14\u753B\u5E73\u6ED1",
    drawingInterpolationStepDesc: "\u6570\u503C\u8D8A\u5C0F\uFF0C\u91C7\u6837\u70B9\u4E4B\u95F4\u8865\u70B9\u8D8A\u591A\uFF0C\u7EBF\u6761\u8D8A\u987A\u3002",
    drawingMinPointDistance: "\u8F93\u5165\u91C7\u6837\u95F4\u8DDD",
    drawingMinPointDistanceDesc: "\u6D82\u9E26\u65F6\u4FDD\u7559\u65B0\u70B9\u7684\u6700\u5C0F\u8DDD\u79BB\uFF0C\u8D8A\u5C0F\u8D8A\u7EC6\u817B\u3002",
    drawingCompactDistance: "\u4FDD\u5B58\u538B\u7F29",
    drawingCompactDistanceDesc: "\u81EA\u52A8\u4FDD\u5B58\u65F6\u7684\u70B9\u4F4D\u538B\u7F29\u5F3A\u5EA6\uFF0C\u8D8A\u5C0F\u8D8A\u4FDD\u7559\u7EC6\u8282\u3002",
    autoSaveDelayMs: "\u81EA\u52A8\u4FDD\u5B58\u5EF6\u8FDF",
    autoSaveDelayMsDesc: "\u6D82\u9E26\u6539\u53D8\u540E\u5199\u5165\u63D2\u4EF6\u6570\u636E\u6587\u4EF6\u5939\u524D\u7684\u7B49\u5F85\u65F6\u95F4\u3002",
    resetBrushDefaults: "\u91CD\u7F6E\u753B\u7B14\u9ED8\u8BA4\u503C",
    resetBrushDefaultsDesc: "\u6062\u590D\u7B14\u548C\u6C34\u5F69\u7B14\u7684\u5185\u7F6E\u9ED8\u8BA4\u989C\u8272\u3001\u5927\u5C0F\u548C\u900F\u660E\u5EA6\u3002",
    resetLayoutDefaults: "\u91CD\u7F6E\u5E03\u5C40\u548C\u4EA4\u4E92",
    resetLayoutDefaultsDesc: "\u6062\u590D\u5DE5\u5177\u680F\u3001\u9009\u62E9\u3001\u5E73\u6ED1\u548C\u81EA\u52A8\u4FDD\u5B58\u7684\u9ED8\u8BA4\u503C\u3002",
    reset: "\u91CD\u7F6E",
    debugLog: "\u8C03\u8BD5\u65E5\u5FD7",
    debugLogDesc: "\u4EC5\u6392\u67E5\u95EE\u9898\u65F6\uFF0C\u628A\u6587\u5B57\u4FDD\u5B58\u8BCA\u65AD\u5199\u5165\u63D2\u4EF6\u6587\u4EF6\u5939\u3002",
    supportTitle: "\u652F\u6301 NoteDraw / \u53CC\u7801",
    supportSubtitle: "\u53EF\u7528\u652F\u4ED8\u5B9D\u6216\u5E01\u5B89\u626B\u7801\u652F\u6301\u540E\u7EED\u7EF4\u62A4\u3002",
    supportCodeAlipay: "\u652F\u4ED8\u5B9D",
    supportCodeBinance: "\u5E01\u5B89",
    bringToFront: "\u7F6E\u9876",
    sendToBack: "\u7F6E\u5E95",
    selectRelatedElements: "\u9009\u62E9\u5173\u8054\u5143\u7D20",
    moveForward: "\u4E0A\u79FB\u4E00\u5C42",
    moveBackward: "\u4E0B\u79FB\u4E00\u5C42",
    lockElement: "\u9501\u5B9A",
    unlockElement: "\u89E3\u9501",
    copyElement: "\u590D\u5236\u5143\u7D20/\u94FE\u63A5",
    copyElementLink: "\u590D\u5236\u5143\u7D20\u94FE\u63A5",
    pasteElement: "\u7C98\u8D34\u5143\u7D20",
    insertIntoNote: "\u63D2\u5165\u7B14\u8BB0",
    removeFromNote: "\u53D6\u6D88\u63D2\u5165\u7B14\u8BB0",
    convertToNoteFlow: "\u8F6C\u4E3A NoteFlow \u5143\u7D20",
    convertToFloating: "\u8F6C\u4E3A\u60AC\u6D6E\u5143\u7D20",
    toggleFlowMode: "\u5207\u6362 NoteFlow / \u60AC\u6D6E",
    copiedElements: "\u5DF2\u590D\u5236 {count} \u4E2A\u5143\u7D20\u3002",
    copiedElementLink: "\u5DF2\u590D\u5236 NoteDraw \u5143\u7D20\u94FE\u63A5\u3002",
    elementLinkLabel: "NoteDraw \u5143\u7D20",
    elementLinkTargetNotFound: "\u6CA1\u6709\u627E\u5230\u94FE\u63A5\u7684 NoteDraw \u5143\u7D20\u3002",
    pastedElements: "\u5DF2\u7C98\u8D34 {count} \u4E2A\u5143\u7D20\u3002",
    nothingToPaste: "\u8FD8\u6CA1\u6709\u590D\u5236 NoteDraw \u5143\u7D20\u3002"
  },
  "zh-TW": {
    toggleCommand: "\u5207\u63DB\u95B1\u8B80\u7DE8\u8F2F\u548C\u5857\u9D09\u6A21\u5F0F",
    openNoteOrWebviewFirst: "\u8ACB\u5148\u958B\u555F\u7B46\u8A18\u6216\u7DB2\u9801\u8996\u5716\u3002",
    failedSaveDrawing: "\u5857\u9D09\u8CC7\u6599\u5132\u5B58\u5931\u6557\u3002",
    failedImportFile: "\u532F\u5165\u6A94\u6848\u5931\u6557\u3002",
    editTextDraw: "\u7DE8\u8F2F\u6587\u5B57 / \u5857\u9D09",
    editTextDrawHidden: "\u7DE8\u8F2F\u6587\u5B57 / \u5857\u9D09\uFF08\u5857\u9D09\u5DF2\u96B1\u85CF\uFF09",
    editWebviewDraw: "\u7DE8\u8F2F\u7DB2\u9801 / \u5857\u9D09",
    selectDrawings: "\u9078\u64C7\u5143\u7D20",
    editMarkdownTool: "\u7DE8\u8F2F MD",
    convertToNoteFlow: "\u8F49\u70BA NoteFlow \u5143\u7D20",
    convertToFloating: "\u8F49\u70BA\u6D6E\u52D5\u5143\u7D20",
    toggleFlowMode: "\u5207\u63DB NoteFlow / \u6D6E\u52D5",
    pen: "\u7B46",
    watercolorBrush: "\u6C34\u5F69\u7B46",
    floatingText: "\u6D6E\u52D5\u6587\u5B57",
    undoLastDrawing: "\u5FA9\u539F\u4E0A\u4E00\u7B46",
    redoDrawing: "\u91CD\u505A\u5857\u9D09",
    deleteSelectedDrawing: "\u522A\u9664\u9078\u53D6\u5143\u7D20",
    penSettings: "\u756B\u7B46\u8A2D\u5B9A",
    advancedColor: "\u9032\u968E\u984F\u8272",
    noColor: "\u7121\u8272",
    blockBackground: "\u76D2\u5B50\u5E95\u8272",
    clearBlockBackground: "\u6E05\u9664\u76D2\u5B50\u5E95\u8272",
    moveMarkdownBlock: "\u79FB\u52D5\u6216\u6392\u5217 Markdown \u5340\u584A",
    failedMoveMarkdownBlock: "Markdown \u5340\u584A\u79FB\u52D5\u5931\u6557\u3002",
    boxElements: "\u6846\u6210\u76D2\u5B50\u7FA4\u7D44",
    fillBoxElements: "\u5BE6\u5FC3\u6846",
    unboxElements: "\u53D6\u6D88\u76D2\u5B50\u6846",
    floatMarkdownBlock: "\u8A2D\u70BA\u6D6E\u52D5 Markdown \u5340\u584A",
    penWidth: "\u7B46\u5BEC",
    penOpacity: "\u7B46\u900F\u660E\u5EA6",
    elementSize: "\u5927\u5C0F",
    elementOpacity: "\u900F\u660E\u5EA6",
    textGroup: "\u6587\u5B57\u9023\u7D50",
    buttonGroup: "\u6309\u9215/\u9023\u63A5",
    textPlain: "\u666E\u901A\u6587\u5B57",
    title: "\u6A19\u984C",
    code: "\u7A0B\u5F0F\u78BC",
    button: "\u6309\u9215",
    primaryButton: "\u4E3B\u6309\u9215",
    outlineButton: "\u77E9\u5F62",
    pillButton: "\u5BE6\u5FC3\u77E9\u5F62",
    arrowUp: "\u4E0A",
    arrowDown: "\u4E0B",
    arrowLeft: "\u5DE6",
    arrowRight: "\u53F3",
    arrow: "\u7BAD\u982D",
    fileTag: "\u6A94\u6848\u6A19\u7C64",
    importGroup: "\u532F\u5165",
    image: "\u5716\u7247",
    video: "\u5F71\u7247",
    file: "\u6A94\u6848",
    previewGroup: "\u9810\u89BD",
    markdown: "MD",
    html: "HTML",
    note: "\u7B46\u8A18",
    bold: "\u7C97\u9AD4",
    italic: "\u659C\u9AD4",
    underline: "\u5E95\u7DDA",
    inlineCode: "\u884C\u5167\u7A0B\u5F0F\u78BC",
    keyboardTag: "\u9375\u76E4\u6A19\u7C64",
    superscript: "\u4E0A\u6A19",
    subscript: "\u4E0B\u6A19",
    codeBlock: "\u7A0B\u5F0F\u78BC\u5340\u584A",
    highlight: "\u9192\u76EE\u63D0\u793A",
    insertBreak: "\u63DB\u884C",
    clearFormat: "\u6E05\u9664\u683C\u5F0F",
    textColor: "\u6587\u5B57\u984F\u8272",
    highlightColor: "\u9192\u76EE\u984F\u8272",
    textSize: "\u5B57\u7D1A",
    size: "\u5B57\u7D1A",
    movePanel: "\u79FB\u52D5\u9762\u677F",
    useColor: "\u4F7F\u7528\u984F\u8272 {color}",
    settingsSectionInterface: "\u4ECB\u9762",
    settingsSectionPen: "\u7B46",
    settingsSectionWatercolor: "\u6C34\u5F69\u7B46",
    settingsSectionInteraction: "\u4E92\u52D5",
    settingsSectionPerformance: "\u6548\u80FD",
    settingsSectionLayout: "\u4F48\u5C40",
    settingsSectionStorage: "\u8CC7\u6599\u5132\u5B58\u8207\u5206\u4EAB",
    settingsSectionDiagnostics: "\u8A3A\u65B7",
    settingsSectionSupport: "\u652F\u6301\u4F5C\u8005",
    settingsLanguage: "\u8A9E\u8A00",
    settingsLanguageDesc: "\u63D2\u4EF6\u4ECB\u9762\u8A9E\u8A00\u3002\u81EA\u52D5\u6A21\u5F0F\u6703\u76E1\u91CF\u8DDF\u96A8 Obsidian\u3002",
    languageAuto: "\u81EA\u52D5",
    drawingStorageMode: "NoteDraw \u8CC7\u6599\u4F4D\u7F6E",
    drawingStorageModeDesc: "\u8A2D\u5B9A\u6BCF\u7BC7\u7B46\u8A18\u7684 NoteDraw \u8CC7\u6599\u6A94\u4F4D\u7F6E\u3002\u9810\u8A2D\u4ECD\u70BA\u63D2\u4EF6\u8A2D\u5B9A\u8CC7\u6599\u593E\u3002",
    drawingStorageConfig: "\u63D2\u4EF6\u8A2D\u5B9A\u8CC7\u6599\u593E\uFF08\u9810\u8A2D\uFF09",
    drawingStorageNoteSubfolder: "\u76EE\u524D\u8CC7\u6599\u593E / notedraw",
    drawingStorageNoteFolder: "\u76EE\u524D\u8CC7\u6599\u593E",
    drawingStorageEmbedded: "\u76EE\u524D Markdown \u6A94\u6848\uFF08\u96B1\u85CF\uFF09",
    shareNoteDrawFile: "\u5206\u4EAB NoteDraw \u6A94\u6848",
    sharePreparing: "\u6B63\u5728\u5C01\u88DD\u7B46\u8A18\u3001NoteDraw \u8CC7\u6599\u8207\u9023\u7D50\u8CC7\u6E90\u2026\u2026",
    shareReady: "NoteDraw \u6A94\u6848\u5DF2\u5C31\u7DD2\uFF0C\u5305\u542B {count} \u500B\u8CC7\u6E90\u3002",
    shareDownloaded: "\u7CFB\u7D71\u5206\u4EAB\u4E0D\u53EF\u7528\uFF0C\u5DF2\u4E0B\u8F09 NoteDraw Markdown \u6A94\u6848\u3002",
    shareFailed: "NoteDraw \u6A94\u6848\u5C01\u88DD\u5931\u6557\u3002",
    defaultPenColor: "\u9810\u8A2D\u7B46\u8272",
    defaultPenColorDesc: "\u65B0\u7B46\u756B\u7684\u521D\u59CB\u984F\u8272\u3002",
    defaultPenWidth: "\u9810\u8A2D\u7B46\u5BEC",
    defaultPenWidthDesc: "\u521D\u59CB\u7B46\u5BEC\u3002",
    defaultPenOpacity: "\u9810\u8A2D\u7B46\u900F\u660E\u5EA6",
    defaultPenOpacityDesc: "\u521D\u59CB\u7B46\u900F\u660E\u5EA6\u3002",
    defaultWatercolorColor: "\u9810\u8A2D\u6C34\u5F69\u984F\u8272",
    defaultWatercolorColorDesc: "\u65B0\u6C34\u5F69\u7B46\u756B\u7684\u521D\u59CB\u984F\u8272\u3002",
    defaultWatercolorWidth: "\u9810\u8A2D\u6C34\u5F69\u7B46\u5BEC",
    defaultWatercolorWidthDesc: "\u521D\u59CB\u6C34\u5F69\u7B46\u5BEC\u3002",
    defaultWatercolorOpacity: "\u9810\u8A2D\u6C34\u5F69\u900F\u660E\u5EA6",
    defaultWatercolorOpacityDesc: "\u521D\u59CB\u6C34\u5F69\u900F\u660E\u5EA6\u3002",
    toolbarTopOffset: "\u5DE5\u5177\u5217\u9802\u90E8\u504F\u79FB",
    toolbarTopOffsetDesc: "\u8DDD\u96E2 Obsidian \u9802\u90E8\u5217\u7684\u984D\u5916\u50CF\u7D20\u3002",
    frameCornerRadius: "\u6846\u5713\u89D2",
    frameCornerRadiusDesc: "\u5143\u7D20\u6846\u3001\u9078\u53D6\u6846\u548C\u9396\u5B9A\u96C6\u6846\u7684\u5713\u89D2\u5927\u5C0F\u3002\u8A2D\u70BA 0 \u6642\u4F7F\u7528\u76F4\u89D2\u3002",
    longPressMs: "\u9577\u6309\u5EF6\u9072",
    longPressMsDesc: "\u9577\u6309\u958B\u555F\u6B21\u7D1A\u63A7\u5236\u7684\u7B49\u5F85\u6642\u9593\u3002",
    selectTapDistance: "\u9EDE\u64CA\u5BB9\u5DEE",
    selectTapDistanceDesc: "\u79FB\u52D5\u591A\u5C11\u50CF\u7D20\u5F8C\u5F9E\u9EDE\u64CA\u8B8A\u6210\u62D6\u52D5\u6216\u5857\u9D09\u3002",
    selectStrokePadding: "\u9078\u53D6\u547D\u4E2D\u7BC4\u570D",
    selectStrokePaddingDesc: "\u5143\u7D20\u5468\u570D\u984D\u5916\u53EF\u9EDE\u7BC4\u570D\uFF0C\u8D8A\u5927\u8D8A\u5BB9\u6613\u9078\u4E2D\u3002",
    selectedStrokeAlpha: "\u9078\u4E2D\u5143\u7D20\u900F\u660E\u5EA6",
    selectedStrokeAlphaDesc: "\u5143\u7D20\u88AB\u9078\u4E2D\u7DE8\u8F2F\u6642\u7684\u9810\u89BD\u900F\u660E\u5EA6\u3002",
    drawingInterpolationStep: "\u7B46\u756B\u5E73\u6ED1",
    drawingInterpolationStepDesc: "\u6578\u503C\u8D8A\u5C0F\uFF0C\u63A1\u6A23\u9EDE\u4E4B\u9593\u88DC\u9EDE\u8D8A\u591A\uFF0C\u7DDA\u689D\u8D8A\u9806\u3002",
    drawingMinPointDistance: "\u8F38\u5165\u63A1\u6A23\u9593\u8DDD",
    drawingMinPointDistanceDesc: "\u5857\u9D09\u6642\u4FDD\u7559\u65B0\u9EDE\u7684\u6700\u5C0F\u8DDD\u96E2\uFF0C\u8D8A\u5C0F\u8D8A\u7D30\u81A9\u3002",
    drawingCompactDistance: "\u5132\u5B58\u58D3\u7E2E",
    drawingCompactDistanceDesc: "\u81EA\u52D5\u5132\u5B58\u6642\u7684\u9EDE\u4F4D\u58D3\u7E2E\u5F37\u5EA6\uFF0C\u8D8A\u5C0F\u8D8A\u4FDD\u7559\u7D30\u7BC0\u3002",
    autoSaveDelayMs: "\u81EA\u52D5\u5132\u5B58\u5EF6\u9072",
    autoSaveDelayMsDesc: "\u5857\u9D09\u6539\u8B8A\u5F8C\u5BEB\u5165\u63D2\u4EF6\u8CC7\u6599\u593E\u524D\u7684\u7B49\u5F85\u6642\u9593\u3002",
    resetBrushDefaults: "\u91CD\u7F6E\u756B\u7B46\u9810\u8A2D\u503C",
    resetBrushDefaultsDesc: "\u6062\u5FA9\u7B46\u548C\u6C34\u5F69\u7B46\u7684\u5167\u5EFA\u9810\u8A2D\u984F\u8272\u3001\u5927\u5C0F\u548C\u900F\u660E\u5EA6\u3002",
    resetLayoutDefaults: "\u91CD\u7F6E\u4F48\u5C40\u548C\u4E92\u52D5",
    resetLayoutDefaultsDesc: "\u6062\u5FA9\u5DE5\u5177\u5217\u3001\u9078\u53D6\u3001\u5E73\u6ED1\u548C\u81EA\u52D5\u5132\u5B58\u7684\u9810\u8A2D\u503C\u3002",
    reset: "\u91CD\u7F6E",
    debugLog: "\u9664\u932F\u65E5\u8A8C",
    debugLogDesc: "\u50C5\u6392\u67E5\u554F\u984C\u6642\uFF0C\u5C07\u6587\u5B57\u5132\u5B58\u8A3A\u65B7\u5BEB\u5165\u63D2\u4EF6\u8CC7\u6599\u593E\u3002",
    supportTitle: "\u652F\u6301 NoteDraw / \u96D9\u78BC",
    supportSubtitle: "\u53EF\u7528\u652F\u4ED8\u5BF6\u6216\u5E63\u5B89\u6383\u78BC\u652F\u6301\u5F8C\u7E8C\u7DAD\u8B77\u3002",
    supportCodeAlipay: "\u652F\u4ED8\u5BF6",
    supportCodeBinance: "\u5E63\u5B89",
    bringToFront: "\u7F6E\u9802",
    sendToBack: "\u7F6E\u5E95",
    moveForward: "\u4E0A\u79FB\u4E00\u5C64",
    moveBackward: "\u4E0B\u79FB\u4E00\u5C64",
    lockElement: "\u9396\u5B9A",
    unlockElement: "\u89E3\u9396"
  },
  ug: {
    toggleCommand: "\u0626\u0648\u0642\u06C7\u0634 \u062A\u06D5\u06BE\u0631\u0649\u0631 \u06CB\u06D5 \u0633\u0649\u0632\u0649\u0634 \u06BE\u0627\u0644\u0649\u062A\u0649\u0646\u0649 \u0626\u0627\u0644\u0645\u0627\u0634\u062A\u06C7\u0631\u06C7\u0634",
    openNoteOrWebviewFirst: "\u0626\u0627\u06CB\u06CB\u0627\u0644 \u062E\u0627\u062A\u0649\u0631\u06D5 \u064A\u0627\u0643\u0649 \u062A\u0648\u0631 \u0643\u06C6\u0632\u0646\u06D5\u0643\u0649\u0646\u0649 \u0626\u06D0\u0686\u0649\u06AD.",
    failedSaveDrawing: "\u0633\u0649\u0632\u0649\u0634 \u0633\u0627\u0646\u0644\u0649\u0642 \u0645\u06D5\u0644\u06C7\u0645\u0627\u062A\u0649\u0646\u0649 \u0633\u0627\u0642\u0644\u0627\u0634 \u0645\u06D5\u063A\u0644\u06C7\u067E \u0628\u0648\u0644\u062F\u0649.",
    failedImportFile: "\u06BE\u06C6\u062C\u062C\u06D5\u062A \u0643\u0649\u0631\u06AF\u06C8\u0632\u06C8\u0634 \u0645\u06D5\u063A\u0644\u06C7\u067E \u0628\u0648\u0644\u062F\u0649.",
    editTextDraw: "\u062A\u06D0\u0643\u0649\u0633\u062A \u062A\u06D5\u06BE\u0631\u0649\u0631\u0644\u06D5\u0634 / \u0633\u0649\u0632\u0649\u0634",
    editTextDrawHidden: "\u062A\u06D0\u0643\u0649\u0633\u062A \u062A\u06D5\u06BE\u0631\u0649\u0631\u0644\u06D5\u0634 / \u0633\u0649\u0632\u0649\u0634 (\u0633\u0649\u0632\u0649\u0634 \u064A\u0648\u0634\u06C7\u0631\u06C7\u0644\u063A\u0627\u0646)",
    editWebviewDraw: "\u062A\u0648\u0631 \u0628\u06D5\u062A\u0646\u0649 \u062A\u06D5\u06BE\u0631\u0649\u0631\u0644\u06D5\u0634 / \u0633\u0649\u0632\u0649\u0634",
    selectDrawings: "\u0626\u06D0\u0644\u06D0\u0645\u06D0\u0646\u062A \u062A\u0627\u0644\u0644\u0627\u0634",
    editMarkdownTool: "MD \u062A\u06D5\u06BE\u0631\u0649\u0631\u0644\u06D5\u0634",
    pen: "\u0642\u06D5\u0644\u06D5\u0645",
    laserPen: "\u0644\u0627\u0632\u06D0\u0631 \u0643\u06C6\u0631\u0633\u06D5\u062A\u0643\u06C8\u0686",
    watercolorBrush: "\u0633\u06C7 \u0628\u0648\u064A\u0627\u0642 \u0642\u06D5\u0644\u06D5\u0645",
    floatingText: "\u0644\u06D5\u064A\u0644\u06D5\u067E \u062A\u06C7\u0631\u063A\u0627\u0646 \u062A\u06D0\u0643\u0649\u0633\u062A",
    undoLastDrawing: "\u0626\u0627\u062E\u0649\u0631\u0642\u0649 \u0633\u0649\u0632\u0649\u0634\u0646\u0649 \u0642\u0627\u064A\u062A\u06C7\u0631\u06C7\u0634",
    redoDrawing: "\u0642\u0627\u064A\u062A\u0627 \u0642\u0649\u0644\u0649\u0634",
    deleteSelectedDrawing: "\u062A\u0627\u0644\u0644\u0627\u0646\u063A\u0627\u0646\u0646\u0649 \u0626\u06C6\u0686\u06C8\u0631\u06C8\u0634",
    penSettings: "\u0642\u06D5\u0644\u06D5\u0645 \u062A\u06D5\u06AD\u0634\u0649\u0643\u0649",
    advancedColor: "\u062A\u06D5\u067E\u0633\u0649\u0644\u0649\u064A \u0631\u06D5\u06AD",
    noColor: "\u0631\u06D5\u06AD\u0633\u0649\u0632",
    penWidth: "\u0642\u06D5\u0644\u06D5\u0645 \u0643\u06D5\u06AD\u0644\u0649\u0643\u0649",
    penOpacity: "\u0642\u06D5\u0644\u06D5\u0645 \u0633\u06C8\u0632\u06C8\u0643\u0644\u06C8\u0643\u0649",
    elementSize: "\u0686\u0648\u06AD\u0644\u06C7\u0642\u0649",
    elementOpacity: "\u0633\u06C8\u0632\u06C8\u0643\u0644\u06C8\u0643\u0649",
    textGroup: "\u062A\u06D0\u0643\u0649\u0633\u062A \u06CB\u06D5 \u0626\u06C7\u0644\u0627\u0646\u0645\u0649\u0644\u0627\u0631",
    buttonGroup: "\u0643\u06C7\u0646\u06C7\u067E\u0643\u0627/\u064A\u06C6\u0646\u0649\u0644\u0649\u0634",
    textPlain: "\u062A\u06D0\u0643\u0649\u0633\u062A",
    title: "\u0645\u0627\u06CB\u0632\u06C7",
    code: "\u0643\u0648\u062F",
    button: "\u0643\u06C7\u0646\u06C7\u067E\u0643\u0627",
    primaryButton: "\u0626\u0627\u0633\u0627\u0633\u0649\u064A",
    outlineButton: "Rectangle",
    pillButton: "Solid rectangle",
    arrowUp: "\u0626\u06C8\u0633\u062A\u0649",
    arrowDown: "\u0626\u0627\u0633\u062A\u0649",
    arrowLeft: "\u0633\u0648\u0644",
    arrowRight: "\u0626\u0648\u06AD",
    arrow: "Arrow",
    fileTag: "\u06BE\u06C6\u062C\u062C\u06D5\u062A \u0628\u06D5\u0644\u06AF\u0649\u0633\u0649",
    importGroup: "\u0643\u0649\u0631\u06AF\u06C8\u0632\u06C8\u0634",
    image: "\u0631\u06D5\u0633\u0649\u0645",
    video: "\u0633\u0649\u0646",
    file: "\u06BE\u06C6\u062C\u062C\u06D5\u062A",
    previewGroup: "\u0626\u0627\u0644\u062F\u0649\u0646 \u0643\u06C6\u0631\u06C8\u0634",
    markdown: "MD",
    html: "HTML",
    note: "\u062E\u0627\u062A\u0649\u0631\u06D5",
    bold: "\u062A\u0648\u0645",
    italic: "\u0642\u0649\u064A\u067E\u0627\u0634",
    underline: "\u0626\u0627\u0633\u062A\u0649 \u0633\u0649\u0632\u0649\u0642",
    inlineCode: "\u0642\u06C7\u0631 \u0626\u0649\u0686\u0649 \u0643\u0648\u062F",
    keyboardTag: "\u0643\u0649\u0631\u06AF\u06C8\u0632\u06AF\u06C8\u0686 \u0628\u06D5\u0644\u06AF\u0649\u0633\u0649",
    superscript: "\u0626\u06C8\u0633\u062A\u0643\u0649 \u0628\u06D5\u0644\u06AF\u06D5",
    subscript: "\u0626\u0627\u0633\u062A\u0649 \u0628\u06D5\u0644\u06AF\u06D5",
    codeBlock: "\u0643\u0648\u062F \u0628\u06C6\u0644\u0649\u0643\u0649",
    highlight: "\u064A\u0648\u0631\u06C7\u062A\u06C7\u0634",
    insertBreak: "\u0642\u06C7\u0631 \u0626\u0627\u0644\u0645\u0627\u0634\u062A\u06C7\u0631\u06C7\u0634",
    clearFormat: "\u0641\u0648\u0631\u0645\u0627\u062A\u0646\u0649 \u062A\u0627\u0632\u0649\u0644\u0627\u0634",
    textColor: "\u062A\u06D0\u0643\u0649\u0633\u062A \u0631\u06D5\u06AD\u06AF\u0649",
    highlightColor: "\u064A\u0648\u0631\u06C7\u062A\u06C7\u0634 \u0631\u06D5\u06AD\u06AF\u0649",
    textSize: "\u062E\u06D5\u062A \u0686\u0648\u06AD\u0644\u06C7\u0642\u0649",
    size: "\u0686\u0648\u06AD\u0644\u06C7\u0642",
    movePanel: "\u062A\u0627\u062E\u062A\u0649\u0646\u0649 \u064A\u06C6\u062A\u0643\u06D5\u0634",
    useColor: "{color} \u0631\u06D5\u06AD\u0646\u0649 \u0626\u0649\u0634\u0644\u0649\u062A\u0649\u0634",
    settingsSectionInterface: "\u0643\u06C6\u0631\u06C8\u0646\u0645\u06D5 \u064A\u06C8\u0632\u0649",
    settingsSectionPen: "\u0642\u06D5\u0644\u06D5\u0645",
    settingsSectionWatercolor: "\u0633\u06C7 \u0628\u0648\u064A\u0627\u0642 \u0642\u06D5\u0644\u06D5\u0645",
    settingsSectionLayout: "\u062C\u0627\u064A\u0644\u0649\u0634\u0649\u0634",
    settingsSectionDiagnostics: "\u062F\u0649\u0626\u0627\u06AF\u0646\u0648\u0632",
    settingsLanguage: "\u062A\u0649\u0644",
    settingsLanguageDesc: "\u0642\u0649\u0633\u062A\u06C7\u0631\u0645\u0627 \u0643\u06C6\u0631\u06C8\u0646\u0645\u06D5 \u064A\u06C8\u0632\u0649 \u062A\u0649\u0644\u0649. \u0626\u0627\u067E\u062A\u0648\u0645\u0627\u062A\u0649\u0643 \u06BE\u0627\u0644\u06D5\u062A Obsidian \u063A\u0627 \u0626\u06D5\u06AF\u0649\u0634\u0649\u062F\u06C7.",
    languageAuto: "\u0626\u0627\u067E\u062A\u0648\u0645\u0627\u062A\u0649\u0643",
    defaultPenColor: "\u0643\u06C6\u06AD\u06C8\u0644\u062F\u0649\u0643\u0649 \u0642\u06D5\u0644\u06D5\u0645 \u0631\u06D5\u06AD\u06AF\u0649",
    defaultPenColorDesc: "\u064A\u06D0\u06AD\u0649 \u0642\u06D5\u0644\u06D5\u0645 \u0633\u0649\u0632\u0649\u0642\u0649\u0646\u0649\u06AD \u062F\u06D5\u0633\u0644\u06D5\u067E\u0643\u0649 \u0631\u06D5\u06AD\u06AF\u0649.",
    defaultPenWidth: "\u0643\u06C6\u06AD\u06C8\u0644\u062F\u0649\u0643\u0649 \u0642\u06D5\u0644\u06D5\u0645 \u0643\u06D5\u06AD\u0644\u0649\u0643\u0649",
    defaultPenWidthDesc: "\u062F\u06D5\u0633\u0644\u06D5\u067E\u0643\u0649 \u0642\u06D5\u0644\u06D5\u0645 \u0643\u06D5\u06AD\u0644\u0649\u0643\u0649.",
    defaultPenOpacity: "\u0643\u06C6\u06AD\u06C8\u0644\u062F\u0649\u0643\u0649 \u0642\u06D5\u0644\u06D5\u0645 \u0633\u06C8\u0632\u06C8\u0643\u0644\u06C8\u0643\u0649",
    defaultPenOpacityDesc: "\u062F\u06D5\u0633\u0644\u06D5\u067E\u0643\u0649 \u0642\u06D5\u0644\u06D5\u0645 \u0633\u06C8\u0632\u06C8\u0643\u0644\u06C8\u0643\u0649.",
    defaultWatercolorColor: "\u0643\u06C6\u06AD\u06C8\u0644\u062F\u0649\u0643\u0649 \u0633\u06C7 \u0628\u0648\u064A\u0627\u0642 \u0631\u06D5\u06AD\u06AF\u0649",
    defaultWatercolorColorDesc: "\u064A\u06D0\u06AD\u0649 \u0633\u06C7 \u0628\u0648\u064A\u0627\u0642 \u0633\u0649\u0632\u0649\u0642\u0649\u0646\u0649\u06AD \u062F\u06D5\u0633\u0644\u06D5\u067E\u0643\u0649 \u0631\u06D5\u06AD\u06AF\u0649.",
    defaultWatercolorWidth: "\u0643\u06C6\u06AD\u06C8\u0644\u062F\u0649\u0643\u0649 \u0633\u06C7 \u0628\u0648\u064A\u0627\u0642 \u0643\u06D5\u06AD\u0644\u0649\u0643\u0649",
    defaultWatercolorWidthDesc: "\u062F\u06D5\u0633\u0644\u06D5\u067E\u0643\u0649 \u0633\u06C7 \u0628\u0648\u064A\u0627\u0642 \u0643\u06D5\u06AD\u0644\u0649\u0643\u0649.",
    defaultWatercolorOpacity: "\u0643\u06C6\u06AD\u06C8\u0644\u062F\u0649\u0643\u0649 \u0633\u06C7 \u0628\u0648\u064A\u0627\u0642 \u0633\u06C8\u0632\u06C8\u0643\u0644\u06C8\u0643\u0649",
    defaultWatercolorOpacityDesc: "\u062F\u06D5\u0633\u0644\u06D5\u067E\u0643\u0649 \u0633\u06C7 \u0628\u0648\u064A\u0627\u0642 \u0633\u06C8\u0632\u06C8\u0643\u0644\u06C8\u0643\u0649.",
    toolbarTopOffset: "\u0642\u0648\u0631\u0627\u0644 \u0628\u0627\u0644\u062F\u0627\u0642 \u0626\u06C8\u0633\u062A\u0649 \u0626\u0627\u0631\u0649\u0644\u0649\u0642\u0649",
    toolbarTopOffsetDesc: "Obsidian \u0628\u0627\u0634 \u0642\u0649\u0633\u0645\u0649\u062F\u0649\u0646 \u0642\u0648\u0634\u06C7\u0645\u0686\u06D5 \u067E\u0649\u0643\u0633\u06D0\u0644.",
    frameCornerRadius: "\u0631\u0627\u0645\u0643\u0627 \u0628\u06C7\u0644\u06C7\u06AD \u0631\u0627\u062F\u0649\u0626\u06C7\u0633\u0649",
    frameCornerRadiusDesc: "\u0626\u06D0\u0644\u06D0\u0645\u06D0\u0646\u062A\u060C \u062A\u0627\u0644\u0644\u0627\u0634 \u06CB\u06D5 \u06AF\u06C7\u0631\u06C7\u067E\u067E\u0627 \u0631\u0627\u0645\u0643\u0649\u0633\u0649\u0646\u0649\u06AD \u0628\u06C7\u0644\u06C7\u06AD\u0649. 0 \u0628\u0648\u0644\u0633\u0627 \u062A\u06C8\u0632 \u0628\u06C7\u0644\u06C7\u06AD.",
    debugLog: "\u0633\u0627\u0632\u0644\u0627\u0634 \u062E\u0627\u062A\u0649\u0631\u0649\u0633\u0649",
    debugLogDesc: "\u067E\u06D5\u0642\u06D5\u062A \u0645\u06D5\u0633\u0649\u0644\u06D5 \u062A\u06D5\u0643\u0634\u06C8\u0631\u06AF\u06D5\u0646\u062F\u06D5 \u062A\u06D0\u0643\u0649\u0633\u062A \u0633\u0627\u0642\u0644\u0627\u0634 \u062F\u0649\u0626\u0627\u06AF\u0646\u0648\u0632\u0649\u0646\u0649 \u0642\u0649\u0633\u062A\u06C7\u0631\u0645\u0627 \u0642\u0649\u0633\u0642\u06C7\u0686\u0649\u063A\u0627 \u064A\u0627\u0632\u0649\u062F\u06C7.",
    supportTitle: "NoteDraw \u0646\u0649 \u0642\u0648\u0644\u0644\u0627\u0634",
    supportSubtitle: "Alipay \u064A\u0627\u0643\u0649 Binance \u0626\u0627\u0631\u0642\u0649\u0644\u0649\u0642 \u0643\u0648\u062F\u0646\u0649 \u0633\u0649\u0643\u0627\u0646\u0649\u0631\u0644\u0627\u067E \u0642\u0648\u0644\u0644\u0649\u0633\u0649\u06AD\u0649\u0632 \u0628\u0648\u0644\u0649\u062F\u06C7.",
    supportCodeAlipay: "Alipay",
    supportCodeBinance: "Binance",
    bringToFront: "\u0626\u06D5\u06AD \u0626\u06C8\u0633\u062A\u0649\u06AF\u06D5",
    sendToBack: "\u0626\u06D5\u06AD \u0626\u0627\u0633\u062A\u0649\u063A\u0627",
    moveForward: "\u0626\u06C8\u0633\u062A\u0649\u06AF\u06D5 \u064A\u06C6\u062A\u0643\u06D5",
    moveBackward: "\u0626\u0627\u0633\u062A\u0649\u063A\u0627 \u064A\u06C6\u062A\u0643\u06D5",
    lockElement: "\u0642\u06C7\u0644\u06C7\u067E\u0644\u0627\u0634",
    unlockElement: "\u0642\u06C7\u0644\u06C7\u067E\u0646\u0649 \u0626\u06D0\u0686\u0649\u0634"
  },
  ru: {
    toggleCommand: "\u041F\u0435\u0440\u0435\u043A\u043B\u044E\u0447\u0438\u0442\u044C \u0440\u0435\u0434\u0430\u043A\u0442\u0438\u0440\u043E\u0432\u0430\u043D\u0438\u0435 \u043F\u0440\u0435\u0434\u043F\u0440\u043E\u0441\u043C\u043E\u0442\u0440\u0430 \u0438 \u0440\u0438\u0441\u043E\u0432\u0430\u043D\u0438\u0435",
    openNoteOrWebviewFirst: "\u0421\u043D\u0430\u0447\u0430\u043B\u0430 \u043E\u0442\u043A\u0440\u043E\u0439\u0442\u0435 \u0437\u0430\u043C\u0435\u0442\u043A\u0443 \u0438\u043B\u0438 webview.",
    failedSaveDrawing: "\u041D\u0435 \u0443\u0434\u0430\u043B\u043E\u0441\u044C \u0441\u043E\u0445\u0440\u0430\u043D\u0438\u0442\u044C \u0434\u0430\u043D\u043D\u044B\u0435 \u0440\u0438\u0441\u0443\u043D\u043A\u0430.",
    failedImportFile: "\u041D\u0435 \u0443\u0434\u0430\u043B\u043E\u0441\u044C \u0438\u043C\u043F\u043E\u0440\u0442\u0438\u0440\u043E\u0432\u0430\u0442\u044C \u0444\u0430\u0439\u043B.",
    editTextDraw: "\u0420\u0435\u0434\u0430\u043A\u0442\u0438\u0440\u043E\u0432\u0430\u0442\u044C \u0442\u0435\u043A\u0441\u0442 / \u0440\u0438\u0441\u043E\u0432\u0430\u0442\u044C",
    editTextDrawHidden: "\u0420\u0435\u0434\u0430\u043A\u0442\u0438\u0440\u043E\u0432\u0430\u0442\u044C \u0442\u0435\u043A\u0441\u0442 / \u0440\u0438\u0441\u043E\u0432\u0430\u0442\u044C (\u0440\u0438\u0441\u0443\u043D\u043A\u0438 \u0441\u043A\u0440\u044B\u0442\u044B)",
    editWebviewDraw: "\u0420\u0435\u0434\u0430\u043A\u0442\u0438\u0440\u043E\u0432\u0430\u0442\u044C webview / \u0440\u0438\u0441\u043E\u0432\u0430\u0442\u044C",
    selectDrawings: "\u0412\u044B\u0431\u0440\u0430\u0442\u044C \u044D\u043B\u0435\u043C\u0435\u043D\u0442\u044B",
    editMarkdownTool: "\u0420\u0435\u0434\u0430\u043A\u0442\u0438\u0440\u043E\u0432\u0430\u0442\u044C MD",
    pen: "\u041F\u0435\u0440\u043E",
    laserPen: "\u041B\u0430\u0437\u0435\u0440\u043D\u0430\u044F \u0443\u043A\u0430\u0437\u043A\u0430",
    watercolorBrush: "\u0410\u043A\u0432\u0430\u0440\u0435\u043B\u044C\u043D\u0430\u044F \u043A\u0438\u0441\u0442\u044C",
    floatingText: "\u041F\u043B\u0430\u0432\u0430\u044E\u0449\u0438\u0439 \u0442\u0435\u043A\u0441\u0442",
    undoLastDrawing: "\u041E\u0442\u043C\u0435\u043D\u0438\u0442\u044C \u043F\u043E\u0441\u043B\u0435\u0434\u043D\u0438\u0439 \u0440\u0438\u0441\u0443\u043D\u043E\u043A",
    redoDrawing: "\u041F\u043E\u0432\u0442\u043E\u0440\u0438\u0442\u044C \u0440\u0438\u0441\u0443\u043D\u043E\u043A",
    deleteSelectedDrawing: "\u0423\u0434\u0430\u043B\u0438\u0442\u044C \u0432\u044B\u0431\u0440\u0430\u043D\u043D\u043E\u0435",
    penSettings: "\u041D\u0430\u0441\u0442\u0440\u043E\u0439\u043A\u0438 \u043F\u0435\u0440\u0430",
    advancedColor: "\u0420\u0430\u0441\u0448\u0438\u0440\u0435\u043D\u043D\u044B\u0439 \u0446\u0432\u0435\u0442",
    noColor: "\u0411\u0435\u0437 \u0446\u0432\u0435\u0442\u0430",
    penWidth: "\u0422\u043E\u043B\u0449\u0438\u043D\u0430 \u043F\u0435\u0440\u0430",
    penOpacity: "\u041F\u0440\u043E\u0437\u0440\u0430\u0447\u043D\u043E\u0441\u0442\u044C \u043F\u0435\u0440\u0430",
    elementSize: "\u0420\u0430\u0437\u043C\u0435\u0440",
    elementOpacity: "\u041F\u0440\u043E\u0437\u0440\u0430\u0447\u043D\u043E\u0441\u0442\u044C",
    textGroup: "\u0422\u0435\u043A\u0441\u0442 \u0438 \u0441\u0441\u044B\u043B\u043A\u0438",
    buttonGroup: "\u041A\u043D\u043E\u043F\u043A\u0438/\u0441\u0442\u0440\u0435\u043B\u043A\u0438",
    textPlain: "\u0422\u0435\u043A\u0441\u0442",
    title: "\u0417\u0430\u0433\u043E\u043B\u043E\u0432\u043E\u043A",
    code: "\u041A\u043E\u0434",
    button: "\u041A\u043D\u043E\u043F\u043A\u0430",
    primaryButton: "\u041E\u0441\u043D\u043E\u0432\u043D\u0430\u044F",
    outlineButton: "\u041F\u0440\u044F\u043C\u043E\u0443\u0433\u043E\u043B\u044C\u043D\u0438\u043A",
    pillButton: "\u0417\u0430\u043F\u043E\u043B\u043D\u0435\u043D\u043D\u044B\u0439 \u043F\u0440\u044F\u043C\u043E\u0443\u0433\u043E\u043B\u044C\u043D\u0438\u043A",
    arrowUp: "\u0412\u0432\u0435\u0440\u0445",
    arrowDown: "\u0412\u043D\u0438\u0437",
    arrowLeft: "\u0412\u043B\u0435\u0432\u043E",
    arrowRight: "\u0412\u043F\u0440\u0430\u0432\u043E",
    arrow: "\u0421\u0442\u0440\u0435\u043B\u043A\u0430",
    fileTag: "\u041C\u0435\u0442\u043A\u0430 \u0444\u0430\u0439\u043B\u0430",
    importGroup: "\u0418\u043C\u043F\u043E\u0440\u0442",
    image: "\u0418\u0437\u043E\u0431\u0440\u0430\u0436\u0435\u043D\u0438\u0435",
    video: "\u0412\u0438\u0434\u0435\u043E",
    file: "\u0424\u0430\u0439\u043B",
    previewGroup: "\u041F\u0440\u0435\u0434\u043F\u0440\u043E\u0441\u043C\u043E\u0442\u0440",
    markdown: "MD",
    html: "HTML",
    note: "\u0417\u0430\u043C\u0435\u0442\u043A\u0430",
    bold: "\u0416\u0438\u0440\u043D\u044B\u0439",
    italic: "\u041A\u0443\u0440\u0441\u0438\u0432",
    underline: "\u041F\u043E\u0434\u0447\u0435\u0440\u043A\u0438\u0432\u0430\u043D\u0438\u0435",
    inlineCode: "\u0412\u0441\u0442\u0440\u043E\u0435\u043D\u043D\u044B\u0439 \u043A\u043E\u0434",
    keyboardTag: "\u0422\u0435\u0433 \u043A\u043B\u0430\u0432\u0438\u0430\u0442\u0443\u0440\u044B",
    superscript: "\u0412\u0435\u0440\u0445\u043D\u0438\u0439 \u0438\u043D\u0434\u0435\u043A\u0441",
    subscript: "\u041D\u0438\u0436\u043D\u0438\u0439 \u0438\u043D\u0434\u0435\u043A\u0441",
    codeBlock: "\u0411\u043B\u043E\u043A \u043A\u043E\u0434\u0430",
    highlight: "\u0412\u044B\u0434\u0435\u043B\u0435\u043D\u0438\u0435",
    insertBreak: "\u041F\u0435\u0440\u0435\u043D\u043E\u0441 \u0441\u0442\u0440\u043E\u043A\u0438",
    clearFormat: "\u041E\u0447\u0438\u0441\u0442\u0438\u0442\u044C \u0444\u043E\u0440\u043C\u0430\u0442\u0438\u0440\u043E\u0432\u0430\u043D\u0438\u0435",
    textColor: "\u0426\u0432\u0435\u0442 \u0442\u0435\u043A\u0441\u0442\u0430",
    highlightColor: "\u0426\u0432\u0435\u0442 \u0432\u044B\u0434\u0435\u043B\u0435\u043D\u0438\u044F",
    textSize: "\u0420\u0430\u0437\u043C\u0435\u0440 \u0442\u0435\u043A\u0441\u0442\u0430",
    size: "\u0420\u0430\u0437\u043C\u0435\u0440",
    movePanel: "\u041F\u0435\u0440\u0435\u043C\u0435\u0441\u0442\u0438\u0442\u044C \u043F\u0430\u043D\u0435\u043B\u044C",
    useColor: "\u0418\u0441\u043F\u043E\u043B\u044C\u0437\u043E\u0432\u0430\u0442\u044C \u0446\u0432\u0435\u0442 {color}",
    settingsSectionInterface: "\u0418\u043D\u0442\u0435\u0440\u0444\u0435\u0439\u0441",
    settingsSectionPen: "\u041F\u0435\u0440\u043E",
    settingsSectionWatercolor: "\u0410\u043A\u0432\u0430\u0440\u0435\u043B\u044C",
    settingsSectionLayout: "\u041C\u0430\u043A\u0435\u0442",
    settingsSectionDiagnostics: "\u0414\u0438\u0430\u0433\u043D\u043E\u0441\u0442\u0438\u043A\u0430",
    settingsLanguage: "\u042F\u0437\u044B\u043A",
    settingsLanguageDesc: "\u042F\u0437\u044B\u043A \u0438\u043D\u0442\u0435\u0440\u0444\u0435\u0439\u0441\u0430 \u043F\u043B\u0430\u0433\u0438\u043D\u0430. \u0410\u0432\u0442\u043E \u043F\u043E \u0432\u043E\u0437\u043C\u043E\u0436\u043D\u043E\u0441\u0442\u0438 \u0441\u043B\u0435\u0434\u0443\u0435\u0442 Obsidian.",
    languageAuto: "\u0410\u0432\u0442\u043E",
    defaultPenColor: "\u0426\u0432\u0435\u0442 \u043F\u0435\u0440\u0430 \u043F\u043E \u0443\u043C\u043E\u043B\u0447\u0430\u043D\u0438\u044E",
    defaultPenColorDesc: "\u041D\u0430\u0447\u0430\u043B\u044C\u043D\u044B\u0439 \u0446\u0432\u0435\u0442 \u043D\u043E\u0432\u044B\u0445 \u0448\u0442\u0440\u0438\u0445\u043E\u0432 \u043F\u0435\u0440\u0430.",
    defaultPenWidth: "\u0422\u043E\u043B\u0449\u0438\u043D\u0430 \u043F\u0435\u0440\u0430 \u043F\u043E \u0443\u043C\u043E\u043B\u0447\u0430\u043D\u0438\u044E",
    defaultPenWidthDesc: "\u041D\u0430\u0447\u0430\u043B\u044C\u043D\u0430\u044F \u0442\u043E\u043B\u0449\u0438\u043D\u0430 \u043F\u0435\u0440\u0430.",
    defaultPenOpacity: "\u041F\u0440\u043E\u0437\u0440\u0430\u0447\u043D\u043E\u0441\u0442\u044C \u043F\u0435\u0440\u0430 \u043F\u043E \u0443\u043C\u043E\u043B\u0447\u0430\u043D\u0438\u044E",
    defaultPenOpacityDesc: "\u041D\u0430\u0447\u0430\u043B\u044C\u043D\u0430\u044F \u043F\u0440\u043E\u0437\u0440\u0430\u0447\u043D\u043E\u0441\u0442\u044C \u043F\u0435\u0440\u0430.",
    defaultWatercolorColor: "\u0426\u0432\u0435\u0442 \u0430\u043A\u0432\u0430\u0440\u0435\u043B\u0438 \u043F\u043E \u0443\u043C\u043E\u043B\u0447\u0430\u043D\u0438\u044E",
    defaultWatercolorColorDesc: "\u041D\u0430\u0447\u0430\u043B\u044C\u043D\u044B\u0439 \u0446\u0432\u0435\u0442 \u0430\u043A\u0432\u0430\u0440\u0435\u043B\u044C\u043D\u044B\u0445 \u0448\u0442\u0440\u0438\u0445\u043E\u0432.",
    defaultWatercolorWidth: "\u0422\u043E\u043B\u0449\u0438\u043D\u0430 \u0430\u043A\u0432\u0430\u0440\u0435\u043B\u0438 \u043F\u043E \u0443\u043C\u043E\u043B\u0447\u0430\u043D\u0438\u044E",
    defaultWatercolorWidthDesc: "\u041D\u0430\u0447\u0430\u043B\u044C\u043D\u0430\u044F \u0442\u043E\u043B\u0449\u0438\u043D\u0430 \u0430\u043A\u0432\u0430\u0440\u0435\u043B\u0438.",
    defaultWatercolorOpacity: "\u041F\u0440\u043E\u0437\u0440\u0430\u0447\u043D\u043E\u0441\u0442\u044C \u0430\u043A\u0432\u0430\u0440\u0435\u043B\u0438 \u043F\u043E \u0443\u043C\u043E\u043B\u0447\u0430\u043D\u0438\u044E",
    defaultWatercolorOpacityDesc: "\u041D\u0430\u0447\u0430\u043B\u044C\u043D\u0430\u044F \u043F\u0440\u043E\u0437\u0440\u0430\u0447\u043D\u043E\u0441\u0442\u044C \u0430\u043A\u0432\u0430\u0440\u0435\u043B\u0438.",
    toolbarTopOffset: "\u0421\u043C\u0435\u0449\u0435\u043D\u0438\u0435 \u043F\u0430\u043D\u0435\u043B\u0438 \u0441\u0432\u0435\u0440\u0445\u0443",
    toolbarTopOffsetDesc: "\u0414\u043E\u043F\u043E\u043B\u043D\u0438\u0442\u0435\u043B\u044C\u043D\u044B\u0435 \u043F\u0438\u043A\u0441\u0435\u043B\u0438 \u043D\u0438\u0436\u0435 \u0437\u0430\u0433\u043E\u043B\u043E\u0432\u043A\u0430 Obsidian.",
    frameCornerRadius: "\u0421\u043A\u0440\u0443\u0433\u043B\u0435\u043D\u0438\u0435 \u0440\u0430\u043C\u043E\u043A",
    frameCornerRadiusDesc: "\u0420\u0430\u0434\u0438\u0443\u0441 \u0443\u0433\u043B\u043E\u0432 \u0440\u0430\u043C\u043E\u043A \u044D\u043B\u0435\u043C\u0435\u043D\u0442\u043E\u0432, \u0432\u044B\u0434\u0435\u043B\u0435\u043D\u0438\u044F \u0438 \u0433\u0440\u0443\u043F\u043F. \u041D\u043E\u043B\u044C \u043E\u0437\u043D\u0430\u0447\u0430\u0435\u0442 \u043F\u0440\u044F\u043C\u044B\u0435 \u0443\u0433\u043B\u044B.",
    debugLog: "\u0416\u0443\u0440\u043D\u0430\u043B \u043E\u0442\u043B\u0430\u0434\u043A\u0438",
    debugLogDesc: "\u0417\u0430\u043F\u0438\u0441\u044B\u0432\u0430\u0442\u044C \u0434\u0438\u0430\u0433\u043D\u043E\u0441\u0442\u0438\u043A\u0443 \u0441\u043E\u0445\u0440\u0430\u043D\u0435\u043D\u0438\u044F \u0442\u0435\u043A\u0441\u0442\u0430 \u0432 \u043F\u0430\u043F\u043A\u0443 \u043F\u043B\u0430\u0433\u0438\u043D\u0430 \u0442\u043E\u043B\u044C\u043A\u043E \u043F\u0440\u0438 \u043E\u0442\u043B\u0430\u0434\u043A\u0435.",
    supportTitle: "\u041F\u043E\u0434\u0434\u0435\u0440\u0436\u0430\u0442\u044C NoteDraw",
    supportSubtitle: "\u0421\u043A\u0430\u043D\u0438\u0440\u0443\u0439\u0442\u0435 \u0447\u0435\u0440\u0435\u0437 Alipay \u0438\u043B\u0438 Binance, \u0447\u0442\u043E\u0431\u044B \u043F\u043E\u0434\u0434\u0435\u0440\u0436\u0430\u0442\u044C \u0440\u0430\u0437\u0440\u0430\u0431\u043E\u0442\u043A\u0443.",
    supportCodeAlipay: "Alipay",
    supportCodeBinance: "Binance",
    bringToFront: "\u041D\u0430 \u043F\u0435\u0440\u0435\u0434\u043D\u0438\u0439 \u043F\u043B\u0430\u043D",
    sendToBack: "\u041D\u0430 \u0437\u0430\u0434\u043D\u0438\u0439 \u043F\u043B\u0430\u043D",
    moveForward: "\u0412\u044B\u0448\u0435",
    moveBackward: "\u041D\u0438\u0436\u0435",
    lockElement: "\u0417\u0430\u043A\u0440\u0435\u043F\u0438\u0442\u044C",
    unlockElement: "\u041E\u0442\u043A\u0440\u0435\u043F\u0438\u0442\u044C"
  }
};
Object.assign(I18N, {
  ar: Object.assign({}, I18N.en, {
    toggleCommand: "\u062A\u0628\u062F\u064A\u0644 \u0648\u0636\u0639 \u062A\u062D\u0631\u064A\u0631 \u0627\u0644\u0645\u0639\u0627\u064A\u0646\u0629 \u0648\u0627\u0644\u0631\u0633\u0645",
    openNoteOrWebviewFirst: "\u0627\u0641\u062A\u062D \u0645\u0644\u0627\u062D\u0638\u0629 \u0623\u0648 \u0639\u0631\u0636 \u0648\u064A\u0628 \u0623\u0648\u0644\u0627.",
    failedSaveDrawing: "\u0641\u0634\u0644 \u062D\u0641\u0638 \u0628\u064A\u0627\u0646\u0627\u062A \u0627\u0644\u0631\u0633\u0645.",
    failedImportFile: "\u0641\u0634\u0644 \u0627\u0633\u062A\u064A\u0631\u0627\u062F \u0627\u0644\u0645\u0644\u0641.",
    editTextDraw: "\u062A\u062D\u0631\u064A\u0631 \u0627\u0644\u0646\u0635 / \u0627\u0644\u0631\u0633\u0645",
    editTextDrawHidden: "\u062A\u062D\u0631\u064A\u0631 \u0627\u0644\u0646\u0635 / \u0627\u0644\u0631\u0633\u0645 (\u0627\u0644\u0631\u0633\u0648\u0645\u0627\u062A \u0645\u062E\u0641\u064A\u0629)",
    editWebviewDraw: "\u062A\u062D\u0631\u064A\u0631 \u0639\u0631\u0636 \u0627\u0644\u0648\u064A\u0628 / \u0627\u0644\u0631\u0633\u0645",
    selectDrawings: "\u062A\u062D\u062F\u064A\u062F \u0627\u0644\u0639\u0646\u0627\u0635\u0631",
    editMarkdownTool: "\u062A\u062D\u0631\u064A\u0631 MD",
    pen: "\u0642\u0644\u0645",
    watercolorBrush: "\u0641\u0631\u0634\u0627\u0629 \u0645\u0627\u0626\u064A\u0629",
    floatingText: "\u0646\u0635 \u0639\u0627\u0626\u0645",
    undoLastDrawing: "\u062A\u0631\u0627\u062C\u0639 \u0639\u0646 \u0622\u062E\u0631 \u0631\u0633\u0645",
    redoDrawing: "\u0625\u0639\u0627\u062F\u0629 \u0627\u0644\u0631\u0633\u0645",
    deleteSelectedDrawing: "\u062D\u0630\u0641 \u0627\u0644\u0645\u062D\u062F\u062F",
    penSettings: "\u0625\u0639\u062F\u0627\u062F\u0627\u062A \u0627\u0644\u0642\u0644\u0645",
    textGroup: "\u0627\u0644\u0646\u0635 \u0648\u0627\u0644\u0631\u0648\u0627\u0628\u0637",
    importGroup: "\u0627\u0633\u062A\u064A\u0631\u0627\u062F",
    image: "\u0635\u0648\u0631\u0629",
    video: "\u0641\u064A\u062F\u064A\u0648",
    file: "\u0645\u0644\u0641",
    previewGroup: "\u0645\u0639\u0627\u064A\u0646\u0629",
    bold: "\u063A\u0627\u0645\u0642",
    italic: "\u0645\u0627\u0626\u0644",
    underline: "\u062A\u062D\u062A\u0647 \u062E\u0637",
    highlight: "\u062A\u0645\u064A\u064A\u0632",
    clearFormat: "\u0645\u0633\u062D \u0627\u0644\u062A\u0646\u0633\u064A\u0642",
    settingsSectionInterface: "\u0627\u0644\u0648\u0627\u062C\u0647\u0629",
    settingsSectionPen: "\u0627\u0644\u0642\u0644\u0645",
    settingsSectionWatercolor: "\u0627\u0644\u0623\u0644\u0648\u0627\u0646 \u0627\u0644\u0645\u0627\u0626\u064A\u0629",
    settingsSectionLayout: "\u0627\u0644\u062A\u062E\u0637\u064A\u0637",
    frameCornerRadius: "\u0627\u0633\u062A\u062F\u0627\u0631\u0629 \u0625\u0637\u0627\u0631 \u0627\u0644\u0639\u0646\u0635\u0631",
    frameCornerRadiusDesc: "\u0627\u0633\u062A\u062F\u0627\u0631\u0629 \u0625\u0637\u0627\u0631\u0627\u062A \u0627\u0644\u0639\u0646\u0627\u0635\u0631 \u0648\u0627\u0644\u062A\u062D\u062F\u064A\u062F \u0648\u0627\u0644\u0645\u062C\u0645\u0648\u0639\u0627\u062A. \u0627\u0644\u0635\u0641\u0631 \u064A\u0639\u0646\u064A \u0632\u0648\u0627\u064A\u0627 \u0642\u0627\u0626\u0645\u0629.",
    settingsSectionDiagnostics: "\u0627\u0644\u062A\u0634\u062E\u064A\u0635",
    movePanel: "\u062A\u062D\u0631\u064A\u0643 \u0627\u0644\u0644\u0648\u062D\u0629",
    settingsLanguage: "\u0627\u0644\u0644\u063A\u0629",
    languageAuto: "\u062A\u0644\u0642\u0627\u0626\u064A",
    supportTitle: "\u062F\u0639\u0645 NoteDraw",
    supportSubtitle: "\u0627\u0645\u0633\u062D \u0639\u0628\u0631 Alipay \u0623\u0648 Binance \u0644\u062F\u0639\u0645 \u0627\u0644\u0635\u064A\u0627\u0646\u0629.",
    supportCodeAlipay: "Alipay",
    supportCodeBinance: "Binance"
  }),
  es: Object.assign({}, I18N.en, {
    toggleCommand: "Cambiar edici\xF3n de vista previa y dibujo",
    openNoteOrWebviewFirst: "Abre primero una nota o webview.",
    failedSaveDrawing: "No se pudieron guardar los datos de dibujo.",
    failedImportFile: "No se pudo importar el archivo.",
    editTextDraw: "Editar texto / dibujar",
    editTextDrawHidden: "Editar texto / dibujar (dibujos ocultos)",
    editWebviewDraw: "Editar webview / dibujar",
    selectDrawings: "Seleccionar elementos",
    editMarkdownTool: "Editar MD",
    pen: "Pluma",
    watercolorBrush: "Pincel acuarela",
    floatingText: "Texto flotante",
    undoLastDrawing: "Deshacer \xFAltimo dibujo",
    redoDrawing: "Rehacer dibujo",
    deleteSelectedDrawing: "Eliminar seleccionado",
    penSettings: "Ajustes de pluma",
    advancedColor: "Color avanzado",
    noColor: "Sin color",
    elementSize: "Tama\xF1o",
    elementOpacity: "Opacidad",
    textGroup: "Texto y enlaces",
    importGroup: "Importar",
    previewGroup: "Vista previa",
    bold: "Negrita",
    italic: "Cursiva",
    underline: "Subrayado",
    clearFormat: "Quitar formato",
    settingsSectionInterface: "Interfaz",
    settingsSectionPen: "Pluma",
    settingsSectionWatercolor: "Acuarela",
    settingsSectionLayout: "Dise\xF1o",
    frameCornerRadius: "Radio de las esquinas",
    frameCornerRadiusDesc: "Radio de los marcos de elementos, selecci\xF3n y grupos. Cero usa esquinas rectas.",
    settingsSectionDiagnostics: "Diagn\xF3stico",
    movePanel: "Mover panel",
    settingsLanguage: "Idioma",
    languageAuto: "Auto",
    supportTitle: "Apoyar NoteDraw",
    supportSubtitle: "Escanea con Alipay o Binance para apoyar el mantenimiento.",
    supportCodeAlipay: "Alipay",
    supportCodeBinance: "Binance"
  }),
  fr: Object.assign({}, I18N.en, {
    toggleCommand: "Basculer \xE9dition de l'aper\xE7u et dessin",
    openNoteOrWebviewFirst: "Ouvrez d'abord une note ou une webview.",
    failedSaveDrawing: "\xC9chec de l'enregistrement du dessin.",
    failedImportFile: "\xC9chec de l'import du fichier.",
    editTextDraw: "Modifier le texte / dessiner",
    editTextDrawHidden: "Modifier le texte / dessiner (dessins masqu\xE9s)",
    editWebviewDraw: "Modifier la webview / dessiner",
    selectDrawings: "S\xE9lectionner les \xE9l\xE9ments",
    editMarkdownTool: "Modifier MD",
    pen: "Stylo",
    watercolorBrush: "Pinceau aquarelle",
    floatingText: "Texte flottant",
    undoLastDrawing: "Annuler le dernier dessin",
    redoDrawing: "R\xE9tablir le dessin",
    deleteSelectedDrawing: "Supprimer la s\xE9lection",
    penSettings: "R\xE9glages du stylo",
    advancedColor: "Couleur avanc\xE9e",
    noColor: "Sans couleur",
    elementSize: "Taille",
    elementOpacity: "Opacit\xE9",
    textGroup: "Texte et liens",
    importGroup: "Importer",
    previewGroup: "Aper\xE7u",
    bold: "Gras",
    italic: "Italique",
    underline: "Soulign\xE9",
    clearFormat: "Effacer le format",
    settingsSectionInterface: "Interface",
    settingsSectionPen: "Stylo",
    settingsSectionWatercolor: "Aquarelle",
    settingsSectionLayout: "Disposition",
    frameCornerRadius: "Rayon des cadres",
    frameCornerRadiusDesc: "Rayon des cadres d'\xE9l\xE9ments, de s\xE9lection et de groupes. Z\xE9ro cr\xE9e des angles droits.",
    settingsSectionDiagnostics: "Diagnostics",
    movePanel: "D\xE9placer le panneau",
    settingsLanguage: "Langue",
    languageAuto: "Auto",
    supportTitle: "Soutenir NoteDraw",
    supportSubtitle: "Scannez avec Alipay ou Binance pour soutenir la maintenance.",
    supportCodeAlipay: "Alipay",
    supportCodeBinance: "Binance"
  }),
  de: Object.assign({}, I18N.en, {
    toggleCommand: "Vorschau-Bearbeitung und Zeichnen umschalten",
    openNoteOrWebviewFirst: "\xD6ffne zuerst eine Notiz oder Webview.",
    failedSaveDrawing: "Zeichnungsdaten konnten nicht gespeichert werden.",
    failedImportFile: "Datei konnte nicht importiert werden.",
    editTextDraw: "Text bearbeiten / zeichnen",
    editTextDrawHidden: "Text bearbeiten / zeichnen (Zeichnungen ausgeblendet)",
    editWebviewDraw: "Webview bearbeiten / zeichnen",
    selectDrawings: "Elemente ausw\xE4hlen",
    editMarkdownTool: "MD bearbeiten",
    pen: "Stift",
    watercolorBrush: "Aquarellpinsel",
    floatingText: "Schwebender Text",
    undoLastDrawing: "Letzte Zeichnung r\xFCckg\xE4ngig",
    redoDrawing: "Zeichnung wiederholen",
    deleteSelectedDrawing: "Auswahl l\xF6schen",
    penSettings: "Stifteinstellungen",
    advancedColor: "Erweiterte Farbe",
    noColor: "Keine Farbe",
    elementSize: "Gr\xF6\xDFe",
    elementOpacity: "Deckkraft",
    textGroup: "Text und Links",
    importGroup: "Import",
    previewGroup: "Vorschau",
    bold: "Fett",
    italic: "Kursiv",
    underline: "Unterstrichen",
    clearFormat: "Formatierung l\xF6schen",
    settingsSectionInterface: "Oberfl\xE4che",
    settingsSectionPen: "Stift",
    settingsSectionWatercolor: "Aquarell",
    settingsSectionLayout: "Layout",
    frameCornerRadius: "Rahmenecken",
    frameCornerRadiusDesc: "Radius f\xFCr Element-, Auswahl- und Gruppenrahmen. Null verwendet rechte Ecken.",
    settingsSectionDiagnostics: "Diagnose",
    movePanel: "Panel verschieben",
    settingsLanguage: "Sprache",
    languageAuto: "Auto",
    supportTitle: "NoteDraw unterst\xFCtzen",
    supportSubtitle: "Mit Alipay oder Binance scannen, um die Wartung zu unterst\xFCtzen.",
    supportCodeAlipay: "Alipay",
    supportCodeBinance: "Binance"
  }),
  ja: Object.assign({}, I18N.en, {
    toggleCommand: "\u30D7\u30EC\u30D3\u30E5\u30FC\u7DE8\u96C6\u3068\u63CF\u753B\u30E2\u30FC\u30C9\u3092\u5207\u308A\u66FF\u3048",
    openNoteOrWebviewFirst: "\u5148\u306B\u30CE\u30FC\u30C8\u307E\u305F\u306F webview \u3092\u958B\u3044\u3066\u304F\u3060\u3055\u3044\u3002",
    failedSaveDrawing: "\u63CF\u753B\u30C7\u30FC\u30BF\u306E\u4FDD\u5B58\u306B\u5931\u6557\u3057\u307E\u3057\u305F\u3002",
    failedImportFile: "\u30D5\u30A1\u30A4\u30EB\u306E\u30A4\u30F3\u30DD\u30FC\u30C8\u306B\u5931\u6557\u3057\u307E\u3057\u305F\u3002",
    editTextDraw: "\u6587\u5B57\u7DE8\u96C6 / \u63CF\u753B",
    editTextDrawHidden: "\u6587\u5B57\u7DE8\u96C6 / \u63CF\u753B\uFF08\u63CF\u753B\u306F\u975E\u8868\u793A\uFF09",
    editWebviewDraw: "webview \u7DE8\u96C6 / \u63CF\u753B",
    selectDrawings: "\u8981\u7D20\u3092\u9078\u629E",
    editMarkdownTool: "MD \u7DE8\u96C6",
    pen: "\u30DA\u30F3",
    watercolorBrush: "\u6C34\u5F69\u30D6\u30E9\u30B7",
    floatingText: "\u30D5\u30ED\u30FC\u30C6\u30A3\u30F3\u30B0\u6587\u5B57",
    undoLastDrawing: "\u6700\u5F8C\u306E\u63CF\u753B\u3092\u5143\u306B\u623B\u3059",
    redoDrawing: "\u63CF\u753B\u3092\u3084\u308A\u76F4\u3059",
    deleteSelectedDrawing: "\u9078\u629E\u3092\u524A\u9664",
    penSettings: "\u30DA\u30F3\u8A2D\u5B9A",
    advancedColor: "\u8A73\u7D30\u30AB\u30E9\u30FC",
    noColor: "\u8272\u306A\u3057",
    elementSize: "\u30B5\u30A4\u30BA",
    elementOpacity: "\u4E0D\u900F\u660E\u5EA6",
    textGroup: "\u30C6\u30AD\u30B9\u30C8\u3068\u30EA\u30F3\u30AF",
    importGroup: "\u30A4\u30F3\u30DD\u30FC\u30C8",
    previewGroup: "\u30D7\u30EC\u30D3\u30E5\u30FC",
    bold: "\u592A\u5B57",
    italic: "\u659C\u4F53",
    underline: "\u4E0B\u7DDA",
    clearFormat: "\u66F8\u5F0F\u3092\u30AF\u30EA\u30A2",
    settingsSectionInterface: "\u30A4\u30F3\u30BF\u30FC\u30D5\u30A7\u30FC\u30B9",
    settingsSectionPen: "\u30DA\u30F3",
    settingsSectionWatercolor: "\u6C34\u5F69",
    settingsSectionLayout: "\u30EC\u30A4\u30A2\u30A6\u30C8",
    frameCornerRadius: "\u67A0\u306E\u89D2\u4E38",
    frameCornerRadiusDesc: "\u8981\u7D20\u3001\u9078\u629E\u3001\u30B0\u30EB\u30FC\u30D7\u67A0\u306E\u89D2\u4E38\u3067\u3059\u30020 \u3067\u76F4\u89D2\u306B\u306A\u308A\u307E\u3059\u3002",
    settingsSectionDiagnostics: "\u8A3A\u65AD",
    movePanel: "\u30D1\u30CD\u30EB\u3092\u79FB\u52D5",
    settingsLanguage: "\u8A00\u8A9E",
    languageAuto: "\u81EA\u52D5",
    supportTitle: "NoteDraw \u3092\u652F\u63F4",
    supportSubtitle: "Alipay \u307E\u305F\u306F Binance \u3067\u30B9\u30AD\u30E3\u30F3\u3057\u3066\u4FDD\u5B88\u3092\u652F\u63F4\u3067\u304D\u307E\u3059\u3002",
    supportCodeAlipay: "Alipay",
    supportCodeBinance: "Binance"
  }),
  ko: Object.assign({}, I18N.en, {
    toggleCommand: "\uBBF8\uB9AC\uBCF4\uAE30 \uD3B8\uC9D1 \uBC0F \uADF8\uB9AC\uAE30 \uBAA8\uB4DC \uC804\uD658",
    openNoteOrWebviewFirst: "\uBA3C\uC800 \uB178\uD2B8 \uB610\uB294 webview\uB97C \uC5EC\uC138\uC694.",
    failedSaveDrawing: "\uADF8\uB9AC\uAE30 \uB370\uC774\uD130\uB97C \uC800\uC7A5\uD558\uC9C0 \uBABB\uD588\uC2B5\uB2C8\uB2E4.",
    failedImportFile: "\uD30C\uC77C\uC744 \uAC00\uC838\uC624\uC9C0 \uBABB\uD588\uC2B5\uB2C8\uB2E4.",
    editTextDraw: "\uD14D\uC2A4\uD2B8 \uD3B8\uC9D1 / \uADF8\uB9AC\uAE30",
    editTextDrawHidden: "\uD14D\uC2A4\uD2B8 \uD3B8\uC9D1 / \uADF8\uB9AC\uAE30(\uADF8\uB9BC \uC228\uAE40)",
    editWebviewDraw: "webview \uD3B8\uC9D1 / \uADF8\uB9AC\uAE30",
    selectDrawings: "\uC694\uC18C \uC120\uD0DD",
    editMarkdownTool: "MD \uD3B8\uC9D1",
    pen: "\uD39C",
    watercolorBrush: "\uC218\uCC44\uD654 \uBE0C\uB7EC\uC2DC",
    floatingText: "\uD50C\uB85C\uD305 \uD14D\uC2A4\uD2B8",
    undoLastDrawing: "\uB9C8\uC9C0\uB9C9 \uADF8\uB9AC\uAE30 \uCDE8\uC18C",
    redoDrawing: "\uADF8\uB9AC\uAE30 \uB2E4\uC2DC \uC2E4\uD589",
    deleteSelectedDrawing: "\uC120\uD0DD \uC0AD\uC81C",
    penSettings: "\uD39C \uC124\uC815",
    advancedColor: "\uACE0\uAE09 \uC0C9\uC0C1",
    noColor: "\uC0C9\uC0C1 \uC5C6\uC74C",
    elementSize: "\uD06C\uAE30",
    elementOpacity: "\uBD88\uD22C\uBA85\uB3C4",
    textGroup: "\uD14D\uC2A4\uD2B8 \uBC0F \uB9C1\uD06C",
    importGroup: "\uAC00\uC838\uC624\uAE30",
    previewGroup: "\uBBF8\uB9AC\uBCF4\uAE30",
    bold: "\uAD75\uAC8C",
    italic: "\uAE30\uC6B8\uC784",
    underline: "\uBC11\uC904",
    clearFormat: "\uC11C\uC2DD \uC9C0\uC6B0\uAE30",
    settingsSectionInterface: "\uC778\uD130\uD398\uC774\uC2A4",
    settingsSectionPen: "\uD39C",
    settingsSectionWatercolor: "\uC218\uCC44\uD654",
    settingsSectionLayout: "\uB808\uC774\uC544\uC6C3",
    frameCornerRadius: "\uD504\uB808\uC784 \uBAA8\uC11C\uB9AC",
    frameCornerRadiusDesc: "\uC694\uC18C, \uC120\uD0DD \uBC0F \uADF8\uB8F9 \uD504\uB808\uC784\uC758 \uBAA8\uC11C\uB9AC \uBC18\uACBD\uC785\uB2C8\uB2E4. 0\uC740 \uC9C1\uAC01\uC785\uB2C8\uB2E4.",
    settingsSectionDiagnostics: "\uC9C4\uB2E8",
    movePanel: "\uD328\uB110 \uC774\uB3D9",
    settingsLanguage: "\uC5B8\uC5B4",
    languageAuto: "\uC790\uB3D9",
    supportTitle: "NoteDraw \uC9C0\uC6D0",
    supportSubtitle: "Alipay \uB610\uB294 Binance\uB85C \uC2A4\uCE94\uD558\uC5EC \uC720\uC9C0 \uAD00\uB9AC\uB97C \uC9C0\uC6D0\uD560 \uC218 \uC788\uC2B5\uB2C8\uB2E4.",
    supportCodeAlipay: "Alipay",
    supportCodeBinance: "Binance"
  }),
  tr: Object.assign({}, I18N.en, {
    toggleCommand: "\xD6nizleme d\xFCzenleme ve \xE7izim modunu de\u011Fi\u015Ftir",
    openNoteOrWebviewFirst: "\xD6nce bir not veya webview a\xE7\u0131n.",
    failedSaveDrawing: "\xC7izim verileri kaydedilemedi.",
    failedImportFile: "Dosya i\xE7e aktar\u0131lamad\u0131.",
    editTextDraw: "Metni d\xFCzenle / \xE7iz",
    editTextDrawHidden: "Metni d\xFCzenle / \xE7iz (\xE7izimler gizli)",
    editWebviewDraw: "Webview d\xFCzenle / \xE7iz",
    selectDrawings: "\xD6geleri se\xE7",
    editMarkdownTool: "MD d\xFCzenle",
    pen: "Kalem",
    watercolorBrush: "Sulu boya f\u0131r\xE7as\u0131",
    floatingText: "Y\xFCzen metin",
    undoLastDrawing: "Son \xE7izimi geri al",
    redoDrawing: "\xC7izimi yinele",
    deleteSelectedDrawing: "Se\xE7ileni sil",
    penSettings: "Kalem ayarlar\u0131",
    advancedColor: "Geli\u015Fmi\u015F renk",
    noColor: "Renksiz",
    elementSize: "Boyut",
    elementOpacity: "Opakl\u0131k",
    textGroup: "Metin ve ba\u011Flant\u0131lar",
    importGroup: "\u0130\xE7e aktar",
    previewGroup: "\xD6nizleme",
    bold: "Kal\u0131n",
    italic: "\u0130talik",
    underline: "Alt\u0131 \xE7izili",
    clearFormat: "Bi\xE7imi temizle",
    settingsSectionInterface: "Aray\xFCz",
    settingsSectionPen: "Kalem",
    settingsSectionWatercolor: "Suluboya",
    settingsSectionLayout: "Yerle\u015Fim",
    frameCornerRadius: "\xC7er\xE7eve k\xF6\u015Fe yar\u0131\xE7ap\u0131",
    frameCornerRadiusDesc: "\xD6\u011Fe, se\xE7im ve grup \xE7er\xE7evelerinin k\xF6\u015Fe yar\u0131\xE7ap\u0131. S\u0131f\u0131r k\xF6\u015Feleri dik yapar.",
    settingsSectionDiagnostics: "Tan\u0131lama",
    movePanel: "Paneli ta\u015F\u0131",
    settingsLanguage: "Dil",
    languageAuto: "Otomatik",
    supportTitle: "NoteDraw'u destekle",
    supportSubtitle: "Bak\u0131m\u0131 desteklemek i\xE7in Alipay veya Binance ile taray\u0131n.",
    supportCodeAlipay: "Alipay",
    supportCodeBinance: "Binance"
  })
});
var DEFAULT_SETTINGS = {
  language: LANGUAGE_AUTO,
  drawingStorageMode: DRAWING_STORAGE_CONFIG,
  defaultPenColor: "#e53935",
  defaultPenWidth: 3,
  defaultPenOpacity: DEFAULT_PEN_OPACITY,
  defaultWatercolorColor: "#3b82f6",
  defaultWatercolorWidth: 9,
  defaultWatercolorOpacity: 0.45,
  brushVariantSettings: {},
  lastToolMode: TOOL_DRAW,
  lastBrushMode: BRUSH_PEN,
  lastPenVariant: BRUSH_VARIANT_DEFAULT,
  lastWatercolorVariant: BRUSH_VARIANT_DEFAULT,
  lastTextPreset: "plain",
  lastReadingZoom: 1,
  mindMapAffectsSource: true,
  defaultSourceEditMarkdown: true,
  frameCornerRadius: 0,
  toolbarTopOffset: 6,
  toolbarPosition: null,
  longPressMs: LONG_PRESS_MS,
  selectTapDistance: SELECT_TAP_DISTANCE,
  selectStrokePadding: SELECT_STROKE_PADDING,
  selectedStrokeAlpha: SELECTED_STROKE_ALPHA,
  drawingInterpolationStep: DRAWING_INTERPOLATION_STEP_PX,
  drawingMinPointDistance: DRAWING_MIN_POINT_DISTANCE_PX,
  drawingCompactDistance: DRAWING_COMPACT_DISTANCE_PX,
  autoSaveDelayMs: 500,
  enableDebugLog: false
};
var METADATA_PROPERTY_SELECTOR = ".metadata-property";
var MARKDOWN_TEXT_SELECTOR = "h1,h2,h3,h4,h5,h6,p,li,blockquote,pre,td,th,.callout-content";
var MARKDOWN_EMBED_SELECTOR = ".internal-embed,.markdown-embed,.markdown-embed-content";
var NOTE_FLOW_RENDERED_OWNER_SELECTOR = [
  ".el-p",
  ".el-h1",
  ".el-h2",
  ".el-h3",
  ".el-h4",
  ".el-h5",
  ".el-h6",
  ".el-blockquote",
  ".el-pre",
  ".el-code",
  ".el-table",
  ".el-hr",
  ".el-img",
  ".callout",
  ".math-block",
  "figure",
  "blockquote",
  "pre",
  "table",
  "hr",
  "img",
  "video",
  "audio",
  "iframe"
].join(",");
var NOTE_FLOW_RENDERED_BLOCK_SELECTOR = [
  ".el-p",
  ".el-h1",
  ".el-h2",
  ".el-h3",
  ".el-h4",
  ".el-h5",
  ".el-h6",
  ".el-blockquote",
  ".el-pre",
  ".el-code",
  ".el-table",
  ".el-hr",
  ".el-img",
  "li",
  "tr",
  "pre",
  "table",
  "blockquote",
  "hr",
  "figure",
  ".callout",
  ".internal-embed",
  ".math-block",
  "img",
  "video",
  "audio",
  "iframe"
].join(",");
var EDITABLE_SELECTOR = [
  ".markdown-preview-view",
  ".markdown-embed-content",
  ".internal-embed"
].flatMap((root) => MARKDOWN_TEXT_SELECTOR.split(",").map((selector) => `${root} ${selector}`)).concat([
  `.metadata-container ${METADATA_PROPERTY_SELECTOR}`
]).join(",");
var BLOCKED_EDIT_SELECTOR = [
  ".notedraw-button",
  ".notedraw-toolbar",
  ".notedraw-palette-panel",
  ".notedraw-brush-panel",
  ".notedraw-selection-menu",
  ".notedraw-static-canvas",
  ".notedraw-canvas",
  "a",
  "button",
  "input",
  "textarea",
  "select",
  "img",
  "svg",
  "canvas",
  ".external-embed",
  ".frontmatter"
].join(",");
var WEBVIEW_EDITABLE_SELECTOR = [
  "h1",
  "h2",
  "h3",
  "h4",
  "h5",
  "h6",
  "p",
  "li",
  "blockquote",
  "td",
  "th",
  "label",
  "summary",
  "figcaption",
  "caption",
  "pre",
  "a",
  "span",
  "div"
].join(",");
var WEBVIEW_BLOCKED_EDIT_SELECTOR = [
  ".notedraw-button",
  ".notedraw-toolbar",
  ".notedraw-palette-panel",
  ".notedraw-brush-panel",
  ".notedraw-text-panel",
  ".notedraw-selection-menu",
  ".notedraw-embed-layer",
  ".notedraw-underlay-embed-layer",
  ".notedraw-underlay-canvas",
  ".notedraw-static-canvas",
  ".notedraw-canvas",
  "button",
  "input",
  "textarea",
  "select",
  "img",
  "svg",
  "canvas",
  "video",
  "audio",
  "webview",
  "iframe"
].join(",");
var NOTEDRAW_OWNED_MUTATION_SELECTOR = [
  ".notedraw-button",
  ".notedraw-fallback-button",
  ".notedraw-webview-button",
  ".notedraw-toolbar",
  ".notedraw-palette-panel",
  ".notedraw-brush-panel",
  ".notedraw-text-panel",
  ".notedraw-selection-menu",
  ".notedraw-format-toolbar",
  ".notedraw-reading-zoom-extent",
  ".notedraw-reading-bottom-spacer",
  ".notedraw-embed-layer",
  ".notedraw-underlay-embed-layer",
  ".notedraw-underlay-canvas",
  ".notedraw-static-canvas",
  ".notedraw-canvas",
  ".notedraw-note-flow-line-spacer",
  ".notedraw-note-flow-block-spacer",
  ".notedraw-md-grid-row",
  ".notedraw-md-line-block",
  ".notedraw-body-control",
  ".notedraw-file-input"
].join(",");
var NoteDrawFileSuggestModal = class extends import_obsidian.FuzzySuggestModal {
  constructor(app, currentFile, onChoose, options = {}) {
    super(app);
    this.currentFile = currentFile;
    this.onChoose = onChoose;
    this.affectsSource = options.affectsSource !== false;
    this.onAffectsSourceChange = options.onAffectsSourceChange;
    this.onModalClose = options.onClose;
    this.setPlaceholder(I18N[detectNoteDrawLanguage(app)]?.chooseMindMapFile || I18N.en.chooseMindMapFile);
    const controls = this.modalEl.createDiv({ cls: "notedraw-mind-map-source-setting" });
    new import_obsidian.Setting(controls).setName(I18N[detectNoteDrawLanguage(app)]?.affectSourceNote || I18N.en.affectSourceNote).addToggle((toggle) => toggle.setValue(this.affectsSource).onChange((value) => {
      this.affectsSource = Boolean(value);
      this.onAffectsSourceChange?.(this.affectsSource);
    }));
    const inputContainer = this.modalEl.querySelector(".prompt-input-container");
    inputContainer?.insertAdjacentElement("afterend", controls);
  }
  getItems() {
    const currentPath = normalizeVaultPath(this.currentFile?.path || "");
    return this.app.vault.getMarkdownFiles().slice().sort((a, b) => {
      if (a.path === currentPath) {
        return -1;
      }
      if (b.path === currentPath) {
        return 1;
      }
      return a.path.localeCompare(b.path);
    });
  }
  getItemText(file2) {
    return file2?.path || file2?.basename || "";
  }
  onChooseItem(file2) {
    this.onChoose?.(file2, this.affectsSource);
  }
  onClose() {
    this.onModalClose?.(this);
  }
};
var NoteDrawCommandSuggestModal = class extends import_obsidian.FuzzySuggestModal {
  constructor(app, onChoose, options = {}) {
    super(app);
    this.onChoose = onChoose;
    this.onModalClose = options.onClose;
    this.setPlaceholder(I18N[detectNoteDrawLanguage(app)]?.chooseButtonCommand || I18N.en.chooseButtonCommand);
  }
  getItems() {
    const source = this.app.commands?.listCommands?.() || this.app.commands?.commands || {};
    const entries = Array.isArray(source) ? source.map((command) => [command?.id, command]) : Object.entries(source);
    return entries.map(([id, command]) => ({
      id: String(command?.id || id || "").trim(),
      name: String(command?.name || command?.id || id || "").trim()
    })).filter((command) => command.id && command.name).sort((left, right) => left.name.localeCompare(right.name));
  }
  getItemText(command) {
    return `${command?.name || command?.id || ""} (${command?.id || ""})`;
  }
  onChooseItem(command) {
    this.onChoose?.(command);
  }
  onClose() {
    this.onModalClose?.(this);
  }
};
var NoteDrawPlugin = class extends import_obsidian.Plugin {
  async onload() {
    const savedSettings = await this.loadData();
    this.runtimeDisposed = false;
    this.noteDrawSettings = sanitizeSettings({ ...DEFAULT_SETTINGS, ...savedSettings || {} });
    this.controllers = /* @__PURE__ */ new WeakMap();
    this.liveControllers = /* @__PURE__ */ new Set();
    this.sourceControllers = /* @__PURE__ */ new Map();
    this.embeddedControllers = /* @__PURE__ */ new Map();
    this.webviewControllers = /* @__PURE__ */ new Map();
    this.workspaceControllers = /* @__PURE__ */ new Map();
    this.registeredSurfaceControllers = /* @__PURE__ */ new Map();
    this.registeredSurfaceRecords = /* @__PURE__ */ new Map();
    this.headerActions = /* @__PURE__ */ new Map();
    this.saveTimers = /* @__PURE__ */ new Map();
    this.saveIdleCallbacks = /* @__PURE__ */ new Map();
    this.pendingDrawingSaves = /* @__PURE__ */ new Map();
    this.drawingWritePromises = /* @__PURE__ */ new Map();
    this.drawingExternalSyncTimers = /* @__PURE__ */ new Map();
    this.suppressedDrawingSaves = [];
    this.drawingStateCache = /* @__PURE__ */ new Map();
    this.portableBundles = /* @__PURE__ */ new Map();
    this.portableBundleLoads = /* @__PURE__ */ new Map();
    this.portableResourceCache = /* @__PURE__ */ new Map();
    this.sharePackagePromises = /* @__PURE__ */ new Map();
    this.previewRenderRecovery = /* @__PURE__ */ new Map();
    this.viewDrawingActive = /* @__PURE__ */ new WeakMap();
    this.viewToolbarState = /* @__PURE__ */ new WeakMap();
    this.viewEditHistory = /* @__PURE__ */ new WeakMap();
    this.viewInteractionController = /* @__PURE__ */ new WeakMap();
    this.textSaveStates = /* @__PURE__ */ new WeakMap();
    this.apiListeners = /* @__PURE__ */ new Map();
    this.elementClipboard = null;
    this.settingsSaveTimer = null;
    this.webviewSyncTimer = null;
    this.embeddedSyncTimer = null;
    this.workspaceSyncTimer = null;
    this.surfaceSyncTimer = null;
    this.surfaceSyncIdleId = null;
    this.surfaceSyncDueAt = 0;
    this.floatingControlsSyncTimer = null;
    this.mindMapPickerTimer = null;
    this.mindMapFileModal = null;
    this.buttonCommandModal = null;
    this.webviewMutationObserver = null;
    this.onElementLinkClick = this.onElementLinkClick.bind(this);
    this.api = this.createPublicApi();
    if (typeof window !== "undefined") {
      window.NoteDraw = this.api;
    }
    cleanupAllDrawingHeaderButtons();
    this.addCommand({
      id: "toggle-draw-mode",
      name: this.t("toggleCommand"),
      callback: () => this.toggleActiveController()
    });
    this.addCommand({
      id: "copy-selected-elements",
      name: this.t("copyElement"),
      checkCallback: (checking) => {
        const controller = this.getActiveController();
        const available = Boolean(controller?.getSelectedStrokeIndexes().length);
        if (available && !checking) {
          controller.copySelectedElements();
        }
        return available;
      }
    });
    this.addCommand({
      id: "copy-selected-element-link",
      name: this.t("copyElementLink"),
      checkCallback: (checking) => {
        const controller = this.getActiveController();
        const available = Boolean(controller?.hasHybridSelection?.());
        if (available && !checking) {
          void controller.copySelectedElementLink();
        }
        return available;
      }
    });
    this.addCommand({
      id: "paste-elements",
      name: this.t("pasteElement"),
      checkCallback: (checking) => {
        const controller = this.getActiveController();
        const available = Boolean(controller && this.elementClipboard?.strokes?.length);
        if (available && !checking) {
          controller.pasteCopiedElements();
        }
        return available;
      }
    });
    this.addCommand({
      id: "share-file",
      name: this.t("shareNoteDrawFile"),
      checkCallback: (checking) => {
        const file2 = this.app.workspace.getActiveFile?.();
        const available = Boolean(file2 && String(file2.extension || "").toLowerCase() === "md");
        if (available && !checking) {
          void this.shareNoteDrawFile(file2);
        }
        return available;
      }
    });
    this.addSettingTab(new NoteDrawSettingTab(this.app, this));
    this.registerEvent(this.app.workspace.on("layout-change", () => {
      this.syncMarkdownModeSurfaces();
      window.requestAnimationFrame(() => this.syncMarkdownModeSurfaces());
      this.scheduleSurfaceSync(120);
    }));
    this.registerEvent(this.app.workspace.on("active-leaf-change", () => this.scheduleSurfaceSync(40)));
    this.registerEvent(this.app.workspace.on("file-open", () => this.scheduleSurfaceSync(60)));
    this.registerEvent(this.app.vault.on("create", (file2) => {
      this.handleVaultDrawingChange(file2);
    }));
    this.registerEvent(this.app.vault.on("modify", (file2) => {
      this.handleVaultDrawingChange(file2);
    }));
    this.registerEvent(this.app.vault.on("raw", (path) => {
      this.handleVaultDrawingChange({ path });
    }));
    this.registerEvent(this.app.vault.on("delete", (file2) => {
      this.handleVaultFileDelete(file2).catch((error) => {
        console.error(`[${PLUGIN_ID}] Failed to clear deleted file NoteDraw data`, error);
      });
    }));
    this.registerEvent(this.app.workspace.on("file-menu", (menu, file2) => {
      if (!file2 || String(file2.extension || "").toLowerCase() !== "md") {
        return;
      }
      menu.addItem((item) => {
        item.setTitle(this.t("shareNoteDrawFile")).setIcon("share-2").onClick(() => {
          void this.shareNoteDrawFile(file2);
        });
      });
    }));
    activeDocument.addEventListener("click", this.onElementLinkClick, true);
    this.installWebviewObserver();
    this.registerMarkdownPostProcessor((el, ctx) => {
      if (this.runtimeDisposed) {
        return;
      }
      const renderedSourcePath = resolveRenderedSourcePath(this.app, el, ctx.sourcePath);
      annotateEditableElements(el, ctx, renderedSourcePath);
      if (el.matches?.("img,video,audio,source,a,.internal-embed") || el.querySelector?.("img,video,audio,source,a,.internal-embed")) {
        void this.hydratePortableMarkdownResources(el, renderedSourcePath);
      }
      if (el.matches?.(MARKDOWN_EMBED_SELECTOR) || el.querySelector?.(MARKDOWN_EMBED_SELECTOR)) {
        this.scheduleEmbeddedMarkdownSync();
      }
      const preview = el.closest(".markdown-preview-view");
      if (!preview || isEmbeddedPreview(preview)) {
        return;
      }
      const view = findOwningMarkdownView(this.app, preview, ctx.sourcePath);
      if (!view || !view.file || !ctx.sourcePath || view.file.path !== ctx.sourcePath) {
        return;
      }
      const existingController = this.controllers.get(preview) || preview._noteDrawController;
      if (!isMarkdownPreviewVisible(view, preview)) {
        const alternateSurfaceVisible = hasVisibleAlternateWorkspaceSurface(view, preview);
        const sourceVisible = isSourceMode(view) && isMarkdownSourceVisible(view);
        if (alternateSurfaceVisible && !sourceVisible && existingController?.plugin === this && existingController.surfaceType === "preview") {
          existingController.destroy();
        }
        if (alternateSurfaceVisible) {
          this.scheduleWebviewSync();
        }
        return;
      }
      if (existingController?.plugin === this && !existingController.destroyed && !isSourceMode(view)) {
        existingController.setFile(view.file).catch((error) => {
          console.error(`[${PLUGIN_ID}] Failed to switch preview controller file`, error);
        });
        return;
      }
      if (!isRootPreviewReady(view, preview)) {
        existingController?.destroy?.();
        resetDormantRootPreview(view, preview);
        return;
      }
      if (existingController?.destroy) {
        existingController.destroy();
      }
      cleanupDrawingUi(preview);
      const controller = new PreviewDrawingController(this, preview, view, view.file);
      this.controllers.set(preview, controller);
      controller.mount();
      this.scheduleWebviewSync();
    });
    this.scheduleSurfaceSync(120);
  }
  onunload() {
    this.runtimeDisposed = true;
    activeDocument.removeEventListener("click", this.onElementLinkClick, true);
    for (const controller of Array.from(this.liveControllers)) {
      controller.destroy();
    }
    this.liveControllers.clear();
    this.sourceControllers.clear();
    this.embeddedControllers.clear();
    this.webviewControllers.clear();
    this.workspaceControllers.clear();
    this.registeredSurfaceControllers?.clear();
    this.registeredSurfaceRecords?.clear();
    this.portableBundles?.clear();
    this.portableBundleLoads?.clear();
    this.portableResourceCache?.clear();
    this.sharePackagePromises?.clear();
    for (const state of this.previewRenderRecovery?.values?.() || []) {
      if (state.timer !== null) {
        window.clearTimeout(state.timer);
      }
    }
    this.previewRenderRecovery?.clear?.();
    for (const state of this.headerActions.values()) {
      state.button?.remove();
    }
    this.headerActions.clear();
    cleanupAllDrawingHeaderButtons();
    for (const path of Array.from(this.pendingDrawingSaves.keys())) {
      this.cancelScheduledDrawingSave(path);
      this.flushDrawingSave(path).catch((error) => {
        console.error(`[${PLUGIN_ID}] Failed to flush drawing data during unload`, error);
      });
    }
    this.saveTimers.clear();
    this.saveIdleCallbacks.clear();
    for (const timer of this.drawingExternalSyncTimers.values()) {
      window.clearTimeout(timer);
    }
    this.drawingExternalSyncTimers.clear();
    this.suppressedDrawingSaves = [];
    if (this.settingsSaveTimer !== null) {
      window.clearTimeout(this.settingsSaveTimer);
      this.settingsSaveTimer = null;
    }
    if (this.webviewSyncTimer !== null) {
      window.clearTimeout(this.webviewSyncTimer);
      this.webviewSyncTimer = null;
    }
    if (this.embeddedSyncTimer !== null) {
      window.clearTimeout(this.embeddedSyncTimer);
      this.embeddedSyncTimer = null;
    }
    if (this.workspaceSyncTimer !== null) {
      window.clearTimeout(this.workspaceSyncTimer);
      this.workspaceSyncTimer = null;
    }
    if (this.surfaceSyncTimer !== null) {
      window.clearTimeout(this.surfaceSyncTimer);
      this.surfaceSyncTimer = null;
    }
    if (this.surfaceSyncIdleId !== null && typeof window.cancelIdleCallback === "function") {
      window.cancelIdleCallback(this.surfaceSyncIdleId);
      this.surfaceSyncIdleId = null;
    }
    this.surfaceSyncDueAt = 0;
    if (this.floatingControlsSyncTimer !== null) {
      window.clearTimeout(this.floatingControlsSyncTimer);
      this.floatingControlsSyncTimer = null;
    }
    if (this.mindMapPickerTimer !== null) {
      window.clearTimeout(this.mindMapPickerTimer);
      this.mindMapPickerTimer = null;
    }
    this.mindMapFileModal?.close?.();
    this.mindMapFileModal = null;
    this.buttonCommandModal?.close?.();
    this.buttonCommandModal = null;
    this.webviewMutationObserver?.disconnect();
    this.webviewMutationObserver = null;
    this.apiListeners?.clear();
    if (typeof window !== "undefined" && window.NoteDraw === this.api) {
      delete window.NoteDraw;
    }
  }
  runSurfaceSync() {
    if (this.runtimeDisposed) {
      return;
    }
    this.pruneDisconnectedControllers();
    this.syncRenderedMarkdownAnnotations();
    this.syncSourceControllers();
    this.syncMarkdownControllerModes();
    this.syncEmbeddedMarkdownControllers();
    this.syncWebviewControllers();
    this.syncWorkspaceControllers();
    for (const controller of this.liveControllers) {
      this.reconcileControllerActivation(controller);
      if (!controller.button?.isConnected) {
        controller.button = this.installHeaderButton(controller);
      }
      controller.syncFloatingControlClasses();
      if (isElementVisibleEnough(controller.previewEl)) {
        controller.scheduleFrozenNoteFlowLayoutRestore();
        controller.scheduleResize({ layout: false, measure: false });
      }
    }
  }
  syncMarkdownModeSurfaces() {
    if (this.runtimeDisposed) {
      return;
    }
    this.syncSourceControllers();
    this.syncMarkdownControllerModes();
    for (const controller of this.liveControllers) {
      if (!controller.destroyed) {
        this.reconcileControllerActivation(controller);
        controller.syncFloatingControlClasses();
      }
    }
  }
  scheduleSurfaceSync(delay = 80) {
    if (this.runtimeDisposed) {
      return;
    }
    const wait = Math.max(0, Number(delay) || 0);
    const dueAt = Date.now() + wait;
    if (this.surfaceSyncTimer !== null && this.surfaceSyncDueAt <= dueAt) {
      return;
    }
    if (this.surfaceSyncTimer !== null) {
      window.clearTimeout(this.surfaceSyncTimer);
      this.surfaceSyncTimer = null;
    }
    if (this.surfaceSyncIdleId !== null && typeof window.cancelIdleCallback === "function") {
      window.cancelIdleCallback(this.surfaceSyncIdleId);
      this.surfaceSyncIdleId = null;
    }
    this.surfaceSyncDueAt = dueAt;
    this.surfaceSyncTimer = window.setTimeout(() => {
      this.surfaceSyncTimer = null;
      const run = () => {
        this.surfaceSyncIdleId = null;
        this.surfaceSyncDueAt = 0;
        this.runSurfaceSync();
      };
      if (typeof window.requestIdleCallback === "function") {
        this.surfaceSyncIdleId = window.requestIdleCallback(run, { timeout: 400 });
      } else {
        run();
      }
    }, wait);
  }
  handleVaultDrawingChange(file2) {
    if (this.runtimeDisposed) {
      return;
    }
    const changedPath = normalizeVaultPath(file2?.path || "");
    if (!changedPath) {
      return;
    }
    const paths = /* @__PURE__ */ new Set();
    for (const controller of this.getAllControllers()) {
      if (!controller?.file?.path || !this.drawingStoragePathMatches(controller.file, changedPath)) {
        continue;
      }
      paths.add(normalizeVaultPath(controller.file.path));
    }
    for (const notePath of paths) {
      this.scheduleExternalDrawingRefresh(notePath, 72);
    }
  }
  drawingStoragePathMatches(file2, changedPath) {
    const notePath = normalizeVaultPath(file2?.path || "");
    const normalizedChangedPath = normalizeVaultPath(changedPath || "");
    if (!notePath || !normalizedChangedPath) {
      return false;
    }
    const storageMode = this.drawingStorageModeForFile(file2);
    if (storageMode === DRAWING_STORAGE_EMBEDDED && notePath === normalizedChangedPath) {
      return true;
    }
    return [
      this.drawingPathForFile(file2, storageMode),
      this.drawingPathForFile(file2, DRAWING_STORAGE_CONFIG),
      this.legacyDrawingPathForFile(file2)
    ].some((path) => normalizeVaultPath(path) === normalizedChangedPath);
  }
  scheduleExternalDrawingRefresh(notePath, delay = 72) {
    const normalizedPath = normalizeVaultPath(notePath || "");
    if (!normalizedPath || this.runtimeDisposed) {
      return;
    }
    const existing = this.drawingExternalSyncTimers.get(normalizedPath);
    if (existing !== void 0) {
      window.clearTimeout(existing);
    }
    const timer = window.setTimeout(() => {
      this.drawingExternalSyncTimers.delete(normalizedPath);
      this.refreshExternalDrawingFile(normalizedPath).catch((error) => {
        if (!this.runtimeDisposed) {
          console.error(`[${PLUGIN_ID}] Failed to refresh externally changed drawing data`, error);
        }
      });
    }, Math.max(40, Number(delay) || 72));
    this.drawingExternalSyncTimers.set(normalizedPath, timer);
  }
  async refreshExternalDrawingFile(notePath) {
    const file2 = getVaultFileByPath(this.app.vault, notePath);
    if (!file2 || this.runtimeDisposed) {
      return 0;
    }
    const controllers = this.getAllControllers().filter((controller) => !controller.destroyed && normalizeVaultPath(controller.file?.path || "") === normalizeVaultPath(file2.path) && controller.drawingsLoaded);
    if (!controllers.length) {
      return 0;
    }
    const busyControllers = controllers.filter((controller) => controller.pointerDown || controller.draggingStroke || controller.resizingSelection);
    if (busyControllers.length) {
      busyControllers.forEach((controller) => {
        controller.externalDrawingRefreshPending = true;
      });
      this.scheduleExternalDrawingRefresh(notePath, 240);
      return 0;
    }
    const data = await this.readDrawings(file2, {
      refresh: true,
      migrateLegacy: false,
      repair: false
    });
    const dataVersion = String(data?.updatedAt || "");
    if (dataVersion && controllers.every((controller) => String(controller.drawingData?.updatedAt || "") === dataVersion)) {
      controllers.forEach((controller) => {
        controller.externalDrawingRefreshPending = false;
      });
      return 0;
    }
    const refreshed = this.refreshControllersForFile(file2, data, { external: true });
    return refreshed;
  }
  scheduleMindMapFilePicker(controller) {
    if (this.mindMapPickerTimer !== null) {
      window.clearTimeout(this.mindMapPickerTimer);
    }
    this.mindMapPickerTimer = window.setTimeout(() => {
      this.mindMapPickerTimer = null;
      if (!this.runtimeDisposed) {
        this.openMindMapFilePicker(controller);
      }
    }, 48);
  }
  openMindMapFilePicker(requestedController) {
    const requestedPath = normalizeVaultPath(requestedController?.file?.path || this.app.workspace.getActiveFile?.()?.path || "");
    const currentFile = getVaultFileByPath(this.app.vault, requestedPath) || requestedController?.file || this.app.workspace.getActiveFile?.();
    const previousModal = this.mindMapFileModal;
    this.mindMapFileModal = null;
    if (previousModal?.modalEl?.isConnected) {
      previousModal.close();
    }
    const affectsSourceByDefault = this.noteDrawSettings?.mindMapAffectsSource !== false;
    const modal = new NoteDrawFileSuggestModal(this.app, currentFile, (file2, affectsSource) => {
      const target = !requestedController?.destroyed && requestedController?.previewEl?.isConnected ? requestedController : this.getActiveController() || this.getAllControllers().find((controller) => controller?.previewEl?.isConnected && normalizeVaultPath(controller.file?.path || "") === requestedPath && !controller.embeddedSurface);
      if (!target) {
        new import_obsidian.Notice(this.t("openNoteOrWebviewFirst"));
        return;
      }
      target.pendingMindMapFile = file2;
      target.pendingMindMapOptions = { affectsSource: Boolean(affectsSource) };
      target.textPreset = "mindMap";
      target.setTextMode();
      target.syncTextPanelButtons();
      new import_obsidian.Notice(this.t("placeMindMap"));
    }, {
      affectsSource: affectsSourceByDefault,
      onAffectsSourceChange: (value) => {
        this.noteDrawSettings.mindMapAffectsSource = Boolean(value);
        this.scheduleSettingsSave();
      },
      onClose: (closedModal) => {
        if (this.mindMapFileModal === closedModal) {
          this.mindMapFileModal = null;
        }
      }
    });
    this.mindMapFileModal = modal;
    modal.open();
  }
  openButtonCommandPicker(requestedController, target) {
    const requestedTarget = typeof target === "object" && target ? { ...target } : { type: "stroke", index: Number(target) };
    const requestedPath = normalizeVaultPath(requestedController?.file?.path || "");
    const previousModal = this.buttonCommandModal;
    this.buttonCommandModal = null;
    previousModal?.close?.();
    const modal = new NoteDrawCommandSuggestModal(this.app, (command) => {
      const target2 = !requestedController?.destroyed && requestedController?.previewEl?.isConnected ? requestedController : this.getAllControllers().find((controller) => controller?.previewEl?.isConnected && normalizeVaultPath(controller.file?.path || "") === requestedPath && !controller.embeddedSurface);
      if (!target2) {
        return;
      }
      target2.setSelectedElementCommand(requestedTarget, command);
    }, {
      onClose: (closedModal) => {
        if (this.buttonCommandModal === closedModal) {
          this.buttonCommandModal = null;
        }
      }
    });
    this.buttonCommandModal = modal;
    modal.open();
  }
  async saveSettings() {
    this.noteDrawSettings = sanitizeSettings(this.noteDrawSettings);
    await this.saveData(this.noteDrawSettings);
    for (const controller of this.getAllControllers()) {
      controller.applySettings();
      controller.refreshLocalizedLabels?.();
    }
    this.refreshLocalizedButtons();
  }
  async changeDrawingStorageMode(value) {
    const nextMode = normalizeDrawingStorageMode(value);
    const previousMode = normalizeDrawingStorageMode(this.noteDrawSettings?.drawingStorageMode);
    if (nextMode === previousMode) {
      return;
    }
    const activeFile = this.app.workspace.getActiveFile?.();
    const interactionController = this.getActiveController();
    const activeController = normalizeVaultPath(interactionController?.file?.path || "") === normalizeVaultPath(activeFile?.path || "") ? interactionController : this.getAllControllers().find((controller) => {
      return normalizeVaultPath(controller?.file?.path || "") === normalizeVaultPath(activeFile?.path || "");
    });
    const activeSnapshot = activeFile && activeController?.drawingData ? normalizeDrawingDataForStorage(activeController.drawingData, activeFile) : null;
    for (const path of Array.from(this.pendingDrawingSaves.keys())) {
      this.cancelScheduledDrawingSave(path);
      await this.flushDrawingSave(path);
    }
    this.noteDrawSettings.drawingStorageMode = nextMode;
    this.drawingStateCache.clear();
    await this.saveSettings();
    if (activeSnapshot) {
      await this.writeDrawings(activeFile, activeSnapshot);
    }
    this.scheduleSurfaceSync(40);
  }
  scheduleSettingsSave() {
    if (this.settingsSaveTimer !== null) {
      window.clearTimeout(this.settingsSaveTimer);
    }
    this.settingsSaveTimer = window.setTimeout(() => {
      this.settingsSaveTimer = null;
      this.saveSettings().catch((error) => {
        console.error(`[${PLUGIN_ID}] Failed to save settings`, error);
      });
    }, SETTINGS_SAVE_DELAY_MS);
  }
  t(key, vars = {}) {
    return translateNoteDraw(this, key, vars);
  }
  setAccessibleLabel(element, key, vars = {}) {
    setAccessibleLabel(element, this.t(key, vars));
  }
  refreshLocalizedButtons() {
    for (const state of this.headerActions.values()) {
      const controller = state.button?._noteDrawController || state.controller;
      this.setAccessibleLabel(state.button, controller?.surfaceType === "webview" ? "editWebviewDraw" : "editTextDraw");
    }
    for (const controller of this.webviewControllers.values()) {
      this.setAccessibleLabel(controller.button, "editWebviewDraw");
    }
  }
  getAllControllers() {
    const controllers = Array.from(this.liveControllers).filter((controller) => !controller.destroyed);
    activeDocument.querySelectorAll(".notedraw-shell").forEach((element) => {
      const controller = element._noteDrawController;
      if (controller?.plugin === this && !controllers.includes(controller)) {
        controllers.push(controller);
      }
    });
    return controllers;
  }
  pruneDisconnectedControllers() {
    for (const controller of Array.from(this.liveControllers)) {
      if (controller.destroyed || !controller.previewEl?.isConnected) {
        controller.destroy();
      }
    }
  }
  registeredSurfaceSelector(record) {
    return {
      owner: record.owner,
      id: record.id,
      surfaceId: record.id,
      surfaceKey: record.key,
      surface: "registered",
      path: record.file?.path
    };
  }
  mergeRegisteredSurfaceOptions(record, options = {}) {
    return { ...this.registeredSurfaceSelector(record), ...options };
  }
  registerApiSurface(options = {}) {
    const record = normalizeRegisteredSurfaceOptions(this.app, options);
    const existing = this.registeredSurfaceControllers.get(record.key);
    if (existing && !existing.destroyed && existing.previewEl !== record.host) {
      existing.destroy();
    }
    const active = this.registeredSurfaceControllers.get(record.key);
    if (active && !active.destroyed && active.previewEl === record.host) {
      active.registeredSurfaceOwner = record.owner;
      active.registeredSurfaceId = record.id;
      active.registeredSurfaceKey = record.key;
      active.registeredSurfaceSource = record.source;
      active.registeredSurfaceCapabilities = record.capabilities;
      active.registeredSurfaceViewport = record.viewport;
      active.disposeRegisteredSurfaceViewport?.();
      active.disposeRegisteredSurfaceViewport = this.bindRegisteredSurfaceViewport(active, record.viewport);
      const ready2 = active.setFile(record.file).catch((error) => {
        console.error(`[${PLUGIN_ID}] Failed to update registered surface file`, error);
        throw error;
      });
      const handle2 = this.createRegisteredSurfaceHandle(record, active, ready2);
      record.controller = active;
      record.handle = handle2;
      this.registeredSurfaceRecords.set(record.key, record);
      return handle2;
    }
    const mountedOnElement = this.controllers.get(record.host) || record.host._noteDrawController;
    if (mountedOnElement?.plugin === this && mountedOnElement !== active) {
      mountedOnElement.destroy();
    }
    cleanupDrawingUi(record.host);
    const controller = new PreviewDrawingController(this, record.host, record.view, record.file, {
      allowTextEdit: record.capabilities.textEditing !== false,
      surfaceType: "registered",
      registeredSurface: true,
      registeredSurfaceOwner: record.owner,
      registeredSurfaceId: record.id,
      registeredSurfaceKey: record.key,
      registeredSurfaceSource: record.source,
      registeredSurfaceCapabilities: record.capabilities,
      registeredSurfaceViewport: record.viewport
    });
    this.controllers.set(record.host, controller);
    this.registeredSurfaceControllers.set(record.key, controller);
    controller.disposeRegisteredSurfaceViewport = this.bindRegisteredSurfaceViewport(controller, record.viewport);
    record.controller = controller;
    const ready = controller.mount().catch((error) => {
      console.error(`[${PLUGIN_ID}] Failed to mount registered surface`, error);
      this.destroyRegisteredSurface(record.key);
      throw error;
    });
    const handle = this.createRegisteredSurfaceHandle(record, controller, ready);
    record.handle = handle;
    this.registeredSurfaceRecords.set(record.key, record);
    return handle;
  }
  createRegisteredSurfaceHandle(record, controller, ready) {
    const withSurface = (options = {}) => this.mergeRegisteredSurfaceOptions(record, options);
    return {
      apiVersion: "1.0",
      ready: Promise.resolve(ready).then(() => void 0),
      capabilities: { ...record.capabilities },
      get surface() {
        return controller.plugin.describeController(controller);
      },
      activate: async (toolOrOptions = {}) => {
        const options = typeof toolOrOptions === "string" ? { tool: toolOrOptions } : toolOrOptions || {};
        return this.activateApi(withSurface(options));
      },
      deactivate: () => this.deactivateApi(withSurface()),
      toggle: async (options = {}) => this.toggleApi(withSurface(options)),
      getState: async (options = {}) => this.getApiState(withSurface(options)),
      setVisibility: async (visible, options = {}) => this.setApiVisibility(visible, withSurface(options)),
      setTool: (tool, options = {}) => this.setApiTool(tool, withSurface(options)),
      setBrush: (options = {}) => this.setApiBrush(withSurface(options)),
      setTextPreset: (preset, options = {}) => this.setApiTextPreset(preset, withSurface(options)),
      getZoom: () => this.describeController(controller)?.zoom ?? 1,
      setZoom: (zoom, options = {}) => this.setApiZoom(zoom, withSurface(options)),
      execute: async (actions, options = {}) => this.executeApiAction(actions, withSurface(options)),
      getElements: async (options = {}) => this.getApiElements(withSurface(options)),
      selectElements: async (options = {}) => this.selectApiElements(withSurface(options)),
      updateElements: async (options = {}) => this.updateApiElements(withSurface(options)),
      deleteElements: async (options = {}) => this.deleteApiElements(withSurface(options)),
      reorderElements: async (options = {}) => this.reorderApiElements(withSurface(options)),
      setElementsLocked: async (options = {}) => this.setApiElementsLocked(withSurface(options)),
      setElementsNoteFlow: async (options = {}) => this.setApiElementsNoteFlow(withSurface(options)),
      createElements: async (options = {}) => this.insertApiElements(withSurface(options)),
      highlightRegion: async (options = {}) => this.insertSurfaceRegionApi("highlight", withSurface(options)),
      insertInk: async (options = {}) => this.insertSurfaceRegionApi("ink", withSurface(options)),
      focusRegion: async (region, options = {}) => this.focusSurfaceRegionApi(region, withSurface(options)),
      updateSource: async (source, options = {}) => this.updateRegisteredSurfaceSourceApi(source, withSurface(options)),
      undo: async (options = {}) => this.runApiHistory("undo", withSurface(options)),
      redo: async (options = {}) => this.runApiHistory("redo", withSurface(options)),
      copyElements: (options = {}) => this.copyElementsApi(withSurface(options)),
      copyElementLink: (options = {}) => controller.copySelectedElementLink({ quiet: Boolean(options.quiet) }),
      pasteElements: (options = {}) => this.pasteElementsApi(withSurface(options)),
      insertMindMap: async (options = {}) => this.insertMindMapApi(withSurface(options)),
      refresh: async () => {
        const data = await this.readDrawings(record.file);
        return { ok: true, refreshed: this.refreshControllersForFile(record.file, data) };
      },
      on: (eventName, listener) => this.onApiEvent(eventName, (detail) => {
        if (typeof listener !== "function") {
          return;
        }
        if (detail?.surfaceKey === record.key || detail?.owner === record.owner && detail?.id === record.id) {
          listener(detail);
        }
      }),
      destroy: () => this.destroyRegisteredSurface(record.key)
    };
  }
  bindRegisteredSurfaceViewport(controller, viewport = {}) {
    if (typeof viewport?.onChange !== "function") {
      return () => void 0;
    }
    try {
      const dispose = viewport.onChange(() => {
        if (controller.destroyed) {
          return;
        }
        controller.scheduleLayoutRefresh({ settle: false });
        controller.requestRender(true);
        this.emitApiEvent("surface-changed", { ...this.describeController(controller), phase: "viewport" });
      });
      return typeof dispose === "function" ? dispose : () => void 0;
    } catch (error) {
      console.error(`[${PLUGIN_ID}] Failed to subscribe to registered surface viewport`, error);
      return () => void 0;
    }
  }
  destroyRegisteredSurface(key) {
    const normalizedKey = String(key || "");
    const controller = this.registeredSurfaceControllers?.get(normalizedKey);
    this.registeredSurfaceControllers?.delete(normalizedKey);
    this.registeredSurfaceRecords?.delete(normalizedKey);
    if (controller && !controller.destroyed) {
      controller.destroy();
    }
    return { ok: true };
  }
  controllerActivationState(controller) {
    const key = this.controllerStateKey(controller);
    return key ? Boolean(this.viewDrawingActive.get(key)) : false;
  }
  controllerStateKey(controller) {
    const view = controller?.view;
    return view?.leaf || findOwningLeaf(this.app, view?.containerEl || controller?.previewEl) || view || controller?.previewEl || null;
  }
  controllerToolbarState(controller) {
    const key = this.controllerStateKey(controller);
    return key ? this.viewToolbarState.get(key) || null : null;
  }
  interactionController(controller) {
    const key = this.controllerStateKey(controller);
    const preferred = key ? this.viewInteractionController.get(key) : null;
    if (preferred && !preferred.destroyed && preferred.previewEl?.isConnected && isElementVisibleEnough(preferred.previewEl)) {
      return preferred;
    }
    const fallback = Array.from(this.liveControllers).find((candidate) => {
      return !candidate.destroyed && !candidate.embeddedSurface && candidate.previewEl?.isConnected && this.controllerStateKey(candidate) === key && isElementVisibleEnough(candidate.previewEl);
    }) || controller;
    if (key && fallback) {
      this.viewInteractionController.set(key, fallback);
    }
    return fallback;
  }
  setInteractionController(controller) {
    const key = this.controllerStateKey(controller);
    if (!key) {
      return;
    }
    this.viewInteractionController.set(key, controller);
    for (const candidate of this.liveControllers) {
      if (!candidate.destroyed && this.controllerStateKey(candidate) === key) {
        candidate.syncFloatingControlClasses();
      }
    }
  }
  enforceSingleVisibleToolbar(controller) {
    if (!controller?.toolbar?.isConnected) {
      return;
    }
    const ownedToolbars = /* @__PURE__ */ new Set();
    for (const candidate of this.liveControllers) {
      if (candidate.destroyed || !candidate.toolbar?.isConnected) {
        continue;
      }
      ownedToolbars.add(candidate.toolbar);
      if (candidate !== controller) {
        candidate.previewEl?.removeClass("is-notedraw-controls-visible");
        for (const element of [candidate.toolbar, candidate.palettePanel, candidate.brushPanel, candidate.textPanel, candidate.selectionMenu, candidate.formatToolbar]) {
          element?.removeClass("is-notedraw-controls-visible");
        }
        candidate.toolbar.setAttribute("aria-hidden", "true");
      }
    }
    controller.toolbar.removeAttribute("aria-hidden");
    for (const toolbar of activeDocument.querySelectorAll(".notedraw-toolbar")) {
      if (!ownedToolbars.has(toolbar)) {
        toolbar.remove();
      } else if (toolbar !== controller.toolbar) {
        toolbar.removeClass("is-notedraw-controls-visible");
        toolbar.setAttribute("aria-hidden", "true");
      }
    }
  }
  findEmbeddedStrokeControllerAtPoint(controller, event) {
    const key = this.controllerStateKey(controller);
    for (const candidate of this.liveControllers) {
      if (candidate === controller || candidate.destroyed || !candidate.embeddedSurface || !candidate.active || !candidate.drawingsVisible || !candidate.previewEl?.isConnected || this.controllerStateKey(candidate) !== key) {
        continue;
      }
      const rect = candidate.canvas?.getBoundingClientRect?.();
      if (!rect || event.clientX < rect.left || event.clientX > rect.right || event.clientY < rect.top || event.clientY > rect.bottom) {
        continue;
      }
      if (candidate.findStrokeAt(candidate.eventToPoint(event), { x: event.clientX, y: event.clientY }) >= 0) {
        return candidate;
      }
    }
    return null;
  }
  setControllerToolbarState(controller, state) {
    const key = this.controllerStateKey(controller);
    if (!key) {
      return;
    }
    const next = {
      ...this.viewToolbarState.get(key) || {},
      ...state
    };
    this.viewToolbarState.set(key, next);
    for (const candidate of this.liveControllers) {
      if (!candidate.destroyed && candidate !== controller && this.controllerStateKey(candidate) === key) {
        candidate.applySharedToolbarState(next);
      }
    }
  }
  controllerHistoryState(controller) {
    const key = this.controllerStateKey(controller);
    if (!key) {
      return null;
    }
    let state = this.viewEditHistory.get(key);
    if (!state) {
      state = { undo: [], redo: [] };
      this.viewEditHistory.set(key, state);
    }
    return state;
  }
  recordControllerHistory(controller, entry, options = {}) {
    const state = this.controllerHistoryState(controller);
    if (!state || !entry) {
      return;
    }
    const last = state.undo[state.undo.length - 1];
    if (options.coalesceKey && last?.coalesceKey === options.coalesceKey && last.kind === entry.kind && last.file?.path === entry.file?.path) {
      last.after = entry.after;
    } else {
      state.undo.push({ ...entry, coalesceKey: options.coalesceKey || null });
      if (state.undo.length > 100) {
        state.undo.shift();
      }
    }
    state.redo = [];
  }
  async undoControllerHistory(controller) {
    const state = this.controllerHistoryState(controller);
    const entry = state?.undo.pop();
    if (!entry) {
      return false;
    }
    if (!await this.applyControllerHistoryEntry(entry, "before", controller)) {
      state.undo.push(entry);
      return false;
    }
    state.redo.push(entry);
    return true;
  }
  async redoControllerHistory(controller) {
    const state = this.controllerHistoryState(controller);
    const entry = state?.redo.pop();
    if (!entry) {
      return false;
    }
    if (!await this.applyControllerHistoryEntry(entry, "after", controller)) {
      state.redo.push(entry);
      return false;
    }
    state.undo.push(entry);
    return true;
  }
  refreshControllerAfterMarkdownHistory(controller) {
    if (!controller || controller.destroyed || !controller.previewEl?.isConnected) {
      return;
    }
    window.setTimeout(() => {
      if (controller.destroyed || !controller.previewEl?.isConnected) {
        return;
      }
      controller.scheduleMarkdownAnnotationRefresh({ layout: controller.hasNoteFlowElements(), delay: 0, force: true });
      controller.scheduleEmbedRepair(0);
      controller.scheduleResize({ layout: false, measure: true });
    }, 48);
  }
  async applyControllerHistoryEntry(entry, direction, controller = null) {
    if (entry.kind === "compound") {
      const markdownFile = getVaultFileByPath(this.app.vault, entry.markdownFile?.path);
      const drawingFile = getVaultFileByPath(this.app.vault, entry.drawingFile?.path) || entry.drawingFile;
      if (!markdownFile || !drawingFile) {
        return false;
      }
      const current = await this.app.vault.read(markdownFile);
      const expected = direction === "before" ? entry.markdownAfter : entry.markdownBefore;
      if (current !== String(expected || "")) {
        return false;
      }
      await this.app.vault.modify(markdownFile, String(direction === "before" ? entry.markdownBefore : entry.markdownAfter || ""));
      const data = cloneDrawingData(direction === "before" ? entry.drawingBefore : entry.drawingAfter, drawingFile);
      this.scheduleDrawingSave(drawingFile, data, { replace: true });
      this.emitApiEvent("markdown-changed", { file: markdownFile.path, history: direction });
      this.refreshControllerAfterMarkdownHistory(controller);
      return true;
    }
    if (entry.kind === "drawing") {
      const data = cloneDrawingData(entry[direction], entry.file);
      this.scheduleDrawingSave(entry.file, data, { replace: true });
      return true;
    }
    if (entry.kind === "markdown") {
      const file2 = getVaultFileByPath(this.app.vault, entry.file?.path);
      if (!file2) {
        return false;
      }
      const current = await this.app.vault.read(file2);
      const expected = direction === "before" ? entry.after : entry.before;
      if (current !== String(expected || "")) {
        return false;
      }
      await this.app.vault.modify(file2, String(entry[direction] || ""));
      this.emitApiEvent("markdown-changed", { file: file2.path, history: direction });
      this.refreshControllerAfterMarkdownHistory(controller);
      return true;
    }
    return false;
  }
  setControllerActivation(controller, active) {
    const key = this.controllerStateKey(controller);
    const enabled = Boolean(active);
    if (key) {
      this.viewDrawingActive.set(key, enabled);
    }
    this.reconcileControllerActivation(controller);
  }
  reconcileControllerActivation(controller = null) {
    const key = this.controllerStateKey(controller);
    if (!key) {
      return null;
    }
    const candidates = Array.from(this.liveControllers).filter((candidate) => {
      return !candidate.destroyed && this.controllerStateKey(candidate) === key;
    });
    const enabled = Boolean(this.viewDrawingActive.get(key));
    const visible = candidates.filter((candidate) => {
      return !candidate.embeddedSurface && candidate.previewEl?.isConnected && isElementVisibleEnough(candidate.previewEl);
    });
    const preferred = enabled ? visible.find((candidate) => candidate === controller) || visible.find((candidate) => candidate === this.viewInteractionController.get(key)) || visible[0] || (controller && !controller.embeddedSurface && controller.previewEl?.isConnected ? controller : null) : null;
    for (const candidate of candidates) {
      const nextActive = Boolean(enabled && candidate === preferred);
      if (candidate.active !== nextActive) {
        candidate.applyActiveState(nextActive, { eager: nextActive || !enabled });
      }
    }
    if (preferred) {
      this.setInteractionController(preferred);
    }
    return preferred;
  }
  installWebviewObserver() {
    if (typeof MutationObserver === "undefined" || !activeDocument?.body) {
      return;
    }
    this.webviewMutationObserver = new MutationObserver((mutations) => {
      if (mutations.some((mutation) => isEmbeddedSurfaceSyncMutation(mutation))) {
        this.scheduleEmbeddedMarkdownSync();
      }
      if (mutations.some((mutation) => isWebviewSyncMutation(mutation))) {
        this.scheduleWebviewSync();
      }
      if (mutations.some((mutation) => isWorkspaceSurfaceMutation(mutation))) {
        this.scheduleWorkspaceSync();
      }
      if (mutations.some((mutation) => isFloatingControlsVisibilityMutation(mutation))) {
        this.scheduleFloatingControlsSync();
      }
    });
    this.webviewMutationObserver.observe(activeDocument.body, {
      subtree: true,
      childList: true,
      attributes: true,
      attributeFilter: ["data-url", "src", "class"]
    });
  }
  scheduleFloatingControlsSync() {
    if (this.floatingControlsSyncTimer !== null) {
      return;
    }
    this.floatingControlsSyncTimer = window.setTimeout(() => {
      this.floatingControlsSyncTimer = null;
      for (const controller of this.liveControllers) {
        controller.syncFloatingControlClasses();
      }
    }, 40);
  }
  scheduleWebviewSync() {
    if (this.webviewSyncTimer !== null) {
      return;
    }
    this.webviewSyncTimer = window.setTimeout(() => {
      this.webviewSyncTimer = null;
      this.syncWebviewControllers();
    }, 120);
  }
  scheduleEmbeddedMarkdownSync() {
    if (this.runtimeDisposed) {
      return;
    }
    if (this.embeddedSyncTimer !== null) {
      window.clearTimeout(this.embeddedSyncTimer);
    }
    this.embeddedSyncTimer = window.setTimeout(() => {
      this.embeddedSyncTimer = null;
      if (!this.runtimeDisposed) {
        this.syncEmbeddedMarkdownControllers();
      }
    }, 80);
  }
  scheduleWorkspaceSync() {
    if (this.workspaceSyncTimer !== null) {
      window.clearTimeout(this.workspaceSyncTimer);
    }
    this.workspaceSyncTimer = window.setTimeout(() => {
      this.workspaceSyncTimer = null;
      this.syncWorkspaceControllers();
    }, 100);
  }
  createPublicApi() {
    const capabilities = Object.freeze({
      responsiveCoordinates: RESPONSIVE_POINT_BASIS,
      responsiveElements: ELEMENT_LAYOUT_BASIS,
      embeddedMarkdownEditing: true,
      readingViewEditing: true,
      sourceViewEditing: true,
      readingViewZoom: true,
      sourceViewLayoutZoom: true,
      workspaceSurfaces: true,
      imageWorkspaceSurfaces: true,
      registeredSurfaces: true,
      surfaceHandles: true,
      regionEditing: true,
      externalFormats: ["html", "docx", "xlsx", "pptx", "pdf", "web"],
      elementClipboard: true,
      readingNoteFlow: true,
      editableMindMaps: true,
      boundConnectors: true,
      boxedConnectorTargets: true,
      hardTextBindings: true,
      groupSelection: true,
      frameStyles: true,
      dataSchemaVersion: 2,
      persistentHeaderActions: true,
      stateBackedWorkspaceSurfaces: true,
      agentActions: true,
      settingsApi: true,
      drawingDataExchange: ["read", "parse", "normalize", "inspect", "serialize"],
      brushVariants: {
        [BRUSH_PEN]: [BRUSH_VARIANT_DEFAULT, PEN_VARIANT_FOUNTAIN, PEN_VARIANT_NOTE, PEN_VARIANT_LASER],
        [BRUSH_WATERCOLOR]: [BRUSH_VARIANT_DEFAULT, WATERCOLOR_VARIANT_TEXT, WATERCOLOR_VARIANT_STRAIGHT]
      },
      events: ["drawings-changed", "markdown-changed", "surface-changed"],
      tools: [TOOL_DRAW, TOOL_SELECT, TOOL_EDIT_MD, TOOL_TEXT],
      sources: ["vault", "url", "virtual"],
      surfaceActions: ["activate", "deactivate", "toggle", "set-visibility", "refresh", "focus-region", "update-source"],
      elementActions: ["get", "select", "update", "delete", "reorder", "set-locked", "set-note-flow", "copy", "copy-link", "paste", "create", "highlight-region", "insert-ink"],
      historyActions: ["undo", "redo"]
    });
    const drawingData = Object.freeze({
      read: async (fileOrPath, options = {}) => this.readDrawingDataApi(fileOrPath, options),
      parse: async (input, options = {}) => this.parseDrawingDataApi(input, options),
      normalize: async (input, options = {}) => this.parseDrawingDataApi(input, options),
      inspect: async (input, options = {}) => this.inspectDrawingDataApi(input, options),
      serialize: async (data, options = {}) => this.serializeDrawingDataApi(data, options)
    });
    const v1 = {
      apiVersion: "1.0",
      capabilities,
      drawingData,
      resolveFile: (fileOrPath, sourcePath = "") => this.resolveApiFile(fileOrPath, sourcePath),
      registerSurface: (options = {}) => this.registerApiSurface(options),
      listSurfaces: () => this.getAllControllers().map((controller) => this.describeController(controller)),
      getActiveSurface: () => this.describeController(this.getActiveController()),
      getState: (options = {}) => this.getApiState(options),
      activate: async (options = {}) => this.activateApi(options),
      deactivate: (options = {}) => this.deactivateApi(options),
      toggle: async (options = {}) => this.toggleApi(options),
      setVisibility: async (visible, options = {}) => this.setApiVisibility(visible, options),
      setTool: (tool, options = {}) => this.setApiTool(tool, options),
      setBrush: (options = {}) => this.setApiBrush(options),
      setTextPreset: (preset, options = {}) => this.setApiTextPreset(preset, options),
      getZoom: (options = {}) => this.describeController(this.findApiController(options))?.zoom ?? 1,
      setZoom: (zoom, options = {}) => this.setApiZoom(zoom, options),
      getElements: async (options = {}) => this.getApiElements(options),
      getElementById: async (id, options = {}) => this.getApiElementById(id, options),
      getConnections: async (options = {}) => this.getApiConnections(options),
      getTextBindings: async (options = {}) => this.getApiTextBindings(options),
      selectElements: async (options = {}) => this.selectApiElements(options),
      updateElements: async (options = {}) => this.updateApiElements(options),
      deleteElements: async (options = {}) => this.deleteApiElements(options),
      reorderElements: async (options = {}) => this.reorderApiElements(options),
      setElementsLocked: async (options = {}) => this.setApiElementsLocked(options),
      setElementsNoteFlow: async (options = {}) => this.setApiElementsNoteFlow(options),
      undo: async (options = {}) => this.runApiHistory("undo", options),
      redo: async (options = {}) => this.runApiHistory("redo", options),
      getSettings: () => ({ ...this.noteDrawSettings }),
      updateSettings: async (patch = {}) => this.updateApiSettings(patch),
      execute: async (action, options = {}) => this.executeApiAction(action, options),
      readDrawings: async (fileOrPath) => {
        const file2 = this.resolveApiFile(fileOrPath);
        if (!file2) {
          throw new Error("NoteDraw could not resolve the requested file");
        }
        return this.readDrawings(file2);
      },
      readDrawingData: drawingData.read,
      parseDrawingData: drawingData.parse,
      serializeDrawingData: drawingData.serialize,
      writeDrawings: async (fileOrPath, data) => {
        const file2 = this.resolveApiFile(fileOrPath);
        if (!file2) {
          throw new Error("NoteDraw could not resolve the requested file");
        }
        const normalized = normalizeDrawingData(data, file2);
        await this.writeDrawings(file2, normalized);
        this.refreshControllersForFile(file2, normalized);
        return normalized;
      },
      getStoragePaths: (fileOrPath) => {
        const file2 = this.resolveApiFile(fileOrPath);
        return file2 ? {
          current: this.drawingPathForFile(file2),
          legacy: this.legacyDrawingPathForFile(file2)
        } : null;
      },
      replaceText: async (options) => this.replaceTextApi(options),
      insertStroke: async (fileOrPath, stroke) => this.insertStrokeApi(fileOrPath, stroke),
      createElements: async (options = {}) => this.insertApiElements(options),
      highlightRegion: async (options = {}) => this.insertSurfaceRegionApi("highlight", options),
      insertInk: async (options = {}) => this.insertSurfaceRegionApi("ink", options),
      focusRegion: async (region, options = {}) => this.focusSurfaceRegionApi(region, options),
      updateSurfaceSource: async (source, options = {}) => this.updateRegisteredSurfaceSourceApi(source, options),
      copyElements: (options = {}) => this.copyElementsApi(options),
      copyElementLink: (options = {}) => this.findApiController(options)?.copySelectedElementLink({ quiet: Boolean(options.quiet) }) || { ok: false, reason: "surface-not-found" },
      pasteElements: (options = {}) => this.pasteElementsApi(options),
      insertMindMap: async (options = {}) => this.insertMindMapApi(options),
      refresh: async (fileOrPath) => {
        const file2 = this.resolveApiFile(fileOrPath);
        if (!file2) {
          return { ok: false, reason: "missing-file" };
        }
        const data = await this.readDrawings(file2);
        return { ok: true, refreshed: this.refreshControllersForFile(file2, data) };
      },
      injectExportSnapshot: async (fileOrPath, container) => {
        const file2 = this.resolveApiFile(fileOrPath);
        return file2 ? this.injectExportSnapshot(file2, container) : null;
      },
      on: (eventName, listener) => this.onApiEvent(eventName, listener)
    };
    return {
      version: this.manifest.version,
      apiVersion: v1.apiVersion,
      capabilities,
      v1,
      drawingData,
      resolveFile: v1.resolveFile,
      registerSurface: v1.registerSurface,
      listSurfaces: v1.listSurfaces,
      getActiveSurface: v1.getActiveSurface,
      getState: v1.getState,
      activate: v1.activate,
      deactivate: v1.deactivate,
      toggle: v1.toggle,
      setVisibility: v1.setVisibility,
      setTool: v1.setTool,
      setBrush: v1.setBrush,
      setTextPreset: v1.setTextPreset,
      getZoom: v1.getZoom,
      setZoom: v1.setZoom,
      getElements: v1.getElements,
      getElementById: v1.getElementById,
      getConnections: v1.getConnections,
      getTextBindings: v1.getTextBindings,
      selectElements: v1.selectElements,
      updateElements: v1.updateElements,
      deleteElements: v1.deleteElements,
      reorderElements: v1.reorderElements,
      setElementsLocked: v1.setElementsLocked,
      setElementsNoteFlow: v1.setElementsNoteFlow,
      undo: v1.undo,
      redo: v1.redo,
      getSettings: v1.getSettings,
      updateSettings: v1.updateSettings,
      execute: v1.execute,
      getActiveController: () => this.getActiveController(),
      readDrawings: v1.readDrawings,
      readDrawingData: v1.readDrawingData,
      parseDrawingData: v1.parseDrawingData,
      normalizeDrawingData: drawingData.normalize,
      inspectDrawingData: drawingData.inspect,
      serializeDrawingData: v1.serializeDrawingData,
      writeDrawings: v1.writeDrawings,
      getStoragePaths: v1.getStoragePaths,
      refresh: v1.refresh,
      injectExportSnapshot: v1.injectExportSnapshot,
      replaceSelectionText: async (file2, originalText, editedText) => this.replaceTextApi({ file: file2, originalText, editedText }),
      insertStroke: v1.insertStroke,
      createElements: v1.createElements,
      highlightRegion: v1.highlightRegion,
      insertInk: v1.insertInk,
      focusRegion: v1.focusRegion,
      updateSurfaceSource: v1.updateSurfaceSource,
      copyElements: v1.copyElements,
      copyElementLink: v1.copyElementLink,
      pasteElements: v1.pasteElements,
      insertMindMap: v1.insertMindMap,
      on: v1.on
    };
  }
  resolveApiFile(fileOrPath, sourcePath = "") {
    if (fileOrPath && typeof fileOrPath === "object" && typeof fileOrPath.path === "string") {
      return getVaultFileByPath(this.app.vault, fileOrPath.path) || fileOrPath;
    }
    const path = normalizeVaultPath(fileOrPath);
    if (!path) {
      return null;
    }
    const surfaceFile = this.getAllControllers().find((controller) => normalizeVaultPath(controller.file?.path) === path)?.file;
    return getVaultFileByPath(this.app.vault, path) || surfaceFile || this.app.metadataCache.getFirstLinkpathDest?.(path, sourcePath || "") || null;
  }
  async readDrawingDataApi(fileOrPath, options = {}) {
    const file2 = this.resolveApiFile(fileOrPath, options.sourcePath || "");
    if (!file2) {
      throw new Error("NoteDraw could not resolve the requested file");
    }
    const key = normalizeVaultPath(file2.path);
    const controller = this.getAllControllers().find((candidate) => normalizeVaultPath(candidate?.file?.path || "") === key && candidate?.drawingsLoaded && candidate?.drawingData);
    const drawing = normalizeDrawingDataForStorage(
      controller?.drawingData || await this.readDrawings(file2, { migrateLegacy: false }),
      file2
    );
    if (options.includeResources === false) {
      return clonePortableValue({
        format: "notedraw-portable",
        version: 1,
        purpose: "storage",
        sourcePath: key,
        updatedAt: drawing.updatedAt || null,
        drawing,
        resources: [],
        skippedResources: []
      });
    }
    const sourceMarkdown = typeof options.sourceMarkdown === "string" ? options.sourceMarkdown : await this.app.vault.cachedRead(file2);
    return clonePortableValue(await this.createPortableBundle(file2, drawing, {
      purpose: options.purpose === "share" ? "share" : "storage",
      sourceMarkdown,
      includeMarkdownLinks: options.includeMarkdownLinks === true
    }));
  }
  async parseDrawingDataApi(input, options = {}) {
    let value = input;
    if (typeof value === "string") {
      const decoded = await decodeNotedrawDataBlock(value);
      if (decoded) {
        value = decoded;
      } else {
        try {
          value = JSON.parse(value);
        } catch {
          throw new Error("NoteDraw data is neither an embedded block nor valid JSON");
        }
      }
    }
    const sourcePath = normalizeVaultPath(
      options.sourcePath || options.file?.path || value?.sourcePath || value?.drawing?.sourcePath || "NoteDraw.md"
    ) || "NoteDraw.md";
    const file2 = this.resolveApiFile(options.file || sourcePath) || createDrawingDataFile(sourcePath);
    const bundle = value?.drawing ? normalizePortableBundle(value, file2) : normalizePortableBundle({
      format: "notedraw-portable",
      version: 1,
      purpose: "storage",
      sourcePath,
      updatedAt: value?.updatedAt || null,
      drawing: value,
      resources: options.resources || []
    }, file2);
    if (!bundle) {
      throw new Error("NoteDraw data is invalid");
    }
    return clonePortableValue(bundle);
  }
  async inspectDrawingDataApi(input, options = {}) {
    const bundle = await this.parseDrawingDataApi(input, options);
    const drawing = bundle?.drawing || {};
    const strokes = Array.isArray(drawing.strokes) ? drawing.strokes : [];
    const markdownBlocks = Array.isArray(drawing.markdownBlocks) ? drawing.markdownBlocks : [];
    const elementGroups = Array.isArray(drawing.elementGroups) ? drawing.elementGroups : [];
    return {
      ok: true,
      format: bundle.format || "notedraw-portable",
      bundleVersion: bundle.version || 1,
      dataSchemaVersion: 2,
      sourcePath: bundle.sourcePath || drawing.sourcePath || "",
      counts: {
        strokes: strokes.length,
        markdownBlocks: markdownBlocks.length,
        groups: elementGroups.length,
        connectors: strokes.filter(isConnectorStroke).length,
        textBindings: strokes.filter((stroke) => normalizeTextHighlightAnchor(stroke?.textAnchor)?.ownerId).length,
        resources: Array.isArray(bundle.resources) ? bundle.resources.length : 0
      }
    };
  }
  async serializeDrawingDataApi(input, options = {}) {
    const bundle = await this.parseDrawingDataApi(input, options);
    const format = String(options.format || "json").trim().toLowerCase();
    if (format === "object") {
      return bundle;
    }
    if (format === "json") {
      return JSON.stringify(bundle, null, options.pretty === false ? 0 : 2);
    }
    if (format === "block") {
      return encodeNotedrawDataBlock(bundle, { compress: options.compress !== false });
    }
    if (format === "markdown") {
      return appendNotedrawDataBlock(options.markdown || "", bundle, { compress: options.compress !== false });
    }
    throw new Error(`Unsupported NoteDraw serialization format: ${format}`);
  }
  registeredSurfaceViewportState(controller) {
    const viewport = controller?.registeredSurfaceViewport;
    if (!viewport) {
      return null;
    }
    let zoom = null;
    let scroll = null;
    try {
      if (typeof viewport.getZoom === "function") {
        const value = Number(viewport.getZoom());
        zoom = Number.isFinite(value) && value > 0 ? value : null;
      }
    } catch (error) {
      void error;
    }
    try {
      if (typeof viewport.getScroll === "function") {
        const value = viewport.getScroll() || {};
        scroll = {
          left: Number.isFinite(Number(value.left)) ? Number(value.left) : 0,
          top: Number.isFinite(Number(value.top)) ? Number(value.top) : 0
        };
      }
    } catch (error) {
      void error;
    }
    return { zoom, scroll };
  }
  describeController(controller) {
    if (!controller || controller.destroyed) {
      return null;
    }
    const viewport = this.registeredSurfaceViewportState(controller);
    return {
      file: controller.file?.path || "",
      surface: controller.surfaceType,
      viewType: controller.workspaceViewType || controller.surfaceType,
      active: Boolean(controller.active),
      tool: controller.toolMode,
      brush: controller.brushMode,
      variant: controller.currentBrushVariant?.() || BRUSH_VARIANT_DEFAULT,
      zoom: viewport?.zoom ?? (controller.readingZoom || 1),
      scroll: viewport?.scroll || null,
      visible: Boolean(controller.drawingsVisible),
      drawingsLoaded: Boolean(controller.drawingsLoaded),
      strokeCount: controller.drawingData?.strokes?.length || 0,
      selected: controller.getSelectedStrokeIndexes?.() || [],
      owner: controller.registeredSurfaceOwner || "",
      id: controller.registeredSurfaceId || "",
      surfaceId: controller.registeredSurfaceId || "",
      surfaceKey: controller.registeredSurfaceKey || "",
      source: controller.registeredSurfaceSource ? { ...controller.registeredSurfaceSource } : null,
      capabilities: controller.registeredSurfaceCapabilities ? { ...controller.registeredSurfaceCapabilities } : null,
      customSurface: Boolean(controller.registeredSurface),
      coordinateBasis: RESPONSIVE_POINT_BASIS
    };
  }
  findApiController(options = {}) {
    const requestedPath = normalizeVaultPath(options.file?.path || options.path || options.file || "");
    const requestedSurface = String(options.surface || "").trim();
    const requestedOwner = String(options.owner || options.surfaceOwner || "").trim();
    const requestedId = String(options.id || options.surfaceId || "").trim();
    const requestedKey = String(options.surfaceKey || "").trim();
    const requestedSource = normalizeRegisteredSurfaceSource(options.source || {});
    const controllers = this.getAllControllers();
    const matches = controllers.filter((controller) => {
      if (requestedPath && normalizeVaultPath(controller.file?.path) !== requestedPath) {
        return false;
      }
      if (requestedSurface && controller.surfaceType !== requestedSurface) {
        return false;
      }
      if (requestedOwner && controller.registeredSurfaceOwner !== requestedOwner) {
        return false;
      }
      if (requestedId && controller.registeredSurfaceId !== requestedId && controller.registeredSurfaceKey !== requestedId) {
        return false;
      }
      if (requestedKey && controller.registeredSurfaceKey !== requestedKey) {
        return false;
      }
      if (requestedSource.kind) {
        const source = controller.registeredSurfaceSource || {};
        if (requestedSource.kind !== source.kind) {
          return false;
        }
        if (requestedSource.path && normalizeVaultPath(requestedSource.path) !== normalizeVaultPath(source.path || controller.file?.path || "")) {
          return false;
        }
        if (requestedSource.url && requestedSource.url !== source.url) {
          return false;
        }
        if (requestedSource.key && requestedSource.key !== source.key) {
          return false;
        }
      }
      return controller.previewEl?.isConnected;
    });
    return matches.find((controller) => isElementVisibleEnough(controller.previewEl)) || matches.find((controller) => controller.active) || matches[0] || this.getActiveController();
  }
  async activateApi(options = {}) {
    const controller = this.findApiController(options);
    if (!controller) {
      return { ok: false, reason: "surface-not-found" };
    }
    if (!controller.active) {
      await controller.toggle();
    }
    if (options.tool) {
      controller.setToolFromApi(options.tool, options);
    }
    if (options.zoom !== void 0 && controller.canZoomReadingSurface?.()) {
      controller.setReadingZoom(options.zoom);
    }
    const surface = this.describeController(controller);
    this.emitApiEvent("surface-changed", surface);
    return { ok: true, surface };
  }
  setApiTool(tool, options = {}) {
    if (tool && typeof tool === "object") {
      options = { ...tool, ...options };
      tool = options.tool;
    }
    const controller = this.findApiController(options);
    if (!controller) {
      return { ok: false, reason: "surface-not-found" };
    }
    const ok = controller.setToolFromApi(tool, options);
    const surface = this.describeController(controller);
    if (ok) {
      this.emitApiEvent("surface-changed", surface);
    }
    return { ok, surface };
  }
  setApiZoom(zoom, options = {}) {
    const controller = this.findApiController(options);
    const viewport = controller?.registeredSurfaceViewport;
    if (controller && typeof viewport?.setZoom === "function") {
      const nextZoom = Number(zoom);
      if (!Number.isFinite(nextZoom) || nextZoom <= 0) {
        return { ok: false, reason: "invalid-zoom" };
      }
      try {
        viewport.setZoom(nextZoom);
        controller.scheduleLayoutRefresh({ settle: false });
        controller.requestRender(true);
        const surface2 = this.describeController(controller);
        this.emitApiEvent("surface-changed", { ...surface2, phase: "viewport" });
        return { ok: true, surface: surface2 };
      } catch (error) {
        return { ok: false, reason: "viewport-set-zoom-failed", message: String(error?.message || error) };
      }
    }
    if (!controller || !controller.canZoomReadingSurface?.()) {
      return { ok: false, reason: "reading-surface-not-found" };
    }
    const ok = controller.setReadingZoom(zoom);
    const surface = this.describeController(controller);
    if (ok) {
      this.emitApiEvent("surface-changed", surface);
    }
    return { ok, surface };
  }
  async getLoadedApiController(options = {}) {
    const controller = this.findApiController(options);
    if (!controller) {
      return null;
    }
    await controller.ensureDrawingsLoaded();
    return controller;
  }
  async getApiState(options = {}) {
    const controller = await this.getLoadedApiController(options);
    return controller ? { ok: true, surface: this.describeController(controller) } : { ok: false, reason: "surface-not-found" };
  }
  deactivateApi(options = {}) {
    const controller = this.findApiController(options);
    if (!controller) {
      return { ok: false, reason: "surface-not-found" };
    }
    this.setControllerActivation(controller, false);
    const surface = this.describeController(controller);
    this.emitApiEvent("surface-changed", { ...surface, phase: "updated" });
    return { ok: true, surface };
  }
  async toggleApi(options = {}) {
    const controller = this.findApiController(options);
    if (!controller) {
      return { ok: false, reason: "surface-not-found" };
    }
    await controller.toggle();
    const surface = this.describeController(controller);
    this.emitApiEvent("surface-changed", { ...surface, phase: "updated" });
    return { ok: true, surface };
  }
  async setApiVisibility(visible, options = {}) {
    if (visible && typeof visible === "object") {
      options = { ...visible, ...options };
      visible = options.visible;
    }
    const controller = await this.getLoadedApiController(options);
    if (!controller) {
      return { ok: false, reason: "surface-not-found" };
    }
    controller.setDrawingsVisible(visible !== false, { persist: true });
    const surface = this.describeController(controller);
    this.emitApiEvent("surface-changed", { ...surface, phase: "updated" });
    return { ok: true, surface };
  }
  setApiBrush(options = {}) {
    const controller = this.findApiController(options);
    if (!controller) {
      return { ok: false, reason: "surface-not-found" };
    }
    if (options.color !== void 0 && !isCssColor(options.color)) {
      return { ok: false, reason: "invalid-color" };
    }
    const brush = options.brush === BRUSH_WATERCOLOR ? BRUSH_WATERCOLOR : BRUSH_PEN;
    controller.brushVariants[brush] = normalizeBrushVariant(brush, options.variant ?? controller.brushVariants[brush]);
    controller.setBrushMode(brush);
    const settings = controller.currentBrushSettings();
    if (options.color !== void 0) {
      settings.color = options.color;
    }
    if (options.width !== void 0 && Number.isFinite(Number(options.width))) {
      settings.width = clamp10(Number(options.width), MIN_BRUSH_WIDTH, MAX_BRUSH_WIDTH);
    }
    if (options.opacity !== void 0 && Number.isFinite(Number(options.opacity))) {
      settings.opacity = clamp10(Number(options.opacity), 0, 1);
    }
    if (options.count !== void 0 && Number.isFinite(Number(options.count))) {
      settings.count = clamp10(Math.round(Number(options.count)), 1, MAX_PEN_COUNT);
    }
    controller.syncCurrentBrushFields();
    controller.syncPaletteInputs();
    controller.updateToolButtons();
    controller.persistCurrentBrushSettings();
    controller.persistInteractionSettings();
    controller.syncSharedToolbarState();
    controller.render();
    const surface = this.describeController(controller);
    this.emitApiEvent("surface-changed", { ...surface, phase: "updated" });
    return { ok: true, surface, brush: { brush, variant: controller.currentBrushVariant(), ...settings } };
  }
  setApiTextPreset(preset, options = {}) {
    if (preset && typeof preset === "object") {
      options = { ...preset, ...options };
      preset = options.preset;
    }
    const controller = this.findApiController(options);
    if (!controller) {
      return { ok: false, reason: "surface-not-found" };
    }
    controller.textPreset = normalizeTextPreset(preset);
    controller.setTextMode();
    controller.syncTextPanelButtons();
    controller.syncSharedToolbarState();
    const surface = this.describeController(controller);
    this.emitApiEvent("surface-changed", { ...surface, phase: "updated" });
    return { ok: true, surface, preset: controller.textPreset };
  }
  resolveApiElementIndexes(controller, options = {}, fallbackToSelection = true) {
    if (!controller?.drawingData?.strokes) {
      return [];
    }
    if (options.clear) {
      return [];
    }
    const selection = options.selection ?? options;
    if (selection === "all" || options.all === true || selection?.all === true) {
      return controller.drawingData.strokes.map((_, index) => index);
    }
    const rawIndexes = Array.isArray(selection) ? selection.filter((value) => Number.isInteger(Number(value))).map(Number) : selection?.indexes ?? options.indexes;
    const rawIds = Array.isArray(selection) ? selection.filter((value) => typeof value === "string") : selection?.ids ?? selection?.elementIds ?? options.ids ?? options.elementIds;
    const indexes = Array.isArray(rawIndexes) ? rawIndexes.map(Number).filter((index) => Number.isInteger(index) && index >= 0 && index < controller.drawingData.strokes.length) : [];
    const ids = new Set(Array.isArray(rawIds) ? rawIds.map(String) : []);
    if (ids.size) {
      controller.drawingData.strokes.forEach((stroke, index) => {
        if (ids.has(strokeElementId(stroke))) {
          indexes.push(index);
        }
      });
    }
    if (Array.isArray(rawIndexes) || Array.isArray(rawIds)) {
      return Array.from(new Set(indexes)).sort((a, b) => a - b);
    }
    return fallbackToSelection ? controller.getSelectedStrokeIndexes() : [];
  }
  async getApiElements(options = {}) {
    const controller = await this.getLoadedApiController(options);
    if (!controller) {
      return { ok: false, reason: "surface-not-found", elements: [] };
    }
    const selected = new Set(controller.getSelectedStrokeIndexes());
    const requested = this.resolveApiElementIndexes(controller, options, false);
    const indexes = options.all === true || options.selection === "all" || requested.length ? requested : controller.drawingData.strokes.map((_, index) => index);
    const include = new Set(Array.isArray(options.include) ? options.include : ["strokes"]);
    const strokes = indexes.map((index) => ({ index, selected: selected.has(index), ...JSON.parse(JSON.stringify(controller.drawingData.strokes[index])) }));
    const result = {
      ok: true,
      surface: this.describeController(controller),
      elements: strokes
    };
    if (include.has("markdownBlocks")) {
      result.markdownBlocks = clonePortableValue(controller.markdownBlockRecords());
    }
    if (include.has("groups")) {
      result.groups = clonePortableValue(controller.elementGroupRecords());
    }
    if (include.has("connectors")) {
      result.connectors = strokes.filter(isConnectorStroke);
    }
    return result;
  }
  async getApiElementById(id, options = {}) {
    const controller = await this.getLoadedApiController(options);
    const wanted = String(id || "");
    if (!controller || !wanted) {
      return null;
    }
    if (wanted.startsWith("md:")) {
      return clonePortableValue(controller.markdownBlockRecords().find((block) => block.id === wanted.slice(3)) || null);
    }
    if (wanted.startsWith("group:")) {
      return clonePortableValue(controller.elementGroup(wanted.slice(6)) || null);
    }
    return clonePortableValue(controller.drawingData.strokes.find((stroke) => strokeElementId(stroke) === wanted) || null);
  }
  async getApiConnections(options = {}) {
    const controller = await this.getLoadedApiController(options);
    if (!controller) {
      return [];
    }
    return clonePortableValue(controller.drawingData.strokes.map((stroke, index) => ({
      index,
      elementId: strokeElementId(stroke),
      connector: normalizeConnector(stroke?.connector)
    })).filter((item) => item.connector));
  }
  async getApiTextBindings(options = {}) {
    const controller = await this.getLoadedApiController(options);
    if (!controller) {
      return [];
    }
    return clonePortableValue(controller.drawingData.strokes.map((stroke, index) => ({
      index,
      elementId: strokeElementId(stroke),
      textAnchor: normalizeTextHighlightAnchor(stroke?.textAnchor)
    })).filter((item) => item.textAnchor));
  }
  async selectApiElements(options = {}) {
    const controller = await this.getLoadedApiController(options);
    if (!controller) {
      return { ok: false, reason: "surface-not-found" };
    }
    const indexes = this.resolveApiElementIndexes(controller, options, false);
    if (options.tool !== false && controller.toolMode !== TOOL_SELECT) {
      controller.toggleSelectMode();
    }
    controller.setSelectedStrokes(indexes);
    controller.render();
    const surface = this.describeController(controller);
    this.emitApiEvent("surface-changed", { ...surface, phase: "updated" });
    return { ok: true, selected: indexes, surface };
  }
  commitApiDrawingMutation(controller, before, indexes = []) {
    controller.redoStack = [];
    controller.rebuildElementRelations();
    controller.invalidateStaticCache();
    controller.setSelectedStrokes(indexes);
    this.scheduleDrawingSave(controller.file, controller.drawingData, { userOperation: true });
    controller.recordDrawingHistory(before);
    controller.scheduleNoteFlowLayout({
      operation: indexes.some((index) => controller.drawingData.strokes[index]?.noteFlow?.enabled)
    });
    controller.scheduleLayoutRefresh({ settle: false });
    controller.render();
  }
  async updateApiElements(options = {}) {
    const controller = await this.getLoadedApiController(options);
    const patch = options.patch;
    if (!controller) {
      return { ok: false, reason: "surface-not-found" };
    }
    if (!patch || typeof patch !== "object" || Array.isArray(patch)) {
      return { ok: false, reason: "missing-patch" };
    }
    const indexes = this.resolveApiElementIndexes(controller, options);
    if (!indexes.length) {
      return { ok: false, reason: "selection-not-found" };
    }
    const before = controller.captureDrawingHistorySnapshot();
    for (const index of indexes) {
      const current = controller.drawingData.strokes[index];
      controller.drawingData.strokes[index] = normalizeStroke({ ...current, ...patch });
    }
    this.commitApiDrawingMutation(controller, before, indexes);
    return { ok: true, updated: indexes.length, selected: indexes, surface: this.describeController(controller) };
  }
  async deleteApiElements(options = {}) {
    const controller = await this.getLoadedApiController(options);
    if (!controller) {
      return { ok: false, reason: "surface-not-found" };
    }
    const indexes = this.resolveApiElementIndexes(controller, options);
    if (!indexes.length) {
      return { ok: false, reason: "selection-not-found" };
    }
    const before = controller.drawingData.strokes.length;
    controller.setSelectedStrokes(indexes);
    controller.deleteSelectedStroke();
    return { ok: true, deleted: before - controller.drawingData.strokes.length, surface: this.describeController(controller) };
  }
  async reorderApiElements(options = {}) {
    const controller = await this.getLoadedApiController(options);
    const direction = String(options.direction || "front").toLowerCase();
    if (!controller) {
      return { ok: false, reason: "surface-not-found" };
    }
    if (!["front", "forward", "backward", "back"].includes(direction)) {
      return { ok: false, reason: "invalid-direction" };
    }
    const indexes = this.resolveApiElementIndexes(controller, options);
    if (!indexes.length) {
      return { ok: false, reason: "selection-not-found" };
    }
    controller.setSelectedStrokes(indexes);
    controller.reorderSelectedStrokes(direction);
    return { ok: true, direction, selected: controller.getSelectedStrokeIndexes(), surface: this.describeController(controller) };
  }
  async setApiElementsLocked(options = {}) {
    return this.updateApiElements({ ...options, patch: { ...options.patch || {}, locked: options.locked !== false } });
  }
  async setApiElementsNoteFlow(options = {}) {
    const controller = await this.getLoadedApiController(options);
    if (!controller) {
      return { ok: false, reason: "surface-not-found" };
    }
    if (!controller.supportsNoteFlow()) {
      return { ok: false, reason: "note-flow-not-supported" };
    }
    const indexes = this.resolveApiElementIndexes(controller, options).filter((index) => !controller.drawingData.strokes[index]?.locked && !isConnectorStroke(controller.drawingData.strokes[index]));
    if (!indexes.length) {
      return { ok: false, reason: "selection-not-found" };
    }
    const before = controller.captureDrawingHistorySnapshot();
    const enabled = options.enabled !== false;
    controller.resetNoteFlowSettle();
    controller.clearNoteFlowLayout();
    controller.noteFlowAvoidanceAnchors.clear();
    for (const index of indexes) {
      const stroke = controller.drawingData.strokes[index];
      stroke.noteFlow = enabled ? controller.captureNoteFlowAnchor(stroke) : null;
      stroke.belowMarkdown = enabled;
    }
    controller.captureResponsiveAnchorsForIndexes(indexes);
    this.commitApiDrawingMutation(controller, before, indexes);
    return { ok: true, enabled, selected: indexes, surface: this.describeController(controller) };
  }
  async runApiHistory(direction, options = {}) {
    const controller = await this.getLoadedApiController(options);
    if (!controller) {
      return { ok: false, reason: "surface-not-found" };
    }
    if (direction === "redo") {
      await controller.redoLastStroke();
    } else {
      await controller.undoLastStroke();
    }
    return { ok: true, direction, surface: this.describeController(controller) };
  }
  async updateApiSettings(patch = {}) {
    if (!patch || typeof patch !== "object" || Array.isArray(patch)) {
      return { ok: false, reason: "invalid-settings" };
    }
    this.noteDrawSettings = sanitizeSettings({ ...this.noteDrawSettings, ...patch });
    await this.saveData(this.noteDrawSettings);
    for (const controller of this.getAllControllers()) {
      controller.applySettings();
    }
    return { ok: true, settings: { ...this.noteDrawSettings } };
  }
  async executeApiAction(action, options = {}) {
    if (Array.isArray(action)) {
      const results = [];
      for (const item of action) {
        results.push(await this.executeApiAction(item, options));
      }
      return {
        ok: results.every((result) => result?.ok !== false),
        count: results.length,
        results,
        surface: this.describeController(this.findApiController(options))
      };
    }
    if (action && typeof action === "object") {
      const actionOptions = { ...options, ...action.options || {}, ...action };
      const name2 = action.op || action.action || action.type || action.name || action.command || "";
      delete actionOptions.op;
      delete actionOptions.action;
      delete actionOptions.type;
      delete actionOptions.name;
      delete actionOptions.command;
      delete actionOptions.options;
      return this.executeApiAction(name2, actionOptions);
    }
    const name = String(action || "").trim().toLowerCase().replace(/[\s_]+/g, "-");
    if (name === "activate") return this.activateApi(options);
    if (name === "deactivate") return this.deactivateApi(options);
    if (name === "toggle") return this.toggleApi(options);
    if (name === "get-state") return this.getApiState(options);
    if (name === "set-visibility") return this.setApiVisibility(options.visible, options);
    if (name === "set-tool") return this.setApiTool(options.tool, options);
    if (name === "set-brush") return this.setApiBrush(options);
    if (name === "set-text-preset") return this.setApiTextPreset(options.preset, options);
    if (name === "set-zoom") return this.setApiZoom(options.zoom, options);
    if (name === "get-elements") return this.getApiElements(options);
    if (name === "get-element" || name === "get-element-by-id") return this.getApiElementById(options.id || options.elementId, options);
    if (name === "get-connections") return this.getApiConnections(options);
    if (name === "get-text-bindings") return this.getApiTextBindings(options);
    if (name === "select-elements") return this.selectApiElements(options);
    if (name === "clear-selection") return this.selectApiElements({ ...options, clear: true });
    if (name === "update-elements") return this.updateApiElements(options);
    if (name === "delete-elements") return this.deleteApiElements(options);
    if (name === "reorder-elements") return this.reorderApiElements(options);
    if (name === "set-elements-locked") return this.setApiElementsLocked(options);
    if (name === "set-elements-note-flow") return this.setApiElementsNoteFlow(options);
    if (name === "undo" || name === "redo") return this.runApiHistory(name, options);
    if (name === "copy") return this.copyElementsApi(options);
    if (name === "copy-link" || name === "copy-element-link") return this.findApiController(options)?.copySelectedElementLink({ quiet: Boolean(options.quiet) }) || { ok: false, reason: "surface-not-found" };
    if (name === "paste") return this.pasteElementsApi(options);
    if (name === "insert-stroke" || name === "insert-element" || name === "insert-elements") return this.insertApiElements(options);
    if (name === "create-elements") return this.insertApiElements(options);
    if (name === "highlight-region") return this.insertSurfaceRegionApi("highlight", options);
    if (name === "insert-ink") return this.insertSurfaceRegionApi("ink", options);
    if (name === "focus-region") return this.focusSurfaceRegionApi(options.region || options, options);
    if (name === "update-source" || name === "update-surface-source") return this.updateRegisteredSurfaceSourceApi(options.source, options);
    if (name === "insert-mind-map" || name === "insert-mindmap") return this.insertMindMapApi(options);
    if (name === "get-settings") return { ok: true, settings: { ...this.noteDrawSettings } };
    if (name === "update-settings") return this.updateApiSettings(options.patch || options.settings || {});
    if (name === "refresh") {
      const controller = this.findApiController(options);
      const file2 = controller?.file || this.resolveApiFile(options.file || options.path);
      if (!file2) return { ok: false, reason: "missing-file" };
      const data = await this.readDrawings(file2);
      return { ok: true, refreshed: this.refreshControllersForFile(file2, data) };
    }
    return { ok: false, reason: "unknown-action", action: name };
  }
  async replaceTextApi(options = {}) {
    const file2 = this.resolveApiFile(options.file || options.path, options.sourcePath || "");
    if (!file2) {
      return { changed: false, reason: "missing-file" };
    }
    const originalText = String(options.originalText || "");
    const editedText = String(options.editedText ?? options.text ?? "");
    const sourceInfo = options.sourceInfo || null;
    const source = await this.app.vault.read(file2);
    const target = resolveSourceEditTarget(source, sourceInfo, originalText);
    if (!target) {
      return { changed: false, reason: "target-not-found" };
    }
    const result = await this.saveTextBlock(file2, originalText, editedText, sourceInfo, target);
    if (result.history) {
      const controller = this.findApiController({ file: file2 });
      controller?.recordMarkdownHistory(file2, result.history.before, result.history.after);
    }
    return result;
  }
  async insertStrokeApi(fileOrPath, stroke) {
    const file2 = this.resolveApiFile(fileOrPath);
    if (!file2) {
      throw new Error("NoteDraw could not resolve the requested file");
    }
    const data = await this.readDrawings(file2);
    const normalized = normalizeStroke(stroke);
    if (normalized.points.length) {
      data.strokes.push(normalized);
      await this.writeDrawings(file2, data);
      this.refreshControllersForFile(file2, data);
    }
    return data;
  }
  async insertApiElements(options = {}) {
    const controller = await this.getLoadedApiController(options);
    if (!controller) {
      return { ok: false, reason: "surface-not-found" };
    }
    const rawElements = Array.isArray(options.elements) ? options.elements : Array.isArray(options.strokes) ? options.strokes : options.stroke ? [options.stroke] : [];
    if (!rawElements.length) {
      return { ok: false, reason: "missing-elements" };
    }
    const before = controller.captureDrawingHistorySnapshot();
    const inserted = [];
    for (const raw of rawElements) {
      const normalized = normalizeStroke(raw);
      if (!normalized.points.length) {
        continue;
      }
      controller.drawingData.strokes.push(normalized);
      inserted.push(controller.drawingData.strokes.length - 1);
    }
    if (!inserted.length) {
      return { ok: false, reason: "empty-elements" };
    }
    this.commitApiDrawingMutation(controller, before, inserted);
    return {
      ok: true,
      inserted: inserted.length,
      selected: inserted,
      elementIds: inserted.map((index) => strokeElementId(controller.drawingData.strokes[index])),
      surface: this.describeController(controller)
    };
  }
  async insertSurfaceRegionApi(kind, options = {}) {
    const controller = await this.getLoadedApiController(options);
    if (!controller) {
      return { ok: false, reason: "surface-not-found" };
    }
    const region = normalizeSurfaceRegion(options.region || options, {
      width: controller.canvasWidth(),
      height: controller.canvasHeight()
    });
    if (!region) {
      return { ok: false, reason: "invalid-region" };
    }
    const points = normalizeSurfaceInkPoints(options.points, region, options.pointBasis);
    const strokePoints = points.length >= 2 ? points : kind === "highlight" ? [
      { x: region.x, y: region.y + region.height / 2 },
      { x: region.x + region.width, y: region.y + region.height / 2 }
    ] : [
      { x: region.x, y: region.y },
      { x: region.x + region.width, y: region.y },
      { x: region.x + region.width, y: region.y + region.height },
      { x: region.x, y: region.y + region.height },
      { x: region.x, y: region.y }
    ];
    const before = controller.captureDrawingHistorySnapshot();
    const stroke = normalizeStroke({
      elementId: createElementLayoutId(controller.drawingData.strokes.length),
      brush: kind === "highlight" ? BRUSH_WATERCOLOR : BRUSH_PEN,
      variant: kind === "highlight" ? WATERCOLOR_VARIANT_STRAIGHT : options.noteFlow === true ? PEN_VARIANT_NOTE : options.variant,
      color: options.color || (kind === "highlight" ? "#fff176" : "#e53935"),
      width: options.width ?? (kind === "highlight" ? clamp10(region.height * controller.canvasHeight() * 0.82, MIN_BRUSH_WIDTH, MAX_BRUSH_WIDTH) : 3),
      opacity: options.opacity ?? (kind === "highlight" ? 0.42 : DEFAULT_PEN_OPACITY),
      noteFlow: options.noteFlow && typeof options.noteFlow === "object" ? options.noteFlow : null,
      belowMarkdown: Boolean(options.noteFlow),
      points: strokePoints.map((point, index2) => ({ ...point, t: Date.now() + index2 }))
    });
    controller.drawingData.strokes.push(stroke);
    const index = controller.drawingData.strokes.length - 1;
    this.commitApiDrawingMutation(controller, before, [index]);
    const surface = this.describeController(controller);
    return {
      ok: true,
      kind,
      region,
      selected: [index],
      elementIds: [strokeElementId(stroke)],
      surface
    };
  }
  async focusSurfaceRegionApi(regionInput, options = {}) {
    const controller = this.findApiController(options);
    if (!controller) {
      return { ok: false, reason: "surface-not-found" };
    }
    const region = normalizeSurfaceRegion(regionInput, {
      width: controller.canvasWidth(),
      height: controller.canvasHeight()
    });
    if (!region) {
      return { ok: false, reason: "invalid-region" };
    }
    const viewport = controller.registeredSurfaceViewport;
    const focus = viewport?.focusRegion || viewport?.scrollToRegion;
    if (typeof focus === "function") {
      try {
        await focus.call(viewport, region);
        return { ok: true, region, surface: this.describeController(controller) };
      } catch (error) {
        return { ok: false, reason: "focus-region-failed", message: String(error?.message || error), region };
      }
    }
    const scroller = controller.scrollContainer;
    if (typeof scroller?.scrollTo === "function") {
      scroller.scrollTo({ top: region.y * Math.max(1, scroller.scrollHeight), behavior: options.behavior || "smooth" });
      return { ok: true, region, surface: this.describeController(controller) };
    }
    return { ok: false, reason: "focus-region-unsupported", region, surface: this.describeController(controller) };
  }
  async updateRegisteredSurfaceSourceApi(sourceInput, options = {}) {
    const controller = this.findApiController(options);
    const key = controller?.registeredSurfaceKey;
    const record = key ? this.registeredSurfaceRecords.get(key) : null;
    if (!controller || !record) {
      return { ok: false, reason: "registered-surface-not-found" };
    }
    const source = normalizeRegisteredSurfaceSource(sourceInput || {}, { owner: record.owner, id: record.id });
    if (!source.kind) {
      return { ok: false, reason: "invalid-source" };
    }
    record.source = source;
    controller.registeredSurfaceSource = source;
    if (options.reload !== false) {
      record.file = createRegisteredSurfaceDrawingFile(this.app, record);
      await controller.setFile(record.file);
    }
    controller.scheduleLayoutRefresh({ settle: false });
    controller.requestRender(true);
    const surface = this.describeController(controller);
    this.emitApiEvent("surface-changed", { ...surface, phase: "source" });
    return { ok: true, source: { ...source }, surface };
  }
  copyElementsApi(options = {}) {
    const controller = this.findApiController(options);
    return controller?.copySelectedElements({ quiet: Boolean(options.quiet) }) || { ok: false, reason: "selection-not-found" };
  }
  pasteElementsApi(options = {}) {
    const controller = this.findApiController(options);
    return controller?.pasteCopiedElements({ point: options.point, quiet: Boolean(options.quiet) }) || { ok: false, reason: "surface-not-found" };
  }
  async insertMindMapApi(options = {}) {
    const controller = this.findApiController(options);
    const sourceFile = this.resolveApiFile(options.source || options.sourcePath || options.file || controller?.file);
    if (!controller || !sourceFile) {
      return { ok: false, reason: "source-or-surface-not-found" };
    }
    const point = options.point || controller.visibleCanvasPastePoint();
    return controller.insertMindMapAt(point, sourceFile, {
      quiet: Boolean(options.quiet),
      affectsSource: options.affectsSource ?? this.noteDrawSettings.mindMapAffectsSource
    });
  }
  refreshControllersForFile(file2, data, options = {}) {
    let refreshed = 0;
    for (const controller of this.getAllControllers()) {
      if (normalizeVaultPath(controller.file?.path) !== normalizeVaultPath(file2?.path) || controller.drawingData === options.excludeData) {
        continue;
      }
      if (controller.pointerDown || controller.draggingStroke || controller.resizingSelection) {
        if (options.external === true) {
          controller.externalDrawingRefreshPending = true;
        }
        continue;
      }
      controller.externalDrawingRefreshPending = false;
      controller.markdownSourceRevisionChanged = Boolean(data?._notedrawSourceRevisionMismatch);
      controller.drawingData = normalizeDrawingData(data, file2);
      controller.rebuildElementRelations();
      controller.drawingsLoaded = true;
      controller.applyDrawingsVisibility(controller.drawingData.visible !== false);
      controller.responsivePointsInitialized = false;
      controller.responsiveLayoutSignature = "";
      controller.responsiveLayoutContext = null;
      controller.surfaceStateGeneration += 1;
      controller.initialReadingLayoutPrepared = false;
      controller.initialReadingLayoutSettlement = null;
      controller.initialReadingLayoutSettled = false;
      controller.initialReadingSurfaceSettlement = null;
      controller.initialReadingCommittedSignature = "";
      controller.cancelLayoutRefresh();
      controller.invalidateStaticCache();
      if (isElementVisibleEnough(controller.previewEl)) {
        controller.scheduleFrozenNoteFlowLayoutRestore();
        controller.syncMarkdownBlockPresentation();
        controller.scheduleLayoutRefresh({ settle: false, layout: false, measure: true });
        controller.requestRender(true);
        if (!controller.active && controller.surfaceType === "preview") {
          controller.queueReadingSurfaceSettlement();
        }
      }
      refreshed += 1;
    }
    return refreshed;
  }
  async handleVaultFileDelete(deletedFile) {
    const deletedFiles = collectDeletedVaultFiles(deletedFile);
    const deletedPaths = new Set(deletedFiles.map((file2) => normalizeVaultPath(file2?.path)).filter(Boolean));
    const rootPath = normalizeVaultPath(deletedFile?.path || "");
    if (rootPath) {
      deletedPaths.add(rootPath);
    }
    if (!deletedPaths.size) {
      return;
    }
    const isDeletedPath = (path) => {
      const normalized = normalizeVaultPath(path);
      return Array.from(deletedPaths).some((deletedPath) => normalized === deletedPath || normalized.startsWith(`${deletedPath}/`));
    };
    for (const controller of this.getAllControllers()) {
      if (isDeletedPath(controller.file?.path)) {
        controller.destroy({ discardEdits: true });
      }
    }
    const storageRecords = deletedFiles.filter((file2) => file2?.extension || String(file2?.path || "").toLowerCase().endsWith(".md"));
    const storagePaths = /* @__PURE__ */ new Set();
    const storageKeys = /* @__PURE__ */ new Set();
    for (const file2 of storageRecords) {
      const filePath = normalizeVaultPath(file2.path);
      const encodedName = this.encodedDrawingNameForFile({ path: filePath });
      const modes = [DRAWING_STORAGE_CONFIG];
      if (String(file2.extension || "").toLowerCase() === "md") {
        modes.push(DRAWING_STORAGE_NOTE_SUBFOLDER, DRAWING_STORAGE_NOTE_FOLDER, DRAWING_STORAGE_EMBEDDED);
      }
      for (const mode of modes) {
        const storagePath = mode === DRAWING_STORAGE_EMBEDDED ? "" : resolveDrawingStoragePath({
          filePath,
          configDir: this.app.vault.configDir,
          pluginId: PLUGIN_ID,
          encodedName,
          mode
        });
        const storageKey = `${mode}:${storagePath}`;
        storageKeys.add(storageKey);
        if (storagePath) {
          storagePaths.add(storagePath);
        }
      }
      this.portableBundles.delete(filePath);
      this.portableBundleLoads.delete(filePath);
    }
    for (const key of Array.from(this.saveTimers.keys())) {
      if (storageKeys.has(key)) {
        this.cancelScheduledDrawingSave(key);
      }
    }
    for (const key of Array.from(this.saveIdleCallbacks.keys())) {
      if (storageKeys.has(key)) {
        this.cancelScheduledDrawingSave(key);
      }
    }
    for (const key of storageKeys) {
      this.pendingDrawingSaves.delete(key);
    }
    await Promise.all(Array.from(storageKeys).map((key) => this.drawingWritePromises.get(key)).filter(Boolean).map((write) => write.catch(() => void 0)));
    for (const key of storageKeys) {
      this.drawingStateCache.delete(key);
      this.drawingWritePromises.delete(key);
    }
    for (const path of storagePaths) {
      try {
        if (await this.app.vault.adapter.exists(path)) {
          await this.app.vault.adapter.remove(path);
        }
      } catch (error) {
        console.error(`[${PLUGIN_ID}] Failed to remove deleted file NoteDraw storage`, path, error);
      }
    }
  }
  onApiEvent(eventName, listener) {
    if (typeof listener !== "function") {
      return () => void 0;
    }
    const name = String(eventName || "");
    let listeners = this.apiListeners.get(name);
    if (!listeners) {
      listeners = /* @__PURE__ */ new Set();
      this.apiListeners.set(name, listeners);
    }
    listeners.add(listener);
    return () => listeners.delete(listener);
  }
  emitApiEvent(eventName, detail) {
    for (const listener of Array.from(this.apiListeners?.get(eventName) || [])) {
      try {
        listener(detail);
      } catch (error) {
        console.error(`[${PLUGIN_ID}] API listener failed`, error);
      }
    }
  }
  resolveEditableFile(element, fallbackFile) {
    const sourcePath = normalizeVaultPath(
      element?.dataset?.noteDrawSourcePath || resolveRenderedSourcePath(this.app, element, fallbackFile?.path || "")
    );
    return this.resolveApiFile(sourcePath) || fallbackFile;
  }
  getActiveController() {
    const activeView = this.app.workspace.getActiveViewOfType(import_obsidian.MarkdownView);
    const activeLeaf = findActiveWorkspaceLeaf(this.app);
    const activeContainer = activeView?.containerEl || activeLeaf?.view?.containerEl;
    if (activeView instanceof import_obsidian.MarkdownView) {
      const surface2 = findPrimaryMarkdownSurface(activeView);
      const controller = surface2 ? this.controllers.get(surface2) || surface2._noteDrawController : null;
      if (controller) {
        return controller;
      }
    }
    if (activeContainer) {
      const activeRegistered = Array.from(this.registeredSurfaceControllers.values()).find((controller) => controller.previewEl?.isConnected && activeContainer.contains(controller.previewEl));
      if (activeRegistered) {
        return activeRegistered;
      }
      const activeWebview = Array.from(this.webviewControllers.values()).find((controller) => controller.previewEl?.isConnected && activeContainer.contains(controller.previewEl));
      if (activeWebview) {
        return activeWebview;
      }
      const activeWorkspace = this.workspaceControllers.get(activeLeaf?.view) || Array.from(this.workspaceControllers.values()).find((controller) => controller.previewEl?.isConnected && activeContainer.contains(controller.previewEl));
      if (activeWorkspace) {
        return activeWorkspace;
      }
    }
    const view = this.app.workspace.getActiveViewOfType(import_obsidian.MarkdownView);
    const surface = view ? findPrimaryMarkdownSurface(view) : null;
    return surface ? this.controllers.get(surface) || surface._noteDrawController : null;
  }
  toggleActiveController() {
    const controller = this.getActiveController();
    if (!controller) {
      new import_obsidian.Notice(this.t("openNoteOrWebviewFirst"));
      return;
    }
    controller.toggle().catch((error) => {
      console.error(`[${PLUGIN_ID}] Failed to toggle NoteDraw`, error);
    });
  }
  onElementLinkClick(event) {
    const anchor = event?.target?.closest?.("a[href]");
    const href = anchor?.getAttribute?.("href") || anchor?.href || "";
    if (!parseNoteDrawElementLink(href)) {
      return;
    }
    event.preventDefault();
    event.stopPropagation();
    event.stopImmediatePropagation?.();
    void this.openElementLink(href).catch((error) => {
      console.error(`[${PLUGIN_ID}] Failed to open NoteDraw element link`, error);
      new import_obsidian.Notice(this.t("elementLinkTargetNotFound"));
    });
  }
  async openElementLink(rawLink) {
    const target = parseNoteDrawElementLink(rawLink);
    const file2 = target ? this.resolveApiFile(target.file) : null;
    if (!target || !file2) {
      new import_obsidian.Notice(this.t("elementLinkTargetNotFound"));
      return { ok: false, reason: "invalid-link" };
    }
    const leaf = this.app.workspace.getLeaf(false);
    await leaf.openFile(file2);
    if (typeof leaf.setViewState === "function") {
      await leaf.setViewState({
        type: "markdown",
        state: { file: file2.path, mode: "preview", source: false },
        active: true
      });
    }
    this.app.workspace.setActiveLeaf(leaf, { focus: true });
    const deadline = Date.now() + 1e4;
    while (Date.now() < deadline) {
      const view = leaf.view;
      const controller = view instanceof import_obsidian.MarkdownView ? this.resolveLivePreviewController(view) : null;
      if (controller?.previewEl?.isConnected && controller.file?.path === file2.path) {
        await controller.ensureDrawingsLoaded();
        if (controller.drawingsLoaded) {
          this.setControllerActivation(controller, true);
          controller.selectElementsById(target.ids);
          controller.scrollSelectionIntoView();
          return { ok: true, file: file2.path, ids: target.ids };
        }
      }
      this.runSurfaceSync();
      await waitForNextFrame();
    }
    new import_obsidian.Notice(this.t("elementLinkTargetNotFound"));
    return { ok: false, reason: "surface-not-ready" };
  }
  syncRenderedMarkdownAnnotations() {
    const leaves = this.app.workspace.getLeavesOfType?.("markdown") || [];
    for (const leaf of leaves) {
      const view = leaf.view;
      if (!(view instanceof import_obsidian.MarkdownView) || !view.file || !view.containerEl) {
        continue;
      }
      if (!isElementVisibleEnough(view.containerEl)) {
        continue;
      }
      annotateVisibleMarkdownElements(this.app, view.containerEl, view.file.path);
    }
  }
  syncSourceControllers() {
    const leaves = this.app.workspace.getLeavesOfType?.("markdown") || [];
    const activeViews = /* @__PURE__ */ new Set();
    for (const leaf of leaves) {
      const view = leaf.view;
      if (!(view instanceof import_obsidian.MarkdownView) || !view.file) {
        continue;
      }
      const sourceEl = findSourceSurfaceForView(view);
      const previewVisible = isMarkdownPreviewVisible(view, findRootPreviewForView(view));
      const sourceVisible = isMarkdownSourceVisible(view, sourceEl);
      const shouldMount = Boolean(sourceEl) && isSourceMode(view) && sourceVisible;
      const existing = this.sourceControllers.get(view);
      if (!shouldMount) {
        if (existing) {
          existing.destroy();
          this.sourceControllers.delete(view);
        }
        continue;
      }
      activeViews.add(view);
      if (existing?.previewEl === sourceEl) {
        existing.setFile(view.file).catch((error) => {
          console.error(`[${PLUGIN_ID}] Failed to switch source controller file`, error);
        });
        continue;
      }
      if (existing) {
        existing.destroy();
      }
      const mountedOnElement = this.controllers.get(sourceEl) || sourceEl._noteDrawController;
      if (mountedOnElement?.destroy) {
        mountedOnElement.destroy();
      }
      cleanupDrawingUi(sourceEl);
      const controller = new PreviewDrawingController(this, sourceEl, view, view.file, {
        allowTextEdit: false,
        surfaceType: "source"
      });
      this.controllers.set(sourceEl, controller);
      this.sourceControllers.set(view, controller);
      controller.mount().catch((error) => {
        console.error(`[${PLUGIN_ID}] Failed to mount source drawing controller`, error);
      });
    }
    for (const [view, controller] of Array.from(this.sourceControllers.entries())) {
      if (!activeViews.has(view) && !controller.previewEl?.isConnected) {
        controller.destroy();
        this.sourceControllers.delete(view);
      }
    }
  }
  clearPreviewRenderRecovery(preview) {
    const state = this.previewRenderRecovery?.get(preview);
    if (state?.timer !== null && state?.timer !== void 0) {
      window.clearTimeout(state.timer);
    }
    this.previewRenderRecovery?.delete(preview);
  }
  schedulePreviewRenderRecovery(view, preview) {
    const lifecycle = rootPreviewLifecycleState(view, preview);
    const renderer = view?.previewMode?.renderer;
    if (this.runtimeDisposed || !shouldRecoverEmptyRootPreview({
      ...lifecycle,
      rendererMatches: renderer?.previewEl === preview
    })) {
      this.clearPreviewRenderRecovery(preview);
      return false;
    }
    const path = normalizeVaultPath(view?.file?.path || "");
    let state = this.previewRenderRecovery.get(preview);
    if (!state || state.path !== path) {
      this.clearPreviewRenderRecovery(preview);
      state = { path, attempts: 0, timer: null };
      this.previewRenderRecovery.set(preview, state);
    }
    if (state.timer !== null || state.attempts >= 2) {
      return false;
    }
    const delay = state.attempts === 0 ? 0 : 120;
    state.attempts += 1;
    state.timer = window.setTimeout(() => {
      state.timer = null;
      if (this.runtimeDisposed || !preview?.isConnected || view?.previewMode?.renderer?.previewEl !== preview) {
        this.clearPreviewRenderRecovery(preview);
        return;
      }
      try {
        view.previewMode?.rerender?.(true);
      } catch (error) {
        void error;
      }
      this.scheduleSurfaceSync(60);
    }, delay);
    return true;
  }
  syncMarkdownControllerModes() {
    const leaves = this.app.workspace.getLeavesOfType?.("markdown") || [];
    for (const leaf of leaves) {
      const view = leaf.view;
      if (!(view instanceof import_obsidian.MarkdownView)) {
        continue;
      }
      const preview = findRootPreviewForView(view);
      const source = findSourceSurfaceForView(view);
      let previewController = preview ? this.controllers.get(preview) || preview._noteDrawController : null;
      const sourceController = source ? this.controllers.get(source) || source._noteDrawController : null;
      const previewVisible = isMarkdownPreviewVisible(view, preview);
      const sourceVisible = isMarkdownSourceVisible(view, source);
      const alternateSurfaceVisible = hasVisibleAlternateWorkspaceSurface(view, preview);
      if (previewController?.plugin === this && view.file && previewController.file?.path !== view.file.path) {
        previewController.destroy();
        previewController = null;
      }
      if (isSourceMode(view) && sourceVisible) {
        for (const rootPreview of findRootPreviewsForView(view)) {
          this.clearPreviewRenderRecovery(rootPreview);
          const controller = this.controllers.get(rootPreview) || rootPreview._noteDrawController;
          if (controller?.plugin === this && controller.previewEl?.isConnected) {
            controller.destroy();
          } else {
            controller?.destroy?.();
            resetDormantRootPreview(view, rootPreview);
          }
        }
        continue;
      }
      sourceController?.syncFloatingControlClasses();
      if (!previewVisible) {
        if (alternateSurfaceVisible) {
          for (const rootPreview of findRootPreviewsForView(view)) {
            const controller = this.controllers.get(rootPreview) || rootPreview._noteDrawController;
            if (controller?.plugin === this && controller.surfaceType === "preview") {
              controller.destroy();
            }
          }
        }
        continue;
      }
      for (const alternatePreview of findRootPreviewsForView(view)) {
        if (alternatePreview === preview) {
          continue;
        }
        const alternateController = this.controllers.get(alternatePreview) || alternatePreview._noteDrawController;
        if (alternateController?.plugin === this && alternateController.surfaceType === "preview") {
          alternateController.destroy();
        }
      }
      if (previewController?.plugin === this && !previewController.destroyed && previewController.file?.path === view.file?.path) {
        previewController.syncFloatingControlClasses();
        continue;
      }
      if (!isRootPreviewReady(view, preview)) {
        previewController?.destroy();
        if (this.schedulePreviewRenderRecovery(view, preview)) {
          continue;
        }
        resetDormantRootPreview(view, preview);
        continue;
      }
      this.clearPreviewRenderRecovery(preview);
      if (previewVisible && (!previewController || previewController.destroyed || previewController.file?.path !== view.file?.path)) {
        previewController?.destroy();
        previewController = this.resolveLivePreviewController(view);
      }
    }
  }
  syncEmbeddedMarkdownControllers() {
    if (this.runtimeDisposed) {
      return;
    }
    const activeSurfaces = /* @__PURE__ */ new Set();
    const leaves = this.app.workspace.getLeavesOfType?.("markdown") || [];
    for (const leaf of leaves) {
      const view = leaf.view;
      if (!(view instanceof import_obsidian.MarkdownView) || !view.file || !view.containerEl) {
        continue;
      }
      const surfaces = Array.from(view.containerEl.querySelectorAll(".markdown-embed-content")).filter((surface) => {
        return isElementLaidOut(surface) && !surface.closest(".notedraw-embed");
      });
      for (const surface of surfaces) {
        const sourcePath = resolveRenderedSourcePath(this.app, surface, view.file.path);
        const file2 = getVaultFileByPath(this.app.vault, sourcePath);
        if (!file2 || normalizeVaultPath(file2.path) === normalizeVaultPath(view.file.path)) {
          continue;
        }
        if (!isElementNearViewport(surface)) {
          continue;
        }
        activeSurfaces.add(surface);
        const existing = this.embeddedControllers.get(surface);
        if (existing?.previewEl === surface && !existing.destroyed) {
          existing.setFile(file2).catch((error) => {
            console.error(`[${PLUGIN_ID}] Failed to switch embedded drawing controller file`, error);
          });
          continue;
        }
        existing?.destroy();
        const mountedOnElement = this.controllers.get(surface) || surface._noteDrawController;
        if (mountedOnElement?.destroy) {
          mountedOnElement.destroy();
        }
        cleanupDrawingUi(surface);
        const controller = new PreviewDrawingController(this, surface, view, file2, {
          allowTextEdit: true,
          surfaceType: "embedded",
          embeddedSurface: true
        });
        this.controllers.set(surface, controller);
        this.embeddedControllers.set(surface, controller);
        controller.mount().catch((error) => {
          console.error(`[${PLUGIN_ID}] Failed to mount embedded drawing controller`, error);
        });
      }
    }
    for (const [surface, controller] of Array.from(this.embeddedControllers.entries())) {
      if (!activeSurfaces.has(surface) || !surface.isConnected) {
        controller.destroy();
        this.embeddedControllers.delete(surface);
      }
    }
  }
  syncWebviewControllers() {
    for (const leaf of collectWorkspaceLeaves(this.app)) {
      const view = leaf?.view;
      if (isNativeGraphWorkspaceType(workspaceViewType(view))) {
        this.cleanupNativeGraphWorkspaceView(view, view?.contentEl || findWorkspaceDrawingSurface(view));
      }
    }
    const surfaces = findWebviewSurfaces(activeDocument);
    const activeSurfaces = /* @__PURE__ */ new Set();
    for (const surface of surfaces) {
      if (!surface?.isConnected) {
        continue;
      }
      const view = findOwningWorkspaceView(this.app, surface);
      if (isNativeGraphWorkspaceType(workspaceViewType(view))) {
        this.cleanupNativeGraphWorkspaceView(view, surface);
        continue;
      }
      if (!isElementVisibleEnough(surface)) {
        continue;
      }
      activeSurfaces.add(surface);
      const file2 = createWebviewDrawingFile(surface, view);
      const existing = this.webviewControllers.get(surface);
      if (existing?.previewEl === surface) {
        existing.setFile(file2).catch((error) => {
          console.error(`[${PLUGIN_ID}] Failed to switch webview controller file`, error);
        });
        continue;
      }
      if (existing) {
        existing.destroy();
      }
      const mountedOnElement = surface._noteDrawController;
      if (mountedOnElement?.plugin === this && mountedOnElement.surfaceType === "webview") {
        mountedOnElement.setFile(file2).catch((error) => {
          console.error(`[${PLUGIN_ID}] Failed to switch webview controller file`, error);
        });
        this.webviewControllers.set(surface, mountedOnElement);
        continue;
      }
      if (mountedOnElement?.destroy) {
        mountedOnElement.destroy();
      }
      cleanupDrawingUi(surface);
      const controller = new PreviewDrawingController(this, surface, view, file2, {
        allowTextEdit: true,
        surfaceType: "webview"
      });
      this.controllers.set(surface, controller);
      this.webviewControllers.set(surface, controller);
      controller.mount().catch((error) => {
        console.error(`[${PLUGIN_ID}] Failed to mount webview drawing controller`, error);
      });
    }
    for (const [surface, controller] of Array.from(this.webviewControllers.entries())) {
      if (!activeSurfaces.has(surface) || !surface.isConnected) {
        controller.destroy();
        this.webviewControllers.delete(surface);
      }
    }
  }
  syncWorkspaceControllers() {
    const activeViews = /* @__PURE__ */ new Set();
    for (const leaf of collectImageWorkspaceLeaves(this.app)) {
      const view = leaf?.view;
      const viewType = workspaceViewType(view) || "image";
      const file2 = view?.file && typeof view.file.path === "string" ? view.file : null;
      const surface = findWorkspaceDrawingSurface(view);
      if (!view?.containerEl || !file2 || !surface?.isConnected) {
        continue;
      }
      activeViews.add(view);
      if (!isElementVisibleEnough(surface)) {
        continue;
      }
      this.mountWorkspaceController(view, viewType, file2, surface);
    }
    for (const leaf of collectWorkspaceLeaves(this.app)) {
      const view = leaf?.view;
      const viewType = workspaceViewType(view);
      const imageWorkspace = isImageWorkspaceView(view, viewType);
      if (isNativeGraphWorkspaceType(viewType)) {
        this.cleanupNativeGraphWorkspaceView(view, view?.contentEl || findWorkspaceDrawingSurface(view));
        continue;
      }
      if (imageWorkspace) {
        continue;
      }
      if (!view?.containerEl || !isMainWorkspaceView(view) || view instanceof import_obsidian.MarkdownView || viewType === "markdown" || viewType === "empty" || isWebviewWorkspaceType(viewType)) {
        continue;
      }
      const file2 = view.file && typeof view.file.path === "string" ? view.file : createWorkspaceDrawingFile(view, viewType);
      const surface = findWorkspaceDrawingSurface(view);
      if (!surface?.isConnected) {
        continue;
      }
      activeViews.add(view);
      if (!isElementVisibleEnough(surface)) {
        continue;
      }
      this.mountWorkspaceController(view, viewType, file2, surface);
    }
    for (const [view, controller] of Array.from(this.workspaceControllers.entries())) {
      if (!activeViews.has(view) || !controller.previewEl?.isConnected) {
        controller.destroy();
        this.workspaceControllers.delete(view);
      }
    }
  }
  mountWorkspaceController(view, viewType, file2, surface) {
    const existing = this.workspaceControllers.get(view);
    if (existing?.previewEl === surface && !existing.destroyed) {
      existing.workspaceViewType = viewType;
      existing.setFile(file2).catch((error) => {
        console.error(`[${PLUGIN_ID}] Failed to switch workspace drawing file`, error);
      });
      if (!existing.button?.isConnected) {
        existing.button = this.installHeaderButton(existing);
      }
      return existing;
    }
    existing?.destroy();
    const mountedOnElement = this.controllers.get(surface) || surface._noteDrawController;
    if (mountedOnElement?.destroy) {
      mountedOnElement.destroy();
    }
    cleanupDrawingUi(surface);
    const controller = new PreviewDrawingController(this, surface, view, file2, {
      allowTextEdit: false,
      surfaceType: "workspace",
      workspaceSurface: true,
      workspaceViewType: viewType
    });
    this.controllers.set(surface, controller);
    this.workspaceControllers.set(view, controller);
    controller.mount().catch((error) => {
      console.error(`[${PLUGIN_ID}] Failed to mount workspace drawing controller`, error);
    });
    return controller;
  }
  cleanupNativeGraphWorkspaceView(view, surface = view?.contentEl || findWorkspaceDrawingSurface(view)) {
    if (!surface) {
      return;
    }
    const candidates = new Set([
      this.workspaceControllers.get(view),
      this.webviewControllers.get(surface),
      this.controllers.get(surface),
      surface._noteDrawController
    ].filter(Boolean));
    const registeredController = Array.from(candidates).find((controller) => controller?.plugin === this && controller.registeredSurface);
    for (const controller of candidates) {
      if (controller?.plugin === this && controller !== registeredController) {
        controller.destroy?.();
      }
    }
    if (registeredController) {
      return;
    }
    cleanupDrawingUi(surface);
    for (const button of view?.containerEl?.querySelectorAll?.(".notedraw-header-button, .notedraw-webview-button") || []) {
      if (!button._noteDrawController?.registeredSurface) {
        button.remove();
      }
    }
  }
  installHeaderButton(controller) {
    if (controller.surfaceType === "webview" || controller.registeredSurface) {
      return this.installSurfaceButton(controller);
    }
    const view = controller.view;
    let state = this.headerActions.get(view);
    if (!state || !state.button?.isConnected) {
      state = {
        button: null,
        controller: null,
        controllers: /* @__PURE__ */ new Set()
      };
      state.clickHandler = (event) => this.resolveHeaderController(view, state, { preferPreviewOnAppleTouch: isAppleTouchEvent(event) })?.onButtonClick(event);
      state.pointerDownHandler = (event) => this.resolveHeaderController(view, state, { preferPreviewOnAppleTouch: false })?.onButtonPointerDown(event);
      state.pointerUpHandler = (event) => this.resolveHeaderController(view, state, { preferPreviewOnAppleTouch: false })?.onButtonPointerUp(event);
      state.touchEndHandler = (event) => this.resolveHeaderController(view, state, { preferPreviewOnAppleTouch: true })?.onButtonTouchEnd(event);
      state.contextMenuHandler = (event) => this.resolveHeaderController(view, state, { preferPreviewOnAppleTouch: false })?.onButtonContextMenu(event);
      let button = null;
      if (typeof view?.addAction === "function") {
        button = view.addAction("wand-sparkles", this.t("editTextDraw"), state.clickHandler);
      }
      if (!button) {
        const actions = view?.containerEl?.querySelector(".view-actions");
        button = activeDocument.createElement("div");
        button.classList.add("clickable-icon", "view-action");
        (0, import_obsidian.setIcon)(button, "wand-sparkles");
        button.addEventListener("click", state.clickHandler);
        if (actions) {
          actions.appendChild(button);
        } else {
          controller.previewEl.appendChild(button);
          button.classList.add("notedraw-fallback-button");
        }
      }
      button.addEventListener("pointerdown", state.pointerDownHandler);
      button.addEventListener("pointerup", state.pointerUpHandler);
      button.addEventListener("pointercancel", state.pointerUpHandler);
      button.addEventListener("pointerleave", state.pointerUpHandler);
      button.addEventListener("touchend", state.touchEndHandler, { passive: false });
      button.addEventListener("contextmenu", state.contextMenuHandler);
      state.button = button;
      this.headerActions.set(view, state);
    }
    if (!state.controllers) {
      state.controllers = /* @__PURE__ */ new Set();
    }
    state.controllers.add(controller);
    state.controller = this.pickHeaderController(view, state, controller);
    state.button._noteDrawController = state.controller || controller;
    state.button.classList.add("notedraw-header-button");
    state.button.classList.toggle("notedraw-webview-button", state.button._noteDrawController?.surfaceType === "webview");
    this.setAccessibleLabel(state.button, state.button._noteDrawController?.surfaceType === "webview" ? "editWebviewDraw" : "editTextDraw");
    state.button.classList.toggle("is-active", Boolean(state.button._noteDrawController?.active));
    this.cleanupHeaderButtons(view, state.button);
    return state.button;
  }
  installSurfaceButton(controller) {
    const actions = findWebviewButtonHost(controller.previewEl, controller.view);
    const inline = actions && !actions.classList?.contains("view-actions");
    const button = activeDocument.createElement(inline ? "button" : "div");
    if (inline) {
      button.setAttribute("type", "button");
    }
    button.classList.add("clickable-icon", "notedraw-webview-button");
    if (inline) {
      button.classList.add("mwv-icon-button", "notedraw-webview-inline-button");
    } else {
      button.classList.add("view-action");
    }
    (0, import_obsidian.setIcon)(button, "wand-sparkles");
    this.setAccessibleLabel(button, controller.surfaceType === "webview" ? "editWebviewDraw" : "editTextDraw");
    button.addEventListener("click", (event) => controller.onButtonClick(event));
    button.addEventListener("pointerdown", (event) => controller.onButtonPointerDown(event));
    button.addEventListener("pointerup", () => controller.onButtonPointerUp());
    button.addEventListener("pointercancel", () => controller.onButtonPointerUp());
    button.addEventListener("pointerleave", () => controller.onButtonPointerUp());
    button.addEventListener("touchend", (event) => controller.onButtonTouchEnd(event), { passive: false });
    button.addEventListener("contextmenu", (event) => controller.onButtonContextMenu(event));
    if (actions) {
      actions.appendChild(button);
    } else {
      controller.previewEl.appendChild(button);
      button.classList.add("notedraw-fallback-button");
    }
    button._noteDrawController = controller;
    return button;
  }
  releaseHeaderButton(controller) {
    if (controller.surfaceType === "webview" || controller.registeredSurface) {
      controller.button?.remove();
      return;
    }
    const state = this.headerActions.get(controller.view);
    if (!state) {
      return;
    }
    state.controllers?.delete(controller);
    if (state.controller === controller) {
      state.controller = this.pickHeaderController(controller.view, state);
    }
    if (state.controller) {
      state.button._noteDrawController = state.controller;
      state.button.classList.toggle("is-active", state.controller.active);
      state.button.classList.toggle("notedraw-webview-button", state.controller.surfaceType === "webview");
      return;
    }
    if (state.button?._noteDrawController) {
      delete state.button._noteDrawController;
    }
    state.button?.classList.remove("is-active");
    if (!controller.view?.containerEl?.isConnected) {
      state.button?.remove();
      this.headerActions.delete(controller.view);
      this.cleanupHeaderButtons(controller.view);
    }
  }
  resolveHeaderController(view, state, options = {}) {
    const controller = this.pickHeaderController(view, state, null, options);
    if (controller) {
      state.controller = controller;
      state.button._noteDrawController = controller;
      state.button.classList.toggle("is-active", controller.active);
      state.button.classList.toggle("notedraw-webview-button", controller.surfaceType === "webview");
    }
    return controller;
  }
  pickHeaderController(view, state, preferred = null, options = {}) {
    const controllers = Array.from(state.controllers || []).filter((controller) => controller?.previewEl?.isConnected && controller.view === view && controller.surfaceType !== "webview");
    if (options.preferPreviewOnAppleTouch && isAppleMobileRuntime() && isReadingSurfaceVisible(view)) {
      const previewController = this.resolveLivePreviewController(view, controllers);
      if (previewController) {
        return previewController;
      }
    }
    const currentMode = currentMarkdownSurfaceType(view);
    const preferredLive = preferred && controllers.includes(preferred) ? preferred : null;
    return controllers.find((controller) => controller.surfaceType === currentMode) || preferredLive || controllers.find((controller) => controller.active) || controllers[0] || null;
  }
  resolveLivePreviewController(view, controllers = []) {
    const preview = findRootPreviewForView(view);
    if (!preview || !view?.file) {
      return null;
    }
    const existing = this.controllers.get(preview) || preview._noteDrawController;
    if (existing?.plugin === this && !existing.destroyed && existing.previewEl?.isConnected && existing.surfaceType === "preview") {
      return existing;
    }
    if (!isRootPreviewReady(view, preview)) {
      resetDormantRootPreview(view, preview);
      return null;
    }
    const registered = controllers.find((controller2) => controller2.surfaceType === "preview" && controller2.previewEl === preview);
    if (registered) {
      return registered;
    }
    cleanupDrawingUi(preview);
    const controller = new PreviewDrawingController(this, preview, view, view.file);
    this.controllers.set(preview, controller);
    controller.mount().catch((error) => {
      console.error(`[${PLUGIN_ID}] Failed to mount preview drawing controller`, error);
    });
    return controller;
  }
  cleanupHeaderButtons(view, keepButton = null) {
    view?.containerEl?.querySelectorAll(".notedraw-header-button").forEach((button) => {
      if (button !== keepButton) {
        button.remove();
      }
    });
  }
  async ensureDrawingDir() {
    const base = this.app.vault.configDir;
    const pluginDir = `${base}/plugins/${PLUGIN_ID}`;
    const drawingDir = `${pluginDir}/drawings`;
    await this.ensureFolder(`${base}/plugins`);
    await this.ensureFolder(pluginDir);
    await this.ensureFolder(drawingDir);
    return drawingDir;
  }
  async ensureAssetDir() {
    const base = this.app.vault.configDir;
    const pluginDir = `${base}/plugins/${PLUGIN_ID}`;
    const assetDir = `${pluginDir}/assets`;
    await this.ensureFolder(`${base}/plugins`);
    await this.ensureFolder(pluginDir);
    await this.ensureFolder(assetDir);
    return assetDir;
  }
  async ensureFolder(path) {
    const adapter = this.app.vault.adapter;
    const parts = normalizeVaultPath(path).split("/").filter(Boolean);
    let current = "";
    for (const part of parts) {
      current = current ? `${current}/${part}` : part;
      if (!await adapter.exists(current)) {
        await adapter.mkdir(current);
      }
    }
  }
  encodedDrawingNameForFile(file2) {
    const encoded = file2.path.replace(/\\/g, "/").replace(/[^a-zA-Z0-9._/-]/g, "_").replace(/\//g, "__");
    return `${encoded}.json`;
  }
  drawingStorageModeForFile(file2, requestedMode = this.noteDrawSettings?.drawingStorageMode) {
    const mode = normalizeDrawingStorageMode(requestedMode);
    if (mode === DRAWING_STORAGE_CONFIG) {
      return mode;
    }
    const realFile = getVaultFileByPath(this.app.vault, file2?.path || "");
    return realFile && String(realFile.extension || "").toLowerCase() === "md" ? mode : DRAWING_STORAGE_CONFIG;
  }
  drawingPathForFile(file2, requestedMode = this.noteDrawSettings?.drawingStorageMode) {
    const mode = this.drawingStorageModeForFile(file2, requestedMode);
    return resolveDrawingStoragePath({
      filePath: file2?.path || "",
      configDir: this.app.vault.configDir,
      pluginId: PLUGIN_ID,
      encodedName: this.encodedDrawingNameForFile(file2),
      mode
    });
  }
  drawingStorageKey(file2, requestedMode = this.noteDrawSettings?.drawingStorageMode) {
    const mode = this.drawingStorageModeForFile(file2, requestedMode);
    return `${mode}:${this.drawingPathForFile(file2, mode)}`;
  }
  async ensureDrawingStorageDir(file2, requestedMode = this.noteDrawSettings?.drawingStorageMode) {
    const mode = this.drawingStorageModeForFile(file2, requestedMode);
    if (mode === DRAWING_STORAGE_EMBEDDED) {
      return "";
    }
    const path = this.drawingPathForFile(file2, mode);
    const slash = path.lastIndexOf("/");
    const directory = slash >= 0 ? path.slice(0, slash) : "";
    if (directory) {
      await this.ensureFolder(directory);
    }
    return directory;
  }
  assetPathForName(name) {
    return `${this.app.vault.configDir}/plugins/${ASSET_DIR}/${sanitizeAssetFileName(name)}`;
  }
  legacyDrawingPathForFile(file2) {
    return `${this.app.vault.configDir}/plugins/${LEGACY_DRAWING_DIR}/${this.encodedDrawingNameForFile(file2)}`;
  }
  debugLogPath() {
    return `${this.app.vault.configDir}/plugins/${PLUGIN_ID}/${DEBUG_LOG_FILE}`;
  }
  async importLocalAsset(fileLike) {
    if (!fileLike) {
      return null;
    }
    await this.ensureAssetDir();
    const originalName = sanitizeAssetFileName(fileLike.name || "attachment.bin");
    const targetName = `${Date.now()}-${Math.random().toString(16).slice(2, 8)}-${originalName}`;
    const targetPath = this.assetPathForName(targetName);
    const buffer = await fileLike.arrayBuffer();
    await this.app.vault.adapter.writeBinary(targetPath, buffer);
    const mime = fileLike.type || guessMimeType(originalName);
    const text = isTextAssetMime(originalName, mime) && typeof fileLike.text === "function" ? await fileLike.text() : "";
    const imageDataUrl = classifyImportedAsset({ name: originalName, mime }) === EMBED_IMAGE ? arrayBufferToDataUrl(buffer, mime) : "";
    return {
      path: targetPath,
      name: originalName,
      mime,
      size: Number(fileLike.size || buffer.byteLength || 0),
      text,
      imageDataUrl
    };
  }
  async assetDataUrl(assetPath, mime = "", file2 = null) {
    if (!assetPath) {
      return "";
    }
    const portable = this.portableResourceUrl(file2, assetPath);
    if (portable) {
      return portable;
    }
    try {
      const buffer = await this.app.vault.adapter.readBinary(normalizeVaultPath(assetPath));
      return arrayBufferToDataUrl(buffer, mime || guessMimeType(assetPath));
    } catch (error) {
      void error;
      return "";
    }
  }
  async createPortableBundle(file2, data, options = {}) {
    const sourceMarkdown = typeof options.sourceMarkdown === "string" ? stripNotedrawDataBlocks(options.sourceMarkdown) : stripNotedrawDataBlocks(await this.app.vault.cachedRead(file2));
    const collected = await this.collectPortableResources(file2, data, {
      includeMarkdownLinks: options.includeMarkdownLinks === true,
      sourceMarkdown
    });
    return {
      format: "notedraw-portable",
      version: 1,
      purpose: options.purpose === "share" ? "share" : "storage",
      sourcePath: normalizeVaultPath(file2.path),
      updatedAt: options.updatedAt || (/* @__PURE__ */ new Date()).toISOString(),
      drawing: normalizeDrawingDataForStorage(data, file2),
      resources: collected.resources,
      skippedResources: collected.failed
    };
  }
  async buildPortableMarkdownCopy(file2) {
    const source = await this.app.vault.cachedRead(file2);
    const bundle = await this.readDrawingDataApi(file2, {
      purpose: "share",
      includeResources: true,
      includeMarkdownLinks: true,
      sourceMarkdown: source
    });
    return {
      bundle,
      markdown: await appendNotedrawDataBlock(source, bundle)
    };
  }
  shareCopyPathForFile(file2) {
    const normalizedPath = normalizeVaultPath(file2?.path || "");
    const slash = normalizedPath.lastIndexOf("/");
    const parent = slash >= 0 ? normalizedPath.slice(0, slash) : "";
    const originalBase = String(file2?.basename || file2?.name || "NoteDraw").replace(/\.notedraw$/i, "");
    const base = sanitizeAssetFileName(originalBase || "NoteDraw");
    let index = 1;
    while (true) {
      const suffix = index === 1 ? "" : `-${index}`;
      const name = `${base}.notedraw${suffix}.md`;
      const path = normalizeVaultPath(parent ? `${parent}/${name}` : name);
      if (!getVaultFileByPath(this.app.vault, path)) {
        return path;
      }
      index += 1;
    }
  }
  async createAndOpenShareCopy(file2, markdown, bundle) {
    const path = this.shareCopyPathForFile(file2);
    const copyFile = await this.app.vault.create(path, markdown);
    this.rememberPortableBundle(copyFile, normalizePortableBundle(bundle, copyFile));
    const leaf = this.app.workspace.getLeaf(false);
    await leaf.openFile(copyFile, { active: true });
    if (typeof leaf.setViewState === "function") {
      await leaf.setViewState({
        type: "markdown",
        state: { file: copyFile.path, mode: "preview", source: false },
        active: true
      });
    }
    this.app.workspace.setActiveLeaf(leaf, { focus: true });
    await this.waitForShareCopyPreview(copyFile, leaf);
    return copyFile;
  }
  async waitForShareCopyPreview(file2, leaf) {
    const path = normalizeVaultPath(file2?.path || "");
    const deadline = Date.now() + 1e4;
    while (Date.now() < deadline) {
      const view = leaf?.view;
      const preview = view instanceof import_obsidian.MarkdownView && normalizeVaultPath(view.file?.path || "") === path ? findRootPreviewForView(view) : null;
      if (preview?.isConnected && isMarkdownPreviewVisible(view, preview) && isRootPreviewReady(view, preview)) {
        await this.hydratePortableMarkdownResources(preview, path);
        const controller = this.resolveLivePreviewController(view);
        if (controller) {
          await controller.ensureDrawingsLoaded();
          controller.requestRender(true);
          await waitForNextFrame();
          await waitForNextFrame();
          if (controller.drawingsLoaded && controller.canvas?.isConnected && controller.renderFrameId === null) {
            return controller;
          }
        }
      }
      this.runSurfaceSync();
      await waitForNextFrame();
    }
    throw new Error("NoteDraw share copy did not finish its first preview frame");
  }
  async collectPortableResources(file2, data, options = {}) {
    const resources = /* @__PURE__ */ new Map();
    const failed = [];
    const existing = normalizePortableResources(this.portableBundles.get(normalizeVaultPath(file2?.path || ""))?.resources);
    const addExisting = (reference, aliases = []) => {
      const resource = findPortableResource(existing, reference, aliases);
      if (!resource) {
        return false;
      }
      resources.set(portableResourceIdentity(resource), resource);
      return true;
    };
    const addReference = async (reference, aliases = []) => {
      const normalizedReference = String(reference || "").trim();
      if (!normalizedReference) {
        return;
      }
      if (addExisting(normalizedReference, aliases)) {
        return;
      }
      try {
        const resource = await this.loadPortableResource(normalizedReference, file2, aliases);
        if (resource) {
          resources.set(portableResourceIdentity(resource), resource);
        } else {
          failed.push(normalizedReference);
        }
      } catch (error) {
        console.warn(`[${PLUGIN_ID}] Skipped portable resource`, normalizedReference, error);
        failed.push(normalizedReference);
      }
    };
    const drawingReferences = [];
    for (const stroke of Array.isArray(data?.strokes) ? data.strokes : []) {
      if (stroke?.assetPath) {
        drawingReferences.push(addReference(stroke.assetPath, [stroke.assetName, stroke.text].filter(Boolean)));
      }
      const renderMode = normalizeTextRenderMode(stroke?.render);
      if (renderMode === TEXT_RENDER_NOTE && stroke?.text) {
        drawingReferences.push(addReference(stroke.text));
      } else if (renderMode === TEXT_RENDER_MARKDOWN) {
        for (const reference of extractPortableMarkdownLinks(stroke?.text)) {
          drawingReferences.push(addReference(reference));
        }
      } else if (renderMode === TEXT_RENDER_HTML) {
        for (const reference of extractPortableHtmlLinks(stroke?.text)) {
          drawingReferences.push(addReference(reference));
        }
      }
      const mindMapSource = normalizeMindMapNode(stroke?.mindMapNode)?.sourcePath;
      if (mindMapSource) {
        drawingReferences.push(addReference(mindMapSource));
      }
    }
    await Promise.all(drawingReferences);
    if (options.includeMarkdownLinks) {
      const cache = this.app.metadataCache.getFileCache?.(file2);
      const links = [...Array.from(cache?.embeds || []), ...Array.from(cache?.links || [])].map((entry) => entry?.link).filter(Boolean);
      const fallbackLinks = extractPortableMarkdownLinks(options.sourceMarkdown);
      const markdownReferences = Array.from(/* @__PURE__ */ new Set([...links, ...fallbackLinks]));
      for (const reference of markdownReferences) {
        await addReference(reference);
      }
    } else if (existing.length) {
      for (const reference of extractPortableMarkdownLinks(options.sourceMarkdown)) {
        addExisting(reference);
      }
    }
    return {
      resources: Array.from(resources.values()),
      failed: Array.from(new Set(failed))
    };
  }
  async loadPortableResource(reference, ownerFile, aliases = []) {
    const raw = String(reference || "").trim();
    if (!raw || raw.startsWith("#") || /^(?:mailto|tel|obsidian):/i.test(raw)) {
      return null;
    }
    const cacheKey = /^(?:https?:|data:)/i.test(raw) ? raw : `${normalizeVaultPath(ownerFile?.path || "")}|${raw}`;
    const existingPromise = this.portableResourceCache.get(cacheKey);
    if (existingPromise) {
      return existingPromise;
    }
    const loading = this.readPortableResource(raw, ownerFile, aliases).finally(() => {
      while (this.portableResourceCache.size > PORTABLE_RESOURCE_CACHE_LIMIT) {
        this.portableResourceCache.delete(this.portableResourceCache.keys().next().value);
      }
    });
    this.portableResourceCache.set(cacheKey, loading);
    return loading;
  }
  async readPortableResource(reference, ownerFile, aliases = []) {
    const raw = String(reference || "").trim();
    if (/^data:[^;,]+;base64,/i.test(raw)) {
      const comma = raw.indexOf(",");
      const mime = raw.slice(5, raw.indexOf(";", 5)) || "application/octet-stream";
      const dataBase64 = raw.slice(comma + 1).replace(/\s+/g, "");
      return normalizePortableResource({
        id: portableResourceId(raw),
        source: raw,
        resolvedPath: raw,
        aliases,
        name: "embedded-resource",
        mime,
        size: Math.floor(dataBase64.length * 0.75),
        dataBase64
      });
    }
    if (/^https?:\/\//i.test(raw)) {
      const response = await (0, import_obsidian.requestUrl)({ url: raw, method: "GET" });
      const buffer2 = response.arrayBuffer;
      const mime = portableResponseMime(response.headers) || portableMimeType(raw);
      return normalizePortableResource({
        id: portableResourceId(raw),
        source: raw,
        resolvedPath: raw,
        aliases,
        name: portableUrlName(raw),
        mime,
        size: buffer2.byteLength,
        dataBase64: arrayBufferToBase64(buffer2)
      });
    }
    const linkPath = unwrapWikiLink(raw.replace(/^!/, "")).split("|")[0].split("#")[0].trim();
    const linkedFile = this.app.metadataCache.getFirstLinkpathDest?.(linkPath, ownerFile?.path || "") || getVaultFileByPath(this.app.vault, linkPath);
    if (linkedFile) {
      const buffer2 = await this.app.vault.readBinary(linkedFile);
      return normalizePortableResource({
        id: portableResourceId(linkedFile.path),
        source: raw,
        resolvedPath: normalizeVaultPath(linkedFile.path),
        aliases: [linkPath, linkedFile.name, ...aliases],
        name: linkedFile.name,
        mime: portableMimeType(linkedFile.name),
        size: buffer2.byteLength,
        dataBase64: arrayBufferToBase64(buffer2)
      });
    }
    const adapterPath = normalizeVaultPath(linkPath || raw);
    if (!adapterPath || !await this.app.vault.adapter.exists(adapterPath)) {
      return null;
    }
    const buffer = await this.app.vault.adapter.readBinary(adapterPath);
    return normalizePortableResource({
      id: portableResourceId(adapterPath),
      source: raw,
      resolvedPath: adapterPath,
      aliases: [adapterPath.split("/").pop(), ...aliases],
      name: adapterPath.split("/").pop() || "attachment.bin",
      mime: portableMimeType(adapterPath),
      size: buffer.byteLength,
      dataBase64: arrayBufferToBase64(buffer)
    });
  }
  portableResource(file2, reference) {
    const bundle = this.portableBundles.get(normalizeVaultPath(file2?.path || ""));
    return findPortableResource(bundle?.resources, reference);
  }
  portableResourceUrl(file2, reference) {
    const resource = this.portableResource(file2, reference);
    return resource ? portableResourceDataUrl(resource) : "";
  }
  async hydratePortableMarkdownResources(root, sourcePath) {
    if (!root?.querySelectorAll) {
      return;
    }
    const file2 = getVaultFileByPath(this.app.vault, sourcePath);
    const bundle = file2 ? await this.loadPortableBundle(file2) : null;
    const resources = normalizePortableResources(bundle?.resources);
    if (!resources.length || !root.isConnected) {
      return;
    }
    const elements = [root, ...Array.from(root.querySelectorAll("img, video, audio, source, a, .internal-embed"))];
    for (const element of elements) {
      if (!element?.matches?.("img, video, audio, source, a, .internal-embed")) {
        continue;
      }
      const references = portableElementReferences(element);
      const resource = references.map((reference) => findPortableResource(resources, reference)).find(Boolean);
      if (!resource) {
        continue;
      }
      const dataUrl = portableResourceDataUrl(resource);
      const tag = String(element.tagName || "").toLowerCase();
      if (["img", "video", "audio", "source"].includes(tag)) {
        element.setAttribute("src", dataUrl);
        element.dataset.notedrawPortableResource = resource.id;
        continue;
      }
      if (tag === "a") {
        element.setAttribute("href", dataUrl);
        element.setAttribute("download", resource.name || "attachment.bin");
        element.classList.remove("internal-link", "is-unresolved");
        element.dataset.notedrawPortableResource = resource.id;
        continue;
      }
      await this.renderPortableInternalEmbed(element, resource, file2);
    }
  }
  async renderPortableInternalEmbed(element, resource, ownerFile) {
    if (element.dataset.notedrawPortableResource === resource.id) {
      return;
    }
    element.dataset.notedrawPortableResource = resource.id;
    const dataUrl = portableResourceDataUrl(resource);
    const mime = String(resource.mime || "").toLowerCase();
    const host = element.createDiv({ cls: "notedraw-portable-resource" });
    if (mime.startsWith("image/")) {
      host.createEl("img", { attr: { src: dataUrl, alt: resource.name || "Image" } });
      return;
    }
    if (mime.startsWith("video/")) {
      host.createEl("video", { attr: { src: dataUrl, controls: "true", playsinline: "true" } });
      return;
    }
    if (mime.startsWith("audio/")) {
      host.createEl("audio", { attr: { src: dataUrl, controls: "true" } });
      return;
    }
    if (mime === "text/markdown") {
      const renderComponent = this.addChild(new import_obsidian.Component());
      await import_obsidian.MarkdownRenderer.render(this.app, portableResourceText(resource), host, ownerFile?.path || "", renderComponent);
      return;
    }
    const link = host.createEl("a", {
      text: resource.name || "Attachment",
      attr: { href: dataUrl, download: resource.name || "attachment.bin" }
    });
    (0, import_obsidian.setIcon)(link.createSpan({ cls: "notedraw-portable-resource-icon" }), "paperclip");
  }
  async shareNoteDrawFile(file2) {
    const key = normalizeVaultPath(file2?.path || "");
    if (!key || this.sharePackagePromises.has(key)) {
      return this.sharePackagePromises.get(key);
    }
    new import_obsidian.Notice(this.t("sharePreparing"));
    const sharing = (async () => {
      try {
        const { markdown, bundle } = await this.buildPortableMarkdownCopy(file2);
        const copyFile = await this.createAndOpenShareCopy(file2, markdown, bundle);
        const name = copyFile.name;
        const shareFile = typeof File === "function" ? new File([markdown], name, { type: "text/markdown;charset=utf-8" }) : null;
        const shareData = shareFile ? { title: copyFile.basename || file2.basename || "NoteDraw", files: [shareFile] } : null;
        let shared = false;
        let canUseNativeShare = Boolean(shareData && typeof navigator !== "undefined" && typeof navigator.share === "function");
        if (canUseNativeShare && typeof navigator.canShare === "function") {
          try {
            canUseNativeShare = navigator.canShare(shareData);
          } catch {
            canUseNativeShare = false;
          }
        }
        if (canUseNativeShare) {
          try {
            await navigator.share(shareData);
            shared = true;
          } catch (error) {
            if (error?.name === "AbortError") {
              return;
            }
          }
        }
        if (!shared) {
          this.downloadPortableMarkdown(markdown, name);
          new import_obsidian.Notice(this.t("shareDownloaded"));
        } else {
          new import_obsidian.Notice(this.t("shareReady", { count: bundle.resources.length }));
        }
      } catch (error) {
        console.error(`[${PLUGIN_ID}] Failed to share portable NoteDraw file`, error);
        new import_obsidian.Notice(this.t("shareFailed"));
      }
    })().finally(() => {
      this.sharePackagePromises.delete(key);
    });
    this.sharePackagePromises.set(key, sharing);
    return sharing;
  }
  downloadPortableMarkdown(markdown, name) {
    const blob = new Blob([markdown], { type: "text/markdown;charset=utf-8" });
    const url = URL.createObjectURL(blob);
    const link = activeDocument.body.createEl("a", {
      attr: { href: url, download: name, "aria-hidden": "true" }
    });
    link.click();
    link.remove();
    window.setTimeout(() => URL.revokeObjectURL(url), 1e3);
  }
  async appendDebugLog(entry) {
    if (!this.noteDrawSettings?.enableDebugLog) {
      return;
    }
    try {
      await this.ensureDrawingDir();
      const path = this.debugLogPath();
      const adapter = this.app.vault.adapter;
      const line = JSON.stringify({
        time: (/* @__PURE__ */ new Date()).toISOString(),
        ...entry
      });
      let lines = [];
      if (await adapter.exists(path)) {
        lines = String(await adapter.read(path) || "").split(/\r?\n/).filter(Boolean).slice(-(DEBUG_LOG_LIMIT - 1));
      }
      lines.push(line);
      await adapter.write(path, `${lines.join("\n")}
`);
    } catch (error) {
      console.error(`[${PLUGIN_ID}] Failed to write debug log`, error);
    }
  }
  async readDrawings(file2, options = {}) {
    const storageMode = this.drawingStorageModeForFile(file2);
    const storageKey = this.drawingStorageKey(file2, storageMode);
    const pending = this.pendingDrawingSaves.get(storageKey);
    if (pending?.entries?.length) {
      const latest = materializeDrawingSaveRequest(
        this.drawingStateCache.get(storageKey),
        pending,
        normalizeDrawingDataForStorage
      );
      return normalizeDrawingData(latest, file2);
    }
    const cached = this.drawingStateCache.get(storageKey);
    if (cached && options.refresh !== true) {
      return normalizeDrawingData(cached, file2);
    }
    try {
      const adapter = this.app.vault.adapter;
      const selectedPath = storageMode === DRAWING_STORAGE_EMBEDDED ? "" : this.drawingPathForFile(file2, storageMode);
      const configPath = this.drawingPathForFile(file2, DRAWING_STORAGE_CONFIG);
      const legacyPath = this.legacyDrawingPathForFile(file2);
      const portableBundle = await this.loadPortableBundle(file2, { refresh: options.refresh === true });
      const candidates = [];
      if (portableBundle?.drawing) {
        candidates.push({
          kind: "portable",
          priority: storageMode === DRAWING_STORAGE_EMBEDDED ? 4 : portableBundle.purpose === "share" ? 3 : 2,
          updatedAt: portableBundle.updatedAt || portableBundle.drawing.updatedAt,
          data: portableBundle.drawing
        });
      }
      const storedPaths = Array.from(new Set([selectedPath, configPath].filter(Boolean)));
      for (const path of storedPaths) {
        if (!await adapter.exists(path)) {
          continue;
        }
        try {
          const data2 = JSON.parse(await adapter.read(path));
          candidates.push({
            kind: path === selectedPath ? "selected" : "config",
            priority: path === selectedPath ? 5 : 2,
            updatedAt: data2?.updatedAt,
            data: data2
          });
        } catch (error) {
          console.warn(`[${PLUGIN_ID}] Skipped invalid drawing data`, path, error);
        }
      }
      if (await adapter.exists(legacyPath)) {
        try {
          const data2 = JSON.parse(await adapter.read(legacyPath));
          candidates.push({ kind: "legacy", priority: 1, updatedAt: data2?.updatedAt, data: data2 });
        } catch (error) {
          console.warn(`[${PLUGIN_ID}] Skipped invalid legacy drawing data`, legacyPath, error);
        }
      }
      candidates.sort((a, b) => portableTimestamp(b.updatedAt) - portableTimestamp(a.updatedAt) || b.priority - a.priority);
      const selected = candidates[0] || null;
      const sourceRevision = sourceRevisionForFile(file2);
      const storedRevision = normalizeSourceRevision(selected?.data?.sourceRevision);
      const storedMarkdownBlockCount = Array.isArray(selected?.data?.markdownBlocks) ? selected.data.markdownBlocks.length : 0;
      const data = selected ? normalizeDrawingData(selected.data, file2) : createEmptyDrawingData(file2);
      const repairedMarkdownBlocks = Boolean(selected && data.markdownBlocks.length < storedMarkdownBlockCount);
      const sourceRevisionMismatch = Boolean(selected && (!storedRevision || storedRevision !== sourceRevision));
      data.sourceRevision = sourceRevision;
      Object.defineProperty(data, "_notedrawSourceRevisionMismatch", {
        configurable: true,
        enumerable: false,
        value: sourceRevisionMismatch
      });
      this.drawingStateCache.set(storageKey, normalizeDrawingData(data, file2));
      if (selected?.kind === "legacy" && options.migrateLegacy !== false || repairedMarkdownBlocks && options.repair !== false) {
        await this.writeDrawings(file2, data, { refresh: false });
      }
      return data;
    } catch (error) {
      console.error(`[${PLUGIN_ID}] Failed to read drawing file`, error);
      return createEmptyDrawingData(file2);
    }
  }
  async loadPortableBundle(file2, options = {}) {
    const realFile = getVaultFileByPath(this.app.vault, file2?.path || "");
    if (!realFile || String(realFile.extension || "").toLowerCase() !== "md") {
      return null;
    }
    const key = normalizeVaultPath(realFile.path);
    if (!options.refresh && this.portableBundles.has(key)) {
      return this.portableBundles.get(key);
    }
    if (!options.refresh && this.portableBundleLoads.has(key)) {
      return this.portableBundleLoads.get(key);
    }
    const sourceRead = options.refresh === true && typeof this.app.vault.adapter?.read === "function" ? this.app.vault.adapter.read(realFile.path) : this.app.vault.cachedRead(realFile);
    const loading = sourceRead.then(async (source) => {
      const decoded = await decodeNotedrawDataBlock(source);
      const bundle = normalizePortableBundle(decoded, realFile);
      this.rememberPortableBundle(realFile, bundle);
      return bundle;
    }).catch((error) => {
      console.error(`[${PLUGIN_ID}] Failed to read embedded NoteDraw data`, error);
      this.rememberPortableBundle(realFile, null);
      return null;
    }).finally(() => {
      if (this.portableBundleLoads.get(key) === loading) {
        this.portableBundleLoads.delete(key);
      }
    });
    this.portableBundleLoads.set(key, loading);
    return loading;
  }
  rememberPortableBundle(file2, bundle) {
    const key = normalizeVaultPath(file2?.path || "");
    if (!key) {
      return;
    }
    this.portableBundles.delete(key);
    this.portableBundles.set(key, bundle || null);
    while (this.portableBundles.size > PORTABLE_RESOURCE_CACHE_LIMIT) {
      this.portableBundles.delete(this.portableBundles.keys().next().value);
    }
  }
  scheduleDrawingSave(file2, data, options = {}) {
    const path = this.drawingStorageKey(file2);
    if (options.replace !== true && options.userOperation !== true) {
      this.suppressedDrawingSaves.push({
        at: Date.now(),
        path,
        strokeCount: Array.isArray(data?.strokes) ? data.strokes.length : 0,
        markdownBlockCount: Array.isArray(data?.markdownBlocks) ? data.markdownBlocks.length : 0,
        stack: new Error().stack || ""
      });
      this.suppressedDrawingSaves.splice(0, Math.max(0, this.suppressedDrawingSaves.length - 20));
      return false;
    }
    this.pendingDrawingSaves.set(path, coalesceDrawingSaveRequest(
      this.pendingDrawingSaves.get(path),
      {
        file: file2,
        data,
        excludeData: options.excludeData || data,
        replace: options.replace === true
      }
    ));
    this.cancelScheduledDrawingSave(path);
    const timer = window.setTimeout(() => {
      this.saveTimers.delete(path);
      const run = () => {
        this.saveIdleCallbacks.delete(path);
        this.flushDrawingSave(path).catch((error) => {
          console.error(`[${PLUGIN_ID}] Failed to save drawing file`, error);
          new import_obsidian.Notice(this.t("failedSaveDrawing"));
        });
      };
      if (typeof window.requestIdleCallback === "function") {
        const idleId = window.requestIdleCallback(run, { timeout: 800 });
        this.saveIdleCallbacks.set(path, idleId);
      } else {
        run();
      }
    }, this.noteDrawSettings?.autoSaveDelayMs ?? DEFAULT_SETTINGS.autoSaveDelayMs);
    this.saveTimers.set(path, timer);
    return true;
  }
  cancelScheduledDrawingSave(path) {
    const timer = this.saveTimers.get(path);
    if (timer !== void 0) {
      window.clearTimeout(timer);
      this.saveTimers.delete(path);
    }
    const idleId = this.saveIdleCallbacks.get(path);
    if (idleId !== void 0) {
      window.cancelIdleCallback?.(idleId);
      this.saveIdleCallbacks.delete(path);
    }
  }
  async flushDrawingSave(path) {
    this.cancelScheduledDrawingSave(path);
    const request = this.pendingDrawingSaves.get(path);
    if (!request) {
      return;
    }
    if (this.pendingDrawingSaves.get(path) === request) {
      this.pendingDrawingSaves.delete(path);
    }
    const previousWrite = this.drawingWritePromises.get(path) || Promise.resolve();
    const write = previousWrite.catch(() => void 0).then(async () => {
      const canonical = materializeDrawingSaveRequest(
        this.drawingStateCache.get(path),
        request,
        normalizeDrawingDataForStorage
      );
      if (!canonical) {
        return;
      }
      this.drawingStateCache.set(path, canonical);
      const lastEntry = request.entries[request.entries.length - 1];
      this.refreshControllersForFile(request.file, canonical, { excludeData: lastEntry?.excludeData });
      const compacted = normalizeDrawingDataForStorage(canonical, request.file);
      compactDrawingData(compacted, this.noteDrawSettings?.drawingCompactDistance ?? DEFAULT_SETTINGS.drawingCompactDistance);
      await this.writeDrawings(request.file, compacted, { normalized: true, refresh: false, updateCache: false });
    });
    this.drawingWritePromises.set(path, write);
    try {
      await write;
    } finally {
      if (this.drawingWritePromises.get(path) === write) {
        this.drawingWritePromises.delete(path);
      }
    }
  }
  async writeDrawings(file2, data, options = {}) {
    const storageMode = this.drawingStorageModeForFile(file2);
    const path = this.drawingPathForFile(file2, storageMode);
    const storageKey = this.drawingStorageKey(file2, storageMode);
    const normalized = options.normalized === true ? data : normalizeDrawingDataForStorage(data, file2);
    const updatedAt = (/* @__PURE__ */ new Date()).toISOString();
    normalized.updatedAt = updatedAt;
    if (options.updateCache !== false) {
      this.drawingStateCache.set(storageKey, normalizeDrawingData(normalized, file2));
    }
    let storagePath = path;
    if (storageMode === DRAWING_STORAGE_EMBEDDED) {
      const realFile = getVaultFileByPath(this.app.vault, file2?.path || "");
      if (!realFile) {
        throw new Error(`Cannot embed NoteDraw data in missing Markdown file: ${file2?.path || ""}`);
      }
      const bundle = await this.createPortableBundle(realFile, normalized, {
        purpose: "storage",
        includeMarkdownLinks: false,
        updatedAt
      });
      const block = await encodeNotedrawDataBlock(bundle);
      if (typeof this.app.vault.process === "function") {
        await this.app.vault.process(realFile, (source) => appendEncodedNotedrawDataBlock(source, block));
      } else {
        const source = await this.app.vault.cachedRead(realFile);
        await this.app.vault.modify(realFile, appendEncodedNotedrawDataBlock(source, block));
      }
      this.rememberPortableBundle(realFile, bundle);
      storagePath = `${realFile.path}#NOTEDRAW_DATA_BEGIN`;
    } else {
      await this.ensureDrawingStorageDir(file2, storageMode);
      const body = JSON.stringify({
        ...normalized,
        sourcePath: file2.path,
        updatedAt
      }, null, 2);
      await this.app.vault.adapter.write(path, body);
    }
    if (options.refresh !== false) {
      this.refreshControllersForFile(file2, normalized, { excludeData: data });
    }
    this.emitApiEvent("drawings-changed", {
      file: file2.path,
      storagePath,
      strokeCount: normalized.strokes.length,
      updatedAt
    });
  }
  async injectExportSnapshot(file2, container) {
    if (!file2 || !(container instanceof HTMLElement)) {
      return null;
    }
    const host = findNoteDrawExportHost(container);
    const liveLayers = Array.from(host.querySelectorAll(".notedraw-underlay-embed-layer, .notedraw-embed-layer"));
    const liveDrawingData = host._noteDrawController?.drawingData || container._noteDrawController?.drawingData || null;
    const imageLayer = await this.injectExportImageCanvasLayer(file2, host, liveDrawingData);
    if (liveLayers.length) {
      await Promise.all(liveLayers.map((layer) => prepareExportImages(layer)));
      return imageLayer || liveLayers[liveLayers.length - 1];
    }
    return imageLayer;
  }
  async injectExportImageCanvasLayer(file2, container, drawingData = null) {
    container.querySelectorAll(".notedraw-export-image-canvas-layer").forEach((element) => element.remove());
    const data = drawingData || await this.readDrawings(file2);
    const imageStrokes = (Array.isArray(data?.strokes) ? data.strokes : []).filter(isImageEmbedStroke);
    if (!imageStrokes.length) {
      return null;
    }
    const rect = container.getBoundingClientRect();
    const width = Math.max(1, Math.ceil(container.scrollWidth || rect.width || 1));
    const height = Math.max(1, Math.ceil(container.scrollHeight || rect.height || 1));
    if (getComputedStyle(container).position === "static") {
      applyElementStyles(container, { position: "relative" });
    }
    const layer = activeDocument.createElement("div");
    layer.className = "notedraw-export-image-canvas-layer";
    applyElementStyles(layer, {
      position: "absolute",
      zIndex: "58",
      top: "0",
      left: "0",
      right: "auto",
      bottom: "auto",
      width: `${width}px`,
      height: `${height}px`,
      pointerEvents: "none",
      userSelect: "none",
      background: "transparent"
    });
    container.appendChild(layer);
    let drewImage = false;
    for (const stroke of imageStrokes) {
      drewImage = await this.drawExportImageStrokeOn(layer, stroke, width, height) || drewImage;
    }
    if (!drewImage) {
      layer.remove();
      return null;
    }
    window.setTimeout(() => {
      layer.remove();
    }, 3e4);
    return layer;
  }
  async drawExportImageStrokeOn(layer, stroke, width, height) {
    const bounds = getStrokeBounds(stroke, width, height);
    if (!bounds) {
      return false;
    }
    const source = normalizeImageDataUrl(stroke.exportImageDataUrl) || await this.assetDataUrl(stroke.assetPath, stroke.assetMime || guessMimeType(stroke.assetName || stroke.assetPath), file);
    if (!source) {
      return false;
    }
    const image = await loadExportImage(source, 2200);
    if (!image?.naturalWidth || !image?.naturalHeight) {
      return false;
    }
    const x = Math.round(bounds.minX);
    const y = Math.round(bounds.minY);
    const boxWidth = Math.max(1, Math.round(bounds.maxX - bounds.minX));
    const boxHeight = Math.max(1, Math.round(bounds.maxY - bounds.minY));
    const fit = objectFitContain(image.naturalWidth, image.naturalHeight, boxWidth, boxHeight);
    const scale = Math.min(2, Math.max(1, Number(window.devicePixelRatio || 1)));
    const canvas = activeDocument.createElement("canvas");
    canvas.className = "notedraw-export-image-canvas";
    canvas.width = Math.ceil(boxWidth * scale);
    canvas.height = Math.ceil(boxHeight * scale);
    const opacity = clamp10(Number(stroke.opacity ?? 1), 0, 1);
    canvas.dataset.notedrawAssetPath = stroke.assetPath || "";
    canvas.dataset.notedrawAssetName = stroke.assetName || "";
    canvas.dataset.notedrawAssetMime = stroke.assetMime || "";
    canvas.dataset.notedrawAssetSize = String(stroke.assetSize || 0);
    applyElementStyles(canvas, {
      position: "absolute",
      left: `${x}px`,
      top: `${y}px`,
      width: `${boxWidth}px`,
      height: `${boxHeight}px`,
      pointerEvents: "none",
      userSelect: "none",
      background: "#fff",
      opacity: String(opacity)
    });
    const context = canvas.getContext("2d");
    if (!context) {
      return false;
    }
    context.setTransform(scale, 0, 0, scale, 0, 0);
    context.fillStyle = "#fff";
    context.fillRect(0, 0, boxWidth, boxHeight);
    context.imageSmoothingEnabled = true;
    context.imageSmoothingQuality = "high";
    context.globalAlpha = opacity;
    context.drawImage(image, fit.x, fit.y, fit.width, fit.height);
    layer.appendChild(canvas);
    return true;
  }
  getPluginAssetPath(relativePath) {
    const pluginDir = this.manifest.dir ?? `${this.app.vault.configDir}/plugins/${this.manifest.id}`;
    return (0, import_obsidian.normalizePath)(`${pluginDir}/${relativePath}`);
  }
  async getOptionalAssetResourcePath(relativePath) {
    const assetPath = this.getPluginAssetPath(relativePath);
    if (!await this.app.vault.adapter.exists(assetPath)) {
      return null;
    }
    return this.app.vault.adapter.getResourcePath(assetPath);
  }
  prepareTextEditState(file2, originalText, element, controller = null) {
    const state = this.getTextSaveState(file2, originalText, element);
    state.file = file2;
    state.baselineText = originalText;
    state.latestText = originalText;
    state.latestSourceInfo = getSourceInfo(element);
    state.target = null;
    state.controller = controller || state.controller;
    state.targetPromise = this.resolveTextEditTarget(file2, originalText, element).then((target) => {
      state.target = target;
      return target;
    }).catch((error) => {
      console.error(`[${PLUGIN_ID}] Failed to resolve text edit target`, error);
      return null;
    });
    return state;
  }
  async resolveTextEditTarget(file2, originalText, element) {
    const sourceInfo = getSourceInfo(element);
    const source = await this.app.vault.read(file2);
    const target = resolveSourceEditTarget(source, sourceInfo, originalText);
    this.appendDebugLog({
      event: "resolve-target",
      file: file2.path,
      sourceInfo: summarizeSourceInfo(sourceInfo),
      original: shortText(originalText),
      hasTarget: Boolean(target),
      target: summarizeTarget(target)
    });
    return target;
  }
  scheduleTextSave(file2, originalText, editedText, element, controller = null) {
    const state = this.getTextSaveState(file2, originalText, element);
    state.file = file2;
    state.latestText = editedText;
    state.latestSourceInfo = getSourceInfo(element);
    state.controller = controller || state.controller;
    state.saveBlocked = false;
    state.saveLogged = false;
    if (!state.target && !state.targetPromise) {
      state.targetPromise = this.resolveTextEditTarget(file2, originalText, element).then((target) => {
        state.target = target;
        return target;
      }).catch((error) => {
        console.error(`[${PLUGIN_ID}] Failed to resolve text edit target`, error);
        return null;
      });
    }
    if (state.timer) {
      window.clearTimeout(state.timer);
    }
    element.addClass("notedraw-saving");
    state.timer = window.setTimeout(() => {
      state.timer = null;
      this.flushTextSave(element);
    }, TEXT_SAVE_DELAY_MS);
  }
  stageTextSave(file2, originalText, editedText, element, controller = null) {
    const state = this.getTextSaveState(file2, originalText, element);
    state.file = file2;
    state.latestText = editedText;
    state.latestSourceInfo = getSourceInfo(element);
    state.controller = controller || state.controller;
    state.saveBlocked = false;
    state.saveLogged = false;
    if (!state.target && !state.targetPromise) {
      state.targetPromise = this.resolveTextEditTarget(file2, originalText, element).then((target) => {
        state.target = target;
        return target;
      }).catch((error) => {
        console.error(`[${PLUGIN_ID}] Failed to resolve text edit target`, error);
        return null;
      });
    }
    if (state.timer) {
      window.clearTimeout(state.timer);
      state.timer = null;
    }
    return state;
  }
  scheduleTextSaveNow(file2, originalText, editedText, element, controller = null) {
    const state = this.getTextSaveState(file2, originalText, element);
    state.file = file2;
    state.latestText = editedText;
    state.latestSourceInfo = getSourceInfo(element);
    state.controller = controller || state.controller;
    state.saveBlocked = false;
    state.saveLogged = false;
    if (!state.target && !state.targetPromise) {
      state.targetPromise = this.resolveTextEditTarget(file2, originalText, element).then((target) => {
        state.target = target;
        return target;
      }).catch((error) => {
        console.error(`[${PLUGIN_ID}] Failed to resolve text edit target`, error);
        return null;
      });
    }
    if (state.timer) {
      window.clearTimeout(state.timer);
      state.timer = null;
    }
    element.addClass("notedraw-saving");
    return this.flushTextSave(element);
  }
  getTextSaveState(file2, originalText, element) {
    let state = this.textSaveStates.get(element);
    if (!state) {
      state = {
        file: file2,
        baselineText: originalText,
        latestText: originalText,
        latestSourceInfo: getSourceInfo(element),
        target: null,
        targetPromise: null,
        timer: null,
        saving: false,
        pending: false,
        saveBlocked: false,
        warningLogged: false,
        saveLogged: false
      };
      this.textSaveStates.set(element, state);
    }
    return state;
  }
  async flushTextSave(element) {
    const state = this.textSaveStates.get(element);
    if (!state) {
      return;
    }
    if (state.saving) {
      state.pending = true;
      return;
    }
    const baselineText = state.baselineText;
    const latestText = state.latestText;
    if (normalizeEditableSourceText(baselineText) === normalizeEditableSourceText(latestText)) {
      element.removeClass("notedraw-saving");
      return;
    }
    state.saving = true;
    try {
      if (state.targetPromise) {
        state.target = await state.targetPromise;
        state.targetPromise = null;
      }
      const result = await this.saveTextBlock(
        state.file,
        baselineText,
        latestText,
        state.latestSourceInfo,
        state.target
      );
      if (result.changed) {
        if (result.history && state.controller) {
          state.controller.recordMarkdownHistory(state.file, result.history.before, result.history.after, state);
        }
        state.baselineText = latestText;
        state.target = result.target || state.target;
        state.warningLogged = false;
        state.saveBlocked = false;
        element.dataset.noteDrawOriginal = latestText;
        element.removeClass("notedraw-save-failed");
        if (!state.saveLogged) {
          this.appendDebugLog({
            event: "save-ok",
            file: state.file?.path,
            sourceInfo: summarizeSourceInfo(state.latestSourceInfo),
            target: summarizeTarget(state.target),
            original: shortText(baselineText),
            edited: shortText(latestText)
          });
          state.saveLogged = true;
        }
      } else {
        element.addClass("notedraw-save-failed");
        state.saveBlocked = true;
        if (!state.warningLogged) {
          console.warn(`[${PLUGIN_ID}] Could not find the original block to update`, {
            path: state.file?.path,
            sourceInfo: state.latestSourceInfo,
            originalLength: String(baselineText || "").length,
            editedLength: String(latestText || "").length
          });
          this.appendDebugLog({
            event: "save-miss",
            file: state.file?.path,
            sourceInfo: summarizeSourceInfo(state.latestSourceInfo),
            target: summarizeTarget(state.target),
            original: shortText(baselineText),
            edited: shortText(latestText)
          });
          state.warningLogged = true;
        }
      }
    } catch (error) {
      console.error(`[${PLUGIN_ID}] Failed to save text block`, error);
      element.addClass("notedraw-save-failed");
      this.appendDebugLog({
        event: "save-error",
        file: state.file?.path,
        sourceInfo: summarizeSourceInfo(state.latestSourceInfo),
        target: summarizeTarget(state.target),
        error: String(error?.message || error)
      });
    } finally {
      state.saving = false;
    }
    if (state.pending || normalizeEditableSourceText(state.baselineText) !== normalizeEditableSourceText(state.latestText)) {
      if (state.saveBlocked) {
        element.removeClass("notedraw-saving");
        return;
      }
      state.pending = false;
      state.timer = window.setTimeout(() => {
        state.timer = null;
        this.flushTextSave(element);
      }, TEXT_SAVE_DELAY_MS);
      return;
    }
    element.removeClass("notedraw-saving");
  }
  async flushTextSaveAndWait(element) {
    const state = this.textSaveStates.get(element);
    if (!state) {
      return true;
    }
    if (state.timer) {
      window.clearTimeout(state.timer);
      state.timer = null;
    }
    for (let attempt = 0; attempt < 200; attempt += 1) {
      await this.flushTextSave(element);
      if (state.saveBlocked) {
        return false;
      }
      const settled = !state.saving && normalizeEditableSourceText(state.baselineText) === normalizeEditableSourceText(state.latestText);
      if (settled) {
        if (state.timer) {
          window.clearTimeout(state.timer);
          state.timer = null;
        }
        state.pending = false;
        return true;
      }
      await new Promise((resolve) => window.setTimeout(resolve, 10));
    }
    return false;
  }
  discardTextSaveState(element) {
    const state = this.textSaveStates.get(element);
    if (state?.timer) {
      window.clearTimeout(state.timer);
    }
    this.textSaveStates.delete(element);
  }
  async saveTextBlock(file2, originalText, editedText, sourceInfo, target) {
    const normalizedOriginal = normalizeRenderedText2(originalText);
    if (!normalizedOriginal || normalizeEditableSourceText(originalText) === normalizeEditableSourceText(editedText)) {
      return { changed: true, target };
    }
    const source = await this.app.vault.read(file2);
    const match = resolveLockedTarget(source, target, originalText) || resolveSourceEditTarget(source, sourceInfo, originalText);
    if (!match) {
      return { changed: false, target };
    }
    const replacement = formatReplacementBlock(match.text, editedText);
    const start = match.start;
    const end = match.end;
    const currentText = source.slice(start, end);
    const nextTarget = createTextEditTarget({
      ...match,
      end: start + replacement.length,
      text: replacement
    }, sourceInfo, editedText);
    if (currentText !== replacement) {
      const nextSource = `${source.slice(0, start)}${replacement}${source.slice(end)}`;
      await this.app.vault.modify(file2, nextSource);
      this.emitApiEvent("markdown-changed", {
        file: file2.path,
        start,
        end,
        replacementLength: replacement.length
      });
      return { changed: true, target: nextTarget, history: { before: source, after: nextSource } };
    }
    return { changed: true, target: nextTarget };
  }
  async reorderTextBlock(file2, movingElement, targetElement, placeAfter = false, sourceState = {}) {
    if (!file2 || !movingElement || !targetElement || movingElement === targetElement) {
      return false;
    }
    const source = await this.app.vault.read(file2);
    const moving = resolveSourceEditTarget(source, sourceState.movingInfo || getSourceInfo(movingElement), sourceState.movingText || movingElement.innerText);
    const target = resolveSourceEditTarget(source, sourceState.targetInfo || getSourceInfo(targetElement), sourceState.targetText || targetElement.innerText);
    if (!moving || !target || moving.start === target.start || moving.start < target.end && moving.end > target.start) {
      return false;
    }
    const block = source.slice(moving.start, moving.end).trim();
    let without = `${source.slice(0, moving.start)}${source.slice(moving.end)}`;
    let insertion = placeAfter ? target.end : target.start;
    if (moving.start < insertion) {
      insertion -= moving.end - moving.start;
    }
    const collapsed = collapseRunawayBlankLines(without, insertion);
    without = collapsed.text;
    insertion = collapsed.position;
    const before = without.slice(0, insertion).replace(/[ \t]+$/g, "");
    const after = without.slice(insertion).replace(/^[ \t]+/g, "");
    const separatorBefore = before && !before.endsWith("\n\n") ? before.endsWith("\n") ? "\n" : "\n\n" : "";
    const separatorAfter = after && !after.startsWith("\n\n") ? after.startsWith("\n") ? "\n" : "\n\n" : "";
    const nextSource = `${before}${separatorBefore}${block}${separatorAfter}${after}`;
    await this.app.vault.modify(file2, nextSource);
    return { changed: true, before: source, after: nextSource };
  }
  async reorderTextBlocks(file2, movingStates, targetElement, placeAfter = false, sourceState = {}) {
    if (!file2 || !Array.isArray(movingStates) || !movingStates.length || !targetElement) {
      return false;
    }
    const source = await this.app.vault.read(file2);
    const lockedMovingTargets = Array.isArray(sourceState.lockedMovingTargets) ? sourceState.lockedMovingTargets : [];
    const moving = movingStates.map((state, index) => {
      const renderedText = state?.sourceText || state?.element?.innerText;
      const locked = lockedMovingTargets[index];
      if (locked) {
        return resolveLockedTarget(source, locked, renderedText);
      }
      return sourceState.strictMoving ? null : resolveSourceEditTarget(source, state?.sourceInfo || getSourceInfo(state?.element), renderedText);
    }).filter(Boolean).sort((a, b) => a.start - b.start);
    const targetText = sourceState.targetText || targetElement.innerText;
    const target = sourceState.lockedTarget ? resolveLockedTarget(source, sourceState.lockedTarget, targetText) : sourceState.strictTarget ? null : resolveSourceEditTarget(
      source,
      sourceState.targetInfo || getSourceInfo(targetElement),
      targetText
    );
    if (moving.length !== movingStates.length || !target || moving.some((item) => item.start === target.start || item.start < target.end && item.end > target.start)) {
      return false;
    }
    const uniqueMoving = moving.filter((item, index) => index === 0 || item.start !== moving[index - 1].start);
    const validMoving = uniqueMoving.filter((item) => isValidSourceRange(source, item.start, item.end) && source.slice(item.start, item.end).trim());
    if (validMoving.length !== uniqueMoving.length) {
      return false;
    }
    const blocks = validMoving.map((item) => source.slice(item.start, item.end).trim());
    if (!blocks.length) {
      return false;
    }
    let insertion = placeAfter ? target.end : target.start;
    if (!Number.isFinite(insertion) || insertion < 0 || insertion > source.length) {
      return false;
    }
    let without = source;
    for (const item of validMoving.slice().sort((a, b) => b.start - a.start)) {
      without = `${without.slice(0, item.start)}${without.slice(item.end)}`;
      if (item.start < insertion) {
        insertion -= item.end - item.start;
      }
    }
    if (insertion < 0 || insertion > without.length) {
      return false;
    }
    const collapsed = collapseRunawayBlankLines(without, insertion);
    without = collapsed.text;
    insertion = collapsed.position;
    const before = without.slice(0, insertion).replace(/[ \t]+$/g, "");
    const after = without.slice(insertion).replace(/^[ \t]+/g, "");
    const joined = blocks.join("\n\n");
    const separatorBefore = before && !before.endsWith("\n\n") ? before.endsWith("\n") ? "\n" : "\n\n" : "";
    const separatorAfter = after && !after.startsWith("\n\n") ? after.startsWith("\n") ? "\n" : "\n\n" : "";
    const nextSource = `${before}${separatorBefore}${joined}${separatorAfter}${after}`;
    if (!preservesAllMovedMarkdownBlocks(nextSource, blocks)) {
      return false;
    }
    if (nextSource === source) {
      return { changed: false, before: source, after: source };
    }
    await this.app.vault.modify(file2, nextSource);
    return { changed: true, before: source, after: nextSource };
  }
};
var PreviewDrawingController = class {
  constructor(plugin, previewEl, view, file2, options = {}) {
    this.plugin = plugin;
    this.previewEl = previewEl;
    this.view = view;
    this.file = file2;
    this.allowTextEdit = options.allowTextEdit !== false;
    this.surfaceType = options.surfaceType || "preview";
    this.embeddedSurface = Boolean(options.embeddedSurface);
    this.workspaceSurface = Boolean(options.workspaceSurface);
    this.workspaceViewType = String(options.workspaceViewType || "");
    this.registeredSurface = Boolean(options.registeredSurface);
    this.registeredSurfaceOwner = String(options.registeredSurfaceOwner || "");
    this.registeredSurfaceId = String(options.registeredSurfaceId || "");
    this.registeredSurfaceKey = String(options.registeredSurfaceKey || "");
    this.registeredSurfaceSource = options.registeredSurfaceSource || null;
    this.registeredSurfaceCapabilities = options.registeredSurfaceCapabilities || null;
    this.registeredSurfaceViewport = options.registeredSurfaceViewport || null;
    this.destroyed = false;
    this.plugin.liveControllers?.add(this);
    this.runtimeSettings = sanitizeSettings(this.plugin?.noteDrawSettings || {});
    this.active = this.plugin.controllerActivationState(this);
    this.drawingData = {
      version: 3,
      sourcePath: file2.path,
      strokes: [],
      markdownBlocks: [],
      elementGroups: [],
      updatedAt: null
    };
    this.currentStroke = null;
    this.laserStrokes = [];
    this.laserFadeTimer = null;
    this.currentEditor = null;
    this.currentEditorFile = null;
    this.currentEditorEmbedded = false;
    this.currentEditorLayoutLock = null;
    this.textCommitBarrier = createAsyncCommitBarrier((error) => {
      console.error(`[${PLUGIN_ID}] Failed to settle an edit before history navigation`, error);
    });
    this.routedPointerController = null;
    this.routedPointerHost = null;
    this.currentTextRange = null;
    this.formatToolbar = null;
    this.formatToolbarManualPosition = null;
    this.formatToolbarDrag = null;
    this.toolbarManualPosition = null;
    this.toolbarDrag = null;
    this.formatColorInput = null;
    this.formatHighlightInput = null;
    this.formatSizeSelect = null;
    this.sourceFormatToolbarCleanup = null;
    this.sourceFormatToolbarRetryTimer = null;
    this.brushMode = this.runtimeSettings.lastBrushMode;
    this.brushVariants = {
      [BRUSH_PEN]: this.runtimeSettings.lastPenVariant,
      [BRUSH_WATERCOLOR]: this.runtimeSettings.lastWatercolorVariant
    };
    this.brushSettings = {
      [BRUSH_PEN]: {
        color: this.runtimeSettings.defaultPenColor,
        width: this.runtimeSettings.defaultPenWidth,
        opacity: this.runtimeSettings.defaultPenOpacity,
        count: 1
      },
      [BRUSH_WATERCOLOR]: {
        color: this.runtimeSettings.defaultWatercolorColor,
        width: this.runtimeSettings.defaultWatercolorWidth,
        opacity: this.runtimeSettings.defaultWatercolorOpacity,
        count: 1
      }
    };
    this.brushVariantSettings = createBrushVariantSettings(this.runtimeSettings.brushVariantSettings, this.brushSettings);
    this.selectedStrokeIndex = -1;
    this.selectedStrokeIndexes = /* @__PURE__ */ new Set();
    this.selectedMarkdownBlockIds = /* @__PURE__ */ new Set();
    this.markdownSelectionActivation = null;
    this.directMarkdownPointerId = null;
    this.directMarkdownPointerElement = null;
    this.directMarkdownTaskClickUntil = 0;
    this.enteredElementGroupIds = /* @__PURE__ */ new Set();
    this.selectionFrameSnapshot = null;
    this.selectionFrameAwaitingMarkdownSync = null;
    this.embedRepairTimer = null;
    this.markdownMutationSyncTimer = null;
    this.deletedTextHighlightPruneTimer = null;
    this.markdownBlockElements = /* @__PURE__ */ new Map();
    this.applySettings();
    this.penColor = this.brushSettings[BRUSH_PEN].color;
    this.penWidth = this.brushSettings[BRUSH_PEN].width;
    this.penOpacity = this.brushSettings[BRUSH_PEN].opacity;
    this.penCount = this.brushSettings[BRUSH_PEN].count;
    this.toolMode = this.surfaceType === "source" && this.runtimeSettings.defaultSourceEditMarkdown ? TOOL_EDIT_MD : normalizeToolMode(this.runtimeSettings.lastToolMode);
    if (this.workspaceSurface && this.toolMode === TOOL_EDIT_MD) {
      this.toolMode = TOOL_DRAW;
    }
    this.pointerDown = false;
    this.startedOnText = false;
    this.pointerStartPoint = null;
    this.pointerStartClient = null;
    this.pointerStartEditable = null;
    this.pointerStartSourceText = false;
    this.activePointerId = null;
    this.pendingSelectionTap = null;
    this.textHighlightLineRects = [];
    this.textHighlightTarget = null;
    this.connectorGesture = null;
    this.connectorReconnect = null;
    this.touchPointers = /* @__PURE__ */ new Map();
    this.multiTouchScrolling = false;
    this.multiTouchGestureMode = "undecided";
    this.multiTouchInitialDistance = null;
    this.multiTouchLastCenter = null;
    this.multiTouchLastDistance = null;
    this.multiTouchPendingCenter = null;
    this.multiTouchPendingDistance = null;
    this.multiTouchFrameId = null;
    this.multiTouchPinching = false;
    this.suppressTouchDrawing = false;
    this.draggingStroke = false;
    this.dragStrokeStartPoint = null;
    this.dragStrokePointerGeometry = null;
    this.dragStrokeOriginalPoints = null;
    this.dragNoteFlowConnectorOriginalStates = /* @__PURE__ */ new Map();
    this.dragStrokeOriginalPointBounds = null;
    this.dragStrokeIndexes = [];
    this.dragShouldSnap = false;
    this.dragStrokeOriginalNoteFlows = null;
    this.pendingDraggedNoteFlowIndexes = /* @__PURE__ */ new Set();
    this.dragNoteFlowLivePreviewOriginals = /* @__PURE__ */ new Map();
    this.dragNoteFlowRawPreviewPoints = null;
    this.dragNoteFlowRawPreviewNoteFlows = null;
    this.dragNoteFlowPreviewElementIds = /* @__PURE__ */ new Set();
    this.dragNoteFlowPlacementClientDelta = null;
    this.dragNoteFlowDomPreview = null;
    this.dragStrokeOriginalBounds = null;
    this.dragStrokeOriginalBounds = null;
    this.dragStrokeMoved = false;
    this.dragStrokeHitIndex = -1;
    this.dragStrokePreserveSelection = false;
    this.dragMarkdownOriginalElements = null;
    this.dragMarkdownSourcePromise = null;
    this.dragMarkdownSourceIndexPromise = null;
    this.dragNoteFlowOriginalBounds = null;
    this.dragMarkdownDropTarget = null;
    this.dragMarkdownDropSide = null;
    this.dragMarkdownLastValidDrop = null;
    this.dragMarkdownDropFrameId = null;
    this.dragMarkdownDropClientX = null;
    this.dragMarkdownDropClientY = null;
    this.dragMarkdownDomPreviewKey = "";
    this.dragMarkdownOriginalClientBounds = null;
    this.dragMarkdownClientDeltaX = 0;
    this.dragMarkdownClientDeltaY = 0;
    this.dragContentLaneRect = null;
    this.dragMarkdownTextCommit = null;
    this.dragElementGroupBounds = null;
    this.dragDropGeometry = null;
    this.dragSnapOtherBounds = null;
    this.dragSnapOtherXValues = null;
    this.dragSnapOtherYValues = null;
    this.dragMovedElementIds = null;
    this.dragPendingConnectorIds = null;
    this.dragConnectorCandidates = null;
    this.dragConnectorById = null;
    this.dragHasBoxBackground = false;
    this.dragLastPointerEvent = null;
    this.dragLastPointerKey = "";
    this.dragNoteFlowPeerAnimationTimers = /* @__PURE__ */ new Map();
    this.dragNoteFlowPeerAnimatingElements = /* @__PURE__ */ new Set();
    this.dragNoteFlowPeerAnimationFrameId = null;
    this.dragNoteFlowStrokeAnimation = null;
    this.dragNoteFlowStrokeAnimationFrameId = null;
    this.dragNoteFlowDropFrameId = null;
    this.dragNoteFlowDropClientX = null;
    this.dragNoteFlowDropClientY = null;
    this.dragNoteFlowPlacement = null;
    this.dragNoteFlowTargetElement = null;
    this.dragNoteFlowReservationStyles = null;
    this.dragNoteFlowRebuildKey = "";
    this.dragNoteFlowRebuildSince = 0;
    this.dragNoteFlowLastRebuildAt = 0;
    this.dragNoteFlowLastAppliedPlacement = null;
    this.previewPrimaryPress = null;
    this.pendingMarkdownIdentityRefresh = null;
    this.allowMarkdownPresentationDuringDrag = false;
    this.noteFlowDropIndicator = null;
    this.resizingSelection = false;
    this.resizeSelectionHandle = null;
    this.resizeSelectionStartPoint = null;
    this.resizeSelectionStartClient = null;
    this.resizeSelectionOriginalBounds = null;
    this.resizeSelectionPreviewBounds = null;
    this.resizeSelectionOriginalStrokes = null;
    this.resizeSelectionOriginalMarkdownBlocks = null;
    this.resizeSelectionPointerGeometry = null;
    this.resizeSelectionClientBounds = null;
    this.resizeSelectionMoved = false;
    this.resizeNoteFlowFrameId = null;
    this.selectingStrokes = false;
    this.selectionStartPoint = null;
    this.selectionCurrentPoint = null;
    this.didMove = false;
    this.redoStack = [];
    this.drawingsVisible = true;
    this.buttonLongPressed = false;
    this.buttonLongPressTimer = null;
    this.suppressNextButtonClick = false;
    this.paletteOpen = false;
    this.brushPanelOpen = false;
    this.brushPanelMode = BRUSH_PEN;
    this.textPanelOpen = false;
    this.selectionMenuOpen = false;
    this.selectionLongPressTimer = null;
    this.selectionLongPressState = null;
    this.selectionLongPressConsumedPointerId = null;
    this.floatingControlsHost = null;
    this.floatingControlsInBody = false;
    this.textPreset = this.runtimeSettings.lastTextPreset;
    this.readingZoom = this.runtimeSettings.lastReadingZoom;
    this.inheritedReadingLayoutZoom = 1;
    this.readingZoomLayoutMode = this.surfaceType === "source";
    this.readingZoomTarget = null;
    this.readingZoomBaseTarget = null;
    this.readingZoomBaseOrigin = null;
    this.readingZoomExtent = null;
    this.readingZoomStage = null;
    this.readingZoomStageParent = null;
    this.readingZoomStageNextSibling = null;
    this.readingZoomStyleState = /* @__PURE__ */ new Map();
    this.readingVirtualSectionFrameId = null;
    this.readingVirtualSectionsManaged = false;
    this.readingVirtualSectionSignature = "";
    this.readingVirtualStyleState = /* @__PURE__ */ new Map();
    this.readingAsyncEmbedFilePath = "";
    this.readingLogicalSizerHeight = 0;
    this.readingZoomInteractionUntil = 0;
    this.readingZoomSettleTimer = null;
    this.initialReadingLayoutPrepared = false;
    this.initialReadingLayoutSettlement = null;
    this.initialReadingLayoutSettled = false;
    this.initialReadingSurfaceSettlement = null;
    this.initialReadingCommittedSignature = "";
    this.readingSurfaceRepairFrameId = null;
    this.surfaceStateGeneration = 0;
    this.readingTouchGuard = createReadingTouchGuardState();
    this.pendingEmbedTool = null;
    this.pendingMindMapFile = null;
    this.pendingMindMapOptions = null;
    this.noteFlowFrameId = null;
    this.noteFlowFallbackTimer = null;
    this.frozenNoteFlowRestoreFrameId = null;
    this.noteFlowOperationPending = false;
    this.noteFlowPersistencePending = false;
    this.noteFlowResizeTimer = null;
    this.noteFlowSuppressResizeUntil = 0;
    this.noteFlowSettleEpoch = "";
    this.noteFlowSettlePasses = 0;
    this.noteFlowAnchorRepairReady = false;
    this.noteFlowAnchorRepairComplete = false;
    this.noteFlowAnchorRepairTimer = null;
    this.noteFlowActivationRepairAttempted = false;
    this.noteFlowReadingRepairAttempted = false;
    this.noteFlowReadingRepairPending = false;
    this.noteFlowLayoutIncomplete = false;
    this.noteFlowStyledElements = /* @__PURE__ */ new Map();
    this.noteFlowLineSpacers = /* @__PURE__ */ new Map();
    this.noteFlowSettledRowExtents = /* @__PURE__ */ new Map();
    this.noteFlowBlockSpacers = /* @__PURE__ */ new Map();
    this.noteFlowMarkdownAnnotationComplete = false;
    this.noteFlowCandidateCache = null;
    this.noteFlowAvoidanceAnchors = /* @__PURE__ */ new Map();
    this.noteFlowLayoutSignature = "";
    this.frozenNoteFlowPreparation = null;
    this.frozenNoteFlowRetryFrameId = null;
    this.frozenNoteFlowRestoreTimer = null;
    this.frozenNoteFlowPostMeasurePending = false;
    this.readingBottomSpacer = null;
    this.readingBottomSpacerHeight = 0;
    this.readingBottomExtentResizeAt = 0;
    const sharedToolbarState = this.plugin.controllerToolbarState(this);
    if (sharedToolbarState) {
      this.brushMode = [BRUSH_PEN, BRUSH_WATERCOLOR].includes(sharedToolbarState.brushMode) ? sharedToolbarState.brushMode : this.brushMode;
      for (const mode of [BRUSH_PEN, BRUSH_WATERCOLOR]) {
        this.brushVariants[mode] = normalizeBrushVariant(mode, sharedToolbarState.brushVariants?.[mode]);
      }
      const sharedToolMode = sharedToolbarState.toolMode || this.toolMode;
      this.toolMode = this.surfaceType === "source" && this.runtimeSettings.defaultSourceEditMarkdown ? TOOL_EDIT_MD : this.surfaceType === "preview" && sharedToolMode === TOOL_EDIT_MD ? TOOL_SELECT : sharedToolMode;
      if (this.surfaceType === "source") {
        this.readingZoom = clamp10(
          Number(sharedToolbarState.sourceLayoutZoom ?? sharedToolbarState.readingZoom) || this.readingZoom || 1,
          MIN_READING_ZOOM,
          MAX_READING_ZOOM
        );
        this.readingZoomLayoutMode = true;
      } else {
        const legacyLayoutZoom = sharedToolbarState.readingZoomLayoutMode === true ? sharedToolbarState.readingZoom : 1;
        this.inheritedReadingLayoutZoom = clamp10(
          Number(sharedToolbarState.sourceLayoutZoom ?? legacyLayoutZoom) || 1,
          MIN_READING_ZOOM,
          MAX_READING_ZOOM
        );
        this.readingZoom = clamp10(
          Number(sharedToolbarState.sourceLayoutZoom !== void 0 && sharedToolbarState.readingZoomLayoutMode === true ? this.readingZoom : sharedToolbarState.readingZoom) || this.readingZoom || 1,
          MIN_READING_ZOOM,
          MAX_READING_ZOOM
        );
        this.readingZoomLayoutMode = false;
      }
      this.paletteOpen = Boolean(sharedToolbarState.paletteOpen);
      this.brushPanelOpen = Boolean(sharedToolbarState.brushPanelOpen);
      this.brushPanelMode = [BRUSH_PEN, BRUSH_WATERCOLOR].includes(sharedToolbarState.brushPanelMode) ? sharedToolbarState.brushPanelMode : this.brushPanelMode;
      this.textPanelOpen = Boolean(sharedToolbarState.textPanelOpen);
      this.textPreset = sharedToolbarState.textPreset || this.textPreset;
      for (const mode of [BRUSH_PEN, BRUSH_WATERCOLOR]) {
        if (sharedToolbarState.brushSettings?.[mode]) {
          this.brushSettings[mode] = { ...this.brushSettings[mode], ...sharedToolbarState.brushSettings[mode] };
        }
      }
      this.syncCurrentBrushFields();
    }
    this.lastTextTap = null;
    this.underlayCanvas = activeDocument.createElement("canvas");
    this.underlayCanvas.width = 1;
    this.underlayCanvas.height = 1;
    this.underlayCtx = null;
    this.underlayCacheDirty = true;
    this.underlayEmbedLayer = null;
    this.embedLayer = null;
    this.embedNodes = /* @__PURE__ */ new Map();
    this.embedRenderTokens = /* @__PURE__ */ new Map();
    this.embedGeometryTokens = /* @__PURE__ */ new Map();
    this.canvasImageCache = /* @__PURE__ */ new Map();
    this.hiddenFileInput = null;
    this.canvasCssWidth = 1;
    this.canvasCssHeight = 1;
    this.canvasWindowTop = 0;
    this.canvasRenderHeight = 1;
    this.canvasBackingScale = 1;
    this.responsiveLayoutSignature = "";
    this.responsivePointsInitialized = false;
    this.responsiveLayoutContext = null;
    this.renderFrameId = null;
    this.pendingDomRender = false;
    this.pendingInteractionRender = false;
    this.resizeFrameId = null;
    this.resizeFallbackTimer = null;
    this.resizeNeedsLayout = false;
    this.resizeNeedsMeasure = false;
    this.resizePreserveNoteFlowAbsolute = false;
    this.resizePreserveAbsolutePlacement = false;
    this.responsiveProjectionPending = null;
    this.responsiveProjectionSettleTimer = null;
    this.responsiveProjectionPreserveNoteFlowAbsolute = false;
    this.scrollSettleTimer = null;
    this.noteFlowScrollStabilityTimer = null;
    this.positionFrameId = null;
    this.layoutRefreshGeneration = 0;
    this.layoutRefreshFrameId = null;
    this.layoutRefreshTimerIds = /* @__PURE__ */ new Set();
    this.markdownAnnotationTimer = null;
    this.markdownAnnotationNeedsLayout = false;
    this.markdownAnnotationForce = false;
    this.lastScrollAt = 0;
    this.markdownRenderObserver = null;
    this.markdownObserverResumeTimer = null;
    this.markdownMutationWindowStartedAt = 0;
    this.markdownMutationBurstCount = 0;
    this.markdownObserverPausedUntil = 0;
    this.markdownObserverPauseStreak = 0;
    this.staticCanvas = activeDocument.createElement("canvas");
    this.staticCanvas.width = 1;
    this.staticCanvas.height = 1;
    this.staticCtx = null;
    this.staticCacheDirty = true;
    this.scrollContainer = null;
    this.scrollEventTarget = null;
    this.layoutMeasureEl = null;
    this.lastObservedPreviewWidth = 0;
    this.drawingsLoaded = false;
    this.loadingDrawings = null;
    this.externalDrawingRefreshPending = false;
    this.drawingLoadGeneration = 0;
    this.markdownSourceRevisionChanged = false;
    this.onPointerDown = this.onPointerDown.bind(this);
    this.onPointerMove = this.onPointerMove.bind(this);
    this.onPointerUp = this.onPointerUp.bind(this);
    this.onCanvasContextMenu = this.onCanvasContextMenu.bind(this);
    this.onPreviewContextMenu = this.onPreviewContextMenu.bind(this);
    this.onCanvasDoubleClick = this.onCanvasDoubleClick.bind(this);
    this.onPreviewDoubleClick = this.onPreviewDoubleClick.bind(this);
    this.onPreviewSecondPress = this.onPreviewSecondPress.bind(this);
    this.onPreviewSecondPointerDown = this.onPreviewSecondPointerDown.bind(this);
    this.onWheel = this.onWheel.bind(this);
    this.onResize = this.onResize.bind(this);
    this.onScroll = this.onScroll.bind(this);
    this.onReadingTouchPointerDown = this.onReadingTouchPointerDown.bind(this);
    this.onReadingTouchPointerMove = this.onReadingTouchPointerMove.bind(this);
    this.onReadingTouchPointerFinish = this.onReadingTouchPointerFinish.bind(this);
    this.onReadingClick = this.onReadingClick.bind(this);
    this.onHeadingCollapseIndicatorClick = this.onHeadingCollapseIndicatorClick.bind(this);
    this.onReadingVirtualScrollCapture = this.onReadingVirtualScrollCapture.bind(this);
    this.onButtonClick = this.onButtonClick.bind(this);
    this.onButtonPointerDown = this.onButtonPointerDown.bind(this);
    this.onButtonPointerUp = this.onButtonPointerUp.bind(this);
    this.onDocumentPointerDown = this.onDocumentPointerDown.bind(this);
    this.onDocumentPointerMove = this.onDocumentPointerMove.bind(this);
    this.onDocumentPointerFinish = this.onDocumentPointerFinish.bind(this);
    this.onDocumentSelectionChange = this.onDocumentSelectionChange.bind(this);
    this.onDocumentKeyDown = this.onDocumentKeyDown.bind(this);
    this.onToolbarPointerDown = this.onToolbarPointerDown.bind(this);
    this.onToolbarDragMove = this.onToolbarDragMove.bind(this);
    this.onToolbarDragEnd = this.onToolbarDragEnd.bind(this);
    this.onFormatToolbarDragMove = this.onFormatToolbarDragMove.bind(this);
    this.onFormatToolbarDragEnd = this.onFormatToolbarDragEnd.bind(this);
  }
  ensureReadingZoomStage(target = findResponsiveContentElement(this.previewEl, this.surfaceType)) {
    if (!this.usesVisualReadingZoom() || !target?.isConnected) {
      return null;
    }
    if (!this.readingZoomStage?.isConnected) {
      const stage = this.previewEl.ownerDocument.createElement("div");
      stage.classList.add("notedraw-reading-stage");
      this.readingZoomStageParent = target.parentElement || this.previewEl;
      this.readingZoomStageNextSibling = target.nextSibling;
      this.readingZoomStageParent.insertBefore(stage, target);
      this.readingZoomStage = stage;
    }
    if (target.parentElement !== this.readingZoomStage) {
      this.readingZoomStage.appendChild(target);
    }
    for (const element of [this.underlayEmbedLayer, this.underlayCanvas, this.staticCanvas, this.canvas, this.embedLayer]) {
      if (element && element.parentElement !== this.readingZoomStage) {
        this.readingZoomStage.appendChild(element);
      }
    }
    return this.readingZoomStage;
  }
  restoreReadingZoomStage() {
    const stage = this.readingZoomStage;
    if (!stage) {
      return;
    }
    const target = this.readingZoomTarget?.parentElement === stage ? this.readingZoomTarget : stage.querySelector?.(".markdown-preview-sizer");
    const parent = this.readingZoomStageParent?.isConnected ? this.readingZoomStageParent : this.previewEl;
    if (target && parent?.isConnected) {
      const next = this.readingZoomStageNextSibling;
      if (next?.parentElement === parent) {
        parent.insertBefore(target, next);
      } else {
        parent.appendChild(target);
      }
    }
    stage.remove();
    this.readingZoomStage = null;
    this.readingZoomStageParent = null;
    this.readingZoomStageNextSibling = null;
  }
  async mount() {
    if (this.destroyed) {
      return;
    }
    cleanupDrawingUi(this.previewEl);
    this.previewEl._noteDrawController = this;
    this.previewEl.addClass("notedraw-shell");
    this.previewEl.addClass("is-notedraw-responsive-layout");
    this.previewEl.toggleClass("is-notedraw-source-shell", this.surfaceType === "source");
    this.previewEl.toggleClass("is-notedraw-webview-shell", this.surfaceType === "webview");
    this.previewEl.toggleClass("is-notedraw-embedded-shell", this.embeddedSurface);
    this.previewEl.toggleClass("is-notedraw-workspace-shell", this.workspaceSurface);
    this.previewEl.toggleClass("is-notedraw-registered-shell", this.registeredSurface);
    this.floatingControlsInBody = shouldUseBodyFloatingControls(this.previewEl, this.surfaceType);
    this.floatingControlsHost = this.floatingControlsInBody ? activeDocument.body : this.previewEl;
    this.previewEl.toggleClass("has-notedraw-body-controls", this.floatingControlsInBody);
    this.button = this.createHeaderButton();
    this.toolbar = createNoteDrawControlElement(this.floatingControlsHost, "notedraw-toolbar");
    this.toolbar._noteDrawController = this;
    this.toolbar.addEventListener("pointerdown", this.onToolbarPointerDown);
    this.selectButton = this.toolbar.createEl("button", {
      attr: { type: "button", title: this.plugin.t("selectDrawings") }
    });
    (0, import_obsidian.setIcon)(this.selectButton, "mouse-pointer-2");
    this.selectButton.addEventListener("click", () => this.toggleSelectMode());
    this.editMarkdownButton = this.surfaceType === "source" ? this.toolbar.createEl("button", {
      attr: { type: "button", title: this.plugin.t("editMarkdownTool") }
    }) : null;
    if (this.editMarkdownButton) {
      (0, import_obsidian.setIcon)(this.editMarkdownButton, "file-pen-line");
      this.editMarkdownButton.addEventListener("click", () => this.setEditMarkdownMode());
    }
    this.penButton = this.toolbar.createEl("button", {
      attr: { type: "button", title: this.plugin.t("pen") }
    });
    (0, import_obsidian.setIcon)(this.penButton, "pen-line");
    bindNoteDrawControlTap(this.penButton, (event) => this.onBrushButtonClick(BRUSH_PEN, event));
    this.watercolorButton = this.toolbar.createEl("button", {
      attr: { type: "button", title: this.plugin.t("watercolorBrush") }
    });
    (0, import_obsidian.setIcon)(this.watercolorButton, "paintbrush");
    bindNoteDrawControlTap(this.watercolorButton, (event) => this.onBrushButtonClick(BRUSH_WATERCOLOR, event));
    this.textButton = this.toolbar.createEl("button", {
      attr: { type: "button", title: this.plugin.t("floatingText") }
    });
    (0, import_obsidian.setIcon)(this.textButton, "type");
    bindNoteDrawControlTap(this.textButton, () => {
      this.setTextMode();
      this.setTextPanelOpen(true);
      this.syncTextPanelButtons();
    });
    this.undoButton = this.toolbar.createEl("button", {
      attr: { type: "button", title: this.plugin.t("undoLastDrawing") }
    });
    (0, import_obsidian.setIcon)(this.undoButton, "undo-2");
    this.undoButton.addEventListener("pointerdown", (event) => event.preventDefault());
    this.undoButton.addEventListener("click", () => this.undoLastStroke());
    this.redoButton = this.toolbar.createEl("button", {
      attr: { type: "button", title: this.plugin.t("redoDrawing") }
    });
    (0, import_obsidian.setIcon)(this.redoButton, "redo-2");
    this.redoButton.addEventListener("pointerdown", (event) => event.preventDefault());
    this.redoButton.addEventListener("click", () => this.redoLastStroke());
    this.deleteButton = this.toolbar.createEl("button", {
      attr: { type: "button", title: this.plugin.t("deleteSelectedDrawing") }
    });
    (0, import_obsidian.setIcon)(this.deleteButton, "trash-2");
    this.deleteButton.addEventListener("click", () => this.deleteSelectedStroke());
    this.paletteButton = this.toolbar.createEl("button", {
      attr: { type: "button", title: this.plugin.t("penSettings") }
    });
    (0, import_obsidian.setIcon)(this.paletteButton, "palette");
    bindNoteDrawControlTap(this.paletteButton, () => {
      if (this.toolMode === TOOL_EDIT_MD || this.toolMode === TOOL_SELECT && !this.hasHybridSelection()) {
        this.setPaletteOpen(false);
        return;
      }
      this.togglePalettePanel();
    });
    this.palettePanel = createNoteDrawControlElement(this.floatingControlsHost, "notedraw-palette-panel");
    this.createColorPalette();
    this.brushPanel = createNoteDrawControlElement(this.floatingControlsHost, "notedraw-brush-panel");
    this.createBrushPanel();
    this.textPanel = createNoteDrawControlElement(this.floatingControlsHost, "notedraw-text-panel");
    this.createTextPanel();
    this.selectionMenu = createNoteDrawControlElement(this.floatingControlsHost, "notedraw-selection-menu");
    this.createSelectionMenu();
    if (this.surfaceType === "source" || this.allowTextEdit && this.surfaceType !== "webview") {
      this.createFormatToolbar();
    }
    this.colorInput = this.palettePanel.createEl("input", {
      cls: "notedraw-advanced-color",
      attr: {
        type: "color",
        value: this.penColor,
        title: this.plugin.t("advancedColor"),
        "aria-label": this.plugin.t("advancedColor")
      }
    });
    this.colorInput.addEventListener("input", () => {
      this.setCurrentBrushColor(this.colorInput.value);
    });
    this.markdownBackgroundInput = this.palettePanel.createEl("input", {
      cls: "notedraw-advanced-color notedraw-md-background-color",
      attr: {
        type: "color",
        value: "#ffffff",
        title: this.plugin.t("blockBackground"),
        "aria-label": this.plugin.t("blockBackground")
      }
    });
    this.markdownBackgroundInput.addEventListener("input", () => {
      this.applyBackgroundToSelectedMarkdownBlocks(this.markdownBackgroundInput.value);
    });
    this.clearMarkdownBackgroundButton = this.palettePanel.createEl("button", {
      cls: "notedraw-clear-md-background",
      attr: { type: "button", title: this.plugin.t("clearBlockBackground"), "aria-label": this.plugin.t("clearBlockBackground") }
    });
    (0, import_obsidian.setIcon)(this.clearMarkdownBackgroundButton, "eraser");
    bindNoteDrawControlTap(this.clearMarkdownBackgroundButton, () => this.applyBackgroundToSelectedMarkdownBlocks(""));
    this.hiddenFileInput = this.floatingControlsHost.createEl("input", {
      cls: "notedraw-file-input",
      attr: {
        type: "file",
        accept: filePickerAcceptForPreset(this.textPreset)
      }
    });
    this.hiddenFileInput.addEventListener("change", () => {
      const file2 = this.hiddenFileInput?.files?.[0] || null;
      const pending = this.pendingEmbedTool;
      this.hiddenFileInput.value = "";
      if (!file2 || !pending?.point) {
        this.pendingEmbedTool = null;
        return;
      }
      this.insertImportedAsset(file2, pending.point).catch((error) => {
        console.error(`[${PLUGIN_ID}] Failed to import asset`, error);
        new import_obsidian.Notice(this.plugin.t("failedImportFile"));
      });
    });
    this.widthInput = this.createPaletteInput("circle", "width", {
      type: "range",
      value: String(brushWidthToPaletteSlider(this.penWidth)),
      min: "0",
      max: "1000",
      step: "1",
      title: this.plugin.t("elementSize")
    });
    this.widthInput.addEventListener("input", () => {
      const width = paletteSliderToBrushWidth(this.widthInput.value);
      this.currentBrushSettings().width = width;
      this.applySizeToSelectedElements(width);
      this.syncCurrentBrushFields();
      this.updateToolButtons();
      this.persistCurrentBrushSettings();
      this.syncSharedToolbarState();
    });
    this.opacityInput = this.createPaletteInput("droplets", "opacity", {
      type: "range",
      value: String(opacityToPaletteSlider(this.penOpacity)),
      min: "0",
      max: "1000",
      step: "1",
      title: this.plugin.t("elementOpacity")
    });
    this.opacityInput.addEventListener("input", () => {
      const opacity = paletteSliderToOpacity(this.opacityInput.value);
      this.currentBrushSettings().opacity = opacity;
      this.applyOpacityToSelectedElements(opacity);
      this.syncCurrentBrushFields();
      this.updateToolButtons();
      this.persistCurrentBrushSettings();
      this.syncSharedToolbarState();
    });
    this.readingZoomExtent = this.previewEl.createDiv({ cls: "notedraw-reading-zoom-extent" });
    const initialZoomTarget = findResponsiveContentElement(this.previewEl, this.surfaceType);
    this.readingZoomTarget = initialZoomTarget;
    const drawingHost = this.ensureReadingZoomStage(initialZoomTarget) || this.previewEl;
    this.underlayEmbedLayer = drawingHost.createDiv({ cls: "notedraw-underlay-embed-layer" });
    this.embedLayer = drawingHost.createDiv({ cls: "notedraw-embed-layer" });
    this.underlayCanvas.classList.add("notedraw-underlay-canvas");
    drawingHost.appendChild(this.underlayCanvas);
    this.staticCanvas.classList.add("notedraw-static-canvas");
    drawingHost.appendChild(this.staticCanvas);
    this.canvas = drawingHost.createEl("canvas", { cls: "notedraw-canvas" });
    this.canvas.width = 1;
    this.canvas.height = 1;
    this.canvas.addEventListener("pointerdown", this.onPointerDown);
    this.canvas.addEventListener("pointermove", this.onPointerMove);
    this.canvas.addEventListener("pointerup", this.onPointerUp);
    this.canvas.addEventListener("pointercancel", this.onPointerUp);
    this.canvas.addEventListener("lostpointercapture", this.onPointerUp);
    this.canvas.addEventListener("dblclick", this.onCanvasDoubleClick);
    this.canvas.addEventListener("contextmenu", this.onCanvasContextMenu);
    this.canvas.addEventListener("wheel", this.onWheel, { passive: false });
    this.previewEl.addEventListener("contextmenu", this.onPreviewContextMenu, true);
    this.previewEl.addEventListener("dblclick", this.onPreviewDoubleClick, true);
    this.previewEl.addEventListener("click", this.onHeadingCollapseIndicatorClick, true);
    this.previewGestureWindow = activeDocument.defaultView || window;
    this.previewGestureWindow.addEventListener("mousedown", this.onPreviewSecondPress, true);
    this.previewGestureWindow.addEventListener("pointerdown", this.onPreviewSecondPointerDown, true);
    this.previewGestureWindow.addEventListener("dblclick", this.onPreviewDoubleClick, true);
    if (this.usesVisualReadingZoom()) {
      this.previewEl.addEventListener("pointerdown", this.onReadingTouchPointerDown, true);
      this.previewEl.addEventListener("pointermove", this.onReadingTouchPointerMove, true);
      this.previewEl.addEventListener("pointerup", this.onReadingTouchPointerFinish, true);
      this.previewEl.addEventListener("pointercancel", this.onReadingTouchPointerFinish, true);
      this.previewEl.addEventListener("click", this.onReadingClick, true);
      this.previewEl.addEventListener("scroll", this.onReadingVirtualScrollCapture, { capture: true, passive: true });
    }
    window.addEventListener("resize", this.onResize);
    window.visualViewport?.addEventListener("resize", this.onResize);
    window.visualViewport?.addEventListener("scroll", this.onResize);
    activeDocument.addEventListener("pointerdown", this.onDocumentPointerDown, true);
    activeDocument.addEventListener("pointermove", this.onDocumentPointerMove, true);
    activeDocument.addEventListener("pointerup", this.onDocumentPointerFinish, true);
    activeDocument.addEventListener("pointercancel", this.onDocumentPointerFinish, true);
    activeDocument.addEventListener("selectionchange", this.onDocumentSelectionChange);
    activeDocument.addEventListener("keydown", this.onDocumentKeyDown, true);
    this.bindSourceFormatToolbarEvents();
    if (typeof ResizeObserver !== "undefined") {
      this.resizeObserver = new ResizeObserver(this.onResize);
      this.resizeObserver.observe(this.previewEl);
      this.layoutMeasureEl = findLayoutMeasureElement(this.previewEl);
      if (this.layoutMeasureEl && this.layoutMeasureEl !== this.previewEl) {
        this.resizeObserver.observe(this.layoutMeasureEl);
      }
    }
    this.refreshScrollContainer();
    this.repairConnectedReadingSections();
    await this.prepareInitialReadingLayout();
    if (this.destroyed || !this.previewEl?.isConnected) {
      return;
    }
    if (typeof MutationObserver !== "undefined") {
      this.markdownRenderObserver = new MutationObserver((mutations) => {
        if (mutations.some((mutation) => isMarkdownContentMutation(mutation))) {
          const now = Date.now();
          if (now < this.markdownObserverPausedUntil) {
            return;
          }
          if (now - this.markdownMutationWindowStartedAt > 1e3) {
            this.markdownMutationWindowStartedAt = now;
            this.markdownMutationBurstCount = 0;
            if (now - this.markdownObserverPausedUntil > 3e4) {
              this.markdownObserverPauseStreak = 0;
            }
          }
          this.markdownMutationBurstCount += Math.max(1, mutations.length);
          if (this.markdownMutationBurstCount > 20) {
            this.markdownRenderObserver.disconnect();
            this.markdownMutationBurstCount = 0;
            this.markdownMutationWindowStartedAt = now;
            this.markdownObserverPauseStreak = Math.min(6, this.markdownObserverPauseStreak + 1);
            const pauseMs = Math.min(6e4, 4e3 * 2 ** (this.markdownObserverPauseStreak - 1));
            this.markdownObserverPausedUntil = now + pauseMs;
            if (this.markdownObserverResumeTimer !== null) {
              window.clearTimeout(this.markdownObserverResumeTimer);
            }
            this.markdownObserverResumeTimer = window.setTimeout(() => {
              this.markdownObserverResumeTimer = null;
              if (this.destroyed || !this.previewEl?.isConnected) {
                return;
              }
              if (Date.now() < this.markdownObserverPausedUntil) {
                this.markdownObserverResumeTimer = window.setTimeout(() => {
                  this.markdownObserverResumeTimer = null;
                  if (!this.destroyed && this.previewEl?.isConnected) {
                    this.markdownRenderObserver.takeRecords();
                    this.markdownRenderObserver.observe(this.previewEl, { subtree: true, childList: true });
                  }
                }, Math.max(100, this.markdownObserverPausedUntil - Date.now()));
                return;
              }
              this.markdownRenderObserver.takeRecords();
              this.markdownRenderObserver.observe(this.previewEl, { subtree: true, childList: true });
            }, pauseMs);
            return;
          }
          if (this.currentEditor && mutations.every((mutation) => mutation.target === this.currentEditor || this.currentEditor.contains?.(mutation.target))) {
            return;
          }
          this.noteFlowMarkdownAnnotationComplete = false;
          if (this.draggingStroke || this.resizingSelection || this.pointerDown) {
            return;
          }
          if (this.pendingMarkdownIdentityRefresh?.expiresAt > Date.now()) {
            this.restorePendingMarkdownIdentityPresentation(mutations);
          }
          this.scheduleMarkdownMutationSync();
        }
      });
      this.markdownRenderObserver.observe(this.previewEl, { subtree: true, childList: true });
    }
    this.updateToolButtons();
    this.syncPaletteInputs();
    this.refreshLocalizedLabels();
    this.applySharedToolbarState(this.plugin.controllerToolbarState(this));
    if (this.surfaceType === "source" && this.runtimeSettings.defaultSourceEditMarkdown) {
      this.toolMode = TOOL_EDIT_MD;
      this.paletteOpen = false;
      this.brushPanelOpen = false;
      this.textPanelOpen = false;
      this.previewEl.removeClass("is-select-mode");
      this.previewEl.removeClass("is-palette-open");
      this.previewEl.removeClass("is-brush-panel-open");
      this.previewEl.removeClass("is-text-panel-open");
      this.updateToolButtons();
      this.syncFloatingControlClasses();
      this.syncSharedToolbarState();
    }
    this.applyReadingZoom();
    this.applyActiveState(this.active);
    await this.ensureDrawingsLoaded();
    this.bindSourceFormatToolbarEvents();
    this.repairConnectedReadingSections();
    this.ensureReadingHeadingCollapseIndicators();
    this.scheduleEmbedRepair();
    this.plugin.emitApiEvent("surface-changed", { ...this.plugin.describeController(this), phase: "mounted" });
  }
  async prepareInitialReadingLayout() {
    if (this.surfaceType !== "preview" || this.initialReadingLayoutPrepared || !this.usesVisualReadingZoom() || !this.drawingsLoaded) {
      return this.initialReadingLayoutPrepared;
    }
    if (!(this.drawingData?.strokes?.length || this.drawingData?.markdownBlocks?.length)) {
      this.initialReadingLayoutPrepared = true;
      this.initialReadingLayoutSettled = true;
      return;
    }
    if (this.initialReadingLayoutSettlement) {
      return this.initialReadingLayoutSettlement;
    }
    const generation = this.drawingLoadGeneration;
    const requestFrame = () => new Promise((resolve) => {
      if (typeof window.requestAnimationFrame === "function") {
        let settled = false;
        let frameId = null;
        let fallbackTimer = null;
        const finish = () => {
          if (settled) {
            return;
          }
          settled = true;
          if (frameId !== null) {
            window.cancelAnimationFrame(frameId);
          }
          if (fallbackTimer !== null) {
            window.clearTimeout(fallbackTimer);
          }
          resolve();
        };
        frameId = window.requestAnimationFrame(finish);
        fallbackTimer = window.setTimeout(finish, 120);
      } else {
        window.setTimeout(resolve, 16);
      }
    });
    const preparation = (async () => {
      await waitForStableReadingLayout(() => {
        const sizer = rootPreviewSizer(this.previewEl);
        return captureInitialReadingLayout(this.previewEl, sizer, this.readingPreviewRenderer());
      }, {
        requestFrame,
        stableFrames: 2,
        maxFrames: 8,
        shouldAbort: () => this.destroyed || generation !== this.drawingLoadGeneration || !this.previewEl?.isConnected
      });
      if (this.destroyed || generation !== this.drawingLoadGeneration || !this.previewEl?.isConnected) {
        return false;
      }
      this.responsiveLayoutContext = null;
      this.responsiveLayoutSignature = "";
      this.initialReadingLayoutPrepared = true;
      return true;
    })();
    this.initialReadingLayoutSettlement = preparation;
    try {
      return await preparation;
    } finally {
      if (this.initialReadingLayoutSettlement === preparation) {
        this.initialReadingLayoutSettlement = null;
      }
    }
  }
  async settleInitialReadingSurface(generation = this.drawingLoadGeneration) {
    if (this.surfaceType !== "preview" || this.active || this.embeddedSurface || !this.drawingsLoaded || !this.previewEl?.isConnected) {
      return false;
    }
    if (generation !== this.drawingLoadGeneration || this.initialReadingLayoutSettled) {
      return false;
    }
    if (this.initialReadingSurfaceSettlement) {
      return this.initialReadingSurfaceSettlement;
    }
    const requestFrame = () => new Promise((resolve) => {
      if (typeof window.requestAnimationFrame === "function") {
        window.requestAnimationFrame(resolve);
      } else {
        window.setTimeout(resolve, 16);
      }
    });
    const settlement = (async () => {
      const surfaceGeneration = this.surfaceStateGeneration;
      await this.prepareInitialReadingLayout();
      if (this.destroyed || this.active || surfaceGeneration !== this.surfaceStateGeneration || generation !== this.drawingLoadGeneration || !this.previewEl?.isConnected) {
        return false;
      }
      const hasNoteFlow = this.hasNoteFlowElements();
      if (hasNoteFlow) {
        await this.prepareFrozenNoteFlowLayout();
        await this.prepareNoteFlowForReading();
      }
      if (this.destroyed || this.active || surfaceGeneration !== this.surfaceStateGeneration || generation !== this.drawingLoadGeneration || !this.previewEl?.isConnected) {
        return false;
      }
      this.repairConnectedReadingSections();
      this.syncMarkdownBlockPresentation();
      this.responsiveLayoutContext = null;
      this.responsiveLayoutSignature = "";
      this.responsivePointsInitialized = false;
      this.resizeCanvas({ layout: true, measure: true });
      await waitForStableReadingLayout(() => {
        const sizer = rootPreviewSizer(this.previewEl);
        return captureInitialReadingLayout(this.previewEl, sizer, this.readingPreviewRenderer());
      }, {
        requestFrame,
        stableFrames: 2,
        maxFrames: 6,
        shouldAbort: () => this.destroyed || this.active || surfaceGeneration !== this.surfaceStateGeneration || generation !== this.drawingLoadGeneration || !this.previewEl?.isConnected
      });
      if (this.destroyed || this.active || surfaceGeneration !== this.surfaceStateGeneration || generation !== this.drawingLoadGeneration || !this.previewEl?.isConnected) {
        return false;
      }
      this.responsiveLayoutContext = null;
      this.responsiveLayoutSignature = "";
      this.responsivePointsInitialized = false;
      this.resizeCanvas({ layout: true, measure: true });
      if (hasNoteFlow) {
        this.restoreFrozenNoteFlowLayout();
      }
      this.render();
      this.initialReadingCommittedSignature = this.readingSurfaceGeometrySignature();
      this.initialReadingLayoutSettled = true;
      return true;
    })();
    this.initialReadingSurfaceSettlement = settlement;
    try {
      return await settlement;
    } finally {
      if (this.initialReadingSurfaceSettlement === settlement) {
        this.initialReadingSurfaceSettlement = null;
      }
    }
  }
  readingSurfaceGeometrySignature() {
    if (this.surfaceType !== "preview" || !this.previewEl?.isConnected) {
      return "";
    }
    const sizer = rootPreviewSizer(this.previewEl);
    const metrics = captureInitialReadingLayout(this.previewEl, sizer, this.readingPreviewRenderer());
    const frame = measureResponsiveContentFrame(
      this.previewEl,
      this.surfaceType,
      Math.max(1, Number(this.previewEl.clientWidth) || Number(metrics.previewWidth) || 1),
      this.canvas,
      this.readingZoomScale()
    );
    return [
      initialReadingLayoutSignature(metrics),
      Math.round(Number(frame?.left) || 0),
      Math.round(Number(frame?.width) || 0),
      Math.round(this.readingZoomScale() * 1e3)
    ].join(":");
  }
  reconcileSettledReadingSurface() {
    if (this.destroyed || this.active || this.surfaceType !== "preview" || this.embeddedSurface || !this.drawingsLoaded || !this.initialReadingLayoutSettled || this.initialReadingSurfaceSettlement || !(this.drawingData?.strokes?.length || this.drawingData?.markdownBlocks?.length)) {
      return false;
    }
    const currentSignature = this.readingSurfaceGeometrySignature();
    if (!currentSignature || !this.initialReadingCommittedSignature || currentSignature === this.initialReadingCommittedSignature) {
      return false;
    }
    const generation = this.drawingLoadGeneration;
    this.initialReadingLayoutPrepared = false;
    this.initialReadingLayoutSettled = false;
    this.settleInitialReadingSurface(generation).catch((error) => {
      if (!this.destroyed && generation === this.drawingLoadGeneration) {
        console.error(`[${PLUGIN_ID}] Failed to settle changed reading layout`, error);
      }
    });
    return true;
  }
  queueReadingSurfaceSettlement() {
    if (this.destroyed || this.active || this.surfaceType !== "preview" || this.embeddedSurface || !this.drawingsLoaded || !this.previewEl?.isConnected || !(this.drawingData?.strokes?.length || this.drawingData?.markdownBlocks?.length)) {
      return false;
    }
    if (this.readingSurfaceRepairFrameId !== null) {
      return true;
    }
    this.surfaceStateGeneration += 1;
    this.initialReadingLayoutSettlement = null;
    this.initialReadingSurfaceSettlement = null;
    this.cancelLayoutRefresh();
    this.initialReadingLayoutPrepared = false;
    this.initialReadingLayoutSettled = false;
    this.initialReadingCommittedSignature = "";
    const generation = this.drawingLoadGeneration;
    const run = () => {
      this.readingSurfaceRepairFrameId = null;
      if (this.destroyed || this.active || generation !== this.drawingLoadGeneration) {
        return;
      }
      this.settleInitialReadingSurface(generation).catch((error) => {
        if (!this.destroyed && generation === this.drawingLoadGeneration) {
          console.error(`[${PLUGIN_ID}] Failed to settle reading surface`, error);
        }
      });
    };
    this.readingSurfaceRepairFrameId = window.requestAnimationFrame?.(run) ?? window.setTimeout(run, 16);
    return true;
  }
  applySettings() {
    const settings = sanitizeSettings(this.plugin?.noteDrawSettings || {});
    this.runtimeSettings = settings;
    setNoteDrawCssProps(this.previewEl, {
      "--notedraw-frame-radius": `${settings.frameCornerRadius}px`
    });
    if (this.brushSettings?.[BRUSH_PEN]) {
      this.brushSettings[BRUSH_PEN].color = settings.defaultPenColor;
      this.brushSettings[BRUSH_PEN].width = settings.defaultPenWidth;
      this.brushSettings[BRUSH_PEN].opacity = settings.defaultPenOpacity;
    }
    if (this.brushSettings?.[BRUSH_WATERCOLOR]) {
      this.brushSettings[BRUSH_WATERCOLOR].color = settings.defaultWatercolorColor;
      this.brushSettings[BRUSH_WATERCOLOR].width = settings.defaultWatercolorWidth;
      this.brushSettings[BRUSH_WATERCOLOR].opacity = settings.defaultWatercolorOpacity;
    }
    if (this.brushVariantSettings) {
      this.brushVariantSettings = createBrushVariantSettings(settings.brushVariantSettings, this.brushSettings);
      this.brushVariantSettings[BRUSH_PEN][BRUSH_VARIANT_DEFAULT] = { ...this.brushSettings[BRUSH_PEN] };
      this.brushVariantSettings[BRUSH_WATERCOLOR][BRUSH_VARIANT_DEFAULT] = { ...this.brushSettings[BRUSH_WATERCOLOR] };
    }
    this.syncCurrentBrushFields?.();
    this.syncPaletteInputs?.();
    this.updateToolButtons?.();
    this.positionToolbar?.();
    this.render?.();
  }
  refreshLocalizedLabels() {
    this.plugin.setAccessibleLabel(this.button, this.surfaceType === "webview" ? "editWebviewDraw" : this.drawingsVisible ? "editTextDraw" : "editTextDrawHidden");
    this.plugin.setAccessibleLabel(this.selectButton, "selectDrawings");
    this.plugin.setAccessibleLabel(this.editMarkdownButton, "editMarkdownTool");
    this.plugin.setAccessibleLabel(this.penButton, "pen");
    this.plugin.setAccessibleLabel(this.watercolorButton, "watercolorBrush");
    this.plugin.setAccessibleLabel(this.textButton, "floatingText");
    this.plugin.setAccessibleLabel(this.undoButton, "undoLastDrawing");
    this.plugin.setAccessibleLabel(this.redoButton, "redoDrawing");
    this.plugin.setAccessibleLabel(this.deleteButton, "deleteSelectedDrawing");
    this.plugin.setAccessibleLabel(this.paletteButton, "penSettings");
    this.plugin.setAccessibleLabel(this.colorInput, "advancedColor");
    this.plugin.setAccessibleLabel(this.advancedColorButton, "advancedColor");
    this.plugin.setAccessibleLabel(this.noColorButton, "noColor");
    this.plugin.setAccessibleLabel(this.markdownBackgroundInput, "blockBackground");
    this.plugin.setAccessibleLabel(this.clearMarkdownBackgroundButton, "clearBlockBackground");
    this.colorSwatchButtons?.forEach((button, index) => {
      const color = COMMON_COLORS[index];
      if (color) {
        button.setAttribute("aria-label", this.plugin.t("useColor", { color }));
      }
    });
    this.widthInput?.setAttribute("title", this.plugin.t("elementSize"));
    this.widthInput?.setAttribute("aria-label", this.plugin.t("elementSize"));
    this.opacityInput?.setAttribute("title", this.plugin.t("elementOpacity"));
    this.opacityInput?.setAttribute("aria-label", this.plugin.t("elementOpacity"));
    if (this.brushPanel) {
      const wasOpen = this.brushPanelOpen;
      this.brushPanel.empty();
      this.createBrushPanel();
      this.brushPanelOpen = wasOpen;
      this.previewEl.toggleClass("is-brush-panel-open", this.brushPanelOpen);
      this.syncBrushPanelButtons();
    }
    if (this.textPanel) {
      const wasOpen = this.textPanelOpen;
      this.textPanel.empty();
      this.createTextPanel();
      this.textPanelOpen = wasOpen;
      this.previewEl.toggleClass("is-text-panel-open", this.textPanelOpen);
      this.syncTextPanelButtons();
    }
    if (this.selectionMenu) {
      const wasOpen = this.selectionMenuOpen;
      this.selectionMenu.empty();
      this.createSelectionMenu();
      this.selectionMenuOpen = wasOpen;
      this.selectionMenu.toggleClass("is-visible", wasOpen);
      this.previewEl.toggleClass("is-selection-menu-open", wasOpen);
    }
    if (this.formatToolbar) {
      this.formatToolbar.querySelectorAll("[data-note-draw-title-key]").forEach((element) => {
        this.plugin.setAccessibleLabel(element, element.dataset.noteDrawTitleKey);
      });
      this.plugin.setAccessibleLabel(this.formatToolbar.querySelector(".notedraw-format-move-button"), "movePanel");
      this.plugin.setAccessibleLabel(this.formatColorInput, "textColor");
      this.plugin.setAccessibleLabel(this.formatHighlightInput, "highlightColor");
      this.plugin.setAccessibleLabel(this.formatSizeSelect, "textSize");
      const firstOption = this.formatSizeSelect?.querySelector?.('option[value=""]');
      if (firstOption) {
        firstOption.textContent = this.plugin.t("size");
      }
    }
  }
  createPaletteInput(icon, cls, attr) {
    const row = this.palettePanel.createDiv({ cls: "notedraw-palette-row" });
    const iconEl = row.createSpan({ cls: "notedraw-palette-icon" });
    (0, import_obsidian.setIcon)(iconEl, icon);
    return row.createEl("input", {
      cls: `notedraw-${cls}`,
      attr
    });
  }
  longPressDelayMs() {
    return this.runtimeSettings?.longPressMs ?? DEFAULT_SETTINGS.longPressMs;
  }
  tapDistancePx() {
    return this.runtimeSettings?.selectTapDistance ?? DEFAULT_SETTINGS.selectTapDistance;
  }
  selectedDragActivationDistancePx(pointerType = "mouse") {
    const preciseThreshold = pointerType === "touch" ? TOUCH_SELECTED_DRAG_ACTIVATION_PX : MOUSE_SELECTED_DRAG_ACTIVATION_PX;
    return Math.min(this.tapDistancePx(), preciseThreshold);
  }
  selectionHitPaddingPx() {
    return this.runtimeSettings?.selectStrokePadding ?? DEFAULT_SETTINGS.selectStrokePadding;
  }
  selectedStrokeAlpha() {
    return this.runtimeSettings?.selectedStrokeAlpha ?? DEFAULT_SETTINGS.selectedStrokeAlpha;
  }
  frameCornerRadius() {
    return clamp10(Number(this.runtimeSettings?.frameCornerRadius ?? DEFAULT_SETTINGS.frameCornerRadius), MIN_FRAME_CORNER_RADIUS, MAX_FRAME_CORNER_RADIUS);
  }
  interpolationStepPx() {
    return this.runtimeSettings?.drawingInterpolationStep ?? DEFAULT_SETTINGS.drawingInterpolationStep;
  }
  minPointDistancePx() {
    return this.runtimeSettings?.drawingMinPointDistance ?? DEFAULT_SETTINGS.drawingMinPointDistance;
  }
  createHeaderButton() {
    return this.plugin.installHeaderButton(this);
  }
  async setFile(file2) {
    if (this.destroyed || !file2) {
      return;
    }
    if (this.file?.path === file2.path) {
      if (!this.drawingsLoaded) {
        await this.ensureDrawingsLoaded();
      } else if (this.previewEl?.isConnected) {
        this.repairConnectedReadingSections();
        this.scheduleResize({ layout: false, measure: true });
        this.requestRender(true);
      }
      return;
    }
    this.endTextEdit();
    this.endFloatingTextInput(true);
    this.surfaceStateGeneration += 1;
    this.initialReadingSurfaceSettlement = null;
    this.initialReadingLayoutSettlement = null;
    this.cancelLayoutRefresh();
    this.drawingLoadGeneration += 1;
    this.cancelNoteFlowLayout();
    this.cancelFrozenNoteFlowLayoutRestore();
    this.frozenNoteFlowPreparation = null;
    this.noteFlowMarkdownAnnotationComplete = false;
    this.restoreDraggedNoteFlowLivePreview();
    this.clearNoteFlowPeerAnimations();
    this.cancelDraggedNoteFlowStrokeAnimation(false);
    this.clearDraggedMarkdownDomMarkers();
    this.clearDraggedNoteFlowPlacement();
    this.clearNoteFlowLayout();
    this.noteFlowLayoutSignature = "";
    this.resetNoteFlowSettle();
    this.noteFlowAnchorRepairReady = false;
    this.noteFlowAnchorRepairComplete = false;
    this.noteFlowPersistencePending = false;
    this.noteFlowActivationRepairAttempted = false;
    this.noteFlowReadingRepairAttempted = false;
    this.noteFlowReadingRepairPending = false;
    this.noteFlowLayoutIncomplete = false;
    this.markdownSourceRevisionChanged = false;
    this.noteFlowAvoidanceAnchors.clear();
    this.cancelRenderFrame();
    if (this.laserFadeTimer !== null) {
      window.clearTimeout(this.laserFadeTimer);
      this.laserFadeTimer = null;
    }
    this.cancelResizeFrame();
    this.cancelResponsiveProjectionSettle();
    this.cancelReadingZoomSettle();
    this.readingZoomBaseTarget = null;
    this.readingZoomBaseOrigin = null;
    this.resetCanvasSurface();
    this.file = file2;
    this.currentStroke = null;
    this.pointerDown = false;
    this.pointerStartEditable = null;
    this.activePointerId = null;
    this.resetTouchGestureState();
    this.draggingStroke = false;
    this.dragStrokeStartPoint = null;
    this.dragStrokeOriginalPoints = null;
    this.dragStrokeOriginalNoteFlows = null;
    this.dragNoteFlowOriginalBounds = null;
    this.pendingDraggedNoteFlowIndexes.clear();
    this.dragNoteFlowLivePreviewOriginals.clear();
    this.dragNoteFlowRawPreviewPoints = null;
    this.dragNoteFlowRawPreviewNoteFlows = null;
    this.dragNoteFlowPreviewElementIds.clear();
    this.dragNoteFlowPlacementClientDelta = null;
    this.dragNoteFlowDomPreview = null;
    this.dragStrokeMoved = false;
    this.dragStrokeHitIndex = -1;
    this.resizingSelection = false;
    this.resizeSelectionHandle = null;
    this.resizeSelectionStartPoint = null;
    this.resizeSelectionStartClient = null;
    this.resizeSelectionOriginalBounds = null;
    this.resizeSelectionPreviewBounds = null;
    this.resizeSelectionOriginalStrokes = null;
    this.resizeSelectionOriginalMarkdownBlocks = null;
    this.resizeSelectionPointerGeometry = null;
    this.resizeSelectionClientBounds = null;
    this.resizeSelectionMoved = false;
    this.selectingStrokes = false;
    this.selectionStartPoint = null;
    this.selectionCurrentPoint = null;
    this.clearSelectionLongPress();
    this.hideSelectionMenu();
    this.redoStack = [];
    this.selectedStrokeIndex = -1;
    this.selectedStrokeIndexes.clear();
    this.selectedMarkdownBlockIds.clear();
    this.markdownSelectionActivation = null;
    this.selectionFrameSnapshot = null;
    this.clearMarkdownBlockPresentation();
    this.embedNodes.forEach((node) => node.remove());
    this.embedNodes.clear();
    this.embedRenderTokens.clear();
    this.embedGeometryTokens.clear();
    this.releaseCanvasImageCache();
    this.responsiveLayoutSignature = "";
    this.responsivePointsInitialized = false;
    this.responsiveLayoutContext = null;
    this.initialReadingLayoutPrepared = false;
    this.initialReadingLayoutSettlement = null;
    this.initialReadingLayoutSettled = false;
    this.initialReadingSurfaceSettlement = null;
    this.initialReadingCommittedSignature = "";
    this.readingTouchGuard = createReadingTouchGuardState();
    this.responsiveProjectionPending = null;
    this.invalidateStaticCache();
    this.drawingsLoaded = false;
    this.loadingDrawings = null;
    this.drawingData = createEmptyDrawingData(file2);
    await this.prepareInitialReadingLayout();
    if (this.destroyed || !this.previewEl?.isConnected) {
      return;
    }
    await this.ensureDrawingsLoaded();
  }
  resetCanvasSurface() {
    this.previewEl.removeClass("has-notedraw-canvas");
    this.ctx = null;
    this.underlayCtx = null;
    this.staticCtx = null;
    this.canvasCssWidth = 1;
    this.canvasCssHeight = 1;
    this.canvasWindowTop = 0;
    this.canvasRenderHeight = 1;
    this.canvasBackingScale = 1;
    this.responsiveLayoutSignature = "";
    this.responsivePointsInitialized = false;
    this.responsiveLayoutContext = null;
    for (const canvas of [this.underlayCanvas, this.staticCanvas, this.canvas]) {
      if (!canvas) {
        continue;
      }
      canvas.width = 1;
      canvas.height = 1;
      for (const property of ["top", "width", "height", "min-width"]) {
        canvas.style.removeProperty(property);
      }
    }
    this.invalidateStaticCache();
  }
  releaseCanvasImageCache() {
    for (const state of this.canvasImageCache?.values?.() || []) {
      const image = state?.image;
      if (!image) {
        continue;
      }
      image.onload = null;
      image.onerror = null;
      try {
        image.removeAttribute?.("src");
      } catch (error) {
        void error;
      }
    }
    this.canvasImageCache?.clear?.();
  }
  destroy(options = {}) {
    if (this.destroyed) {
      return;
    }
    const surfaceBeforeDestroy = this.plugin.describeController(this);
    if (options.discardEdits === true) {
      this.endTextEdit({ save: false });
      this.endFloatingTextInput(false);
    } else {
      this.endTextEdit();
      this.endFloatingTextInput(true);
    }
    this.restoreDraggedNoteFlowLivePreview();
    this.clearNoteFlowPeerAnimations();
    this.cancelDraggedNoteFlowStrokeAnimation(false);
    this.clearDraggedMarkdownDomMarkers();
    this.clearDraggedNoteFlowPlacement();
    this.destroyed = true;
    this.drawingLoadGeneration += 1;
    this.layoutRefreshGeneration += 1;
    this.clearButtonLongPress();
    this.cancelRenderFrame();
    if (this.readingSurfaceRepairFrameId !== null) {
      window.cancelAnimationFrame?.(this.readingSurfaceRepairFrameId);
      window.clearTimeout?.(this.readingSurfaceRepairFrameId);
      this.readingSurfaceRepairFrameId = null;
    }
    this.cancelResizeFrame();
    this.cancelResponsiveProjectionSettle();
    this.cancelPositionFrame();
    this.cancelReadingVirtualSectionSync();
    this.cancelReadingZoomSettle();
    if (this.scrollSettleTimer !== null) {
      window.clearTimeout(this.scrollSettleTimer);
      this.scrollSettleTimer = null;
    }
    this.endNoteFlowScrollStability();
    this.cancelMultiTouchFrame();
    this.cancelSelectedResizeNoteFlowLayout();
    this.resizeObserver?.disconnect();
    this.markdownRenderObserver?.disconnect();
    this.markdownRenderObserver = null;
    if (this.markdownObserverResumeTimer !== null) {
      window.clearTimeout(this.markdownObserverResumeTimer);
      this.markdownObserverResumeTimer = null;
    }
    this.markdownMutationWindowStartedAt = 0;
    this.markdownMutationBurstCount = 0;
    this.markdownObserverPausedUntil = 0;
    this.markdownObserverPauseStreak = 0;
    if (this.markdownAnnotationTimer !== null) {
      window.clearTimeout(this.markdownAnnotationTimer);
      this.markdownAnnotationTimer = null;
    }
    this.markdownAnnotationNeedsLayout = false;
    this.markdownAnnotationForce = false;
    this.scrollEventTarget?.removeEventListener("scroll", this.onScroll);
    this.scrollContainer = null;
    this.scrollEventTarget = null;
    this.layoutMeasureEl = null;
    window.removeEventListener("resize", this.onResize);
    window.visualViewport?.removeEventListener("resize", this.onResize);
    window.visualViewport?.removeEventListener("scroll", this.onResize);
    activeDocument.removeEventListener("pointerdown", this.onDocumentPointerDown, true);
    activeDocument.removeEventListener("pointermove", this.onDocumentPointerMove, true);
    activeDocument.removeEventListener("pointerup", this.onDocumentPointerFinish, true);
    activeDocument.removeEventListener("pointercancel", this.onDocumentPointerFinish, true);
    activeDocument.removeEventListener("selectionchange", this.onDocumentSelectionChange);
    activeDocument.removeEventListener("keydown", this.onDocumentKeyDown, true);
    this.cancelNoteFlowLayout();
    this.cancelFrozenNoteFlowLayoutRestore();
    this.cancelLayoutRefresh();
    this.clearNoteFlowLayout();
    this.stopFormatToolbarDrag();
    this.stopToolbarDrag();
    this.sourceFormatToolbarCleanup?.();
    this.sourceFormatToolbarCleanup = null;
    if (this.embedRepairTimer !== null) {
      window.clearTimeout(this.embedRepairTimer);
      this.embedRepairTimer = null;
    }
    if (this.markdownMutationSyncTimer !== null) {
      window.clearTimeout(this.markdownMutationSyncTimer);
      this.markdownMutationSyncTimer = null;
    }
    if (this.deletedTextHighlightPruneTimer !== null) {
      window.clearTimeout(this.deletedTextHighlightPruneTimer);
      this.deletedTextHighlightPruneTimer = null;
    }
    if (this.sourceFormatToolbarRetryTimer !== null) {
      window.clearTimeout(this.sourceFormatToolbarRetryTimer);
      this.sourceFormatToolbarRetryTimer = null;
    }
    this.canvas?.removeEventListener("pointerdown", this.onPointerDown);
    this.canvas?.removeEventListener("pointermove", this.onPointerMove);
    this.canvas?.removeEventListener("pointerup", this.onPointerUp);
    this.canvas?.removeEventListener("pointercancel", this.onPointerUp);
    this.canvas?.removeEventListener("lostpointercapture", this.onPointerUp);
    this.canvas?.removeEventListener("dblclick", this.onCanvasDoubleClick);
    this.canvas?.removeEventListener("contextmenu", this.onCanvasContextMenu);
    this.canvas?.removeEventListener("wheel", this.onWheel);
    this.previewEl?.removeEventListener("contextmenu", this.onPreviewContextMenu, true);
    this.previewEl?.removeEventListener("dblclick", this.onPreviewDoubleClick, true);
    this.previewEl?.removeEventListener("click", this.onHeadingCollapseIndicatorClick, true);
    this.previewGestureWindow?.removeEventListener("mousedown", this.onPreviewSecondPress, true);
    this.previewGestureWindow?.removeEventListener("pointerdown", this.onPreviewSecondPointerDown, true);
    this.previewGestureWindow?.removeEventListener("dblclick", this.onPreviewDoubleClick, true);
    this.previewGestureWindow = null;
    this.previewEl?.removeEventListener("pointerdown", this.onReadingTouchPointerDown, true);
    this.previewEl?.removeEventListener("pointermove", this.onReadingTouchPointerMove, true);
    this.previewEl?.removeEventListener("pointerup", this.onReadingTouchPointerFinish, true);
    this.previewEl?.removeEventListener("pointercancel", this.onReadingTouchPointerFinish, true);
    this.previewEl?.removeEventListener("click", this.onReadingClick, true);
    this.previewEl?.removeEventListener("scroll", this.onReadingVirtualScrollCapture, true);
    this.plugin.releaseHeaderButton(this);
    this.toolbar?.remove();
    this.palettePanel?.remove();
    this.brushPanel?.remove();
    this.textPanel?.remove();
    this.selectionMenu?.remove();
    this.formatToolbar?.remove();
    this.noteFlowDropIndicator?.remove();
    this.noteFlowDropIndicator = null;
    this.hiddenFileInput?.remove();
    this.clearMarkdownBlockPresentation();
    this.embedNodes.forEach((node) => node.remove());
    this.underlayEmbedLayer?.remove();
    this.embedLayer?.remove();
    this.embedNodes.clear();
    this.embedRenderTokens.clear();
    this.embedGeometryTokens.clear();
    this.releaseCanvasImageCache();
    this.resetCanvasSurface();
    this.underlayCanvas?.remove();
    this.staticCanvas?.remove();
    this.canvas?.remove();
    this.underlayCanvas = null;
    this.staticCanvas = null;
    this.canvas = null;
    this.clearReadingZoomStyles();
    this.restoreReadingZoomStage();
    this.disposeRegisteredSurfaceViewport?.();
    this.disposeRegisteredSurfaceViewport = null;
    this.readingZoomExtent?.remove();
    this.readingZoomExtent = null;
    this.readingBottomSpacer?.remove();
    this.readingBottomSpacer = null;
    this.readingBottomSpacerHeight = 0;
    this.previewEl.removeClass("notedraw-shell");
    this.previewEl.removeClass("is-notedraw-responsive-layout");
    this.previewEl.removeClass("is-notedraw-controls-visible");
    this.previewEl.removeClass("is-drawing-active");
    this.previewEl.removeClass("is-drawing-hidden");
    this.previewEl.removeClass("is-select-mode");
    this.previewEl.removeClass("is-palette-open");
    this.previewEl.removeClass("is-brush-panel-open");
    this.previewEl.removeClass("is-text-panel-open");
    this.previewEl.removeClass("is-selection-menu-open");
    this.previewEl.removeClass("is-watercolor-mode");
    this.previewEl.removeClass("is-edit-md-mode");
    this.previewEl.removeClass("is-notedraw-source-shell");
    this.previewEl.removeClass("is-notedraw-webview-shell");
    this.previewEl.removeClass("is-notedraw-workspace-shell");
    this.previewEl.removeClass("is-notedraw-registered-shell");
    this.previewEl.removeClass("has-notedraw-body-controls");
    this.previewEl.removeClass("has-notedraw-canvas");
    this.previewEl.removeClass("is-resizing-selection");
    this.previewEl.removeClass("is-native-text-editing");
    this.previewEl.removeClass("is-reading-zoomed");
    this.previewEl.removeClass("is-editing-layout-zoomed");
    this.clearSelectionLongPress();
    if (this.previewEl._noteDrawController === this) {
      delete this.previewEl._noteDrawController;
    }
    if (this.plugin.controllers?.get(this.previewEl) === this) {
      this.plugin.controllers.delete(this.previewEl);
    }
    if (this.plugin.sourceControllers?.get(this.view) === this) {
      this.plugin.sourceControllers.delete(this.view);
    }
    if (this.plugin.webviewControllers?.get(this.previewEl) === this) {
      this.plugin.webviewControllers.delete(this.previewEl);
    }
    if (this.plugin.workspaceControllers?.get(this.view) === this) {
      this.plugin.workspaceControllers.delete(this.view);
    }
    if (this.registeredSurfaceKey && this.plugin.registeredSurfaceControllers?.get(this.registeredSurfaceKey) === this) {
      this.plugin.registeredSurfaceControllers.delete(this.registeredSurfaceKey);
      this.plugin.registeredSurfaceRecords?.delete(this.registeredSurfaceKey);
    }
    this.plugin.liveControllers?.delete(this);
    if (surfaceBeforeDestroy) {
      this.plugin.emitApiEvent("surface-changed", { ...surfaceBeforeDestroy, phase: "unmounted" });
    }
    this.currentStroke = null;
    this.drawingData = null;
    this.markdownBlockElements.clear();
    this.redoStack = [];
  }
  async toggle() {
    if (this.destroyed) {
      return;
    }
    const nextActive = !this.active;
    this.plugin.setControllerActivation(this, nextActive);
    if (!nextActive) {
      return;
    }
    if (nextActive) {
      await this.ensureDrawingsLoaded();
      if (this.destroyed || !this.active) {
        return;
      }
      if (!this.drawingsVisible) {
        this.setDrawingsVisible(true);
      }
      this.prepareNoteFlowForEditing();
    }
  }
  applyActiveState(active, options = {}) {
    if (this.destroyed) {
      return;
    }
    const eager = options.eager !== false;
    const wasActive = this.active;
    this.active = Boolean(active);
    if (wasActive !== this.active) {
      this.surfaceStateGeneration += 1;
      this.initialReadingSurfaceSettlement = null;
      this.initialReadingLayoutSettlement = null;
    }
    if (this.active && !this.embeddedSurface && isElementVisibleEnough(this.previewEl)) {
      this.plugin.setInteractionController(this);
    }
    this.previewEl.toggleClass("is-drawing-active", this.active);
    this.syncFloatingControlClasses();
    if (this.active && this.controlsShouldBeVisible()) {
      this.plugin.enforceSingleVisibleToolbar(this);
    }
    this.button?.classList.toggle("is-active", this.active);
    if (this.active && !wasActive && this.drawingsLoaded && this.drawingsVisible) {
      this.prepareNoteFlowForEditing();
    }
    if (!this.active && wasActive) {
      this.cancelNoteFlowLayout();
      this.endTextEdit();
      this.endFloatingTextInput(true);
      this.setPaletteOpen(false);
      this.setBrushPanelOpen(false);
      this.setTextPanelOpen(false);
      this.hideSelectionMenu();
      this.clearSelectionLongPress();
      this.cancelCurrentStroke();
      this.cancelSelectionDrag(true);
      this.cancelSelectedStrokeDrag(true);
      this.cancelSelectedStrokeResize(true);
      this.clearSelectedStrokes();
      this.resetTouchGestureState();
      this.syncMarkdownBlockPresentation();
      this.scheduleFrozenNoteFlowLayoutRestore();
      this.render();
      this.queueReadingSurfaceSettlement();
    } else if (this.active && eager && (!wasActive || !this.drawingsLoaded)) {
      this.ensureDrawingsLoaded().catch((error) => {
        console.error(`[${PLUGIN_ID}] Failed to load drawings`, error);
      });
      if (this.toolMode === TOOL_EDIT_MD) {
        this.scheduleMarkdownAnnotationRefresh({ layout: false });
      }
    } else if (this.active && !eager) {
      this.layoutRefreshGeneration += 1;
    }
    if (wasActive !== this.active && this.drawingsLoaded) {
      this.scheduleResize({ layout: false, measure: false });
    }
  }
  controlsShouldBeVisible() {
    if (this.embeddedSurface || !this.active || this.destroyed || !this.previewEl?.isConnected || isBlockingObsidianOverlayOpen(activeDocument)) {
      return false;
    }
    const activeLeaf = this.plugin.app.workspace?.activeLeaf;
    const ownerLeaf = this.view?.leaf || findOwningLeaf(this.plugin.app, this.view?.containerEl || this.previewEl);
    if (activeLeaf && ownerLeaf && activeLeaf !== ownerLeaf) {
      return false;
    }
    if (this.plugin.interactionController(this) !== this) {
      return false;
    }
    return isElementVisibleEnough(this.previewEl);
  }
  syncFloatingControlClasses() {
    const visible = this.controlsShouldBeVisible();
    this.previewEl?.toggleClass("is-notedraw-controls-visible", visible);
    this.toolbar?.toggleAttribute("aria-hidden", !visible);
    for (const element of [this.toolbar, this.palettePanel, this.brushPanel, this.textPanel, this.selectionMenu, this.formatToolbar]) {
      element?.toggleClass("is-drawing-active", Boolean(this.active));
      element?.toggleClass("is-notedraw-controls-visible", visible);
      element?.toggleClass("is-palette-open", Boolean(this.paletteOpen));
      element?.toggleClass("is-brush-panel-open", Boolean(this.brushPanelOpen));
      element?.toggleClass("is-text-panel-open", Boolean(this.textPanelOpen));
      element?.toggleClass("is-selection-menu-open", Boolean(this.selectionMenuOpen));
    }
    if (visible && this.active && !this.drawingsLoaded && !this.loadingDrawings) {
      this.ensureDrawingsLoaded().then(() => {
        this.scheduleLayoutRefresh({ settle: false });
      }).catch((error) => {
        console.error(`[${PLUGIN_ID}] Failed to load drawings`, error);
      });
    }
  }
  async ensureDrawingsLoaded() {
    if (this.drawingsLoaded) {
      return;
    }
    if (this.loadingDrawings) {
      await this.loadingDrawings;
      return;
    }
    const file2 = this.file;
    const generation = ++this.drawingLoadGeneration;
    const loading = this.plugin.readDrawings(file2).then(async (data) => {
      if (this.destroyed || generation !== this.drawingLoadGeneration || this.file?.path !== file2?.path) {
        return;
      }
      this.drawingData = data;
      const relationGroupsChanged = this.syncConnectorElementGroups();
      if (relationGroupsChanged) {
        this.plugin.scheduleDrawingSave(file2, this.drawingData, { userOperation: true, replace: true });
      }
      this.markdownSourceRevisionChanged = Boolean(data?._notedrawSourceRevisionMismatch);
      const clearedUnboundRelations = this.rebuildElementRelations();
      if (clearedUnboundRelations) {
        this.plugin.scheduleDrawingSave(file2, this.drawingData, { userOperation: true, replace: true });
      }
      this.drawingsLoaded = true;
      this.applyDrawingsVisibility(data.visible !== false);
      this.invalidateStaticCache();
      if (this.destroyed || generation !== this.drawingLoadGeneration || this.file?.path !== file2?.path) {
        return;
      }
      if (!this.active && this.surfaceType === "preview") {
        this.initialReadingLayoutPrepared = false;
        this.initialReadingLayoutSettled = false;
        await this.settleInitialReadingSurface(generation);
      }
      if (this.destroyed || generation !== this.drawingLoadGeneration || this.file?.path !== file2?.path) {
        return;
      }
      await this.prepareFrozenNoteFlowLayout().catch((error) => {
        void error;
        return false;
      });
      if (this.destroyed || generation !== this.drawingLoadGeneration || this.file?.path !== file2?.path) {
        return;
      }
      this.syncMarkdownBlockPresentation();
      this.resizeCanvas({ layout: false, measure: true });
      if (this.surfaceType === "source") {
        this.resizeCanvas({ layout: true, measure: false });
      }
      if (!this.active && this.hasNoteFlowElements()) {
        this.restoreFrozenNoteFlowLayout();
        this.scheduleFrozenNoteFlowLayoutRestoreAfterMeasurement();
      }
      this.render();
      if (!this.active && this.hasNoteFlowElements()) {
        await this.prepareNoteFlowForReading().catch((error) => {
          void error;
        });
      }
      if (this.markdownBlockRecords().length) {
        this.scheduleMarkdownAnnotationRefresh({ layout: false, delay: 0, force: true });
      }
      if (this.active && this.drawingsVisible) {
        this.prepareNoteFlowForEditing();
      }
    }).finally(() => {
      if (this.loadingDrawings === loading) {
        this.loadingDrawings = null;
      }
    });
    this.loadingDrawings = loading;
    await loading;
  }
  onResize() {
    if (this.embeddedSurface && !isElementLaidOut(this.previewEl)) {
      return;
    }
    this.syncFloatingControlClasses();
    this.applyReadingZoom();
    const previewWidth = Math.max(0, Number(this.previewEl?.clientWidth) || 0);
    const widthChanged = this.lastObservedPreviewWidth > 0 && Math.abs(previewWidth - this.lastObservedPreviewWidth) >= 1;
    this.lastObservedPreviewWidth = previewWidth;
    this.reconcileSettledReadingSurface();
    if (this.active || this.drawingsLoaded || this.ctx) {
      this.scheduleResize({ layout: false, measure: widthChanged });
    }
  }
  onReadingVirtualScrollCapture() {
    if (!this.usesVisualReadingZoom() || Math.abs(this.readingZoomScale() - 1) < 1e-3) {
      return;
    }
    const renderer = this.readingPreviewRenderer();
    if (renderer?.previewEl === this.previewEl) {
      renderer.lastScroll = Number(this.previewEl.scrollTop) || 0;
    }
  }
  onScroll() {
    this.lastScrollAt = Date.now();
    recordReadingTouchScroll(this.readingTouchGuard, { now: this.lastScrollAt });
    if (!this.isReadingZoomInteractionActive()) {
      this.scheduleReadingVirtualSectionSync();
    }
    if (this.embeddedSurface && !isElementNearViewport(this.previewEl)) {
      if (this.scrollSettleTimer !== null) {
        window.clearTimeout(this.scrollSettleTimer);
        this.scrollSettleTimer = null;
      }
      return;
    }
    if (!this.embeddedSurface) {
      this.syncFloatingControlClasses();
      this.scheduleFloatingControlsPosition();
      this.plugin.scheduleEmbeddedMarkdownSync();
    }
    if (this.active || this.drawingsLoaded || this.ctx) {
      if (this.multiTouchScrolling) {
        return;
      }
      this.scheduleResize({ layout: false, measure: false });
      if (this.scrollSettleTimer !== null) {
        window.clearTimeout(this.scrollSettleTimer);
      }
      this.scrollSettleTimer = window.setTimeout(() => {
        this.scrollSettleTimer = null;
        if (this.destroyed) {
          return;
        }
        this.scheduleResize({ layout: false, measure: false });
      }, 90);
    }
  }
  beginNoteFlowScrollStability() {
    if (this.destroyed || this.surfaceType !== "preview" || !this.previewEl?.isConnected) {
      return;
    }
    if (this.noteFlowScrollStabilityTimer !== null) {
      window.clearTimeout(this.noteFlowScrollStabilityTimer);
      this.noteFlowScrollStabilityTimer = null;
    }
    this.previewEl.addClass("is-note-flow-settling");
    this.noteFlowScrollStabilityTimer = window.setTimeout(() => {
      this.noteFlowScrollStabilityTimer = null;
      this.endNoteFlowScrollStability();
    }, 900);
  }
  endNoteFlowScrollStability() {
    if (this.noteFlowScrollStabilityTimer !== null) {
      window.clearTimeout(this.noteFlowScrollStabilityTimer);
      this.noteFlowScrollStabilityTimer = null;
    }
    this.previewEl?.removeClass?.("is-note-flow-settling");
  }
  isReadingProjectionSettleSurface() {
    return this.surfaceType === "preview" && this.responsivePointsInitialized;
  }
  scheduleResponsiveProjectionSettle(delay = 180, options = {}) {
    if (this.destroyed || !this.isReadingProjectionSettleSurface()) {
      return;
    }
    this.responsiveProjectionPreserveNoteFlowAbsolute = this.responsiveProjectionPreserveNoteFlowAbsolute || options.preserveNoteFlowAbsolute === true;
    if (this.responsiveProjectionSettleTimer !== null) {
      window.clearTimeout(this.responsiveProjectionSettleTimer);
    }
    const wait = Math.max(40, Number(delay) || 180);
    this.responsiveProjectionSettleTimer = window.setTimeout(() => {
      this.responsiveProjectionSettleTimer = null;
      if (this.destroyed || !this.previewEl?.isConnected) {
        return;
      }
      const sinceScroll = Date.now() - this.lastScrollAt;
      if (this.lastScrollAt > 0 && sinceScroll < 260) {
        this.scheduleResponsiveProjectionSettle(260 - sinceScroll + 40, {
          preserveNoteFlowAbsolute: this.responsiveProjectionPreserveNoteFlowAbsolute
        });
        return;
      }
      const preserveNoteFlowAbsolute = this.responsiveProjectionPreserveNoteFlowAbsolute;
      this.responsiveProjectionPreserveNoteFlowAbsolute = false;
      this.scheduleResize({ layout: true, preserveNoteFlowAbsolute });
    }, wait);
  }
  cancelResponsiveProjectionSettle() {
    if (this.responsiveProjectionSettleTimer !== null) {
      window.clearTimeout(this.responsiveProjectionSettleTimer);
      this.responsiveProjectionSettleTimer = null;
    }
    this.responsiveProjectionPreserveNoteFlowAbsolute = false;
  }
  scheduleResize(options = {}) {
    const noteFlowResizeSuppressed = Date.now() < this.noteFlowSuppressResizeUntil;
    const readingZoomInteractionActive = this.isReadingZoomInteractionActive();
    const sinceScroll = Date.now() - this.lastScrollAt;
    const readingScrollActive = this.isReadingProjectionSettleSurface() && this.lastScrollAt > 0 && sinceScroll < 260;
    if (readingScrollActive && this.resizeNeedsLayout) {
      this.resizeNeedsLayout = false;
      this.resizePreserveNoteFlowAbsolute = false;
    }
    if (options.layout !== false && readingScrollActive) {
      this.scheduleResponsiveProjectionSettle(260 - sinceScroll + 40, options);
    }
    const wantsLayout = options.layout !== false && !this.draggingStroke && !this.resizingSelection && !noteFlowResizeSuppressed && !readingZoomInteractionActive && !readingScrollActive;
    const wantsMeasure = options.measure !== false && !this.resizingSelection && !readingZoomInteractionActive && !readingScrollActive;
    if (wantsLayout) {
      if (!this.resizeNeedsLayout) {
        this.resizePreserveNoteFlowAbsolute = options.preserveNoteFlowAbsolute === true;
      } else if (options.preserveNoteFlowAbsolute !== true) {
        this.resizePreserveNoteFlowAbsolute = false;
      }
      this.resizeNeedsLayout = true;
    }
    this.resizePreserveAbsolutePlacement = this.resizePreserveAbsolutePlacement || options.preserveAbsolutePlacement === true;
    this.resizeNeedsMeasure = this.resizeNeedsMeasure || wantsMeasure || wantsLayout;
    if (this.resizeFrameId !== null || this.resizeFallbackTimer !== null) {
      return;
    }
    this.resizeFrameId = window.requestAnimationFrame(() => this.flushScheduledResize());
    this.resizeFallbackTimer = window.setTimeout(() => this.flushScheduledResize(), 120);
  }
  flushScheduledResize() {
    if (this.resizeFrameId !== null) {
      window.cancelAnimationFrame(this.resizeFrameId);
      this.resizeFrameId = null;
    }
    if (this.resizeFallbackTimer !== null) {
      window.clearTimeout(this.resizeFallbackTimer);
      this.resizeFallbackTimer = null;
    }
    if (this.destroyed) {
      return;
    }
    const layout = this.resizeNeedsLayout;
    const measure = this.resizeNeedsMeasure;
    const preserveNoteFlowAbsolute = layout && this.resizePreserveNoteFlowAbsolute;
    const preserveAbsolutePlacement = this.resizePreserveAbsolutePlacement;
    this.resizeNeedsLayout = false;
    this.resizeNeedsMeasure = false;
    this.resizePreserveNoteFlowAbsolute = false;
    this.resizePreserveAbsolutePlacement = false;
    const canvasChanged = this.resizeCanvas({ layout, measure, preserveNoteFlowAbsolute, preserveAbsolutePlacement });
    if ((canvasChanged || measure) && !this.active && this.hasNoteFlowElements()) {
      this.scheduleFrozenNoteFlowLayoutRestoreAfterMeasurement();
    }
    this.updateFloatingControlsPosition();
    this.positionFormatToolbar();
    this.positionFloatingTextInput();
    if (canvasChanged) {
      if (layout) {
        this.render();
      } else {
        this.renderCanvas();
      }
    }
  }
  cancelResizeFrame() {
    if (this.resizeFrameId !== null) {
      window.cancelAnimationFrame(this.resizeFrameId);
      this.resizeFrameId = null;
    }
    if (this.resizeFallbackTimer !== null) {
      window.clearTimeout(this.resizeFallbackTimer);
      this.resizeFallbackTimer = null;
    }
    this.resizeNeedsLayout = false;
    this.resizeNeedsMeasure = false;
    this.resizePreserveNoteFlowAbsolute = false;
    this.resizePreserveAbsolutePlacement = false;
  }
  scheduleFloatingControlsPosition() {
    if (this.positionFrameId !== null) {
      return;
    }
    this.positionFrameId = window.requestAnimationFrame(() => {
      this.positionFrameId = null;
      this.updateFloatingControlsPosition();
      this.positionFormatToolbar();
      this.positionFloatingTextInput();
    });
  }
  cancelPositionFrame() {
    if (this.positionFrameId !== null) {
      window.cancelAnimationFrame(this.positionFrameId);
      this.positionFrameId = null;
    }
  }
  refreshScrollContainer() {
    const nextContainer = findScrollableAncestor(this.previewEl);
    const nextTarget = getScrollEventTarget(nextContainer);
    if (nextContainer === this.scrollContainer && nextTarget === this.scrollEventTarget) {
      return;
    }
    this.scrollEventTarget?.removeEventListener("scroll", this.onScroll);
    this.scrollContainer = nextContainer;
    this.scrollEventTarget = nextTarget;
    this.scrollEventTarget?.addEventListener("scroll", this.onScroll, { passive: true });
  }
  scheduleLayoutRefresh(options = {}) {
    this.cancelLayoutRefresh();
    const settle = options.settle !== false;
    const generation = ++this.layoutRefreshGeneration;
    const refresh = () => {
      if (!this.destroyed && generation === this.layoutRefreshGeneration) {
        if (this.active || this.drawingsLoaded || isElementVisibleEnough(this.previewEl)) {
          this.scheduleResize({ layout: options.layout === true, measure: options.measure !== false });
        }
      }
    };
    refresh();
    this.layoutRefreshFrameId = window.requestAnimationFrame?.(() => {
      this.layoutRefreshFrameId = null;
      refresh();
    }) ?? null;
    const scheduleRefresh = (delay) => {
      const timerId = window.setTimeout(() => {
        this.layoutRefreshTimerIds.delete(timerId);
        refresh();
      }, delay);
      this.layoutRefreshTimerIds.add(timerId);
    };
    scheduleRefresh(80);
    if (settle) {
      scheduleRefresh(350);
      scheduleRefresh(900);
      if (isMobileRuntime()) {
        scheduleRefresh(1600);
      }
    }
  }
  cancelLayoutRefresh() {
    if (this.layoutRefreshFrameId !== null) {
      window.cancelAnimationFrame?.(this.layoutRefreshFrameId);
      this.layoutRefreshFrameId = null;
    }
    for (const timerId of this.layoutRefreshTimerIds || []) {
      window.clearTimeout(timerId);
    }
    this.layoutRefreshTimerIds?.clear?.();
  }
  scheduleMarkdownAnnotationRefresh(options = {}) {
    const requestedForce = options.force === true || this.hasNoteFlowElements();
    this.markdownAnnotationForce = this.markdownAnnotationForce || requestedForce;
    if (requestedForce) {
      this.noteFlowMarkdownAnnotationComplete = false;
    }
    const editingLayout = this.markdownAnnotationForce || this.active && (this.toolMode === TOOL_EDIT_MD || this.noteFlowOperationPending || this.draggingStroke || this.resizingSelection);
    if (!editingLayout) {
      return;
    }
    this.markdownAnnotationNeedsLayout = this.markdownAnnotationNeedsLayout || options.layout !== false;
    const delay = Number.isFinite(Number(options.delay)) ? Math.max(0, Math.min(120, Number(options.delay))) : 120;
    if (this.markdownAnnotationTimer !== null && requestedForce && delay === 0) {
      window.clearTimeout(this.markdownAnnotationTimer);
      this.markdownAnnotationTimer = null;
    }
    if (this.markdownAnnotationTimer !== null || this.destroyed) {
      return;
    }
    this.markdownAnnotationTimer = window.setTimeout(() => {
      this.markdownAnnotationTimer = null;
      const layout = this.markdownAnnotationNeedsLayout;
      const force = this.markdownAnnotationForce || this.hasNoteFlowElements();
      this.markdownAnnotationNeedsLayout = false;
      this.markdownAnnotationForce = false;
      if (this.destroyed || !this.previewEl?.isConnected) {
        return;
      }
      this.applyReadingZoom();
      annotateVisibleMarkdownElements(this.plugin.app, this.previewEl, this.file.path);
      annotateRenderedMarkdownLines(this.plugin.app, this.previewEl, this.file.path, { force }).then(() => {
        if (force) {
          this.noteFlowMarkdownAnnotationComplete = true;
        }
      }).catch((error) => {
        void error;
      }).finally(() => {
        if (this.destroyed || !this.previewEl?.isConnected) {
          return;
        }
        this.responsiveLayoutContext = null;
        this.syncMarkdownBlockPresentation();
        if (this.noteFlowOperationPending) {
          this.scheduleNoteFlowAnchorRepair();
        }
        if (this.noteFlowOperationPending && this.hasNoteFlowElements()) {
          this.scheduleNoteFlowLayout();
        }
        if (this.drawingsLoaded) {
          if (layout) {
            this.responsiveLayoutSignature = "";
          }
          this.scheduleResize({ layout });
          if (!this.active && this.hasNoteFlowElements()) {
            this.scheduleFrozenNoteFlowLayoutRestoreAfterMeasurement();
          }
        }
      });
    }, delay);
  }
  scheduleMarkdownMutationSync() {
    if (this.markdownMutationSyncTimer !== null) {
      return;
    }
    this.markdownMutationSyncTimer = window.setTimeout(() => {
      this.markdownMutationSyncTimer = null;
      if (this.destroyed || !this.previewEl?.isConnected) {
        return;
      }
      this.repairConnectedReadingSections();
      this.reconcileSettledReadingSurface();
      const editingLayout = this.active && (this.toolMode === TOOL_EDIT_MD || this.noteFlowOperationPending || this.draggingStroke || this.resizingSelection);
      const identityMutationPending = this.pendingMarkdownIdentityRefresh?.expiresAt > Date.now();
      if (editingLayout || identityMutationPending) {
        this.scheduleMarkdownAnnotationRefresh({
          layout: false,
          delay: identityMutationPending ? 0 : void 0,
          force: identityMutationPending
        });
      } else {
        this.syncMarkdownBlockPresentation();
        this.scheduleFrozenNoteFlowLayoutRestore();
        this.scheduleReadingVirtualSectionSync();
        this.scheduleResize({ layout: false, measure: false });
        this.scheduleEmbedRepair();
      }
    }, 40);
  }
  updateFloatingControlsPosition() {
    this.syncFloatingControlClasses();
    if (!this.button || !this.toolbar) {
      return;
    }
    const host = this.view?.containerEl || this.previewEl.closest?.(".workspace-leaf-content") || this.previewEl;
    const hostRect = host.getBoundingClientRect();
    const buttonRect = this.button.getBoundingClientRect();
    const viewHeader = this.view?.containerEl?.querySelector?.(".view-header") || this.button.closest?.(".view-header") || null;
    const chromeRect = viewHeader?.getBoundingClientRect?.() || this.button.closest?.(".view-actions")?.getBoundingClientRect?.() || null;
    const toolbarHeight = Math.max(36, this.toolbar.getBoundingClientRect().height || 36);
    const viewport = window.visualViewport;
    const viewportLeft = viewport?.offsetLeft || 0;
    const viewportTop = viewport?.offsetTop || 0;
    const viewportWidth = Math.max(160, viewport?.width || window.innerWidth || 160);
    const viewportHeight = Math.max(120, viewport?.height || window.innerHeight || 120);
    const viewportRight = viewportLeft + viewportWidth;
    const buttonVisible = buttonRect.width > 0 && buttonRect.height > 0 && buttonRect.bottom > viewportTop && buttonRect.top < viewportTop + viewportHeight;
    const anchorRight = hostRect.right > 0 ? hostRect.right : buttonVisible ? buttonRect.right : viewportRight;
    const headerBottom = chromeRect && chromeRect.bottom > 0 ? chromeRect.bottom : 48;
    const anchorBottom = Math.max(
      buttonVisible ? buttonRect.bottom : 0,
      headerBottom
    );
    const compactViewport = isMobileRuntime() || viewportWidth < 640;
    let right = compactViewport ? "auto" : clamp10(viewportRight - anchorRight + 10, 8, Math.max(8, viewportWidth - 48));
    let left = compactViewport ? viewportLeft + 8 : "auto";
    const minTop = Math.max(viewportTop + 8, headerBottom + 6);
    const maxTop = Math.max(minTop, viewportTop + viewportHeight - toolbarHeight - 8);
    const topOffset = sanitizeSettings(this.plugin?.noteDrawSettings || {}).toolbarTopOffset;
    let top = clamp10(anchorBottom + topOffset, minTop, maxTop);
    const savedPosition = normalizeToolbarPosition(this.runtimeSettings?.toolbarPosition);
    const availableWidth = Math.max(0, viewportWidth - this.toolbar.getBoundingClientRect().width - 16);
    const availableHeight = Math.max(0, viewportHeight - toolbarHeight - 16);
    if (this.toolbarManualPosition) {
      left = clamp10(this.toolbarManualPosition.left, viewportLeft + 8, viewportLeft + 8 + availableWidth);
      top = clamp10(this.toolbarManualPosition.top, viewportTop + 8, viewportTop + 8 + availableHeight);
      right = "auto";
    } else if (savedPosition) {
      left = viewportLeft + 8 + availableWidth * savedPosition.xRatio;
      top = viewportTop + 8 + availableHeight * savedPosition.yRatio;
      right = "auto";
    }
    const panelTop = top + toolbarHeight + 6;
    const panelLeft = (button, panel, fallbackWidth) => {
      const buttonBounds = button?.getBoundingClientRect?.();
      const measuredWidth = panel?.getBoundingClientRect?.().width || fallbackWidth;
      const width = clamp10(measuredWidth, 34, Math.max(34, viewportWidth - 16));
      const rightInset = typeof right === "number" ? right : 8;
      const anchorX = buttonBounds?.width > 0 ? buttonBounds.left + buttonBounds.width / 2 : viewportRight - rightInset - 14;
      return clamp10(anchorX - width / 2, viewportLeft + 8, Math.max(viewportLeft + 8, viewportRight - width - 8));
    };
    const props = {
      "--notedraw-toolbar-right": typeof right === "number" ? `${Math.round(right)}px` : right,
      "--notedraw-toolbar-left": typeof left === "number" ? `${Math.round(left)}px` : left,
      "--notedraw-toolbar-top": `${Math.round(top)}px`,
      "--notedraw-palette-top": `${Math.round(panelTop)}px`,
      "--notedraw-palette-left": `${Math.round(panelLeft(this.paletteButton, this.palettePanel, 220))}px`,
      "--notedraw-palette-right": "auto",
      "--notedraw-brush-panel-top": `${Math.round(panelTop)}px`,
      "--notedraw-brush-panel-left": `${Math.round(panelLeft(this.brushPanelMode === BRUSH_WATERCOLOR ? this.watercolorButton : this.penButton, this.brushPanel, this.brushPanelMode === BRUSH_WATERCOLOR ? 114 : 77))}px`,
      "--notedraw-brush-panel-right": "auto",
      "--notedraw-text-panel-top": `${Math.round(panelTop)}px`,
      "--notedraw-text-panel-left": `${Math.round(panelLeft(this.textButton, this.textPanel, 236))}px`,
      "--notedraw-text-panel-right": "auto"
    };
    setNoteDrawCssProps(this.previewEl, props);
    if (this.floatingControlsInBody) {
      for (const element of [this.toolbar, this.palettePanel, this.brushPanel, this.textPanel, this.selectionMenu, this.formatToolbar]) {
        setNoteDrawCssProps(element, props);
      }
    }
  }
  onToolbarPointerDown(event) {
    if (!this.toolbar || event.button !== 0 || event.target?.closest?.("button, input, select, textarea, a, [contenteditable='true']")) {
      return;
    }
    const rect = this.toolbar.getBoundingClientRect();
    this.toolbarDrag = {
      pointerId: event.pointerId,
      startX: event.clientX,
      startY: event.clientY,
      startTop: rect.top,
      startLeft: rect.left
    };
    this.toolbarManualPosition = { top: rect.top, left: rect.left };
    this.toolbar.addClass("is-moving");
    activeDocument.addEventListener("pointermove", this.onToolbarDragMove, true);
    activeDocument.addEventListener("pointerup", this.onToolbarDragEnd, true);
    activeDocument.addEventListener("pointercancel", this.onToolbarDragEnd, true);
    event.preventDefault();
    event.stopPropagation();
  }
  onToolbarDragMove(event) {
    if (!this.toolbarDrag || !this.toolbar || event.pointerId !== this.toolbarDrag.pointerId) {
      return;
    }
    const rect = this.toolbar.getBoundingClientRect();
    const viewport = window.visualViewport;
    const viewportLeft = viewport?.offsetLeft || 0;
    const viewportTop = viewport?.offsetTop || 0;
    const viewportWidth = Math.max(160, viewport?.width || window.innerWidth || 160);
    const viewportHeight = Math.max(120, viewport?.height || window.innerHeight || 120);
    this.toolbarManualPosition = {
      left: clamp10(this.toolbarDrag.startLeft + event.clientX - this.toolbarDrag.startX, viewportLeft + 8, Math.max(viewportLeft + 8, viewportLeft + viewportWidth - rect.width - 8)),
      top: clamp10(this.toolbarDrag.startTop + event.clientY - this.toolbarDrag.startY, viewportTop + 8, Math.max(viewportTop + 8, viewportTop + viewportHeight - rect.height - 8))
    };
    this.updateFloatingControlsPosition();
    event.preventDefault();
    event.stopPropagation();
  }
  onToolbarDragEnd(event) {
    if (!this.toolbarDrag || event?.pointerId !== this.toolbarDrag.pointerId) {
      return;
    }
    event.preventDefault();
    event.stopPropagation();
    const viewport = window.visualViewport;
    const viewportLeft = viewport?.offsetLeft || 0;
    const viewportTop = viewport?.offsetTop || 0;
    const viewportWidth = Math.max(160, viewport?.width || window.innerWidth || 160);
    const viewportHeight = Math.max(120, viewport?.height || window.innerHeight || 120);
    const rect = this.toolbar?.getBoundingClientRect?.();
    const availableWidth = Math.max(0, viewportWidth - (rect?.width || 0) - 16);
    const availableHeight = Math.max(0, viewportHeight - (rect?.height || 0) - 16);
    const position = this.toolbarManualPosition || { left: rect?.left || viewportLeft + 8, top: rect?.top || viewportTop + 8 };
    this.plugin.noteDrawSettings.toolbarPosition = {
      xRatio: availableWidth > 0 ? clamp10((position.left - viewportLeft - 8) / availableWidth, 0, 1) : 0,
      yRatio: availableHeight > 0 ? clamp10((position.top - viewportTop - 8) / availableHeight, 0, 1) : 0
    };
    this.runtimeSettings.toolbarPosition = this.plugin.noteDrawSettings.toolbarPosition;
    this.toolbarManualPosition = null;
    this.stopToolbarDrag();
    this.updateFloatingControlsPosition();
    this.plugin.scheduleSettingsSave();
  }
  stopToolbarDrag() {
    this.toolbarDrag = null;
    this.toolbar?.removeClass("is-moving");
    activeDocument.removeEventListener("pointermove", this.onToolbarDragMove, true);
    activeDocument.removeEventListener("pointerup", this.onToolbarDragEnd, true);
    activeDocument.removeEventListener("pointercancel", this.onToolbarDragEnd, true);
  }
  onBrushButtonClick(mode, event) {
    event?.preventDefault?.();
    event?.stopPropagation?.();
    if (this.brushPanelOpen && this.brushPanelMode === mode) {
      this.setBrushPanelOpen(false);
      return;
    }
    this.setBrushMode(mode);
    this.brushPanelMode = mode;
    this.setBrushPanelOpen(true);
  }
  createBrushPanel() {
    const options = [
      { mode: BRUSH_PEN, variant: BRUSH_VARIANT_DEFAULT, labelKey: "standardPen", icon: "pen-line" },
      { mode: BRUSH_PEN, variant: PEN_VARIANT_FOUNTAIN, labelKey: "fountainPen", icon: "pen-tool" },
      { mode: BRUSH_PEN, variant: PEN_VARIANT_NOTE, labelKey: "notePen", icon: "notebook-pen" },
      { mode: BRUSH_PEN, variant: PEN_VARIANT_LASER, labelKey: "laserPen", icon: "scan-line" },
      { mode: BRUSH_WATERCOLOR, variant: BRUSH_VARIANT_DEFAULT, labelKey: "standardWatercolor", icon: "paintbrush" },
      { mode: BRUSH_WATERCOLOR, variant: WATERCOLOR_VARIANT_TEXT, labelKey: "textWatercolor", icon: "highlighter" },
      { mode: BRUSH_WATERCOLOR, variant: WATERCOLOR_VARIANT_STRAIGHT, labelKey: "straightWatercolor", icon: "ruler" }
    ];
    for (const option of options) {
      const label = this.plugin.t(option.labelKey);
      const button = this.brushPanel.createEl("button", {
        cls: "notedraw-brush-option",
        attr: { type: "button", title: label, "aria-label": label }
      });
      button.dataset.noteDrawBrush = option.mode;
      button.dataset.noteDrawBrushVariant = option.variant;
      (0, import_obsidian.setIcon)(button, option.icon);
      bindNoteDrawControlTap(button, () => {
        this.brushVariants[option.mode] = option.variant;
        this.setBrushMode(option.mode);
        this.setBrushPanelOpen(false);
      });
    }
    this.syncBrushPanelButtons();
  }
  syncBrushPanelButtons() {
    this.brushPanel?.querySelectorAll(".notedraw-brush-option").forEach((button) => {
      const mode = button.dataset.noteDrawBrush;
      const variant = button.dataset.noteDrawBrushVariant;
      button.toggleAttribute("hidden", mode !== this.brushPanelMode || variant === PEN_VARIANT_NOTE && !this.supportsNoteFlow());
      button.classList.toggle("is-active", mode === this.brushMode && variant === this.currentBrushVariant());
    });
  }
  setBrushPanelOpen(open) {
    this.brushPanelOpen = Boolean(open);
    this.previewEl.toggleClass("is-brush-panel-open", this.brushPanelOpen);
    this.syncFloatingControlClasses();
    if (this.brushPanelOpen) {
      this.syncBrushPanelButtons();
      this.setPaletteOpen(false);
      this.setTextPanelOpen(false);
      this.updateFloatingControlsPosition();
    }
    this.syncSharedToolbarState();
  }
  setBrushMode(mode) {
    if (![BRUSH_PEN, BRUSH_WATERCOLOR].includes(mode)) {
      return;
    }
    this.brushMode = mode;
    this.toolMode = TOOL_DRAW;
    this.previewEl.removeClass("is-select-mode");
    if (this.isNoteFlowPenActive()) {
      this.clearSelectedStrokes();
    } else {
      this.hideSelectionMenu();
    }
    this.endTextEdit();
    this.cancelCurrentStroke();
    this.cancelSelectionDrag(true);
    this.syncCurrentBrushFields();
    this.syncPaletteInputs();
    this.syncBrushPanelButtons();
    this.updateToolButtons();
    if (this.isNoteFlowPenActive()) {
      this.scheduleMarkdownAnnotationRefresh({ layout: false, delay: 0, force: true });
    }
    this.persistInteractionSettings();
    this.syncSharedToolbarState();
    this.render();
  }
  setTextMode() {
    this.toolMode = TOOL_TEXT;
    this.previewEl.removeClass("is-select-mode");
    this.hideSelectionMenu();
    this.setPaletteOpen(false);
    this.setBrushPanelOpen(false);
    this.setTextPanelOpen(false);
    this.endTextEdit();
    this.cancelCurrentStroke();
    this.cancelSelectionDrag(true);
    this.updateToolButtons();
    this.persistInteractionSettings();
    this.syncSharedToolbarState();
    this.render();
  }
  setEditMarkdownMode() {
    this.toolMode = TOOL_EDIT_MD;
    this.previewEl.removeClass("is-select-mode");
    this.hideSelectionMenu();
    this.clearSelectedStrokes();
    this.setPaletteOpen(false);
    this.setBrushPanelOpen(false);
    this.setTextPanelOpen(false);
    this.endFloatingTextInput(true);
    this.endTextEdit();
    if (this.surfaceType === "source") {
      this.formatToolbar?.addClass("is-visible");
      this.scheduleFloatingControlsPosition();
    }
    this.cancelCurrentStroke();
    this.cancelSelectionDrag(true);
    this.updateToolButtons();
    this.persistInteractionSettings();
    this.syncSharedToolbarState();
    this.scheduleMarkdownAnnotationRefresh({ layout: false });
    this.render();
  }
  syncSharedToolbarState() {
    const sourceSurface = this.surfaceType === "source";
    this.plugin.setControllerToolbarState(this, {
      brushMode: this.brushMode,
      brushVariants: { ...this.brushVariants },
      brushPanelOpen: this.brushPanelOpen,
      brushPanelMode: this.brushPanelMode,
      brushSettings: {
        [BRUSH_PEN]: { ...this.brushSettings[BRUSH_PEN] },
        [BRUSH_WATERCOLOR]: { ...this.brushSettings[BRUSH_WATERCOLOR] }
      },
      brushVariantSettings: cloneBrushVariantSettings(this.brushVariantSettings),
      toolMode: this.toolMode,
      paletteOpen: this.paletteOpen,
      textPanelOpen: this.textPanelOpen,
      textPreset: this.textPreset,
      // Source zoom is a layout baseline. Preview zoom is an independent
      // visual scale relative to that baseline.
      readingZoom: sourceSurface ? 1 : this.readingZoom,
      sourceLayoutZoom: sourceSurface ? this.readingZoom : this.inheritedReadingLayoutZoom,
      readingZoomLayoutMode: sourceSurface
    });
  }
  applySharedToolbarState(state) {
    if (!state || this.destroyed) {
      return;
    }
    const previousToolMode = this.toolMode;
    const previousReadingZoom = this.readingZoom;
    const previousReadingZoomLayoutMode = this.readingZoomLayoutMode;
    const previousInheritedReadingLayoutZoom = this.inheritedReadingLayoutZoom;
    this.brushMode = [BRUSH_PEN, BRUSH_WATERCOLOR].includes(state.brushMode) ? state.brushMode : this.brushMode;
    for (const mode of [BRUSH_PEN, BRUSH_WATERCOLOR]) {
      this.brushVariants[mode] = normalizeBrushVariant(mode, state.brushVariants?.[mode] ?? this.brushVariants[mode]);
    }
    for (const mode of [BRUSH_PEN, BRUSH_WATERCOLOR]) {
      if (state.brushSettings?.[mode]) {
        this.brushSettings[mode] = { ...this.brushSettings[mode], ...state.brushSettings[mode] };
      }
    }
    if (state.brushVariantSettings) {
      this.brushVariantSettings = createBrushVariantSettings(state.brushVariantSettings, this.brushSettings);
    }
    const sharedToolMode = state.toolMode || this.toolMode;
    this.toolMode = this.surfaceType === "source" && this.runtimeSettings.defaultSourceEditMarkdown ? TOOL_EDIT_MD : this.surfaceType === "preview" && sharedToolMode === TOOL_EDIT_MD ? TOOL_SELECT : sharedToolMode;
    if (this.surfaceType === "source") {
      this.readingZoom = clamp10(
        Number(state.sourceLayoutZoom ?? state.readingZoom) || this.readingZoom || 1,
        MIN_READING_ZOOM,
        MAX_READING_ZOOM
      );
      this.readingZoomLayoutMode = true;
    } else {
      const legacyLayoutZoom = state.readingZoomLayoutMode === true ? state.readingZoom : 1;
      this.inheritedReadingLayoutZoom = clamp10(
        Number(state.sourceLayoutZoom ?? legacyLayoutZoom) || 1,
        MIN_READING_ZOOM,
        MAX_READING_ZOOM
      );
      this.readingZoom = clamp10(
        Number(state.sourceLayoutZoom !== void 0 && state.readingZoomLayoutMode === true ? this.readingZoom : state.readingZoom) || this.readingZoom || 1,
        MIN_READING_ZOOM,
        MAX_READING_ZOOM
      );
      this.readingZoomLayoutMode = false;
    }
    this.paletteOpen = Boolean(state.paletteOpen) && this.toolMode !== TOOL_EDIT_MD && (this.toolMode !== TOOL_SELECT || this.hasHybridSelection());
    this.brushPanelOpen = Boolean(state.brushPanelOpen) && this.toolMode === TOOL_DRAW;
    this.brushPanelMode = [BRUSH_PEN, BRUSH_WATERCOLOR].includes(state.brushPanelMode) ? state.brushPanelMode : this.brushPanelMode;
    this.textPanelOpen = Boolean(state.textPanelOpen);
    this.textPreset = state.textPreset || this.textPreset;
    this.syncCurrentBrushFields();
    this.previewEl.toggleClass("is-select-mode", this.toolMode === TOOL_SELECT);
    this.previewEl.toggleClass("is-palette-open", this.paletteOpen);
    this.previewEl.toggleClass("is-brush-panel-open", this.brushPanelOpen);
    this.previewEl.toggleClass("is-text-panel-open", this.textPanelOpen);
    if (this.isNoteFlowPenActive() && this.hasHybridSelection()) {
      this.clearSelectedStrokes();
    }
    this.syncPaletteInputs();
    this.syncBrushPanelButtons?.();
    this.syncTextPanelButtons?.();
    const zoomChanged = Math.abs(this.readingZoom - previousReadingZoom) >= 1e-3;
    const zoomPresentationChanged = zoomChanged || this.readingZoomLayoutMode !== previousReadingZoomLayoutMode || Math.abs(this.inheritedReadingLayoutZoom - previousInheritedReadingLayoutZoom) >= 1e-3;
    if (zoomPresentationChanged) {
      this.applyReadingZoom();
      this.responsiveLayoutContext = null;
      const visualReadingZoom = this.usesVisualReadingZoom();
      this.scheduleResize({ layout: !visualReadingZoom, measure: !visualReadingZoom });
    }
    this.updateToolButtons();
    this.syncFloatingControlClasses();
    if (previousToolMode !== this.toolMode && (this.ctx || this.drawingsLoaded)) {
      this.requestRender();
    }
  }
  setToolFromApi(tool, options = {}) {
    const normalized = String(tool || "").trim().toLowerCase();
    if (normalized === TOOL_EDIT_MD || normalized === "edit" || normalized === "markdown") {
      if (this.workspaceSurface) {
        return false;
      }
      this.setEditMarkdownMode();
      return true;
    }
    if (normalized === TOOL_SELECT || normalized === "selection") {
      if (this.toolMode !== TOOL_SELECT) {
        this.toggleSelectMode();
      }
      return true;
    }
    if (normalized === TOOL_TEXT) {
      this.textPreset = String(options.preset || "plain");
      this.setTextMode();
      return true;
    }
    if (normalized === TOOL_DRAW || normalized === BRUSH_PEN || normalized === BRUSH_WATERCOLOR) {
      const brush = normalized === BRUSH_WATERCOLOR || options.brush === BRUSH_WATERCOLOR ? BRUSH_WATERCOLOR : BRUSH_PEN;
      this.brushVariants[brush] = normalizeBrushVariant(brush, options.variant);
      this.setBrushMode(brush);
      return true;
    }
    return false;
  }
  currentBrushSettings() {
    if (!this.brushSettings[this.brushMode]) {
      this.brushMode = BRUSH_PEN;
    }
    return this.brushSettingsFor(this.brushMode, this.currentBrushVariant());
  }
  brushSettingsFor(mode, variant = this.brushVariants?.[mode]) {
    var _a, _b;
    const normalizedMode = mode === BRUSH_WATERCOLOR ? BRUSH_WATERCOLOR : BRUSH_PEN;
    const normalizedVariant = normalizeBrushVariant(normalizedMode, variant);
    this.brushVariantSettings || (this.brushVariantSettings = createBrushVariantSettings(null, this.brushSettings));
    (_a = this.brushVariantSettings)[normalizedMode] || (_a[normalizedMode] = {});
    (_b = this.brushVariantSettings[normalizedMode])[normalizedVariant] || (_b[normalizedVariant] = normalizeBrushToolSettings(
      null,
      this.brushSettings[normalizedMode]
    ));
    return this.brushVariantSettings[normalizedMode][normalizedVariant];
  }
  currentBrushVariant() {
    const variant = normalizeBrushVariant(this.brushMode, this.brushVariants[this.brushMode]);
    return variant === PEN_VARIANT_NOTE && !this.supportsNoteFlow() ? BRUSH_VARIANT_DEFAULT : variant;
  }
  isNoteFlowPenActive() {
    return this.toolMode === TOOL_DRAW && this.brushMode === BRUSH_PEN && this.currentBrushVariant() === PEN_VARIANT_NOTE && this.supportsNoteFlow();
  }
  isLaserPenActive() {
    return this.toolMode === TOOL_DRAW && this.brushMode === BRUSH_PEN && this.currentBrushVariant() === PEN_VARIANT_LASER;
  }
  supportsNoteFlow() {
    return this.surfaceType === "preview" && !this.embeddedSurface;
  }
  hasNoteFlowElements() {
    return (this.drawingData?.strokes || []).some((stroke) => stroke?.noteFlow?.enabled && !isConnectorStroke(stroke));
  }
  prepareNoteFlowForEditing() {
    if (this.destroyed || !this.active || !this.drawingsLoaded || !this.drawingsVisible || !this.supportsNoteFlow() || this.noteFlowActivationRepairAttempted) {
      return false;
    }
    const strokes = this.drawingData?.strokes || [];
    if (!this.hasNoteFlowElements()) {
      return false;
    }
    this.noteFlowActivationRepairAttempted = true;
    const frozen = normalizeFrozenNoteFlowLayout(this.drawingData?.noteFlowLayout);
    if (!noteFlowNeedsActivationRepair(strokes, frozen)) {
      this.restoreFrozenNoteFlowLayout();
      return false;
    }
    this.cancelFrozenNoteFlowLayoutRestore();
    this.noteFlowAnchorRepairReady = false;
    this.noteFlowAnchorRepairComplete = false;
    this.noteFlowPersistencePending = false;
    this.noteFlowOperationPending = true;
    this.scheduleMarkdownAnnotationRefresh({ layout: false, delay: 32 });
    return true;
  }
  async prepareNoteFlowForReading() {
    if (this.destroyed || this.surfaceType !== "preview" || this.active || !this.drawingsLoaded || !this.drawingsVisible || !this.supportsNoteFlow() || this.noteFlowReadingRepairAttempted || !this.hasNoteFlowElements()) {
      return false;
    }
    this.noteFlowReadingRepairAttempted = true;
    this.noteFlowReadingRepairPending = true;
    this.noteFlowPersistencePending = false;
    this.noteFlowOperationPending = true;
    const filePath = this.file?.path || "";
    const generation = this.drawingLoadGeneration;
    const surfaceGeneration = this.surfaceStateGeneration;
    try {
      this.applyReadingZoom();
      annotateVisibleMarkdownElements(this.plugin.app, this.previewEl, filePath);
      await annotateRenderedMarkdownLines(this.plugin.app, this.previewEl, filePath, { force: true });
      if (this.destroyed || this.active || surfaceGeneration !== this.surfaceStateGeneration || generation !== this.drawingLoadGeneration || this.file?.path !== filePath) {
        return false;
      }
      this.noteFlowMarkdownAnnotationComplete = true;
      const changed = this.applyNoteFlowLayout();
      this.restoreFrozenNoteFlowLayout();
      this.noteFlowOperationPending = false;
      this.noteFlowPersistencePending = false;
      this.noteFlowLayoutIncomplete = false;
      if (changed) {
        this.scheduleResize({ layout: false, measure: true });
      }
      this.render();
      return changed;
    } finally {
      this.noteFlowReadingRepairPending = false;
      this.noteFlowOperationPending = false;
      this.noteFlowPersistencePending = false;
    }
  }
  syncCurrentBrushFields() {
    const settings = this.currentBrushSettings();
    this.penColor = settings.color;
    this.penWidth = settings.width;
    this.penOpacity = settings.opacity;
    this.penCount = settings.count;
  }
  syncPaletteInputs() {
    if (this.colorInput) {
      this.colorInput.value = this.currentPaletteColor();
    }
    const selectedBackground = [
      ...this.selectedWholeElementGroups().map((group) => group.backgroundColor),
      ...this.getSelectedMarkdownBlocks().map((block) => block.backgroundColor)
    ].find((color) => isCssColor(color));
    if (this.markdownBackgroundInput && selectedBackground) {
      this.markdownBackgroundInput.value = selectedBackground;
    }
    this.syncColorSwatches();
    if (this.widthInput) {
      this.widthInput.value = String(brushWidthToPaletteSlider(this.currentPaletteWidth()));
    }
    if (this.opacityInput) {
      this.opacityInput.value = String(opacityToPaletteSlider(this.currentPaletteOpacity()));
    }
  }
  persistCurrentBrushSettings() {
    const settings = this.currentBrushSettings();
    const noteDrawSettings = this.plugin?.noteDrawSettings || {};
    noteDrawSettings.brushVariantSettings = cloneBrushVariantSettings(this.brushVariantSettings);
    if (this.brushMode === BRUSH_WATERCOLOR && this.currentBrushVariant() === BRUSH_VARIANT_DEFAULT) {
      noteDrawSettings.defaultWatercolorColor = settings.color;
      noteDrawSettings.defaultWatercolorWidth = settings.width;
      noteDrawSettings.defaultWatercolorOpacity = settings.opacity;
      this.brushSettings[BRUSH_WATERCOLOR] = { ...settings };
    } else if (this.brushMode === BRUSH_PEN && this.currentBrushVariant() === BRUSH_VARIANT_DEFAULT) {
      noteDrawSettings.defaultPenColor = settings.color;
      noteDrawSettings.defaultPenWidth = settings.width;
      noteDrawSettings.defaultPenOpacity = settings.opacity;
      this.brushSettings[BRUSH_PEN] = { ...settings };
    }
    this.plugin.noteDrawSettings = noteDrawSettings;
    this.plugin.scheduleSettingsSave?.();
  }
  persistInteractionSettings() {
    const noteDrawSettings = this.plugin?.noteDrawSettings || {};
    noteDrawSettings.lastToolMode = normalizeToolMode(this.toolMode);
    noteDrawSettings.lastBrushMode = this.brushMode === BRUSH_WATERCOLOR ? BRUSH_WATERCOLOR : BRUSH_PEN;
    noteDrawSettings.lastPenVariant = normalizeBrushVariant(BRUSH_PEN, this.brushVariants[BRUSH_PEN]);
    noteDrawSettings.lastWatercolorVariant = normalizeBrushVariant(BRUSH_WATERCOLOR, this.brushVariants[BRUSH_WATERCOLOR]);
    noteDrawSettings.lastTextPreset = normalizeTextPreset(this.textPreset);
    noteDrawSettings.lastReadingZoom = clamp10(Number(this.readingZoom) || 1, MIN_READING_ZOOM, MAX_READING_ZOOM);
    this.plugin.noteDrawSettings = noteDrawSettings;
    this.plugin.scheduleSettingsSave?.();
  }
  updateToolButtons() {
    const penActive = this.toolMode === TOOL_DRAW && this.brushMode === BRUSH_PEN;
    const watercolorActive = this.toolMode === TOOL_DRAW && this.brushMode === BRUSH_WATERCOLOR;
    const sourceMarkdownEditActive = this.surfaceType === "source" && this.toolMode === TOOL_EDIT_MD;
    this.applyBrushButtonState(this.penButton, this.brushSettingsFor(BRUSH_PEN), penActive);
    this.applyBrushButtonState(this.watercolorButton, this.brushSettingsFor(BRUSH_WATERCOLOR), watercolorActive);
    this.textButton?.classList.toggle("is-active", this.toolMode === TOOL_TEXT || this.textPanelOpen);
    this.textButton?.toggleAttribute("hidden", false);
    this.selectButton?.classList.toggle("is-active", this.toolMode === TOOL_SELECT);
    this.editMarkdownButton?.classList.toggle("is-active", this.toolMode === TOOL_EDIT_MD);
    if (this.surfaceType === "source") {
      this.formatToolbar?.classList.toggle("is-visible", sourceMarkdownEditActive);
    }
    const selectedElements = this.hasHybridSelection();
    const paletteDisabled = this.toolMode === TOOL_EDIT_MD || this.toolMode === TOOL_SELECT && !selectedElements;
    this.paletteButton?.toggleAttribute("disabled", paletteDisabled);
    this.paletteButton?.classList.toggle("is-selection-available", this.toolMode === TOOL_SELECT && selectedElements);
    const markdownSelected = this.getSelectedMarkdownBlocks().length > 0 || this.selectedWholeElementGroups().some((group) => group.boxed);
    this.markdownBackgroundInput?.toggleAttribute("hidden", !markdownSelected);
    this.clearMarkdownBackgroundButton?.toggleAttribute("hidden", !markdownSelected);
    this.previewEl.toggleClass("is-watercolor-mode", this.toolMode === TOOL_DRAW && this.brushMode === BRUSH_WATERCOLOR);
    this.previewEl.toggleClass("is-edit-md-mode", this.toolMode === TOOL_EDIT_MD);
  }
  createTextPanel() {
    const groups = [
      {
        labelKey: "textGroup",
        items: [
          { id: "plain", labelKey: "textPlain", icon: "type" },
          { id: "rectangle", labelKey: "outlineButton", icon: "square" },
          { id: "circle", labelKey: "pillButton", icon: "square" },
          { id: "arrow", labelKey: "arrow", icon: "move-up-right" }
        ]
      },
      {
        labelKey: "importGroup",
        items: [
          { id: "image", labelKey: "image", icon: "image" },
          { id: "video", labelKey: "video", icon: "film" },
          { id: "attachment", labelKey: "file", icon: "paperclip" },
          { id: "button", labelKey: "button", icon: "square" }
        ]
      },
      {
        labelKey: "previewGroup",
        items: [
          { id: "markdown", labelKey: "markdown", icon: "pilcrow" },
          { id: "html", labelKey: "html", icon: "code" },
          { id: "note", labelKey: "note", icon: "file-text" },
          { id: "mindMap", labelKey: "mindMap", icon: "git-fork" }
        ]
      }
    ];
    for (const group of groups) {
      const groupEl = this.textPanel.createDiv({ cls: "notedraw-text-group" });
      groupEl.createDiv({ cls: "notedraw-text-group-label", text: this.plugin.t(group.labelKey) });
      const gridEl = groupEl.createDiv({ cls: "notedraw-text-grid" });
      for (const item of group.items) {
        const label = this.plugin.t(item.labelKey);
        const button = gridEl.createEl("button", {
          cls: "notedraw-text-option",
          attr: { type: "button", title: label, "aria-label": label }
        });
        button.dataset.noteDrawTextPreset = item.id;
        (0, import_obsidian.setIcon)(button, item.icon);
        button.createSpan({ text: label });
        bindNoteDrawControlTap(button, () => {
          if (item.id === "mindMap") {
            this.setTextPanelOpen(false);
            this.scheduleMindMapFilePicker();
            return;
          }
          this.textPreset = item.id;
          this.pendingButtonCommandPicker = item.id === "button";
          this.setTextMode();
          this.setTextPanelOpen(false);
          this.syncTextPanelButtons();
          this.syncSharedToolbarState();
        });
      }
    }
    this.syncTextPanelButtons();
  }
  scheduleMindMapFilePicker() {
    this.plugin.scheduleMindMapFilePicker(this);
  }
  openMindMapFilePicker() {
    this.plugin.openMindMapFilePicker(this);
  }
  syncTextPanelButtons() {
    this.textPanel?.querySelectorAll(".notedraw-text-option").forEach((button) => {
      button.classList.toggle("is-active", button.dataset.noteDrawTextPreset === this.textPreset);
    });
  }
  createSelectionMenu() {
    const actions = [
      { icon: "terminal", key: "buttonCommand", action: () => this.openSelectedButtonCommandPicker() },
      { icon: "copy", key: "copyElement", action: () => this.copySelectedElements() },
      { icon: "clipboard-paste", key: "pasteElement", action: () => this.pasteCopiedElements() },
      { icon: "external-link", key: "openSourceNote", action: () => void this.openSelectedMindMapSource() },
      { icon: "repeat-2", key: "toggleFlowMode", action: () => this.toggleSelectedFlowMode() },
      { icon: "bring-to-front", key: "bringToFront", action: () => this.reorderSelectedStrokes("front") },
      { icon: "move-up", key: "moveForward", action: () => this.reorderSelectedStrokes("forward") },
      { icon: "move-down", key: "moveBackward", action: () => this.reorderSelectedStrokes("backward") },
      { icon: "send-to-back", key: "sendToBack", action: () => this.reorderSelectedStrokes("back") },
      { icon: "box-select", key: "boxElements", action: () => this.toggleSelectedBoxOrButtonStyle() },
      { icon: "lock", key: "lockElement", action: () => this.toggleSelectedStrokeLock() }
    ];
    for (const item of actions) {
      const title = this.plugin.t(item.key);
      const button = this.selectionMenu.createEl("button", {
        cls: "notedraw-selection-menu-button",
        attr: { type: "button", title, "aria-label": title }
      });
      button.dataset.noteDrawTitleKey = item.key;
      (0, import_obsidian.setIcon)(button, item.icon);
      button.addEventListener("click", (event) => {
        event.preventDefault();
        event.stopPropagation();
        item.action();
      });
    }
    this.syncSelectionMenuButtons();
  }
  syncSelectionMenuButtons() {
    if (!this.selectionMenu) {
      return;
    }
    const selectedLocks = [
      ...this.getSelectedStrokeIndexes().map((index) => Boolean(this.drawingData.strokes[index]?.locked)),
      ...this.getSelectedMarkdownBlocks().map((block) => Boolean(block.locked))
    ];
    const locked = selectedLocks.length > 0 && selectedLocks.every(Boolean);
    const lockButton = this.selectionMenu.querySelector('[data-note-draw-title-key="lockElement"], [data-note-draw-title-key="unlockElement"]');
    if (lockButton) {
      const key = locked ? "unlockElement" : "lockElement";
      lockButton.dataset.noteDrawTitleKey = key;
      this.plugin.setAccessibleLabel(lockButton, key);
      (0, import_obsidian.setIcon)(lockButton, locked ? "unlock" : "lock");
    }
    const pasteButton = this.selectionMenu.querySelector('[data-note-draw-title-key="pasteElement"]');
    pasteButton?.toggleAttribute("disabled", !this.plugin.elementClipboard?.strokes?.length);
    const sourceButton = this.selectionMenu.querySelector('[data-note-draw-title-key="openSourceNote"]');
    sourceButton?.toggleAttribute("hidden", !this.selectedMindMapSource());
    const commandButton = this.selectionMenu.querySelector('[data-note-draw-title-key="buttonCommand"]');
    commandButton?.toggleAttribute("hidden", !this.selectedCommandTarget());
    const flowSelection = this.selectedFlowModeElements();
    const flowModeButton = this.selectionMenu.querySelector('[data-note-draw-title-key="toggleFlowMode"], [data-note-draw-title-key="convertToNoteFlow"], [data-note-draw-title-key="convertToFloating"]');
    if (flowModeButton) {
      const key = flowSelection.noteFlowCount === flowSelection.total ? "convertToFloating" : flowSelection.floatingCount === flowSelection.total ? "convertToNoteFlow" : "toggleFlowMode";
      flowModeButton.dataset.noteDrawTitleKey = key;
      this.plugin.setAccessibleLabel(flowModeButton, key);
      (0, import_obsidian.setIcon)(flowModeButton, key === "convertToFloating" ? "pin" : key === "convertToNoteFlow" ? "wrap-text" : "repeat-2");
      flowModeButton.toggleAttribute("hidden", !this.supportsNoteFlow() || flowSelection.total === 0);
    }
    const selectedBox = this.selectedWholeElementGroups().find((group) => group.boxed);
    const boxButton = this.selectionMenu.querySelector('[data-note-draw-title-key="boxElements"], [data-note-draw-title-key="fillBoxElements"], [data-note-draw-title-key="unboxElements"], [data-note-draw-title-key="outlineButton"]');
    if (boxButton) {
      const buttonIndex = this.getSelectedMarkdownBlocks().length === 0 && this.getSelectedStrokeIndexes().length === 1 ? this.getSelectedStrokeIndexes()[0] : -1;
      const selectedButton = buttonIndex >= 0 ? this.drawingData?.strokes?.[buttonIndex] : null;
      const key = selectedButton?.uiRole === "button" && !selectedButton.groupId ? normalizeButtonStyle(selectedButton.buttonStyle) === "solid" ? "outlineButton" : "fillBoxElements" : !selectedBox ? "boxElements" : selectedBox.backgroundColor ? "unboxElements" : "fillBoxElements";
      boxButton.dataset.noteDrawTitleKey = key;
      this.plugin.setAccessibleLabel(boxButton, key);
      (0, import_obsidian.setIcon)(boxButton, key === "boxElements" ? "box-select" : key === "unboxElements" ? "package-open" : "square");
    }
  }
  selectedButtonCommandIndex() {
    const indexes = this.getSelectedStrokeIndexes();
    if (this.getSelectedMarkdownBlocks().length !== 0 || indexes.length !== 1) {
      return -1;
    }
    const stroke = this.drawingData?.strokes?.[indexes[0]];
    return stroke?.uiRole === "button" && isTextLikeStroke(stroke) ? indexes[0] : -1;
  }
  selectedCommandTarget() {
    const indexes = this.getSelectedStrokeIndexes();
    const blocks = this.getSelectedMarkdownBlocks();
    if (indexes.length === 1 && blocks.length === 0) {
      const stroke = this.drawingData?.strokes?.[indexes[0]];
      return this.elementHasCommandFrame(stroke) ? { type: "stroke", index: indexes[0], elementId: strokeElementId(stroke) } : null;
    }
    if (indexes.length === 0 && blocks.length === 1) {
      return this.elementHasCommandFrame(blocks[0]) ? { type: "markdown", id: blocks[0].id } : null;
    }
    return null;
  }
  elementHasCommandFrame(item) {
    if (!item) {
      return false;
    }
    const ownFrame = Boolean(item.boxed || item.borderColor || item.backgroundColor);
    const groupFrame = Boolean(item.groupId && this.elementGroup(item.groupId)?.boxed);
    return ownFrame || groupFrame;
  }
  clearElementCommand(item) {
    if (!item) {
      return false;
    }
    const changed = Boolean(String(item.commandId || "").trim() || String(item.commandName || "").trim());
    item.commandId = "";
    item.commandName = "";
    return changed;
  }
  clearElementGroupCommands(groupId) {
    let changed = false;
    for (const index of this.groupMemberStrokeIndexes(groupId)) {
      changed = this.clearElementCommand(this.drawingData?.strokes?.[index]) || changed;
    }
    for (const block of this.groupMemberMarkdownBlocks(groupId)) {
      changed = this.clearElementCommand(block) || changed;
    }
    return changed;
  }
  toggleSelectedBoxOrButtonStyle() {
    const index = this.getSelectedMarkdownBlocks().length === 0 && this.getSelectedStrokeIndexes().length === 1 ? this.getSelectedStrokeIndexes()[0] : -1;
    const stroke = index >= 0 ? this.drawingData?.strokes?.[index] : null;
    if (stroke?.uiRole === "button" && !stroke.groupId) {
      const historyBefore = this.captureDrawingHistorySnapshot();
      stroke.buttonStyle = normalizeButtonStyle(stroke.buttonStyle) === "solid" ? "outline" : "solid";
      stroke.boxed = true;
      this.redoStack = [];
      this.invalidateStaticCache();
      this.plugin.scheduleDrawingSave(this.file, this.drawingData, { userOperation: true });
      this.recordDrawingHistory(historyBefore);
      this.syncSelectionMenuButtons();
      this.render();
      return true;
    }
    this.toggleSelectedElementBox();
    return true;
  }
  openSelectedButtonCommandPicker() {
    const target = this.selectedCommandTarget();
    if (!target) {
      return false;
    }
    this.hideSelectionMenu();
    this.plugin.openButtonCommandPicker(this, target);
    return true;
  }
  setSelectedElementCommand(target, command) {
    const item = target?.type === "markdown" ? this.markdownBlockRecords().find((block) => block.id === target.id) : this.drawingData?.strokes?.[Number(target?.index)];
    if (!item || !command?.id || !this.elementHasCommandFrame(item)) {
      return false;
    }
    const historyBefore = this.captureDrawingHistorySnapshot();
    item.commandId = String(command.id);
    item.commandName = String(command.name || command.id);
    this.redoStack = [];
    this.invalidateStaticCache();
    this.plugin.scheduleDrawingSave(this.file, this.drawingData, { userOperation: true });
    this.recordDrawingHistory(historyBefore);
    if (target?.type === "markdown") {
      this.selectedStrokeIndexes.clear();
      this.selectedStrokeIndex = -1;
      this.selectedMarkdownBlockIds = /* @__PURE__ */ new Set([item.id]);
    } else {
      this.setSelectedStrokes(Number(target?.index));
    }
    this.syncSelectionMenuButtons();
    this.render();
    new import_obsidian.Notice(this.plugin.t("commandAssigned", { name: item.commandName }));
    return true;
  }
  setSelectedButtonCommand(index, command) {
    return this.setSelectedElementCommand({ type: "stroke", index: Number(index) }, command);
  }
  selectedFlowModeElements() {
    const strokes = this.getSelectedStrokeIndexes().filter((index) => {
      const stroke = this.drawingData?.strokes?.[index];
      return stroke && !stroke.locked && !isConnectorStroke(stroke);
    });
    const markdownBlocks = this.getSelectedMarkdownBlocks().filter((block) => !block.locked);
    const noteFlowCount = strokes.filter((index) => Boolean(normalizeNoteFlow(this.drawingData.strokes[index]?.noteFlow))).length + markdownBlocks.filter((block) => !block.floating).length;
    const total = strokes.length + markdownBlocks.length;
    return {
      strokes,
      markdownBlocks,
      noteFlowCount,
      floatingCount: total - noteFlowCount,
      total
    };
  }
  selectedMindMapSource() {
    for (const index of this.getSelectedStrokeIndexes()) {
      const node = normalizeMindMapNode(this.drawingData.strokes[index]?.mindMapNode);
      if (node?.sourcePath) {
        return node;
      }
    }
    return null;
  }
  async openSelectedMindMapSource() {
    const node = this.selectedMindMapSource();
    const file2 = node ? this.plugin.resolveApiFile(node.sourcePath, this.file?.path || "") : null;
    if (!file2) {
      return false;
    }
    this.hideSelectionMenu();
    const leaf = this.plugin.app.workspace.getLeaf(false);
    await leaf.openFile(file2, Number.isFinite(node.sourceLine) && node.sourceLine >= 0 ? { eState: { line: node.sourceLine } } : void 0);
    const editor = leaf.view?.editor;
    if (editor && Number.isFinite(node.sourceLine) && node.sourceLine >= 0) {
      editor.setCursor({ line: node.sourceLine, ch: 0 });
      editor.scrollIntoView?.({ from: { line: node.sourceLine, ch: 0 }, to: { line: node.sourceLine, ch: 0 } }, true);
    }
    return true;
  }
  showSelectionMenu(clientPoint = null) {
    if (!this.hasHybridSelection() || !this.selectionMenu) {
      return;
    }
    this.syncSelectionMenuButtons();
    const fallback = this.getSelectedFrameCanvasRect();
    const canvasRect = this.canvas.getBoundingClientRect();
    const x = Number.isFinite(clientPoint?.x) ? clientPoint.x : fallback ? canvasRect.left + fallback.x + fallback.width / 2 : canvasRect.left + 20;
    const y = Number.isFinite(clientPoint?.y) ? clientPoint.y : fallback ? canvasRect.top + fallback.y : canvasRect.top + 20;
    const desiredWidth = Math.max(210, this.selectionMenu.getBoundingClientRect().width || 276);
    const height = Math.max(38, this.selectionMenu.getBoundingClientRect().height || 38);
    const viewport = window.visualViewport;
    const viewportTop = viewport?.offsetTop || 0;
    const viewportLeft = viewport?.offsetLeft || 0;
    const viewportWidth = Math.max(160, viewport?.width || window.innerWidth || 160);
    const viewportHeight = Math.max(120, viewport?.height || window.innerHeight || 120);
    const width = Math.min(desiredWidth, Math.max(144, viewportWidth - 16));
    const left = clamp10(Math.round(x - width / 2), viewportLeft + 8, Math.max(viewportLeft + 8, viewportLeft + viewportWidth - width - 8));
    const top = clamp10(Math.round(y - height - 14), viewportTop + 8, Math.max(viewportTop + 8, viewportTop + viewportHeight - height - 8));
    setNoteDrawCssProps(this.selectionMenu, {
      "--notedraw-selection-menu-left": `${left}px`,
      "--notedraw-selection-menu-top": `${top}px`
    });
    this.selectionMenuOpen = true;
    this.selectionMenu.addClass("is-visible");
    this.previewEl.addClass("is-selection-menu-open");
    this.syncFloatingControlClasses();
  }
  hideSelectionMenu() {
    this.selectionMenuOpen = false;
    this.selectionMenu?.removeClass("is-visible");
    this.previewEl?.removeClass("is-selection-menu-open");
    this.syncFloatingControlClasses();
  }
  shouldToggleSelectionOnLongPress(action) {
    if (this.toolMode !== TOOL_SELECT || !this.hasHybridSelection() || !action) {
      return false;
    }
    return [
      "select-stroke",
      "edit-stroke-or-drag",
      "drag-stroke-or-toggle",
      "select-markdown",
      "edit-markdown-or-drag",
      "drag-markdown-or-toggle"
    ].includes(action.type);
  }
  toggleSelectionFromLongPress(action) {
    if (!this.shouldToggleSelectionOnLongPress(action)) {
      return false;
    }
    if (["select-stroke", "edit-stroke-or-drag", "drag-stroke-or-toggle"].includes(action.type)) {
      this.toggleStrokeSelection(Number(action.index));
      return true;
    }
    if (["select-markdown", "edit-markdown-or-drag", "drag-markdown-or-toggle"].includes(action.type)) {
      this.toggleMarkdownBlockSelection(action.element);
      return true;
    }
    return false;
  }
  startSelectionLongPress(event, options = {}) {
    this.clearSelectionLongPress();
    if (!this.hasHybridSelection() && !options.pendingAction) {
      return;
    }
    this.selectionLongPressState = {
      pointerId: event.pointerId,
      client: { x: event.clientX, y: event.clientY },
      pendingAction: options.pendingAction || null
    };
    this.selectionLongPressTimer = window.setTimeout(() => {
      if (!this.selectionLongPressState || this.selectionLongPressState.pointerId !== this.activePointerId || this.dragStrokeMoved || this.resizeSelectionMoved) {
        return;
      }
      const state = this.selectionLongPressState;
      this.selectionLongPressConsumedPointerId = state.pointerId;
      const toggledSelection = this.toggleSelectionFromLongPress(state.pendingAction);
      if (!toggledSelection && state.pendingAction && ["select-stroke", "select-markdown", "select-group"].includes(state.pendingAction.type)) {
        this.applyPendingSelectionTap(state.pendingAction);
      }
      this.pendingSelectionTap = null;
      this.releasePointerCapture(state.pointerId);
      if (this.draggingStroke) {
        this.clearSelectedStrokeDragState();
      }
      if (this.hasHybridSelection()) {
        this.showSelectionMenu(state.client);
      }
      this.clearSelectionLongPress();
      this.render();
    }, this.longPressDelayMs());
  }
  clearSelectionLongPress() {
    if (this.selectionLongPressTimer) {
      window.clearTimeout(this.selectionLongPressTimer);
      this.selectionLongPressTimer = null;
    }
    this.selectionLongPressState = null;
  }
  onCanvasContextMenu(event) {
    if (!this.active) {
      return;
    }
    if (this.shouldPassThroughHeaderPoint(event)) {
      return;
    }
    if (this.isNoteFlowPenActive()) {
      event.preventDefault();
      event.stopPropagation();
      return;
    }
    const point = this.eventToPoint(event);
    const hitStrokeIndex = this.findStrokeAt(point, { x: event.clientX, y: event.clientY });
    if (hitStrokeIndex >= 0) {
      if (!this.isStrokeSelected(hitStrokeIndex)) {
        this.setSelectedStrokes(hitStrokeIndex);
      }
      this.clearSelectionLongPress();
      this.showSelectionMenu({ x: event.clientX, y: event.clientY });
      this.render();
      event.preventDefault();
      event.stopPropagation();
      return;
    }
    if (this.hasHybridSelection() && this.selectedStrokeFrameContains(point)) {
      this.clearSelectionLongPress();
      this.showSelectionMenu({ x: event.clientX, y: event.clientY });
      event.preventDefault();
      event.stopPropagation();
    }
  }
  onPreviewContextMenu(event) {
    if (!this.active || this.shouldPassThroughHeaderPoint(event)) {
      return;
    }
    if (this.isNoteFlowPenActive()) {
      return;
    }
    const target = event.target;
    const targetShell = target?.closest?.(".notedraw-shell");
    if (targetShell && targetShell !== this.previewEl) {
      return;
    }
    if (target?.closest?.(NOTEDRAW_OWNED_MUTATION_SELECTOR)) {
      const embed = target.closest(".notedraw-embed");
      const index = Number(embed?.dataset?.noteDrawStrokeIndex);
      if (!Number.isInteger(index) || index < 0 || !this.drawingData?.strokes?.[index]) {
        return;
      }
      if (!this.isStrokeSelected(index)) {
        this.setSelectedStrokes(index);
      }
      this.clearSelectionLongPress();
      this.showSelectionMenu({ x: event.clientX, y: event.clientY });
      this.render();
      event.preventDefault();
      event.stopPropagation();
      return;
    }
    if (target?.closest?.(".notedraw-toolbar, .notedraw-palette-panel, .notedraw-brush-panel, .notedraw-text-panel, .notedraw-selection-menu, .notedraw-format-toolbar")) {
      return;
    }
    const point = this.eventToPoint(event);
    const hitStrokeIndex = this.findStrokeAt(point, { x: event.clientX, y: event.clientY });
    if (hitStrokeIndex < 0 && !(this.hasHybridSelection() && this.selectedStrokeFrameContains(point))) {
      return;
    }
    if (hitStrokeIndex >= 0 && !this.isStrokeSelected(hitStrokeIndex)) {
      this.setSelectedStrokes(hitStrokeIndex);
    }
    this.clearSelectionLongPress();
    this.showSelectionMenu({ x: event.clientX, y: event.clientY });
    this.render();
    event.preventDefault();
    event.stopPropagation();
  }
  createFormatToolbar() {
    this.formatToolbar = createNoteDrawControlElement(this.floatingControlsHost || this.previewEl, "notedraw-format-toolbar");
    this.formatToolbar.addEventListener("mousedown", (event) => {
      if (event.target?.closest?.("button")) {
        event.preventDefault();
      }
    });
    this.createFormatMoveButton();
    this.createFormatButton("align-left", "alignLeft", "align-left", () => this.applyTextAlignment("left"));
    this.createFormatButton("align-center", "alignCenter", "align-center", () => this.applyTextAlignment("center"));
    this.createFormatButton("align-right", "alignRight", "align-right", () => this.applyTextAlignment("right"));
    this.createFormatButton("align-justify", "alignJustify", "align-justify", () => this.applyTextAlignment("justify"));
    this.createFormatButton("bold", "bold", "bold", () => this.applyTextInlineFormat("strong"));
    this.createFormatButton("italic", "italic", "italic", () => this.applyTextInlineFormat("em"));
    this.createFormatButton("underline", "underline", "underline", () => this.applyTextInlineFormat("u"));
    this.createFormatButton("code-2", "inlineCode", "code", () => this.applyTextInlineFormat("code"));
    this.createFormatButton("keyboard", "keyboardTag", "kbd", () => this.applyTextInlineFormat("kbd"));
    this.createFormatButton("superscript", "superscript", "sup", () => this.applyTextInlineFormat("sup"));
    this.createFormatButton("subscript", "subscript", "sub", () => this.applyTextInlineFormat("sub"));
    this.createFormatButton("square-code", "codeBlock", "block-code", () => this.applyTextBlockFormat("code"));
    this.createFormatButton("highlighter", "highlight", "mark", () => this.applyTextInlineFormat("mark", { backgroundColor: this.formatHighlightInput?.value || "#fff59d" }));
    this.createFormatButton("wrap-text", "insertBreak", "br", () => this.insertTextBreak());
    this.createFormatButton("eraser", "clearFormat", "clear-format", () => this.clearTextFormat());
    this.formatColorInput = this.formatToolbar.createEl("input", {
      cls: "notedraw-format-color",
      attr: {
        type: "color",
        value: "#e53935",
        title: this.plugin.t("textColor"),
        "aria-label": this.plugin.t("textColor")
      }
    });
    this.formatColorInput.addEventListener("input", () => this.applyTextInlineFormat("span", { color: this.formatColorInput.value }));
    this.formatHighlightInput = this.formatToolbar.createEl("input", {
      cls: "notedraw-format-color",
      attr: {
        type: "color",
        value: "#fff59d",
        title: this.plugin.t("highlightColor"),
        "aria-label": this.plugin.t("highlightColor")
      }
    });
    this.formatHighlightInput.addEventListener("input", () => this.applyTextInlineFormat("mark", { backgroundColor: this.formatHighlightInput.value }));
    this.formatSizeSelect = this.formatToolbar.createEl("select", {
      cls: "notedraw-format-size",
      attr: { title: this.plugin.t("textSize"), "aria-label": this.plugin.t("textSize") }
    });
    [
      ["", this.plugin.t("size")],
      ["0.85em", "S"],
      ["1em", "M"],
      ["1.25em", "L"],
      ["1.5em", "XL"],
      ["2em", "XXL"]
    ].forEach(([value, label]) => {
      this.formatSizeSelect.createEl("option", { text: label, attr: { value } });
    });
    this.formatSizeSelect.addEventListener("change", () => {
      const size = this.formatSizeSelect.value;
      if (size) {
        this.applyTextInlineFormat("span", { fontSize: size });
      }
      this.formatSizeSelect.value = "";
    });
  }
  createFormatMoveButton() {
    const button = this.formatToolbar.createEl("button", {
      cls: "notedraw-format-button notedraw-format-move-button",
      attr: { type: "button", title: this.plugin.t("movePanel"), "aria-label": this.plugin.t("movePanel") }
    });
    (0, import_obsidian.setIcon)(button, "move");
    button.addEventListener("mousedown", (event) => event.preventDefault());
    button.addEventListener("pointerdown", (event) => this.startFormatToolbarDrag(event));
    return button;
  }
  createFormatButton(icon, titleKey, id, action) {
    const title = this.plugin.t(titleKey);
    const button = this.formatToolbar.createEl("button", {
      cls: "notedraw-format-button",
      attr: { type: "button", title, "aria-label": title }
    });
    button.dataset.noteDrawTitleKey = titleKey;
    button.dataset.noteDrawFormat = id;
    (0, import_obsidian.setIcon)(button, icon);
    button.addEventListener("mousedown", (event) => event.preventDefault());
    button.addEventListener("click", (event) => {
      event.preventDefault();
      event.stopPropagation();
      action();
    });
    return button;
  }
  sourceCodeMirrorView() {
    return this.surfaceType === "source" ? getCodeMirrorView(this.view, this.previewEl) : null;
  }
  bindSourceFormatToolbarEvents() {
    if (this.surfaceType !== "source") {
      return;
    }
    this.sourceFormatToolbarCleanup?.();
    this.sourceFormatToolbarCleanup = null;
    const cmView = this.sourceCodeMirrorView();
    const dom = cmView?.dom;
    if (!dom?.addEventListener) {
      if (this.sourceFormatToolbarRetryTimer === null && !this.destroyed) {
        this.sourceFormatToolbarRetryTimer = window.setTimeout(() => {
          this.sourceFormatToolbarRetryTimer = null;
          this.bindSourceFormatToolbarEvents();
        }, 120);
      }
      return;
    }
    if (this.sourceFormatToolbarRetryTimer !== null) {
      window.clearTimeout(this.sourceFormatToolbarRetryTimer);
      this.sourceFormatToolbarRetryTimer = null;
    }
    const refresh = () => {
      if (this.toolMode === TOOL_EDIT_MD) {
        this.formatToolbar?.addClass("is-visible");
        this.positionFormatToolbar();
      }
    };
    for (const type of ["mouseup", "keyup", "input", "scroll", "focus"]) {
      dom.addEventListener(type, refresh, true);
    }
    this.sourceFormatToolbarCleanup = () => {
      for (const type of ["mouseup", "keyup", "input", "scroll", "focus"]) {
        dom.removeEventListener(type, refresh, true);
      }
    };
  }
  sourceSelectionRange() {
    if (this.currentEditor && this.currentEditorEmbedded) {
      return null;
    }
    const cmView = this.sourceCodeMirrorView();
    const selection = cmView?.state?.selection?.main;
    if (!cmView || !selection || !Number.isFinite(selection.from) || !Number.isFinite(selection.to)) {
      return null;
    }
    return { cmView, from: selection.from, to: selection.to };
  }
  dispatchSourceSelection(changes, selection = null) {
    const range = this.sourceSelectionRange();
    if (!range || !Array.isArray(changes) || !changes.length) {
      return false;
    }
    const nextSelection = selection || {
      anchor: range.from,
      head: range.from
    };
    range.cmView.focus?.();
    range.cmView.dispatch({
      changes,
      selection: nextSelection,
      scrollIntoView: false
    });
    this.formatToolbar?.addClass("is-visible");
    this.positionFormatToolbar();
    return true;
  }
  sourceInlineFormatMarkers(tagName, styles = {}) {
    if (tagName === "strong") {
      return ["**", "**"];
    }
    if (tagName === "em") {
      return ["*", "*"];
    }
    if (tagName === "code") {
      return ["`", "`"];
    }
    if (tagName === "kbd") {
      return ["<kbd>", "</kbd>"];
    }
    if (tagName === "sup" || tagName === "sub" || tagName === "u") {
      return [`<${tagName}>`, `</${tagName}>`];
    }
    if (tagName === "mark") {
      return styles.backgroundColor ? [`<mark style="background-color: ${styles.backgroundColor};">`, "</mark>"] : ["==", "=="];
    }
    if (tagName === "span") {
      const css = Object.entries(styles).filter(([key, value]) => value && isSafeCssSize(value) || key === "color" && isCssColor(value)).map(([key, value]) => `${key.replace(/[A-Z]/g, (letter) => `-${letter.toLowerCase()}`)}: ${value}`).join("; ");
      return css ? [`<span style="${css}">`, "</span>"] : ["", ""];
    }
    return ["", ""];
  }
  applySourceTextInlineFormat(tagName, styles = {}) {
    const range = this.sourceSelectionRange();
    if (!range || range.from === range.to) {
      return false;
    }
    const selected = range.cmView.state.sliceDoc(range.from, range.to);
    const [prefix, suffix] = this.sourceInlineFormatMarkers(tagName, styles);
    if (!prefix && !suffix) {
      return false;
    }
    return this.dispatchSourceSelection([
      { from: range.from, to: range.to, insert: `${prefix}${selected}${suffix}` }
    ], {
      anchor: range.from + prefix.length,
      head: range.from + prefix.length + selected.length
    });
  }
  applySourceTextBlockFormat(kind) {
    const range = this.sourceSelectionRange();
    if (!range || kind !== "code") {
      return false;
    }
    const selected = range.cmView.state.sliceDoc(range.from, range.to);
    const insert = `
\`\`\`
${selected}
\`\`\`
`;
    return this.dispatchSourceSelection([{ from: range.from, to: range.to, insert }], {
      anchor: range.from + 5,
      head: range.from + 5 + selected.length
    });
  }
  sourceTextAlignmentMarkers(alignment) {
    const safeAlignment = ["left", "center", "right", "justify"].includes(alignment) ? alignment : "left";
    return [`<div style="text-align: ${safeAlignment};">`, "</div>"];
  }
  applySourceTextAlignment(alignment) {
    const range = this.sourceSelectionRange();
    if (!range) {
      return false;
    }
    const source = range.cmView.state.doc.toString();
    const from = source.lastIndexOf("\n", Math.max(0, range.from - 1)) + 1;
    const lineEnd = source.indexOf("\n", range.to);
    const to = lineEnd < 0 ? source.length : lineEnd;
    const selected = source.slice(from, to);
    if (!selected.trim()) {
      return false;
    }
    const [prefix, suffix] = this.sourceTextAlignmentMarkers(alignment);
    const selectionFrom = prefix.length + (range.from - from);
    const selectionTo = selectionFrom + (range.to - range.from);
    return this.dispatchSourceSelection([{
      from,
      to,
      insert: `${prefix}${selected}${suffix}`
    }], {
      anchor: from + selectionFrom,
      head: from + selectionTo
    });
  }
  insertSourceTextBreak() {
    const range = this.sourceSelectionRange();
    if (!range) {
      return false;
    }
    return this.dispatchSourceSelection([{ from: range.from, to: range.to, insert: "\n" }], {
      anchor: range.from + 1,
      head: range.from + 1
    });
  }
  clearSourceTextFormat() {
    const range = this.sourceSelectionRange();
    if (!range || range.from === range.to) {
      return false;
    }
    const selected = range.cmView.state.sliceDoc(range.from, range.to).replace(/\*\*|__|~~|`|==/g, "").replace(/<\/?(?:u|kbd|sup|sub|mark|span)(?:\s[^>]*)?>/gi, "");
    return this.dispatchSourceSelection([{ from: range.from, to: range.to, insert: selected }], {
      anchor: range.from,
      head: range.from + selected.length
    });
  }
  onDocumentSelectionChange() {
    if (this.surfaceType === "source" && this.toolMode === TOOL_EDIT_MD && !this.currentEditorEmbedded) {
      this.positionFormatToolbar();
      return;
    }
    if (!this.currentEditor || !this.formatToolbar) {
      return;
    }
    const selection = window.getSelection?.();
    if (!selection || !selection.rangeCount) {
      return;
    }
    const range = selection.getRangeAt(0);
    if (!this.currentEditor.contains(range.commonAncestorContainer)) {
      return;
    }
    this.currentTextRange = range.cloneRange();
    this.positionFormatToolbar();
  }
  positionFormatToolbar() {
    if (!this.formatToolbar) {
      return;
    }
    if (this.surfaceType === "source" && this.toolMode === TOOL_EDIT_MD && !this.currentEditorEmbedded) {
      const cmView = this.sourceCodeMirrorView();
      const selection2 = cmView?.state?.selection?.main;
      const from = Number.isFinite(selection2?.from) ? cmView.coordsAtPos(selection2.from) : null;
      const to = Number.isFinite(selection2?.to) ? cmView.coordsAtPos(selection2.to) : from;
      const sourceRect = from && to ? {
        left: Math.min(from.left, to.left),
        right: Math.max(from.right ?? from.left, to.right ?? to.left),
        top: Math.min(from.top, to.top),
        bottom: Math.max(from.bottom, to.bottom)
      } : this.previewEl.getBoundingClientRect?.();
      if (!sourceRect) {
        return;
      }
      const toolbarRect2 = this.formatToolbar.getBoundingClientRect();
      const width2 = Math.max(180, toolbarRect2.width || 280);
      const height2 = Math.max(34, toolbarRect2.height || 34);
      const viewport2 = window.visualViewport;
      const viewportTop2 = viewport2?.offsetTop || 0;
      const viewportLeft2 = viewport2?.offsetLeft || 0;
      const viewportHeight2 = Math.max(120, viewport2?.height || window.innerHeight || 120);
      const viewportWidth2 = Math.max(160, viewport2?.width || window.innerWidth || 160);
      const minTop2 = viewportTop2 + 8;
      const maxTop2 = Math.max(minTop2, viewportTop2 + viewportHeight2 - height2 - 8);
      const left2 = clamp10(Math.round((sourceRect.left + sourceRect.right - width2) / 2), viewportLeft2 + 8, Math.max(viewportLeft2 + 8, viewportLeft2 + viewportWidth2 - width2 - 8));
      const top2 = clamp10(Math.round(sourceRect.bottom + 14), minTop2, maxTop2);
      setNoteDrawCssProps(this.formatToolbar, {
        "--notedraw-format-top": `${top2}px`,
        "--notedraw-format-left": `${left2}px`
      });
      return;
    }
    if (!this.currentEditor) {
      return;
    }
    const selection = window.getSelection?.();
    const range = selection?.rangeCount ? selection.getRangeAt(0) : this.currentTextRange;
    const rect = range ? rangeLineRect(range) || this.currentEditor.getBoundingClientRect() : this.currentEditor.getBoundingClientRect();
    const toolbarRect = this.formatToolbar.getBoundingClientRect();
    const width = Math.max(180, toolbarRect.width || 280);
    const height = Math.max(34, toolbarRect.height || 34);
    const viewport = window.visualViewport;
    const viewportTop = viewport?.offsetTop || 0;
    const viewportLeft = viewport?.offsetLeft || 0;
    const viewportHeight = Math.max(120, viewport?.height || window.innerHeight || 120);
    const viewportWidth = Math.max(160, viewport?.width || window.innerWidth || 160);
    const minTop = viewportTop + 8;
    const maxTop = Math.max(minTop, viewportTop + viewportHeight - height - 8);
    const gap = 14;
    const preferredLeft = rect.left + rect.width / 2 - width / 2;
    const left = clamp10(Math.round(preferredLeft), viewportLeft + 8, Math.max(viewportLeft + 8, viewportLeft + viewportWidth - width - 8));
    if (this.formatToolbarManualPosition) {
      const top2 = clamp10(Math.round(this.formatToolbarManualPosition.top), minTop, maxTop);
      const manualLeft = clamp10(Math.round(this.formatToolbarManualPosition.left), viewportLeft + 8, Math.max(viewportLeft + 8, viewportLeft + viewportWidth - width - 8));
      this.formatToolbarManualPosition = { top: top2, left: manualLeft };
      setNoteDrawCssProps(this.formatToolbar, {
        "--notedraw-format-top": `${top2}px`,
        "--notedraw-format-left": `${manualLeft}px`
      });
      return;
    }
    const lineStep = Math.max(22, Math.round(rect.height + 6));
    const belowOneLine = rect.bottom + gap + lineStep;
    const above = rect.top - height - gap;
    const below = rect.bottom + gap;
    const top = belowOneLine <= maxTop ? belowOneLine : below <= maxTop ? below : above >= minTop ? above : clamp10(Math.round(belowOneLine), minTop, maxTop);
    setNoteDrawCssProps(this.formatToolbar, {
      "--notedraw-format-top": `${top}px`,
      "--notedraw-format-left": `${left}px`
    });
  }
  startFormatToolbarDrag(event) {
    if (!this.formatToolbar) {
      return;
    }
    event.preventDefault();
    event.stopPropagation();
    const rect = this.formatToolbar.getBoundingClientRect();
    this.formatToolbarDrag = {
      pointerId: event.pointerId,
      startX: event.clientX,
      startY: event.clientY,
      startTop: rect.top,
      startLeft: rect.left
    };
    this.formatToolbarManualPosition = { top: rect.top, left: rect.left };
    this.formatToolbar.addClass("is-moving");
    activeDocument.addEventListener("pointermove", this.onFormatToolbarDragMove, true);
    activeDocument.addEventListener("pointerup", this.onFormatToolbarDragEnd, true);
    activeDocument.addEventListener("pointercancel", this.onFormatToolbarDragEnd, true);
  }
  onFormatToolbarDragMove(event) {
    if (!this.formatToolbarDrag || !this.formatToolbar || event.pointerId !== this.formatToolbarDrag.pointerId) {
      return;
    }
    event.preventDefault();
    event.stopPropagation();
    const toolbarRect = this.formatToolbar.getBoundingClientRect();
    const viewport = window.visualViewport;
    const viewportTop = viewport?.offsetTop || 0;
    const viewportLeft = viewport?.offsetLeft || 0;
    const viewportHeight = Math.max(120, viewport?.height || window.innerHeight || 120);
    const viewportWidth = Math.max(160, viewport?.width || window.innerWidth || 160);
    const top = clamp10(
      this.formatToolbarDrag.startTop + event.clientY - this.formatToolbarDrag.startY,
      viewportTop + 8,
      Math.max(viewportTop + 8, viewportTop + viewportHeight - toolbarRect.height - 8)
    );
    const left = clamp10(
      this.formatToolbarDrag.startLeft + event.clientX - this.formatToolbarDrag.startX,
      viewportLeft + 8,
      Math.max(viewportLeft + 8, viewportLeft + viewportWidth - toolbarRect.width - 8)
    );
    this.formatToolbarManualPosition = { top, left };
    setNoteDrawCssProps(this.formatToolbar, {
      "--notedraw-format-top": `${Math.round(top)}px`,
      "--notedraw-format-left": `${Math.round(left)}px`
    });
  }
  onFormatToolbarDragEnd(event) {
    if (this.formatToolbarDrag && event?.pointerId !== this.formatToolbarDrag.pointerId) {
      return;
    }
    event?.preventDefault?.();
    event?.stopPropagation?.();
    this.stopFormatToolbarDrag();
  }
  stopFormatToolbarDrag() {
    this.formatToolbarDrag = null;
    this.formatToolbar?.removeClass("is-moving");
    activeDocument.removeEventListener("pointermove", this.onFormatToolbarDragMove, true);
    activeDocument.removeEventListener("pointerup", this.onFormatToolbarDragEnd, true);
    activeDocument.removeEventListener("pointercancel", this.onFormatToolbarDragEnd, true);
  }
  restoreTextRange() {
    if (!this.currentEditor || !this.currentTextRange) {
      return false;
    }
    const selection = window.getSelection?.();
    if (!selection) {
      return false;
    }
    selection.removeAllRanges();
    selection.addRange(this.currentTextRange);
    return true;
  }
  applyTextInlineFormat(tagName, styles = {}) {
    if (this.surfaceType === "source" && this.toolMode === TOOL_EDIT_MD && !this.currentEditorEmbedded) {
      return this.applySourceTextInlineFormat(tagName, styles);
    }
    if (!this.currentEditor || !this.restoreTextRange()) {
      return;
    }
    const selection = window.getSelection?.();
    if (!selection?.rangeCount) {
      return;
    }
    const range = selection.getRangeAt(0);
    if (!this.currentEditor.contains(range.commonAncestorContainer) || range.collapsed) {
      return;
    }
    const wrapper = activeDocument.createElement(tagName);
    applyInlineFormatStyles(wrapper, styles);
    const fragment = range.extractContents();
    wrapper.appendChild(fragment);
    range.insertNode(wrapper);
    selectNodeContents(wrapper);
    this.currentTextRange = window.getSelection()?.rangeCount ? window.getSelection().getRangeAt(0).cloneRange() : null;
    this.queueCurrentTextSave(true);
    this.positionFormatToolbar();
  }
  applyTextBlockFormat(kind) {
    if (this.surfaceType === "source" && this.toolMode === TOOL_EDIT_MD && !this.currentEditorEmbedded) {
      return this.applySourceTextBlockFormat(kind);
    }
    if (!this.currentEditor || !this.restoreTextRange()) {
      return;
    }
    const selection = window.getSelection?.();
    if (!selection?.rangeCount) {
      return;
    }
    const range = selection.getRangeAt(0);
    if (!this.currentEditor.contains(range.commonAncestorContainer)) {
      return;
    }
    const text = selection.toString() || "";
    if (kind === "code") {
      const pre = activeDocument.createElement("pre");
      const code = activeDocument.createElement("code");
      code.textContent = text || "code";
      pre.appendChild(code);
      range.deleteContents();
      range.insertNode(pre);
      selectNodeContents(code);
      this.currentTextRange = window.getSelection()?.rangeCount ? window.getSelection().getRangeAt(0).cloneRange() : null;
      this.queueCurrentTextSave(true);
      this.positionFormatToolbar();
    }
  }
  applyTextAlignment(alignment) {
    const safeAlignment = ["left", "center", "right", "justify"].includes(alignment) ? alignment : "left";
    if (this.surfaceType === "source" && this.toolMode === TOOL_EDIT_MD && !this.currentEditorEmbedded) {
      return this.applySourceTextAlignment(safeAlignment);
    }
    if (!this.currentEditor || !this.restoreTextRange()) {
      return false;
    }
    const selection = window.getSelection?.();
    if (!selection?.rangeCount) {
      return false;
    }
    const range = selection.getRangeAt(0);
    if (!this.currentEditor.contains(range.commonAncestorContainer)) {
      return false;
    }
    const command = {
      left: "justifyLeft",
      center: "justifyCenter",
      right: "justifyRight",
      justify: "justifyFull"
    }[safeAlignment];
    const applied = activeDocument.execCommand?.(command, false, null);
    if (applied !== false) {
      this.currentEditor.dispatchEvent?.(new Event("input", { bubbles: true }));
      this.currentTextRange = window.getSelection()?.rangeCount ? window.getSelection().getRangeAt(0).cloneRange() : this.currentTextRange;
      this.positionFormatToolbar();
      return true;
    }
    const block = (range.commonAncestorContainer.nodeType === 1 ? range.commonAncestorContainer : range.commonAncestorContainer.parentElement)?.closest?.("p, li, blockquote, pre, h1, h2, h3, h4, h5, h6, div") || this.currentEditor;
    if (typeof block.setCssStyles === "function") {
      block.setCssStyles({ textAlign: safeAlignment });
    } else {
      setNoteDrawCssProps(block, { "--notedraw-text-align": safeAlignment });
    }
    this.currentEditor.dispatchEvent?.(new Event("input", { bubbles: true }));
    this.positionFormatToolbar();
    return true;
  }
  insertTextBreak() {
    if (this.surfaceType === "source" && this.toolMode === TOOL_EDIT_MD && !this.currentEditorEmbedded) {
      return this.insertSourceTextBreak();
    }
    if (!this.currentEditor || !this.restoreTextRange()) {
      return;
    }
    const selection = window.getSelection?.();
    if (!selection?.rangeCount) {
      return;
    }
    const range = selection.getRangeAt(0);
    if (!this.currentEditor.contains(range.commonAncestorContainer)) {
      return;
    }
    range.deleteContents();
    const br = activeDocument.createElement("br");
    range.insertNode(br);
    range.setStartAfter(br);
    range.collapse(true);
    selection.removeAllRanges();
    selection.addRange(range);
    this.currentTextRange = range.cloneRange();
    this.queueCurrentTextSave(true);
    this.positionFormatToolbar();
  }
  clearTextFormat() {
    if (this.surfaceType === "source" && this.toolMode === TOOL_EDIT_MD && !this.currentEditorEmbedded) {
      return this.clearSourceTextFormat();
    }
    if (!this.currentEditor || !this.restoreTextRange()) {
      return;
    }
    const selection = window.getSelection?.();
    if (!selection?.rangeCount) {
      return;
    }
    const range = selection.getRangeAt(0);
    if (!this.currentEditor.contains(range.commonAncestorContainer)) {
      return;
    }
    const clearWholeEditor = range.collapsed;
    const plainText = clearWholeEditor ? this.currentEditor.innerText || this.currentEditor.textContent || "" : selection.toString() || range.cloneContents().textContent || "";
    const textNode = activeDocument.createTextNode(plainText);
    if (clearWholeEditor) {
      this.currentEditor.replaceChildren(textNode);
    } else {
      const marker = activeDocument.createElement("span");
      marker.dataset.noteDrawClearFormat = "true";
      marker.textContent = plainText;
      range.deleteContents();
      range.insertNode(marker);
      hoistPlainTextMarker(marker, this.currentEditor, isClearableInlineFormattingElement);
      marker.replaceWith(textNode);
    }
    const nextRange = activeDocument.createRange();
    nextRange.setStart(textNode, 0);
    nextRange.setEnd(textNode, textNode.nodeValue?.length || 0);
    selection.removeAllRanges();
    selection.addRange(nextRange);
    this.currentTextRange = nextRange.cloneRange();
    this.queueCurrentTextSave(true);
    this.positionFormatToolbar();
  }
  queueTextSaveAndWait(file2, original, editedSource, element) {
    const scheduled = this.plugin.scheduleTextSaveNow(file2, original, editedSource, element, this);
    return this.textCommitBarrier.track(Promise.resolve(scheduled).then(() => this.plugin.flushTextSaveAndWait(element)));
  }
  queueCurrentTextSave(immediate = true) {
    const element = this.currentEditor;
    if (!element || this.surfaceType === "webview") {
      return;
    }
    const original = element.dataset.noteDrawOriginal || "";
    const editedSource = serializeControllerEditableSource(element, this.currentEditorEmbedded);
    const file2 = this.currentEditorFile || this.file;
    this.plugin.stageTextSave(file2, original, editedSource, element, this);
  }
  toggleTextPanel() {
    this.setTextPanelOpen(!this.textPanelOpen);
  }
  setTextPanelOpen(open) {
    this.textPanelOpen = Boolean(open);
    this.previewEl.toggleClass("is-text-panel-open", this.textPanelOpen);
    this.syncFloatingControlClasses();
    this.textButton?.classList.toggle("is-active", this.toolMode === TOOL_TEXT || this.textPanelOpen);
    if (this.textPanelOpen) {
      this.setPaletteOpen(false);
      this.setBrushPanelOpen(false);
      this.updateFloatingControlsPosition();
      this.syncTextPanelButtons();
    }
    this.syncSharedToolbarState();
  }
  createColorPalette() {
    const row = this.palettePanel.createDiv({ cls: "notedraw-palette-row notedraw-color-row" });
    const iconEl = row.createSpan({ cls: "notedraw-palette-icon" });
    (0, import_obsidian.setIcon)(iconEl, "palette");
    this.colorSwatchGrid = row.createDiv({ cls: "notedraw-color-grid" });
    this.colorSwatchButtons = COMMON_COLORS.map((color) => {
      const button = this.colorSwatchGrid.createEl("button", {
        cls: "notedraw-color-swatch",
        attr: {
          type: "button",
          title: color,
          "aria-label": this.plugin.t("useColor", { color })
        }
      });
      setNoteDrawCssProps(button, { "--notedraw-swatch-color": color });
      bindNoteDrawControlTap(button, () => {
        this.setCurrentBrushColor(color);
      });
      return button;
    });
    this.noColorButton = this.colorSwatchGrid.createEl("button", {
      cls: "notedraw-color-swatch notedraw-color-none",
      attr: {
        type: "button",
        title: this.plugin.t("noColor"),
        "aria-label": this.plugin.t("noColor")
      }
    });
    (0, import_obsidian.setIcon)(this.noColorButton, "ban");
    bindNoteDrawControlTap(this.noColorButton, () => this.setCurrentBrushColor(NO_COLOR));
    this.advancedColorButton = this.colorSwatchGrid.createEl("button", {
      cls: "notedraw-color-advanced",
      attr: {
        type: "button",
        title: this.plugin.t("advancedColor"),
        "aria-label": this.plugin.t("advancedColor")
      }
    });
    (0, import_obsidian.setIcon)(this.advancedColorButton, "sliders-horizontal");
    bindNoteDrawControlTap(this.advancedColorButton, () => {
      this.colorInput?.click();
    });
  }
  setCurrentBrushColor(color) {
    if (!isPaletteColor(color)) {
      return;
    }
    if (color === NO_COLOR) {
      if (this.toolMode === TOOL_SELECT && this.hasHybridSelection()) {
        this.applyColorToSelectedElements(color);
        this.syncPaletteInputs();
      }
      return;
    }
    this.currentBrushSettings().color = color;
    this.applyColorToSelectedElements(color);
    this.syncCurrentBrushFields();
    this.syncPaletteInputs();
    this.updateToolButtons();
    this.persistCurrentBrushSettings();
    this.syncSharedToolbarState();
  }
  currentPaletteColor() {
    if (this.toolMode === TOOL_SELECT) {
      const selectedGroupColor = this.selectedWholeElementGroups().filter((group) => group.boxed).map((group) => group.backgroundColor || group.borderColor).find((color) => isPaletteColor(color));
      if (selectedGroupColor) {
        return selectedGroupColor;
      }
      const selectedColor = this.getSelectedStrokeIndexes().map((index) => this.drawingData.strokes[index]?.color).find((color) => isPaletteColor(color));
      if (selectedColor) {
        return selectedColor;
      }
      const selectedBlockColor = this.getSelectedMarkdownBlocks().map((block) => block.contentColor || block.borderColor).find((color) => isPaletteColor(color));
      if (selectedBlockColor) {
        return selectedBlockColor;
      }
    }
    return this.currentBrushSettings().color;
  }
  currentPaletteWidth() {
    if (this.toolMode === TOOL_SELECT) {
      const selectedWidth = this.getSelectedStrokeIndexes().map((index) => this.drawingData.strokes[index]?.width).find((width) => Number.isFinite(Number(width)));
      if (Number.isFinite(Number(selectedWidth))) {
        return clamp10(Number(selectedWidth), MIN_BRUSH_WIDTH, MAX_BRUSH_WIDTH);
      }
      const blockSize = this.getSelectedMarkdownBlocks().map((block) => block.contentScale).find((size) => Number.isFinite(Number(size)));
      if (Number.isFinite(Number(blockSize))) {
        return elementScaleToPaletteBrushWidth(blockSize);
      }
    }
    return this.currentBrushSettings().width;
  }
  currentPaletteOpacity() {
    if (this.toolMode === TOOL_SELECT) {
      const selectedOpacity = this.getSelectedStrokeIndexes().map((index) => this.drawingData.strokes[index]?.opacity).find((opacity) => Number.isFinite(Number(opacity)));
      if (Number.isFinite(Number(selectedOpacity))) {
        return clamp10(Number(selectedOpacity), 0, 1);
      }
      const blockOpacity = this.getSelectedMarkdownBlocks().map((block) => block.contentOpacity).find((opacity) => Number.isFinite(Number(opacity)));
      if (Number.isFinite(Number(blockOpacity))) {
        return clamp10(Number(blockOpacity), 0, 1);
      }
    }
    return this.currentBrushSettings().opacity;
  }
  applyColorToSelectedElements(color) {
    const indexes = this.getSelectedStrokeIndexes();
    const boxedGroups = this.selectedWholeElementGroups().filter((group) => group.boxed);
    const boxedGroupIds = new Set(boxedGroups.map((group) => group.id));
    const changed = indexes.filter((index) => {
      const stroke = this.drawingData.strokes[index];
      return stroke && !boxedGroupIds.has(stroke.groupId) && stroke.color !== color;
    });
    const changedBlocks = this.getSelectedMarkdownBlocks().filter((block) => !boxedGroupIds.has(block.groupId) && (block.contentColor !== color || block.borderColor && block.borderColor !== color));
    const changedGroups = boxedGroups.filter((group) => group.borderColor !== color || isCssColor(group.backgroundColor) && group.backgroundColor !== color);
    if (!changed.length && !changedBlocks.length && !changedGroups.length) {
      return false;
    }
    const historyBefore = this.captureDrawingHistorySnapshot();
    for (const index of changed) {
      this.drawingData.strokes[index].color = color;
    }
    for (const block of changedBlocks) {
      block.contentColor = color;
      if (block.borderColor) {
        block.borderColor = color;
      }
    }
    for (const group of changedGroups) {
      group.borderColor = color;
      if (group.backgroundColor) {
        group.backgroundColor = color;
      }
    }
    this.redoStack = [];
    this.invalidateStaticCache();
    this.plugin.scheduleDrawingSave(this.file, this.drawingData, { userOperation: true });
    this.recordDrawingHistory(historyBefore);
    this.refreshMarkdownBlockPresentation(changedBlocks.map((block) => block.id));
    this.render();
    return true;
  }
  applyBackgroundToSelectedMarkdownBlocks(color) {
    const groups = this.selectedWholeElementGroups().filter((group) => group.boxed);
    const boxedGroupIds = new Set(groups.map((group) => group.id));
    const blocks = this.getSelectedMarkdownBlocks().filter((block) => !boxedGroupIds.has(block.groupId) && block.backgroundColor !== color);
    const changedGroups = groups.filter((group) => group.backgroundColor !== color);
    if (!blocks.length && !changedGroups.length) {
      return false;
    }
    const historyBefore = this.captureDrawingHistorySnapshot();
    for (const block of blocks) {
      block.backgroundColor = color;
    }
    for (const group of changedGroups) {
      group.backgroundColor = color;
    }
    this.redoStack = [];
    this.invalidateStaticCache();
    this.plugin.scheduleDrawingSave(this.file, this.drawingData, { userOperation: true });
    this.recordDrawingHistory(historyBefore);
    this.render();
    return true;
  }
  applyOpacityToSelectedElements(opacity) {
    const nextOpacity = clamp10(Number(opacity), 0, 1);
    const indexes = this.getSelectedStrokeIndexes();
    const changed = indexes.filter((index) => {
      const stroke = this.drawingData.strokes[index];
      return stroke && Math.abs(clamp10(Number(stroke.opacity ?? 1), 0, 1) - nextOpacity) >= 1e-3;
    });
    const changedBlocks = this.getSelectedMarkdownBlocks().filter((block) => Math.abs(clamp10(Number(block.contentOpacity ?? 1), 0, 1) - nextOpacity) >= 1e-3);
    const changedGroups = this.selectedWholeElementGroups().filter((group) => Math.abs(clamp10(Number(group.opacity ?? 1), 0, 1) - nextOpacity) >= 1e-3);
    if (!changed.length && !changedBlocks.length && !changedGroups.length) {
      return false;
    }
    const historyBefore = this.captureDrawingHistorySnapshot();
    for (const index of changed) {
      this.drawingData.strokes[index].opacity = nextOpacity;
    }
    for (const block of changedBlocks) {
      block.contentOpacity = nextOpacity;
    }
    for (const group of changedGroups) {
      group.opacity = nextOpacity;
    }
    this.redoStack = [];
    this.invalidateStaticCache();
    this.plugin.scheduleDrawingSave(this.file, this.drawingData, { userOperation: true });
    this.recordDrawingHistory(historyBefore);
    this.refreshMarkdownBlockPresentation(changedBlocks.map((block) => block.id));
    this.render();
    return true;
  }
  applySizeToSelectedElements(width) {
    const nextWidth = clamp10(Number(width), MIN_BRUSH_WIDTH, MAX_BRUSH_WIDTH);
    const indexes = this.getSelectedStrokeIndexes();
    const changed = indexes.filter((index) => {
      const stroke = this.drawingData.strokes[index];
      return stroke && Math.abs(clamp10(Number(stroke.width || this.penWidth), MIN_BRUSH_WIDTH, MAX_BRUSH_WIDTH) - nextWidth) >= 0.01;
    });
    const nextContentScale = paletteBrushWidthToElementScale(nextWidth);
    const changedBlocks = this.getSelectedMarkdownBlocks().filter((block) => Math.abs(clamp10(Number(block.contentScale ?? 1), 0.5, 3) - nextContentScale) >= 1e-3);
    if (!changed.length && !changedBlocks.length) {
      return false;
    }
    const historyBefore = this.captureDrawingHistorySnapshot();
    for (const index of changed) {
      const stroke = this.drawingData.strokes[index];
      const previousWidth = clamp10(Number(stroke.width || this.penWidth), MIN_BRUSH_WIDTH, MAX_BRUSH_WIDTH);
      const scale = clamp10(nextWidth / Math.max(MIN_BRUSH_WIDTH, previousWidth), 0.15, 8);
      stroke.width = nextWidth;
      if (isTextStroke(stroke)) {
        stroke.fontSize = clamp10(Number(stroke.fontSize || 18) * scale, 10, 72);
        if (Number(stroke.textWidth) > 0) {
          stroke.textWidth = clamp10(Number(stroke.textWidth) * scale, 24, 900);
        }
      }
      if (isTextLikeStroke(stroke) || isEmbedStroke(stroke)) {
        stroke.previewWidth = clamp10(Number(stroke.previewWidth || 260) * scale, 80, 900);
        stroke.previewHeight = clamp10(Number(stroke.previewHeight || 160) * scale, 40, 700);
      }
      if (isConnectorStroke(stroke)) {
        stroke.width = nextWidth;
      }
    }
    for (const block of changedBlocks) {
      block.contentScale = nextContentScale;
    }
    this.redoStack = [];
    this.invalidateStaticCache();
    this.plugin.scheduleDrawingSave(this.file, this.drawingData, { userOperation: true });
    this.recordDrawingHistory(historyBefore);
    this.refreshMarkdownBlockPresentation(changedBlocks.map((block) => block.id));
    if (changedBlocks.length) {
      this.scheduleResize({ layout: false, measure: true });
      this.scheduleNoteFlowLayout({ operation: true });
    }
    this.render();
    return true;
  }
  syncColorSwatches() {
    if (!this.colorSwatchButtons?.length) {
      return;
    }
    const currentColor = this.currentPaletteColor()?.toLowerCase?.();
    this.colorSwatchButtons.forEach((button, index) => {
      const color = COMMON_COLORS[index].toLowerCase();
      button.classList.toggle("is-active", color === currentColor);
    });
    this.noColorButton?.classList.toggle("is-active", currentColor === NO_COLOR);
    this.noColorButton?.toggleAttribute("disabled", !(this.toolMode === TOOL_SELECT && this.hasHybridSelection()));
    this.advancedColorButton?.classList.toggle(
      "is-active",
      Boolean(currentColor) && currentColor !== NO_COLOR && !COMMON_COLORS.some((color) => color.toLowerCase() === currentColor)
    );
  }
  applyBrushButtonState(button, settings, active) {
    if (!button) {
      return;
    }
    const color = isCssColor(settings?.color) ? settings.color : DEFAULT_SETTINGS.defaultPenColor;
    button.classList.add("notedraw-brush-button");
    button.classList.toggle("is-active", active);
    button.classList.toggle("is-brush-color-active", active);
    setNoteDrawCssProps(button, {
      "--notedraw-brush-button-color": color,
      "--notedraw-brush-button-contrast": contrastTextColor(color)
    });
  }
  toggleSelectMode() {
    this.toolMode = this.toolMode === TOOL_SELECT ? TOOL_DRAW : TOOL_SELECT;
    this.previewEl.toggleClass("is-select-mode", this.toolMode === TOOL_SELECT);
    if (this.toolMode === TOOL_SELECT) {
      this.setPaletteOpen(false);
      this.setBrushPanelOpen(false);
      this.setTextPanelOpen(false);
    } else {
      this.hideSelectionMenu();
    }
    this.updateToolButtons();
    this.endTextEdit();
    this.cancelCurrentStroke();
    this.cancelSelectionDrag(true);
    this.persistInteractionSettings();
    this.syncSharedToolbarState();
    this.render();
  }
  togglePalettePanel() {
    if (this.toolMode === TOOL_EDIT_MD || this.toolMode === TOOL_SELECT && !this.hasHybridSelection()) {
      this.setPaletteOpen(false);
      return;
    }
    this.setPaletteOpen(!this.paletteOpen);
    if (this.paletteOpen) {
      this.setTextPanelOpen(false);
    }
  }
  setPaletteOpen(open) {
    this.paletteOpen = Boolean(open);
    this.previewEl.toggleClass("is-palette-open", this.paletteOpen);
    this.syncFloatingControlClasses();
    this.paletteButton?.classList.toggle("is-active", this.paletteOpen);
    if (this.paletteOpen) {
      this.setBrushPanelOpen(false);
      this.updateFloatingControlsPosition();
    }
    this.syncSharedToolbarState();
  }
  onDocumentPointerDown(event) {
    if (!this.active && this.surfaceType === "preview" && event?.button === 0 && this.previewEl?.contains?.(event.target)) {
      const point = this.eventToPoint(event);
      const index = this.findStrokeAt(point, { x: event.clientX, y: event.clientY });
      const stroke = index >= 0 ? this.drawingData?.strokes?.[index] : null;
      const markdownElement = this.markdownBlockElementForTarget(event.target, { x: event.clientX, y: event.clientY });
      const markdownBlock = markdownElement ? this.findMarkdownBlockRecordForElement(markdownElement) : null;
      const commandTarget = stroke && this.elementHasCommandFrame(stroke) && String(stroke.commandId || "").trim() ? stroke : markdownBlock && this.elementHasCommandFrame(markdownBlock) && String(markdownBlock.commandId || "").trim() ? markdownBlock : null;
      if (commandTarget) {
        event.preventDefault();
        event.stopPropagation();
        event.stopImmediatePropagation?.();
        this.executeElementCommand(commandTarget);
        return;
      }
    }
    if (!this.paletteOpen && !this.brushPanelOpen && !this.textPanelOpen && !this.selectionMenuOpen && !this.currentEditor) {
      return;
    }
    if (!this.controlsShouldBeVisible()) {
      return;
    }
    const target = event.target;
    const path = typeof event.composedPath === "function" ? event.composedPath() : [];
    const containsEvent = (element) => Boolean(element) && (path.includes(element) || element.contains?.(target));
    if ([
      this.palettePanel,
      this.paletteButton,
      this.brushPanel,
      this.penButton,
      this.watercolorButton,
      this.textPanel,
      this.textButton,
      this.selectionMenu,
      this.toolbar,
      this.formatToolbar,
      this.currentEditor,
      this.floatingTextInput?.element
    ].some(containsEvent)) {
      return;
    }
    this.setPaletteOpen(false);
    this.setBrushPanelOpen(false);
    this.setTextPanelOpen(false);
    this.hideSelectionMenu();
    if (this.currentEditor) {
      this.endTextEdit();
    }
  }
  onDocumentPointerMove(event) {
    if (this.directMarkdownPointerId !== event.pointerId) {
      return;
    }
    this.onPointerMove(event);
    event.preventDefault?.();
    event.stopPropagation?.();
    event.stopImmediatePropagation?.();
  }
  onButtonPointerDown(event) {
    if (event?.button !== void 0 && event.button !== 0) {
      return;
    }
    this.clearButtonLongPress();
    this.buttonLongPressTimer = window.setTimeout(() => {
      this.buttonLongPressed = true;
      this.toggleDrawingsVisiblePersisted().catch((error) => {
        console.error(`[${PLUGIN_ID}] Failed to toggle NoteDraw visibility`, error);
      });
    }, this.longPressDelayMs());
  }
  onButtonPointerUp() {
    this.clearButtonLongPress();
  }
  onButtonTouchEnd(event) {
    this.clearButtonLongPress();
    if (!isAppleMobileRuntime()) {
      return;
    }
    if (this.buttonLongPressed) {
      this.buttonLongPressed = false;
      event?.preventDefault?.();
      event?.stopPropagation?.();
      return;
    }
    event?.preventDefault?.();
    event?.stopPropagation?.();
    this.suppressNextButtonClick = true;
    window.setTimeout(() => {
      this.suppressNextButtonClick = false;
    }, 500);
    this.toggle().catch((error) => {
      console.error(`[${PLUGIN_ID}] Failed to toggle NoteDraw`, error);
    });
  }
  onButtonClick(event) {
    if (this.suppressNextButtonClick) {
      this.suppressNextButtonClick = false;
      event?.preventDefault?.();
      event?.stopPropagation?.();
      return;
    }
    if (this.buttonLongPressed) {
      this.buttonLongPressed = false;
      event?.preventDefault();
      event?.stopPropagation();
      return;
    }
    this.toggle().catch((error) => {
      console.error(`[${PLUGIN_ID}] Failed to toggle NoteDraw`, error);
    });
  }
  onButtonContextMenu(event) {
    event?.preventDefault?.();
    event?.stopPropagation?.();
    this.clearButtonLongPress();
    this.buttonLongPressed = false;
    this.toggleDrawingsVisiblePersisted().catch((error) => {
      console.error(`[${PLUGIN_ID}] Failed to toggle NoteDraw visibility`, error);
    });
  }
  clearButtonLongPress() {
    if (this.buttonLongPressTimer) {
      window.clearTimeout(this.buttonLongPressTimer);
      this.buttonLongPressTimer = null;
    }
  }
  toggleDrawingsVisible() {
    this.setDrawingsVisible(!this.drawingsVisible, { persist: true });
  }
  async toggleDrawingsVisiblePersisted() {
    await this.ensureDrawingsLoaded();
    this.toggleDrawingsVisible();
  }
  setDrawingsVisible(visible, options = {}) {
    this.applyDrawingsVisibility(visible);
    this.drawingData.visible = this.drawingsVisible;
    if (options.persist === true) {
      this.plugin.scheduleDrawingSave(this.file, this.drawingData, { userOperation: true });
    }
    this.syncSharedToolbarState();
  }
  applyDrawingsVisibility(visible) {
    this.drawingsVisible = Boolean(visible);
    this.previewEl.toggleClass("is-drawing-hidden", !this.drawingsVisible);
    if (this.drawingsVisible) {
      this.scheduleFrozenNoteFlowLayoutRestore();
    } else {
      this.cancelFrozenNoteFlowLayoutRestore();
      this.clearNoteFlowLayout();
    }
    if (this.drawingsLoaded) {
      this.scheduleResize({ layout: false, measure: false });
    }
    this.plugin.setAccessibleLabel(
      this.button,
      this.surfaceType === "webview" ? "editWebviewDraw" : this.drawingsVisible ? "editTextDraw" : "editTextDrawHidden"
    );
  }
  getResponsiveContentFrame() {
    return measureResponsiveContentFrame(this.previewEl, this.surfaceType, this.canvasWidth(), this.canvas, this.readingZoomScale());
  }
  responsiveViewportScale() {
    return this.usesVisualReadingZoom() ? 1 : this.readingZoomScale();
  }
  getResponsiveLayoutContext(refresh = false) {
    if (this.responsiveLayoutContext && !refresh) {
      return this.responsiveLayoutContext;
    }
    this.responsiveLayoutContext = {
      frame: this.getResponsiveContentFrame(),
      viewportHeight: measureResponsiveViewportHeight(this.previewEl, this.scrollContainer, this.responsiveViewportScale()),
      lineAnchors: [
        ...collectRenderedLineAnchors(this.previewEl, this.canvas, this.canvasWindowTop, this.readingZoomScale()),
        ...collectVirtualMarkdownLineAnchors(this.view, this.previewEl, this.canvas, this.canvasWindowTop, this.file?.path || "", this.readingZoomScale())
      ],
      codeMirror: this.surfaceType === "source" ? getCodeMirrorView(this.view, this.previewEl) : null
    };
    return this.responsiveLayoutContext;
  }
  captureLineLocation(canvasX, canvasY, context = this.getResponsiveLayoutContext(), options = {}) {
    const rendered = captureRenderedLineLocation(context.lineAnchors, canvasX, canvasY, options);
    if (rendered) {
      return rendered;
    }
    if (!context.codeMirror || !this.canvas) {
      return null;
    }
    const canvasRect = this.canvas.getBoundingClientRect();
    const canvasScaleX = canvasRect.width > 0 ? canvasRect.width / this.canvasWidth() : 1;
    const canvasScaleY = canvasRect.height > 0 ? canvasRect.height / Math.max(1, this.canvasRenderHeight) : 1;
    return captureCodeMirrorLineLocation(
      context.codeMirror,
      canvasRect.left + canvasX * canvasScaleX,
      canvasRect.top + (canvasY - this.canvasWindowTop) * canvasScaleY,
      this.file?.path || ""
    );
  }
  projectLineLocation(path, line, context = this.getResponsiveLayoutContext()) {
    const renderedY = projectRenderedLineLocation(context.lineAnchors, path, line);
    if (Number.isFinite(renderedY)) {
      return renderedY;
    }
    if (!context.codeMirror || normalizeVaultPath(path) !== normalizeVaultPath(this.file?.path) || !this.canvas) {
      return NaN;
    }
    const clientY = projectCodeMirrorLineLocation(context.codeMirror, line);
    if (!Number.isFinite(clientY)) {
      return NaN;
    }
    const canvasRect = this.canvas.getBoundingClientRect();
    const canvasScaleY = canvasRect.height > 0 ? canvasRect.height / Math.max(1, this.canvasRenderHeight) : 1;
    return (clientY - canvasRect.top) / Math.max(0.01, canvasScaleY) + this.canvasWindowTop;
  }
  captureResponsivePoint(point, context = this.getResponsiveLayoutContext()) {
    const canvasX = clamp10(Number(point?.x || 0), 0, 1) * this.canvasWidth();
    const canvasY = clamp10(Number(point?.y || 0), 0, 1) * this.canvasHeight();
    const lineLocation = this.captureLineLocation(canvasX, canvasY, context);
    return {
      ...point,
      ...createResponsivePoint({
        canvasX,
        canvasY,
        canvasWidth: this.canvasWidth(),
        canvasHeight: this.canvasHeight(),
        frame: context.frame,
        sourcePath: lineLocation?.path || this.file?.path || "",
        linePosition: lineLocation?.line ?? null,
        lineConfidence: lineLocation?.lineConfidence ?? null,
        time: point?.t
      })
    };
  }
  captureResponsiveAnchorsForIndexes(indexes) {
    const context = this.getResponsiveLayoutContext(true);
    for (const index of indexes) {
      const stroke = this.drawingData?.strokes?.[index];
      if (stroke?.points?.length && !isConnectorStroke(stroke)) {
        stroke.points = stroke.points.map((point) => this.captureResponsivePoint(point, context));
        this.captureNoteFlowResponsiveAnchors(stroke, context);
        this.captureElementLayoutForStroke(stroke, context, index);
      }
    }
    this.rebuildElementRelations();
  }
  captureNoteFlowResponsiveAnchors(stroke, context = this.getResponsiveLayoutContext()) {
    const noteFlow = normalizeNoteFlow(stroke?.noteFlow);
    if (!noteFlow || !stroke?.points?.length) {
      return false;
    }
    const linePosition = noteFlow.positionBasis === "above" && Number.isFinite(noteFlow.positionLine) ? noteFlow.positionLine + 0.999999 : null;
    const anchorY = linePosition === null ? NaN : this.projectLineLocation(noteFlow.positionPath, linePosition, context);
    const useLineAnchor = linePosition !== null && Number.isFinite(anchorY);
    const useDocumentAnchor = noteFlow.positionBasis === "document" && noteFlow.positionVersion >= 1;
    stroke.points = stroke.points.map((point) => {
      const canvasX = clamp10(Number(point?.x || 0), 0, 1) * this.canvasWidth();
      const canvasY = clamp10(Number(point?.y || 0), 0, 1) * this.canvasHeight();
      return {
        ...point,
        ...createResponsivePoint({
          canvasX,
          canvasY,
          canvasWidth: this.canvasWidth(),
          canvasHeight: this.canvasHeight(),
          frame: context.frame,
          sourcePath: useLineAnchor ? noteFlow.positionPath : this.file?.path || "",
          linePosition: useLineAnchor ? linePosition : null,
          lineConfidence: useLineAnchor ? 1 : null,
          lineOffsetY: useLineAnchor ? canvasY - anchorY : useDocumentAnchor ? canvasY : 0,
          time: point?.t
        })
      };
    });
    return true;
  }
  captureElementLayoutForStroke(stroke, context = this.getResponsiveLayoutContext(), index = -1, options = {}) {
    const bounds = getStrokeBounds(stroke, this.canvasWidth(), this.canvasHeight());
    if (!bounds) {
      return null;
    }
    const cornerPoints = {
      topLeft: { x: bounds.minX, y: bounds.minY },
      topRight: { x: bounds.maxX, y: bounds.minY },
      bottomRight: { x: bounds.maxX, y: bounds.maxY },
      bottomLeft: { x: bounds.minX, y: bounds.maxY }
    };
    const cornerLocations = Object.fromEntries(Object.entries(cornerPoints).map(([name, point]) => [
      name,
      this.captureLineLocation(point.x, point.y, context, { maxDistance: 112 }) || { path: this.file?.path || "", line: null }
    ]));
    const previous = normalizeElementLayout(stroke.layout);
    const elementId = strokeElementId(stroke) || createElementLayoutId(index);
    stroke.layout = createElementLayout({
      id: previous?.id || elementId,
      bounds,
      canvasWidth: this.canvasWidth(),
      canvasHeight: this.canvasHeight(),
      frame: context.frame,
      viewportHeight: context.viewportHeight,
      sourcePath: this.file?.path || "",
      cornerLocations,
      metrics: {
        width: stroke.width,
        fontSize: stroke.fontSize,
        textWidth: stroke.textWidth,
        previewWidth: stroke.previewWidth,
        previewHeight: stroke.previewHeight
      },
      // Proximity is not a binding. Only explicit groups/connectors may move
      // other elements together, so layout projection must stay independent.
      relations: []
    });
    stroke.elementId = stroke.layout?.id || elementId;
    return stroke.layout;
  }
  rebuildElementRelations() {
    const items = (this.drawingData?.strokes || []).flatMap((stroke, index) => {
      if (isConnectorStroke(stroke)) {
        return [];
      }
      const layout = normalizeElementLayout(stroke.layout);
      const bounds = getStrokeBounds(stroke, this.canvasWidth(), this.canvasHeight());
      return layout?.id && bounds ? [{ id: layout.id, bounds, index }] : [];
    });
    const intersectionRelations = captureElementRelations(items, {
      nearDistance: 0,
      maxRelations: 3
    });
    let changed = false;
    for (const stroke of this.drawingData?.strokes || []) {
      if (isConnectorStroke(stroke)) {
        continue;
      }
      const layout = normalizeElementLayout(stroke.layout);
      if (layout?.id) {
        const relations = (intersectionRelations.get(layout.id) || []).filter((relation) => relation.kind === "intersection");
        if (JSON.stringify(layout.relations) !== JSON.stringify(relations)) {
          stroke.layout = { ...layout, relations };
          changed = true;
        }
      }
    }
    return changed;
  }
  onDocumentPointerFinish(event) {
    if (this.directMarkdownPointerId === event.pointerId) {
      this.directMarkdownTaskClickUntil = Date.now() + 600;
      this.onPointerUp(event);
      this.directMarkdownPointerId = null;
      this.directMarkdownPointerElement = null;
      event.preventDefault?.();
      event.stopPropagation?.();
      event.stopImmediatePropagation?.();
      return;
    }
    if (event.pointerType === "touch" && this.touchPointers.has(event.pointerId)) {
      this.completeTrackedTouch(event.pointerId);
    }
  }
  projectStrokePointsForLayoutRepair(stroke, context, layout) {
    const lineToCanvasY = (path, line) => this.projectLineLocation(path, line, context);
    const sourceFrame = layout?.sourceFrame;
    const trustAnchorX = Boolean(sourceFrame) && isStableResponsiveCaptureFrame(sourceFrame.surfaceWidth, { width: sourceFrame.contentWidth });
    return (Array.isArray(stroke?.points) ? stroke.points : []).map((point) => {
      const projected = projectResponsivePoint(point, {
        canvasWidth: this.canvasWidth(),
        canvasHeight: this.canvasHeight(),
        frame: context.frame,
        lineToCanvasY
      });
      if (trustAnchorX || !point?.anchor) {
        return projected;
      }
      return {
        ...projected,
        x: clamp10(Number(point.x), 0, 1)
      };
    });
  }
  preserveAbsoluteStrokePlacement(previousWidth, previousHeight) {
    const nextWidth = this.canvasWidth();
    const nextHeight = this.canvasHeight();
    const sourceWidth = Math.max(1, Number(previousWidth) || nextWidth);
    const sourceHeight = Math.max(1, Number(previousHeight) || nextHeight);
    if (Math.abs(sourceWidth - nextWidth) < 0.01 && Math.abs(sourceHeight - nextHeight) < 0.01) {
      return;
    }
    const preserve = (points) => (Array.isArray(points) ? points : []).map((point) => ({
      ...point,
      x: clamp10(Number(point?.x || 0) * sourceWidth / Math.max(1, nextWidth), 0, 1),
      y: clamp10(Number(point?.y || 0) * sourceHeight / Math.max(1, nextHeight), 0, 1)
    }));
    for (const stroke of this.drawingData?.strokes || []) {
      stroke.points = preserve(stroke.points);
    }
    if (this.currentStroke?.points?.length) {
      this.currentStroke.points = preserve(this.currentStroke.points);
    }
  }
  initializeAndProjectResponsivePoints(context, signature, options = {}) {
    const hadResponsivePoints = this.responsivePointsInitialized;
    const previousCanvasWidth = Number(options.previousCanvasWidth) > 1 ? Number(options.previousCanvasWidth) : this.canvasWidth();
    const previousCanvasHeight = Number(options.previousCanvasHeight) > 1 ? Number(options.previousCanvasHeight) : this.canvasHeight();
    let migrated = false;
    const elementIds = /* @__PURE__ */ new Set();
    if (needsElementLayoutMigration(this.drawingData?.strokes) && !isStableResponsiveCaptureFrame(this.canvasWidth(), context.frame)) {
      this.responsivePointsInitialized = false;
      this.responsiveLayoutSignature = "";
      return;
    }
    const lineToCanvasY = (path, line) => this.projectLineLocation(path, line, context);
    for (const [index, stroke] of (this.drawingData?.strokes || []).entries()) {
      if (isConnectorStroke(stroke)) {
        continue;
      }
      const noteFlow = normalizeNoteFlow(stroke?.noteFlow);
      if (noteFlow && (!noteFlow.positionBasis || noteFlow.positionVersion < 1)) {
        const recaptured = this.captureNoteFlowAnchor(stroke, { preservePlacement: true });
        stroke.noteFlow = recaptured;
        if (recaptured?.positionBasis) {
          this.captureNoteFlowResponsiveAnchors(stroke, context);
        }
      }
      const existingLayout = normalizeElementLayout(stroke.layout);
      const needsLayoutRepair = Boolean(existingLayout) && (elementLayoutNeedsRepair(existingLayout) || elementLayoutExceedsTarget(existingLayout, this.canvasHeight()));
      const hasUniqueElementLayout = Boolean(existingLayout?.id) && !elementIds.has(existingLayout.id) && !needsLayoutRepair;
      if (!hasUniqueElementLayout) {
        if (needsLayoutRepair) {
          stroke.points = this.projectStrokePointsForLayoutRepair(stroke, context, existingLayout);
          stroke.layout = null;
        } else if (existingLayout) {
          stroke.layout = { ...existingLayout, id: "" };
        }
        if (!needsLayoutRepair) {
          stroke.points = stroke.points.map((point) => this.captureResponsivePoint(point, context));
        }
        this.captureElementLayoutForStroke(stroke, context, index, { preserveRelations: !needsLayoutRepair });
        migrated = true;
      }
      const elementId = normalizeElementLayout(stroke.layout)?.id;
      if (elementId) {
        elementIds.add(elementId);
      }
    }
    if (migrated) {
      this.rebuildElementRelations();
    }
    const projected = [];
    const layoutsById = /* @__PURE__ */ new Map();
    const currentBoundsById = /* @__PURE__ */ new Map();
    for (const stroke of this.drawingData?.strokes || []) {
      if (isConnectorStroke(stroke)) {
        continue;
      }
      const layout = normalizeElementLayout(stroke.layout);
      if (layout?.id) {
        const currentBounds = getStrokeBounds(stroke, previousCanvasWidth, previousCanvasHeight);
        if (currentBounds) {
          currentBoundsById.set(layout.id, currentBounds);
        }
      }
      if (normalizeNoteFlow(stroke?.noteFlow)) {
        continue;
      }
      const box = projectElementLayout(layout, {
        canvasWidth: this.canvasWidth(),
        canvasHeight: this.canvasHeight(),
        frame: context.frame,
        viewportHeight: context.viewportHeight,
        lineToCanvasY,
        preferDocumentFlow: isMobileRuntime()
      });
      if (layout?.id && box) {
        projected.push(box);
        layoutsById.set(layout.id, layout);
      }
    }
    const transitionProjected = [...projected];
    const noteFlowProjectedById = /* @__PURE__ */ new Map();
    for (const [index, stroke] of (this.drawingData?.strokes || []).entries()) {
      let noteFlow = normalizeNoteFlow(stroke?.noteFlow);
      if (!noteFlow || isConnectorStroke(stroke)) {
        continue;
      }
      const transitionId = `note-flow:${strokeElementId(stroke) || index}`;
      const previousBounds = getStrokeBounds(stroke, previousCanvasWidth, previousCanvasHeight);
      const layout = normalizeElementLayout(stroke.layout);
      if (previousBounds && !(noteFlow.boxWidthRatio > 0 && noteFlow.boxHeightRatio > 0)) {
        const sourceLeft = Number(layout?.sourceFrame?.contentLeft ?? context.frame?.left ?? 0);
        const sourceWidth = Math.max(1, Number(layout?.sourceFrame?.contentWidth ?? context.frame?.width ?? previousCanvasWidth));
        stroke.noteFlow = {
          ...stroke.noteFlow,
          boxLeftRatio: (previousBounds.minX - sourceLeft) / sourceWidth,
          boxWidthRatio: Math.max(2 / sourceWidth, (previousBounds.maxX - previousBounds.minX) / sourceWidth),
          boxHeightRatio: Math.max(2 / sourceWidth, (previousBounds.maxY - previousBounds.minY) / sourceWidth)
        };
        noteFlow = normalizeNoteFlow(stroke.noteFlow);
        migrated = true;
      }
      const projectedPoints = (stroke.points || []).map((point) => {
        const projectedPoint = projectResponsivePoint(point, {
          canvasWidth: this.canvasWidth(),
          canvasHeight: this.canvasHeight(),
          frame: context.frame,
          lineToCanvasY
        });
        return noteFlow.positionBasis === "document" && noteFlow.positionVersion >= 1 ? projectNoteFlowDocumentPoint(point, projectedPoint, { canvasHeight: this.canvasHeight() }) : projectedPoint;
      });
      const projectedBounds2 = getStrokeBounds({ ...stroke, points: projectedPoints }, this.canvasWidth(), this.canvasHeight());
      const layoutBox = projectElementLayout(layout, {
        canvasWidth: this.canvasWidth(),
        canvasHeight: this.canvasHeight(),
        frame: context.frame,
        viewportHeight: context.viewportHeight,
        lineToCanvasY,
        preferDocumentFlow: isMobileRuntime()
      });
      const projectedBox = layoutBox && projectedBounds2 ? {
        ...layoutBox,
        y: clamp10(projectedBounds2.minY, 0, Math.max(0, this.canvasHeight() - layoutBox.height)),
        anchorY: clamp10(projectedBounds2.minY, 0, Math.max(0, this.canvasHeight() - layoutBox.height))
      } : null;
      if (projectedBox) {
        noteFlowProjectedById.set(transitionId, projectedBox);
      }
      if (previousBounds && (projectedBox || projectedBounds2)) {
        const transitionBox2 = projectedBox || {
          x: projectedBounds2.minX,
          y: projectedBounds2.minY,
          width: projectedBounds2.maxX - projectedBounds2.minX,
          height: projectedBounds2.maxY - projectedBounds2.minY
        };
        currentBoundsById.set(transitionId, previousBounds);
        transitionProjected.push({
          ...transitionBox2,
          id: transitionId
        });
      }
    }
    if (this.surfaceType === "preview" && hadResponsivePoints && !migrated) {
      const transition = settleProjectedElementTransition(
        transitionProjected,
        currentBoundsById,
        this.responsiveProjectionPending,
        { now: Date.now() }
      );
      this.responsiveProjectionPending = transition.pending;
      if (!transition.commit) {
        this.preserveAbsoluteStrokePlacement(previousCanvasWidth, previousCanvasHeight);
        this.scheduleResponsiveProjectionSettle(200, {
          preserveNoteFlowAbsolute: options.preserveNoteFlowAbsolute === true
        });
        return false;
      }
      this.cancelResponsiveProjectionSettle();
    } else {
      this.responsiveProjectionPending = null;
    }
    const projectedById = new Map(projected.map((box) => {
      const stabilized = stabilizeProjectedElementBox(box, currentBoundsById.get(box.id));
      return [box.id, {
        ...stabilized,
        x: clamp10(stabilized.x, 0, Math.max(0, this.canvasWidth() - stabilized.width)),
        y: clamp10(stabilized.y, 0, Math.max(0, this.canvasHeight() - stabilized.height))
      }];
    }));
    for (const [index, stroke] of (this.drawingData?.strokes || []).entries()) {
      if (isConnectorStroke(stroke)) {
        continue;
      }
      if (normalizeNoteFlow(stroke?.noteFlow)) {
        if (options.preserveNoteFlowAbsolute) {
          stroke.points = preserveAbsoluteNoteFlowPoints(stroke.points, {
            previousWidth: options.previousCanvasWidth,
            previousHeight: options.previousCanvasHeight,
            nextWidth: this.canvasWidth(),
            nextHeight: this.canvasHeight()
          });
          continue;
        }
        const noteFlow = normalizeNoteFlow(stroke.noteFlow);
        const layout2 = normalizeElementLayout(stroke.layout);
        const transitionId = `note-flow:${strokeElementId(stroke) || index}`;
        const projectedBox = noteFlowProjectedById.get(transitionId);
        const box2 = projectedBox ? stabilizeProjectedElementBox(projectedBox, currentBoundsById.get(transitionId)) : null;
        const sourceContentWidth = Number(layout2?.sourceFrame?.contentWidth) || 0;
        const targetContentWidth = Number(context.frame?.width) || this.canvasWidth();
        const layoutWidthRatio = sourceContentWidth >= 24 && Number(layout2?.box?.width) > 0 ? Number(layout2.box.width) / sourceContentWidth : 0;
        const layoutHeightRatio = sourceContentWidth >= 24 && Number(layout2?.box?.height) > 0 ? Number(layout2.box.height) / sourceContentWidth : 0;
        const fallbackBox = box2 || projectedBounds;
        const stableFlowBox = noteFlow && (layout2 || fallbackBox) ? {
          ...box2 || {},
          ...projectStableNoteFlowBox({
            boxLeftRatio: noteFlow.boxWidthRatio > 0 ? noteFlow.boxLeftRatio : sourceContentWidth >= 24 ? (Number(layout2?.box?.x) - Number(layout2?.sourceFrame?.contentLeft || 0)) / sourceContentWidth : (Number(fallbackBox?.x) - Number(context.frame?.left || 0)) / Math.max(24, targetContentWidth),
            boxWidthRatio: noteFlow.boxWidthRatio > 0 ? noteFlow.boxWidthRatio : layoutWidthRatio || Number(fallbackBox?.width) / Math.max(24, targetContentWidth),
            boxHeightRatio: noteFlow.boxHeightRatio > 0 ? noteFlow.boxHeightRatio : layoutHeightRatio || Number(fallbackBox?.height) / Math.max(24, targetContentWidth),
            contentLeft: context.frame?.left,
            contentWidth: context.frame?.width,
            canvasWidth: this.canvasWidth(),
            fallbackWidth: sourceContentWidth >= 24 ? Number(layout2?.box?.width) * targetContentWidth / sourceContentWidth : Number(fallbackBox?.width) || Number(layout2?.box?.width) || 0,
            fallbackHeight: sourceContentWidth >= 24 ? Number(layout2?.box?.height) * targetContentWidth / sourceContentWidth : Number(fallbackBox?.height) || Number(layout2?.box?.height) || 0,
            y: this.noteFlowStoredRowCanvasY(noteFlow) ?? box2?.y ?? layout2?.box?.y ?? 0
          })
        } : box2;
        if (layout2 && stableFlowBox) {
          stroke.points = projectElementPoints(stroke.points, layout2, stableFlowBox, {
            canvasWidth: this.canvasWidth(),
            canvasHeight: this.canvasHeight(),
            preserveAspectRatio: !isTextLikeStroke(stroke) && !isEmbedStroke(stroke)
          });
          const uniformScale = Math.min(
            Number(stableFlowBox.width) / Math.max(0.01, Number(layout2.box.width)),
            Number(stableFlowBox.height) / Math.max(0.01, Number(layout2.box.height))
          );
          const metrics = scaleElementMetrics(layout2.metrics, !isTextLikeStroke(stroke) && !isEmbedStroke(stroke) ? { scale: Number.isFinite(uniformScale) && uniformScale > 0 ? uniformScale : 1 } : stableFlowBox);
          if (metrics.width) {
            stroke.width = metrics.width;
          }
          continue;
        }
        const previousPoints = stroke.points;
        const projectedPoints = stroke.points.map((point) => {
          const projectedPoint = projectResponsivePoint(point, {
            canvasWidth: this.canvasWidth(),
            canvasHeight: this.canvasHeight(),
            frame: context.frame,
            lineToCanvasY
          });
          return noteFlow.positionBasis === "document" && noteFlow.positionVersion >= 1 ? projectNoteFlowDocumentPoint(point, projectedPoint, { canvasHeight: this.canvasHeight() }) : projectedPoint;
        });
        stroke.points = stabilizeNoteFlowPointProjection(previousPoints, projectedPoints, {
          canvasWidth: this.canvasWidth(),
          canvasHeight: this.canvasHeight()
        });
        continue;
      }
      const layout = normalizeElementLayout(stroke.layout);
      const box = layout?.id ? projectedById.get(layout.id) : null;
      if (layout && box) {
        stroke.points = projectElementPoints(stroke.points, layout, box, {
          canvasWidth: this.canvasWidth(),
          canvasHeight: this.canvasHeight(),
          preserveAspectRatio: !isTextLikeStroke(stroke) && !isEmbedStroke(stroke)
        });
        const uniformScale = Math.min(
          Number(box.width) / Math.max(0.01, Number(layout.box.width)),
          Number(box.height) / Math.max(0.01, Number(layout.box.height))
        );
        const metrics = scaleElementMetrics(layout.metrics, !isTextLikeStroke(stroke) && !isEmbedStroke(stroke) ? { scale: Number.isFinite(uniformScale) && uniformScale > 0 ? uniformScale : 1 } : box);
        if (metrics.width) {
          stroke.width = metrics.width;
        }
        if (isTextLikeStroke(stroke) && metrics.fontSize) {
          stroke.fontSize = metrics.fontSize;
        }
        if (isTextStroke(stroke)) {
          stroke.textWidth = metrics.textWidth || null;
        }
        if ((isTextLikeStroke(stroke) || isEmbedStroke(stroke)) && metrics.previewWidth && metrics.previewHeight) {
          stroke.previewWidth = metrics.previewWidth;
          stroke.previewHeight = metrics.previewHeight;
        }
      } else {
        stroke.points = stroke.points.map((point) => projectResponsivePoint(point, {
          canvasWidth: this.canvasWidth(),
          canvasHeight: this.canvasHeight(),
          frame: context.frame,
          lineToCanvasY
        }));
      }
    }
    this.restoreTextHighlightAnchors();
    this.syncBoundConnectors({ recover: true });
    if (this.currentStroke?.points?.length) {
      this.currentStroke.points = this.currentStroke.points.map((point) => projectResponsivePoint(point, {
        canvasWidth: this.canvasWidth(),
        canvasHeight: this.canvasHeight(),
        frame: context.frame,
        lineToCanvasY
      }));
    }
    this.responsivePointsInitialized = true;
    this.responsiveLayoutSignature = signature;
    this.responsiveProjectionPending = null;
    return true;
  }
  resizeCanvas(options = {}) {
    this.refreshScrollContainer();
    const previousCanvasWidth = this.canvasCssWidth;
    const previousCanvasHeight = this.canvasCssHeight;
    const initialMeasure = this.canvasCssWidth <= 1 || this.canvasCssHeight <= 1;
    const interactionActive = this.isReadingZoomInteractionActive();
    const refreshGeometry = options.measure !== false && !interactionActive || initialMeasure;
    const refreshLayout = options.layout === true && !interactionActive;
    const visualScale = this.readingZoomScale();
    let measured;
    let width;
    let height;
    if (refreshGeometry) {
      this.layoutMeasureEl = findLayoutMeasureElement(this.previewEl);
      measured = measureCanvasExtent(this.previewEl, this.layoutMeasureEl, visualScale);
      width = Math.max(1, Math.round(measured.width));
      const measuredHeight = Math.max(1, Math.round(measured.height));
      const extentFrame = measureResponsiveContentFrame(this.previewEl, this.surfaceType, width, this.canvas, visualScale);
      height = this.drawingsLoaded ? estimateStableElementLayoutExtent((this.drawingData?.strokes || []).map((stroke) => stroke.layout), {
        canvasWidth: width,
        frame: extentFrame,
        minHeight: measuredHeight
      }) : measuredHeight;
    } else {
      width = this.canvasCssWidth;
      height = this.canvasCssHeight;
      measured = { visibleWidth: Math.max(1, this.previewEl.getBoundingClientRect?.().width || width) };
    }
    const visible = measureVisibleSurfaceWindow(this.previewEl, this.scrollContainer, height, visualScale);
    const isMobile = isMobileRuntime();
    const devicePixelRatio = window.devicePixelRatio || 1;
    const maxDevicePixelRatio = isMobile ? 4 : 3;
    const maxPixels = isMobile ? 6 * 1024 * 1024 : 16 * 1024 * 1024;
    const maxWindowHeight = calculateQualityWindowLimit({
      cssWidth: width,
      viewportHeight: visible.height,
      devicePixelRatio,
      maxDevicePixelRatio,
      maxPixels
    });
    const canvasWindow = calculateCanvasWindow({
      documentHeight: height,
      viewportTop: visible.top,
      viewportHeight: visible.height,
      previousTop: this.canvasWindowTop,
      previousHeight: this.canvasRenderHeight,
      minWindowHeight: calculateZoomAwareWindowFloor({ visualScale }),
      maxWindowHeight
    });
    const backingStore = calculateCanvasBackingStore({
      cssWidth: width,
      cssHeight: canvasWindow.height,
      devicePixelRatio,
      maxDevicePixelRatio,
      maxDimension: isMobile ? 8192 : 16384,
      maxPixels
    });
    const geometryChanged = width !== this.canvasCssWidth || height !== this.canvasCssHeight || canvasWindow.changed || Math.abs(backingStore.scale - this.canvasBackingScale) > 1e-6;
    const layerBacking = this.drawingsVisible && this.drawingsLoaded ? backingStore : { width: 1, height: 1, scale: 1 };
    const activeBacking = this.drawingsVisible && this.drawingsLoaded && this.active ? backingStore : { width: 1, height: 1, scale: 1 };
    const backingStoreChanged = this.canvas.width !== activeBacking.width || this.canvas.height !== activeBacking.height || this.underlayCanvas.width !== layerBacking.width || this.underlayCanvas.height !== layerBacking.height || this.staticCanvas.width !== layerBacking.width || this.staticCanvas.height !== layerBacking.height;
    this.canvasCssWidth = width;
    this.canvasCssHeight = height;
    this.canvasWindowTop = canvasWindow.top;
    this.canvasRenderHeight = canvasWindow.height;
    this.canvasBackingScale = backingStore.scale;
    if (options.preserveAbsolutePlacement === true && previousCanvasWidth > 1 && previousCanvasHeight > 1) {
      this.preserveAbsoluteStrokePlacement(previousCanvasWidth, previousCanvasHeight);
    }
    applyElementStyles(this.embedLayer, {
      width: `${width}px`,
      height: `${height}px`
    });
    applyElementStyles(this.underlayEmbedLayer, {
      width: `${width}px`,
      height: `${height}px`
    });
    for (const canvas of [this.underlayCanvas, this.staticCanvas, this.canvas]) {
      applyElementStyles(canvas, {
        top: `${canvasWindow.top}px`,
        width: `${width}px`,
        height: `${canvasWindow.height}px`
      });
    }
    if (this.canvas.width !== activeBacking.width || this.canvas.height !== activeBacking.height) {
      this.canvas.width = activeBacking.width;
      this.canvas.height = activeBacking.height;
    }
    this.ctx = this.canvas.getContext("2d");
    if (!this.ctx) {
      return false;
    }
    this.ctx.setTransform(activeBacking.scale, 0, 0, activeBacking.scale, 0, -canvasWindow.top * activeBacking.scale);
    if (this.underlayCanvas.width !== layerBacking.width || this.underlayCanvas.height !== layerBacking.height) {
      this.underlayCanvas.width = layerBacking.width;
      this.underlayCanvas.height = layerBacking.height;
      this.underlayCtx = this.underlayCanvas.getContext("2d");
    }
    if (!this.underlayCtx) {
      this.underlayCtx = this.underlayCanvas.getContext("2d");
    }
    if (!this.underlayCtx) {
      return false;
    }
    this.underlayCtx.setTransform(layerBacking.scale, 0, 0, layerBacking.scale, 0, -canvasWindow.top * layerBacking.scale);
    if (this.staticCanvas.width !== layerBacking.width || this.staticCanvas.height !== layerBacking.height) {
      this.staticCanvas.width = layerBacking.width;
      this.staticCanvas.height = layerBacking.height;
      this.staticCtx = this.staticCanvas.getContext("2d");
    }
    if (!this.staticCtx) {
      this.staticCtx = this.staticCanvas.getContext("2d");
    }
    if (!this.staticCtx) {
      return false;
    }
    this.staticCtx.setTransform(layerBacking.scale, 0, 0, layerBacking.scale, 0, -canvasWindow.top * layerBacking.scale);
    if (this.drawingsLoaded && refreshLayout) {
      const frame = this.getResponsiveContentFrame();
      const viewportHeight = measureResponsiveViewportHeight(this.previewEl, this.scrollContainer, this.responsiveViewportScale());
      const signature = responsiveLayoutSignature(width, height, frame, this.surfaceType, viewportHeight);
      if (!this.responsivePointsInitialized || signature !== this.responsiveLayoutSignature) {
        this.responsiveLayoutContext = null;
        const context = this.getResponsiveLayoutContext(true);
        this.initializeAndProjectResponsivePoints(context, signature, {
          preserveNoteFlowAbsolute: options.preserveNoteFlowAbsolute === true && previousCanvasWidth > 1 && previousCanvasHeight > 1,
          previousCanvasWidth,
          previousCanvasHeight
        });
      } else if (this.responsiveProjectionPending) {
        this.preserveAbsoluteStrokePlacement(previousCanvasWidth, previousCanvasHeight);
        this.responsiveProjectionPending = null;
        this.cancelResponsiveProjectionSettle();
      }
    }
    if (geometryChanged || backingStoreChanged) {
      this.invalidateStaticCache();
    }
    if (measured.visibleWidth > 0) {
      for (const canvas of [this.underlayCanvas, this.staticCanvas, this.canvas]) {
        applyElementStyles(canvas, { minWidth: `${Math.round(measured.visibleWidth)}px` });
      }
    }
    this.updateReadingZoomExtent(visualScale, this.readingZoomTarget);
    this.previewEl.addClass("has-notedraw-canvas");
    return geometryChanged || backingStoreChanged;
  }
  onPointerDown(event, routed = false) {
    if (!this.active || event.button !== 0) {
      return;
    }
    let ownedBlankSpaceStrokeIndex = -1;
    if (!routed) {
      this.plugin.setInteractionController(this);
      const ownPoint = this.eventToPoint(event);
      if (this.toolMode === TOOL_SELECT) {
        ownedBlankSpaceStrokeIndex = this.findOwnedBlankSpaceStrokeAtClientPoint(event.clientX, event.clientY);
      }
      if (this.toolMode !== TOOL_EDIT_MD && this.findStrokeAt(ownPoint, { x: event.clientX, y: event.clientY }) < 0 && ownedBlankSpaceStrokeIndex < 0) {
        const embeddedController = this.plugin.findEmbeddedStrokeControllerAtPoint(this, event);
        if (embeddedController) {
          this.routedPointerController = embeddedController;
          embeddedController.routedPointerHost = this;
          this.activePointerId = event.pointerId;
          try {
            this.canvas.setPointerCapture(event.pointerId);
          } catch (error) {
            void error;
          }
          embeddedController.onPointerDown(event, true);
          return;
        }
      }
    }
    if (this.shouldPassThroughHeaderPoint(event)) {
      return;
    }
    if (event.pointerType === "touch") {
      if (event.isPrimary && this.touchPointers.size && !this.pointerDown && this.activePointerId === null) {
        this.resetTouchGestureState();
      }
      this.touchPointers.set(event.pointerId, { x: event.clientX, y: event.clientY });
      if (this.suppressTouchDrawing || this.touchPointers.size >= 2) {
        this.startMultiTouchScroll(event);
        return;
      }
    }
    const target = this.directMarkdownPointerId === event.pointerId && this.directMarkdownPointerElement?.isConnected ? this.directMarkdownPointerElement : this.elementBelowCanvas(event.clientX, event.clientY);
    const metadataTarget = Array.from(activeDocument.elementsFromPoint?.(event.clientX, event.clientY) || []).map((element) => element?.closest?.(METADATA_PROPERTY_SELECTOR)).find((element) => element && this.previewEl?.contains?.(element));
    const targetForInteraction = metadataTarget || target;
    const canEditMarkdownText = this.toolMode === TOOL_EDIT_MD;
    const clientPoint = { x: event.clientX, y: event.clientY };
    const editableCandidate = canEditMarkdownText ? findEditableTarget(targetForInteraction, this.previewEl, clientPoint) : null;
    const markdownSelectionCandidate = this.toolMode === TOOL_SELECT ? this.markdownBlockElementForTarget(target, clientPoint) || this.markdownBlockElementForTarget(metadataTarget, clientPoint) : null;
    const selectedMarkdownEditableCandidate = markdownSelectionCandidate ? findEditableTarget(target, this.previewEl, clientPoint) || findEditableTarget(markdownSelectionCandidate, this.previewEl, clientPoint) || findEditableTarget(targetForInteraction, this.previewEl, clientPoint) : null;
    const editableFile = editableCandidate ? this.plugin.resolveEditableFile(editableCandidate, this.file) : null;
    const editsEmbeddedFile = Boolean(editableFile?.path && editableFile.path !== this.file?.path);
    const editable = editableCandidate && (this.allowTextEdit || editsEmbeddedFile) ? editableCandidate : null;
    const sourceTextTarget = this.surfaceType === "source" && canEditMarkdownText && isSourceTextTarget(targetForInteraction, this.previewEl);
    const point = this.eventToPoint(event);
    const noteFlowPenActive = this.isNoteFlowPenActive();
    if (noteFlowPenActive && this.hasHybridSelection()) {
      this.clearSelectedStrokes();
    }
    let hitStrokeIndex = noteFlowPenActive ? -1 : this.findStrokeAt(point, clientPoint);
    const selectedStrokeHit = hitStrokeIndex >= 0 && this.isStrokeSelected(hitStrokeIndex);
    if (markdownSelectionCandidate && hitStrokeIndex >= 0 && !selectedStrokeHit && shouldPlaceStrokeBelowMarkdown(this.drawingData.strokes[hitStrokeIndex])) {
      hitStrokeIndex = -1;
    }
    const markdownSelectionRecord = markdownSelectionCandidate ? this.findMarkdownBlockRecordForElement(markdownSelectionCandidate) : null;
    const markdownCandidateSelected = Boolean(markdownSelectionRecord && this.selectedMarkdownBlockIds.has(markdownSelectionRecord.id));
    let resizeHandle = noteFlowPenActive ? null : this.findSelectionHandleAt(point);
    const connectorEndpointHit = this.toolMode === TOOL_SELECT && !noteFlowPenActive ? this.findSelectedConnectorEndpointAt(point) : null;
    if (this.toolMode === TOOL_SELECT && hitStrokeIndex < 0 && !resizeHandle && !markdownSelectionCandidate) {
      hitStrokeIndex = ownedBlankSpaceStrokeIndex >= 0 ? ownedBlankSpaceStrokeIndex : this.findOwnedBlankSpaceStrokeAtClientPoint(event.clientX, event.clientY);
    }
    const hadSelection = this.hasHybridSelection();
    const selectedDrawGesture = resolveSelectedDrawGesture({
      toolMode: this.toolMode,
      hasSelection: hadSelection,
      hitStrokeIndex,
      insideSelectionFrame: this.selectedStrokeFrameContains(point)
    });
    if (this.toolMode === TOOL_TEXT && this.textPreset === "arrow") {
      this.startConnectorGesture(event, point, routed);
      return;
    }
    if (connectorEndpointHit) {
      this.startConnectorReconnectGesture(event, connectorEndpointHit, routed);
      return;
    }
    if (resizeHandle) {
      this.startSelectedStrokeResize(event, resizeHandle);
      return;
    }
    const lockedGroupFrameHit = this.toolMode === TOOL_SELECT && hitStrokeIndex < 0 && !markdownSelectionCandidate ? this.findLockedElementGroupFrameAtPoint(point) : null;
    if (lockedGroupFrameHit) {
      if (!this.isElementGroupFullySelected(lockedGroupFrameHit.id)) {
        this.startPendingSelectionTap(event, {
          type: "select-group",
          groupId: lockedGroupFrameHit.id
        });
      } else {
        this.startSelectedStrokeDrag(event, point, -1, { preserveSelection: true });
      }
      return;
    }
    if (canEditMarkdownText) {
      this.currentStroke = null;
      this.pointerDown = false;
      this.pointerStartEditable = null;
      this.clearSelectedStrokes();
      if (editable) {
        this.startTextEdit(editable, { x: event.clientX, y: event.clientY });
      } else if (sourceTextTarget) {
        this.endTextEdit();
        this.focusSourceEditorAt({ x: event.clientX, y: event.clientY });
      } else {
        this.endTextEdit();
        this.render();
      }
      event.preventDefault();
      event.stopPropagation();
      return;
    }
    if (hitStrokeIndex >= 0 && isTextLikeStroke(this.drawingData.strokes[hitStrokeIndex])) {
      const repeatedSelectionTap = this.toolMode === TOOL_SELECT && this.isRepeatTextTap(hitStrokeIndex, point, event);
      if (event.detail >= 2 || repeatedSelectionTap) {
        this.editFloatingTextStroke(hitStrokeIndex);
        this.lastTextTap = null;
        event.preventDefault();
        event.stopPropagation();
        return;
      }
      if (this.toolMode === TOOL_SELECT) {
        this.rememberTextTap(hitStrokeIndex, point, event);
      }
    }
    if (this.toolMode === TOOL_SELECT && hitStrokeIndex >= 0) {
      const additiveSelect = event.shiftKey || event.ctrlKey || event.metaKey;
      const hitGroupId = this.drawingData.strokes[hitStrokeIndex]?.groupId || "";
      const hitGroupIsLocked = Boolean(hitGroupId && this.elementGroup(hitGroupId)?.locked);
      if (!additiveSelect && hitGroupId && this.isElementGroupFullySelected(hitGroupId) && !this.enteredElementGroupIds.has(hitGroupId)) {
        this.startPendingSelectionTap(event, {
          type: "enter-stroke-group",
          index: hitStrokeIndex,
          groupId: hitGroupId
        });
        return;
      }
      if (!additiveSelect && hitGroupIsLocked && !this.enteredElementGroupIds.has(hitGroupId)) {
        this.startPendingSelectionTap(event, {
          type: "enter-stroke-group",
          index: hitStrokeIndex,
          groupId: hitGroupId
        });
        return;
      }
      if (additiveSelect) {
        this.startPendingSelectionTap(event, { type: "toggle-stroke", index: hitStrokeIndex });
        return;
      }
      const wasSelected = this.isStrokeSelected(hitStrokeIndex);
      if (!wasSelected) {
        this.startPendingSelectionTap(event, { type: "select-stroke", index: hitStrokeIndex });
        return;
      }
      const preserveGroupSelection = Boolean(hitGroupId && this.isElementGroupFullySelected(hitGroupId));
      if (!additiveSelect && isTextLikeStroke(this.drawingData.strokes[hitStrokeIndex])) {
        this.startPendingSelectionTap(event, {
          type: "edit-stroke-or-drag",
          index: hitStrokeIndex,
          preserveSelection: preserveGroupSelection
        });
        return;
      }
      if (!additiveSelect && this.hasHybridSelection()) {
        this.startPendingSelectionTap(event, {
          type: "drag-stroke-or-toggle",
          index: hitStrokeIndex,
          preserveSelection: preserveGroupSelection
        });
        return;
      }
      this.startSelectedStrokeDrag(event, point, hitStrokeIndex, { preserveSelection: preserveGroupSelection });
      return;
    }
    if (this.toolMode === TOOL_SELECT && hitStrokeIndex < 0 && markdownSelectionCandidate) {
      const additiveSelect = event.shiftKey || event.ctrlKey || event.metaKey;
      const existing = markdownSelectionRecord;
      const wasSelected = Boolean(existing && this.selectedMarkdownBlockIds.has(existing.id));
      const selectionActivated = this.markdownSelectionCanEditOrDrag(existing, markdownSelectionCandidate);
      const hitGroupId = existing?.groupId || "";
      const hitGroupIsLocked = Boolean(hitGroupId && this.elementGroup(hitGroupId)?.locked);
      if (!additiveSelect && hitGroupId && this.isElementGroupFullySelected(hitGroupId) && !this.enteredElementGroupIds.has(hitGroupId)) {
        this.startPendingSelectionTap(event, {
          type: "enter-markdown-group",
          element: markdownSelectionCandidate,
          groupId: hitGroupId
        });
        return;
      }
      if (!additiveSelect && hitGroupIsLocked && !this.enteredElementGroupIds.has(hitGroupId)) {
        this.startPendingSelectionTap(event, {
          type: "enter-markdown-group",
          element: markdownSelectionCandidate,
          groupId: hitGroupId
        });
        return;
      }
      if (additiveSelect) {
        this.startPendingSelectionTap(event, {
          type: "toggle-markdown",
          element: markdownSelectionCandidate
        });
        return;
      } else if (!wasSelected || !selectionActivated) {
        this.startPendingSelectionTap(event, {
          type: "select-markdown",
          element: markdownSelectionCandidate
        });
        return;
      }
      if (!additiveSelect && selectedMarkdownEditableCandidate) {
        this.startPendingSelectionTap(event, {
          type: "edit-markdown-or-drag",
          element: markdownSelectionCandidate,
          editable: selectedMarkdownEditableCandidate,
          preserveSelection: Boolean(hitGroupId && this.isElementGroupFullySelected(hitGroupId))
        });
        return;
      }
      if (!additiveSelect && this.hasHybridSelection()) {
        this.startPendingSelectionTap(event, {
          type: "drag-markdown-or-toggle",
          element: markdownSelectionCandidate,
          preserveSelection: Boolean(hitGroupId && this.isElementGroupFullySelected(hitGroupId))
        });
        return;
      }
      this.startSelectedStrokeDrag(event, point, -1, { preserveSelection: additiveSelect });
      return;
    }
    if (this.toolMode === TOOL_SELECT && hitStrokeIndex < 0 && !markdownSelectionCandidate) {
      if (event.pointerType === "touch") {
        const scrollRow = this.parallelScrollRowAt(event.clientX, event.clientY);
        if (scrollRow && scrollRow.scrollWidth > scrollRow.clientWidth + 1) {
          this.startPendingSelectionTap(event, {
            type: "scroll-row",
            scrollRow
          });
          return;
        }
      }
      const lockedGroup = this.findLockedElementGroupFrameAtPoint(point);
      if (lockedGroup) {
        if (!this.isElementGroupFullySelected(lockedGroup.id)) {
          this.startPendingSelectionTap(event, {
            type: "select-group",
            groupId: lockedGroup.id
          });
        } else {
          this.startSelectedStrokeDrag(event, point, -1, { preserveSelection: true });
        }
        return;
      }
      const boxedGroup = this.findBoxedElementGroupAtPoint(point);
      if (boxedGroup) {
        if (!this.isElementGroupFullySelected(boxedGroup.id)) {
          this.startPendingSelectionTap(event, {
            type: "select-group",
            groupId: boxedGroup.id
          });
          return;
        }
        this.startSelectedStrokeDrag(event, point);
        return;
      }
    }
    if (selectedDrawGesture === SELECTED_DRAW_GESTURE_MANIPULATE && hitStrokeIndex >= 0 && !this.isStrokeSelected(hitStrokeIndex)) {
      this.setSelectedStrokes(hitStrokeIndex);
      this.startSelectedStrokeDrag(event, point, hitStrokeIndex);
      return;
    }
    if (this.hasHybridSelection() && !this.selectedStrokeFrameContains(point) && hitStrokeIndex < 0) {
      if (selectedDrawGesture !== SELECTED_DRAW_GESTURE_DRAW_OR_DESELECT) {
        this.clearSelectedStrokes();
        if (!editable && this.toolMode !== TOOL_SELECT && this.toolMode !== TOOL_TEXT) {
          this.render();
          event.preventDefault();
          event.stopPropagation();
          return;
        }
      }
    }
    if (hitStrokeIndex >= 0 && isTextLikeStroke(this.drawingData.strokes[hitStrokeIndex]) && event.detail >= 2) {
      this.editFloatingTextStroke(hitStrokeIndex, point);
      this.lastTextTap = null;
      event.preventDefault();
      event.stopPropagation();
      return;
    }
    if (hitStrokeIndex >= 0 && isTextLikeStroke(this.drawingData.strokes[hitStrokeIndex]) && this.toolMode === TOOL_TEXT) {
      if (this.isRepeatTextTap(hitStrokeIndex, point, event)) {
        this.editFloatingTextStroke(hitStrokeIndex, point);
        this.lastTextTap = null;
        event.preventDefault();
        event.stopPropagation();
        return;
      }
      this.rememberTextTap(hitStrokeIndex, point, event);
      if (this.isStrokeSelected(hitStrokeIndex)) {
        this.startSelectedStrokeDrag(event, point, hitStrokeIndex);
        return;
      }
      this.setSelectedStrokes(hitStrokeIndex);
      this.render();
      event.preventDefault();
      event.stopPropagation();
      return;
    }
    const selectedTextIndex = this.getSelectedStrokeIndexes().find((index) => isTextLikeStroke(this.drawingData.strokes[index]));
    if (event.detail >= 2 && selectedTextIndex >= 0 && this.selectedStrokeFrameContains(point)) {
      this.editFloatingTextStroke(selectedTextIndex, point);
      this.lastTextTap = null;
      event.preventDefault();
      event.stopPropagation();
      return;
    }
    if (this.selectedStrokeFrameContains(point)) {
      this.startSelectedStrokeDrag(event, point, hitStrokeIndex);
      return;
    }
    if (this.toolMode === TOOL_SELECT) {
      this.startSelectionDrag(event, point);
      return;
    }
    this.startedOnText = Boolean(editable);
    this.pointerDown = true;
    this.didMove = false;
    this.pointerStartClient = { x: event.clientX, y: event.clientY };
    this.pointerStartPoint = point;
    this.pointerStartEditable = editable;
    this.pointerStartSourceText = sourceTextTarget;
    this.activePointerId = event.pointerId;
    if (!editable) {
      this.endTextEdit();
    }
    if (!routed) {
      try {
        this.canvas.setPointerCapture(event.pointerId);
      } catch (error) {
        void error;
      }
    }
    if (this.toolMode === TOOL_TEXT) {
      this.currentStroke = null;
      event.preventDefault();
      event.stopPropagation();
      return;
    }
    const brush = this.currentBrushSettings();
    this.currentStroke = {
      brush: this.brushMode,
      variant: this.currentBrushVariant(),
      color: brush.color,
      width: brush.width,
      opacity: brush.opacity,
      count: brush.count,
      noteFlow: this.currentBrushVariant() === PEN_VARIANT_NOTE && this.supportsNoteFlow() ? {
        enabled: true,
        path: this.file?.path || "",
        line: null,
        side: null,
        blockStart: null,
        blockEnd: null,
        blockKey: "",
        positionBasis: null,
        positionPath: this.file?.path || "",
        positionLine: null,
        gap: 12
      } : null,
      belowMarkdown: this.currentBrushVariant() === PEN_VARIANT_NOTE && this.supportsNoteFlow(),
      points: [this.pointerStartPoint]
    };
    if (this.isTextHighlightStroke(this.currentStroke)) {
      this.prepareTextHighlightGesture({ x: event.clientX, y: event.clientY });
    }
    event.preventDefault();
    event.stopPropagation();
  }
  startConnectorGesture(event, point, routed = false) {
    this.endFloatingTextInput(true);
    this.endTextEdit();
    this.clearSelectedStrokes();
    this.ensureSnapElementIds();
    const snapTargets = this.collectSnapTargets();
    const snapBoundsById = new Map(snapTargets.map((target) => [target.id, target.bounds]));
    this.pointerDown = true;
    this.didMove = false;
    this.pointerStartPoint = { ...point };
    this.pointerStartClient = { x: event.clientX, y: event.clientY };
    this.pointerStartEditable = null;
    this.pointerStartSourceText = false;
    this.activePointerId = event.pointerId;
    const fromId = this.findSnapElementIdAtPoint(point, "", snapTargets);
    const brush = this.currentBrushSettings();
    this.connectorGesture = {
      fromId,
      snapTargets,
      snapBoundsById,
      historyBefore: this.captureDrawingHistorySnapshot()
    };
    this.currentStroke = {
      elementId: createElementLayoutId(this.drawingData.strokes.length),
      brush: BRUSH_PEN,
      color: brush.color || this.penColor,
      width: clamp10(Number(brush.width || 2.5), 1, 8),
      opacity: clamp10(Number(brush.opacity ?? 0.9), 0, 1),
      count: 1,
      connector: { kind: "connector", fromId, toId: "", style: "curve", arrow: true },
      points: buildFreeConnectorPoints(point, point, this.canvasWidth(), this.canvasHeight())
    };
    if (!routed) {
      try {
        this.canvas.setPointerCapture(event.pointerId);
      } catch (error) {
        void error;
      }
    }
    event.preventDefault();
    event.stopPropagation();
  }
  updateConnectorGesture(event) {
    const endPoint = this.eventToPoint(event);
    const movedDistance = this.pointerStartClient ? pointerDistance(this.pointerStartClient, { x: event.clientX, y: event.clientY }) : 0;
    this.didMove = movedDistance > this.tapDistancePx();
    if (this.currentStroke && this.pointerStartPoint) {
      const snapTargets = this.connectorGesture?.snapTargets;
      const fromId = this.connectorGesture?.fromId || this.findSnapElementIdAtPoint(this.pointerStartPoint, "", snapTargets);
      let toId = this.findSnapElementIdAtPoint(endPoint, fromId, snapTargets);
      if (toId === fromId) {
        toId = "";
      }
      this.connectorGesture.fromId = fromId;
      this.connectorGesture.toId = toId;
      this.currentStroke.connector = { kind: "connector", fromId, toId, style: "curve", arrow: true };
      this.currentStroke.points = this.buildConnectorPointsForBindings(fromId, toId, this.pointerStartPoint, endPoint, this.connectorGesture?.snapBoundsById);
      this.requestRender();
    }
    event.preventDefault();
    event.stopPropagation();
  }
  finishConnectorGesture(event) {
    const startPoint = this.pointerStartPoint || this.eventToPoint(event);
    let endPoint = this.eventToPoint(event);
    if (!this.didMove) {
      endPoint = {
        ...startPoint,
        x: clamp10(startPoint.x + Math.min(0.22, 96 / this.canvasWidth()), 0, 1),
        t: Date.now(),
        anchor: null
      };
    }
    const snapTargets = this.connectorGesture?.snapTargets;
    const fromId = this.connectorGesture?.fromId || this.findSnapElementIdAtPoint(startPoint, "", snapTargets);
    let toId = this.connectorGesture?.toId || this.findSnapElementIdAtPoint(endPoint, fromId, snapTargets);
    if (toId === fromId) {
      toId = "";
    }
    const stroke = normalizeStroke({
      ...this.currentStroke,
      connector: { kind: "connector", fromId, toId, style: "curve", arrow: true },
      points: this.buildConnectorPointsForBindings(fromId, toId, startPoint, endPoint, this.connectorGesture?.snapBoundsById)
    });
    this.drawingData.strokes.push(stroke);
    const insertedIndex = this.drawingData.strokes.length - 1;
    this.syncBoundConnectors();
    this.setSelectedStrokes(insertedIndex);
    this.redoStack = [];
    this.invalidateStaticCache();
    this.plugin.scheduleDrawingSave(this.file, this.drawingData, { userOperation: true });
    this.recordDrawingHistory(this.connectorGesture?.historyBefore);
    this.currentStroke = null;
    this.connectorGesture = null;
    this.pointerDown = false;
    this.finishPointerInteraction(event);
  }
  findSelectedConnectorEndpointAt(point) {
    const canvasPoint2 = this.pointToCanvas(point);
    const threshold = Math.max(14, this.selectionHitPaddingPx() * 1.5);
    let best = null;
    for (const index of this.getSelectedStrokeIndexes()) {
      const stroke = this.drawingData?.strokes?.[index];
      if (!isConnectorStroke(stroke) || !stroke.points?.length) {
        continue;
      }
      const endpoints = [
        { end: "from", point: stroke.points[0] },
        { end: "to", point: stroke.points[stroke.points.length - 1] }
      ];
      for (const endpoint of endpoints) {
        const target = this.pointToCanvas(endpoint.point);
        const distance = pointerDistance(canvasPoint2, target);
        if (distance <= threshold && (!best || distance < best.distance)) {
          best = { index, end: endpoint.end, distance };
        }
      }
    }
    return best;
  }
  startConnectorReconnectGesture(event, hit, routed = false) {
    const stroke = this.drawingData?.strokes?.[hit?.index];
    const connector = normalizeConnector(stroke?.connector);
    if (!stroke || !connector || !stroke.points?.length) {
      return false;
    }
    this.endFloatingTextInput(true);
    this.endTextEdit();
    this.ensureSnapElementIds();
    const snapTargets = this.collectSnapTargets();
    this.connectorReconnect = {
      index: hit.index,
      end: hit.end === "from" ? "from" : "to",
      connector: { ...connector },
      points: stroke.points.map((point) => ({ ...point })),
      snapTargets,
      snapBoundsById: new Map(snapTargets.map((target) => [target.id, target.bounds])),
      historyBefore: this.captureDrawingHistorySnapshot()
    };
    this.pointerDown = true;
    this.didMove = false;
    this.pointerStartPoint = this.eventToPoint(event);
    this.pointerStartClient = { x: event.clientX, y: event.clientY };
    this.activePointerId = event.pointerId;
    if (!routed) {
      try {
        this.canvas.setPointerCapture(event.pointerId);
      } catch (error) {
        void error;
      }
    }
    event.preventDefault();
    event.stopPropagation();
    return true;
  }
  updateConnectorReconnectGesture(event) {
    const reconnect = this.connectorReconnect;
    const stroke = this.drawingData?.strokes?.[reconnect?.index];
    if (!reconnect || !stroke) {
      return false;
    }
    const point = this.eventToPoint(event);
    const oppositeId = reconnect.end === "from" ? reconnect.connector.toId : reconnect.connector.fromId;
    let targetId = this.findSnapElementIdAtPoint(point, oppositeId, reconnect.snapTargets);
    if (targetId === oppositeId) {
      targetId = "";
    }
    const connector = {
      ...reconnect.connector,
      [reconnect.end === "from" ? "fromId" : "toId"]: targetId
    };
    if (connector.fromId === connector.toId) {
      connector[reconnect.end === "from" ? "fromId" : "toId"] = "";
    }
    const fromPoint = reconnect.end === "from" ? point : reconnect.points[0];
    const toPoint = reconnect.end === "to" ? point : reconnect.points[reconnect.points.length - 1];
    stroke.connector = connector;
    stroke.points = this.buildConnectorPointsForBindings(
      connector.fromId,
      connector.toId,
      fromPoint,
      toPoint,
      reconnect.snapBoundsById
    );
    this.didMove = true;
    this.invalidateStaticCache();
    this.requestRender();
    event.preventDefault();
    event.stopPropagation();
    return true;
  }
  finishConnectorReconnectGesture(event) {
    const reconnect = this.connectorReconnect;
    if (!reconnect) {
      return;
    }
    if (event.type === "pointercancel") {
      this.cancelConnectorReconnectGesture(event);
      return;
    }
    this.updateConnectorReconnectGesture(event);
    this.connectorReconnect = null;
    this.syncConnectorElementGroups();
    this.redoStack = [];
    this.plugin.scheduleDrawingSave(this.file, this.drawingData, { userOperation: true });
    this.recordDrawingHistory(reconnect.historyBefore);
    this.finishPointerInteraction(event);
  }
  cancelConnectorReconnectGesture(event = null) {
    const reconnect = this.connectorReconnect;
    const stroke = this.drawingData?.strokes?.[reconnect?.index];
    if (reconnect && stroke) {
      stroke.connector = reconnect.connector ? { ...reconnect.connector } : null;
      stroke.points = reconnect.points.map((point) => ({ ...point }));
    }
    this.connectorReconnect = null;
    this.pointerDown = false;
    this.finishPointerInteraction(event);
  }
  findSnapElementIdAtPoint(point, excludeId = "", targets = null) {
    const canvasPoint2 = this.pointToCanvas(point);
    const threshold = connectorSnapThreshold(this.selectionHitPaddingPx());
    let best = null;
    const candidates = Array.isArray(targets) ? targets : this.collectSnapTargets();
    for (const target of candidates) {
      if (target.id === excludeId) {
        continue;
      }
      const dx = Math.max(target.bounds.minX - canvasPoint2.x, 0, canvasPoint2.x - target.bounds.maxX);
      const dy = Math.max(target.bounds.minY - canvasPoint2.y, 0, canvasPoint2.y - target.bounds.maxY);
      const distance = Math.hypot(dx, dy);
      if (distance <= threshold && (!best || distance < best.distance)) {
        best = { id: target.id, distance };
      }
    }
    return best?.id || "";
  }
  collectSnapTargets() {
    const targets = [];
    for (let index = (this.drawingData?.strokes?.length || 0) - 1; index >= 0; index -= 1) {
      const stroke = this.drawingData.strokes[index];
      const id = strokeElementId(stroke);
      if (!id || isConnectorStroke(stroke) || !isSnapStroke(stroke)) {
        continue;
      }
      const bounds = getStrokeBounds(stroke, this.canvasWidth(), this.canvasHeight());
      if (bounds) {
        targets.push({ id, bounds, type: "stroke" });
      }
    }
    for (const block of this.markdownBlockRecords()) {
      if (!block?.id || !block.borderColor && !block.backgroundColor) {
        continue;
      }
      const bounds = this.markdownElementCanvasBounds(this.markdownBlockElement(block), { forSelection: true });
      if (bounds) {
        targets.push({ id: `md:${block.id}`, bounds, type: "markdown" });
      }
    }
    for (const group of this.elementGroupRecords()) {
      if (!group?.id || !group.boxed) {
        continue;
      }
      const bounds = this.getElementGroupBounds(group.id);
      if (bounds) {
        targets.push({ id: `group:${group.id}`, bounds, type: "group" });
      }
    }
    return targets;
  }
  connectorTargetBoundsById() {
    return new Map(this.collectSnapTargets().map((target) => [target.id, target.bounds]));
  }
  ensureSnapElementIds() {
    const strokes = this.drawingData?.strokes || [];
    const idCounts = /* @__PURE__ */ new Map();
    for (const stroke of strokes) {
      const id = strokeElementId(stroke);
      if (id) {
        idCounts.set(id, (idCounts.get(id) || 0) + 1);
      }
    }
    const occupied = new Set(idCounts.keys());
    let changed = false;
    for (const [index, stroke] of strokes.entries()) {
      if (!stroke || isConnectorStroke(stroke) || !isSnapStroke(stroke)) {
        continue;
      }
      const existingId = strokeElementId(stroke);
      let elementId = existingId;
      if (!elementId || (idCounts.get(elementId) || 0) > 1) {
        do {
          elementId = createElementLayoutId(index);
        } while (occupied.has(elementId));
        occupied.add(elementId);
      }
      if (stroke.elementId !== elementId) {
        stroke.elementId = elementId;
        changed = true;
      }
      const layout = normalizeElementLayout(stroke.layout);
      if (layout && layout.id !== elementId) {
        stroke.layout = { ...layout, id: elementId };
        changed = true;
      }
    }
    return changed;
  }
  buildConnectorPointsForBindings(fromId, toId, fromPoint, toPoint, boundsById = null) {
    const resolvedBounds = boundsById || this.connectorTargetBoundsById();
    return buildSnappedConnectorPoints({
      fromBounds: fromId ? resolvedBounds.get(fromId) || null : null,
      toBounds: toId ? resolvedBounds.get(toId) || null : null,
      fromPoint,
      toPoint,
      canvasWidth: this.canvasWidth(),
      canvasHeight: this.canvasHeight()
    });
  }
  shouldPassThroughHeaderPoint(event) {
    if (this.surfaceType !== "preview" || !isAppleMobileRuntime()) {
      return false;
    }
    const header = this.view?.containerEl?.querySelector?.(".view-header") || this.button?.closest?.(".view-header");
    const headerRect = header?.getBoundingClientRect?.();
    if (!headerRect || event.clientY > headerRect.bottom + 4) {
      return false;
    }
    const target = this.elementBelowCanvas(event.clientX, event.clientY);
    const noteDrawButton = target?.closest?.(".notedraw-header-button, .notedraw-fallback-button, .notedraw-webview-button");
    this.toggle().catch((error) => {
      console.error(`[${PLUGIN_ID}] Failed to toggle NoteDraw`, error);
    });
    if (!noteDrawButton) {
      dispatchMouseClickThroughOverlay(this.canvas, { x: event.clientX, y: event.clientY });
    }
    event.preventDefault();
    event.stopPropagation();
    return true;
  }
  elementBelowCanvas(clientX, clientY) {
    const canvases = Array.from(activeDocument.elementsFromPoint?.(clientX, clientY) || []).filter((element) => element?.classList?.contains("notedraw-canvas"));
    if (!canvases.includes(this.canvas)) {
      canvases.unshift(this.canvas);
    }
    const previous = canvases.map((canvas) => canvas.style.pointerEvents);
    canvases.forEach((canvas) => applyElementStyles(canvas, { pointerEvents: "none" }));
    const target = activeDocument.elementFromPoint(clientX, clientY);
    canvases.forEach((canvas, index) => applyElementStyles(canvas, { pointerEvents: previous[index] || "" }));
    return target;
  }
  onPointerMove(event) {
    if (this.routedPointerController && event.pointerId === this.activePointerId) {
      this.routedPointerController.onPointerMove(event);
      return;
    }
    if (event.pointerType === "touch" && this.touchPointers.has(event.pointerId)) {
      this.touchPointers.set(event.pointerId, { x: event.clientX, y: event.clientY });
      if (this.multiTouchScrolling) {
        this.handleMultiTouchScroll(event);
        return;
      }
    }
    if (this.connectorReconnect && event.pointerId === this.activePointerId) {
      this.updateConnectorReconnectGesture(event);
      return;
    }
    if (this.draggingStroke && event.pointerId === this.activePointerId) {
      this.moveSelectedStroke(event);
      return;
    }
    if (this.resizingSelection && event.pointerId === this.activePointerId) {
      this.moveSelectedStrokeResize(event);
      return;
    }
    if (this.pendingSelectionTap && event.pointerId === this.activePointerId) {
      this.updatePendingSelectionTap(event);
      return;
    }
    if (this.selectingStrokes && event.pointerId === this.activePointerId) {
      this.updateSelectionDrag(event);
      return;
    }
    if (this.connectorGesture && event.pointerId === this.activePointerId) {
      this.updateConnectorGesture(event);
      return;
    }
    if (!this.active || !this.pointerDown || event.pointerId !== this.activePointerId) {
      return;
    }
    if (this.toolMode === TOOL_TEXT && !this.currentStroke) {
      const movedDistance = this.pointerStartClient ? pointerDistance(this.pointerStartClient, { x: event.clientX, y: event.clientY }) : 0;
      this.didMove = movedDistance > this.tapDistancePx();
      event.preventDefault();
      event.stopPropagation();
      return;
    }
    if (!this.currentStroke) {
      return;
    }
    const wasDrawing = this.didMove;
    this.addPointerSamples(event);
    if (this.didMove && !wasDrawing) {
      this.endTextEdit();
      this.clearSelectedStrokes();
      if (this.currentStroke.noteFlow?.enabled) {
        this.noteFlowOperationPending = true;
        this.scheduleMarkdownAnnotationRefresh({ layout: false });
      }
    }
    if (this.didMove) {
      this.requestRender();
    }
    event.preventDefault();
    event.stopPropagation();
  }
  onPointerUp(event) {
    if (this.routedPointerHost) {
      this.routedPointerHost.routedPointerController = null;
      this.routedPointerHost.activePointerId = null;
      this.routedPointerHost = null;
    }
    if (this.routedPointerController && event.pointerId === this.activePointerId) {
      const routedController = this.routedPointerController;
      this.routedPointerController = null;
      routedController.onPointerUp(event);
      this.activePointerId = null;
      try {
        if (this.canvas.hasPointerCapture?.(event.pointerId)) {
          this.canvas.releasePointerCapture(event.pointerId);
        }
      } catch (error) {
        void error;
      }
      return;
    }
    if (event.type === "lostpointercapture" && this.multiTouchScrolling) {
      return;
    }
    if (event.pointerType === "touch") {
      this.completeTrackedTouch(event.pointerId);
      if (this.multiTouchScrolling || this.suppressTouchDrawing) {
        event.preventDefault();
        event.stopPropagation();
        return;
      }
    }
    if (this.selectionLongPressConsumedPointerId === event.pointerId) {
      this.selectionLongPressConsumedPointerId = null;
      this.cancelPendingSelectionTap();
      if (this.draggingStroke) {
        this.clearSelectedStrokeDragState();
      }
      event.preventDefault();
      event.stopPropagation();
      return;
    }
    if (this.connectorReconnect && event.pointerId === this.activePointerId) {
      this.finishConnectorReconnectGesture(event);
      return;
    }
    if (this.draggingStroke && event.pointerId === this.activePointerId) {
      this.finishSelectedStrokeDrag(event);
      return;
    }
    if (this.pendingSelectionTap && event.pointerId === this.activePointerId) {
      if (event.type !== "pointerup") {
        this.cancelPendingSelectionTap();
      } else {
        this.finishPendingSelectionTap(event);
      }
      return;
    }
    if (this.resizingSelection && event.pointerId === this.activePointerId) {
      this.finishSelectedStrokeResize(event);
      return;
    }
    if (this.selectingStrokes && event.pointerId === this.activePointerId) {
      this.finishSelectionDrag(event);
      return;
    }
    if (this.connectorGesture && event.pointerId === this.activePointerId) {
      this.finishConnectorGesture(event);
      return;
    }
    if (!this.active || !this.pointerDown || event.pointerId !== this.activePointerId) {
      return;
    }
    this.pointerDown = false;
    const movedDistance = this.pointerStartClient ? pointerDistance(this.pointerStartClient, { x: event.clientX, y: event.clientY }) : 0;
    const editable = this.pointerStartEditable;
    const canEditMarkdownText = this.toolMode === TOOL_EDIT_MD;
    if (this.toolMode === TOOL_TEXT && !this.currentStroke) {
      const point = this.pointerStartPoint || this.eventToPoint(event);
      if (movedDistance <= this.tapDistancePx() && !this.didMove) {
        this.handleTextToolTap(point);
      }
      this.finishPointerInteraction(event);
      return;
    }
    if (!this.currentStroke) {
      this.finishPointerInteraction(event);
      return;
    }
    this.addPointerSamples(event);
    if (!this.didMove || movedDistance <= this.tapDistancePx() || this.currentStroke.points.length < 2) {
      const point = this.pointerStartPoint || this.eventToPoint(event);
      this.currentStroke = null;
      if (editable && canEditMarkdownText) {
        this.startTextEdit(editable, this.pointerStartClient || { x: event.clientX, y: event.clientY });
      } else if (this.pointerStartSourceText && canEditMarkdownText) {
        this.focusSourceEditorAt(this.pointerStartClient || { x: event.clientX, y: event.clientY });
        this.clearSelectedStrokes();
      } else if (this.toolMode === TOOL_TEXT) {
        this.handleTextToolTap(point);
      } else if (this.isNoteFlowPenActive()) {
        this.clearSelectedStrokes();
      } else {
        this.setSelectedStrokes(this.findStrokeAt(point, { x: event.clientX, y: event.clientY }));
      }
    } else if (this.currentStroke.kind === TOOL_TEXT) {
      this.currentStroke = null;
    } else if (this.isLaserPenActive()) {
      this.finalizeBrushVariant(this.currentStroke);
      const now = Date.now();
      this.laserStrokes.push({
        ...this.currentStroke,
        points: this.currentStroke.points.map((point) => ({ ...point })),
        laserStartedAt: now,
        laserExpiresAt: now + 3e3
      });
      this.currentStroke = null;
      this.scheduleLaserFade();
    } else {
      const historyBefore = this.captureDrawingHistorySnapshot();
      this.finalizeBrushVariant(this.currentStroke);
      const responsiveContext = this.getResponsiveLayoutContext();
      this.currentStroke.points = this.currentStroke.points.map((point) => this.captureResponsivePoint(point, responsiveContext));
      this.drawingData.strokes.push(this.currentStroke);
      const insertedIndex = this.drawingData.strokes.length - 1;
      const insertedNoteFlow = Boolean(this.currentStroke.noteFlow?.enabled);
      if (insertedNoteFlow) {
        this.resetNoteFlowSettle();
        if (this.noteFlowMarkdownAnnotationComplete) {
          this.currentStroke.noteFlow = this.captureNoteFlowAnchor(this.currentStroke);
        } else {
          this.scheduleMarkdownAnnotationRefresh({ layout: false, delay: 0, force: true });
        }
      }
      this.captureResponsiveAnchorsForIndexes([insertedIndex]);
      this.syncConnectorElementGroups();
      this.clearSelectedStrokes();
      this.redoStack = [];
      this.invalidateStaticCache();
      if (!insertedNoteFlow) {
        this.plugin.scheduleDrawingSave(this.file, this.drawingData, { userOperation: true });
      }
      this.recordDrawingHistory(historyBefore);
      this.scheduleNoteFlowLayout({ operation: insertedNoteFlow });
      this.currentStroke = null;
    }
    this.finishPointerInteraction(event);
  }
  finishPointerInteraction(event) {
    try {
      if (event && this.canvas.hasPointerCapture?.(event.pointerId)) {
        this.canvas.releasePointerCapture(event.pointerId);
      }
    } catch (error) {
      void error;
    }
    this.pointerStartPoint = null;
    this.pointerStartClient = null;
    this.pointerStartEditable = null;
    this.pointerStartSourceText = false;
    this.activePointerId = null;
    this.didMove = false;
    this.resetTextHighlightGesture();
    this.render();
    event?.preventDefault();
    event?.stopPropagation();
  }
  scheduleLaserFade() {
    if (this.destroyed || this.laserFadeTimer !== null || !this.laserStrokes.length) {
      return;
    }
    this.laserFadeTimer = window.setTimeout(() => {
      this.laserFadeTimer = null;
      const now = Date.now();
      this.laserStrokes = this.laserStrokes.filter((stroke) => Number(stroke.laserExpiresAt) > now);
      this.requestRender();
      if (this.laserStrokes.length) {
        this.scheduleLaserFade();
      }
    }, 50);
  }
  onCanvasDoubleClick(event) {
    if (!this.active || event.button !== 0) {
      return;
    }
    if (this.isNoteFlowPenActive()) {
      event.preventDefault();
      event.stopPropagation();
      return;
    }
    const point = this.eventToPoint(event);
    const resizeHandle = this.findSelectionHandleAt(point);
    if (resizeHandle === "se" && this.resetSelectedElementsToBestFit()) {
      this.lastTextTap = null;
      event.preventDefault();
      event.stopPropagation();
      return;
    }
    const hitStrokeIndex = this.findStrokeAt(point, { x: event.clientX, y: event.clientY });
    if (hitStrokeIndex >= 0 && isTextLikeStroke(this.drawingData.strokes[hitStrokeIndex])) {
      this.editFloatingTextStroke(hitStrokeIndex, point);
      this.lastTextTap = null;
      event.preventDefault();
      event.stopPropagation();
      return;
    }
    const selectedTextIndex = this.getSelectedStrokeIndexes().find((index) => isTextLikeStroke(this.drawingData.strokes[index]));
    if (selectedTextIndex >= 0 && this.selectedStrokeFrameContains(point)) {
      this.editFloatingTextStroke(selectedTextIndex, point);
      this.lastTextTap = null;
      event.preventDefault();
      event.stopPropagation();
    }
  }
  onPreviewDoubleClick(event) {
    if (!this.isReadingPreviewGesture(event) || event.button !== void 0 && event.button !== 0) {
      return;
    }
    if (this.active) {
      this.onCanvasDoubleClick(event);
    }
    event.preventDefault();
    event.stopPropagation();
    event.stopImmediatePropagation?.();
  }
  onPreviewSecondPointerDown(event) {
    if (!this.isReadingPreviewGesture(event) || event.button !== void 0 && event.button !== 0 || event.isPrimary === false) {
      return;
    }
    const point = { x: Number(event.clientX) || 0, y: Number(event.clientY) || 0 };
    const directTaskItem = event.target?.closest?.("li.task-list-item");
    const directTaskBlock = directTaskItem && this.previewEl.contains(directTaskItem) ? this.markdownBlockElementForTarget(directTaskItem, point) : null;
    if (this.active && this.toolMode === TOOL_SELECT && event.target !== this.canvas && directTaskBlock) {
      this.directMarkdownPointerId = event.pointerId;
      this.directMarkdownPointerElement = directTaskBlock;
      this.onPointerDown(event, true);
      event.preventDefault();
      event.stopPropagation();
      event.stopImmediatePropagation?.();
      return;
    }
    this.rememberMarkdownIdentityMutation(event);
    const now = Number(event.timeStamp) || Date.now();
    const previous = this.previewPrimaryPress;
    this.previewPrimaryPress = { ...point, time: now };
    const repeated = previous && now >= previous.time && now - previous.time <= 550 && Math.hypot(point.x - previous.x, point.y - previous.y) <= Math.max(18, this.tapDistancePx() * 2);
    if (!repeated) {
      return;
    }
    const path = event?.composedPath?.() || [];
    if (this.active && (event.target === this.canvas || path.includes(this.canvas))) {
      event.preventDefault();
      return;
    }
    event.preventDefault();
    event.stopPropagation();
    event.stopImmediatePropagation?.();
  }
  isReadingPreviewGesture(event) {
    const target = event?.target;
    const path = event?.composedPath?.() || [];
    return this.surfaceType === "preview" && !isSourceMode(this.view) && Boolean(target && this.previewEl?.isConnected && (this.previewEl.contains?.(target) || path.includes(this.previewEl)));
  }
  onPreviewSecondPress(event) {
    if (!this.isReadingPreviewGesture(event) || event.button !== void 0 && event.button !== 0 || Number(event.detail) < 2) {
      return;
    }
    event.preventDefault();
    event.stopPropagation();
    event.stopImmediatePropagation?.();
  }
  rememberMarkdownIdentityMutation(event) {
    const directTarget = event?.target;
    const underlyingTarget = directTarget === this.canvas ? this.elementBelowCanvas(Number(event?.clientX) || 0, Number(event?.clientY) || 0) : directTarget;
    const checkbox = underlyingTarget?.closest?.("input.task-list-item-checkbox, input[type='checkbox']");
    if (!checkbox || !this.previewEl?.contains?.(checkbox)) {
      return;
    }
    const element = this.markdownBlockElementForTarget(checkbox, {
      x: Number(event.clientX) || 0,
      y: Number(event.clientY) || 0
    });
    const block = this.findMarkdownBlockRecordForElement(element);
    if (!block || !Number.isFinite(Number(block.lineStart))) {
      return;
    }
    const row = this.markdownDropRowMetrics(element);
    const records = new Map(this.markdownBlockRecords().map((item) => [item.id, item]));
    const rowIds = Array.from(/* @__PURE__ */ new Set([
      ...row?.orderedMemberIds || [],
      block.id
    ])).filter((id) => records.has(id));
    const members = rowIds.map((id, order) => {
      const memberBlock = records.get(id);
      const memberElement = id === block.id ? element : this.markdownBlockElement(memberBlock);
      if (!memberBlock || !memberElement?.isConnected) {
        return null;
      }
      const flowElement = this.markdownBlockFlowElement(memberElement) || memberElement;
      const liveInlineSpan = flowElement.classList?.contains("notedraw-md-inline-grid-item") ? Number.parseInt(flowElement.style?.getPropertyValue?.("--notedraw-md-inline-span"), 10) : Number.NaN;
      const persistedSpan = Number(memberBlock.span);
      const span = Number.isFinite(liveInlineSpan) && liveInlineSpan >= 1 && liveInlineSpan < 12 ? Number.isFinite(persistedSpan) && persistedSpan >= 1 && persistedSpan < 12 ? persistedSpan : liveInlineSpan : Number.isFinite(persistedSpan) && persistedSpan >= 1 && persistedSpan < 12 ? persistedSpan : null;
      if (!(Number(span) >= 1 && Number(span) < 12)) {
        return null;
      }
      return {
        id: memberBlock.id,
        path: memberBlock.path,
        lineStart: Number(memberBlock.lineStart),
        lineEnd: Number.isFinite(Number(memberBlock.lineEnd)) ? Number(memberBlock.lineEnd) : Number(memberBlock.lineStart),
        textHint: normalizeRenderedText2(renderedMarkdownIdentityText(memberElement)).slice(0, 240),
        flowElement,
        span,
        widthScale: normalizeMarkdownBlockWidthScale(memberBlock.widthScale),
        order
      };
    }).filter(Boolean);
    if (!members.length) {
      return;
    }
    this.pendingMarkdownIdentityRefresh = {
      triggerId: block.id,
      members,
      expiresAt: Date.now() + 3e3
    };
    for (const delay of [0, 48, 180]) {
      window.setTimeout(() => {
        if (this.destroyed || this.pendingMarkdownIdentityRefresh?.expiresAt <= Date.now()) {
          return;
        }
        this.restorePendingMarkdownIdentityPresentation();
        this.syncMarkdownBlockPresentation();
      }, delay);
    }
  }
  restorePendingMarkdownIdentityPresentation(mutations = []) {
    const pending = this.pendingMarkdownIdentityRefresh;
    if (!pending || pending.expiresAt <= Date.now() || this.surfaceType !== "preview" || !this.previewEl?.isConnected) {
      return false;
    }
    const records = new Map(this.markdownBlockRecords().map((item) => [item.id, item]));
    const members = (pending.members || []).filter((member) => {
      const memberBlock = records.get(member.id);
      return memberBlock && !memberBlock.floating && Number(member.span) >= 1 && Number(member.span) < 12;
    });
    if (!members.length) {
      return false;
    }
    const addedCandidates = [];
    const directReplacementCandidates = [];
    const seen = /* @__PURE__ */ new Set();
    const addCandidate = (element, directReplacement = false) => {
      if (!element?.isConnected || !this.previewEl.contains(element)) {
        return;
      }
      if (seen.has(element)) {
        if (directReplacement && !directReplacementCandidates.includes(element)) {
          directReplacementCandidates.push(element);
        }
        return;
      }
      seen.add(element);
      addedCandidates.push(element);
      if (directReplacement) {
        directReplacementCandidates.push(element);
      }
    };
    for (const mutation of mutations || []) {
      const replacedPendingFlow = members.some((member) => Array.from(mutation?.removedNodes || []).some((node) => node === member.flowElement || node?.contains?.(member.flowElement) || member.flowElement?.contains?.(node)));
      for (const node of mutation?.addedNodes || []) {
        if (node?.nodeType !== 1 || !this.previewEl.contains(node)) {
          continue;
        }
        addCandidate(markdownBlockCandidateElementForTarget(node, this.previewEl), replacedPendingFlow);
        for (const candidate of markdownBlockCandidateElements(node)) {
          addCandidate(candidate, replacedPendingFlow);
        }
      }
    }
    const documentOrder = new Map(markdownBlockCandidateElements(this.previewEl).map((element, order) => [element, order]));
    addedCandidates.sort((left, right) => (documentOrder.get(left) ?? Number.MAX_SAFE_INTEGER) - (documentOrder.get(right) ?? Number.MAX_SAFE_INTEGER));
    directReplacementCandidates.sort((left, right) => (documentOrder.get(left) ?? Number.MAX_SAFE_INTEGER) - (documentOrder.get(right) ?? Number.MAX_SAFE_INTEGER));
    const candidateMeta = addedCandidates.map((element, order) => ({
      element,
      id: element.dataset?.noteDrawMarkdownBlockId || "",
      path: normalizeVaultPath(element.dataset?.noteDrawSourcePath || this.file?.path || ""),
      info: getSourceInfo(element),
      hint: normalizeRenderedText2(renderedMarkdownIdentityText(element)).slice(0, 240),
      order
    }));
    const used = /* @__PURE__ */ new Set();
    const matched = /* @__PURE__ */ new Map();
    const takeUnique = (values) => values.length === 1 && !used.has(values[0].element) ? values[0].element : null;
    for (const member of members) {
      const matchingRange = candidateMeta.filter((meta) => !used.has(meta.element) && meta.path === member.path && Number(meta.info?.lineStart) === member.lineStart && Number(meta.info?.lineEnd ?? meta.info?.lineStart) === member.lineEnd);
      const matchingHint = member.textHint ? candidateMeta.filter((meta) => !used.has(meta.element) && meta.path === member.path && meta.hint === member.textHint) : [];
      const freshElement = candidateMeta.find((meta) => !used.has(meta.element) && meta.id === member.id)?.element || takeUnique(matchingRange) || takeUnique(matchingHint);
      if (freshElement) {
        used.add(freshElement);
        matched.set(member.id, freshElement);
      }
    }
    const unmatchedMembers = members.filter((member) => !matched.has(member.id)).sort((left, right) => left.order - right.order);
    const unmatchedDirectCandidates = directReplacementCandidates.filter((element) => !used.has(element));
    if (unmatchedMembers.length === unmatchedDirectCandidates.length) {
      unmatchedMembers.forEach((member, index) => matched.set(member.id, unmatchedDirectCandidates[index]));
    }
    let restored = 0;
    for (const member of members) {
      const block = records.get(member.id);
      block.span = Number(member.span);
      block.widthScale = normalizeMarkdownBlockWidthScale(member.widthScale);
      block.noteFlowAutoSpan = false;
      const freshElement = matched.get(member.id);
      if (!freshElement) {
        continue;
      }
      const freshInfo = getSourceInfo(freshElement);
      const freshHint = normalizeRenderedText2(renderedMarkdownIdentityText(freshElement)).slice(0, 240);
      if (Number.isFinite(freshInfo.lineStart) && !taskSourceRangeLooksRelative(block, freshElement, freshInfo)) {
        block.lineStart = freshInfo.lineStart;
        block.lineEnd = freshInfo.lineEnd ?? freshInfo.lineStart;
      }
      if (freshHint) {
        block.textHint = freshHint;
      }
      freshElement.dataset.noteDrawMarkdownBlockId = block.id;
      freshElement.addClass("notedraw-md-block");
      freshElement.toggleClass("is-selected", this.selectedMarkdownBlockIds.has(block.id));
      freshElement.toggleClass("is-locked", Boolean(block.locked));
      freshElement.toggleClass("is-floating", false);
      this.applyMarkdownBlockFlowPresentation(block, freshElement);
      this.applyMarkdownBlockWidthPresentation(block, freshElement);
      setNoteDrawCssProps(freshElement, {
        "--notedraw-md-border": block.borderColor || "transparent",
        "--notedraw-md-background": block.backgroundColor || "transparent",
        "--notedraw-md-content-color": block.contentColor || "inherit",
        "--notedraw-md-content-opacity": String(block.contentOpacity ?? 1),
        "--notedraw-md-content-scale": String(block.contentScale ?? 1)
      });
      this.applyMarkdownBlockHeightPresentation(block, freshElement);
      this.markdownBlockElements.set(block.id, freshElement);
      restored += 1;
    }
    if (restored > 0) {
      if (members.some((member) => this.selectedMarkdownBlockIds.has(member.id))) {
        this.invalidateSelectionFrameSnapshot();
      }
      return true;
    }
    this.syncMarkdownBlockPresentation();
    return true;
  }
  rememberTextTap(index, point, event) {
    this.lastTextTap = {
      index,
      point,
      time: Number(event?.timeStamp) || Date.now()
    };
  }
  isRepeatTextTap(index, point, event) {
    if (!this.lastTextTap || this.lastTextTap.index !== index) {
      return false;
    }
    const now = Number(event?.timeStamp) || Date.now();
    const elapsed = now - this.lastTextTap.time;
    if (elapsed < 0 || elapsed > 500) {
      return false;
    }
    const distance = pointDistanceOnCanvas(
      this.lastTextTap.point,
      point,
      this.canvasWidth(),
      this.canvasHeight()
    );
    return distance <= this.tapDistancePx() * 2;
  }
  floatingTextPointToClient(point) {
    const canvasPoint2 = this.pointToCanvas(point);
    const canvasRect = this.canvas.getBoundingClientRect();
    const scaleX = canvasRect.width > 0 ? canvasRect.width / this.canvasWidth() : 1;
    const scaleY = canvasRect.height > 0 ? canvasRect.height / Math.max(1, this.canvasRenderHeight) : 1;
    return {
      x: canvasRect.left + canvasPoint2.x * scaleX,
      y: canvasRect.top + (canvasPoint2.y - this.canvasWindowTop) * scaleY
    };
  }
  floatingTextEditorInsets(element) {
    const view = element?.ownerDocument?.defaultView || window;
    const style = view.getComputedStyle(element);
    const number = (property) => Number.parseFloat(style.getPropertyValue(property)) || 0;
    return {
      left: number("border-left-width") + number("padding-left"),
      top: number("border-top-width") + number("padding-top"),
      horizontal: number("border-left-width") + number("padding-left") + number("padding-right") + number("border-right-width"),
      vertical: number("border-top-width") + number("padding-top") + number("padding-bottom") + number("border-bottom-width")
    };
  }
  positionFloatingTextInput(state = this.floatingTextInput) {
    if (!state?.element?.isConnected || !state.point || !this.canvas?.isConnected) {
      return;
    }
    const textarea = state.element;
    if (state.inPlace && state.clientRect) {
      const rect2 = state.clientRect;
      applyElementStyles(textarea, {
        left: `${Math.round(rect2.left)}px`,
        top: `${Math.round(rect2.top)}px`,
        width: `${Math.max(40, Math.round(rect2.width))}px`,
        height: `${Math.max(28, Math.round(rect2.height))}px`
      });
      state.centered = false;
      return;
    }
    const view = textarea.ownerDocument?.defaultView || window.activeWindow || window;
    const anchor = this.floatingTextPointToClient(state.point);
    const rect = textarea.getBoundingClientRect();
    const canvasRect = this.canvas.getBoundingClientRect();
    const viewport = view.visualViewport;
    const viewportLeft = viewport?.offsetLeft || 0;
    const viewportTop = viewport?.offsetTop || 0;
    const viewportWidth = viewport?.width || view.innerWidth || 1;
    const viewportHeight = viewport?.height || view.innerHeight || 1;
    const viewportRight = viewportLeft + viewportWidth;
    const viewportBottom = viewportTop + viewportHeight;
    const anchorVisible = anchor.x >= Math.max(viewportLeft, canvasRect.left) && anchor.x <= Math.min(viewportRight, canvasRect.right) && anchor.y >= Math.max(viewportTop, canvasRect.top) && anchor.y <= Math.min(viewportBottom, canvasRect.bottom);
    const insets = this.floatingTextEditorInsets(textarea);
    const placement = placeFloatingTextEditor({
      anchorX: anchor.x,
      anchorY: anchor.y,
      width: rect.width,
      height: rect.height,
      viewportWidth,
      viewportHeight,
      viewportOffsetLeft: viewportLeft,
      viewportOffsetTop: viewportTop,
      contentInsetX: insets.left,
      contentInsetY: insets.top,
      margin: 8,
      anchorVisible
    });
    applyElementStyles(textarea, {
      left: `${Math.round(placement.left)}px`,
      top: `${Math.round(placement.top)}px`,
      width: `${Math.round(placement.width)}px`,
      height: `${Math.round(placement.height)}px`
    });
    state.centered = placement.centered;
    if (state.index < 0) {
      if (placement.centered) {
        const placedRect = textarea.getBoundingClientRect();
        state.commitPoint = this.eventToPoint({
          clientX: placedRect.left + insets.left,
          clientY: placedRect.top + insets.top
        });
      } else {
        state.commitPoint = { ...state.point };
      }
    }
  }
  floatingTextContentWidth(element) {
    const rect = element.getBoundingClientRect();
    const insets = this.floatingTextEditorInsets(element);
    return clamp10(rect.width - insets.horizontal, 24, 900);
  }
  openFloatingTextInput(point, index = -1) {
    this.endFloatingTextInput(true);
    this.endTextEdit();
    const existing = index >= 0 ? this.drawingData.strokes[index] : null;
    const brushColor = this.currentBrushSettings().color || this.penColor;
    const preset = isTextLikeStroke(existing) ? existing : createTextPreset(this.textPreset, " ", brushColor);
    const editorDocument = this.canvas?.ownerDocument || this.previewEl?.ownerDocument || activeDocument;
    const editorWindow = editorDocument.defaultView || window.activeWindow || window;
    const inPlace = Boolean(existing);
    const textarea = editorDocument.body.createEl(inPlace ? "div" : "textarea", {
      cls: `notedraw-floating-text-input${inPlace ? " is-in-place" : ""}`,
      attr: inPlace ? {
        contenteditable: "plaintext-only",
        spellcheck: "true",
        role: "textbox"
      } : {
        rows: "1",
        spellcheck: "true"
      }
    });
    if (inPlace) {
      textarea.textContent = existing.text;
    } else {
      textarea.value = "";
    }
    applyElementStyles(textarea, {
      color: isTextLikeStroke(existing) ? existing.color : brushColor,
      fontSize: `${isTextLikeStroke(existing) ? clamp10(Number(existing.fontSize || 18), 10, 72) : 18}px`,
      fontWeight: isTextLikeStroke(existing) && existing.bold ? "700" : "400",
      fontFamily: isTextLikeStroke(existing) && existing.code ? "monospace" : "sans-serif"
    });
    const preferredContentWidth = isRichTextStroke(existing) ? existing.previewWidth : Number(existing?.textWidth) > 0 ? Number(existing.textWidth) : isRichTextStroke(preset) && Number(preset.previewWidth) > 0 ? Number(preset.previewWidth) : null;
    const canvasRect = this.canvas?.getBoundingClientRect?.();
    const strokeBounds = inPlace ? getStrokeBounds(existing, this.canvasWidth(), this.canvasHeight()) : null;
    const clientRect = strokeBounds && canvasRect?.width > 0 && canvasRect?.height > 0 ? {
      left: canvasRect.left + strokeBounds.minX * canvasRect.width / Math.max(1, this.canvasWidth()),
      top: canvasRect.top + (strokeBounds.minY - this.canvasWindowTop) * canvasRect.height / Math.max(1, this.canvasRenderHeight),
      width: Math.max(40, (strokeBounds.maxX - strokeBounds.minX) * canvasRect.width / Math.max(1, this.canvasWidth())),
      height: Math.max(28, (strokeBounds.maxY - strokeBounds.minY) * canvasRect.height / Math.max(1, this.canvasRenderHeight))
    } : null;
    const state = {
      element: textarea,
      point: { ...point, t: Date.now() },
      commitPoint: null,
      preferredContentWidth,
      index,
      committed: false,
      cancelled: false,
      composing: false,
      commitAfterComposition: false,
      centered: false,
      inPlace,
      clientRect
    };
    this.floatingTextInput = state;
    const resize = () => {
      if (!textarea.isConnected) {
        return;
      }
      const viewport = editorWindow.visualViewport;
      const maxWidth = Math.max(96, Math.min(520, (viewport?.width || editorWindow.innerWidth || 320) - 16));
      const maxHeight = Math.max(64, (viewport?.height || editorWindow.innerHeight || 480) - 16);
      const insets = this.floatingTextEditorInsets(textarea);
      if (state.inPlace) {
        this.positionFloatingTextInput(state);
        return;
      }
      applyElementStyles(textarea, { height: "auto", width: "auto", overflowY: "hidden" });
      const naturalWidth = textarea.scrollWidth + 2;
      const preferredWidth = Number(state.preferredContentWidth) > 0 ? Number(state.preferredContentWidth) + insets.horizontal : 0;
      const width = Math.min(maxWidth, Math.max(120, preferredWidth, naturalWidth));
      applyElementStyles(textarea, { width: `${Math.round(width)}px`, height: "auto" });
      const wantedHeight = Math.max(32, textarea.scrollHeight + 2);
      applyElementStyles(textarea, {
        height: `${Math.round(Math.min(maxHeight, wantedHeight))}px`,
        overflowY: wantedHeight > maxHeight ? "auto" : "hidden"
      });
      this.positionFloatingTextInput(state);
    };
    const commit = () => this.commitFloatingTextInput(state);
    const cancel = () => this.endFloatingTextInput(false, state);
    textarea.addEventListener("input", resize);
    textarea.addEventListener("compositionstart", () => {
      state.composing = true;
    });
    textarea.addEventListener("compositionend", () => {
      state.composing = false;
      resize();
      if (state.commitAfterComposition) {
        state.commitAfterComposition = false;
        editorWindow.setTimeout(commit, 0);
      }
    });
    textarea.addEventListener("blur", () => editorWindow.setTimeout(commit, 0));
    textarea.addEventListener("keydown", (event) => {
      if (event.key === "Escape") {
        event.preventDefault();
        cancel();
      } else if ((event.ctrlKey || event.metaKey) && event.key === "Enter") {
        event.preventDefault();
        commit();
      }
    });
    state.reposition = resize;
    editorWindow.addEventListener("resize", resize);
    editorWindow.visualViewport?.addEventListener("resize", resize);
    editorWindow.visualViewport?.addEventListener("scroll", resize);
    editorWindow.requestAnimationFrame(() => {
      resize();
      textarea.focus();
      if (typeof textarea.select === "function") {
        textarea.select();
      } else {
        const selection = editorWindow.getSelection?.();
        const range = editorDocument.createRange?.();
        if (selection && range) {
          range.selectNodeContents(textarea);
          range.collapse(false);
          selection.removeAllRanges();
          selection.addRange(range);
        }
      }
    });
  }
  commitFloatingTextInput(state = this.floatingTextInput) {
    if (!state || state.committed || state.cancelled) {
      return;
    }
    if (state.composing) {
      state.commitAfterComposition = true;
      return;
    }
    state.committed = true;
    const text = String(state.inPlace ? state.element.innerText : state.element.value).trim();
    if (!text) {
      this.endFloatingTextInput(false, state);
      return;
    }
    const historyBefore = this.captureDrawingHistorySnapshot();
    let mindMapStrokeToSync = null;
    if (state.index >= 0 && isTextLikeStroke(this.drawingData.strokes[state.index])) {
      const stroke = this.drawingData.strokes[state.index];
      const textChanged = stroke.text !== text;
      stroke.text = text;
      stroke.render = normalizeTextRenderMode(stroke.render);
      stroke.fontSize = clamp10(Number(stroke.fontSize || 18), 10, 72);
      if (isRichTextStroke(stroke)) {
        stroke.previewWidth = stroke.previewWidth || 300;
        stroke.previewHeight = stroke.previewHeight || 180;
      } else {
        stroke.textWidth = this.floatingTextContentWidth(state.element);
      }
      this.setSelectedStrokes(state.index);
      if (textChanged && normalizeMindMapNode(stroke.mindMapNode)?.affectsSource) {
        mindMapStrokeToSync = stroke;
      }
    } else {
      const brush = this.currentBrushSettings();
      const preset = createTextPreset(this.textPreset, text, brush.color || this.penColor);
      const stroke = {
        kind: preset.kind || TOOL_TEXT,
        brush: BRUSH_PEN,
        color: preset.color,
        width: 3,
        opacity: 1,
        count: 1,
        text: preset.text,
        render: preset.render || TEXT_RENDER_PLAIN,
        fontSize: clamp10(Number(preset.fontSize || 18), 10, 72),
        bold: preset.bold,
        code: preset.code,
        boxed: preset.boxed,
        file: preset.file,
        previewWidth: preset.previewWidth,
        previewHeight: preset.previewHeight,
        uiRole: preset.uiRole,
        buttonStyle: preset.buttonStyle,
        snap: preset.snap,
        locked: false,
        textWidth: normalizeTextRenderMode(preset.render) === TEXT_RENDER_PLAIN ? this.floatingTextContentWidth(state.element) : null,
        points: [{ ...state.commitPoint || state.point, t: Date.now() }]
      };
      this.drawingData.strokes.push(stroke);
      const insertedIndex = this.drawingData.strokes.length - 1;
      this.setSelectedStrokes(insertedIndex);
      if (stroke.uiRole === "button" && this.pendingButtonCommandPicker) {
        this.pendingButtonCommandPicker = false;
        window.setTimeout(() => {
          if (!this.destroyed && this.drawingData?.strokes?.[insertedIndex]?.uiRole === "button") {
            this.plugin.openButtonCommandPicker(this, insertedIndex);
          }
        }, 0);
      }
    }
    this.captureResponsiveAnchorsForIndexes(this.getSelectedStrokeIndexes());
    this.redoStack = [];
    this.invalidateStaticCache();
    this.plugin.scheduleDrawingSave(this.file, this.drawingData, { userOperation: true });
    if (mindMapStrokeToSync) {
      this.textCommitBarrier.track(this.syncMindMapNodeToSource(mindMapStrokeToSync, text, { drawingBefore: historyBefore }));
    } else {
      this.recordDrawingHistory(historyBefore);
    }
    this.scheduleLayoutRefresh({ settle: false });
    this.endFloatingTextInput(false, state);
    this.render();
    this.requestRender(true);
  }
  endFloatingTextInput(commit = true, state = this.floatingTextInput) {
    if (!state) {
      return;
    }
    if (commit && !state.committed) {
      if (state.composing) {
        state.composing = false;
        state.commitAfterComposition = false;
      }
      this.commitFloatingTextInput(state);
      return;
    }
    if (!commit && !state.committed) {
      state.cancelled = true;
    }
    const view = state.element?.ownerDocument?.defaultView;
    if (view && state.reposition) {
      view.removeEventListener("resize", state.reposition);
      view.visualViewport?.removeEventListener("resize", state.reposition);
      view.visualViewport?.removeEventListener("scroll", state.reposition);
    }
    state.element?.remove();
    if (this.floatingTextInput === state) {
      this.floatingTextInput = null;
    }
  }
  editFloatingTextStroke(index) {
    const stroke = this.drawingData.strokes[index];
    if (!isTextLikeStroke(stroke) || stroke.locked) {
      return;
    }
    this.setSelectedStrokes(index);
    this.openFloatingTextInput(stroke.points[0], index);
  }
  handleTextToolTap(point) {
    const snappedPoint = this.snapPointForPreset(point, this.textPreset);
    if (this.textPreset === "mindMap") {
      if (!this.pendingMindMapFile) {
        this.openMindMapFilePicker();
        return;
      }
      const sourceFile = this.pendingMindMapFile;
      const options = this.pendingMindMapOptions || { affectsSource: this.plugin.noteDrawSettings?.mindMapAffectsSource !== false };
      this.pendingMindMapFile = null;
      this.pendingMindMapOptions = null;
      void this.insertMindMapAt(snappedPoint, sourceFile, options);
      return;
    }
    if (isAssetTextPreset(this.textPreset)) {
      this.pendingEmbedTool = { preset: this.textPreset, point: snappedPoint };
      if (this.hiddenFileInput) {
        this.hiddenFileInput.accept = filePickerAcceptForPreset(this.textPreset);
        this.hiddenFileInput.click();
      }
      return;
    }
    const instantText = instantTextForPreset(this.textPreset);
    if (instantText) {
      this.insertTextPresetAt(snappedPoint, this.textPreset, instantText);
      return;
    }
    this.openFloatingTextInput(snappedPoint);
  }
  async insertMindMapAt(point, sourceFile, options = {}) {
    const file2 = this.plugin.resolveApiFile(sourceFile, this.file?.path || "");
    if (!file2) {
      return { ok: false, reason: "source-not-found" };
    }
    const source = await this.plugin.app.vault.cachedRead(file2);
    const model = parseMarkdownMindMap(source, { title: file2.basename || file2.name || "Mind map", maxNodes: 240 });
    if (model.nodes.length <= 1 && !String(source || "").trim()) {
      if (!options.quiet) {
        new import_obsidian.Notice(this.plugin.t("emptyMindMap"));
      }
      return { ok: false, reason: "empty-source" };
    }
    const historyBefore = this.captureDrawingHistorySnapshot();
    const width = this.canvasWidth();
    const height = this.canvasHeight();
    const frame = this.getResponsiveContentFrame();
    const originX = clamp10(Number(point?.x) * width, Math.max(8, frame.left + 8), Math.max(8, width - 260));
    const originY = clamp10(Number(point?.y) * height, 12, Math.max(12, height - 80));
    const geometry = layoutMindMap(model, {
      originX,
      originY,
      canvasWidth: width,
      canvasHeight: height,
      nodeWidth: 176,
      columnGap: 72,
      rowGap: 18
    });
    const mapId = `map-${Date.now().toString(36)}-${Math.random().toString(36).slice(2, 8)}`;
    const affectsSource = options.affectsSource !== false;
    const idMap = /* @__PURE__ */ new Map();
    const depthColors = ["#2563eb", "#0f766e", "#b45309", "#be123c", "#4338ca", "#047857"];
    const nodeStrokes = geometry.nodes.map((node, index) => {
      const elementId = createElementLayoutId(this.drawingData.strokes.length + index);
      idMap.set(node.id, elementId);
      const color = depthColors[Math.min(node.depth, depthColors.length - 1) % depthColors.length];
      return normalizeStroke({
        elementId,
        kind: TOOL_TEXT,
        brush: BRUSH_PEN,
        color,
        width: node.type === "root" ? 2.4 : 1.8,
        opacity: 1,
        count: 1,
        text: node.markdown || node.text,
        render: TEXT_RENDER_MARKDOWN,
        fontSize: node.fontSize,
        bold: node.type === "root" || node.type === "heading",
        code: node.type === "code",
        boxed: true,
        file: false,
        previewWidth: Math.max(104, node.width),
        previewHeight: Math.max(40, node.height),
        buttonStyle: "outline",
        snap: true,
        locked: false,
        textWidth: null,
        mindMapNode: {
          mapId,
          nodeId: node.id,
          parentId: node.parentId || "",
          depth: node.depth,
          role: node.type === "root" ? "root" : node.type === "heading" ? "heading" : "node",
          type: node.type,
          sourcePath: file2.path,
          sourceLine: node.sourceLine,
          sourceEndLine: node.sourceEndLine,
          sourceText: node.sourceText,
          affectsSource
        },
        points: [{
          x: clamp10(node.x / width, 0, 1),
          y: clamp10(node.y / height, 0, 1),
          t: Date.now(),
          anchor: null
        }]
      });
    });
    const connectorStrokes = geometry.edges.map((edge, index) => normalizeStroke({
      elementId: createElementLayoutId(this.drawingData.strokes.length + nodeStrokes.length + index),
      brush: BRUSH_PEN,
      color: depthColors[Math.min(edge.depth, depthColors.length - 1) % depthColors.length],
      width: edge.depth <= 1 ? 2.6 : 2,
      opacity: 0.88,
      count: 1,
      connector: {
        fromId: idMap.get(edge.fromId),
        toId: idMap.get(edge.toId),
        style: "mindmap",
        arrow: true
      },
      points: [
        { x: clamp10(originX / width, 0, 1), y: clamp10(originY / height, 0, 1), t: Date.now(), anchor: null },
        { x: clamp10((originX + 1) / width, 0, 1), y: clamp10((originY + 1) / height, 0, 1), t: Date.now(), anchor: null }
      ]
    }));
    const startIndex = this.drawingData.strokes.length;
    this.drawingData.strokes.push(...connectorStrokes, ...nodeStrokes);
    const connectorIndexes = connectorStrokes.map((_, index) => startIndex + index);
    const nodeIndexes = nodeStrokes.map((_, index) => startIndex + connectorStrokes.length + index);
    this.captureResponsiveAnchorsForIndexes(nodeIndexes);
    this.syncBoundConnectors();
    this.setSelectedStrokes([...connectorIndexes, ...nodeIndexes]);
    this.syncTextPanelButtons();
    this.redoStack = [];
    this.invalidateStaticCache();
    this.plugin.scheduleDrawingSave(this.file, this.drawingData, { userOperation: true });
    this.recordDrawingHistory(historyBefore);
    this.scheduleLayoutRefresh({ settle: false });
    this.render();
    if (model.truncated && !options.quiet) {
      new import_obsidian.Notice(this.plugin.t("mindMapTruncated"));
    }
    return { ok: true, nodes: nodeStrokes.length, connectors: connectorStrokes.length, truncated: model.truncated, affectsSource };
  }
  async syncMindMapNodeToSource(stroke, text, options = {}) {
    const fallbackToDrawingHistory = () => {
      if (options.drawingBefore) {
        this.recordDrawingHistory(options.drawingBefore);
      }
      return false;
    };
    const node = normalizeMindMapNode(stroke?.mindMapNode);
    if (!node?.affectsSource || node.role === "root" || !node.sourcePath) {
      return fallbackToDrawingHistory();
    }
    const file2 = this.plugin.resolveApiFile(node.sourcePath, this.file?.path || "");
    if (!file2) {
      return fallbackToDrawingHistory();
    }
    try {
      const source = await this.plugin.app.vault.cachedRead(file2);
      const currentModel = parseMarkdownMindMap(source, { title: file2.basename || file2.name || "Mind map", maxNodes: 240 });
      const currentSourceNode = currentModel.nodes.find((candidate) => candidate.id === node.nodeId);
      const result = replaceMarkdownMindMapNodeText(source, { ...node, ...currentSourceNode || {} }, text);
      if (!result.changed) {
        if (result.reason === "source-range-not-found") {
          new import_obsidian.Notice(this.plugin.t("sourceNoteUpdateFailed"));
        }
        return fallbackToDrawingHistory();
      }
      await this.plugin.app.vault.modify(file2, result.source);
      this.refreshMindMapSourceMetadata(node.mapId, node.sourcePath, result.source);
      this.plugin.scheduleDrawingSave(this.file, this.drawingData, { userOperation: true });
      if (options.drawingBefore) {
        this.recordCompoundHistory(file2, source, result.source, options.drawingBefore);
      }
      return true;
    } catch (error) {
      console.error(`[${PLUGIN_ID}] Failed to update mind map source note`, error);
      new import_obsidian.Notice(this.plugin.t("sourceNoteUpdateFailed"));
      return fallbackToDrawingHistory();
    }
  }
  refreshMindMapSourceMetadata(mapId, sourcePath, source) {
    const file2 = this.plugin.resolveApiFile(sourcePath, this.file?.path || "");
    const model = parseMarkdownMindMap(source, { title: file2?.basename || file2?.name || "Mind map", maxNodes: 240 });
    const byId = new Map(model.nodes.map((node) => [node.id, node]));
    for (const stroke of this.drawingData.strokes) {
      const linked = normalizeMindMapNode(stroke.mindMapNode);
      const sourceNode = linked?.mapId === mapId ? byId.get(linked.nodeId) : null;
      if (!sourceNode) {
        continue;
      }
      stroke.mindMapNode = {
        ...linked,
        sourceLine: sourceNode.sourceLine,
        sourceEndLine: sourceNode.sourceEndLine,
        sourceText: sourceNode.sourceText,
        type: sourceNode.type
      };
    }
  }
  insertTextPresetAt(point, presetId, text) {
    const historyBefore = this.captureDrawingHistorySnapshot();
    const brush = this.currentBrushSettings();
    const preset = createTextPreset(presetId, text, brush.color || this.penColor);
    const stroke = {
      kind: preset.kind || TOOL_TEXT,
      brush: BRUSH_PEN,
      color: preset.color,
      width: 3,
      opacity: 1,
      count: 1,
      text: preset.text,
      render: preset.render || TEXT_RENDER_PLAIN,
      fontSize: clamp10(Number(preset.fontSize || 18), 10, 72),
      bold: preset.bold,
      code: preset.code,
      boxed: preset.boxed,
      file: preset.file,
      previewWidth: preset.previewWidth,
      previewHeight: preset.previewHeight,
      uiRole: preset.uiRole,
      buttonStyle: preset.buttonStyle,
      snap: preset.snap,
      locked: false,
      points: [{ ...point, t: Date.now() }]
    };
    this.drawingData.strokes.push(stroke);
    this.setSelectedStrokes(this.drawingData.strokes.length - 1);
    this.captureResponsiveAnchorsForIndexes([this.drawingData.strokes.length - 1]);
    this.redoStack = [];
    this.invalidateStaticCache();
    this.plugin.scheduleDrawingSave(this.file, this.drawingData, { userOperation: true });
    this.recordDrawingHistory(historyBefore);
    this.scheduleLayoutRefresh({ settle: false });
    this.render();
  }
  async insertImportedAsset(fileLike, point) {
    const asset = await this.plugin.importLocalAsset(fileLike);
    if (!asset) {
      return;
    }
    const historyBefore = this.captureDrawingHistorySnapshot();
    const previewRenderMode = classifyImportedPreviewRender(asset);
    if (previewRenderMode) {
      const isHtmlPreview = previewRenderMode === TEXT_RENDER_HTML;
      const stroke2 = {
        kind: TOOL_TEXT,
        brush: BRUSH_PEN,
        color: "#1f2937",
        width: 3,
        opacity: 1,
        count: 1,
        text: asset.text || asset.name,
        render: previewRenderMode,
        fontSize: 16,
        bold: false,
        code: isHtmlPreview,
        boxed: true,
        file: true,
        assetPath: asset.path,
        assetName: asset.name,
        assetMime: asset.mime,
        assetSize: asset.size,
        previewWidth: 320,
        previewHeight: isHtmlPreview ? 200 : 220,
        locked: false,
        points: [{ ...point, t: Date.now() }]
      };
      this.pendingEmbedTool = null;
      this.drawingData.strokes.push(stroke2);
      this.setSelectedStrokes(this.drawingData.strokes.length - 1);
      this.captureResponsiveAnchorsForIndexes([this.drawingData.strokes.length - 1]);
      this.redoStack = [];
      this.invalidateStaticCache();
      this.plugin.scheduleDrawingSave(this.file, this.drawingData, { userOperation: true });
      this.recordDrawingHistory(historyBefore);
      this.render();
      return;
    }
    const kind = classifyImportedAsset(asset);
    const stroke = {
      kind: TOOL_EMBED,
      embedType: kind,
      brush: BRUSH_PEN,
      color: this.currentBrushSettings().color || this.penColor,
      width: 3,
      opacity: 1,
      count: 1,
      text: asset.name,
      assetPath: asset.path,
      assetName: asset.name,
      assetMime: asset.mime,
      assetSize: asset.size,
      exportImageDataUrl: kind === EMBED_IMAGE ? asset.imageDataUrl || "" : "",
      previewWidth: kind === EMBED_FILE ? 260 : 320,
      previewHeight: kind === EMBED_FILE ? 74 : 200,
      locked: false,
      points: [{ ...point, t: Date.now() }]
    };
    this.pendingEmbedTool = null;
    this.drawingData.strokes.push(stroke);
    this.setSelectedStrokes(this.drawingData.strokes.length - 1);
    this.captureResponsiveAnchorsForIndexes([this.drawingData.strokes.length - 1]);
    this.redoStack = [];
    this.invalidateStaticCache();
    this.plugin.scheduleDrawingSave(this.file, this.drawingData, { userOperation: true });
    this.recordDrawingHistory(historyBefore);
    this.render();
  }
  onWheel(event) {
    if (!this.active) {
      return;
    }
    if ((event.ctrlKey || event.metaKey) && this.canZoomReadingSurface()) {
      const factor = Math.exp(-clamp10(Number(event.deltaY) || 0, -80, 80) * 25e-4);
      this.setReadingZoom(this.readingZoom * factor, { x: event.clientX, y: event.clientY });
      event.preventDefault();
      event.stopPropagation();
      return;
    }
    const row = this.parallelScrollRowAt(event.clientX, event.clientY);
    const horizontalDelta = Math.abs(Number(event.deltaX) || 0) > Math.abs(Number(event.deltaY) || 0) ? Number(event.deltaX) || 0 : event.shiftKey ? Number(event.deltaY) || 0 : 0;
    if (row && horizontalDelta && row.scrollWidth > row.clientWidth + 1) {
      row.scrollLeft += horizontalDelta;
      event.preventDefault();
      event.stopPropagation();
      return;
    }
    this.scheduleFloatingControlsPosition();
  }
  parallelScrollRowAt(clientX, clientY, element = null) {
    const target = element || this.elementBelowCanvas(clientX, clientY);
    const flowElement = this.markdownBlockFlowElement(target) || target;
    const row = flowElement?.closest?.(".notedraw-md-grid-row");
    return row && this.previewEl?.contains?.(row) ? row : null;
  }
  onReadingTouchPointerDown(event) {
    if (this.active || event.pointerType !== "touch" || event.isPrimary === false) {
      return;
    }
    beginReadingTouch(this.readingTouchGuard, {
      id: event.pointerId,
      x: event.clientX,
      y: event.clientY,
      now: Date.now()
    });
  }
  onReadingTouchPointerMove(event) {
    if (this.active || event.pointerType !== "touch") {
      return;
    }
    moveReadingTouch(this.readingTouchGuard, {
      id: event.pointerId,
      x: event.clientX,
      y: event.clientY,
      now: Date.now()
    });
  }
  onReadingTouchPointerFinish(event) {
    if (this.active || event.pointerType !== "touch") {
      return;
    }
    const moved = finishReadingTouch(this.readingTouchGuard, {
      id: event.pointerId,
      now: Date.now()
    });
    if (moved && event.type !== "pointercancel") {
      event.preventDefault();
      event.stopPropagation();
      event.stopImmediatePropagation?.();
    }
  }
  onReadingClick(event) {
    if (this.active) {
      if (this.toolMode === TOOL_SELECT && Date.now() <= this.directMarkdownTaskClickUntil && event.target?.closest?.("li.task-list-item")) {
        event.preventDefault();
        event.stopPropagation();
        event.stopImmediatePropagation?.();
      }
      return;
    }
    if (!shouldSuppressReadingClick(this.readingTouchGuard, Date.now())) {
      return;
    }
    event.preventDefault();
    event.stopPropagation();
    event.stopImmediatePropagation?.();
  }
  canZoomReadingSurface() {
    return ["preview", "source"].includes(this.surfaceType) && !this.embeddedSurface;
  }
  usesVisualReadingZoom() {
    return this.surfaceType === "preview" && !this.embeddedSurface;
  }
  readingZoomScale() {
    if (!this.canZoomReadingSurface()) {
      return 1;
    }
    const scale = clamp10(Number(this.readingZoom) || 1, MIN_READING_ZOOM, MAX_READING_ZOOM);
    return Math.abs(scale - 1) < READING_ZOOM_NORMALIZATION_EPSILON ? 1 : scale;
  }
  isReadingZoomInteractionActive() {
    return Boolean(this.multiTouchScrolling) || Date.now() < this.readingZoomInteractionUntil;
  }
  scheduleReadingZoomSettle(delay = 180) {
    if (!this.usesVisualReadingZoom() || this.destroyed) {
      return;
    }
    const wait = Math.max(40, Number(delay) || 180);
    this.readingZoomInteractionUntil = Math.max(this.readingZoomInteractionUntil, Date.now() + wait);
    if (this.readingZoomSettleTimer !== null) {
      window.clearTimeout(this.readingZoomSettleTimer);
    }
    this.readingZoomSettleTimer = window.setTimeout(() => {
      this.readingZoomSettleTimer = null;
      const remaining = this.readingZoomInteractionUntil - Date.now();
      if (remaining > 0 || this.multiTouchScrolling) {
        this.scheduleReadingZoomSettle(Math.max(40, remaining));
        return;
      }
      this.readingZoomInteractionUntil = 0;
      if (Math.abs(this.readingZoomScale() - 1) < 1e-3) {
        this.restoreReadingVirtualSections();
        this.readingZoomBaseTarget = null;
        this.readingZoomBaseOrigin = null;
      } else {
        this.scheduleReadingVirtualSectionSync();
      }
      this.scheduleResize({ layout: false, measure: false });
      this.requestRender(true);
    }, wait);
  }
  cancelReadingZoomSettle() {
    if (this.readingZoomSettleTimer !== null) {
      window.clearTimeout(this.readingZoomSettleTimer);
      this.readingZoomSettleTimer = null;
    }
    this.readingZoomInteractionUntil = 0;
  }
  readingZoomElements(target = this.readingZoomTarget) {
    const elements = this.usesVisualReadingZoom() ? [this.ensureReadingZoomStage(target)] : [target];
    return Array.from(new Set(elements.filter(Boolean))).filter((element) => !elements.some((parent) => parent && parent !== element && parent.contains?.(element)));
  }
  applyVisualReadingZoomElement(element, zoom) {
    if (!element?.isConnected) {
      return;
    }
    this.rememberReadingZoomStyles(element);
    const state = this.readingZoomStyleState.get(element);
    const originalTransform = state?.transform?.value;
    const origin = this.readingZoomOrigin(element);
    element.style.setProperty("transform", `scale(${zoom})${originalTransform && originalTransform !== "none" ? ` ${originalTransform}` : ""}`);
    element.style.setProperty("transform-origin", `${-origin.x}px ${-origin.y}px`);
  }
  applyInheritedReadingLayoutZoom(target) {
    if (this.surfaceType !== "preview" || !target?.isConnected) {
      return;
    }
    const zoom = clamp10(Number(this.inheritedReadingLayoutZoom) || 1, MIN_READING_ZOOM, MAX_READING_ZOOM);
    this.rememberReadingZoomStyles(target);
    if (Math.abs(zoom - 1) < 1e-3) {
      target.style.removeProperty("zoom");
      return;
    }
    target.style.setProperty("zoom", String(zoom));
  }
  readingPreviewRenderer() {
    const renderer = this.view?.previewMode?.renderer;
    return renderer?.previewEl === this.previewEl && Array.isArray(renderer?.sections) ? renderer : null;
  }
  readingHasPendingAsyncFileEmbeds(renderer = this.readingPreviewRenderer()) {
    const filePath = normalizeVaultPath(this.file?.path || "");
    const roots = [this.previewEl, ...(renderer?.sections || []).map((section) => section?.el)].filter(Boolean);
    const found = roots.some((root) => {
      if (isAsyncFileEmbedElement(root) && !root.closest?.(".obcc-inline-workbench-embed")) {
        return true;
      }
      return Array.from(root.querySelectorAll?.(
        ".internal-embed[src], .internal-embed[data-src]"
      ) || []).some((element) => isAsyncFileEmbedElement(element) && !element.closest?.(".obcc-inline-workbench-embed"));
    });
    if (found) {
      this.readingAsyncEmbedFilePath = filePath;
    } else if (this.readingAsyncEmbedFilePath === filePath) {
      this.readingAsyncEmbedFilePath = "";
    }
    return found;
  }
  repairAsyncFileEmbedSections(renderer = this.readingPreviewRenderer()) {
    let repaired = 0;
    for (const section of renderer?.sections || []) {
      if (!section?.el?.isConnected || section.shown === false) {
        continue;
      }
      const placeholders = Array.from(section.el.querySelectorAll?.(
        ".internal-embed[src], .internal-embed[data-src]"
      ) || []).filter((element) => isAsyncFileEmbedElement(element) && !element.closest?.(".obcc-inline-workbench-embed") && !element.classList?.contains("file-embed") && element.dataset?.noteDrawAsyncEmbedRepair !== "true");
      for (const element of placeholders) {
        repaired += this.promoteAsyncFileEmbedPlaceholder(element) ? 1 : 0;
      }
    }
    return repaired + this.repairConnectedReadingSections(renderer);
  }
  promoteAsyncFileEmbedPlaceholder(element) {
    if (!element?.isConnected || !isAsyncFileEmbedElement(element) || element.classList?.contains("file-embed") || element.dataset?.noteDrawAsyncEmbedRepair === "true") {
      return false;
    }
    const replacement = element.cloneNode(true);
    replacement.classList.add("file-embed", "mod-generic", "is-loaded");
    replacement.dataset.noteDrawAsyncEmbedRepair = "true";
    element.replaceWith(replacement);
    return true;
  }
  repairConnectedReadingSections(renderer = this.readingPreviewRenderer()) {
    if (!renderer || !this.previewEl?.isConnected) {
      return 0;
    }
    let repaired = 0;
    try {
      renderer.updateVirtualDisplay?.();
      for (const section of renderer.sections || []) {
        if (section?.shown === false || !section?.el?.isConnected || section.rendered !== false) {
          continue;
        }
        section.render?.();
        renderer.measureSection?.(section);
        repaired += 1;
      }
      renderer.updateVirtualDisplay?.();
    } catch (error) {
      void error;
    }
    return repaired;
  }
  captureReadingLogicalSizerHeight(renderer = this.readingPreviewRenderer(), options = {}) {
    const sizer = renderer?.sizerEl;
    const allowGrowth = options.allowGrowth === true;
    const frozenByZoom = Math.abs(this.readingZoomScale() - 1) >= 1e-3 && this.readingLogicalSizerHeight > 0;
    if (!sizer?.isConnected || !allowGrowth && (this.readingVirtualSectionsManaged || frozenByZoom)) {
      return this.readingLogicalSizerHeight;
    }
    const sectionHeight = (renderer.sections || []).reduce((total, section) => {
      return total + (section?.shown === false ? 0 : Math.max(0, Number(section?.height) || 0));
    }, Math.max(0, Number(renderer.topSpace) || 0));
    const viewportHeight = Math.max(1, Number(this.previewEl?.clientHeight) || 1);
    const orphanedManagedHeight = sizer.hasAttribute("data-note-draw-virtual-height") && !this.readingVirtualStyleState.has(sizer);
    const staleLegacyHeight = !sizer.hasAttribute("data-note-draw-virtual-height") && shouldClearStaleReadingVirtualMinHeight({
      inlineMinHeight: Number.parseFloat(sizer.style.minHeight) || 0,
      sectionHeight,
      viewportHeight
    });
    if (orphanedManagedHeight || staleLegacyHeight) {
      sizer.style.removeProperty("min-height");
      sizer.removeAttribute("data-note-draw-virtual-height");
    }
    const observedHeight = Math.max(
      sectionHeight,
      Math.max(0, Number(sizer.scrollHeight) || 0) - Math.max(0, Number(this.readingBottomSpacerHeight) || 0),
      Math.max(0, Number(sizer.offsetHeight) || 0)
    );
    const runawayThreshold = Math.max(8e3, sectionHeight * 4, viewportHeight * 8);
    const priorHeight = this.readingLogicalSizerHeight;
    const transientGrowth = observedHeight > runawayThreshold && sectionHeight > 0 && observedHeight > sectionHeight * 4;
    const staleGrowth = priorHeight > runawayThreshold && sectionHeight > 0 && observedHeight < priorHeight * 0.55;
    const candidate = transientGrowth || staleGrowth ? sectionHeight + Math.max(192, Math.min(640, sectionHeight * 0.35)) : observedHeight;
    this.readingLogicalSizerHeight = staleGrowth || transientGrowth ? Math.max(sectionHeight, Math.ceil(candidate)) : Math.max(this.readingLogicalSizerHeight, candidate);
    return this.readingLogicalSizerHeight;
  }
  readingVirtualZoomOrigin(target) {
    void target;
    return 0;
  }
  readingViewportMetrics() {
    const scroller = this.scrollContainer?.isConnected ? this.scrollContainer : findScrollableAncestor(this.previewEl);
    const previewRect = this.previewEl?.getBoundingClientRect?.();
    const scrollerRect = scroller?.getBoundingClientRect?.();
    if (!scroller || scroller === this.previewEl || !previewRect || !scrollerRect) {
      return {
        scroller: this.previewEl,
        scrollTop: Math.max(0, Number(this.previewEl?.scrollTop) || 0),
        scrollLeft: Math.max(0, Number(this.previewEl?.scrollLeft) || 0),
        viewportHeight: Math.max(1, Number(this.previewEl?.clientHeight) || Number(previewRect?.height) || 1),
        viewportWidth: Math.max(1, Number(this.previewEl?.clientWidth) || Number(previewRect?.width) || 1)
      };
    }
    return {
      scroller,
      scrollTop: Math.max(0, scrollerRect.top - previewRect.top),
      scrollLeft: Math.max(0, scrollerRect.left - previewRect.left),
      viewportHeight: Math.max(1, Number(scroller.clientHeight) || Number(scrollerRect.height) || 1),
      viewportWidth: Math.max(1, Number(scroller.clientWidth) || Number(scrollerRect.width) || 1)
    };
  }
  scheduleReadingVirtualSectionSync() {
    if (this.destroyed || !this.usesVisualReadingZoom() || Math.abs(this.readingZoomScale() - 1) < 1e-3 || !this.readingPreviewRenderer()) {
      return;
    }
    if (this.isReadingZoomInteractionActive()) {
      return;
    }
    if (this.readingVirtualSectionFrameId !== null) {
      return;
    }
    this.readingVirtualSectionFrameId = window.requestAnimationFrame(() => {
      this.readingVirtualSectionFrameId = null;
      this.syncReadingVirtualSections();
    });
  }
  cancelReadingVirtualSectionSync() {
    if (this.readingVirtualSectionFrameId !== null) {
      window.cancelAnimationFrame(this.readingVirtualSectionFrameId);
      this.readingVirtualSectionFrameId = null;
    }
  }
  restoreReadingVirtualSections() {
    this.cancelReadingVirtualSectionSync();
    if (!this.readingVirtualSectionsManaged && !this.readingVirtualStyleState.size) {
      return;
    }
    this.readingVirtualSectionsManaged = false;
    this.readingVirtualSectionSignature = "";
    this.restoreReadingVirtualStyles();
    const renderer = this.readingPreviewRenderer();
    if (!renderer || !this.previewEl?.isConnected) {
      return;
    }
    this.repairConnectedReadingSections(renderer);
    this.readingLogicalSizerHeight = 0;
    this.captureReadingLogicalSizerHeight(renderer);
    if (this.readingBottomSpacerHeight > 0) {
      this.ensureReadingBottomSpacer();
    }
  }
  rememberReadingVirtualStyle(element, property) {
    if (!element?.style) {
      return;
    }
    let properties = this.readingVirtualStyleState.get(element);
    if (!properties) {
      properties = /* @__PURE__ */ new Map();
      this.readingVirtualStyleState.set(element, properties);
    }
    if (!properties.has(property)) {
      properties.set(property, {
        value: element.style.getPropertyValue(property),
        priority: element.style.getPropertyPriority(property)
      });
    }
  }
  restoreReadingVirtualStyles() {
    for (const [element, properties] of this.readingVirtualStyleState) {
      for (const [property, state] of properties) {
        if (state.value) {
          element.style?.setProperty(property, state.value, state.priority || "");
        } else {
          element.style?.removeProperty(property);
        }
      }
      element.removeAttribute?.("data-note-draw-virtual-height");
      element.removeAttribute?.("data-note-draw-virtual-pusher");
    }
    this.readingVirtualStyleState.clear();
  }
  syncReadingVirtualSections() {
    if (this.isReadingZoomInteractionActive()) {
      return false;
    }
    const renderer = this.readingPreviewRenderer();
    const target = this.readingZoomTarget;
    const zoom = this.readingZoomScale();
    const sizer = renderer?.sizerEl || rootPreviewSizer(this.previewEl);
    const pusher = renderer?.pusherEl;
    const sections = renderer?.sections || [];
    if (!renderer || target !== sizer || !target?.isConnected || !this.readingZoomStage?.contains?.(sizer) || !pusher || !sections.length || Math.abs(zoom - 1) < 1e-3) {
      return false;
    }
    if (this.readingHasPendingAsyncFileEmbeds(renderer)) {
      this.restoreReadingVirtualSections();
      this.repairAsyncFileEmbedSections(renderer);
      this.scheduleEmbedRepair(0);
      return false;
    }
    this.rememberReadingVirtualStyle(sizer, "min-height");
    this.rememberReadingVirtualStyle(pusher, "margin-bottom");
    const shown = sections.filter((section) => section?.shown !== false && section?.el);
    const knownHeights = shown.map((section) => Number(section.height) || 0).filter((height) => height > 0);
    const averageHeight = knownHeights.length ? knownHeights.reduce((sum, height) => sum + height, 0) / knownHeights.length : 48;
    const topSpace = Math.max(0, Number(renderer.topSpace) || 0);
    let cursor = topSpace;
    const records = [];
    for (const section of shown) {
      const height = Math.max(1, Number(section.height) || averageHeight);
      records.push({ section, start: cursor, end: cursor + height, height });
      cursor += height;
    }
    const documentHeight = Math.max(1, cursor - topSpace, this.captureReadingLogicalSizerHeight(renderer));
    const metrics = this.readingViewportMetrics();
    const viewport = calculateVisualZoomLogicalWindow({
      scrollTop: metrics.scrollTop,
      viewportHeight: metrics.viewportHeight,
      zoom,
      origin: 0
    });
    const logicalTop = clamp10(viewport.top, 0, Math.max(0, documentHeight - viewport.height));
    const logicalBottom = logicalTop + viewport.height;
    const buffer = Math.max(
      240,
      viewport.height * Math.max(0.5, Number(renderer.renderExtra) || 1),
      (Number(renderer.renderExtraMinPx) || 0) / zoom
    );
    let visible = records.filter((record) => record.end >= logicalTop - buffer && record.start <= logicalBottom + buffer);
    if (!visible.length && records.length) {
      visible = [records.reduce((nearest, record) => {
        const distance = Math.min(Math.abs(record.start - logicalTop), Math.abs(record.end - logicalTop));
        return !nearest || distance < nearest.distance ? { record, distance } : nearest;
      }, null).record];
    }
    const selection = this.previewEl.ownerDocument?.defaultView?.getSelection?.();
    if (selection?.rangeCount) {
      const range = selection.getRangeAt(0);
      if (!range.collapsed && this.previewEl.contains(range.commonAncestorContainer)) {
        visible = records.filter((record) => {
          return visible.includes(record) || record.section.el === range.startContainer || record.section.el === range.endContainer || record.section.el.contains?.(range.startContainer) || record.section.el.contains?.(range.endContainer);
        });
      }
    }
    const selectedIndexes = visible.map((record) => records.indexOf(record)).filter((index) => index >= 0);
    if (selectedIndexes.length > 1) {
      const firstSelected = Math.min(...selectedIndexes);
      const lastSelected = Math.max(...selectedIndexes);
      visible = records.slice(firstSelected, lastSelected + 1);
    }
    visible.sort((a, b) => sections.indexOf(a.section) - sections.indexOf(b.section));
    for (const record of visible) {
      record.section.render?.();
    }
    const first = visible[0] || records[0];
    const sectionElements = visible.map((record) => record.section.el);
    const validSectionElements = sectionElements.length > 0 && sectionElements.every((element) => element && element !== sizer && element.ownerDocument === sizer.ownerDocument && !element.contains?.(sizer));
    if (!validSectionElements || typeof sizer.setChildrenInPlace !== "function") {
      return false;
    }
    const bottomSpacer = this.readingBottomSpacerHeight > 0 ? this.readingBottomSpacer : null;
    const desiredChildren = [pusher, ...sectionElements, ...bottomSpacer ? [bottomSpacer] : []];
    const currentChildren = Array.from(sizer.children || []);
    const childrenChanged = currentChildren.length !== desiredChildren.length || currentChildren.some((child, index) => child !== desiredChildren[index]);
    if (childrenChanged) {
      sizer.setChildrenInPlace(desiredChildren);
    }
    const pusherOffset = Math.max(0, (first?.start || topSpace) - topSpace);
    const pusherMargin = `${pusherOffset}px`;
    if (pusher.style.marginBottom !== pusherMargin) {
      pusher.style.marginBottom = pusherMargin;
    }
    const minimumHeight = `${Math.max(0, documentHeight + Number(this.readingBottomSpacerHeight || 0) - 1)}px`;
    if (sizer.style.minHeight !== minimumHeight) {
      sizer.style.minHeight = minimumHeight;
    }
    sizer.setAttribute("data-note-draw-virtual-height", "true");
    pusher.setAttribute("data-note-draw-virtual-pusher", "true");
    const visibleIndexes = visible.map((record) => sections.indexOf(record.section)).filter((index) => index >= 0);
    const firstIndex = visibleIndexes[0] ?? 0;
    const lastIndex = visibleIndexes[visibleIndexes.length - 1] ?? firstIndex;
    const measureThrough = lastIndex === sections.length - 1 ? lastIndex : lastIndex - 1;
    for (let index = firstIndex; index <= measureThrough; index += 1) {
      if (sections[index]?.shown !== false && sections[index]?.el?.isConnected) {
        renderer.measureSection?.(sections[index]);
      }
    }
    renderer.renderHighlights?.(firstIndex, lastIndex);
    this.readingVirtualSectionsManaged = true;
    const signature = visibleIndexes.join(",");
    if (signature !== this.readingVirtualSectionSignature) {
      this.readingVirtualSectionSignature = signature;
    }
    const hasRenderedAnchors = sectionElements.some((element) => element.querySelector?.("[data-note-draw-line-start]"));
    if (this.noteFlowOperationPending && this.hasNoteFlowElements() && !hasRenderedAnchors) {
      this.scheduleMarkdownAnnotationRefresh({ layout: false });
    }
    return true;
  }
  rememberReadingZoomStyles(element) {
    if (!element || this.readingZoomStyleState.has(element)) {
      return;
    }
    const properties = ["zoom", "transform", "transform-origin", "margin-bottom"];
    const computedStyle = window.getComputedStyle(element);
    this.readingZoomStyleState.set(element, Object.fromEntries(properties.map((property) => [property, {
      value: element.style.getPropertyValue(property),
      priority: element.style.getPropertyPriority(property),
      computedValue: computedStyle.getPropertyValue(property)
    }])));
  }
  restoreReadingZoomStyles() {
    for (const [element, properties] of this.readingZoomStyleState) {
      for (const [property, state] of Object.entries(properties)) {
        if (state.value) {
          element.style.setProperty(property, state.value, state.priority || "");
        } else {
          element.style.removeProperty(property);
        }
      }
    }
    this.readingZoomStyleState.clear();
  }
  measureReadingZoomOrigin(element) {
    if (element === this.readingZoomStage) {
      return { x: 0, y: 0 };
    }
    if (this.readingZoomStage?.contains?.(element)) {
      let x = 0;
      let y = 0;
      let current = element;
      while (current && current !== this.readingZoomStage) {
        x += Number(current.offsetLeft) || 0;
        y += Number(current.offsetTop) || 0;
        current = current.offsetParent;
      }
      return { x, y };
    }
    const previewRect = this.previewEl?.getBoundingClientRect?.();
    const elementRect = element?.getBoundingClientRect?.();
    if (!previewRect || !elementRect) {
      return { x: 0, y: 0 };
    }
    return {
      x: elementRect.left - previewRect.left + (Number(this.previewEl.scrollLeft) || 0),
      y: elementRect.top - previewRect.top + (Number(this.previewEl.scrollTop) || 0)
    };
  }
  readingZoomOrigin(element) {
    if (element === this.readingZoomBaseTarget && this.readingZoomBaseOrigin) {
      return { ...this.readingZoomBaseOrigin };
    }
    return this.measureReadingZoomOrigin(element);
  }
  captureReadingZoomBaseOrigin(target) {
    if (!target?.isConnected) {
      return { x: 0, y: 0 };
    }
    if (this.readingZoomBaseTarget !== target || !this.readingZoomBaseOrigin) {
      this.readingZoomBaseTarget = target;
      this.readingZoomBaseOrigin = this.measureReadingZoomOrigin(target);
    }
    return { ...this.readingZoomBaseOrigin };
  }
  readingZoomScrollerOrigin(scroller, target = this.readingZoomTarget) {
    void target;
    if (!scroller || scroller === this.previewEl) {
      return { x: 0, y: 0 };
    }
    const previewRect = this.previewEl?.getBoundingClientRect?.();
    const scrollerRect = scroller?.getBoundingClientRect?.();
    if (!previewRect || !scrollerRect) {
      return { x: 0, y: 0 };
    }
    return {
      x: previewRect.left - scrollerRect.left + (Number(scroller.scrollLeft) || 0),
      y: previewRect.top - scrollerRect.top + (Number(scroller.scrollTop) || 0)
    };
  }
  updateReadingZoomExtent(zoom, target) {
    if (!this.readingZoomExtent) {
      return;
    }
    if (!this.usesVisualReadingZoom() || Math.abs(zoom - 1) < 1e-3) {
      this.readingZoomExtent.style.removeProperty("width");
      this.readingZoomExtent.style.removeProperty("height");
      this.readingZoomExtent.removeAttribute("data-active");
      return;
    }
    const targetOrigin = this.readingZoomOrigin(target);
    const logicalWidth = Math.max(this.canvasCssWidth || 0, targetOrigin.x + (target?.scrollWidth || target?.offsetWidth || 0), this.previewEl.clientWidth || 0);
    const logicalHeight = Math.max(this.canvasCssHeight || 0, targetOrigin.y + (target?.scrollHeight || target?.offsetHeight || 0), this.previewEl.clientHeight || 0);
    const width = `${Math.max(1, Math.ceil(logicalWidth * zoom))}px`;
    const height = `${Math.max(1, Math.ceil(logicalHeight * zoom))}px`;
    if (this.readingZoomExtent.style.width !== width) {
      this.readingZoomExtent.style.setProperty("width", width);
    }
    if (this.readingZoomExtent.style.height !== height) {
      this.readingZoomExtent.style.setProperty("height", height);
    }
    this.readingZoomExtent.setAttribute("data-active", "true");
  }
  applyReadingZoom() {
    if (!this.canZoomReadingSurface()) {
      return false;
    }
    const target = findResponsiveContentElement(this.previewEl, this.surfaceType);
    if (!target) {
      return false;
    }
    const previousTarget = this.readingZoomTarget;
    if (previousTarget && previousTarget !== target) {
      this.restoreReadingZoomStyles();
      this.readingZoomBaseTarget = null;
      this.readingZoomBaseOrigin = null;
    }
    this.readingZoomTarget = target;
    this.captureReadingLogicalSizerHeight();
    const visualTarget = this.usesVisualReadingZoom() ? this.ensureReadingZoomStage(target) : target;
    if (!visualTarget) {
      return false;
    }
    const zoom = this.readingZoomScale();
    this.readingZoom = zoom;
    if (this.usesVisualReadingZoom()) {
      this.applyInheritedReadingLayoutZoom(target);
    }
    if (Math.abs(zoom - 1) >= 1e-3) {
      this.captureReadingZoomBaseOrigin(target);
    }
    const elements = this.readingZoomElements(target);
    if (Math.abs(zoom - 1) < 1e-3) {
      this.restoreReadingZoomStyles();
      if (this.usesVisualReadingZoom()) {
        this.applyInheritedReadingLayoutZoom(target);
      }
      if (!this.isReadingZoomInteractionActive()) {
        this.restoreReadingVirtualSections();
        this.readingZoomBaseTarget = null;
        this.readingZoomBaseOrigin = null;
      }
      this.previewEl.removeClass("is-reading-zoomed");
      this.previewEl.toggleClass(
        "is-editing-layout-zoomed",
        Math.abs(Number(this.inheritedReadingLayoutZoom) - 1) >= 1e-3
      );
    } else {
      if (this.usesVisualReadingZoom()) {
        for (const element of elements) {
          this.applyVisualReadingZoomElement(element, zoom);
        }
        const targetState = this.readingZoomStyleState.get(visualTarget);
        const originalMarginBottom = Number.parseFloat(targetState?.["margin-bottom"]?.computedValue || targetState?.["margin-bottom"]?.value) || 0;
        const targetHeight = Math.max(1, visualTarget?.offsetHeight || visualTarget?.scrollHeight || 1);
        if (zoom < 1) {
          visualTarget.style.setProperty("margin-bottom", `${calculateReadingZoomMargin(originalMarginBottom, targetHeight, zoom)}px`);
        } else if (targetState?.["margin-bottom"]?.value) {
          visualTarget.style.setProperty("margin-bottom", targetState["margin-bottom"].value, targetState["margin-bottom"].priority || "");
        } else {
          visualTarget.style.removeProperty("margin-bottom");
        }
      } else {
        for (const element of elements) {
          this.rememberReadingZoomStyles(element);
          element.style.setProperty("zoom", String(zoom));
          applyElementStyles(element, { transformOrigin: "top left" });
        }
      }
      this.previewEl.toggleClass("is-reading-zoomed", this.usesVisualReadingZoom());
      this.previewEl.toggleClass("is-editing-layout-zoomed", !this.usesVisualReadingZoom() || Math.abs(Number(this.inheritedReadingLayoutZoom) - 1) >= 1e-3);
    }
    this.updateReadingZoomExtent(zoom, target);
    this.scheduleReadingVirtualSectionSync();
    return true;
  }
  clearReadingZoomStyles() {
    this.cancelReadingZoomSettle();
    this.restoreReadingZoomStyles();
    this.restoreReadingVirtualSections();
    this.updateReadingZoomExtent(1, this.readingZoomTarget);
    this.previewEl?.removeClass("is-reading-zoomed");
    this.previewEl?.removeClass("is-editing-layout-zoomed");
    this.readingZoomTarget = null;
    this.readingZoomBaseTarget = null;
    this.readingZoomBaseOrigin = null;
  }
  setReadingZoom(value, clientPoint = null, options = {}) {
    if (!this.canZoomReadingSurface()) {
      return false;
    }
    const next = clamp10(Number(value) || 1, MIN_READING_ZOOM, MAX_READING_ZOOM);
    const previous = this.readingZoom || 1;
    const zoomChanged = Math.abs(next - previous) >= 1e-3;
    const previousClientPoint = options.previousClientPoint || clientPoint;
    if (!zoomChanged && !previousClientPoint) {
      return true;
    }
    const scroller = findScrollableAncestor(this.previewEl);
    const scrollerRect = scroller?.getBoundingClientRect?.();
    const center = (point) => ({
      x: Number.isFinite(point?.x) && scrollerRect ? point.x - scrollerRect.left : (scroller?.clientWidth || 0) / 2,
      y: Number.isFinite(point?.y) && scrollerRect ? point.y - scrollerRect.top : (scroller?.clientHeight || 0) / 2
    });
    const previousCenter = center(previousClientPoint);
    const nextCenter = center(clientPoint);
    const scrollLeft = Number(scroller?.scrollLeft) || 0;
    const scrollTop = Number(scroller?.scrollTop) || 0;
    if (this.usesVisualReadingZoom() && (zoomChanged || previousClientPoint)) {
      this.scheduleReadingZoomSettle(options.settleDelay);
    }
    if (zoomChanged) {
      this.readingZoom = next;
      this.applyReadingZoom();
    }
    const ratio = next / previous;
    if (scroller && Number.isFinite(ratio)) {
      const origin = this.usesVisualReadingZoom() ? this.readingZoomScrollerOrigin(scroller) : { x: 0, y: 0 };
      const scroll = calculatePinchPanScroll({
        scrollLeft,
        scrollTop,
        previousCenter,
        nextCenter,
        zoomRatio: ratio,
        originX: origin.x,
        originY: origin.y,
        maxScrollLeft: Math.max(0, scroller.scrollWidth - scroller.clientWidth),
        maxScrollTop: Math.max(0, scroller.scrollHeight - scroller.clientHeight)
      });
      if (Math.abs(scroller.scrollLeft - scroll.left) >= 0.25) {
        scroller.scrollLeft = scroll.left;
      }
      if (Math.abs(scroller.scrollTop - scroll.top) >= 0.25) {
        scroller.scrollTop = scroll.top;
      }
    }
    if (zoomChanged) {
      if (!this.usesVisualReadingZoom()) {
        this.responsiveLayoutContext = null;
      }
      if (options.resize !== false) {
        const visualReadingZoom = this.usesVisualReadingZoom();
        this.scheduleResize({ layout: !visualReadingZoom, measure: !visualReadingZoom });
      }
      if (options.persist !== false) {
        this.persistInteractionSettings();
      }
      if (options.share !== false) {
        this.syncSharedToolbarState();
      }
    }
    return true;
  }
  startMultiTouchScroll(event) {
    this.suppressTouchDrawing = true;
    this.multiTouchScrolling = true;
    this.multiTouchLastCenter = this.getTouchCenter();
    this.multiTouchLastDistance = this.getTouchDistance();
    this.multiTouchInitialDistance = this.multiTouchLastDistance;
    this.multiTouchGestureMode = "undecided";
    this.multiTouchPendingCenter = null;
    this.multiTouchPendingDistance = null;
    this.multiTouchPinching = false;
    if (this.scrollSettleTimer !== null) {
      window.clearTimeout(this.scrollSettleTimer);
      this.scrollSettleTimer = null;
    }
    this.previewEl.addClass("is-two-finger-scroll");
    this.cancelCurrentStroke();
    this.cancelSelectedStrokeDrag(true);
    event.preventDefault();
    event.stopPropagation();
  }
  completeTrackedTouch(pointerId) {
    if (!this.touchPointers.has(pointerId)) {
      return false;
    }
    const finishingMultiTouch = this.multiTouchScrolling || this.multiTouchPinching || this.suppressTouchDrawing;
    if (this.multiTouchScrolling && this.touchPointers.size >= 2) {
      this.flushMultiTouchGesture();
    }
    this.touchPointers.delete(pointerId);
    if (this.touchPointers.size < 2) {
      this.multiTouchScrolling = false;
      this.multiTouchLastCenter = null;
      this.multiTouchLastDistance = null;
      this.multiTouchInitialDistance = null;
      this.multiTouchGestureMode = "undecided";
      this.multiTouchPendingCenter = null;
      this.multiTouchPendingDistance = null;
      this.previewEl.removeClass("is-two-finger-scroll");
    }
    if (this.touchPointers.size === 0) {
      this.suppressTouchDrawing = false;
      if (finishingMultiTouch && this.multiTouchPinching) {
        this.persistInteractionSettings();
        this.syncSharedToolbarState();
      }
      this.multiTouchPinching = false;
      if (finishingMultiTouch) {
        this.scheduleReadingZoomSettle(80);
        this.scheduleResize({ layout: false, measure: false });
        this.requestRender(true);
      }
    }
    return true;
  }
  resetTouchGestureState() {
    this.cancelMultiTouchFrame();
    this.touchPointers.clear();
    this.multiTouchScrolling = false;
    this.multiTouchGestureMode = "undecided";
    this.multiTouchInitialDistance = null;
    this.multiTouchLastCenter = null;
    this.multiTouchLastDistance = null;
    this.multiTouchPendingCenter = null;
    this.multiTouchPendingDistance = null;
    this.multiTouchPinching = false;
    this.suppressTouchDrawing = false;
    this.previewEl?.removeClass("is-two-finger-scroll");
  }
  handleMultiTouchScroll(event) {
    if (!this.multiTouchScrolling || this.touchPointers.size < 2) {
      return;
    }
    this.multiTouchPendingCenter = this.getTouchCenter();
    this.multiTouchPendingDistance = this.getTouchDistance();
    if (this.multiTouchFrameId === null) {
      this.multiTouchFrameId = window.requestAnimationFrame(() => {
        this.multiTouchFrameId = null;
        this.flushMultiTouchGesture();
      });
    }
    event.preventDefault();
    event.stopPropagation();
  }
  cancelMultiTouchFrame() {
    if (this.multiTouchFrameId !== null) {
      window.cancelAnimationFrame(this.multiTouchFrameId);
      this.multiTouchFrameId = null;
    }
  }
  flushMultiTouchGesture() {
    this.cancelMultiTouchFrame();
    const center = this.multiTouchPendingCenter;
    const previous = this.multiTouchLastCenter;
    const distance = this.multiTouchPendingDistance;
    const previousDistance = this.multiTouchLastDistance;
    this.multiTouchPendingCenter = null;
    this.multiTouchPendingDistance = null;
    if (!this.multiTouchScrolling || this.touchPointers.size < 2 || !center || !previous) {
      return;
    }
    let nextZoom = this.readingZoom;
    this.multiTouchGestureMode = resolveMultiTouchGestureMode({
      mode: this.multiTouchGestureMode,
      initialDistance: this.multiTouchInitialDistance,
      distance,
      centerMovement: pointerDistance(previous, center),
      minimumDistanceChange: 12,
      minimumScaleChange: 0.06,
      minimumPinchDominance: 1.35
    });
    if (this.canZoomReadingSurface() && this.multiTouchGestureMode === "pinch" && distance && previousDistance) {
      const rawRatio = distance / previousDistance;
      const ratioDelta = rawRatio - 1;
      if (Math.abs(ratioDelta) >= 0.012) {
        this.multiTouchPinching = true;
        const dampedRatio = 1 + clamp10(ratioDelta * 0.42, -0.12, 0.12);
        nextZoom = this.readingZoom * dampedRatio;
      }
    }
    this.setReadingZoom(nextZoom, center, {
      previousClientPoint: previous,
      persist: false,
      resize: false,
      share: false
    });
    this.multiTouchLastCenter = center;
    if (distance) {
      this.multiTouchLastDistance = distance;
    }
  }
  getTouchCenter() {
    if (!this.touchPointers.size) {
      return null;
    }
    let x = 0;
    let y = 0;
    for (const point of this.touchPointers.values()) {
      x += point.x;
      y += point.y;
    }
    return {
      x: x / this.touchPointers.size,
      y: y / this.touchPointers.size
    };
  }
  getTouchDistance() {
    if (this.touchPointers.size < 2) {
      return null;
    }
    const points = Array.from(this.touchPointers.values());
    return pointerDistance(points[0], points[1]);
  }
  cancelCurrentStroke() {
    if (this.activePointerId !== null) {
      try {
        if (this.canvas.hasPointerCapture?.(this.activePointerId)) {
          this.canvas.releasePointerCapture(this.activePointerId);
        }
      } catch (error) {
        void error;
      }
    }
    this.currentStroke = null;
    this.connectorGesture = null;
    if (this.connectorReconnect) {
      const reconnect = this.connectorReconnect;
      const stroke = this.drawingData?.strokes?.[reconnect.index];
      if (stroke) {
        stroke.connector = reconnect.connector ? { ...reconnect.connector } : null;
        stroke.points = reconnect.points.map((point) => ({ ...point }));
      }
      this.connectorReconnect = null;
    }
    this.pointerDown = false;
    this.pointerStartPoint = null;
    this.pointerStartClient = null;
    this.pointerStartEditable = null;
    this.pointerStartSourceText = false;
    this.activePointerId = null;
    this.pendingSelectionTap = null;
    this.didMove = false;
    this.resetTextHighlightGesture();
    this.render();
  }
  startPendingSelectionTap(event, action) {
    this.endTextEdit();
    this.cancelCurrentStroke();
    this.pendingSelectionTap = {
      ...action,
      pointerId: event.pointerId,
      startClient: { x: event.clientX, y: event.clientY },
      startPoint: this.eventToPoint(event),
      moved: false,
      scrollRow: action.scrollRow || null,
      scrollStartLeft: action.scrollRow?.scrollLeft || 0,
      scrollingRow: false
    };
    this.pointerStartClient = { x: event.clientX, y: event.clientY };
    this.activePointerId = event.pointerId;
    this.startSelectionLongPress(event, { pendingAction: action });
    try {
      this.canvas.setPointerCapture(event.pointerId);
    } catch (error) {
      void error;
    }
    event.preventDefault();
    event.stopPropagation();
  }
  updatePendingSelectionTap(event) {
    const pending = this.pendingSelectionTap;
    if (!pending || event.pointerId !== pending.pointerId) {
      return;
    }
    const deltaX = event.clientX - pending.startClient.x;
    const deltaY = event.clientY - pending.startClient.y;
    const scrollActivation = this.selectedDragActivationDistancePx(event.pointerType);
    if (pending.type === "scroll-row" && (pending.scrollingRow || pending.scrollRow?.isConnected && pending.scrollRow.scrollWidth > pending.scrollRow.clientWidth + 1 && Math.abs(deltaX) > scrollActivation && Math.abs(deltaX) > Math.abs(deltaY) * 1.15)) {
      pending.moved = true;
      pending.scrollingRow = true;
      this.clearSelectionLongPress();
      pending.scrollRow.scrollLeft = pending.scrollStartLeft - deltaX;
      event.preventDefault();
      event.stopPropagation();
      return;
    }
    const canBecomeElementDrag = [
      "edit-markdown-or-drag",
      "edit-stroke-or-drag",
      "select-stroke",
      "select-markdown",
      "select-group",
      "enter-stroke-group",
      "enter-markdown-group",
      "drag-selection"
    ].includes(pending.type);
    const activationDistance = canBecomeElementDrag ? this.selectedDragActivationDistancePx(event.pointerType) : this.tapDistancePx();
    if (pointerDistance(pending.startClient, { x: event.clientX, y: event.clientY }) > activationDistance) {
      pending.moved = true;
      this.clearSelectionLongPress();
      if (canBecomeElementDrag) {
        this.pendingSelectionTap = null;
        if (pending.type === "select-stroke") {
          this.setSelectedStrokes(pending.index);
        } else if (pending.type === "select-markdown") {
          const block = this.selectMarkdownBlock(pending.element);
          this.activateMarkdownSelection(block, pending.element);
        } else if (pending.type === "select-group") {
          this.enteredElementGroupIds.delete(pending.groupId);
          this.selectElementGroup(pending.groupId);
        } else if (pending.type === "enter-stroke-group") {
          this.enteredElementGroupIds.add(pending.groupId);
          this.selectedMarkdownBlockIds.clear();
          this.setSelectedStrokes(pending.index, { skipGroupExpansion: true });
        } else if (pending.type === "enter-markdown-group") {
          this.enteredElementGroupIds.add(pending.groupId);
          this.selectedStrokeIndexes.clear();
          this.selectedStrokeIndex = -1;
          this.selectMarkdownBlock(pending.element, { skipGroupExpansion: true });
        }
        this.startSelectedStrokeDrag(event, pending.startPoint || this.eventToPoint(event), pending.index ?? -1, {
          preserveSelection: pending.preserveSelection || pending.type === "select-group",
          startClient: pending.startClient
        });
        this.moveSelectedStroke(event);
        return;
      }
    }
    event.preventDefault();
    event.stopPropagation();
  }
  applyPendingSelectionTap(pending) {
    if (!pending) {
      return;
    }
    if (pending.type === "select-stroke") {
      this.setSelectedStrokes(pending.index);
    } else if (pending.type === "toggle-stroke") {
      this.toggleStrokeSelection(pending.index);
    } else if (pending.type === "enter-stroke-group") {
      this.enteredElementGroupIds.add(pending.groupId);
      this.selectedMarkdownBlockIds.clear();
      this.setSelectedStrokes(pending.index, { skipGroupExpansion: true });
    } else if (pending.type === "select-markdown") {
      const block = this.selectMarkdownBlock(pending.element);
      this.activateMarkdownSelection(block, pending.element);
    } else if (pending.type === "toggle-markdown") {
      this.toggleMarkdownBlockSelection(pending.element);
    } else if (pending.type === "enter-markdown-group") {
      this.enteredElementGroupIds.add(pending.groupId);
      this.selectedStrokeIndexes.clear();
      this.selectedStrokeIndex = -1;
      this.selectMarkdownBlock(pending.element, { skipGroupExpansion: true });
    } else if (pending.type === "select-group") {
      this.enteredElementGroupIds.delete(pending.groupId);
      this.selectElementGroup(pending.groupId);
    } else if (pending.type === "edit-markdown-or-drag") {
      return this.startTextEdit(pending.editable || pending.element, pending.clientPoint || null);
    } else if (pending.type === "edit-stroke-or-drag") {
      this.editFloatingTextStroke(pending.index);
    }
  }
  executeButtonCommand(index) {
    const stroke = this.drawingData?.strokes?.[Number(index)];
    return this.executeElementCommand(stroke);
  }
  executeElementCommand(element) {
    if (!this.elementHasCommandFrame(element)) {
      return false;
    }
    const commandId = String(element?.commandId || "").trim();
    if (!commandId) {
      return false;
    }
    try {
      const executed = this.plugin.app.commands?.executeCommandById?.(commandId);
      if (executed === false) {
        new import_obsidian.Notice(this.plugin.t("commandUnavailable", { name: element.commandName || commandId }));
      }
    } catch (error) {
      console.error(`[${PLUGIN_ID}] Failed to execute button command`, error);
      new import_obsidian.Notice(this.plugin.t("commandUnavailable", { name: element.commandName || commandId }));
    }
    return true;
  }
  finishPendingSelectionTap(event) {
    const pending = this.pendingSelectionTap;
    const movedDistance = pending ? pointerDistance(pending.startClient, { x: event.clientX, y: event.clientY }) : Number.POSITIVE_INFINITY;
    this.pendingSelectionTap = null;
    this.clearSelectionLongPress();
    this.pointerStartClient = null;
    this.activePointerId = null;
    this.releasePointerCapture(event.pointerId);
    if (pending && !pending.moved && movedDistance <= this.tapDistancePx()) {
      pending.clientPoint = { x: event.clientX, y: event.clientY };
      const applied = this.applyPendingSelectionTap(pending);
      if (pending.type !== "edit-markdown-or-drag" || applied === false) {
        this.render();
      }
    }
    event.preventDefault();
    event.stopPropagation();
  }
  cancelPendingSelectionTap() {
    const pointerId = this.activePointerId;
    this.pendingSelectionTap = null;
    this.clearSelectionLongPress();
    this.pointerStartClient = null;
    this.activePointerId = null;
    if (pointerId !== null) {
      this.releasePointerCapture(pointerId);
    }
  }
  startSelectionDrag(event, point, options = {}) {
    this.endTextEdit();
    this.cancelCurrentStroke();
    this.selectingStrokes = true;
    this.selectionStartPoint = point;
    this.selectionCurrentPoint = point;
    this.pointerStartClient = options.startClient ? { x: options.startClient.x, y: options.startClient.y } : { x: event.clientX, y: event.clientY };
    this.activePointerId = event.pointerId;
    this.previewEl.addClass("is-selecting-strokes");
    try {
      this.canvas.setPointerCapture(event.pointerId);
    } catch (error) {
      void error;
    }
    event.preventDefault();
    event.stopPropagation();
  }
  updateSelectionDrag(event) {
    this.selectionCurrentPoint = this.eventToPoint(event);
    this.requestRender();
    event.preventDefault();
    event.stopPropagation();
  }
  finishSelectionDrag(event) {
    const point = this.eventToPoint(event);
    const movedDistance = this.pointerStartClient ? pointerDistance(this.pointerStartClient, { x: event.clientX, y: event.clientY }) : 0;
    if (movedDistance <= this.tapDistancePx() || !this.selectionStartPoint || !this.selectionCurrentPoint) {
      this.setSelectedStrokes(this.findStrokeAt(point, { x: event.clientX, y: event.clientY }));
    } else {
      this.setSelectedStrokes(this.findStrokesInSelection(this.selectionStartPoint, this.selectionCurrentPoint));
      this.selectedMarkdownBlockIds = new Set(this.findMarkdownBlocksInSelection(this.selectionStartPoint, this.selectionCurrentPoint));
      this.invalidateSelectionFrameSnapshot();
      this.expandSelectedGroups();
    }
    this.releasePointerCapture(event.pointerId);
    this.clearSelectionDragState();
    this.requestRender(true);
    event.preventDefault();
    event.stopPropagation();
  }
  cancelSelectionDrag(render = false) {
    if (this.activePointerId !== null) {
      this.releasePointerCapture(this.activePointerId);
    }
    this.clearSelectionDragState();
    if (render) {
      this.render();
    }
  }
  clearSelectionDragState() {
    this.selectingStrokes = false;
    this.selectionStartPoint = null;
    this.selectionCurrentPoint = null;
    this.pointerStartClient = null;
    this.activePointerId = null;
    this.previewEl.removeClass("is-selecting-strokes");
  }
  startSelectedStrokeDrag(event, point, hitIndex = -1, options = {}) {
    const indexes = this.getSelectedStrokeIndexes();
    const markdownBlocks = this.getSelectedMarkdownBlocks();
    if (!indexes.length && !markdownBlocks.length) {
      return;
    }
    const movableIndexes = indexes.filter((index) => {
      const stroke = this.drawingData.strokes[index];
      return this.canManipulateGroupedElement(stroke);
    });
    const movableMarkdownBlocks = markdownBlocks.filter((block) => this.canManipulateGroupedElement(block) && this.markdownBlockElement(block));
    this.dragMarkdownTextCommit = this.endTextEdit();
    this.restoreDraggedNoteFlowLivePreview();
    this.clearNoteFlowPeerAnimations();
    this.cancelDraggedNoteFlowStrokeAnimation(false);
    this.clearDraggedMarkdownDomMarkers();
    this.clearDraggedNoteFlowPlacement();
    this.cancelMarkdownBlockDropFrame();
    this.resetDragDropGeometry();
    this.dragSnapOtherBounds = null;
    this.pointerDown = false;
    this.currentStroke = null;
    if (!movableIndexes.length && !movableMarkdownBlocks.length) {
      this.startPendingSelectionTap(event, { type: "locked-selection" });
      return;
    }
    this.draggingStroke = true;
    this.dragStrokeStartPoint = options.startPoint || point;
    this.dragStrokeIndexes = movableIndexes;
    this.dragShouldSnap = this.shouldSnapStrokeIndexes(movableIndexes);
    this.dragMovedElementIds = this.connectorTargetIdsForStrokeIndexes(movableIndexes);
    this.dragPendingConnectorIds = null;
    this.dragConnectorById = new Map((this.drawingData?.strokes || []).map((stroke) => [strokeElementId(stroke), stroke]).filter(([id]) => id));
    this.dragConnectorCandidates = this.dragMovedElementIds.size ? (this.drawingData?.strokes || []).filter((stroke) => {
      const connector = normalizeConnector(stroke?.connector);
      return connector && (this.dragMovedElementIds.has(connector.fromId) || this.dragMovedElementIds.has(connector.toId));
    }) : [];
    this.dragLastPointerEvent = null;
    this.dragLastPointerKey = "";
    this.dragStrokeOriginalPoints = new Map(movableIndexes.map((index) => [
      index,
      this.drawingData.strokes[index].points.map((strokePoint) => ({ ...strokePoint }))
    ]));
    this.dragNoteFlowConnectorOriginalStates.clear();
    let pointMinX = 1;
    let pointMinY = 1;
    let pointMaxX = 0;
    let pointMaxY = 0;
    for (const points of this.dragStrokeOriginalPoints.values()) {
      for (const strokePoint of points) {
        pointMinX = Math.min(pointMinX, Number(strokePoint.x) || 0);
        pointMinY = Math.min(pointMinY, Number(strokePoint.y) || 0);
        pointMaxX = Math.max(pointMaxX, Number(strokePoint.x) || 0);
        pointMaxY = Math.max(pointMaxY, Number(strokePoint.y) || 0);
      }
    }
    this.dragStrokeOriginalPointBounds = this.dragStrokeOriginalPoints.size ? { minX: pointMinX, minY: pointMinY, maxX: pointMaxX, maxY: pointMaxY } : null;
    this.dragMarkdownOriginalElements = new Map(movableMarkdownBlocks.map((block) => {
      const element = this.markdownBlockElement(block);
      const flowElement = this.markdownBlockFlowElement(element) || element;
      const clientRect = this.markdownElementVisibleClientRect(element) || element.getBoundingClientRect();
      const sourceIdentity = markdownDragSourceIdentity(element);
      return [block.id, {
        block,
        element,
        dragElement: block.floating ? element : flowElement,
        sourceInfo: sourceIdentity.sourceInfo,
        sourceText: sourceIdentity.sourceText,
        embedDestination: sourceIdentity.embedDestination,
        span: block.span,
        noteFlowAutoSpan: Boolean(block.noteFlowAutoSpan),
        widthScale: normalizeMarkdownBlockWidthScale(block.widthScale),
        inlineWidthMode: normalizeMarkdownInlineWidthMode(block.inlineWidthMode, block.span, block.widthScale),
        floatBox: block.floatBox ? { ...block.floatBox } : null,
        floatingExplicit: Boolean(block.floatingExplicit),
        canvasBounds: this.markdownElementCanvasBounds(element, { forSelection: true }),
        clientRect: {
          left: clientRect.left,
          right: clientRect.right,
          top: clientRect.top,
          bottom: clientRect.bottom
        }
      }];
    }));
    const markedDragElements = /* @__PURE__ */ new Set();
    for (const state of this.dragMarkdownOriginalElements.values()) {
      const dragElement = state.dragElement || state.element;
      if (!dragElement?.parentNode || markedDragElements.has(dragElement)) {
        continue;
      }
      const marker = dragElement.ownerDocument.createComment("notedraw-note-flow-drag-origin");
      dragElement.parentNode.insertBefore(marker, dragElement);
      state.domMarker = marker;
      markedDragElements.add(dragElement);
    }
    this.dragMarkdownSourcePromise = this.dragMarkdownOriginalElements.size ? this.plugin.app.vault.read(this.file).catch(() => null) : null;
    this.dragMarkdownSourceIndexPromise = this.dragMarkdownSourcePromise?.then((source) => {
      return source ? createMarkdownSourceIndex(source) : null;
    }) || null;
    for (const state of this.dragMarkdownOriginalElements.values()) {
      state.lockedTargetPromise = this.dragMarkdownSourcePromise && this.dragMarkdownSourceIndexPromise ? Promise.all([this.dragMarkdownSourcePromise, this.dragMarkdownSourceIndexPromise]).then(([source, sourceIndex]) => {
        return source ? resolveSourceDropTarget(source, state.sourceInfo, state.sourceText, sourceIndex, state.embedDestination) : null;
      }) : Promise.resolve(null);
    }
    const markdownClientRects = Array.from(this.dragMarkdownOriginalElements.values()).map((state) => state.clientRect);
    this.dragMarkdownOriginalClientBounds = markdownClientRects.length ? {
      left: Math.min(...markdownClientRects.map((rect) => rect.left)),
      right: Math.max(...markdownClientRects.map((rect) => rect.right)),
      top: Math.min(...markdownClientRects.map((rect) => rect.top)),
      bottom: Math.max(...markdownClientRects.map((rect) => rect.bottom))
    } : null;
    this.dragMarkdownClientDeltaX = 0;
    this.dragMarkdownClientDeltaY = 0;
    const lane = this.layoutMeasureEl?.isConnected ? this.layoutMeasureEl : findLayoutMeasureElement(this.previewEl) || this.previewEl;
    const laneRect = lane?.getBoundingClientRect?.();
    this.dragContentLaneRect = laneRect?.width > 1 ? {
      left: laneRect.left,
      right: laneRect.right,
      top: laneRect.top,
      bottom: laneRect.bottom
    } : null;
    this.dragNoteFlowOriginalBounds = new Map(movableIndexes.flatMap((index) => {
      const stroke = this.drawingData.strokes[index];
      if (!normalizeNoteFlow(stroke?.noteFlow) || isConnectorStroke(stroke)) {
        return [];
      }
      const bounds = getStrokeBounds(stroke, this.canvasWidth(), this.canvasHeight());
      return bounds ? [[index, bounds]] : [];
    }));
    this.dragStrokeOriginalNoteFlows = new Map(movableIndexes.map((index) => {
      const noteFlow = normalizeNoteFlow(this.drawingData.strokes[index]?.noteFlow);
      return [index, noteFlow ? { ...noteFlow } : null];
    }));
    this.pendingDraggedNoteFlowIndexes.clear();
    this.dragNoteFlowLivePreviewOriginals.clear();
    this.dragNoteFlowRawPreviewPoints = null;
    this.dragNoteFlowRawPreviewNoteFlows = null;
    this.dragNoteFlowPreviewElementIds.clear();
    this.dragNoteFlowPlacementClientDelta = null;
    this.dragNoteFlowDomPreview = null;
    this.dragDrawingHistoryBefore = this.captureDrawingHistorySnapshot();
    this.dragStrokeOriginalBounds = this.getStrokeIndexesNormalizedBounds(movableIndexes);
    this.dragElementGroupBounds = new Map(this.elementGroupRecords().filter((group) => group.boxed || group.locked).map((group) => [group.id, this.getElementGroupBounds(group.id)]));
    this.dragHasBoxBackground = Array.from(this.dragElementGroupBounds.keys()).some((id) => Boolean(this.elementGroup(id)?.backgroundColor));
    this.dragMarkdownLastValidDrop = null;
    this.dragStrokeMoved = false;
    this.dragStrokeHitIndex = hitIndex;
    this.dragStrokePreserveSelection = Boolean(options.preserveSelection);
    this.pointerStartClient = options.startClient ? { x: options.startClient.x, y: options.startClient.y } : { x: event.clientX, y: event.clientY };
    this.dragStrokePointerGeometry = this.captureCanvasPointerGeometry();
    this.activePointerId = event.pointerId;
    this.previewEl.addClass("is-moving-selection");
    for (const state of this.dragMarkdownOriginalElements.values()) {
      state.dragElement?.addClass?.("is-notedraw-md-dragging");
    }
    try {
      this.canvas.setPointerCapture(event.pointerId);
    } catch (error) {
      void error;
    }
    this.startSelectionLongPress(event);
    event.preventDefault();
    event.stopPropagation();
  }
  connectorTargetIdsForStrokeIndexes(indexes) {
    const ids = /* @__PURE__ */ new Set();
    for (const index of indexes || []) {
      const stroke = this.drawingData?.strokes?.[index];
      const elementId = strokeElementId(stroke);
      if (elementId) {
        ids.add(elementId);
      }
      const connector = normalizeConnector(stroke?.connector);
      if (connector?.fromId) {
        ids.add(connector.fromId);
      }
      if (connector?.toId) {
        ids.add(connector.toId);
      }
    }
    return ids;
  }
  clearMarkdownBlockDropTarget() {
    this.dragMarkdownDropTarget?.removeClass?.(
      "notedraw-text-sort-target-before",
      "notedraw-text-sort-target-after",
      "notedraw-text-sort-target-left",
      "notedraw-text-sort-target-right"
    );
    this.dragMarkdownDropTarget = null;
    this.dragMarkdownDropSide = null;
  }
  cancelMarkdownBlockDropFrame() {
    if (this.dragMarkdownDropFrameId !== null) {
      window.cancelAnimationFrame(this.dragMarkdownDropFrameId);
      this.dragMarkdownDropFrameId = null;
    }
    this.dragMarkdownDropClientX = null;
    this.dragMarkdownDropClientY = null;
  }
  queueMarkdownBlockDropTarget(clientX, clientY) {
    if (!this.dragMarkdownOriginalElements?.size || !Number.isFinite(Number(clientX)) || !Number.isFinite(Number(clientY))) {
      return;
    }
    this.dragMarkdownDropClientX = Number(clientX);
    this.dragMarkdownDropClientY = Number(clientY);
    if (this.dragMarkdownDropFrameId !== null) {
      return;
    }
    this.dragMarkdownDropFrameId = window.requestAnimationFrame(() => {
      this.dragMarkdownDropFrameId = null;
      const pendingX = this.dragMarkdownDropClientX;
      const pendingY = this.dragMarkdownDropClientY;
      this.dragMarkdownDropClientX = null;
      this.dragMarkdownDropClientY = null;
      const previousKey = this.dragMarkdownDomPreviewKey;
      const drop = this.updateMarkdownBlockDropTarget(pendingX, pendingY);
      const nextKey = drop?.element && drop?.side ? `${drop.element.dataset?.noteDrawMarkdownBlockId || ""}:${drop.side}` : "";
      if (drop && nextKey !== previousKey) {
        if (this.dragNoteFlowDomPreview) {
          this.restoreDraggedNoteFlowDomPreview();
        }
        this.applyDraggedMarkdownDomPreview(drop);
        this.dragMarkdownDomPreviewKey = nextKey;
      }
    });
  }
  flushMarkdownBlockDropTarget(clientX, clientY) {
    this.cancelMarkdownBlockDropFrame();
    if (this.dragMarkdownOriginalElements?.size && Number.isFinite(Number(clientX)) && Number.isFinite(Number(clientY))) {
      return this.updateMarkdownBlockDropTarget(clientX, clientY);
    }
    return null;
  }
  dragDropScrollKey() {
    const scroller = this.scrollContainer;
    const viewport = window.visualViewport;
    return [
      Number(scroller?.scrollLeft) || 0,
      Number(scroller?.scrollTop) || 0,
      Number(window.scrollX) || 0,
      Number(window.scrollY) || 0,
      Number(viewport?.offsetLeft) || 0,
      Number(viewport?.offsetTop) || 0
    ].join(":");
  }
  resetDragDropGeometry() {
    this.dragDropGeometry = null;
  }
  draggedSelectionClientLeft() {
    const values = [];
    if (this.dragMarkdownOriginalClientBounds) {
      values.push(this.dragMarkdownOriginalClientBounds.left + (Number(this.dragMarkdownClientDeltaX) || 0));
    }
    const indexes = this.draggedNoteFlowIndexes(this.dragStrokeIndexes || []);
    const bounds = this.getStrokeIndexesBounds(indexes);
    const canvasRect = this.dragStrokePointerGeometry?.rect;
    if (bounds && canvasRect?.width > 0) {
      const scaleX = canvasRect.width / Math.max(1, this.canvasWidth());
      values.push(canvasRect.left + bounds.minX * scaleX);
    }
    return values.length ? Math.min(...values) : null;
  }
  draggedNoteFlowMarkdownStates() {
    return Array.from(this.dragMarkdownOriginalElements?.values?.() || []).filter((state) => !state?.block?.floating);
  }
  draggedNoteFlowClientBounds() {
    const rects = this.draggedNoteFlowMarkdownStates().map((state) => ({
      left: state.clientRect.left + (Number(this.dragMarkdownClientDeltaX) || 0),
      right: state.clientRect.right + (Number(this.dragMarkdownClientDeltaX) || 0),
      top: state.clientRect.top + (Number(this.dragMarkdownClientDeltaY) || 0),
      bottom: state.clientRect.bottom + (Number(this.dragMarkdownClientDeltaY) || 0)
    }));
    const strokeBounds = this.getStrokeIndexesBounds(this.draggedNoteFlowIndexes());
    const canvasRect = this.noteFlowCanvasRect();
    if (strokeBounds && canvasRect?.width > 1 && canvasRect?.height > 1) {
      const scaleX = canvasRect.width / Math.max(1, this.canvasWidth());
      const scaleY = canvasRect.height / Math.max(1, this.canvasRenderHeight);
      rects.push({
        left: canvasRect.left + strokeBounds.minX * scaleX,
        right: canvasRect.left + strokeBounds.maxX * scaleX,
        top: canvasRect.top + (strokeBounds.minY - this.canvasWindowTop) * scaleY,
        bottom: canvasRect.top + (strokeBounds.maxY - this.canvasWindowTop) * scaleY
      });
    }
    if (!rects.length) {
      return null;
    }
    return {
      left: Math.min(...rects.map((rect) => rect.left)),
      right: Math.max(...rects.map((rect) => rect.right)),
      top: Math.min(...rects.map((rect) => rect.top)),
      bottom: Math.max(...rects.map((rect) => rect.bottom))
    };
  }
  dragDropGeometrySnapshot() {
    const scrollKey = this.dragDropScrollKey();
    const current = this.dragDropGeometry;
    if (current && current.scrollKey === scrollKey && (this.draggingStroke || current.layoutGeneration === this.layoutRefreshGeneration)) {
      return current;
    }
    const lane = this.layoutMeasureEl?.isConnected ? this.layoutMeasureEl : findLayoutMeasureElement(this.previewEl) || this.previewEl;
    const laneBounds = lane?.getBoundingClientRect?.();
    const laneRect = laneBounds && laneBounds.width > 1 && laneBounds.height > 0 ? { left: laneBounds.left, right: laneBounds.right, top: laneBounds.top, bottom: laneBounds.bottom, width: laneBounds.width, height: laneBounds.height } : null;
    const movingElements = new Set(Array.from(this.dragMarkdownOriginalElements?.values?.() || []).map((state) => state.element));
    const markdownCandidates = Array.from(/* @__PURE__ */ new Set([
      ...this.markdownBlockElements.values(),
      ...Array.from(this.previewEl.querySelectorAll?.(EDITABLE_SELECTOR) || [])
    ])).filter((element) => {
      if (!element?.isConnected || movingElements.has(element) || !isConcreteMarkdownBlockElement(element)) {
        return false;
      }
      if (Array.from(movingElements).some((moving) => moving.contains?.(element) || element.contains?.(moving))) {
        return false;
      }
      const file2 = this.plugin.resolveEditableFile(element, this.file);
      return normalizeVaultPath(file2?.path) === normalizeVaultPath(this.file?.path);
    }).map((element) => {
      const bounds = this.markdownElementVisibleClientRect(element);
      return bounds && bounds.width > 1 && bounds.height > 1 ? { element, rect: { left: bounds.left, right: bounds.right, top: bounds.top, bottom: bounds.bottom, width: bounds.width, height: bounds.height } } : null;
    }).filter(Boolean);
    const noteFlowCandidates = this.noteFlowCandidates().filter((candidate) => {
      const candidateElements = [
        candidate?.element,
        candidate?.sourceElement,
        candidate?.inlineElement
      ].filter(Boolean);
      return !candidateElements.some((candidateElement) => movingElements.has(candidateElement) || Array.from(movingElements).some((moving) => moving.contains?.(candidateElement) || candidateElement.contains?.(moving)));
    });
    const noteFlowCandidateByMarkdownId = /* @__PURE__ */ new Map();
    for (const candidate of noteFlowCandidates) {
      const rawTarget = candidate.inlineElement || candidate.sourceElement || candidate.element;
      const target = this.canonicalMarkdownFlowTarget(rawTarget);
      const blockId = target?.dataset?.noteDrawMarkdownBlockId || this.findMarkdownBlockRecordForElement(target)?.id;
      if (!blockId) {
        continue;
      }
      const rect = this.noteFlowCandidateRect(candidate, "inline");
      const area = rect ? Math.max(1, rect.right - rect.left) * Math.max(1, rect.bottom - rect.top) : Number.POSITIVE_INFINITY;
      const current2 = noteFlowCandidateByMarkdownId.get(blockId);
      if (!current2 || area < current2.area) {
        noteFlowCandidateByMarkdownId.set(blockId, { candidate, area });
      }
    }
    const markdownCandidateByElement = new Map(markdownCandidates.map((entry) => [entry.element, entry]));
    const markdownBlockRecords = new Map(this.markdownBlockRecords().map((item) => [item.id, item]));
    const snapshot = {
      scrollKey,
      layoutGeneration: this.layoutRefreshGeneration,
      laneRect,
      movingElements,
      markdownCandidates,
      markdownCandidateByElement,
      markdownBlockRecords,
      noteFlowCandidates,
      noteFlowCandidateByMarkdownId: new Map(Array.from(noteFlowCandidateByMarkdownId, ([id, value]) => [id, value.candidate])),
      noteFlowDropIndex: createNoteFlowDropIndex(noteFlowCandidates),
      rowMetrics: /* @__PURE__ */ new Map()
    };
    this.dragDropGeometry = snapshot;
    return snapshot;
  }
  markdownEdgeDropTarget(clientX, clientY, movingElements = /* @__PURE__ */ new Set()) {
    const geometry = this.dragDropGeometrySnapshot();
    const laneRect = geometry?.laneRect;
    if (!laneRect || laneRect.width <= 1) {
      return null;
    }
    let nearest = null;
    let nearestDistance = Number.POSITIVE_INFINITY;
    let nearestCenterDistance = Number.POSITIVE_INFINITY;
    for (const candidate of geometry.markdownCandidates) {
      if (movingElements.has(candidate.element)) {
        continue;
      }
      const rect = candidate.rect;
      const distance = clientY < rect.top ? rect.top - clientY : clientY > rect.bottom ? clientY - rect.bottom : 0;
      const centerDistance = Math.abs(clientY - (rect.top + rect.bottom) / 2);
      if (distance < nearestDistance || distance === nearestDistance && centerDistance < nearestCenterDistance) {
        nearest = candidate;
        nearestDistance = distance;
        nearestCenterDistance = centerDistance;
      }
    }
    if (!nearest) {
      return null;
    }
    const maxDistance = Math.max(80, nearest.rect.height * 1.5);
    const verticalDistance = clientY < nearest.rect.top ? nearest.rect.top - clientY : clientY > nearest.rect.bottom ? clientY - nearest.rect.bottom : 0;
    const row = this.markdownDropRowMetrics(nearest.element, movingElements);
    const horizontalRoom = row.canFit;
    const intent = resolveDragDropHorizontalIntent({
      clientX,
      targetLeft: nearest.rect.left,
      targetRight: nearest.rect.right,
      laneLeft: laneRect.left,
      laneRight: laneRect.right,
      draggedLeft: this.draggedSelectionClientLeft(),
      horizontalRoom,
      requireRightIntent: true
    });
    return verticalDistance <= maxDistance && intent !== "vertical" ? { element: nearest.element, intent } : null;
  }
  markdownVerticalDropTarget(clientX, clientY, movingElements = /* @__PURE__ */ new Set()) {
    const geometry = this.dragDropGeometrySnapshot();
    const candidates = (geometry?.markdownCandidates || []).filter((candidate) => !movingElements.has(candidate.element));
    return resolveVerticalMarkdownDropTarget({
      clientX,
      clientY,
      laneRect: geometry?.laneRect,
      candidates
    });
  }
  captureMarkdownBlockDropTarget(target, side, row = null) {
    if (!target || !side) {
      return null;
    }
    const targetBlock = this.findMarkdownBlockRecordForElement(target);
    const sourceIdentity = markdownDragSourceIdentity(target);
    const rawInfo = sourceIdentity.sourceInfo;
    const targetInfo = {
      ...rawInfo,
      lineStart: rawInfo.lineStart ?? targetBlock?.lineStart ?? null,
      lineEnd: rawInfo.lineEnd ?? targetBlock?.lineEnd ?? rawInfo.lineStart ?? targetBlock?.lineStart ?? null
    };
    const targetText = sourceIdentity.sourceText;
    const targetFile = this.file;
    const path = normalizeVaultPath(targetFile?.path || "");
    const sourcePromise = path === normalizeVaultPath(this.file?.path) && this.dragMarkdownSourcePromise ? this.dragMarkdownSourcePromise : path ? this.plugin.app.vault.read(targetFile).catch(() => null) : Promise.resolve(null);
    const sourceIndexPromise = path === normalizeVaultPath(this.file?.path) && this.dragMarkdownSourceIndexPromise ? this.dragMarkdownSourceIndexPromise : sourcePromise.then((source) => source ? createMarkdownSourceIndex(source) : null);
    const lockedTargetPromise = path && targetText ? Promise.all([sourcePromise, sourceIndexPromise]).then(([source, sourceIndex]) => {
      return source ? resolveSourceDropTarget(source, targetInfo, targetText, sourceIndex, sourceIdentity.embedDestination) : null;
    }) : Promise.resolve(null);
    return {
      element: target,
      side,
      path,
      blockId: targetBlock?.id || target.dataset?.noteDrawMarkdownBlockId || "",
      targetInfo,
      targetText,
      embedDestination: sourceIdentity.embedDestination,
      row: row ? { ...row } : null,
      lockedTargetPromise
    };
  }
  updateMarkdownBlockDropTarget(clientX, clientY) {
    const geometry = this.dragDropGeometrySnapshot();
    const movingElements = geometry?.movingElements || new Set(Array.from(this.dragMarkdownOriginalElements?.values?.() || []).map((state) => state.element));
    let targetEntry = null;
    let targetArea = Number.POSITIVE_INFINITY;
    for (const candidate of geometry?.markdownCandidates || []) {
      const rect2 = candidate.rect;
      if (movingElements.has(candidate.element) || clientX < rect2.left || clientX > rect2.right || clientY < rect2.top || clientY > rect2.bottom) {
        continue;
      }
      const area = rect2.width * rect2.height;
      if (area < targetArea) {
        targetEntry = candidate;
        targetArea = area;
      }
    }
    let target = targetEntry?.element || null;
    let forcedIntent = null;
    if (!target) {
      const edgeTarget = this.markdownEdgeDropTarget(clientX, clientY, movingElements);
      target = edgeTarget?.element || null;
      forcedIntent = edgeTarget?.intent || null;
      targetEntry = geometry?.markdownCandidateByElement?.get(target) || null;
    }
    let verticalSide = null;
    if (!target) {
      const verticalTarget = this.markdownVerticalDropTarget(clientX, clientY, movingElements);
      target = verticalTarget?.element || null;
      verticalSide = verticalTarget?.side || null;
      targetEntry = geometry?.markdownCandidateByElement?.get(target) || null;
    }
    if (!target) {
      const movingList = Array.from(movingElements);
      const stack = activeDocument.elementsFromPoint?.(clientX, clientY) || [activeDocument.elementFromPoint?.(clientX, clientY)];
      for (const candidate of stack) {
        const editable = findEditableTarget(candidate, this.previewEl, { x: clientX, y: clientY });
        if (!editable || movingElements.has(editable) || movingList.some((element) => element.contains?.(editable) || editable.contains?.(element))) {
          continue;
        }
        const targetFile = this.plugin.resolveEditableFile(editable, this.file);
        if (normalizeVaultPath(targetFile?.path) === normalizeVaultPath(this.file?.path)) {
          target = editable;
          break;
        }
      }
    }
    if (!target) {
      return this.dragMarkdownLastValidDrop ? { ...this.dragMarkdownLastValidDrop } : null;
    }
    const rect = targetEntry?.rect || this.markdownElementVisibleClientRect(target);
    if (!rect) {
      return this.dragMarkdownLastValidDrop ? { ...this.dragMarkdownLastValidDrop } : null;
    }
    const row = this.markdownDropRowMetrics(target, movingElements);
    const horizontalRoom = row.canFit;
    const intent = forcedIntent || resolveDragDropHorizontalIntent({
      clientX,
      targetLeft: rect.left,
      targetRight: rect.right,
      laneLeft: geometry?.laneRect?.left ?? rect.left,
      laneRight: geometry?.laneRect?.right ?? rect.right,
      draggedLeft: this.draggedSelectionClientLeft(),
      horizontalRoom,
      requireRightIntent: true
    });
    let side = verticalSide || (clientY >= rect.top + rect.height / 2 ? "after" : "before");
    if (horizontalRoom && intent === "inline-right") {
      side = "right";
    } else if (horizontalRoom && intent === "line-start") {
      side = "left";
    }
    if ((this.dragMarkdownDropSide === "right" || this.dragMarkdownDropSide === "left") && this.dragMarkdownDropTarget === target) {
      if (this.dragMarkdownDropSide === "right" && clientX >= rect.left + rect.width * 0.3) {
        side = "right";
      } else if (this.dragMarkdownDropSide === "left" && clientX <= rect.left + rect.width * 0.7) {
        side = "left";
      }
    }
    if (target !== this.dragMarkdownDropTarget || side !== this.dragMarkdownDropSide) {
      this.clearMarkdownBlockDropTarget();
      this.dragMarkdownDropTarget = target;
      this.dragMarkdownDropSide = side;
    }
    const previous = this.dragMarkdownLastValidDrop;
    const drop = previous?.element === target && previous?.side === side ? previous : this.captureMarkdownBlockDropTarget(target, side, row);
    this.dragMarkdownLastValidDrop = drop;
    return drop || null;
  }
  markdownDropRowMetrics(target, movingElements = /* @__PURE__ */ new Set()) {
    const geometry = this.draggingStroke ? this.dragDropGeometry : null;
    const cached = geometry?.rowMetrics?.get(target);
    if (cached) {
      return cached;
    }
    const movingMarkdownCount = movingElements.size || this.dragMarkdownOriginalElements?.size || 0;
    const targetRect = geometry?.markdownCandidateByElement?.get(target)?.rect || target?.getBoundingClientRect?.();
    const parent = this.markdownBlockLayoutLane(target);
    const inlineLaneWidth = Number(parent?.clientWidth) || Number(geometry?.laneRect?.right) - Number(geometry?.laneRect?.left) || Number(this.dragContentLaneRect?.width) || Number(this.layoutMeasureEl?.clientWidth) || Number(this.previewEl?.clientWidth) || Number(targetRect?.width) || 0;
    const memberEntries = [];
    const targetBlock = this.findMarkdownBlockRecordForElement(target);
    const targetId = targetBlock?.id || target?.dataset?.noteDrawMarkdownBlockId || "__notedraw-inline-target__";
    if (parent && targetRect?.height > 0) {
      const blockRecords = geometry?.markdownBlockRecords || new Map(this.markdownBlockRecords().map((item) => [item.id, item]));
      for (const [id, element] of this.markdownBlockElements.entries()) {
        if (!element?.isConnected || element === target || movingElements.has(element) || this.markdownBlockLayoutLane(element) !== parent) {
          continue;
        }
        const rect = geometry?.markdownCandidateByElement?.get(element)?.rect || element.getBoundingClientRect();
        if (Math.abs(rect.top - targetRect.top) > Math.max(3, Math.min(rect.height, targetRect.height) * 0.2)) {
          continue;
        }
        const block = blockRecords.get(id);
        if (!block || block.floating) {
          continue;
        }
        const liveSpan = element.classList?.contains("notedraw-md-inline-grid-item") ? Number.parseInt(element.style?.getPropertyValue?.("--notedraw-md-inline-span"), 10) : Number.NaN;
        memberEntries.push({
          id,
          rect,
          span: Number.isFinite(liveSpan) ? liveSpan : this.markdownInlinePreferredSpan(block, element, inlineLaneWidth)
        });
      }
    }
    const targetLiveSpan = target?.classList?.contains("notedraw-md-inline-grid-item") ? Number.parseInt(target.style?.getPropertyValue?.("--notedraw-md-inline-span"), 10) : Number.NaN;
    memberEntries.push({
      id: targetId,
      rect: targetRect,
      span: Number.isFinite(targetLiveSpan) ? targetLiveSpan : this.markdownInlinePreferredSpan(targetBlock, target, inlineLaneWidth)
    });
    memberEntries.sort((left, right) => Number(left.rect?.left) - Number(right.rect?.left) || Number(left.rect?.top) - Number(right.rect?.top) || left.id.localeCompare(right.id));
    const orderedMemberIds = Array.from(new Set(memberEntries.map((entry) => entry.id)));
    const memberIds = orderedMemberIds.filter((id) => id !== targetId);
    const movingMarkdownIds = Array.from(this.dragMarkdownOriginalElements?.values?.() || []).filter((state) => state?.block?.id && (!movingElements.size || movingElements.has(state.element))).map((state) => state.block.id);
    const movingStrokeCount = this.draggedNoteFlowIndexes().length ? 1 : 0;
    const movingItemCount = Math.max(1, movingMarkdownCount + movingStrokeCount);
    const totalCount = orderedMemberIds.length + movingItemCount;
    const laneWidth = geometry?.laneRect ? Number(geometry.laneRect.right) - Number(geometry.laneRect.left) : 0;
    const measuredLaneWidth = this.dragContentLaneRect ? Number(this.dragContentLaneRect.right) - Number(this.dragContentLaneRect.left) : Number(this.layoutMeasureEl?.clientWidth) || Number(this.previewEl?.clientWidth) || 0;
    const availableWidth = Number(parent?.clientWidth) || laneWidth || measuredLaneWidth || Number(targetRect?.width) || 0;
    const requiredWidth = totalCount * MIN_INLINE_NOTE_FLOW_ITEM_WIDTH_PX + Math.max(0, totalCount - 1) * INLINE_NOTE_FLOW_GAP_PX;
    const spans = distributeInlineRowSpans(totalCount);
    const canFit = spans.length === totalCount;
    const result = {
      canFit,
      span: canFit ? spans[0] : 12,
      spans,
      totalCount,
      availableWidth,
      requiredWidth,
      memberIds,
      orderedMemberIds,
      targetId,
      movingItemCount,
      movingMarkdownIds,
      movingStrokeCount,
      preferredSpanById: new Map(memberEntries.map((entry) => [
        entry.id,
        clamp10(Math.round(Number(entry.span) || 12), 1, 12)
      ]))
    };
    geometry?.rowMetrics?.set(target, result);
    return result;
  }
  markdownInlinePreferredSpan(block, element, laneWidth) {
    const normalizedMode = normalizeMarkdownInlineWidthMode(
      block?.inlineWidthMode,
      block?.span,
      block?.widthScale
    );
    const currentSpan = clamp10(
      Math.round(Number(block?.span) || 12),
      1,
      12
    );
    if (normalizedMode === "fixed") {
      return currentSpan;
    }
    const availableWidth = Math.max(1, Number(laneWidth) || 0);
    const owner = this.markdownBlockFlowElement(element) || element;
    const visibleRect = this.markdownElementVisibleClientRect(owner) || owner?.getBoundingClientRect?.();
    const textRect = this.markdownElementTextClientRect(owner, visibleRect);
    const mediaRect = owner?.querySelector?.(
      "img,video,audio,iframe,.internal-embed,.markdown-embed,.markdown-embed-content,figure"
    )?.getBoundingClientRect?.();
    const checkboxRect = this.markdownTaskCheckboxRect(owner, visibleRect);
    const contentWidth = Math.max(
      Number(textRect?.width) || 0,
      Number(mediaRect?.width) || 0,
      checkboxRect ? Math.max(0, checkboxRect.right - (visibleRect?.left || checkboxRect.left)) : 0
    );
    if (!(contentWidth > 0)) {
      return currentSpan;
    }
    const gap = owner?.matches?.("li") ? INLINE_NOTE_FLOW_LIST_GAP_PX : INLINE_NOTE_FLOW_GAP_PX;
    const paddedWidth = Math.min(availableWidth, contentWidth + gap + 12);
    return clamp10(Math.ceil(paddedWidth / availableWidth * 12), 1, 12);
  }
  markdownInlineRowAllocation(row, targetBlock, moving = [], side = "right") {
    const movingIds = moving.map((state) => state?.block?.id || state?.id).filter(Boolean);
    const knownMovingCount = Math.max(
      movingIds.length,
      Number(row?.movingItemCount) || 0,
      1
    );
    const preferredSpanById = new Map(row?.preferredSpanById instanceof Map ? row.preferredSpanById : []);
    if (targetBlock?.id && !preferredSpanById.has(targetBlock.id)) {
      preferredSpanById.set(
        targetBlock.id,
        this.markdownInlinePreferredSpan(targetBlock, this.markdownBlockElement(targetBlock), row?.availableWidth)
      );
    }
    for (const state of moving) {
      const id = state?.block?.id || state?.id;
      if (id) {
        preferredSpanById.set(
          id,
          this.markdownInlinePreferredSpan(
            state?.block,
            state?.element,
            row?.availableWidth
          )
        );
      }
    }
    for (let index = 0; index < Math.max(0, knownMovingCount - movingIds.length); index += 1) {
      preferredSpanById.set(`__notedraw-inline-extra-${index}__`, 12);
    }
    return allocateInlineRow({
      existingIds: row?.orderedMemberIds || [
        ...row?.memberIds || [],
        targetBlock?.id || row?.targetId || "__notedraw-inline-target__"
      ],
      movingIds,
      targetId: targetBlock?.id || row?.targetId || "__notedraw-inline-target__",
      side,
      extraMovingCount: Math.max(0, knownMovingCount - movingIds.length),
      preferredSpanById
    });
  }
  markdownInlineRowSpan(allocation, id, fallback = 12) {
    return allocation?.spanById?.get?.(id) || clamp10(Math.round(Number(fallback) || 12), 1, 12);
  }
  markdownDropInlineAllocation(drop, targetBlock, moving = []) {
    const row = drop?.row;
    const key = [
      drop?.side || "right",
      targetBlock?.id || row?.targetId || "",
      ...row?.orderedMemberIds || [],
      "moving",
      ...moving.map((state) => state?.block?.id || state?.id || "").filter(Boolean)
    ].join("\0");
    if (drop?.inlineAllocationKey === key && drop.inlineAllocation) {
      return drop.inlineAllocation;
    }
    const allocation = this.markdownInlineRowAllocation(row, targetBlock, moving, drop?.side);
    if (drop) {
      drop.inlineAllocationKey = key;
      drop.inlineAllocation = allocation;
    }
    return allocation;
  }
  async commitDraggedMarkdownBlocks(drop, drawingHistoryBefore) {
    const moving = Array.isArray(drop?.moving) ? drop.moving.filter((state) => state?.element && state?.block) : [];
    const target = drop?.element;
    const textCommitted = await Promise.resolve(drop?.textCommit).catch(() => false);
    if (!moving.length || !target || moving.some((state) => state.element === target)) {
      this.recordDrawingHistory(drawingHistoryBefore);
      return false;
    }
    if (textCommitted === false) {
      this.recordDrawingHistory(drawingHistoryBefore);
      return false;
    }
    const file2 = this.file;
    if (normalizeVaultPath(drop?.path) !== normalizeVaultPath(file2?.path)) {
      this.recordDrawingHistory(drawingHistoryBefore);
      return false;
    }
    const lockedTarget = await Promise.resolve(drop?.lockedTargetPromise).catch(() => null);
    if (!lockedTarget) {
      this.recordDrawingHistory(drawingHistoryBefore);
      return false;
    }
    const lockedMovingTargets = await Promise.all(moving.map((state) => {
      return Promise.resolve(state.lockedTargetPromise).catch(() => null);
    }));
    if (lockedMovingTargets.some((target2) => !target2)) {
      this.recordDrawingHistory(drawingHistoryBefore);
      return false;
    }
    const targetState = {
      lockedTarget,
      lockedMovingTargets,
      strictMoving: true,
      strictTarget: true,
      targetText: drop.targetText
    };
    const targetBlock = this.markdownBlockRecords().find((block) => block.id === drop.blockId) || this.ensureMarkdownBlockRecord(target);
    const row = drop.row || this.markdownDropRowMetrics(target, new Set(moving.map((state) => state.element)));
    const rowBlocks = new Map((row.memberIds || []).map((id) => {
      const block = this.markdownBlockRecords().find((item) => item.id === id);
      return block ? [id, block] : null;
    }).filter(Boolean));
    if (targetBlock) {
      rowBlocks.set(targetBlock.id, targetBlock);
    }
    const originalRowBlocks = new Map(Array.from(rowBlocks, ([id, block]) => [id, {
      span: block.span || 12,
      noteFlowAutoSpan: Boolean(block.noteFlowAutoSpan),
      widthScale: normalizeMarkdownBlockWidthScale(block.widthScale),
      inlineWidthMode: normalizeMarkdownInlineWidthMode(block.inlineWidthMode, block.span, block.widthScale)
    }]));
    const originalTargetLineStart = targetBlock?.lineStart;
    const originalTargetLineEnd = targetBlock?.lineEnd;
    if (targetBlock && Number.isFinite(lockedTarget.line)) {
      targetBlock.lineStart = lockedTarget.line;
      targetBlock.lineEnd = lockedTarget.endLine ?? lockedTarget.line;
    }
    const requestedHorizontal = drop.side === "left" || drop.side === "right";
    const allocation = this.markdownDropInlineAllocation(drop, targetBlock, moving);
    const horizontal = requestedHorizontal && allocation.canFit;
    const effectiveSide = horizontal ? drop.side : drop.side === "right" ? "after" : drop.side === "left" ? "before" : drop.side;
    if (horizontal && targetBlock) {
      for (const [id, block] of rowBlocks) {
        const nextSpan = this.markdownInlineRowSpan(allocation, id, block.span);
        if (Number(block.span) !== Number(nextSpan)) {
          block.widthScale = 1;
        }
        block.span = nextSpan;
        block.noteFlowAutoSpan = false;
        block.inlineWidthMode = "fixed";
      }
      for (const state of moving) {
        const nextSpan = this.markdownInlineRowSpan(allocation, state.block.id, state.block.span);
        if (Number(state.block.span) !== Number(nextSpan)) {
          state.block.widthScale = 1;
        }
        state.block.span = nextSpan;
        state.block.noteFlowAutoSpan = false;
        state.block.inlineWidthMode = "fixed";
        state.block.floating = false;
        state.block.floatingExplicit = false;
        state.block.floatBox = null;
      }
    } else {
      for (const state of moving) {
        state.block.span = 12;
        state.block.widthScale = 1;
        state.block.noteFlowAutoSpan = false;
        state.block.inlineWidthMode = "fixed";
        state.block.floating = false;
        state.block.floatingExplicit = false;
        state.block.floatBox = null;
      }
    }
    const result = await this.plugin.reorderTextBlocks(
      file2,
      moving,
      target,
      effectiveSide === "after" || effectiveSide === "right",
      targetState
    );
    if (!result) {
      if (targetBlock) {
        targetBlock.lineStart = originalTargetLineStart;
        targetBlock.lineEnd = originalTargetLineEnd;
      }
      for (const [id, original] of originalRowBlocks) {
        const block = rowBlocks.get(id);
        if (!block) {
          continue;
        }
        block.span = original.span;
        block.noteFlowAutoSpan = original.noteFlowAutoSpan;
        block.widthScale = original.widthScale;
        block.inlineWidthMode = original.inlineWidthMode;
      }
      for (const state of moving) {
        state.block.span = state.span;
        state.block.noteFlowAutoSpan = state.noteFlowAutoSpan;
        state.block.widthScale = state.widthScale;
        state.block.inlineWidthMode = state.inlineWidthMode;
        state.block.floating = Boolean(state.floatBox);
        state.block.floatingExplicit = Boolean(state.floatingExplicit);
        state.block.floatBox = state.floatBox ? { ...state.floatBox } : null;
      }
      this.recordDrawingHistory(drawingHistoryBefore);
      this.render();
      return false;
    }
    this.redoStack = [];
    this.plugin.scheduleDrawingSave(file2, this.drawingData, { userOperation: true });
    this.recordCompoundHistory(file2, result.before, result.after, drawingHistoryBefore);
    if (this.selectionFrameAwaitingMarkdownSync) {
      this.selectionFrameAwaitingMarkdownSync.committed = true;
    }
    const hasNoteFlow = this.hasNoteFlowElements();
    if (hasNoteFlow) {
      this.scheduleNoteFlowLayout({ operation: true, defer: true });
    }
    this.scheduleMarkdownAnnotationRefresh({ layout: hasNoteFlow, delay: 48, force: true });
    this.scheduleResize({ layout: false, measure: true });
    this.requestRender(this.selectionHasDomStrokes() ? "interaction" : false);
    return {
      changed: result.changed !== false,
      before: result.before,
      after: result.after
    };
  }
  updateDraggedElementGroupMembership(event, strokeIndexes, markdownBlocks) {
    const point = this.pointToCanvas(this.eventToPoint(event));
    const selectedGroupIds = new Set([
      ...strokeIndexes.map((index) => this.drawingData.strokes[index]?.groupId),
      ...markdownBlocks.map((block) => block?.groupId)
    ].filter(Boolean));
    const wholeGroups = new Set(Array.from(selectedGroupIds).filter((id) => this.isElementGroupFullySelected(id)));
    let destination = null;
    for (const group of this.elementGroupRecords().filter((item) => item.boxed || item.locked)) {
      if (wholeGroups.has(group.id)) {
        continue;
      }
      const bounds = this.dragElementGroupBounds?.get(group.id) || this.getElementGroupBounds(group.id);
      if (bounds && point.x >= bounds.minX - 10 && point.x <= bounds.maxX + 10 && point.y >= bounds.minY - 10 && point.y <= bounds.maxY + 10) {
        destination = group;
        break;
      }
    }
    const updateItem = (item) => {
      if (!item) {
        return;
      }
      if (destination) {
        item.groupId = destination.id;
        item.locked = true;
        return;
      }
      if (wholeGroups.has(item.groupId)) {
        return;
      }
      if (item.groupId) {
        const originalBounds = this.dragElementGroupBounds?.get(item.groupId);
        const remainsInside = originalBounds && point.x >= originalBounds.minX - 10 && point.x <= originalBounds.maxX + 10 && point.y >= originalBounds.minY - 10 && point.y <= originalBounds.maxY + 10;
        if (!remainsInside) {
          item.groupId = "";
          item.locked = false;
        }
      }
    };
    strokeIndexes.forEach((index) => updateItem(this.drawingData.strokes[index]));
    markdownBlocks.forEach(updateItem);
    this.pruneElementGroups();
  }
  updateDraggedFloatingMarkdownBlocks(event, createFloating = false) {
    if (!this.dragMarkdownOriginalElements?.size || !this.pointerStartClient) {
      return false;
    }
    const canvasRect = this.canvas?.getBoundingClientRect?.();
    if (!canvasRect?.width || !canvasRect?.height) {
      return false;
    }
    const dx = (event.clientX - this.pointerStartClient.x) / canvasRect.width;
    const canvasDy = (event.clientY - this.pointerStartClient.y) * this.canvasRenderHeight / canvasRect.height;
    const dy = canvasDy / this.canvasHeight();
    let changed = false;
    for (const state of this.dragMarkdownOriginalElements.values()) {
      let sourceBox = state.floatBox;
      if (!sourceBox && createFloating && state.canvasBounds) {
        sourceBox = normalizeMarkdownFloatBox({
          x: state.canvasBounds.minX / this.canvasWidth(),
          y: state.canvasBounds.minY / this.canvasHeight(),
          width: (state.canvasBounds.maxX - state.canvasBounds.minX) / this.canvasWidth(),
          height: (state.canvasBounds.maxY - state.canvasBounds.minY) / this.canvasHeight()
        });
      }
      if (!sourceBox) {
        continue;
      }
      state.block.floating = true;
      state.previewFloatBox = normalizeMarkdownFloatBox({
        ...sourceBox,
        x: sourceBox.x + dx,
        y: sourceBox.y + dy
      });
      state.block.floatBox = { ...state.previewFloatBox };
      changed = true;
    }
    return changed;
  }
  applyDraggedEdgeInsertion(event, strokeIndexes) {
    const selectedIndexes = new Set(strokeIndexes);
    const point = this.eventToPoint(event);
    const hit = this.pointToCanvas(point);
    let targetBounds = null;
    for (let index = this.drawingData.strokes.length - 1; index >= 0; index -= 1) {
      if (selectedIndexes.has(index)) {
        continue;
      }
      const bounds = getStrokeBounds(this.drawingData.strokes[index], this.canvasWidth(), this.canvasHeight());
      if (bounds && hit.x >= bounds.minX && hit.x <= bounds.maxX && hit.y >= bounds.minY && hit.y <= bounds.maxY) {
        targetBounds = bounds;
        break;
      }
    }
    if (!targetBounds) {
      const stack = activeDocument.elementsFromPoint?.(event.clientX, event.clientY) || [];
      for (const candidate of stack) {
        const editable = findEditableTarget(candidate, this.previewEl, { x: event.clientX, y: event.clientY });
        if (!editable || Array.from(this.dragMarkdownOriginalElements?.values?.() || []).some((state) => state.element === editable)) {
          continue;
        }
        targetBounds = this.markdownElementCanvasBounds(editable);
        if (targetBounds) {
          break;
        }
      }
    }
    if (!targetBounds || targetBounds.maxX - targetBounds.minX < 80) {
      return false;
    }
    const ratioX = (hit.x - targetBounds.minX) / Math.max(1, targetBounds.maxX - targetBounds.minX);
    if (ratioX > 0.34 && ratioX < 0.66) {
      return false;
    }
    const selectedBounds = this.getSelectedStrokeBounds();
    if (!selectedBounds) {
      return false;
    }
    const gap = 10;
    const dx = ratioX <= 0.34 ? targetBounds.minX - gap - selectedBounds.maxX : targetBounds.maxX + gap - selectedBounds.minX;
    const dy = targetBounds.minY - selectedBounds.minY;
    const canvasWidth = this.canvasWidth();
    const canvasHeight = this.canvasHeight();
    const boundedDx = clamp10(dx, -selectedBounds.minX, canvasWidth - selectedBounds.maxX);
    const boundedDy = clamp10(dy, -selectedBounds.minY, canvasHeight - selectedBounds.maxY);
    const normalizedDx = boundedDx / canvasWidth;
    const normalizedDy = boundedDy / canvasHeight;
    for (const index of strokeIndexes) {
      const stroke = this.drawingData.strokes[index];
      if (!stroke) {
        continue;
      }
      stroke.points = stroke.points.map((strokePoint) => ({
        ...strokePoint,
        x: clamp10(strokePoint.x + normalizedDx, 0, 1),
        y: clamp10(strokePoint.y + normalizedDy, 0, 1)
      }));
    }
    for (const state of this.dragMarkdownOriginalElements?.values?.() || []) {
      const box = state.block.floating && state.block.floatBox ? state.block.floatBox : null;
      if (!box) {
        continue;
      }
      state.block.floating = true;
      state.block.floatBox = normalizeMarkdownFloatBox({ ...box, x: box.x + normalizedDx, y: box.y + normalizedDy });
    }
    return true;
  }
  // Connector groups are synchronized from persisted connector endpoints.
  // The old ad-hoc related-selection command is intentionally unavailable.
  dragEventToPoint(event) {
    const geometry = this.dragStrokePointerGeometry || this.captureCanvasPointerGeometry();
    const mapped = mapClientPointToCanvas(event, geometry);
    return {
      x: clamp10(mapped.canvasX / Math.max(1, mapped.canvasWidth), 0, 1),
      y: clamp10(mapped.canvasY / Math.max(1, mapped.canvasHeight), 0, 1)
    };
  }
  moveSelectedStroke(event) {
    if (!this.dragStrokeStartPoint || !this.dragStrokeOriginalPoints?.size && !this.dragMarkdownOriginalElements?.size) {
      return;
    }
    if (this.dragLastPointerEvent === event) {
      return;
    }
    const pointerKey = [
      event.pointerId,
      event.clientX,
      event.clientY,
      event.timeStamp
    ].join(":");
    if (this.dragLastPointerKey === pointerKey) {
      return;
    }
    this.dragLastPointerEvent = event;
    this.dragLastPointerKey = pointerKey;
    const dragUsesNoteFlowPlacement = this.usesDraggedNoteFlowPlacement();
    const coalescedEvents = event.getCoalescedEvents?.() || [];
    const dragEvent = coalescedEvents[coalescedEvents.length - 1] || event;
    const point = this.dragEventToPoint(dragEvent);
    const originalPointBounds = this.dragStrokeOriginalPointBounds;
    const minX = originalPointBounds?.minX ?? 0;
    const maxX = originalPointBounds?.maxX ?? 1;
    const minY = originalPointBounds?.minY ?? 0;
    const maxY = originalPointBounds?.maxY ?? 1;
    const strokeIndexes = this.dragStrokeIndexes;
    let minimumDx = -minX;
    const laneRect = this.dragContentLaneRect;
    const canvasRect = this.dragStrokePointerGeometry?.rect;
    if (this.dragStrokeOriginalBounds && laneRect && canvasRect?.width > 0) {
      const laneLeft = (laneRect.left - canvasRect.left) / canvasRect.width;
      minimumDx = Math.max(minimumDx, laneLeft - this.dragStrokeOriginalBounds.minX);
    }
    const dx = clamp10(point.x - this.dragStrokeStartPoint.x, Math.min(minimumDx, 1 - maxX), 1 - maxX);
    const dy = clamp10(point.y - this.dragStrokeStartPoint.y, -minY, 1 - maxY);
    const movedDistance = pointDistanceOnCanvas(
      this.dragStrokeStartPoint,
      point,
      this.canvasWidth(),
      this.canvasHeight()
    );
    if (!this.dragStrokeMoved && movedDistance <= this.selectedDragActivationDistancePx(event.pointerType)) {
      event.preventDefault();
      event.stopPropagation();
      return;
    }
    if (!this.dragStrokeMoved) {
      this.dragStrokeMoved = true;
      this.clearSelectionLongPress();
      this.cancelResizeFrame();
      if (this.dragShouldSnap && !this.dragNoteFlowOriginalBounds?.size) {
        this.prepareDragSnapBounds(strokeIndexes);
      }
      this.prepareReadingBottomExtentForDrag();
    }
    this.extendReadingBottomExtentDuringDrag(event);
    const hasDraggedNoteFlow = Boolean(this.dragNoteFlowOriginalBounds?.size);
    let snappedDx = dx;
    let snappedDy = dy;
    if (!hasDraggedNoteFlow && this.dragShouldSnap && this.dragStrokeOriginalBounds) {
      const snap = this.computeSnapDeltaForNormalizedBounds(translateNormalizedBounds(this.dragStrokeOriginalBounds, dx, dy), strokeIndexes);
      snappedDx = clamp10(dx + snap.dx, -minX, 1 - maxX);
      snappedDy = clamp10(dy + snap.dy, -minY, 1 - maxY);
    }
    const previewDx = hasDraggedNoteFlow ? dx : snappedDx;
    const previewDy = hasDraggedNoteFlow ? dy : snappedDy;
    for (const [index, points] of this.dragStrokeOriginalPoints.entries()) {
      const stroke = this.drawingData.strokes[index];
      if (!stroke) {
        continue;
      }
      stroke.points = points.map((strokePoint) => ({
        ...strokePoint,
        x: clamp10(strokePoint.x + previewDx, 0, 1),
        y: clamp10(strokePoint.y + previewDy, 0, 1)
      }));
    }
    this.captureDraggedNoteFlowRawPreview();
    this.dragNoteFlowPlacementClientDelta = null;
    const localScaleX = canvasRect?.width > 0 ? canvasRect.width / Math.max(1, this.canvasWidth()) : 1;
    const localScaleY = canvasRect?.height > 0 ? canvasRect.height / Math.max(1, this.canvasRenderHeight) : 1;
    const rawClientDx = dragEvent.clientX - this.pointerStartClient.x;
    const minimumClientDx = this.dragMarkdownOriginalClientBounds && laneRect ? laneRect.left - this.dragMarkdownOriginalClientBounds.left : Number.NEGATIVE_INFINITY;
    const boundedClientDx = Math.max(rawClientDx, minimumClientDx);
    this.dragMarkdownClientDeltaX = Number.isFinite(boundedClientDx) ? boundedClientDx : rawClientDx;
    const clientDx = this.dragMarkdownClientDeltaX / Math.max(1e-4, localScaleX);
    const clientDy = (dragEvent.clientY - this.pointerStartClient.y) / Math.max(1e-4, localScaleY);
    this.dragMarkdownClientDeltaY = dragEvent.clientY - this.pointerStartClient.y;
    const movedFloatingMarkdown = this.updateDraggedFloatingMarkdownBlocks(dragEvent, false);
    const usesNoteFlowPlacement = this.usesDraggedNoteFlowPlacement();
    for (const state of this.dragMarkdownOriginalElements?.values?.() || []) {
      const floating = Boolean(state.block?.floating);
      setNoteDrawCssProps(state.dragElement || state.element, {
        "--notedraw-md-drag-x": floating || !usesNoteFlowPlacement ? `${Math.round(clientDx)}px` : "0px",
        "--notedraw-md-drag-y": floating || !usesNoteFlowPlacement ? `${Math.round(clientDy)}px` : "0px"
      });
    }
    if (this.dragHasBoxBackground) {
      this.invalidateStaticCache();
    }
    if (this.dragMovedElementIds?.size) {
      this.dragPendingConnectorIds || (this.dragPendingConnectorIds = /* @__PURE__ */ new Set());
      for (const id of this.dragMovedElementIds) {
        this.dragPendingConnectorIds.add(id);
      }
    }
    if (dragUsesNoteFlowPlacement) {
      this.applyDraggedNoteFlowPlacementFrame(dragEvent.clientX, dragEvent.clientY);
    } else {
      if (this.draggedNoteFlowMarkdownStates().length) {
        this.queueMarkdownBlockDropTarget(event.clientX, event.clientY);
      }
      this.queueDraggedNoteFlowRefresh(strokeIndexes);
    }
    if (this.syncDraggedTextHighlightAnchors()) {
      this.invalidateStaticCache();
    }
    this.invalidateSelectionFrameSnapshot();
    if (strokeIndexes.length || this.dragConnectorCandidates?.length || this.dragHasBoxBackground || movedFloatingMarkdown) {
      this.requestRender(this.selectionHasDomStrokes() ? "interaction" : false);
    }
    event.preventDefault();
    event.stopPropagation();
  }
  finishSelectedStrokeDrag(event) {
    this.clearSelectionLongPress();
    this.cancelDraggedNoteFlowStrokeAnimation(true);
    const didMove = this.dragStrokeMoved;
    if (didMove) {
      this.beginNoteFlowScrollStability();
    }
    let requestedDropPlacement = null;
    let previewCommitStates = null;
    let settleNoteFlowImmediately = false;
    if (didMove && this.usesDraggedNoteFlowPlacement()) {
      const visiblePreviewPlacement = this.dragNoteFlowLastAppliedPlacement;
      const pendingDropClientX = this.dragNoteFlowDropClientX;
      const pendingDropClientY = this.dragNoteFlowDropClientY;
      if (this.dragNoteFlowDropFrameId !== null) {
        window.cancelAnimationFrame(this.dragNoteFlowDropFrameId);
        this.dragNoteFlowDropFrameId = null;
      }
      this.dragNoteFlowDropClientX = null;
      this.dragNoteFlowDropClientY = null;
      if (!visiblePreviewPlacement && Number.isFinite(Number(pendingDropClientX)) && Number.isFinite(Number(pendingDropClientY))) {
        this.updateDraggedNoteFlowPlacement(Number(pendingDropClientX), Number(pendingDropClientY));
      }
      const previewedPlacement = visiblePreviewPlacement || this.dragNoteFlowLastAppliedPlacement || this.dragNoteFlowPlacement;
      requestedDropPlacement = previewedPlacement ? {
        path: previewedPlacement.path,
        line: previewedPlacement.line,
        side: previewedPlacement.side,
        horizontalSide: previewedPlacement.horizontalSide,
        leftSnap: previewedPlacement.leftSnap,
        boundary: previewedPlacement.boundary,
        inlineBoundary: previewedPlacement.inlineBoundary,
        flowOrder: previewedPlacement.flowOrder,
        noteFlowBoundary: previewedPlacement.noteFlowBoundary,
        candidate: previewedPlacement.candidate,
        previewCandidates: previewedPlacement.previewCandidates,
        previewRowOffsets: previewedPlacement.previewRowOffsets
      } : null;
      if (requestedDropPlacement) {
        const previewIndexes = /* @__PURE__ */ new Set([
          ...Array.from(this.dragStrokeOriginalPoints?.keys?.() || []),
          ...Array.from(this.dragNoteFlowLivePreviewOriginals?.keys?.() || [])
        ]);
        previewCommitStates = new Map(Array.from(previewIndexes).flatMap((index) => {
          const stroke = this.drawingData?.strokes?.[index];
          return stroke ? [[index, {
            points: stroke.points.map((point) => ({ ...point })),
            noteFlow: stroke.noteFlow ? { ...stroke.noteFlow } : null
          }]] : [];
        }));
      }
    }
    const preserveMarkdownDomPreview = didMove && Boolean(this.dragNoteFlowDomPreview) && Boolean(this.dragMarkdownLastValidDrop?.element?.isConnected);
    const committedDomPreview = preserveMarkdownDomPreview ? this.dragNoteFlowDomPreview : null;
    if (preserveMarkdownDomPreview) {
      for (const state of this.dragMarkdownOriginalElements?.values?.() || []) {
        state.preserveDomUntilCommit = true;
      }
    }
    this.restoreDraggedNoteFlowLivePreview({ preserveDom: preserveMarkdownDomPreview });
    this.restoreDraggedNoteFlowReservationStyles();
    this.clearDraggedNoteFlowPlacement();
    if (didMove && this.draggedNoteFlowMarkdownStates().length) {
      const visibleDrop = this.dragMarkdownDropTarget?.isConnected ? this.dragMarkdownLastValidDrop?.element === this.dragMarkdownDropTarget && this.dragMarkdownLastValidDrop?.side === this.dragMarkdownDropSide ? this.dragMarkdownLastValidDrop : this.captureMarkdownBlockDropTarget(
        this.dragMarkdownDropTarget,
        this.dragMarkdownDropSide,
        this.markdownDropRowMetrics(this.dragMarkdownDropTarget, new Set(
          Array.from(this.dragMarkdownOriginalElements.values()).map((state) => state.element)
        ))
      ) : null;
      if (visibleDrop) {
        this.dragMarkdownLastValidDrop = visibleDrop;
        this.cancelMarkdownBlockDropFrame();
      } else if (!this.dragMarkdownLastValidDrop?.element?.isConnected) {
        this.flushMarkdownBlockDropTarget(event.clientX, event.clientY);
      }
    }
    const lastDrop = this.dragMarkdownLastValidDrop?.element?.isConnected ? this.dragMarkdownLastValidDrop : this.dragMarkdownDropTarget?.isConnected ? this.captureMarkdownBlockDropTarget(
      this.dragMarkdownDropTarget,
      this.dragMarkdownDropSide,
      this.markdownDropRowMetrics(this.dragMarkdownDropTarget, new Set(
        Array.from(this.dragMarkdownOriginalElements?.values?.() || []).map((state) => state.element)
      ))
    ) : null;
    const markdownDrop = lastDrop ? {
      ...lastDrop,
      moving: Array.from(this.dragMarkdownOriginalElements?.values?.() || []),
      textCommit: this.dragMarkdownTextCommit,
      domPreview: committedDomPreview
    } : null;
    const noOpMarkdownDrop = didMove && markdownDrop ? this.markdownDropIsNoOp(markdownDrop) : false;
    if (didMove && markdownDrop && !noOpMarkdownDrop) {
      this.selectionFrameAwaitingMarkdownSync = { committed: false };
      this.invalidateSelectionFrameSnapshot();
    }
    const drawingHistoryBefore = this.dragDrawingHistoryBefore;
    if (noOpMarkdownDrop) {
      this.restoreDraggedNoteFlowLivePreview();
      this.restoreDraggedNoteFlowReservationStyles();
      this.dragMarkdownLastValidDrop = null;
      this.clearMarkdownBlockDropTarget();
      this.dragMarkdownDropTarget = null;
      this.dragMarkdownDropSide = null;
    } else if (didMove) {
      const movedIndexes = Array.from(this.dragStrokeOriginalPoints?.keys() || []);
      this.detachTextHighlightAnchors(movedIndexes);
      const movedNoteFlowIndexes = this.draggedNoteFlowIndexes(movedIndexes);
      const affectsNoteFlow = movedNoteFlowIndexes.length > 0;
      if (!markdownDrop) {
        for (const state of this.dragMarkdownOriginalElements?.values?.() || []) {
          if (state.previewFloatBox) {
            state.block.floating = true;
            state.block.floatingExplicit = true;
            state.block.floatBox = { ...state.previewFloatBox };
          }
        }
      }
      if (!affectsNoteFlow && movedIndexes.length && (!markdownDrop || markdownDrop.side === "left" || markdownDrop.side === "right")) {
        this.applyDraggedEdgeInsertion(event, movedIndexes);
      }
      this.updateDraggedElementGroupMembership(event, movedIndexes, Array.from(this.dragMarkdownOriginalElements?.values?.() || []).map((state) => state.block));
      this.invalidateStaticCache();
      if (affectsNoteFlow) {
        this.resetNoteFlowSettle();
        this.noteFlowAvoidanceAnchors.clear();
      }
      this.resetDragDropGeometry();
      let resolvedDropPlacement = affectsNoteFlow ? this.resolveDraggedNoteFlowPlacement(requestedDropPlacement, movedIndexes) : null;
      if (resolvedDropPlacement && previewCommitStates?.size) {
        const refreshedCandidate = this.refreshDraggedNoteFlowPreviewCandidate(resolvedDropPlacement.candidate) || resolvedDropPlacement.candidate;
        resolvedDropPlacement = this.remeasureDraggedNoteFlowPlacement({
          ...resolvedDropPlacement,
          candidate: refreshedCandidate
        });
      }
      if (resolvedDropPlacement?.horizontalSide) {
        this.prepareMarkdownAnchorForInlineNoteFlow(resolvedDropPlacement);
        this.allowMarkdownPresentationDuringDrag = true;
        this.syncMarkdownBlockPresentation();
        this.allowMarkdownPresentationDuringDrag = false;
      }
      if (resolvedDropPlacement && previewCommitStates?.size) {
        for (const [index, state] of previewCommitStates) {
          const stroke = this.drawingData?.strokes?.[index];
          if (!stroke) {
            continue;
          }
          stroke.points = state.points.map((point) => ({ ...point }));
          stroke.noteFlow = state.noteFlow ? { ...state.noteFlow } : null;
        }
      } else if (resolvedDropPlacement) {
        this.snapDraggedSelectionToNoteFlowPlacement(resolvedDropPlacement, movedIndexes);
      } else if (affectsNoteFlow) {
        for (const index of movedNoteFlowIndexes) {
          const originalPoints = this.dragStrokeOriginalPoints?.get(index);
          if (originalPoints?.length) {
            this.drawingData.strokes[index].points = originalPoints.map((point) => ({ ...point }));
          }
        }
      }
      const flowedIndexes = resolvedDropPlacement && !previewCommitStates?.size ? this.reflowNoteFlowElementsAfterDrag(movedIndexes, resolvedDropPlacement, {
        candidates: resolvedDropPlacement.previewCandidates
      }) : [];
      const affectedIndexes = Array.from(/* @__PURE__ */ new Set([
        ...movedIndexes,
        ...flowedIndexes,
        ...Array.from(previewCommitStates?.keys?.() || [])
      ]));
      const droppedNoteFlowIndexes = new Set(this.draggedNoteFlowIndexes(movedIndexes));
      for (const index of affectedIndexes) {
        const stroke = this.drawingData.strokes[index];
        if (stroke?.noteFlow?.enabled) {
          const rejectedDrop = droppedNoteFlowIndexes.has(index) && !resolvedDropPlacement;
          if (rejectedDrop) {
            stroke.noteFlow = { ...this.dragStrokeOriginalNoteFlows.get(index) };
          } else if (droppedNoteFlowIndexes.has(index)) {
            stroke.noteFlow = this.captureNoteFlowAnchor(stroke, {
              placement: droppedNoteFlowIndexes.has(index) ? resolvedDropPlacement : null,
              preservePlacement: !droppedNoteFlowIndexes.has(index),
              rowOffset: droppedNoteFlowIndexes.has(index) ? previewCommitStates?.size ? null : resolvedDropPlacement?.previewRowOffsets?.[index] : null,
              preserveBoxGeometry: droppedNoteFlowIndexes.has(index) ? resolvedDropPlacement?.horizontalSide ? null : this.dragStrokeOriginalNoteFlows.get(index) : null
            });
          } else if (!previewCommitStates?.has(index)) {
            stroke.noteFlow = this.captureNoteFlowAnchor(stroke, {
              preservePlacement: true
            });
          }
        }
      }
      this.reconcileAutoNoteFlowMarkdownSpans();
      this.captureResponsiveAnchorsForIndexes(affectedIndexes);
      this.syncBoundConnectors({ elementIds: this.connectorTargetIdsForStrokeIndexes(affectedIndexes) });
      this.lastTextTap = null;
      this.redoStack = [];
      if (!affectsNoteFlow) {
        this.plugin.scheduleDrawingSave(this.file, this.drawingData, { userOperation: true });
      }
      if (!markdownDrop) {
        this.recordDrawingHistory(drawingHistoryBefore, { knownChanged: true });
      }
      if (affectedIndexes.some((index) => this.drawingData.strokes[index]?.noteFlow?.enabled)) {
        this.scheduleNoteFlowLayout({ operation: true, defer: true });
        settleNoteFlowImmediately = true;
      }
    } else if (!this.dragStrokePreserveSelection && this.getSelectedStrokeIndexes().length > 1 && this.dragStrokeHitIndex >= 0) {
      this.setSelectedStrokes(this.dragStrokeHitIndex);
    }
    if (didMove) {
      this.settleReadingBottomExtent();
    }
    this.releasePointerCapture(event.pointerId);
    this.clearSelectedStrokeDragState({ preserveMarkdownDom: Boolean(markdownDrop?.domPreview && !noOpMarkdownDrop) });
    if (this.restoreTextHighlightAnchors()) {
      this.invalidateStaticCache();
    }
    if (settleNoteFlowImmediately) {
      this.scheduleNoteFlowLayout({ immediate: true });
    }
    if (didMove) {
      this.invalidateSelectionFrameSnapshot();
      if (!this.selectionFrameAwaitingMarkdownSync) {
        this.captureSelectionFrameSnapshot({ force: true });
      }
    }
    if (this.externalDrawingRefreshPending) {
      this.externalDrawingRefreshPending = false;
      this.plugin.scheduleExternalDrawingRefresh(this.file?.path, 0);
    }
    this.render();
    if (didMove && markdownDrop && !noOpMarkdownDrop) {
      this.commitDraggedMarkdownBlocks(markdownDrop, drawingHistoryBefore).then((committed) => {
        const committedChanged = Boolean(committed && committed.changed !== false);
        const horizontalCommit = markdownDrop.side === "left" || markdownDrop.side === "right";
        const revertBlockState = !committedChanged && !horizontalCommit;
        if (revertBlockState) {
          for (const state of markdownDrop.moving || []) {
            if (!state?.block) {
              continue;
            }
            state.block.span = state.span;
            state.block.noteFlowAutoSpan = state.noteFlowAutoSpan;
            state.block.widthScale = state.widthScale;
            state.block.inlineWidthMode = state.inlineWidthMode;
            state.block.floating = Boolean(state.floatBox);
            state.block.floatingExplicit = Boolean(state.floatingExplicit);
            state.block.floatBox = state.floatBox ? { ...state.floatBox } : null;
          }
          this.refreshMarkdownBlockPresentation(Array.from(markdownDrop.moving || []).map((state) => state.block?.id).filter(Boolean));
        }
        this.settleCommittedMarkdownDomPreview(markdownDrop, committedChanged && !revertBlockState);
        if (committedChanged) {
          this.repairMarkdownDomAfterCommit(markdownDrop);
          this.scheduleMarkdownMoveRepair(markdownDrop.moving);
        }
        if (!committed && this.selectionFrameAwaitingMarkdownSync) {
          this.selectionFrameAwaitingMarkdownSync = null;
          this.invalidateSelectionFrameSnapshot();
          this.captureSelectionFrameSnapshot({ force: true });
          this.render();
        }
      }).catch((error) => {
        this.settleCommittedMarkdownDomPreview(markdownDrop, false);
        this.selectionFrameAwaitingMarkdownSync = null;
        this.invalidateSelectionFrameSnapshot();
        this.captureSelectionFrameSnapshot({ force: true });
        this.render();
        console.error(`[${PLUGIN_ID}] Failed to move Markdown blocks`, error);
        this.recordDrawingHistory(drawingHistoryBefore, { knownChanged: true });
        new import_obsidian.Notice(this.plugin.t("failedMoveMarkdownBlock"));
      });
    }
    event.preventDefault();
    event.stopPropagation();
  }
  cancelSelectedStrokeDrag(restoreOriginal = false) {
    this.clearSelectionLongPress();
    this.cancelDraggedNoteFlowStrokeAnimation(false);
    this.restoreDraggedNoteFlowLivePreview();
    this.clearNoteFlowPeerAnimations();
    this.clearDraggedNoteFlowPlacement();
    const restoredDrag = Boolean(restoreOriginal && this.dragStrokeOriginalPoints?.size);
    if (restoredDrag) {
      this.resetNoteFlowSettle();
      this.clearNoteFlowLayout();
      for (const [index, points] of this.dragStrokeOriginalPoints.entries()) {
        const stroke = this.drawingData.strokes[index];
        if (stroke) {
          stroke.points = points.map((strokePoint) => ({ ...strokePoint }));
          const originalNoteFlow = this.dragStrokeOriginalNoteFlows?.get(index);
          stroke.noteFlow = originalNoteFlow ? { ...originalNoteFlow } : null;
        }
      }
    }
    if (restoreOriginal && this.dragNoteFlowConnectorOriginalStates.size) {
      for (const [index, points] of this.dragNoteFlowConnectorOriginalStates) {
        const stroke = this.drawingData?.strokes?.[index];
        if (!stroke) {
          continue;
        }
        stroke.points = points.map((point) => ({ ...point }));
      }
    }
    if (this.activePointerId !== null) {
      this.releasePointerCapture(this.activePointerId);
    }
    this.clearSelectedStrokeDragState();
    if (this.externalDrawingRefreshPending) {
      this.externalDrawingRefreshPending = false;
      this.plugin.scheduleExternalDrawingRefresh(this.file?.path, 0);
    }
    if (restoredDrag) {
      this.scheduleNoteFlowLayout({ operation: true });
    }
    this.render();
  }
  clearSelectedStrokeDragState(options = {}) {
    this.cancelDraggedNoteFlowStrokeAnimation(false);
    this.clearDraggedNoteFlowPlacement();
    this.cancelMarkdownBlockDropFrame();
    this.resetDragDropGeometry();
    this.draggingStroke = false;
    this.dragStrokeStartPoint = null;
    this.dragStrokePointerGeometry = null;
    this.dragStrokeOriginalPoints = null;
    this.dragNoteFlowConnectorOriginalStates.clear();
    this.dragStrokeOriginalPointBounds = null;
    this.dragStrokeIndexes = [];
    this.dragShouldSnap = false;
    this.dragStrokeOriginalNoteFlows = null;
    this.pendingDraggedNoteFlowIndexes.clear();
    this.dragNoteFlowLivePreviewOriginals.clear();
    this.dragNoteFlowRawPreviewPoints = null;
    this.dragNoteFlowRawPreviewNoteFlows = null;
    this.dragNoteFlowPreviewElementIds.clear();
    this.dragNoteFlowPlacementClientDelta = null;
    if (!options.preserveMarkdownDom) {
      this.dragNoteFlowDomPreview = null;
    }
    this.dragNoteFlowReservationStyles?.clear();
    this.dragNoteFlowReservationStyles = null;
    this.dragStrokeOriginalBounds = null;
    this.dragStrokeMoved = false;
    this.dragStrokeHitIndex = -1;
    this.dragStrokePreserveSelection = false;
    for (const state of this.dragMarkdownOriginalElements?.values?.() || []) {
      const dragElement = state.dragElement || state.element;
      dragElement?.removeClass?.("is-notedraw-md-dragging");
      dragElement?.style?.removeProperty("--notedraw-md-drag-x");
      dragElement?.style?.removeProperty("--notedraw-md-drag-y");
    }
    this.clearDraggedMarkdownDomMarkers({ preserve: options.preserveMarkdownDom === true });
    this.dragMarkdownOriginalElements = null;
    this.dragMarkdownSourcePromise = null;
    this.dragMarkdownSourceIndexPromise = null;
    this.dragNoteFlowOriginalBounds = null;
    this.dragMarkdownOriginalClientBounds = null;
    this.dragMarkdownClientDeltaX = 0;
    this.dragMarkdownClientDeltaY = 0;
    this.dragContentLaneRect = null;
    this.dragMarkdownTextCommit = null;
    this.dragElementGroupBounds = null;
    this.dragSnapOtherBounds = null;
    this.dragSnapOtherXValues = null;
    this.dragSnapOtherYValues = null;
    this.dragMovedElementIds = null;
    this.dragPendingConnectorIds = null;
    this.dragConnectorCandidates = null;
    this.dragConnectorById = null;
    this.dragHasBoxBackground = false;
    this.dragLastPointerEvent = null;
    this.dragLastPointerKey = "";
    this.dragMarkdownLastValidDrop = null;
    this.dragMarkdownDomPreviewKey = "";
    this.clearMarkdownBlockDropTarget();
    this.dragDrawingHistoryBefore = null;
    this.pointerStartClient = null;
    this.activePointerId = null;
    this.previewEl.removeClass("is-moving-selection");
    this.captureSelectionFrameSnapshot({ force: true });
  }
  isReadingBottomExtentSurface() {
    return this.surfaceType === "preview" && !this.embeddedSurface && !this.workspaceSurface;
  }
  ensureReadingBottomSpacer() {
    if (!this.isReadingBottomExtentSurface()) {
      return null;
    }
    if (this.readingBottomSpacer?.isConnected) {
      return this.readingBottomSpacer;
    }
    const host = rootPreviewSizer(this.previewEl) || this.layoutMeasureEl || this.previewEl;
    if (!host?.appendChild) {
      return null;
    }
    const spacer = activeDocument.createElement("div");
    spacer.className = "notedraw-reading-bottom-spacer";
    spacer.setAttribute("aria-hidden", "true");
    host.appendChild(spacer);
    this.readingBottomSpacer = spacer;
    this.readingBottomSpacerHeight = 0;
    return spacer;
  }
  readingBottomBaseDocumentHeight() {
    const spacer = this.readingBottomSpacer;
    const host = spacer?.parentElement || rootPreviewSizer(this.previewEl) || this.layoutMeasureEl || this.previewEl;
    const total = Number(host?.scrollHeight || host?.offsetHeight || 0);
    return Math.max(Number(this.previewEl?.clientHeight || 0), total - Number(this.readingBottomSpacerHeight || 0));
  }
  setReadingBottomSpacerHeight(height, { resize = false } = {}) {
    const spacer = this.ensureReadingBottomSpacer();
    if (!spacer) {
      return false;
    }
    const next = clamp10(Math.round(Number(height) || 0), 0, 2400);
    if (next === this.readingBottomSpacerHeight) {
      return false;
    }
    this.readingBottomSpacerHeight = next;
    spacer.style.height = `${next}px`;
    spacer.style.minHeight = `${next}px`;
    this.markNoteFlowLayoutMutation();
    if (resize) {
      this.resizeCanvas({ layout: false, measure: true, preserveAbsolutePlacement: true });
      this.render();
    } else {
      this.scheduleResize({ layout: false, measure: true, preserveAbsolutePlacement: true });
    }
    return true;
  }
  selectedReadingBottomDocumentY() {
    const indexes = this.getSelectedStrokeIndexes();
    if (!indexes.length) {
      return 0;
    }
    const height = this.canvasHeight();
    return Math.max(0, ...indexes.map((index) => {
      const bounds = getStrokeBounds(this.drawingData?.strokes?.[index], this.canvasWidth(), height);
      return bounds?.maxY || 0;
    }));
  }
  prepareReadingBottomExtentForDrag() {
    if (!this.isReadingBottomExtentSurface()) {
      return;
    }
    this.ensureReadingBottomSpacer();
    const baseHeight = this.readingBottomBaseDocumentHeight();
    const selectedBottom = this.selectedReadingBottomDocumentY();
    const needed = Math.max(0, selectedBottom - baseHeight + 48);
    if (needed > this.readingBottomSpacerHeight) {
      this.setReadingBottomSpacerHeight(Math.min(480, needed), { resize: true });
      this.readingBottomExtentResizeAt = Date.now();
    }
  }
  scrollReadingViewportBy(delta) {
    const amount = Number(delta) || 0;
    if (!amount || !this.scrollContainer) {
      return;
    }
    const root = activeDocument.scrollingElement || activeDocument.documentElement;
    if (this.scrollContainer === root || this.scrollContainer === activeDocument.body || this.scrollContainer === activeDocument.documentElement) {
      window.scrollBy?.(0, amount);
      return;
    }
    this.scrollContainer.scrollTop += amount;
  }
  extendReadingBottomExtentDuringDrag(event) {
    if (!this.isReadingBottomExtentSurface() || !event) {
      return;
    }
    const rect = this.scrollContainer?.getBoundingClientRect?.() || {
      top: 0,
      bottom: window.innerHeight || 0,
      height: window.innerHeight || 0
    };
    const edge = clamp10(rect.height * 0.16, 56, 112);
    if (event.clientY < rect.bottom - edge) {
      return;
    }
    const now = Date.now();
    if (now - this.readingBottomExtentResizeAt > 90) {
      this.ensureReadingBottomSpacer();
      this.setReadingBottomSpacerHeight(this.readingBottomSpacerHeight + 220, { resize: true });
      this.readingBottomExtentResizeAt = now;
    }
    const intensity = clamp10((event.clientY - (rect.bottom - edge)) / edge, 0, 1);
    this.scrollReadingViewportBy(4 + intensity * 14);
  }
  settleReadingBottomExtent() {
    if (!this.isReadingBottomExtentSurface() || !this.readingBottomSpacer) {
      return;
    }
    const baseHeight = this.readingBottomBaseDocumentHeight();
    const allBottom = Math.max(0, ...(this.drawingData?.strokes || []).filter((stroke) => this.isStrokeVisibleOnSurface(stroke)).map((stroke) => {
      const bounds = getStrokeBounds(stroke, this.canvasWidth(), this.canvasHeight());
      return bounds?.maxY || 0;
    }));
    const needed = Math.max(0, allBottom - baseHeight + 32);
    this.setReadingBottomSpacerHeight(needed, { resize: true });
    this.readingBottomExtentResizeAt = Date.now();
  }
  startSelectedStrokeResize(event, handle) {
    const indexes = this.getSelectedStrokeIndexes();
    const resizableIndexes = indexes.filter((index) => this.canManipulateGroupedElement(this.drawingData.strokes[index]));
    const resizableMarkdownBlocks = this.getSelectedMarkdownBlocks().filter((block) => this.canManipulateGroupedElement(block));
    const bounds = this.getSelectedStrokeNormalizedBounds();
    if (!resizableIndexes.length && !resizableMarkdownBlocks.length || !bounds) {
      return;
    }
    this.endTextEdit();
    this.cancelResizeFrame();
    this.pointerDown = false;
    this.currentStroke = null;
    this.resizingSelection = true;
    this.resizeSelectionHandle = handle;
    this.resizeSelectionPointerGeometry = this.captureCanvasPointerGeometry();
    this.resizeSelectionStartClient = { x: event.clientX, y: event.clientY };
    const canvasRect = this.resizeSelectionPointerGeometry.rect;
    const scrollRect = this.scrollContainer?.getBoundingClientRect?.();
    const viewport = window.visualViewport;
    const viewportLeft = Number(viewport?.offsetLeft) || 0;
    const viewportTop = Number(viewport?.offsetTop) || 0;
    const viewportRight = viewportLeft + (Number(viewport?.width) || window.innerWidth || canvasRect.width);
    const viewportBottom = viewportTop + (Number(viewport?.height) || window.innerHeight || canvasRect.height);
    const visibleLeft = Math.max(canvasRect.left, Number(scrollRect?.left) || canvasRect.left, viewportLeft);
    const visibleRight = Math.min(canvasRect.left + canvasRect.width, Number(scrollRect?.right) || canvasRect.left + canvasRect.width, viewportRight);
    const visibleTop = Math.max(canvasRect.top, Number(scrollRect?.top) || canvasRect.top, viewportTop);
    const visibleBottom = Math.min(canvasRect.top + canvasRect.height, Number(scrollRect?.bottom) || canvasRect.top + canvasRect.height, viewportBottom);
    this.resizeSelectionClientBounds = {
      left: Math.min(event.clientX, visibleLeft),
      right: Math.max(event.clientX, visibleRight),
      top: Math.min(event.clientY, visibleTop),
      bottom: Math.max(event.clientY, visibleBottom)
    };
    this.resizeSelectionStartPoint = getSelectionResizeCorner(bounds, handle);
    this.resizeSelectionOriginalBounds = bounds;
    this.resizeSelectionPreviewBounds = { ...bounds };
    this.resizeSelectionOriginalStrokes = new Map(resizableIndexes.map((index) => [
      index,
      {
        bounds: getStrokeBounds(this.drawingData.strokes[index], this.canvasWidth(), this.canvasHeight()),
        noteFlow: this.drawingData.strokes[index].noteFlow ? { ...this.drawingData.strokes[index].noteFlow } : null,
        width: this.drawingData.strokes[index].width || this.penWidth,
        fontSize: this.drawingData.strokes[index].fontSize || 18,
        textWidth: Number(this.drawingData.strokes[index].textWidth) > 0 ? Number(this.drawingData.strokes[index].textWidth) : null,
        previewWidth: this.drawingData.strokes[index].previewWidth || 260,
        previewHeight: this.drawingData.strokes[index].previewHeight || 160,
        points: this.drawingData.strokes[index].points.map((strokePoint) => ({ ...strokePoint }))
      }
    ]));
    this.resizeSelectionOriginalMarkdownBlocks = new Map(resizableMarkdownBlocks.map((block) => {
      const element = this.markdownBlockElement(block);
      const naturalHeight = this.markdownBlockNaturalHeight(element);
      const currentHeight = Math.max(
        naturalHeight,
        normalizeMarkdownBlockMinHeight(block.minHeight)
      );
      const elementRect = element?.getBoundingClientRect?.();
      const visualScaleY = canvasRect.height > 0 ? canvasRect.height / Math.max(1, this.resizeSelectionPointerGeometry.canvasRenderHeight) : 1;
      const resizesFromBottom = handle === "se" || handle === "sw";
      const availableHeight = elementRect ? resizesFromBottom ? (this.resizeSelectionClientBounds.bottom - elementRect.top) / Math.max(1e-4, visualScaleY) : (elementRect.bottom - this.resizeSelectionClientBounds.top) / Math.max(1e-4, visualScaleY) : 2400;
      return [block.id, {
        block,
        span: block.span,
        widthScale: normalizeMarkdownBlockWidthScale(block.widthScale),
        inlineWidthMode: normalizeMarkdownInlineWidthMode(block.inlineWidthMode, block.span, block.widthScale),
        minHeight: block.minHeight,
        naturalHeight,
        currentHeight,
        maxHeight: Math.max(naturalHeight, availableHeight),
        floating: block.floating,
        floatBox: block.floatBox ? { ...block.floatBox } : null
      }];
    }));
    this.resizeDrawingHistoryBefore = this.captureDrawingHistorySnapshot();
    this.resizeSelectionMoved = false;
    this.pointerStartClient = { x: event.clientX, y: event.clientY };
    this.activePointerId = event.pointerId;
    this.previewEl.addClass("is-resizing-selection");
    try {
      this.canvas.setPointerCapture(event.pointerId);
    } catch (error) {
      void error;
    }
    event.preventDefault();
    event.stopPropagation();
  }
  moveSelectedStrokeResize(event) {
    if (!this.resizeSelectionOriginalBounds || !this.resizeSelectionOriginalStrokes?.size && !this.resizeSelectionOriginalMarkdownBlocks?.size || !this.resizeSelectionStartPoint) {
      return;
    }
    const point = this.resizeEventToPoint(event);
    const movedDistance = this.resizeSelectionStartClient ? pointerDistance(this.resizeSelectionStartClient, { x: event.clientX, y: event.clientY }) : pointDistanceOnCanvas(this.resizeSelectionStartPoint, point, this.canvasWidth(), this.canvasHeight());
    if (!this.resizeSelectionMoved && movedDistance <= this.selectedDragActivationDistancePx(event.pointerType)) {
      event.preventDefault();
      event.stopPropagation();
      return;
    }
    if (!this.resizeSelectionMoved) {
      this.resizeSelectionMoved = true;
    }
    this.applySelectedStrokeResize(point);
    this.requestRender(this.selectionHasDomStrokes() ? "interaction" : false);
    event.preventDefault();
    event.stopPropagation();
  }
  resizeEventToPoint(event) {
    const geometry = this.resizeSelectionPointerGeometry;
    const startClient = this.resizeSelectionStartClient;
    const corner = this.resizeSelectionStartPoint;
    const point = mapResizeClientDeltaToPoint(event, {
      startClient,
      corner,
      geometry,
      clientBounds: this.resizeSelectionClientBounds
    });
    if (!point) {
      return this.eventToPoint(event, geometry);
    }
    return {
      ...corner,
      ...point
    };
  }
  applySelectedStrokeResize(point) {
    const bounds = this.resizeSelectionOriginalBounds;
    const handle = this.resizeSelectionHandle;
    const originalStrokes = this.resizeSelectionOriginalStrokes;
    const originalMarkdownBlocks = this.resizeSelectionOriginalMarkdownBlocks;
    if (!bounds || !handle || !originalStrokes?.size && !originalMarkdownBlocks?.size) {
      return;
    }
    const anchor = getSelectionResizeAnchor(bounds, handle);
    const corner = getSelectionResizeCorner(bounds, handle);
    const originalDx = corner.x - anchor.x;
    const originalDy = corner.y - anchor.y;
    let scaleX = originalDx === 0 ? 1 : (point.x - anchor.x) / originalDx;
    let scaleY = originalDy === 0 ? 1 : (point.y - anchor.y) / originalDy;
    ({ scaleX, scaleY } = resolveSelectionResizeScales({ scaleX, scaleY }));
    const markdownScaleLimits = Array.from(originalMarkdownBlocks?.values?.() || []).filter((state) => !state.floating && Number(state.maxHeight) > 0).map((state) => Number(state.maxHeight) / Math.max(1, Number(state.currentHeight) || 1));
    if (markdownScaleLimits.length) {
      scaleY = Math.min(scaleY, ...markdownScaleLimits);
    }
    scaleX = limitSelectionResizeScaleToCanvas(scaleX, bounds, anchor, "x");
    scaleY = limitSelectionResizeScaleToCanvas(scaleY, bounds, anchor, "y");
    this.resizeSelectionPreviewBounds = scaleNormalizedBoundsFromAnchor(bounds, anchor, scaleX, scaleY);
    const strokeScale = clamp10((Math.abs(scaleX) + Math.abs(scaleY)) / 2, 0.2, 8);
    const contentFrame = this.getResponsiveContentFrame();
    const stableContentFrame = isStableResponsiveCaptureFrame(this.canvasWidth(), contentFrame);
    const nextByIndex = /* @__PURE__ */ new Map();
    for (const [index, original] of originalStrokes.entries()) {
      nextByIndex.set(index, {
        width: clamp10((original.width || this.penWidth) * strokeScale, MIN_BRUSH_WIDTH, MAX_BRUSH_WIDTH),
        fontSize: clamp10((original.fontSize || 18) * strokeScale, 10, 72),
        textWidth: Number(original.textWidth) > 0 ? clamp10(original.textWidth * Math.abs(scaleX), 24, 900) : null,
        previewWidth: clamp10((original.previewWidth || 260) * Math.abs(scaleX), 80, 900),
        previewHeight: clamp10((original.previewHeight || 160) * Math.abs(scaleY), 40, 700),
        points: original.points.map((strokePoint) => ({
          ...strokePoint,
          x: anchor.x + (strokePoint.x - anchor.x) * scaleX,
          y: anchor.y + (strokePoint.y - anchor.y) * scaleY
        }))
      });
    }
    for (const [index, next] of nextByIndex.entries()) {
      const stroke = this.drawingData.strokes[index];
      if (!stroke) {
        continue;
      }
      stroke.width = next.width;
      if (isTextStroke(stroke)) {
        stroke.fontSize = next.fontSize;
        stroke.textWidth = next.textWidth;
      }
      if (isTextLikeStroke(stroke) || isEmbedStroke(stroke)) {
        stroke.previewWidth = next.previewWidth;
        stroke.previewHeight = next.previewHeight;
      }
      stroke.points = next.points.map((strokePoint) => ({
        ...strokePoint,
        x: clamp10(strokePoint.x, 0, 1),
        y: clamp10(strokePoint.y, 0, 1)
      }));
      if (stableContentFrame && normalizeNoteFlow(stroke.noteFlow)) {
        const original = originalStrokes.get(index);
        const geometry = resizeNoteFlowGeometry(original?.noteFlow || stroke.noteFlow, {
          originalBounds: original?.bounds,
          resizedBounds: getStrokeBounds(stroke, this.canvasWidth(), this.canvasHeight()),
          contentLeft: contentFrame.left,
          contentWidth: contentFrame.width
        });
        if (geometry) {
          stroke.noteFlow = { ...stroke.noteFlow, ...geometry };
        }
      }
    }
    for (const state of originalMarkdownBlocks?.values?.() || []) {
      const block = state.block;
      if (state.floating && state.floatBox) {
        block.floating = true;
        block.floatBox = normalizeMarkdownFloatBox({
          x: anchor.x + (state.floatBox.x - anchor.x) * scaleX,
          y: state.floatBox.y,
          width: state.floatBox.width * Math.abs(scaleX),
          height: clamp10(state.naturalHeight / Math.max(1, this.canvasHeight()), 0.02, 1)
        });
      } else {
        const originalWidthUnits = Math.max(2, Number(state.span) || 12) * normalizeMarkdownBlockWidthScale(state.widthScale);
        const desiredWidthUnits = clamp10(originalWidthUnits * Math.abs(scaleX), 2, 12);
        const nextSpan = clamp10(Math.ceil(desiredWidthUnits), 2, 12);
        block.span = nextSpan;
        block.widthScale = normalizeMarkdownBlockWidthScale(desiredWidthUnits / nextSpan);
        block.inlineWidthMode = "fixed";
        block.minHeight = resizeMarkdownBlockMinHeight({
          currentHeight: state.currentHeight,
          naturalHeight: state.naturalHeight,
          scaleY,
          maxHeight: state.maxHeight
        });
      }
    }
    if (originalMarkdownBlocks?.size) {
      this.refreshMarkdownBlockPresentation(originalMarkdownBlocks.keys());
    }
    const resizedGroupIds = new Set([
      ...Array.from(originalStrokes.keys()).map((index) => this.drawingData.strokes[index]?.groupId),
      ...Array.from(originalMarkdownBlocks?.values?.() || []).map((state) => state.block?.groupId)
    ].filter(Boolean));
    if (Array.from(resizedGroupIds).some((id) => {
      const group = this.elementGroup(id);
      return group?.boxed && group.backgroundColor;
    })) {
      this.invalidateStaticCache();
    }
    const resizedElementIds = this.connectorTargetIdsForStrokeIndexes(this.resizeSelectionOriginalStrokes.keys());
    if (this.syncBoundConnectors({ elementIds: resizedElementIds })) {
      this.invalidateStaticCache();
    }
    if (this.selectedResizeAffectsNoteFlowLayout()) {
      this.flushSelectedResizeNoteFlowLayout({ immediate: true });
      this.queueSelectedResizeNoteFlowLayout();
    }
  }
  selectedResizeAffectsNoteFlowLayout() {
    return Boolean(this.resizeSelectionOriginalMarkdownBlocks?.size) || Array.from(this.resizeSelectionOriginalStrokes?.keys?.() || []).some((index) => this.drawingData.strokes[index]?.noteFlow?.enabled);
  }
  queueSelectedResizeNoteFlowLayout() {
    this.scheduleNoteFlowLayout({ operation: true, defer: true });
    if (this.noteFlowFrameId !== null) {
      window.cancelAnimationFrame(this.noteFlowFrameId);
      this.noteFlowFrameId = null;
    }
    if (this.noteFlowFallbackTimer !== null) {
      window.clearTimeout(this.noteFlowFallbackTimer);
      this.noteFlowFallbackTimer = null;
    }
    if (this.resizeNoteFlowFrameId !== null || this.destroyed) {
      return;
    }
    this.resizeNoteFlowFrameId = window.requestAnimationFrame(() => {
      this.resizeNoteFlowFrameId = null;
      this.flushSelectedResizeNoteFlowLayout();
    });
  }
  flushSelectedResizeNoteFlowLayout(options = {}) {
    if (this.destroyed || !this.resizingSelection) {
      return false;
    }
    this.previewEl?.getBoundingClientRect?.();
    let changed = false;
    let aligned = false;
    const maxPasses = 3;
    for (let pass = 0; pass < maxPasses; pass += 1) {
      this.noteFlowSettledRowExtents = /* @__PURE__ */ new Map();
      const layoutChanged = this.applyNoteFlowLayout();
      aligned = this.alignNoteFlowStrokesToReservedRows(null, { interaction: true });
      const passChanged = layoutChanged || aligned;
      changed = changed || passChanged;
      if (!passChanged) {
        break;
      }
      if (pass + 1 < maxPasses) {
        this.previewEl?.getBoundingClientRect?.();
      }
    }
    if (changed || aligned) {
      this.invalidateSelectionFrameSnapshot();
      this.requestRender(this.selectionHasDomStrokes() ? "interaction" : false);
    }
    return changed || aligned;
  }
  cancelSelectedResizeNoteFlowLayout() {
    if (this.resizeNoteFlowFrameId !== null) {
      window.cancelAnimationFrame(this.resizeNoteFlowFrameId);
      this.resizeNoteFlowFrameId = null;
    }
  }
  finishSelectedStrokeResize(event) {
    if (this.resizeSelectionMoved) {
      this.beginNoteFlowScrollStability();
    }
    if (this.resizeSelectionMoved) {
      const resizedMarkdownBlocks = Boolean(this.resizeSelectionOriginalMarkdownBlocks?.size);
      const resizedNoteFlowStrokes = Array.from(this.resizeSelectionOriginalStrokes?.keys?.() || []).some((index) => this.drawingData.strokes[index]?.noteFlow?.enabled);
      this.cancelSelectedResizeNoteFlowLayout();
      this.flushSelectedResizeNoteFlowLayout();
      const resizedIndexes = Array.from(this.resizeSelectionOriginalStrokes?.keys() || []);
      this.detachTextHighlightAnchors(resizedIndexes);
      this.captureResponsiveAnchorsForIndexes(resizedIndexes);
      this.syncBoundConnectors({ elementIds: this.connectorTargetIdsForStrokeIndexes(resizedIndexes) });
      this.redoStack = [];
      this.plugin.scheduleDrawingSave(this.file, this.drawingData, { userOperation: true });
      this.recordDrawingHistory(this.resizeDrawingHistoryBefore);
      if (resizedMarkdownBlocks || resizedNoteFlowStrokes) {
        this.alignNoteFlowStrokesToReservedRows(null, { measureOnly: true });
        this.scheduleNoteFlowLayout({ operation: true });
      }
    } else {
      this.cancelSelectedStrokeResize(false);
    }
    this.releasePointerCapture(event.pointerId);
    this.clearSelectedStrokeResizeState();
    this.render();
    event.preventDefault();
    event.stopPropagation();
  }
  resetSelectedElementsToBestFit() {
    const blocks = this.getSelectedMarkdownBlocks().filter((block) => !block.locked || Boolean(block.groupId));
    const resizableStrokes = this.getSelectedStrokeIndexes().filter((index) => {
      const stroke = this.drawingData?.strokes?.[index];
      return Boolean(stroke) && (!stroke.locked || Boolean(stroke.groupId));
    });
    if (!blocks.length && !resizableStrokes.length) {
      return false;
    }
    const historyBefore = this.captureDrawingHistorySnapshot();
    let changed = false;
    for (const block of blocks) {
      const before = `${block.span}:${block.widthScale}:${block.minHeight}:${block.floating ? 1 : 0}`;
      block.span = 12;
      block.widthScale = 1;
      block.inlineWidthMode = "fit";
      block.minHeight = null;
      block.floating = false;
      block.floatingExplicit = false;
      block.floatBox = null;
      block.noteFlowAutoSpan = false;
      changed = changed || before !== `${block.span}:${block.widthScale}:${block.minHeight}:${block.floating ? 1 : 0}`;
    }
    if (blocks.length) {
      this.refreshMarkdownBlockPresentation(blocks.map((block) => block.id));
    }
    if (changed || resizableStrokes.length) {
      this.redoStack = [];
      this.invalidateStaticCache();
      this.invalidateSelectionFrameSnapshot();
      this.plugin.scheduleDrawingSave(this.file, this.drawingData, { userOperation: true });
      this.recordDrawingHistory(historyBefore);
    }
    this.syncMarkdownBlockPresentation();
    this.scheduleNoteFlowLayout({ operation: true, defer: true });
    this.captureSelectionFrameSnapshot({ force: true });
    this.scheduleResize({ layout: false, measure: true });
    this.render();
    return true;
  }
  cancelSelectedStrokeResize(restoreOriginal = false) {
    if (restoreOriginal && this.resizeSelectionOriginalStrokes?.size) {
      for (const [index, original] of this.resizeSelectionOriginalStrokes.entries()) {
        const stroke = this.drawingData.strokes[index];
        if (stroke) {
          stroke.width = original.width;
          if (isTextStroke(stroke)) {
            stroke.fontSize = original.fontSize;
            stroke.textWidth = original.textWidth;
          }
          if (isTextLikeStroke(stroke) || isEmbedStroke(stroke)) {
            stroke.previewWidth = original.previewWidth;
            stroke.previewHeight = original.previewHeight;
          }
          stroke.points = original.points.map((strokePoint) => ({ ...strokePoint }));
          stroke.noteFlow = original.noteFlow ? { ...original.noteFlow } : null;
        }
      }
    }
    if (restoreOriginal && this.resizeSelectionOriginalMarkdownBlocks?.size) {
      for (const state of this.resizeSelectionOriginalMarkdownBlocks.values()) {
        state.block.span = state.span;
        state.block.widthScale = state.widthScale;
        state.block.inlineWidthMode = state.inlineWidthMode;
        state.block.minHeight = state.minHeight;
        state.block.floating = state.floating;
        state.block.floatBox = state.floatBox ? { ...state.floatBox } : null;
      }
      this.refreshMarkdownBlockPresentation(this.resizeSelectionOriginalMarkdownBlocks.keys());
    }
    if (restoreOriginal && this.active && this.selectedResizeAffectsNoteFlowLayout()) {
      this.scheduleNoteFlowLayout({ operation: true, defer: true });
      this.flushSelectedResizeNoteFlowLayout();
    }
    if (this.activePointerId !== null) {
      this.releasePointerCapture(this.activePointerId);
    }
    this.clearSelectedStrokeResizeState();
    this.render();
  }
  clearSelectedStrokeResizeState() {
    const needsGeometrySettle = this.resizingSelection;
    this.cancelSelectedResizeNoteFlowLayout();
    this.resizingSelection = false;
    this.resizeSelectionHandle = null;
    this.resizeSelectionStartPoint = null;
    this.resizeSelectionStartClient = null;
    this.resizeSelectionOriginalBounds = null;
    this.resizeSelectionPreviewBounds = null;
    this.resizeSelectionOriginalStrokes = null;
    this.resizeSelectionOriginalMarkdownBlocks = null;
    this.resizeSelectionPointerGeometry = null;
    this.resizeSelectionClientBounds = null;
    this.resizeSelectionMoved = false;
    this.resizeDrawingHistoryBefore = null;
    this.pointerStartClient = null;
    this.activePointerId = null;
    this.previewEl.removeClass("is-resizing-selection");
    this.captureSelectionFrameSnapshot({ force: true });
    if (needsGeometrySettle && !this.destroyed) {
      if (this.noteFlowOperationPending) {
        this.scheduleNoteFlowLayout();
      }
      this.scheduleResize({ layout: false, measure: true });
    }
  }
  releasePointerCapture(pointerId) {
    try {
      if (this.canvas.hasPointerCapture?.(pointerId)) {
        this.canvas.releasePointerCapture(pointerId);
      }
    } catch (error) {
      void error;
    }
  }
  finalizeBrushVariant(stroke) {
    const variant = normalizeBrushVariant(stroke?.brush, stroke?.variant);
    stroke.variant = variant;
    if (stroke?.brush !== BRUSH_WATERCOLOR) {
      return;
    }
    if (variant === WATERCOLOR_VARIANT_STRAIGHT) {
      const straightened = straightenWatercolorPoints(stroke.points, {
        canvasWidth: this.canvasWidth(),
        canvasHeight: this.canvasHeight()
      });
      stroke.points = straightened.points;
      return;
    }
    if (variant === WATERCOLOR_VARIANT_TEXT) {
      this.snapWatercolorStrokeToTextLine(stroke);
    }
  }
  isTextHighlightStroke(stroke) {
    return stroke?.brush === BRUSH_WATERCOLOR && normalizeBrushVariant(stroke.brush, stroke.variant) === WATERCOLOR_VARIANT_TEXT;
  }
  prepareTextHighlightGesture(clientPoint) {
    this.textHighlightLineRects = collectTextLineRectsBelowCanvas(this.canvas, this.previewEl);
    this.textHighlightTarget = pickTextHighlightLine(this.textHighlightLineRects, [clientPoint]);
    if (this.textHighlightTarget && this.currentStroke) {
      this.alignTextHighlightStroke(this.currentStroke, this.textHighlightTarget);
    }
  }
  resetTextHighlightGesture() {
    this.textHighlightLineRects = [];
    this.textHighlightTarget = null;
  }
  snapTextHighlightPoint(point, clientPoint) {
    if (!this.isTextHighlightStroke(this.currentStroke)) {
      return point;
    }
    if (!this.textHighlightLineRects.length) {
      this.textHighlightLineRects = collectTextLineRectsBelowCanvas(this.canvas, this.previewEl);
    }
    if (!this.textHighlightTarget) {
      this.textHighlightTarget = pickTextHighlightLine(this.textHighlightLineRects, [clientPoint]);
      if (this.textHighlightTarget) {
        this.alignTextHighlightStroke(this.currentStroke, this.textHighlightTarget);
      }
    }
    return this.textHighlightTarget ? this.alignTextHighlightPoint(point, this.textHighlightTarget) : point;
  }
  alignTextHighlightPoint(point, lineRect) {
    const canvasRect = this.canvas?.getBoundingClientRect?.();
    if (!canvasRect || canvasRect.width <= 1 || canvasRect.height <= 1 || !lineRect) {
      return point;
    }
    const canvasWidth = this.canvasWidth();
    const scaleX = canvasRect.width / canvasWidth;
    const scaleY = canvasRect.height / Math.max(1, this.canvasRenderHeight);
    const canvasPoint2 = this.pointToCanvas(point);
    const clientX = canvasRect.left + canvasPoint2.x * scaleX;
    const lineCenterY = lineRect.top + lineRect.height * 0.58;
    return {
      ...point,
      x: clamp10((clamp10(clientX, lineRect.left, lineRect.right) - canvasRect.left) / scaleX / canvasWidth, 0, 1),
      y: clamp10(((lineCenterY - canvasRect.top) / scaleY + this.canvasWindowTop) / this.canvasHeight(), 0, 1)
    };
  }
  alignTextHighlightStroke(stroke, lineRect) {
    if (!stroke?.points?.length || !lineRect) {
      return false;
    }
    stroke.points = stroke.points.map((point) => this.alignTextHighlightPoint(point, lineRect));
    this.captureTextHighlightAnchor(stroke, lineRect);
    return true;
  }
  captureTextHighlightAnchor(stroke, lineRect) {
    const canvasRect = this.canvas?.getBoundingClientRect?.();
    if (!stroke?.points?.length || !canvasRect?.width || !lineRect?.width || !Number.isFinite(lineRect.lineStart)) {
      return false;
    }
    const owner = this.ensureMarkdownBlockRecord(lineRect.element);
    if (!owner?.id) {
      return false;
    }
    const scaleX = canvasRect.width / Math.max(1, this.canvasWidth());
    stroke.textAnchor = {
      mode: "hard",
      ownerType: "markdown",
      ownerId: owner.id,
      path: normalizeVaultPath(lineRect.sourcePath || owner.path),
      lineStart: lineRect.lineStart,
      lineEnd: Number.isFinite(lineRect.lineEnd) ? lineRect.lineEnd : lineRect.lineStart,
      blockId: owner.id,
      textHint: owner.textHint || normalizeRenderedText2(lineRect.element?.innerText || "").slice(0, 240),
      baseline: 0.58,
      lineIndex: Number.isInteger(lineRect.lineIndex) ? lineRect.lineIndex : null,
      lineKey: typeof lineRect.lineKey === "string" ? lineRect.lineKey : "",
      u: stroke.points.map((point) => {
        const canvasPoint2 = this.pointToCanvas(point);
        const clientX = canvasRect.left + canvasPoint2.x * scaleX;
        return clamp10((clientX - lineRect.left) / Math.max(1, lineRect.width), 0, 1);
      })
    };
    return true;
  }
  restoreTextHighlightAnchors() {
    return this.restoreTextHighlightAnchorsForOwners(null);
  }
  syncDraggedTextHighlightAnchors() {
    const ownerIds = new Set(Array.from(this.dragMarkdownOriginalElements?.keys?.() || []));
    return ownerIds.size ? this.restoreTextHighlightAnchorsForOwners(ownerIds) : false;
  }
  restoreTextHighlightAnchorsForOwners(ownerIds = null) {
    const anchored = (this.drawingData?.strokes || []).filter((stroke) => {
      if (!this.isTextHighlightStroke(stroke) || !stroke.textAnchor?.path) {
        return false;
      }
      const anchor = normalizeTextHighlightAnchor(stroke.textAnchor);
      return !ownerIds || Boolean(anchor?.ownerId && ownerIds.has(anchor.ownerId));
    });
    if (!anchored.length || !this.canvas) {
      return false;
    }
    const lineRects = collectTextLineRectsBelowCanvas(this.canvas, this.previewEl, ownerIds);
    let changed = false;
    for (const stroke of anchored) {
      const anchor = normalizeTextHighlightAnchor(stroke.textAnchor);
      if (!anchor || anchor.mode === "detached") {
        continue;
      }
      const ownerRects = lineRects.filter((rect) => anchor.ownerId && rect.blockId === anchor.ownerId);
      const rangeRects = lineRects.filter((rect) => normalizeVaultPath(rect.sourcePath) === anchor?.path && rect.lineStart === anchor.lineStart && (rect.lineEnd === anchor.lineEnd || !Number.isFinite(anchor.lineEnd)));
      const candidateRects = ownerRects.length ? ownerRects : rangeRects;
      const anchorCanvasRect = this.canvas.getBoundingClientRect();
      const anchorScaleY = anchorCanvasRect.height / Math.max(1, this.canvasRenderHeight);
      const preferredY = stroke.points?.length ? anchorCanvasRect.top + (this.pointToCanvas(stroke.points[Math.floor(stroke.points.length / 2)]).y - this.canvasWindowTop) * anchorScaleY : Number.NaN;
      const lineRect = pickAnchoredTextHighlightLine(candidateRects, {
        lineIndex: anchor.lineIndex,
        preferredY
      }) || pickAnchoredTextHighlightLine(lineRects.filter((rect) => normalizeVaultPath(rect.sourcePath) === anchor?.path), {
        lineIndex: anchor.lineIndex,
        preferredY
      });
      if (!anchor || !lineRect || !stroke.points?.length) {
        continue;
      }
      const canvasRect = this.canvas.getBoundingClientRect();
      const scaleX = canvasRect.width / Math.max(1, this.canvasWidth());
      const scaleY = canvasRect.height / Math.max(1, this.canvasRenderHeight);
      const nextPoints = stroke.points.map((point, index) => {
        const uIndex = anchor.u.length > 1 && stroke.points.length > 1 ? Math.round(index / (stroke.points.length - 1) * (anchor.u.length - 1)) : 0;
        const clientX = lineRect.left + (anchor.u[uIndex] ?? 0) * lineRect.width;
        const clientY = lineRect.top + lineRect.height * anchor.baseline;
        return {
          ...point,
          x: clamp10((clientX - canvasRect.left) / Math.max(1e-4, scaleX) / this.canvasWidth(), 0, 1),
          y: clamp10(((clientY - canvasRect.top) / Math.max(1e-4, scaleY) + this.canvasWindowTop) / this.canvasHeight(), 0, 1)
        };
      });
      if (!sameStrokePointPath(stroke.points, nextPoints)) {
        stroke.points = nextPoints;
        changed = true;
      }
    }
    return changed;
  }
  scheduleDeletedTextHighlightPrune() {
    if (!this.noteFlowMarkdownAnnotationComplete || this.deletedTextHighlightPruneTimer !== null || !this.file) {
      return;
    }
    this.deletedTextHighlightPruneTimer = window.setTimeout(async () => {
      this.deletedTextHighlightPruneTimer = null;
      if (this.destroyed || !this.noteFlowMarkdownAnnotationComplete || !this.file) {
        return;
      }
      const connectedIds = new Set(this.markdownBlockElements?.keys?.() || []);
      const candidates = (this.drawingData?.strokes || []).filter((stroke) => {
        const anchor = normalizeTextHighlightAnchor(stroke?.textAnchor);
        return this.isTextHighlightStroke(stroke) && anchor?.mode === "hard" && anchor.ownerId && anchor.path === normalizeVaultPath(this.file.path) && !connectedIds.has(anchor.ownerId);
      });
      if (!candidates.length) {
        return;
      }
      let source = "";
      try {
        source = await this.plugin.app.vault.cachedRead(this.file);
      } catch {
        return;
      }
      const sourceLower = source.toLocaleLowerCase();
      const deleted = new Set(candidates.filter((stroke) => {
        const hint = normalizeTextHighlightAnchor(stroke.textAnchor)?.textHint?.trim().toLocaleLowerCase() || "";
        if (!hint || sourceLower.includes(hint)) {
          return false;
        }
        const evidence = textBindingEvidenceTokens(hint);
        return evidence.length > 0 && !evidence.some((term) => sourceLower.includes(term));
      }));
      if (!deleted.size) {
        return;
      }
      this.drawingData.strokes = this.drawingData.strokes.filter((stroke) => !deleted.has(stroke));
      this.invalidateStaticCache();
      this.rebuildElementRelations();
      this.plugin.scheduleDrawingSave(this.file, this.drawingData, { userOperation: true, replace: true });
      this.render();
    }, 260);
  }
  detachTextHighlightAnchors(indexes) {
    let changed = false;
    for (const index of indexes || []) {
      const stroke = this.drawingData?.strokes?.[index];
      if (!this.isTextHighlightStroke(stroke) || !stroke.textAnchor || stroke.textAnchor.mode === "detached") {
        continue;
      }
      stroke.textAnchor = { ...stroke.textAnchor, mode: "detached" };
      changed = true;
    }
    return changed;
  }
  snapWatercolorStrokeToTextLine(stroke) {
    if (!stroke?.points?.length || !this.canvas) {
      return false;
    }
    const canvasRect = this.canvas.getBoundingClientRect();
    const canvasWidth = this.canvasWidth();
    const visibleHeight = Math.max(1, this.canvasRenderHeight);
    const scaleX = canvasRect.width > 0 ? canvasRect.width / canvasWidth : 1;
    const scaleY = canvasRect.height > 0 ? canvasRect.height / visibleHeight : 1;
    const clientPoints = stroke.points.map((point) => {
      const canvasPoint2 = this.pointToCanvas(point);
      return {
        x: canvasRect.left + canvasPoint2.x * scaleX,
        y: canvasRect.top + (canvasPoint2.y - this.canvasWindowTop) * scaleY
      };
    });
    const lineRects = this.textHighlightLineRects.length ? this.textHighlightLineRects : collectTextLineRectsBelowCanvas(this.canvas, this.previewEl);
    const lineRect = this.textHighlightTarget || pickTextHighlightLine(lineRects, clientPoints, {
      maxVerticalDistance: Math.max(36, Number(stroke.width || 0) * 1.5),
      maxHorizontalDistance: 160
    });
    if (!lineRect) {
      return false;
    }
    return this.alignTextHighlightStroke(stroke, lineRect);
  }
  addPointerSamples(event) {
    const samples = typeof event.getCoalescedEvents === "function" ? event.getCoalescedEvents() : null;
    const events = samples?.length ? samples : [event];
    for (const sample of events) {
      if (this.pointerStartClient && pointerDistance(this.pointerStartClient, {
        x: sample.clientX,
        y: sample.clientY
      }) > this.tapDistancePx()) {
        this.didMove = true;
      }
      const point = this.eventToPoint(sample);
      this.addStrokePoint(this.snapTextHighlightPoint(point, { x: sample.clientX, y: sample.clientY }));
    }
  }
  addStrokePoint(point) {
    if (!this.currentStroke?.points?.length) {
      return;
    }
    const points = this.currentStroke.points;
    const from = points[points.length - 1];
    const distance = pointDistanceOnCanvas(from, point, this.canvasWidth(), this.canvasHeight());
    if (distance <= this.minPointDistancePx()) {
      return;
    }
    const steps = Math.max(1, Math.ceil(distance / this.interpolationStepPx()));
    for (let index = 1; index <= steps; index += 1) {
      const ratio = index / steps;
      points.push({
        x: from.x + (point.x - from.x) * ratio,
        y: from.y + (point.y - from.y) * ratio,
        t: Math.round(
          (Number.isFinite(Number(from.t)) ? Number(from.t) : Date.now()) + ((Number.isFinite(Number(point.t)) ? Number(point.t) : Date.now()) - (Number.isFinite(Number(from.t)) ? Number(from.t) : Date.now())) * ratio
        )
      });
    }
  }
  captureCanvasPointerGeometry() {
    const rect = this.canvas.getBoundingClientRect();
    return {
      rect: {
        left: rect.left,
        top: rect.top,
        width: rect.width,
        height: rect.height
      },
      canvasWidth: this.canvasWidth(),
      canvasHeight: this.canvasHeight(),
      canvasRenderHeight: this.canvasRenderHeight,
      canvasWindowTop: this.canvasWindowTop,
      responsiveLayoutContext: this.getResponsiveLayoutContext()
    };
  }
  eventToPoint(event, geometry = null) {
    const pointerGeometry = geometry || this.captureCanvasPointerGeometry();
    const mapped = mapClientPointToCanvas(event, pointerGeometry);
    const x = mapped.canvasX;
    const y = mapped.canvasY;
    const width = mapped.canvasWidth;
    const height = mapped.canvasHeight;
    const context = pointerGeometry.responsiveLayoutContext || this.getResponsiveLayoutContext();
    const lineLocation = this.captureLineLocation(x, y, context);
    return createResponsivePoint({
      canvasX: x,
      canvasY: y,
      canvasWidth: width,
      canvasHeight: height,
      frame: context.frame,
      sourcePath: lineLocation?.path || this.file?.path || "",
      linePosition: lineLocation?.line ?? null,
      time: Number.isFinite(Number(event.timeStamp)) ? Number(event.timeStamp) : Date.now()
    });
  }
  pointToCanvas(point) {
    return {
      x: point.x * this.canvasWidth(),
      y: point.y * this.canvasHeight()
    };
  }
  canvasWidth() {
    return Math.max(1, this.canvasCssWidth || this.canvas?.clientWidth || 1);
  }
  canvasHeight() {
    return Math.max(1, this.canvasCssHeight || this.canvas?.clientHeight || 1);
  }
  requestRender(refreshDom = false) {
    this.pendingDomRender = this.pendingDomRender || refreshDom === true;
    this.pendingInteractionRender = this.pendingInteractionRender || refreshDom === "interaction";
    if (this.renderFrameId !== null) {
      return;
    }
    this.renderFrameId = window.requestAnimationFrame(() => {
      this.renderFrameId = null;
      const shouldRefreshDom = this.pendingDomRender;
      const shouldRefreshInteraction = this.pendingInteractionRender;
      this.pendingDomRender = false;
      this.pendingInteractionRender = false;
      if (shouldRefreshDom) {
        this.render();
      } else if (shouldRefreshInteraction) {
        this.renderInteractionFrame();
      } else {
        this.renderCanvas();
      }
    });
  }
  cancelRenderFrame() {
    if (this.renderFrameId !== null) {
      window.cancelAnimationFrame(this.renderFrameId);
      this.renderFrameId = null;
    }
    this.pendingDomRender = false;
    this.pendingInteractionRender = false;
  }
  render() {
    if (!this.ctx) {
      return;
    }
    if (this.syncBoundConnectors()) {
      this.invalidateStaticCache();
    }
    this.applyWebEdits();
    this.syncMarkdownBlockPresentation();
    this.updateEmbedLayer();
    this.renderCanvas();
  }
  renderInteractionFrame() {
    if (!this.ctx) {
      return;
    }
    if (this.getSelectedMarkdownBlocks().length || this.usesDraggedNoteFlowPlacement()) {
      this.refreshMarkdownBlockPresentation();
    }
    const embedIndexes = this.getSelectedStrokeIndexes().filter((index) => {
      const stroke = this.drawingData.strokes[index];
      return isEmbedStroke(stroke) || isRichTextStroke(stroke);
    });
    if (embedIndexes.length) {
      this.updateEmbedLayer({ indexes: embedIndexes });
    }
    this.renderCanvas();
  }
  syncPendingDraggedConnectors() {
    if (!this.dragPendingConnectorIds?.size || !this.dragMovedElementIds?.size) {
      return false;
    }
    this.dragPendingConnectorIds = null;
    return this.syncBoundConnectors({ elementIds: this.dragMovedElementIds });
  }
  renderCanvas() {
    if (!this.ctx) {
      return;
    }
    if (this.draggingStroke && this.syncPendingDraggedConnectors()) {
      this.invalidateStaticCache();
    }
    if (!this.drawingsVisible) {
      clearCanvasContext(this.ctx, this.canvas);
      clearCanvasContext(this.underlayCtx, this.underlayCanvas);
      clearCanvasContext(this.staticCtx, this.staticCanvas);
      return;
    }
    const selectedUnderlayMoving = (this.draggingStroke || this.resizingSelection) && this.getSelectedStrokeIndexes().some((index) => {
      return shouldPlaceStrokeBelowMarkdown(this.drawingData.strokes[index]);
    });
    const currentUnderlayDrawing = Boolean(this.currentStroke && this.didMove && shouldPlaceStrokeBelowMarkdown(this.currentStroke));
    if (selectedUnderlayMoving || currentUnderlayDrawing) {
      this.invalidateStaticCache();
    }
    clearCanvasContext(this.ctx, this.canvas);
    this.ensureStaticCache();
    for (const [index, stroke] of this.drawingData.strokes.entries()) {
      if (this.isStrokeVisibleOnSurface(stroke) && this.isStrokeSelected(index) && !shouldPlaceStrokeBelowMarkdown(stroke) && this.isStrokeInCanvasWindow(stroke)) {
        this.drawStroke(stroke, this.selectedStrokeAlpha());
      }
    }
    this.drawSelection();
    const laserNow = Date.now();
    for (const stroke of this.laserStrokes) {
      const remaining = Math.max(0, Number(stroke.laserExpiresAt) - laserNow);
      const alpha = remaining >= 700 ? 1 : remaining / 700;
      if (alpha > 0) {
        this.drawStrokeOn(this.ctx, stroke, alpha);
      }
    }
    if (this.selectingStrokes && this.selectionStartPoint && this.selectionCurrentPoint) {
      this.drawSelectionDragRect(this.selectionStartPoint, this.selectionCurrentPoint);
    }
    if (this.currentStroke && this.didMove) {
      if (shouldPlaceStrokeBelowMarkdown(this.currentStroke)) {
        this.drawStrokeOn(this.underlayCtx, this.currentStroke);
      } else {
        this.drawStroke(this.currentStroke);
      }
    }
  }
  ensureStaticCache() {
    if (!this.staticCtx || !this.underlayCtx || !this.staticCacheDirty) {
      return;
    }
    clearCanvasContext(this.underlayCtx, this.underlayCanvas);
    clearCanvasContext(this.staticCtx, this.staticCanvas);
    this.drawElementGroupBackgrounds();
    for (const [index, stroke] of this.drawingData.strokes.entries()) {
      const belowMarkdown = shouldPlaceStrokeBelowMarkdown(stroke);
      if (!this.isStrokeVisibleOnSurface(stroke) || this.isStrokeSelected(index) && !belowMarkdown || !this.isStrokeInCanvasWindow(stroke)) {
        continue;
      }
      if (belowMarkdown) {
        this.drawStrokeOn(this.underlayCtx, stroke);
      } else {
        this.drawStrokeOn(this.staticCtx, stroke);
      }
      this.drawStaticStrokeBoxOn(belowMarkdown ? this.underlayCtx : this.staticCtx, stroke);
    }
    this.drawElementGroups(this.staticCtx);
    this.staticCacheDirty = false;
    this.underlayCacheDirty = false;
  }
  invalidateStaticCache() {
    this.staticCacheDirty = true;
    this.underlayCacheDirty = true;
  }
  isStrokeInCanvasWindow(stroke) {
    const bounds = getStrokeBounds(stroke, this.canvasWidth(), this.canvasHeight());
    if (!bounds) {
      return false;
    }
    const padding = Math.max(32, Number(stroke.width || 0) + this.selectionHitPaddingPx());
    return rectsIntersect(bounds, {
      minX: -padding,
      minY: this.canvasWindowTop - padding,
      maxX: this.canvasWidth() + padding,
      maxY: this.canvasWindowTop + this.canvasRenderHeight + padding
    });
  }
  isStrokeVisibleOnSurface(stroke) {
    return shouldRenderStrokeOnSurface(stroke, this.surfaceType);
  }
  drawStroke(stroke, alpha = 1) {
    this.drawStrokeOn(this.ctx, stroke, alpha);
  }
  updateEmbedLayer(options = {}) {
    if (!this.embedLayer || !this.drawingData?.strokes) {
      return;
    }
    const scopedIndexes = Array.isArray(options.indexes) ? Array.from(new Set(options.indexes)).filter((index) => Number.isInteger(index) && index >= 0 && index < this.drawingData.strokes.length) : null;
    const entries = scopedIndexes ? scopedIndexes.map((index) => [index, this.drawingData.strokes[index]]) : this.drawingData.strokes.entries();
    const liveKeys = /* @__PURE__ */ new Set();
    const width = this.canvasWidth();
    const height = this.canvasHeight();
    for (const [index, stroke] of entries) {
      if (!this.isStrokeVisibleOnSurface(stroke)) {
        continue;
      }
      if (!isEmbedStroke(stroke) && !isRichTextStroke(stroke)) {
        continue;
      }
      const belowMarkdown = shouldPlaceStrokeBelowMarkdown(stroke);
      if (belowMarkdown && isImageEmbedStroke(stroke)) {
        continue;
      }
      if (!stroke.points.length || this.floatingTextInput?.index === index) {
        continue;
      }
      const key = String(index);
      liveKeys.add(key);
      const bounds = getStrokeBounds(stroke, width, height);
      if (!bounds) {
        continue;
      }
      let node = this.embedNodes.get(key);
      const layer = belowMarkdown ? this.underlayEmbedLayer : this.embedLayer;
      if (!node) {
        node = layer.createDiv({ cls: "notedraw-embed" });
        this.embedNodes.set(key, node);
      } else if (node.parentElement !== layer) {
        layer.appendChild(node);
      }
      node.toggleClass("is-selected", this.isStrokeSelected(index));
      node.toggleClass("is-rich-text", isRichTextStroke(stroke));
      node.toggleClass("is-asset", isEmbedStroke(stroke));
      node.toggleClass("is-boxed", Boolean(stroke.boxed));
      node.toggleClass("is-locked", Boolean(stroke.locked));
      node.toggleClass("is-mind-map-node", Boolean(normalizeMindMapNode(stroke.mindMapNode)));
      node.toggleClass("is-below-markdown", belowMarkdown);
      node.dataset.noteDrawStrokeIndex = key;
      const geometry = {
        left: `${Math.round(bounds.minX)}px`,
        top: `${Math.round(bounds.minY)}px`,
        width: `${Math.max(32, Math.round(bounds.maxX - bounds.minX))}px`,
        height: `${Math.max(28, Math.round(bounds.maxY - bounds.minY))}px`,
        opacity: String(clamp10(Number(stroke.opacity ?? 1), 0, 1))
      };
      const geometryToken = `${geometry.left}|${geometry.top}|${geometry.width}|${geometry.height}|${geometry.opacity}|${strokePaletteColor(stroke, "#64748b")}`;
      if (this.embedGeometryTokens.get(key) !== geometryToken) {
        this.embedGeometryTokens.set(key, geometryToken);
        setNoteDrawCssProps(node, { "--notedraw-node-color": strokePaletteColor(stroke, "#64748b") });
        applyElementStyles(node, geometry);
      }
      this.renderEmbedNode(node, stroke, index);
    }
    if (!scopedIndexes) {
      for (const [key, node] of this.embedNodes.entries()) {
        if (!liveKeys.has(key)) {
          node.remove();
          this.embedNodes.delete(key);
          this.embedRenderTokens.delete(key);
          this.embedGeometryTokens.delete(key);
        }
      }
    }
  }
  renderEmbedNode(node, stroke, index) {
    const token = getEmbedRenderToken(stroke);
    const key = String(index);
    if (this.embedRenderTokens.get(key) === token) {
      return;
    }
    this.embedRenderTokens.set(key, token);
    node.empty();
    if (isRichTextStroke(stroke)) {
      this.renderRichTextEmbed(node, stroke, index).catch((error) => {
        console.error(`[${PLUGIN_ID}] Failed to render preview`, error);
        node.setText(String(stroke.text || ""));
      });
      return;
    }
    if (stroke.embedType === EMBED_IMAGE) {
      node.createEl("img", {
        attr: {
          alt: stroke.assetName || "Image",
          src: this.assetResourceUrl(stroke.assetPath || stroke.assetName)
        }
      });
      return;
    }
    if (stroke.embedType === EMBED_VIDEO) {
      node.createEl("video", {
        attr: {
          src: this.assetResourceUrl(stroke.assetPath || stroke.assetName),
          controls: "true",
          playsinline: "true"
        }
      });
      return;
    }
    const fileCard = node.createDiv({ cls: "notedraw-file-card" });
    const iconEl = fileCard.createSpan({ cls: "notedraw-file-icon" });
    (0, import_obsidian.setIcon)(iconEl, "paperclip");
    const body = fileCard.createDiv({ cls: "notedraw-file-body" });
    body.createDiv({ cls: "notedraw-file-name", text: stroke.assetName || stroke.text || "Attachment" });
    body.createDiv({ cls: "notedraw-file-meta", text: formatBytes(stroke.assetSize) });
  }
  async renderRichTextEmbed(node, stroke, index) {
    const isCommandButton = this.elementHasCommandFrame(stroke) && Boolean(String(stroke?.commandId || "").trim());
    node.toggleClass("is-command-button", isCommandButton);
    node.toggleAttribute("role", isCommandButton);
    node.toggleAttribute("tabindex", isCommandButton);
    if (isCommandButton) {
      node.setAttribute("role", "button");
      node.setAttribute("tabindex", "0");
      node.setAttribute("title", stroke.commandName || stroke.commandId);
    } else {
      node.removeAttribute("title");
    }
    node.onclick = (event) => {
      if (!isCommandButton || this.active || this.surfaceType !== "preview") {
        return;
      }
      event.preventDefault();
      event.stopPropagation();
      this.executeButtonCommand(index);
    };
    node.onkeydown = (event) => {
      if (!isCommandButton || this.active || this.surfaceType !== "preview" || !["Enter", " "].includes(event.key)) {
        return;
      }
      event.preventDefault();
      this.executeButtonCommand(index);
    };
    const renderMode = normalizeTextRenderMode(stroke.render);
    const content = String(stroke.text || "");
    if (renderMode === TEXT_RENDER_HTML) {
      node.appendChild(sanitizeHTMLToDomSafe(content));
      await this.plugin.hydratePortableMarkdownResources(node, this.file.path);
      return;
    }
    if (renderMode === TEXT_RENDER_NOTE) {
      const noteContent = await this.resolveNotePreviewContent(content);
      await import_obsidian.MarkdownRenderer.render(this.plugin.app, noteContent, node, this.file.path, this.plugin);
      return;
    }
    const sourcePath = normalizeMindMapNode(stroke.mindMapNode)?.sourcePath || this.file.path;
    await import_obsidian.MarkdownRenderer.render(this.plugin.app, content, node, sourcePath, this.plugin);
    this.bindMindMapTaskInteractions(node, stroke);
  }
  bindMindMapTaskInteractions(node, stroke) {
    if (!normalizeMindMapNode(stroke.mindMapNode) || normalizeMindMapNode(stroke.mindMapNode)?.type !== "task") {
      return;
    }
    node.querySelectorAll?.("input.task-list-item-checkbox, input[type='checkbox']").forEach((checkbox) => {
      checkbox.addEventListener("change", () => {
        const nextChecked = Boolean(checkbox.checked);
        const currentText = String(stroke.text || "");
        const nextText = currentText.replace(/(\[[ xX]\])/, nextChecked ? "[x]" : "[ ]");
        if (nextText === currentText) {
          return;
        }
        const historyBefore = this.captureDrawingHistorySnapshot();
        stroke.text = nextText;
        this.redoStack = [];
        this.invalidateStaticCache();
        this.plugin.scheduleDrawingSave(this.file, this.drawingData, { userOperation: true });
        this.render();
        if (normalizeMindMapNode(stroke.mindMapNode)?.affectsSource) {
          this.textCommitBarrier.track(this.syncMindMapNodeToSource(stroke, nextText.replace(/^\s*(?:[-+*]|\d+[.)])\s+/, ""), { drawingBefore: historyBefore }));
        } else {
          this.recordDrawingHistory(historyBefore);
        }
      });
    });
  }
  async resolveNotePreviewContent(text) {
    const link = String(text || "").trim();
    const normalized = unwrapWikiLink(link);
    const file2 = this.plugin.app.metadataCache.getFirstLinkpathDest(normalized, this.file.path) || getVaultFileByPath(this.plugin.app.vault, normalized);
    if (file2) {
      return this.plugin.app.vault.cachedRead(file2);
    }
    const portable = this.plugin.portableResource(this.file, normalized || link);
    if (portable && (String(portable.mime || "").startsWith("text/") || /\.md$/i.test(portable.name || ""))) {
      return portableResourceText(portable);
    }
    return `> ${link || "Note not found"}`;
  }
  assetResourceUrl(assetPath) {
    if (!assetPath) {
      return "";
    }
    const portable = this.plugin.portableResourceUrl(this.file, assetPath);
    if (portable) {
      return portable;
    }
    try {
      return this.plugin.app.vault.adapter.getResourcePath(normalizeVaultPath(assetPath));
    } catch (error) {
      void error;
      return "";
    }
  }
  drawStrokeOn(ctx, stroke, alpha = 1) {
    if (!stroke.points.length) {
      return;
    }
    if (isConnectorStroke(stroke)) {
      this.drawBoundConnectorOn(ctx, stroke, alpha);
      return;
    }
    if (isImageEmbedStroke(stroke)) {
      this.drawImageStrokeOn(ctx, stroke);
      return;
    }
    if (isEmbedStroke(stroke) || isRichTextStroke(stroke)) {
      return;
    }
    if (isTextStroke(stroke)) {
      this.drawTextStrokeOn(ctx, stroke, alpha);
      return;
    }
    if ((stroke.brush || BRUSH_PEN) === BRUSH_WATERCOLOR) {
      this.drawWatercolorStrokeOn(ctx, stroke, alpha);
      return;
    }
    if ([PEN_VARIANT_FOUNTAIN, PEN_VARIANT_NOTE].includes(normalizeBrushVariant(BRUSH_PEN, stroke.variant))) {
      this.drawFountainPenStrokeOn(ctx, stroke, alpha);
      return;
    }
    const count = clamp10(Math.round(Number(stroke.count || 1)), 1, MAX_PEN_COUNT);
    const opacity = clamp10(Number(stroke.opacity ?? DEFAULT_PEN_OPACITY), 0, 1);
    const offsets = getPenOffsets(count, stroke.width || this.penWidth);
    ctx.save();
    ctx.globalAlpha = alpha * opacity;
    ctx.lineCap = "round";
    ctx.lineJoin = "round";
    ctx.strokeStyle = strokePaletteColor(stroke, this.penColor);
    ctx.lineWidth = stroke.width || this.penWidth;
    for (const offset of offsets) {
      ctx.beginPath();
      const first = this.pointToCanvas(stroke.points[0]);
      ctx.moveTo(first.x + offset.x, first.y + offset.y);
      for (let pointIndex = 1; pointIndex < stroke.points.length; pointIndex += 1) {
        const next = this.pointToCanvas(stroke.points[pointIndex]);
        ctx.lineTo(next.x + offset.x, next.y + offset.y);
      }
      ctx.stroke();
    }
    ctx.restore();
  }
  syncBoundConnectors(options = {}) {
    const strokes = this.drawingData?.strokes || [];
    const recover = options.recover === true;
    const targetIds = options.elementIds instanceof Set ? options.elementIds : null;
    const useDragCache = this.draggingStroke && targetIds === this.dragMovedElementIds;
    let changed = recover ? this.ensureSnapElementIds() : false;
    const byId = options.byId instanceof Map ? options.byId : useDragCache && this.dragConnectorById instanceof Map ? this.dragConnectorById : new Map(strokes.map((stroke) => [strokeElementId(stroke), stroke]).filter(([id]) => id));
    const connectorStrokes = Array.isArray(options.strokes) ? options.strokes : useDragCache && Array.isArray(this.dragConnectorCandidates) ? this.dragConnectorCandidates : strokes;
    const needsExternalTargetBounds = recover || connectorStrokes.some((stroke) => {
      const connector = normalizeConnector(stroke?.connector);
      return connector && [connector.fromId, connector.toId].some((id) => /^(?:md|group):/.test(id));
    });
    const snapTargets = needsExternalTargetBounds ? this.collectSnapTargets() : [];
    const targetBounds = new Map(snapTargets.map((target) => [target.id, target.bounds]));
    for (const stroke of connectorStrokes) {
      let connector = normalizeConnector(stroke.connector);
      if (!connector) {
        continue;
      }
      if (targetIds && !targetIds.has(connector.fromId) && !targetIds.has(connector.toId)) {
        continue;
      }
      let fromBounds = getStrokeBounds(byId.get(connector.fromId), this.canvasWidth(), this.canvasHeight()) || targetBounds.get(connector.fromId);
      let toBounds = getStrokeBounds(byId.get(connector.toId), this.canvasWidth(), this.canvasHeight()) || targetBounds.get(connector.toId);
      if (recover && !fromBounds && stroke.points?.[0]) {
        const recoveredId = this.findSnapElementIdAtPoint(stroke.points[0], toBounds ? connector.toId : "", snapTargets);
        if (recoveredId) {
          connector = { ...connector, fromId: recoveredId };
          fromBounds = getStrokeBounds(byId.get(recoveredId), this.canvasWidth(), this.canvasHeight()) || targetBounds.get(recoveredId);
        }
      }
      if (recover && !toBounds && stroke.points?.length) {
        const recoveredId = this.findSnapElementIdAtPoint(stroke.points[stroke.points.length - 1], fromBounds ? connector.fromId : "", snapTargets);
        if (recoveredId && recoveredId !== connector.fromId) {
          connector = { ...connector, toId: recoveredId };
          toBounds = getStrokeBounds(byId.get(recoveredId), this.canvasWidth(), this.canvasHeight()) || targetBounds.get(recoveredId);
        }
      }
      if (connector.fromId === connector.toId) {
        connector = { ...connector, toId: "" };
        toBounds = null;
      }
      if (stroke.connector?.fromId !== connector.fromId || stroke.connector?.toId !== connector.toId) {
        stroke.connector = connector;
        changed = true;
      }
      if (!fromBounds && !toBounds) {
        continue;
      }
      const nextPoints = buildSnappedConnectorPoints({
        fromBounds,
        toBounds,
        fromPoint: stroke.points?.[0],
        toPoint: stroke.points?.[stroke.points.length - 1],
        canvasWidth: this.canvasWidth(),
        canvasHeight: this.canvasHeight()
      });
      if (!sameStrokePointPath(stroke.points, nextPoints)) {
        stroke.points = nextPoints;
        changed = true;
      }
    }
    if (!this.draggingStroke && options.syncGroups !== false && this.syncConnectorElementGroups()) {
      changed = true;
    }
    return changed;
  }
  drawBoundConnectorOn(ctx, stroke, alpha = 1) {
    if (!stroke.points?.length) {
      return;
    }
    const opacity = clamp10(Number(stroke.opacity ?? 0.9), 0, 1);
    const width = clamp10(Number(stroke.width || 2.25), 1, 8);
    const points = stroke.points.map((point) => this.pointToCanvas(point));
    ctx.save();
    ctx.globalAlpha = alpha * opacity;
    ctx.strokeStyle = strokePaletteColor(stroke, "#64748b");
    ctx.fillStyle = strokePaletteColor(stroke, "#64748b");
    ctx.lineWidth = width;
    ctx.lineCap = "round";
    ctx.lineJoin = "round";
    ctx.beginPath();
    ctx.moveTo(points[0].x, points[0].y);
    if (points.length === 3) {
      ctx.quadraticCurveTo(points[1].x, points[1].y, points[2].x, points[2].y);
    } else {
      for (let index = 1; index < points.length; index += 1) {
        ctx.lineTo(points[index].x, points[index].y);
      }
    }
    ctx.stroke();
    const first = points[0];
    const last = points[points.length - 1];
    const previous = points.length === 3 ? points[1] : points[Math.max(0, points.length - 3)];
    const angle = Math.atan2(last.y - previous.y, last.x - previous.x);
    const arrowSize = Math.max(6, width * 3.4);
    ctx.beginPath();
    ctx.moveTo(last.x, last.y);
    ctx.lineTo(last.x - Math.cos(angle - Math.PI / 6) * arrowSize, last.y - Math.sin(angle - Math.PI / 6) * arrowSize);
    ctx.lineTo(last.x - Math.cos(angle + Math.PI / 6) * arrowSize, last.y - Math.sin(angle + Math.PI / 6) * arrowSize);
    ctx.closePath();
    ctx.fill();
    ctx.beginPath();
    ctx.arc(first.x, first.y, Math.max(2.2, width * 1.15), 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
  }
  drawFountainPenStrokeOn(ctx, stroke, alpha = 1) {
    const segments = buildFountainPenSegments(stroke.points, {
      canvasWidth: this.canvasWidth(),
      canvasHeight: this.canvasHeight(),
      baseWidth: stroke.width || this.penWidth,
      baseOpacity: stroke.opacity ?? DEFAULT_PEN_OPACITY
    });
    if (!segments.length) {
      return;
    }
    ctx.save();
    ctx.globalAlpha = alpha * segments[0].opacity;
    ctx.fillStyle = strokePaletteColor(stroke, this.penColor);
    const centers = [segments[0].from, ...segments.map((segment) => segment.to)].map((point) => this.pointToCanvas(point));
    const widths = [segments[0].fromWidth, ...segments.map((segment) => segment.toWidth)];
    const left = [];
    const right = [];
    for (let index = 0; index < centers.length; index += 1) {
      const previous = centers[Math.max(0, index - 1)];
      const next = centers[Math.min(centers.length - 1, index + 1)];
      const dx = next.x - previous.x;
      const dy = next.y - previous.y;
      const length = Math.max(1e-3, Math.hypot(dx, dy));
      const radius = Math.max(0.04, Number(widths[index]) / 2);
      const normalX = -dy / length * radius;
      const normalY = dx / length * radius;
      left.push({ x: centers[index].x + normalX, y: centers[index].y + normalY });
      right.push({ x: centers[index].x - normalX, y: centers[index].y - normalY });
    }
    ctx.beginPath();
    ctx.moveTo(left[0].x, left[0].y);
    for (let index = 1; index < left.length - 1; index += 1) {
      const midpoint = {
        x: (left[index].x + left[index + 1].x) / 2,
        y: (left[index].y + left[index + 1].y) / 2
      };
      if (typeof ctx.quadraticCurveTo === "function") {
        ctx.quadraticCurveTo(left[index].x, left[index].y, midpoint.x, midpoint.y);
      } else {
        ctx.lineTo(midpoint.x, midpoint.y);
      }
    }
    ctx.lineTo(left.at(-1).x, left.at(-1).y);
    ctx.lineTo(right.at(-1).x, right.at(-1).y);
    for (let index = right.length - 2; index > 0; index -= 1) {
      const midpoint = {
        x: (right[index].x + right[index - 1].x) / 2,
        y: (right[index].y + right[index - 1].y) / 2
      };
      if (typeof ctx.quadraticCurveTo === "function") {
        ctx.quadraticCurveTo(right[index].x, right[index].y, midpoint.x, midpoint.y);
      } else {
        ctx.lineTo(midpoint.x, midpoint.y);
      }
    }
    ctx.lineTo(right[0].x, right[0].y);
    const start = centers[0];
    const firstNext = centers[1] || start;
    const startLength = Math.max(1e-3, Math.hypot(firstNext.x - start.x, firstNext.y - start.y));
    const startRadius = Math.max(0.04, Number(widths[0]) / 2);
    const capX = start.x - (firstNext.x - start.x) / startLength * startRadius;
    const capY = start.y - (firstNext.y - start.y) / startLength * startRadius;
    if (typeof ctx.quadraticCurveTo === "function") {
      ctx.quadraticCurveTo(capX, capY, left[0].x, left[0].y);
    }
    ctx.closePath();
    ctx.fill();
    ctx.restore();
  }
  drawImageStrokeOn(ctx, stroke) {
    const image = this.getCanvasImageForStroke(stroke);
    if (!image || !image.complete || !image.naturalWidth || !image.naturalHeight) {
      return;
    }
    const bounds = getStrokeBounds(stroke, this.canvasWidth(), this.canvasHeight());
    if (!bounds) {
      return;
    }
    const x = Math.round(bounds.minX);
    const y = Math.round(bounds.minY);
    const width = Math.max(1, Math.round(bounds.maxX - bounds.minX));
    const height = Math.max(1, Math.round(bounds.maxY - bounds.minY));
    const fit = objectFitContain(image.naturalWidth, image.naturalHeight, width, height);
    ctx.save();
    ctx.globalAlpha = clamp10(Number(stroke.opacity ?? 1), 0, 1);
    ctx.fillStyle = "#fff";
    ctx.fillRect(x, y, width, height);
    ctx.imageSmoothingEnabled = true;
    ctx.imageSmoothingQuality = "high";
    ctx.drawImage(image, x + fit.x, y + fit.y, fit.width, fit.height);
    ctx.restore();
  }
  getCanvasImageForStroke(stroke) {
    const key = getImageStrokeCacheKey(stroke);
    if (!key) {
      return null;
    }
    const cached = this.canvasImageCache.get(key);
    if (cached?.image) {
      return cached.image;
    }
    const image = new Image();
    image.decoding = "sync";
    image.onload = () => {
      if (this.destroyed) {
        return;
      }
      this.invalidateStaticCache();
      this.requestRender();
    };
    image.onerror = () => {
      this.canvasImageCache.delete(key);
    };
    this.canvasImageCache.set(key, { image });
    const embedded = normalizeImageDataUrl(stroke.exportImageDataUrl);
    if (embedded) {
      image.src = embedded;
      return image;
    }
    this.plugin.assetDataUrl(stroke.assetPath, stroke.assetMime || guessMimeType(stroke.assetName || stroke.assetPath), this.file).then((dataUrl) => {
      if (this.destroyed) {
        return;
      }
      const state = this.canvasImageCache.get(key);
      if (state?.image === image && dataUrl) {
        image.src = dataUrl;
      }
    });
    return image;
  }
  drawTextStrokeOn(ctx, stroke, alpha = 1) {
    const text = String(stroke.text || "").trim();
    if (!text || !stroke.points.length) {
      return;
    }
    const point = this.pointToCanvas(stroke.points[0]);
    const fontSize = clamp10(Number(stroke.fontSize || 18), 10, 72);
    const opacity = clamp10(Number(stroke.opacity ?? 1), 0, 1);
    ctx.save();
    ctx.globalAlpha = alpha * opacity;
    ctx.font = `${stroke.bold ? "700 " : ""}${fontSize}px ${stroke.code ? "monospace" : "sans-serif"}`;
    ctx.textBaseline = "top";
    ctx.fillStyle = strokePaletteColor(stroke, this.penColor);
    const layout = getTextStrokeLayout(stroke, this.canvasWidth(), (value) => ctx.measureText(value).width);
    if (layout.paddingX || layout.paddingY) {
      const style = normalizeButtonStyle(stroke.buttonStyle);
      if (style === "solid") {
        ctx.fillStyle = strokePaletteColor(stroke, this.penColor);
      } else if (style === "outline") {
        ctx.fillStyle = "rgba(255, 255, 255, 0.82)";
      } else {
        ctx.fillStyle = stroke.code ? "rgba(127, 127, 127, 0.14)" : "rgba(255, 255, 255, 0.74)";
      }
      ctx.strokeStyle = strokePaletteColor(stroke, this.penColor);
      ctx.lineWidth = 1.25;
      if (style === "circle") {
        ctx.beginPath();
        ctx.arc(point.x - layout.paddingX + layout.width / 2, point.y - layout.paddingY + layout.height / 2, layout.width / 2, 0, Math.PI * 2);
      } else {
        const radius = style === "pill" ? Math.min(999, layout.height / 2) : 6;
        roundRect(ctx, point.x - layout.paddingX, point.y - layout.paddingY, layout.width, layout.height, radius);
      }
      ctx.fill();
      ctx.stroke();
    }
    ctx.fillStyle = normalizeButtonStyle(stroke.buttonStyle) === "solid" ? "#ffffff" : strokePaletteColor(stroke, this.penColor);
    layout.lines.forEach((line, index) => {
      ctx.fillText(line, point.x, point.y + index * layout.lineHeight);
    });
    ctx.restore();
  }
  drawWatercolorStroke(stroke, alpha = 1) {
    this.drawWatercolorStrokeOn(this.ctx, stroke, alpha);
  }
  drawWatercolorStrokeOn(ctx, stroke, alpha = 1) {
    if (!stroke.points.length) {
      return;
    }
    const width = Math.max(MIN_BRUSH_WIDTH, stroke.width || this.penWidth);
    const opacity = clamp10(Number(stroke.opacity ?? 0.45), 0, 1);
    ctx.save();
    ctx.globalAlpha = alpha * opacity;
    ctx.lineCap = "round";
    ctx.lineJoin = "round";
    ctx.strokeStyle = strokePaletteColor(stroke, this.penColor);
    ctx.lineWidth = width;
    ctx.beginPath();
    const first = this.pointToCanvas(stroke.points[0]);
    ctx.moveTo(first.x, first.y);
    for (let pointIndex = 1; pointIndex < stroke.points.length; pointIndex += 1) {
      const next = this.pointToCanvas(stroke.points[pointIndex]);
      ctx.lineTo(next.x, next.y);
    }
    ctx.stroke();
    ctx.restore();
  }
  drawStrokeSegment(stroke, fromPoint, toPoint) {
    if (!fromPoint || !toPoint || !this.ctx) {
      return;
    }
    const from = this.pointToCanvas(fromPoint);
    const to = this.pointToCanvas(toPoint);
    this.ctx.save();
    this.ctx.lineCap = "round";
    this.ctx.lineJoin = "round";
    this.ctx.strokeStyle = strokePaletteColor(stroke, this.penColor);
    this.ctx.lineWidth = stroke.width || this.penWidth;
    this.ctx.beginPath();
    this.ctx.moveTo(from.x, from.y);
    this.ctx.lineTo(to.x, to.y);
    this.ctx.stroke();
    this.ctx.restore();
  }
  drawSelection() {
    if (!this.hasHybridSelection()) {
      return;
    }
    let frame = this.getVisibleSelectionFrameCanvasRect();
    if (!frame) {
      return;
    }
    frame = clampCanvasFrameForDisplay(frame, this.canvasWidth(), this.canvasHeight());
    if (!frame) {
      return;
    }
    const { x, y, width, height } = frame;
    const dragging = this.draggingStroke || this.resizingSelection;
    this.ctx.save();
    this.ctx.strokeStyle = SELECTION_FRAME_COLOR;
    this.ctx.lineWidth = dragging ? SELECTION_FRAME_LINE_WIDTH + 1.25 : SELECTION_FRAME_LINE_WIDTH;
    this.ctx.lineCap = "round";
    this.ctx.lineJoin = "round";
    this.ctx.setLineDash([3, 2]);
    if (dragging) {
      this.ctx.fillStyle = SELECTION_FRAME_DRAG_FILL;
      roundRect(this.ctx, frame.x, frame.y, frame.width, frame.height, this.frameCornerRadius());
      this.ctx.fill();
    }
    roundRect(this.ctx, frame.x, frame.y, frame.width, frame.height, this.frameCornerRadius());
    this.ctx.stroke();
    this.ctx.setLineDash([]);
    this.ctx.fillStyle = "rgba(255, 255, 255, 0.96)";
    this.ctx.strokeStyle = SELECTION_FRAME_COLOR;
    this.ctx.lineWidth = SELECTION_FRAME_LINE_WIDTH;
    for (const handle of getSelectionHandlePointsFromRect({ x, y, width, height })) {
      this.ctx.fillRect(
        handle.x - SELECT_RESIZE_HANDLE_SIZE / 2,
        handle.y - SELECT_RESIZE_HANDLE_SIZE / 2,
        SELECT_RESIZE_HANDLE_SIZE,
        SELECT_RESIZE_HANDLE_SIZE
      );
      this.ctx.strokeRect(
        handle.x - SELECT_RESIZE_HANDLE_SIZE / 2,
        handle.y - SELECT_RESIZE_HANDLE_SIZE / 2,
        SELECT_RESIZE_HANDLE_SIZE,
        SELECT_RESIZE_HANDLE_SIZE
      );
    }
    const selectedConnectors = this.getSelectedStrokeIndexes().map((index) => this.drawingData?.strokes?.[index]).filter((stroke) => isConnectorStroke(stroke) && stroke.points?.length);
    for (const stroke of selectedConnectors) {
      for (const point of [stroke.points[0], stroke.points[stroke.points.length - 1]]) {
        const endpoint = this.pointToCanvas(point);
        this.ctx.beginPath();
        this.ctx.arc(endpoint.x, endpoint.y, 5, 0, Math.PI * 2);
        this.ctx.fill();
        this.ctx.stroke();
      }
    }
    this.ctx.restore();
  }
  drawElementGroups(ctx = this.ctx) {
    if (!ctx) {
      return;
    }
    for (const group of this.elementGroupRecords().filter((item) => item.boxed || item.locked && this.groupFrameShouldBeVisible(item.id))) {
      const frame = clampCanvasFrameForDisplay(this.elementGroupFrameRect(group.id), this.canvasWidth(), this.canvasHeight());
      if (!frame) {
        continue;
      }
      ctx.save();
      ctx.globalAlpha = (group.boxed ? 0.92 : 0.58) * clamp10(Number(group.opacity ?? 1), 0, 1);
      ctx.strokeStyle = group.boxed ? group.borderColor || SELECTION_FRAME_COLOR : "#8b8b96";
      ctx.lineWidth = group.boxed ? SELECTION_FRAME_LINE_WIDTH : 1.25;
      ctx.setLineDash(group.boxed ? [] : [3, 2]);
      roundRect(ctx, frame.x, frame.y, frame.width, frame.height, this.frameCornerRadius());
      ctx.stroke();
      ctx.restore();
    }
  }
  drawStaticStrokeBoxOn(ctx, stroke) {
    if (!ctx || !stroke?.boxed || isTextLikeStroke(stroke) || isConnectorStroke(stroke) || isEmbedStroke(stroke)) {
      return;
    }
    const bounds = getStrokeBounds(stroke, this.canvasWidth(), this.canvasHeight());
    const frame = bounds ? clampCanvasFrameForDisplay({
      x: bounds.minX,
      y: bounds.minY,
      width: bounds.maxX - bounds.minX,
      height: bounds.maxY - bounds.minY
    }, this.canvasWidth(), this.canvasHeight()) : null;
    if (!frame) {
      return;
    }
    ctx.save();
    ctx.globalAlpha = clamp10(Number(stroke.opacity ?? 1), 0, 1);
    ctx.strokeStyle = strokePaletteColor(stroke, this.penColor);
    ctx.lineWidth = 1.25;
    ctx.setLineDash([]);
    roundRect(ctx, frame.x, frame.y, frame.width, frame.height, this.frameCornerRadius());
    ctx.stroke();
    ctx.restore();
  }
  drawElementGroupBackgrounds() {
    if (!this.underlayCtx) {
      return;
    }
    for (const group of this.elementGroupRecords().filter((item) => item.boxed && item.backgroundColor)) {
      const frame = clampCanvasFrameForDisplay(this.elementGroupFrameRect(group.id), this.canvasWidth(), this.canvasHeight());
      if (!frame) {
        continue;
      }
      this.underlayCtx.save();
      this.underlayCtx.globalAlpha = 0.14 * clamp10(Number(group.opacity ?? 1), 0, 1);
      this.underlayCtx.fillStyle = group.backgroundColor;
      roundRect(
        this.underlayCtx,
        frame.x,
        frame.y,
        frame.width,
        frame.height,
        this.frameCornerRadius()
      );
      this.underlayCtx.fill();
      this.underlayCtx.restore();
    }
  }
  drawSelectionDragRect(startPoint, endPoint) {
    const start = this.pointToCanvas(startPoint);
    const end = this.pointToCanvas(endPoint);
    const x = Math.min(start.x, end.x);
    const y = Math.min(start.y, end.y);
    const width = Math.abs(end.x - start.x);
    const height = Math.abs(end.y - start.y);
    this.ctx.save();
    this.ctx.strokeStyle = "rgba(96, 165, 250, 0.95)";
    this.ctx.fillStyle = "rgba(96, 165, 250, 0.12)";
    this.ctx.lineWidth = 2;
    this.ctx.setLineDash([5, 4]);
    this.ctx.fillRect(x, y, width, height);
    this.ctx.strokeRect(x, y, width, height);
    this.ctx.restore();
  }
  findStrokeAt(point, clientPoint = null) {
    const hitPoint = this.pointToCanvas(point);
    const width = this.canvasWidth();
    const height = this.canvasHeight();
    let boxHit = -1;
    for (let index = this.drawingData.strokes.length - 1; index >= 0; index -= 1) {
      const stroke = this.drawingData.strokes[index];
      if (!this.isStrokeVisibleOnSurface(stroke)) {
        continue;
      }
      const padding = this.selectionHitPaddingPx();
      const threshold = Math.max(padding, (stroke.width || this.penWidth) / 2 + padding);
      const domNode = isEmbedStroke(stroke) || isRichTextStroke(stroke) ? this.embedNodes?.get?.(String(index)) : null;
      const domRect = domNode?.isConnected ? domNode.getBoundingClientRect?.() : null;
      const hit = domRect && clientPoint ? clientPointInRect(domRect, clientPoint) : strokeHitTest(stroke, hitPoint, width, height, threshold);
      if (!hit) {
        continue;
      }
      if (isTextLikeStroke(stroke) || isEmbedStroke(stroke)) {
        if (boxHit < 0) {
          boxHit = index;
        }
      } else {
        return index;
      }
    }
    return boxHit;
  }
  findNoteFlowOwnerStrokeIndex(record = {}) {
    const strokes = this.drawingData?.strokes || [];
    if (record.ownerId) {
      const exact = strokes.findIndex((stroke) => strokeElementId(stroke) === record.ownerId && this.isStrokeVisibleOnSurface(stroke));
      if (exact >= 0) {
        return exact;
      }
    }
    const path = normalizeVaultPath(record.path || this.file?.path || "");
    const line = Number(record.line);
    const side = record.side === "after" || record.property === "padding-bottom" ? "after" : "before";
    const recordBlockKey = noteFlowBlockKey(record, this.file?.path || "");
    const matches = [];
    for (const [index, stroke] of strokes.entries()) {
      const flow = normalizeNoteFlow(stroke?.noteFlow);
      if (!flow || !this.isStrokeVisibleOnSurface(stroke)) {
        continue;
      }
      const bounds = getStrokeBounds(stroke, this.canvasWidth(), this.canvasHeight());
      const candidate = { index, bottom: bounds?.maxY || 0 };
      const avoidance = noteFlowAvoidanceReference(flow, this.file?.path || "");
      const anchorPath = normalizeVaultPath(flow.path || this.file?.path || "");
      const anchorLine = Number(flow.line);
      const anchorBlockKey = noteFlowBlockKey(flow, this.file?.path || "");
      if (avoidance?.path === path && avoidance.line === line || anchorPath === path && (recordBlockKey && anchorBlockKey === recordBlockKey || anchorLine === line) && (flow.side || "before") === side) {
        matches.push(candidate);
      }
    }
    matches.sort((a, b) => b.bottom - a.bottom || b.index - a.index);
    return matches[0]?.index ?? -1;
  }
  readingBottomOwnerStrokeIndex() {
    const ranked = (this.drawingData?.strokes || []).map((stroke, index) => ({ stroke, index })).filter(({ stroke }) => {
      return this.isStrokeVisibleOnSurface(stroke);
    }).map(({ stroke, index }) => ({
      index,
      bottom: getStrokeBounds(stroke, this.canvasWidth(), this.canvasHeight())?.maxY || 0
    })).sort((a, b) => b.bottom - a.bottom || b.index - a.index);
    return ranked[0]?.index ?? -1;
  }
  findOwnedBlankSpaceStrokeAtClientPoint(clientX, clientY) {
    if (!this.drawingsVisible || !this.drawingData) {
      return -1;
    }
    const candidates = [];
    for (const [element, states] of this.noteFlowStyledElements || []) {
      const rect = element?.getBoundingClientRect?.();
      if (!element?.isConnected || !rect || rect.width <= 0 || rect.height <= 0) {
        continue;
      }
      const localHeight = Number(element.offsetHeight) || 0;
      const scale = localHeight > 0 ? rect.height / localHeight : this.readingZoomScale();
      for (const state of states.values()) {
        const ownerIndex = Number.isInteger(state.ownerStrokeIndex) && state.ownerStrokeIndex >= 0 ? state.ownerStrokeIndex : this.findNoteFlowOwnerStrokeIndex(state.ownerRecord);
        const owner = this.drawingData.strokes[ownerIndex];
        if (ownerIndex < 0 || !owner || !this.isStrokeVisibleOnSurface(owner)) {
          continue;
        }
        const ownerBounds = getStrokeBounds(owner, this.canvasWidth(), this.canvasHeight());
        const canvasRect = this.canvas?.getBoundingClientRect?.();
        const ownerPadding = Math.max(4, Number(owner.width) / 2 || 0);
        const ownerRect = ownerBounds && canvasRect?.width > 0 && canvasRect?.height > 0 ? {
          left: canvasRect.left + (ownerBounds.minX - ownerPadding) * canvasRect.width / this.canvasWidth(),
          right: canvasRect.left + (ownerBounds.maxX + ownerPadding) * canvasRect.width / this.canvasWidth()
        } : null;
        candidates.push({
          ownerIndex,
          rect,
          ownerRect,
          property: state.property,
          styleProperty: state.styleProperty,
          applied: state.applied,
          scale
        });
      }
    }
    if (this.readingBottomSpacer?.isConnected && this.readingBottomSpacerHeight > 0) {
      const rect = this.readingBottomSpacer.getBoundingClientRect?.();
      const surfaceRect = (this.layoutMeasureEl?.isConnected ? this.layoutMeasureEl : this.previewEl)?.getBoundingClientRect?.();
      const ownerIndex = this.readingBottomOwnerStrokeIndex();
      if (rect?.height > 0 && ownerIndex >= 0) {
        const localHeight = Number(this.readingBottomSpacer.offsetHeight) || this.readingBottomSpacerHeight;
        candidates.push({
          ownerIndex,
          rect: {
            left: surfaceRect?.left ?? rect.left,
            right: surfaceRect?.right ?? rect.right,
            top: rect.top,
            bottom: rect.bottom
          },
          property: "height",
          styleProperty: "height",
          applied: this.readingBottomSpacerHeight,
          scale: localHeight > 0 ? rect.height / localHeight : 1
        });
      }
    }
    return selectOwnedBlankSpaceCandidate(candidates, { clientX, clientY })?.ownerIndex ?? -1;
  }
  findStrokesInSelection(startPoint, endPoint) {
    const start = this.pointToCanvas(startPoint);
    const end = this.pointToCanvas(endPoint);
    const rect = normalizeCanvasRect(start, end);
    const indexes = [];
    for (let index = 0; index < this.drawingData.strokes.length; index += 1) {
      const stroke = this.drawingData.strokes[index];
      if (!this.isStrokeVisibleOnSurface(stroke)) {
        continue;
      }
      const bounds = getStrokeBounds(stroke, this.canvasWidth(), this.canvasHeight());
      if (bounds && rectsIntersect(rect, bounds)) {
        indexes.push(index);
      }
    }
    return indexes;
  }
  findMarkdownBlocksInSelection(startPoint, endPoint) {
    if (this.surfaceType !== "preview") {
      return [];
    }
    const selection = normalizeCanvasRect(this.pointToCanvas(startPoint), this.pointToCanvas(endPoint));
    const ids = [];
    const seen = /* @__PURE__ */ new Set();
    const candidates = markdownBlockCandidateElements(this.previewEl);
    for (const candidate of candidates) {
      const element = candidate;
      const bounds = this.markdownElementCanvasBounds(element, { forSelection: true });
      if (!bounds || !rectsIntersect(selection, bounds)) {
        continue;
      }
      const block = this.ensureMarkdownBlockRecord(element);
      if (block && !seen.has(block.id)) {
        seen.add(block.id);
        ids.push(block.id);
      }
    }
    return ids;
  }
  markdownBlockRecords() {
    if (!Array.isArray(this.drawingData?.markdownBlocks)) {
      this.drawingData.markdownBlocks = [];
    }
    return this.drawingData.markdownBlocks;
  }
  elementGroupRecords() {
    if (!Array.isArray(this.drawingData?.elementGroups)) {
      this.drawingData.elementGroups = [];
    }
    return this.drawingData.elementGroups;
  }
  syncConnectorElementGroups() {
    const strokes = this.drawingData?.strokes || [];
    const strokeNodes = strokes.filter((stroke) => !isConnectorStroke(stroke) && strokeElementId(stroke)).map((stroke) => ({
      id: strokeElementId(stroke),
      stroke,
      block: null
    }));
    const markdownNodes = this.markdownBlockRecords().filter((block) => block?.id && (block.borderColor || block.backgroundColor)).map((block) => ({
      id: `md:${block.id}`,
      stroke: null,
      block
    }));
    const nodes = [...strokeNodes, ...markdownNodes];
    if (!nodes.length) {
      return false;
    }
    const parent = new Map(nodes.map((node) => [node.id, node.id]));
    const find = (id) => {
      let root = parent.get(id) || id;
      while (parent.get(root) && parent.get(root) !== root) {
        root = parent.get(root);
      }
      let current = id;
      while (parent.get(current) && parent.get(current) !== root) {
        const next = parent.get(current);
        parent.set(current, root);
        current = next;
      }
      return root;
    };
    const join = (fromId, toId) => {
      if (!parent.has(fromId) || !parent.has(toId)) {
        return;
      }
      const fromRoot = find(fromId);
      const toRoot = find(toId);
      if (fromRoot !== toRoot) {
        parent.set(toRoot, fromRoot < toRoot ? fromRoot : toRoot);
      }
    };
    for (const stroke of strokes) {
      const connector = normalizeConnector(stroke?.connector);
      if (connector?.fromId && connector?.toId) {
        join(connector.fromId, connector.toId);
      }
    }
    const connectedIds = /* @__PURE__ */ new Set();
    for (const stroke of strokes) {
      const connector = normalizeConnector(stroke?.connector);
      if (connector?.fromId && connector?.toId && parent.has(connector.fromId) && parent.has(connector.toId)) {
        connectedIds.add(connector.fromId);
        connectedIds.add(connector.toId);
      }
    }
    const components = /* @__PURE__ */ new Map();
    for (const node of nodes) {
      const id = node.id;
      if (!connectedIds.has(id)) {
        continue;
      }
      const root = find(id);
      const members = components.get(root) || [];
      members.push(node);
      components.set(root, members);
    }
    const flowComponents = /* @__PURE__ */ new Map();
    for (const stroke of strokes.filter((item) => !isConnectorStroke(item) && strokeElementId(item) && isNoteFlowInkStroke(item))) {
      const flow = normalizeNoteFlow(stroke.noteFlow);
      if (!flow || flow.blockStart === null && !Number.isFinite(flow.line)) {
        continue;
      }
      const key = flow?.blockKey || `${flow?.path || this.file?.path || ""}:${flow?.line ?? "x"}`;
      if (!key) {
        continue;
      }
      const members = flowComponents.get(key) || [];
      members.push(stroke);
      flowComponents.set(key, members);
    }
    const desired = /* @__PURE__ */ new Map();
    for (const members of components.values()) {
      if (members.length < 2) {
        continue;
      }
      const ids = members.map((member) => member.id).sort();
      desired.set(`connector:${ids[0]}`, { members, relationOwned: true });
    }
    for (const members of flowComponents.values()) {
      if (members.length < 2) {
        continue;
      }
      const ids = members.map(strokeElementId).sort();
      desired.set(`flow:${ids[0]}`, { members: members.map((stroke) => ({ id: strokeElementId(stroke), stroke, block: null })), relationOwned: true });
    }
    const existingByRelation = new Map(this.elementGroupRecords().filter((group) => group.relationOwned).map((group) => [group.relationKey, group]));
    const usedGroupIds = /* @__PURE__ */ new Set();
    const desiredMemberIdsByGroup = /* @__PURE__ */ new Map();
    let changed = false;
    for (const [relationKey, record] of desired) {
      const existing = existingByRelation.get(relationKey);
      const groupId = existing?.id || `relation-${hashString(`${this.file?.path || ""}:${relationKey}`)}`;
      let group = this.elementGroup(groupId);
      if (!group) {
        group = {
          id: groupId,
          boxed: false,
          locked: true,
          boxOwnedLock: false,
          relationOwned: true,
          relationKey,
          borderColor: "#64748b",
          backgroundColor: ""
        };
        this.elementGroupRecords().push(group);
        changed = true;
      } else if (!group.relationOwned || group.relationKey !== relationKey || !group.locked) {
        group.relationOwned = true;
        group.relationKey = relationKey;
        group.locked = true;
        changed = true;
      }
      usedGroupIds.add(groupId);
      desiredMemberIdsByGroup.set(groupId, new Set(record.members.map((member) => member.id)));
      for (const member of record.members) {
        const element = member.stroke || member.block;
        if (element.groupId !== groupId || !element.locked) {
          element.groupId = groupId;
          element.locked = true;
          changed = true;
        }
      }
    }
    for (const group of this.elementGroupRecords().filter((item) => item.relationOwned && usedGroupIds.has(item.id))) {
      const desiredIds = desiredMemberIdsByGroup.get(group.id) || /* @__PURE__ */ new Set();
      for (const stroke of strokes) {
        if (stroke.groupId === group.id && !desiredIds.has(strokeElementId(stroke))) {
          stroke.groupId = "";
          stroke.locked = false;
          changed = true;
        }
      }
      for (const block of this.markdownBlockRecords()) {
        if (block.groupId === group.id && !desiredIds.has(`md:${block.id}`)) {
          block.groupId = "";
          block.locked = false;
          changed = true;
        }
      }
    }
    for (const group of this.elementGroupRecords().filter((item) => item.relationOwned)) {
      if (usedGroupIds.has(group.id)) {
        continue;
      }
      for (const stroke of strokes) {
        if (stroke.groupId === group.id) {
          stroke.groupId = "";
          stroke.locked = false;
          changed = true;
        }
      }
      for (const block of this.markdownBlockRecords()) {
        if (block.groupId === group.id) {
          block.groupId = "";
          block.locked = false;
          changed = true;
        }
      }
    }
    const liveIds = new Set([
      ...strokes.map((stroke) => stroke?.groupId),
      ...this.markdownBlockRecords().map((block) => block?.groupId)
    ].filter(Boolean));
    const nextGroups = this.elementGroupRecords().filter((group) => liveIds.has(group.id));
    if (nextGroups.length !== this.elementGroupRecords().length) {
      this.drawingData.elementGroups = nextGroups;
      changed = true;
    }
    if (changed) {
      this.invalidateStaticCache();
    }
    return changed;
  }
  elementGroup(groupId) {
    return this.elementGroupRecords().find((group) => group.id === groupId) || null;
  }
  canManipulateGroupedElement(item) {
    if (!item?.locked) {
      return true;
    }
    const groupId = String(item.groupId || "");
    return Boolean(groupId && (this.enteredElementGroupIds.has(groupId) || this.isElementGroupFullySelected(groupId)));
  }
  groupMemberStrokeIndexes(groupId) {
    const indexes = [];
    for (const [index, stroke] of this.drawingData.strokes.entries()) {
      if (stroke?.groupId === groupId) {
        indexes.push(index);
      }
    }
    return indexes;
  }
  groupMemberMarkdownBlocks(groupId) {
    return this.markdownBlockRecords().filter((block) => block.groupId === groupId);
  }
  getElementGroupBounds(groupId) {
    let result = this.getStrokeIndexesBounds(this.groupMemberStrokeIndexes(groupId));
    for (const block of this.groupMemberMarkdownBlocks(groupId)) {
      const bounds = this.markdownElementCanvasBounds(this.markdownBlockElement(block), { forSelection: true });
      if (!bounds) {
        continue;
      }
      result = result ? {
        minX: Math.min(result.minX, bounds.minX),
        maxX: Math.max(result.maxX, bounds.maxX),
        minY: Math.min(result.minY, bounds.minY),
        maxY: Math.max(result.maxY, bounds.maxY)
      } : { ...bounds };
    }
    return result;
  }
  elementGroupFramePaddingPx(groupId) {
    return this.elementFramePaddingPx(
      this.groupMemberStrokeIndexes(groupId),
      this.groupMemberMarkdownBlocks(groupId).length
    );
  }
  elementGroupFrameRect(groupId) {
    const bounds = this.getElementGroupBounds(groupId);
    if (!bounds) {
      return null;
    }
    const padding = this.elementGroupFramePaddingPx(groupId);
    let minX = bounds.minX - padding;
    let maxX = bounds.maxX + padding;
    const strokes = this.groupMemberStrokeIndexes(groupId);
    const blocks = this.groupMemberMarkdownBlocks(groupId);
    if (!strokes.length && blocks.length === 1 && Number(blocks[0].span || 12) < 12) {
      const element = this.markdownBlockElement(blocks[0]);
      const canvasRect = this.canvas?.getBoundingClientRect?.();
      const elementRect = this.markdownElementVisibleClientRect(element) || element?.getBoundingClientRect?.();
      if (canvasRect?.width > 0 && elementRect?.width > 0) {
        const limits = this.markdownInlineSelectionClientLimits(element, elementRect, { includeSelectedPeers: true });
        const scaleX = this.canvasWidth() / canvasRect.width;
        if (Number.isFinite(limits.left)) {
          minX = Math.max(minX, (limits.left - canvasRect.left) * scaleX);
        }
        if (Number.isFinite(limits.right)) {
          maxX = Math.min(maxX, (limits.right - canvasRect.left) * scaleX);
        }
      }
    }
    if (maxX <= minX) {
      minX = bounds.minX;
      maxX = bounds.maxX;
    }
    return {
      x: minX,
      y: bounds.minY - padding,
      width: maxX - minX,
      height: bounds.maxY - bounds.minY + padding * 2
    };
  }
  groupFrameShouldBeVisible(groupId) {
    if (!groupId) {
      return false;
    }
    return this.groupMemberStrokeIndexes(groupId).some((index) => this.isStrokeSelected(index)) || this.groupMemberMarkdownBlocks(groupId).some((block) => this.selectedMarkdownBlockIds.has(block.id));
  }
  findBoxedElementGroupAtPoint(point) {
    const hit = this.pointToCanvas(point);
    const groups = this.elementGroupRecords().filter((group) => group.boxed);
    for (let index = groups.length - 1; index >= 0; index -= 1) {
      if (this.elementGroupFrameBorderContains(groups[index].id, hit)) {
        return groups[index];
      }
    }
    return null;
  }
  findLockedElementGroupFrameAtPoint(point) {
    const hit = this.pointToCanvas(point);
    const groups = this.elementGroupRecords().filter((group) => group.locked && this.groupFrameShouldBeVisible(group.id) && this.groupMemberStrokeIndexes(group.id).length + this.groupMemberMarkdownBlocks(group.id).length > 1);
    for (let index = groups.length - 1; index >= 0; index -= 1) {
      const group = groups[index];
      if (this.elementGroupFrameBorderContains(group.id, hit)) {
        return group;
      }
    }
    return null;
  }
  elementGroupFrameBorderContains(groupId, hit) {
    const frame = this.elementGroupFrameRect(groupId);
    if (!frame || !hit) {
      return false;
    }
    const tolerance = Math.max(5, this.selectionHitPaddingPx());
    const outerLeft = frame.x - tolerance;
    const outerRight = frame.x + frame.width + tolerance;
    const outerTop = frame.y - tolerance;
    const outerBottom = frame.y + frame.height + tolerance;
    if (hit.x < outerLeft || hit.x > outerRight || hit.y < outerTop || hit.y > outerBottom) {
      return false;
    }
    const innerLeft = frame.x + tolerance;
    const innerRight = frame.x + frame.width - tolerance;
    const innerTop = frame.y + tolerance;
    const innerBottom = frame.y + frame.height - tolerance;
    return !(hit.x > innerLeft && hit.x < innerRight && hit.y > innerTop && hit.y < innerBottom);
  }
  isElementGroupFullySelected(groupId) {
    if (!groupId) {
      return false;
    }
    const strokes = this.groupMemberStrokeIndexes(groupId);
    const blocks = this.groupMemberMarkdownBlocks(groupId);
    return strokes.length + blocks.length > 0 && strokes.every((index) => this.isStrokeSelected(index)) && blocks.every((block) => this.selectedMarkdownBlockIds.has(block.id));
  }
  selectElementGroup(groupId) {
    if (!groupId) {
      return false;
    }
    this.selectedStrokeIndexes = new Set(this.groupMemberStrokeIndexes(groupId));
    const strokes = this.getSelectedStrokeIndexes();
    this.selectedStrokeIndex = strokes.length ? strokes[strokes.length - 1] : -1;
    this.selectedMarkdownBlockIds = new Set(this.groupMemberMarkdownBlocks(groupId).map((block) => block.id));
    this.invalidateSelectionFrameSnapshot();
    this.hideSelectionMenu();
    this.invalidateStaticCache();
    this.syncPaletteInputs();
    this.updateToolButtons();
    this.syncMarkdownBlockPresentation();
    return this.hasHybridSelection();
  }
  selectedWholeElementGroups() {
    const ids = new Set([
      ...this.getSelectedStrokeIndexes().map((index) => this.drawingData.strokes[index]?.groupId),
      ...this.getSelectedMarkdownBlocks().map((block) => block.groupId)
    ].filter(Boolean));
    return Array.from(ids).filter((id) => this.isElementGroupFullySelected(id)).map((id) => this.elementGroup(id)).filter(Boolean);
  }
  getSelectedMarkdownBlocks() {
    const selected = this.selectedMarkdownBlockIds || /* @__PURE__ */ new Set();
    return this.markdownBlockRecords().filter((block) => selected.has(block.id));
  }
  hasHybridSelection() {
    return this.getSelectedStrokeIndexes().length > 0 || this.getSelectedMarkdownBlocks().length > 0;
  }
  markdownBlockElement(blockOrId) {
    const id = typeof blockOrId === "string" ? blockOrId : blockOrId?.id;
    const element = id ? this.markdownBlockElements.get(id) : null;
    return element?.isConnected ? element : null;
  }
  markdownBlockFlowElement(element) {
    if (!element || !this.previewEl?.contains?.(element)) {
      return null;
    }
    return findNoteFlowMarkdownBlockElement(element, this.previewEl) || element;
  }
  ensureExplicitMarkdownLineBlocks() {
    if (this.surfaceType !== "preview" || !this.previewEl?.isConnected || this.draggingStroke || this.resizingSelection) {
      return false;
    }
    const jobs = [];
    for (const paragraph of Array.from(this.previewEl.querySelectorAll?.("p") || [])) {
      if (paragraph.closest?.(NOTEDRAW_OWNED_MUTATION_SELECTOR) || paragraph.querySelector?.(".notedraw-md-line-block")) {
        continue;
      }
      const nodes = Array.from(paragraph.childNodes || []);
      const breakIndexes = nodes.map((node, index) => node.nodeName === "BR" ? index : -1).filter((index) => index >= 0);
      if (!breakIndexes.length) {
        continue;
      }
      const owner = findNoteFlowMarkdownBlockElement(paragraph, this.previewEl);
      const start = parseInteger(owner?.dataset?.noteDrawLineStart);
      const end = parseInteger(owner?.dataset?.noteDrawLineEnd) ?? start;
      if (!owner || !Number.isFinite(start) || !Number.isFinite(end) || end < start) {
        continue;
      }
      const segments = [];
      let segmentStart = 0;
      for (const breakIndex of breakIndexes) {
        segments.push(nodes.slice(segmentStart, breakIndex));
        segmentStart = breakIndex + 1;
      }
      segments.push(nodes.slice(segmentStart));
      const renderedSegments = segments.filter((segment) => segment.some((node) => node.nodeType === Node.ELEMENT_NODE || String(node.nodeValue || "").trim()));
      if (renderedSegments.length <= 1) {
        continue;
      }
      const path = normalizeVaultPath(owner.dataset.noteDrawSourcePath || this.file?.path || "");
      const group = `${path}\0${start}\0${end}\0${hashString(renderedMarkdownIdentityText(owner))}`;
      jobs.push({ paragraph, segments, start, end, path, group });
    }
    if (!jobs.length) {
      return false;
    }
    this.mutateMarkdownGridRows(() => {
      for (const job of jobs) {
        const { paragraph, segments, start, end, path, group } = job;
        const wrappers = [];
        segments.forEach((segment, index) => {
          if (!segment.some((node) => node.nodeType === Node.ELEMENT_NODE || String(node.nodeValue || "").trim())) {
            return;
          }
          const wrapper = paragraph.ownerDocument.createElement("span");
          const line = clamp10(Math.round(start + index * (end - start) / Math.max(1, segments.length - 1)), start, end);
          wrapper.className = "notedraw-md-line-block";
          wrapper.dataset.noteDrawExplicitLine = "true";
          wrapper.dataset.noteDrawExplicitLineGroup = group;
          wrapper.dataset.noteDrawSourcePath = path;
          wrapper.dataset.noteDrawLineStart = String(line);
          wrapper.dataset.noteDrawLineEnd = String(line);
          wrapper.dataset.noteDrawLineConfidence = "1";
          wrapper.dataset.noteDrawLineMapped = "true";
          wrapper._noteDrawSourceText = segment.map((node) => node.textContent || node.nodeValue || "").join("");
          for (const node of segment) {
            wrapper.appendChild(node);
          }
          wrappers.push(wrapper);
        });
        paragraph.replaceChildren(...wrappers);
        paragraph.dataset.noteDrawExplicitLineBlocks = "true";
      }
    });
    return true;
  }
  expandExplicitMarkdownBlockRecords(candidates) {
    const records = this.markdownBlockRecords();
    const groups = /* @__PURE__ */ new Map();
    for (const candidate of candidates || []) {
      const group = candidate?.dataset?.noteDrawExplicitLineGroup;
      const line = parseInteger(candidate?.dataset?.noteDrawLineStart);
      if (!group || !Number.isFinite(line)) {
        continue;
      }
      const list = groups.get(group) || [];
      list.push({ candidate, line });
      groups.set(group, list);
    }
    let changed = false;
    for (const [group, entries] of groups) {
      const ordered = entries.slice().sort((a, b) => a.line - b.line);
      const first = ordered[0]?.line;
      const last = ordered.at(-1)?.line;
      if (!Number.isFinite(first) || !Number.isFinite(last)) {
        continue;
      }
      const path = normalizeVaultPath(ordered[0].candidate.dataset.noteDrawSourcePath || this.file?.path || "");
      const aggregate = records.find((block) => block.path === path && block.lineStart === first && block.lineEnd > first && block.lineEnd >= last && block.explicitLineGroup !== group);
      if (aggregate) {
        const index = records.indexOf(aggregate);
        const split = ordered.map(({ candidate, line }, entryIndex) => ({
          ...aggregate,
          id: entryIndex === 0 ? aggregate.id : `${aggregate.id}-${hashString(`${group}:${line}`)}`,
          lineStart: line,
          lineEnd: line,
          textHint: normalizeRenderedText2(renderedMarkdownIdentityText(candidate)).slice(0, 240),
          explicitLineGroup: group
        }));
        records.splice(index, 1, ...split);
        changed = true;
        continue;
      }
      const template = records.find((item) => item.path === path && item.explicitLineGroup === group && item.lineStart >= first && item.lineEnd <= last) || null;
      for (const { candidate, line } of ordered) {
        const hint = normalizeRenderedText2(renderedMarkdownIdentityText(candidate)).slice(0, 240);
        let block = records.find((item) => item.path === path && item.lineStart === line && item.lineEnd === line && item.explicitLineGroup === group && item.textHint === hint);
        if (!block) {
          const base = template || {
            path,
            span: 12,
            noteFlowAutoSpan: false,
            widthScale: 1,
            minHeight: 0,
            borderColor: "",
            backgroundColor: "",
            contentColor: "",
            contentOpacity: 1,
            contentScale: 1,
            floating: false,
            floatingExplicit: false,
            floatBox: null,
            locked: false,
            groupId: ""
          };
          block = normalizeMarkdownBlocks([{
            ...base,
            id: `${base.id || `md-${Date.now().toString(36)}`}-${hashString(`${group}:${line}:${hint}`)}`,
            path,
            lineStart: line,
            lineEnd: line,
            textHint: hint,
            explicitLineGroup: group
          }], this.file)[0] || null;
          if (block) {
            records.push(block);
            changed = true;
          }
        }
        if (!block) {
          continue;
        }
        if (block.explicitLineGroup !== group || block.textHint !== hint) {
          block.explicitLineGroup = group;
          block.textHint = hint;
          changed = true;
        }
      }
    }
    return changed;
  }
  restoreExplicitMarkdownLineBlocks() {
    if (!this.previewEl?.isConnected) {
      return false;
    }
    const paragraphs = new Set(Array.from(this.previewEl.querySelectorAll?.("p[data-note-draw-explicit-line-blocks='true']") || []));
    for (const wrapper of Array.from(this.previewEl.querySelectorAll?.(".notedraw-md-line-block") || [])) {
      if (wrapper.parentElement?.tagName === "P") {
        paragraphs.add(wrapper.parentElement);
      }
    }
    if (!paragraphs.size) {
      return false;
    }
    this.mutateMarkdownGridRows(() => {
      for (const paragraph of paragraphs) {
        const wrappers = Array.from(paragraph.children || []).filter((child) => child.classList?.contains("notedraw-md-line-block"));
        if (!wrappers.length) {
          delete paragraph.dataset.noteDrawExplicitLineBlocks;
          continue;
        }
        const fragment = paragraph.ownerDocument.createDocumentFragment();
        wrappers.forEach((wrapper, index) => {
          if (index > 0) {
            fragment.appendChild(paragraph.ownerDocument.createElement("br"));
          }
          while (wrapper.firstChild) {
            fragment.appendChild(wrapper.firstChild);
          }
          wrapper.remove();
        });
        paragraph.replaceChildren(fragment);
        delete paragraph.dataset.noteDrawExplicitLineBlocks;
      }
    });
    return true;
  }
  isOwnedMarkdownGridRow(element) {
    return Boolean(element?.classList?.contains("notedraw-md-grid-row"));
  }
  markdownGridRowSpan(row) {
    return Array.from(row?.children || []).reduce((sum, child) => sum + clamp10(Math.round(Number(child.dataset?.noteDrawGridSpan) || 12), 1, 12), 0);
  }
  mutateMarkdownGridRows(callback) {
    const observer = this.markdownRenderObserver;
    observer?.disconnect?.();
    try {
      return callback();
    } finally {
      observer?.takeRecords?.();
      if (observer && !this.destroyed && this.previewEl?.isConnected) {
        observer.observe(this.previewEl, { subtree: true, childList: true });
      }
      this.noteFlowCandidateCache = null;
    }
  }
  releaseMarkdownBlockGridRow(flowElement) {
    const row = flowElement?.parentElement;
    if (!this.isOwnedMarkdownGridRow(row) || !row.parentNode) {
      return false;
    }
    this.mutateMarkdownGridRows(() => {
      const parent = row.parentNode;
      const children = Array.from(row.children || []);
      const index = children.indexOf(flowElement);
      const before = index >= 0 ? children.slice(0, index) : [];
      const after = index >= 0 ? children.slice(index + 1) : [];
      if (before.length) {
        parent.insertBefore(flowElement, row.nextSibling);
      } else {
        parent.insertBefore(flowElement, row);
      }
      delete flowElement.dataset.noteDrawGridSpan;
      if (after.length) {
        const trailing = flowElement.ownerDocument.createElement("div");
        trailing.className = "notedraw-md-grid notedraw-md-grid-row";
        trailing.setAttribute("data-note-draw-grid-row", "true");
        parent.insertBefore(trailing, flowElement.nextSibling);
        for (const child of after) {
          trailing.appendChild(child);
        }
      }
      if (!before.length) {
        row.remove();
      }
    });
    return true;
  }
  ensureMarkdownBlockGridRow(block, element, options = {}) {
    const flowElement = this.markdownBlockFlowElement(element);
    const span = clamp10(Math.round(Number(block?.span) || 12), 1, 12);
    if (!flowElement?.isConnected) {
      return null;
    }
    if (block?.floating || span >= 12) {
      this.releaseMarkdownBlockGridRow(flowElement);
      return null;
    }
    if (this.markdownBlockInlineLane(flowElement)) {
      this.releaseMarkdownBlockGridRow(flowElement);
      return null;
    }
    const current = flowElement.parentElement;
    if (!current || !this.previewEl?.contains?.(current)) {
      return null;
    }
    if (this.isOwnedMarkdownGridRow(current)) {
      flowElement.dataset.noteDrawGridSpan = String(span);
      return current;
    }
    if (!isNoteFlowCollectionBlock(current)) {
      return current;
    }
    const previous = flowElement.previousElementSibling;
    const next = flowElement.nextElementSibling;
    const previousRow = this.isOwnedMarkdownGridRow(previous) && this.markdownGridRowSpan(previous) + span <= 12 ? previous : null;
    const nextRow = this.isOwnedMarkdownGridRow(next) && this.markdownGridRowSpan(next) + span <= 12 ? next : null;
    let row = previousRow || nextRow;
    this.mutateMarkdownGridRows(() => {
      var _a;
      if (!row) {
        row = flowElement.ownerDocument.createElement("div");
        row.className = "notedraw-md-grid notedraw-md-grid-row";
        row.setAttribute("data-note-draw-grid-row", "true");
        current.insertBefore(row, flowElement);
        if (options.preview === true) {
          this.dragNoteFlowDomPreview || (this.dragNoteFlowDomPreview = { styles: /* @__PURE__ */ new Map(), classes: /* @__PURE__ */ new Map() });
          (_a = this.dragNoteFlowDomPreview).createdRows || (_a.createdRows = /* @__PURE__ */ new Set());
          this.dragNoteFlowDomPreview.createdRows.add(row);
        }
      }
      flowElement.dataset.noteDrawGridSpan = String(span);
      if (row === nextRow) {
        row.insertBefore(flowElement, row.firstChild);
      } else {
        row.appendChild(flowElement);
      }
    });
    return row;
  }
  unwrapMarkdownGridRows(rows = this.previewEl?.querySelectorAll?.(".notedraw-md-grid-row") || []) {
    this.mutateMarkdownGridRows(() => {
      for (const row of Array.from(rows || [])) {
        if (!this.isOwnedMarkdownGridRow(row) || !row.parentNode) {
          continue;
        }
        const parent = row.parentNode;
        for (const child of Array.from(row.children || [])) {
          delete child.dataset.noteDrawGridSpan;
          parent.insertBefore(child, row);
        }
        row.remove();
      }
    });
  }
  markdownBlockGridContainer(element) {
    const flowElement = this.markdownBlockFlowElement(element);
    const container = flowElement?.parentElement;
    if (!container || !this.previewEl?.contains?.(container)) {
      return null;
    }
    return isNoteFlowCollectionBlock(container) ? null : container;
  }
  markdownBlockInlineLane(element) {
    const flowElement = this.markdownBlockFlowElement(element);
    let container = flowElement?.parentElement;
    if (this.isOwnedMarkdownGridRow(container)) {
      container = container.parentElement;
    }
    while (container && this.previewEl?.contains?.(container)) {
      if (container.matches?.(".markdown-preview-section,.markdown-rendered,.callout-content,.contains-task-list,ul,ol,.el-ul,.el-ol")) {
        return container;
      }
      container = container.parentElement;
    }
    return null;
  }
  markdownBlockLayoutLane(element) {
    return this.markdownBlockInlineLane(element) || this.markdownBlockGridContainer(element);
  }
  applyDraggedMarkdownInlinePresentation(element, span) {
    const flowElement = this.markdownBlockFlowElement(element) || element;
    if (!flowElement?.isConnected || !this.markdownBlockInlineLane(flowElement)) {
      return false;
    }
    const block = this.findMarkdownBlockRecordForElement(element);
    this.setDraggedNoteFlowDomClass(flowElement, "notedraw-md-inline-grid-item", true);
    this.setDraggedNoteFlowDomClass(flowElement, "notedraw-md-grid-item", false);
    this.setDraggedNoteFlowDomStyle(flowElement, "--notedraw-md-inline-span", String(clamp10(Math.round(Number(span) || 12), 1, 12)));
    const widthScale = Number(block?.span) === Number(span) ? block?.widthScale : 1;
    this.setDraggedNoteFlowDomStyle(flowElement, "--notedraw-md-inline-width", markdownInlineWidthCss(span, widthScale, flowElement));
    this.setDraggedNoteFlowDomStyle(flowElement, "grid-column", null);
    return true;
  }
  applyMarkdownBlockFlowPresentation(block, element) {
    const flowElement = this.markdownBlockFlowElement(element);
    const inlineSpan = clamp10(Math.round(Number(block?.span) || 12), 1, 12);
    const inlineLane = !block?.floating ? this.markdownBlockInlineLane(flowElement) : null;
    if (inlineLane && inlineSpan < 12) {
      this.releaseMarkdownBlockGridRow(flowElement);
      flowElement.classList.add("notedraw-md-inline-grid-item");
      flowElement.classList.remove("notedraw-md-grid-item");
      flowElement.style.setProperty("--notedraw-md-inline-span", String(inlineSpan));
      flowElement.style.setProperty("--notedraw-md-inline-width", markdownInlineWidthCss(inlineSpan, block?.widthScale, flowElement));
      flowElement.style.removeProperty("grid-column");
      return flowElement;
    }
    flowElement?.classList?.remove("notedraw-md-inline-grid-item");
    flowElement?.style?.removeProperty("--notedraw-md-inline-span");
    flowElement?.style?.removeProperty("--notedraw-md-inline-width");
    const gridContainer = this.ensureMarkdownBlockGridRow(block, element) || this.markdownBlockGridContainer(element);
    element?.style?.removeProperty("grid-column");
    if (!flowElement) {
      return null;
    }
    flowElement.classList.toggle("notedraw-md-grid-item", Boolean(gridContainer));
    if (!gridContainer || block?.floating) {
      flowElement.style.removeProperty("grid-column");
    } else {
      flowElement.style.gridColumn = `span ${clamp10(Math.round(Number(block?.span) || 12), 1, 12)}`;
    }
    return flowElement;
  }
  clearMarkdownBlockFlowPresentation(element) {
    const flowElement = this.markdownBlockFlowElement(element);
    if (!flowElement) {
      return;
    }
    flowElement.classList.remove("notedraw-md-grid-item", "is-notedraw-md-dragging");
    flowElement.classList.remove("notedraw-md-inline-grid-item");
    flowElement.classList.remove("notedraw-md-fit-content");
    flowElement.style.removeProperty("grid-column");
    flowElement.style.removeProperty("--notedraw-md-inline-span");
    flowElement.style.removeProperty("--notedraw-md-inline-width");
    flowElement.style.removeProperty("--notedraw-md-drag-x");
    flowElement.style.removeProperty("--notedraw-md-drag-y");
  }
  findMarkdownBlockRecordForElement(element) {
    const blockElement = element?.matches?.(".notedraw-md-line-block") ? element : element?.closest?.(".notedraw-md-line-block") || element?.closest?.(".notedraw-md-block") || element;
    const existingId = blockElement?.dataset?.noteDrawMarkdownBlockId;
    if (existingId) {
      return this.markdownBlockRecords().find((block) => block.id === existingId) || null;
    }
    if (!blockElement) {
      return null;
    }
    const path = normalizeVaultPath(blockElement.dataset.noteDrawSourcePath || this.file?.path || "");
    const info = getSourceInfo(blockElement);
    const hint = normalizeRenderedText2(renderedMarkdownIdentityText(blockElement)).slice(0, 240);
    const candidates = this.markdownBlockRecords().filter((block) => block.path === path);
    const explicitGroup = String(blockElement.dataset?.noteDrawExplicitLineGroup || "");
    const compatible = (block) => explicitGroup ? String(block.explicitLineGroup || "") === explicitGroup : !block.explicitLineGroup;
    const mapped = candidates.find((block) => this.markdownBlockElements?.get?.(block.id) === blockElement);
    const rangeMatching = candidates.filter((block) => compatible(block) && Number.isFinite(info.lineStart) && block.lineStart === info.lineStart && block.lineEnd === (info.lineEnd ?? info.lineStart));
    const matching = candidates.filter((block) => compatible(block) && (!block.textHint || block.textHint === hint));
    return mapped || rangeMatching[0] || matching.find((block) => Number.isFinite(info.lineStart) && block.lineStart === info.lineStart) || matching.find((block) => block.textHint && block.textHint === hint) || null;
  }
  markdownBlockElementForTarget(target, clientPoint = null) {
    if (this.surfaceType !== "preview" || !target || !this.previewEl?.contains?.(target)) {
      return null;
    }
    const embeddedBlock = findMarkdownEmbedBlockElement(target, this.previewEl);
    if (embeddedBlock) {
      return this.markdownElementContainsClientPoint(embeddedBlock, clientPoint) ? embeddedBlock : null;
    }
    const renderedBlock = markdownBlockCandidateElementForTarget(target, this.previewEl);
    if (renderedBlock && this.markdownElementContainsClientPoint(renderedBlock, clientPoint)) {
      return renderedBlock;
    }
    const marked = target.closest?.(".notedraw-md-block");
    if (marked && this.previewEl.contains(marked) && isConcreteMarkdownBlockElement(marked)) {
      return this.markdownElementContainsClientPoint(marked, clientPoint) ? marked : null;
    }
    const taskCheckbox = target.closest?.("input.task-list-item-checkbox, input[type='checkbox']");
    const taskItem = taskCheckbox?.closest?.("li");
    if (taskItem && this.previewEl.contains(taskItem)) {
      const taskBlocks = [taskItem, ...Array.from(taskItem.querySelectorAll?.(MARKDOWN_TEXT_SELECTOR) || [])].filter((element) => {
        return element?.closest?.("li") === taskItem && isConcreteMarkdownBlockElement(element);
      });
      const taskBlock = taskBlocks.find((element) => element.classList?.contains("notedraw-md-block")) || taskBlocks[0];
      if (taskBlock) {
        return this.markdownElementContainsClientPoint(taskBlock, clientPoint) ? taskBlock : null;
      }
    }
    const editable = findEditableTarget(target, this.previewEl);
    if (!editable) {
      return null;
    }
    const block = this.findMarkdownBlockRecordForElement(editable);
    const blockElement = block ? this.markdownBlockElement(block) || editable : editable;
    return this.markdownElementContainsClientPoint(blockElement, clientPoint) ? blockElement : null;
  }
  canonicalMarkdownFlowTarget(rawTarget) {
    const candidate = isConcreteMarkdownBlockElement(rawTarget) ? rawTarget : findNoteFlowInlineMeasureElement(rawTarget);
    if (!candidate) {
      return null;
    }
    const block = this.findMarkdownBlockRecordForElement(candidate);
    return this.markdownBlockElement(block) || candidate.closest?.(".notedraw-md-block") || candidate;
  }
  ensureMarkdownBlockRecord(element) {
    if (!element || this.surfaceType !== "preview") {
      return null;
    }
    const blockElement = element.closest?.(".notedraw-md-block") || element;
    const existing = this.findMarkdownBlockRecordForElement(blockElement);
    if (existing) {
      existing.renderKind = markdownElementRenderKind(blockElement) || existing.renderKind || "";
      return existing;
    }
    const info = getSourceInfo(blockElement);
    const path = normalizeVaultPath(blockElement.dataset.noteDrawSourcePath || this.file?.path || "");
    const textHint = normalizeRenderedText2(renderedMarkdownIdentityText(blockElement)).slice(0, 240);
    if (!path || !textHint) {
      return null;
    }
    const record = normalizeMarkdownBlocks([{
      id: `md-${Date.now().toString(36)}-${hashString(`${path}:${info.lineStart ?? "x"}:${textHint}`)}`,
      path,
      lineStart: info.lineStart,
      lineEnd: info.lineEnd,
      textHint,
      renderKind: markdownElementRenderKind(blockElement),
      span: 12
    }], this.file)[0];
    if (!record) {
      return null;
    }
    this.markdownBlockRecords().push(record);
    this.markdownBlockElements.set(record.id, blockElement);
    blockElement.dataset.noteDrawMarkdownBlockId = record.id;
    return record;
  }
  clearMarkdownBlockPresentation() {
    const parents = /* @__PURE__ */ new Set();
    for (const element of this.markdownBlockElements?.values?.() || []) {
      if (!element) {
        continue;
      }
      parents.add(element.parentElement);
      parents.add(this.markdownBlockGridContainer(element));
      this.clearMarkdownBlockElementPresentation(element);
    }
    for (const parent of parents) {
      parent?.removeClass?.("notedraw-md-grid");
    }
    this.unwrapMarkdownGridRows();
    this.restoreExplicitMarkdownLineBlocks();
    this.markdownBlockElements?.clear?.();
  }
  clearMarkdownBlockElementPresentation(element) {
    if (!element) {
      return;
    }
    this.clearMarkdownBlockFlowPresentation(element);
    element.removeClass("notedraw-md-block", "is-selected", "is-locked", "is-floating", "is-notedraw-md-dragging");
    if (element.dataset) {
      delete element.dataset.noteDrawMarkdownBlockId;
      delete element.dataset.noteDrawSortDragging;
      delete element.dataset.noteDrawResizedHeight;
    }
    for (const property of ["grid-column", "width", "--notedraw-md-inline-width", "--notedraw-md-border", "--notedraw-md-background", "--notedraw-md-min-height", "--notedraw-md-drag-x", "--notedraw-md-drag-y", "--notedraw-md-float-x", "--notedraw-md-float-y", "--notedraw-md-float-width"]) {
      element.style?.removeProperty(property);
    }
  }
  markdownBlockNaturalHeight(element) {
    if (!element?.isConnected) {
      return 1;
    }
    const resized = element.hasAttribute("data-note-draw-resized-height");
    const value = element.style.getPropertyValue("--notedraw-md-min-height");
    const priority = element.style.getPropertyPriority("--notedraw-md-min-height");
    element.removeAttribute("data-note-draw-resized-height");
    element.style.removeProperty("--notedraw-md-min-height");
    const flowInsets = this.noteFlowAppliedVerticalInsets(element);
    const natural = Math.max(1, (Number(element.offsetHeight) || 1) - flowInsets.top - flowInsets.bottom);
    if (value) {
      element.style.setProperty("--notedraw-md-min-height", value, priority);
    }
    if (resized) {
      element.setAttribute("data-note-draw-resized-height", "true");
    }
    return natural;
  }
  applyMarkdownBlockHeightPresentation(block, element) {
    const height = markdownBlockPresentationMinHeight(block);
    const previousHeight = element.style.getPropertyValue("--notedraw-md-min-height");
    const wasResized = element.hasAttribute("data-note-draw-resized-height");
    element.toggleAttribute("data-note-draw-resized-height", height > 0);
    if (height > 0) {
      element.style.setProperty("--notedraw-md-min-height", `${height}px`);
    } else {
      element.style.removeProperty("--notedraw-md-min-height");
    }
    if (wasResized !== height > 0 || previousHeight !== (height > 0 ? `${height}px` : "")) {
      this.readingLogicalSizerHeight = 0;
    }
  }
  applyMarkdownBlockWidthPresentation(block, element) {
    if (!element) {
      return;
    }
    const flowElement = this.markdownBlockFlowElement(element) || element;
    if (block?.floating) {
      flowElement.classList?.remove?.("notedraw-md-fit-content");
      element.style.removeProperty("width");
      flowElement.style.removeProperty("--notedraw-md-inline-width");
      return;
    }
    const widthScale = normalizeMarkdownBlockWidthScale(block?.widthScale);
    if (flowElement.classList?.contains("notedraw-md-inline-grid-item")) {
      flowElement.classList?.remove?.("notedraw-md-fit-content");
      flowElement.style.setProperty("--notedraw-md-inline-width", markdownInlineWidthCss(block?.span, widthScale, flowElement));
      element.style.removeProperty("width");
      return;
    }
    const fitContent = normalizeMarkdownInlineWidthMode(
      block?.inlineWidthMode,
      block?.span,
      block?.widthScale
    ) === "fit";
    flowElement.classList?.toggle?.("notedraw-md-fit-content", fitContent);
    if (fitContent) {
      element.style.removeProperty("width");
      return;
    }
    flowElement.classList?.remove?.("notedraw-md-fit-content");
    if (widthScale >= 0.999) {
      element.style.removeProperty("width");
      return;
    }
    element.style.width = `${Math.round(widthScale * 1e3) / 10}%`;
  }
  refreshMarkdownBlockPresentation(blockIds = this.selectedMarkdownBlockIds) {
    if (this.surfaceType !== "preview" || !this.previewEl?.isConnected || !this.drawingData) {
      return;
    }
    if (this.draggingStroke && this.dragNoteFlowDomPreview && !this.allowMarkdownPresentationDuringDrag) {
      return;
    }
    const wanted = new Set(blockIds || []);
    if (!wanted.size) {
      return;
    }
    const records = new Map(this.markdownBlockRecords().map((block) => [block.id, block]));
    const touchedParents = /* @__PURE__ */ new Set();
    for (const id of wanted) {
      const block = records.get(id);
      const element = this.markdownBlockElements.get(id);
      if (!block || !element?.isConnected) {
        continue;
      }
      touchedParents.add(this.markdownBlockGridContainer(element));
      element.toggleClass("is-selected", this.selectedMarkdownBlockIds.has(block.id));
      element.toggleClass("is-locked", Boolean(block.locked));
      element.toggleClass("is-floating", Boolean(block.floating && block.floatBox));
      this.applyMarkdownBlockFlowPresentation(block, element);
      this.applyMarkdownBlockWidthPresentation(block, element);
      setNoteDrawCssProps(element, {
        "--notedraw-md-border": block.borderColor || "transparent",
        "--notedraw-md-background": block.backgroundColor || "transparent",
        "--notedraw-md-content-color": block.contentColor || "inherit",
        "--notedraw-md-content-opacity": String(block.contentOpacity ?? 1),
        "--notedraw-md-content-scale": String(block.contentScale ?? 1)
      });
      this.applyMarkdownBlockHeightPresentation(block, element);
      if (block.floating && block.floatBox) {
        const canvasRect = this.canvas?.getBoundingClientRect?.();
        if (canvasRect?.width > 0 && canvasRect?.height > 0) {
          const horizontalSurface = this.layoutMeasureEl?.isConnected ? this.layoutMeasureEl : findLayoutMeasureElement(this.previewEl);
          const horizontalRect = horizontalSurface?.getBoundingClientRect?.();
          const logicalWidth = Math.max(1, Number(horizontalSurface?.offsetWidth) || Number(horizontalSurface?.clientWidth) || this.previewEl.clientWidth || this.canvasWidth());
          const logicalHeight = Math.max(1, this.canvasHeight());
          const scaleX = horizontalRect?.width > 0 ? horizontalRect.width / logicalWidth : canvasRect.width / Math.max(1, this.canvasWidth());
          const scaleY = canvasRect.height / Math.max(1, this.canvasRenderHeight);
          const localWidth = Math.max(80, block.floatBox.width * logicalWidth);
          const dragging = element.hasClass?.("is-notedraw-md-dragging") || element.classList?.contains("is-notedraw-md-dragging");
          const hasStableOffset = element.style.getPropertyValue("--notedraw-md-float-x") && element.style.getPropertyValue("--notedraw-md-float-y");
          setNoteDrawCssProps(element, { "--notedraw-md-float-width": `${localWidth}px` });
          if (!dragging || !hasStableOffset) {
            setNoteDrawCssProps(element, {
              "--notedraw-md-float-x": "0px",
              "--notedraw-md-float-y": "0px"
            });
            const naturalRect = element.getBoundingClientRect();
            const targetLeft = (horizontalRect?.left ?? canvasRect.left) + block.floatBox.x * logicalWidth * scaleX;
            const targetTop = canvasRect.top + (block.floatBox.y * logicalHeight - this.canvasWindowTop) * scaleY;
            setNoteDrawCssProps(element, {
              "--notedraw-md-float-x": `${Math.round((targetLeft - naturalRect.left) / Math.max(1e-4, scaleX))}px`,
              "--notedraw-md-float-y": `${Math.round((targetTop - naturalRect.top) / Math.max(1e-4, scaleY))}px`
            });
          }
        }
      } else {
        for (const property of ["--notedraw-md-float-x", "--notedraw-md-float-y", "--notedraw-md-float-width"]) {
          element.style.removeProperty(property);
        }
      }
    }
    for (const parent of touchedParents) {
      const hasGridBlock = Array.from(this.markdownBlockElements.entries()).some(([id, element]) => {
        const block = records.get(id);
        return this.markdownBlockGridContainer(element) === parent && !block?.floating && (block?.span || 12) < 12;
      });
      parent?.toggleClass?.("notedraw-md-grid", hasGridBlock);
    }
  }
  syncMarkdownBlockPresentation() {
    if (this.surfaceType !== "preview" || !this.previewEl?.isConnected || !this.drawingData) {
      return;
    }
    if (this.draggingStroke && this.dragNoteFlowDomPreview && !this.allowMarkdownPresentationDuringDrag) {
      return;
    }
    const explicitDomChanged = this.ensureExplicitMarkdownLineBlocks();
    const previousMarkdownBlockElements = this.markdownBlockElements;
    const previousElements = new Set(previousMarkdownBlockElements.values());
    const previousParents = new Set(Array.from(previousElements).flatMap((element) => [
      element?.parentElement,
      this.markdownBlockGridContainer(element)
    ]).filter(Boolean));
    const candidates = markdownBlockCandidateElements(this.previewEl);
    const recordsById = new Map(this.markdownBlockRecords().map((block) => [block.id, block]));
    const taskOwnerTemplates = /* @__PURE__ */ new Map();
    for (const [id, element] of previousMarkdownBlockElements) {
      if (!element?.matches?.("li.task-list-item")) {
        continue;
      }
      const owner = element.closest?.("ul.contains-task-list,ol.contains-task-list") || element.parentElement;
      const block = recordsById.get(id);
      if (!owner || !block || block.floating) {
        continue;
      }
      const templates = taskOwnerTemplates.get(owner) || [];
      templates.push(block);
      taskOwnerTemplates.set(owner, templates);
    }
    if (taskOwnerTemplates.size) {
      for (const candidate of candidates) {
        if (!candidate.matches?.("li.task-list-item")) {
          continue;
        }
        const owner = candidate.closest?.("ul.contains-task-list,ol.contains-task-list") || candidate.parentElement;
        const templates = taskOwnerTemplates.get(owner);
        if (!templates?.length) {
          continue;
        }
        const existing = this.findMarkdownBlockRecordForElement(candidate);
        const block = existing || this.ensureMarkdownBlockRecord(candidate);
        if (!existing && block) {
          const template = templates.find((item) => Number(item.span) >= 1 && Number(item.span) < 12) || templates[0];
          block.span = clamp10(Math.round(Number(template.span) || 12), 1, 12);
          block.widthScale = normalizeMarkdownBlockWidthScale(template.widthScale);
          block.noteFlowAutoSpan = false;
          block.floating = false;
          block.floatingExplicit = false;
          block.floatBox = null;
        }
      }
    }
    let explicitRecordsChanged = this.expandExplicitMarkdownBlockRecords(candidates);
    const currentRecords = this.markdownBlockRecords();
    const compactedRecords = dedupeMarkdownBlockRecords(currentRecords);
    if (compactedRecords.length < currentRecords.length) {
      this.drawingData.markdownBlocks = compactedRecords;
      const retainedIds = new Set(compactedRecords.map((block) => block.id));
      this.selectedMarkdownBlockIds = new Set(Array.from(this.selectedMarkdownBlockIds || []).filter((id) => retainedIds.has(id)));
      explicitRecordsChanged = true;
    }
    const candidateMeta = /* @__PURE__ */ new Map();
    const idCandidates = /* @__PURE__ */ new Map();
    const rangeCandidates = /* @__PURE__ */ new Map();
    const exactCandidates = /* @__PURE__ */ new Map();
    const hintCandidates = /* @__PURE__ */ new Map();
    const queueCandidate = (map, key, element) => {
      const values = map.get(key) || [];
      values.push(element);
      map.set(key, values);
    };
    for (const [candidateOrder, element] of candidates.entries()) {
      const path = normalizeVaultPath(element.dataset.noteDrawSourcePath || this.file?.path || "");
      const info = getSourceInfo(element);
      const hint = normalizeRenderedText2(renderedMarkdownIdentityText(element)).slice(0, 240);
      candidateMeta.set(element, { path, info, hint, order: candidateOrder });
      const mappedId = element.dataset.noteDrawMarkdownBlockId;
      if (mappedId) {
        queueCandidate(idCandidates, mappedId, element);
      }
      if (Number.isFinite(info.lineStart)) {
        queueCandidate(rangeCandidates, `${path}\0${info.lineStart}\0${info.lineEnd ?? info.lineStart}\0${String(element.dataset?.noteDrawExplicitLineGroup || "")}`, element);
        queueCandidate(exactCandidates, `${path}\0${info.lineStart}\0${hint}`, element);
      }
      queueCandidate(hintCandidates, `${path}\0${hint}`, element);
    }
    const used = /* @__PURE__ */ new Set();
    let markdownMetadataChanged = explicitDomChanged || explicitRecordsChanged;
    const explicitGroupMatches = (block, element) => {
      const group = String(block?.explicitLineGroup || "");
      return !group || String(element?.dataset?.noteDrawExplicitLineGroup || "") === group;
    };
    const pendingIdentity = this.pendingMarkdownIdentityRefresh?.expiresAt > Date.now() ? this.pendingMarkdownIdentityRefresh : null;
    if (!pendingIdentity) {
      this.pendingMarkdownIdentityRefresh = null;
    }
    const pendingIdentityMembers = new Map((pendingIdentity?.members || []).map((member) => [member.id, member]));
    for (const [id, member] of pendingIdentityMembers) {
      const block = this.markdownBlockRecords().find((item) => item.id === id);
      if (!block || block.floating || !(Number(member.span) >= 1 && Number(member.span) < 12)) {
        continue;
      }
      block.span = Number(member.span);
      block.widthScale = normalizeMarkdownBlockWidthScale(member.widthScale);
      block.noteFlowAutoSpan = false;
    }
    const taskRecords = this.markdownBlockRecords().filter((block) => {
      const previous = previousMarkdownBlockElements.get(block.id);
      return block.renderKind === "task" || previous?.matches?.("li.task-list-item");
    }).sort((left, right) => Number(left.lineStart) - Number(right.lineStart));
    const taskCandidateGroups = /* @__PURE__ */ new Map();
    for (const [element, meta] of candidateMeta) {
      if (!element.matches?.("li.task-list-item")) {
        continue;
      }
      const owner = element.closest?.("ul.contains-task-list,ol.contains-task-list") || element.parentElement;
      const group = taskCandidateGroups.get(owner) || [];
      group.push({ element, meta });
      taskCandidateGroups.set(owner, group);
    }
    const taskOrdinalMatches = /* @__PURE__ */ new Map();
    const taskOrdinalElements = /* @__PURE__ */ new Set();
    for (const group of taskCandidateGroups.values()) {
      let anchoredSequence = null;
      for (const [candidateIndex, candidate] of group.entries()) {
        const matchingRecordIndexes = taskRecords.map((block, recordIndex) => normalizeRenderedText2(block.textHint) === candidate.meta.hint ? recordIndex : -1).filter((recordIndex) => recordIndex >= 0);
        if (matchingRecordIndexes.length !== 1) {
          continue;
        }
        const startIndex = matchingRecordIndexes[0] - candidateIndex;
        const sequence = taskRecords.slice(startIndex, startIndex + group.length);
        if (startIndex >= 0 && sequence.length === group.length && sequence.every((block) => block.path === candidate.meta.path)) {
          anchoredSequence = sequence;
          break;
        }
      }
      if (!anchoredSequence && taskCandidateGroups.size === 1 && taskRecords.length === group.length) {
        anchoredSequence = taskRecords;
      }
      anchoredSequence?.forEach((block, index) => {
        const element = group[index]?.element;
        if (!element || taskOrdinalMatches.has(block.id) || taskOrdinalElements.has(element)) {
          return;
        }
        taskOrdinalMatches.set(block.id, element);
        taskOrdinalElements.add(element);
      });
    }
    const identityMatches = (block, element) => {
      if (!element || !explicitGroupMatches(block, element)) {
        return false;
      }
      if (!block?.textHint) {
        return true;
      }
      const hint = normalizeRenderedText2(renderedMarkdownIdentityText(element)).slice(0, 240);
      return hint === normalizeRenderedText2(block.textHint);
    };
    const takeUnused = (values, block = null) => values?.find((element) => !used.has(element) && identityMatches(block, element)) || null;
    const takeUnusedRange = (values, block = null) => values?.find((element) => !used.has(element) && explicitGroupMatches(block, element)) || null;
    const next = /* @__PURE__ */ new Map();
    for (const block of this.markdownBlockRecords()) {
      const previous = this.markdownBlockElements.get(block.id);
      const previousMeta = candidateMeta.get(previous);
      const previousMatches = previousMeta && !used.has(previous) && previousMeta.path === block.path && previous?.dataset?.noteDrawMarkdownBlockId === block.id && identityMatches(block, previous);
      const exactKey = `${block.path}\0${block.lineStart}\0${block.textHint}`;
      const rangeKey = `${block.path}\0${block.lineStart}\0${block.lineEnd ?? block.lineStart}\0${String(block.explicitLineGroup || "")}`;
      const hintKey = `${block.path}\0${block.textHint}`;
      let element = previousMatches ? previous : takeUnused(idCandidates.get(block.id), block) || takeUnusedRange(rangeCandidates.get(rangeKey), block) || takeUnused(exactCandidates.get(exactKey), block) || takeUnused(hintCandidates.get(hintKey), block) || (!used.has(taskOrdinalMatches.get(block.id)) ? taskOrdinalMatches.get(block.id) : null);
      const pendingMember = pendingIdentityMembers.get(block.id);
      if (!element && pendingMember?.path === block.path) {
        element = candidates.find((candidate) => {
          if (used.has(candidate) || !explicitGroupMatches(block, candidate)) {
            return false;
          }
          const meta2 = candidateMeta.get(candidate);
          return meta2?.path === block.path && Number(meta2.info?.lineStart) === pendingMember.lineStart && Number(meta2.info?.lineEnd ?? meta2.info?.lineStart) === pendingMember.lineEnd;
        }) || null;
      }
      if (!element) {
        const blockHint = normalizeRenderedText2(block.textHint);
        const scored = candidates.map((candidate, candidateOrder) => {
          if (used.has(candidate) || !identityMatches(block, candidate)) {
            return null;
          }
          const meta2 = candidateMeta.get(candidate);
          if (!meta2 || meta2.path !== block.path) {
            return null;
          }
          const candidateHint = normalizeRenderedText2(meta2.hint);
          const hintExact = Boolean(blockHint && candidateHint && blockHint === candidateHint);
          const score = hintExact ? 1e3 : 0;
          return { candidate, score };
        }).filter(Boolean).sort((left, right) => right.score - left.score)[0];
        if (scored && scored.score >= 1e3) {
          element = scored.candidate;
        }
      }
      if (!element && previous?.isConnected && !used.has(previous) && previous?.dataset?.noteDrawMarkdownBlockId === block.id && (!candidateMeta.has(previous) || candidateMeta.get(previous)?.path === block.path) && identityMatches(block, previous)) {
        element = previous;
      }
      if (!element) {
        continue;
      }
      used.add(element);
      next.set(block.id, element);
      const meta = candidateMeta.get(element) || { info: getSourceInfo(element), hint: normalizeRenderedText2(renderedMarkdownIdentityText(element)).slice(0, 240) };
      const info = meta.info;
      if (Number.isFinite(info.lineStart) && !taskSourceRangeLooksRelative(block, element, info)) {
        markdownMetadataChanged = markdownMetadataChanged || block.lineStart !== info.lineStart || block.lineEnd !== (info.lineEnd ?? info.lineStart);
        block.lineStart = info.lineStart;
        block.lineEnd = info.lineEnd ?? info.lineStart;
      }
      if (meta.hint) {
        markdownMetadataChanged = markdownMetadataChanged || block.textHint !== meta.hint;
        block.textHint = meta.hint;
      }
      element.dataset.noteDrawMarkdownBlockId = block.id;
      block.renderKind = markdownElementRenderKind(element) || block.renderKind || "";
      element.addClass("notedraw-md-block");
      element.toggleClass("is-selected", this.selectedMarkdownBlockIds.has(block.id));
      element.toggleClass("is-locked", Boolean(block.locked));
      element.toggleClass("is-floating", Boolean(block.floating && block.floatBox));
      const liveInlineSpan = element.classList?.contains("notedraw-md-inline-grid-item") ? Number.parseInt(element.style?.getPropertyValue?.("--notedraw-md-inline-span"), 10) : Number.NaN;
      if (!block.floating && Number.isFinite(liveInlineSpan) && liveInlineSpan >= 1 && liveInlineSpan < 12 && (!Number.isFinite(Number(block.span)) || Number(block.span) >= 12)) {
        block.span = liveInlineSpan;
        block.noteFlowAutoSpan = false;
      }
      this.applyMarkdownBlockFlowPresentation(block, element);
      this.applyMarkdownBlockWidthPresentation(block, element);
      setNoteDrawCssProps(element, {
        "--notedraw-md-border": block.borderColor || "transparent",
        "--notedraw-md-background": block.backgroundColor || "transparent",
        "--notedraw-md-content-color": block.contentColor || "inherit",
        "--notedraw-md-content-opacity": String(block.contentOpacity ?? 1),
        "--notedraw-md-content-scale": String(block.contentScale ?? 1)
      });
      this.applyMarkdownBlockHeightPresentation(block, element);
      if (block.floating && block.floatBox) {
        const canvasRect = this.canvas?.getBoundingClientRect?.();
        if (canvasRect?.width > 0 && canvasRect?.height > 0) {
          const horizontalSurface = this.layoutMeasureEl?.isConnected ? this.layoutMeasureEl : findLayoutMeasureElement(this.previewEl);
          const horizontalRect = horizontalSurface?.getBoundingClientRect?.();
          const logicalWidth = Math.max(1, Number(horizontalSurface?.offsetWidth) || Number(horizontalSurface?.clientWidth) || this.previewEl.clientWidth || this.canvasWidth());
          const logicalHeight = Math.max(1, this.canvasHeight());
          const scaleX = horizontalRect?.width > 0 ? horizontalRect.width / logicalWidth : canvasRect.width / Math.max(1, this.canvasWidth());
          const scaleY = canvasRect.height / Math.max(1, this.canvasRenderHeight);
          const localWidth = Math.max(80, block.floatBox.width * logicalWidth);
          const dragging = element.hasClass?.("is-notedraw-md-dragging") || element.classList?.contains("is-notedraw-md-dragging");
          const hasStableOffset = element.style.getPropertyValue("--notedraw-md-float-x") && element.style.getPropertyValue("--notedraw-md-float-y");
          setNoteDrawCssProps(element, { "--notedraw-md-float-width": `${localWidth}px` });
          if (!dragging || !hasStableOffset) {
            setNoteDrawCssProps(element, {
              "--notedraw-md-float-x": "0px",
              "--notedraw-md-float-y": "0px"
            });
            const naturalRect = element.getBoundingClientRect();
            const targetLeft = (horizontalRect?.left ?? canvasRect.left) + block.floatBox.x * logicalWidth * scaleX;
            const targetTop = canvasRect.top + (block.floatBox.y * logicalHeight - this.canvasWindowTop) * scaleY;
            setNoteDrawCssProps(element, {
              "--notedraw-md-float-x": `${Math.round((targetLeft - naturalRect.left) / Math.max(1e-4, scaleX))}px`,
              "--notedraw-md-float-y": `${Math.round((targetTop - naturalRect.top) / Math.max(1e-4, scaleY))}px`
            });
          }
        }
      } else {
        for (const property of ["--notedraw-md-float-x", "--notedraw-md-float-y", "--notedraw-md-float-width"]) {
          element.style.removeProperty(property);
        }
      }
    }
    for (const element of previousElements) {
      const id = element.dataset?.noteDrawMarkdownBlockId;
      if (id && next.get(id) === element) {
        continue;
      }
      this.clearMarkdownBlockElementPresentation(element);
    }
    const connectedIds = new Set(next.keys());
    const selectedIds = new Set(Array.from(this.selectedMarkdownBlockIds || []).filter((id) => connectedIds.has(id)));
    const selectedSizeChanged = selectedIds.size !== this.selectedMarkdownBlockIds.size;
    const selectedElementChanged = Array.from(selectedIds).some((id) => previousMarkdownBlockElements.get(id) !== next.get(id));
    this.markdownBlockElements = next;
    if (selectedSizeChanged || selectedElementChanged) {
      this.selectedMarkdownBlockIds = selectedIds;
      this.invalidateSelectionFrameSnapshot();
      this.rebindMarkdownSelectionActivation();
      if (selectedSizeChanged) {
        this.hideSelectionMenu();
      }
    }
    const nextParents = new Set(Array.from(next.entries()).filter(([id]) => {
      const block = this.markdownBlockRecords().find((item) => item.id === id);
      return !block?.floating && (block?.span || 12) < 12;
    }).map(([, element]) => this.markdownBlockGridContainer(element)).filter(Boolean));
    for (const parent of previousParents) {
      parent?.toggleClass?.("notedraw-md-grid", nextParents.has(parent));
    }
    for (const parent of nextParents) {
      parent?.addClass?.("notedraw-md-grid");
    }
    let repairedImplicitFloatingOverlap = false;
    if (!this.draggingStroke) {
      const records = new Map(this.markdownBlockRecords().map((block) => [block.id, block]));
      const hasImplicitFloating = Array.from(records.values()).some((block) => block.floating && !block.floatingExplicit);
      const visibleRects = hasImplicitFloating ? new Map(Array.from(next.entries()).map(([id, element]) => [id, this.markdownElementVisibleClientRect(element)])) : /* @__PURE__ */ new Map();
      for (const block of records.values()) {
        if (!block.floating || block.floatingExplicit) {
          continue;
        }
        const element = next.get(block.id);
        const rect = visibleRects.get(block.id);
        if (!rect) {
          continue;
        }
        const overlapsFlow = Array.from(next.keys()).some((otherId) => {
          if (otherId === block.id || records.get(otherId)?.floating) {
            return false;
          }
          return markdownClientRectsOverlap(rect, visibleRects.get(otherId));
        });
        if (!overlapsFlow) {
          continue;
        }
        block.floating = false;
        block.floatingExplicit = false;
        block.floatBox = null;
        block.span = 12;
        block.widthScale = 1;
        element.removeClass("is-floating");
        this.applyMarkdownBlockFlowPresentation(block, element);
        for (const property of ["--notedraw-md-float-x", "--notedraw-md-float-y", "--notedraw-md-float-width"]) {
          element.style.removeProperty(property);
        }
        repairedImplicitFloatingOverlap = true;
      }
    }
    markdownMetadataChanged = markdownMetadataChanged || repairedImplicitFloatingOverlap;
    if (repairedImplicitFloatingOverlap) {
      this.readingLogicalSizerHeight = 0;
      this.invalidateSelectionFrameSnapshot();
      this.scheduleResize({ layout: false, measure: true });
    }
    if (this.selectionFrameAwaitingMarkdownSync?.committed) {
      this.selectionFrameAwaitingMarkdownSync = null;
      this.invalidateSelectionFrameSnapshot();
      this.captureSelectionFrameSnapshot({ force: true });
      this.render();
    }
    if (this.restoreTextHighlightAnchors()) {
      this.invalidateStaticCache();
    }
    this.restorePendingMarkdownIdentityWidths();
    const markdownConnectorIds = new Set((this.drawingData?.strokes || []).flatMap((stroke) => {
      const connector = normalizeConnector(stroke?.connector);
      return connector ? [connector.fromId, connector.toId].filter((id) => String(id || "").startsWith("md:")) : [];
    }));
    if (markdownConnectorIds.size && this.syncBoundConnectors({ elementIds: markdownConnectorIds, syncGroups: false })) {
      this.invalidateStaticCache();
      this.requestRender(false);
    }
    this.scheduleDeletedTextHighlightPrune();
    this.ensureReadingHeadingCollapseIndicators();
  }
  restorePendingMarkdownIdentityWidths() {
    const pending = this.pendingMarkdownIdentityRefresh;
    if (!pending || pending.expiresAt <= Date.now()) {
      return false;
    }
    let restored = false;
    const records = new Map(this.markdownBlockRecords().map((block) => [block.id, block]));
    for (const member of pending.members || []) {
      const block = records.get(member.id);
      const element = block ? this.markdownBlockElement(block) : null;
      if (!block || block.floating || !element?.isConnected || !(Number(member.span) >= 1 && Number(member.span) < 12)) {
        continue;
      }
      const span = Number(member.span);
      const widthScale = normalizeMarkdownBlockWidthScale(member.widthScale);
      if (Number(block.span) === span && normalizeMarkdownBlockWidthScale(block.widthScale) === widthScale && !block.noteFlowAutoSpan) {
        continue;
      }
      block.span = span;
      block.widthScale = widthScale;
      block.noteFlowAutoSpan = false;
      this.applyMarkdownBlockFlowPresentation(block, element);
      this.applyMarkdownBlockWidthPresentation(block, element);
      this.applyMarkdownBlockHeightPresentation(block, element);
      restored = true;
    }
    if (restored) {
      this.readingLogicalSizerHeight = 0;
      this.scheduleResize({ layout: false, measure: true });
    }
    return restored;
  }
  ensureReadingHeadingCollapseIndicators() {
    if (this.surfaceType !== "preview" || this.embeddedSurface || !this.previewEl?.isConnected) {
      return;
    }
    if (!this.previewEl.classList?.contains?.("allow-fold-headings")) {
      return;
    }
    const headingSelector = "h1[data-heading],h2[data-heading],h3[data-heading],h4[data-heading],h5[data-heading],h6[data-heading]";
    const headings = this.previewEl.querySelectorAll?.(headingSelector) || [];
    for (const heading of headings) {
      if (heading.querySelector(".heading-collapse-indicator, .collapse-indicator")) {
        continue;
      }
      const doc = heading.ownerDocument || activeDocument;
      const arrow = doc.createElement?.("span") || document.createElement("span");
      arrow.className = "heading-collapse-indicator collapse-indicator collapse-icon";
      arrow.setAttribute("data-note-draw-heading-collapse", "true");
      const svg = doc.createElementNS?.("http://www.w3.org/2000/svg", "svg");
      if (svg) {
        svg.setAttribute("width", "24");
        svg.setAttribute("height", "24");
        svg.setAttribute("viewBox", "0 0 24 24");
        svg.setAttribute("fill", "none");
        svg.setAttribute("stroke", "currentColor");
        svg.setAttribute("stroke-width", "2");
        svg.setAttribute("stroke-linecap", "round");
        svg.setAttribute("stroke-linejoin", "round");
        svg.classList.add("svg-icon", "right-triangle");
        const path = doc.createElementNS("http://www.w3.org/2000/svg", "path");
        path.setAttribute("d", "M3 8L12 17L21 8");
        svg.appendChild(path);
        arrow.appendChild(svg);
      }
      heading.insertBefore(arrow, heading.firstChild);
    }
  }
  onHeadingCollapseIndicatorClick(event) {
    const arrow = event?.target?.closest?.(".heading-collapse-indicator");
    if (!arrow || !this.previewEl?.contains?.(arrow)) {
      return;
    }
    if (arrow.dataset?.noteDrawHeadingCollapse !== "true") {
      return;
    }
    const heading = arrow.closest("h1,h2,h3,h4,h5,h6");
    if (!heading) {
      return;
    }
    const renderer = this.readingPreviewRenderer();
    const section = renderer?.getSectionForElement?.(heading);
    const owner = heading.closest(".el-h1,.el-h2,.el-h3,.el-h4,.el-h5,.el-h6,li") || heading;
    const wasCollapsed = section ? Boolean(section.headingCollapsed) : owner.classList.contains("is-collapsed");
    const willCollapse = !wasCollapsed;
    if (event.cancelable) {
      event.preventDefault();
    }
    event.stopPropagation?.();
    if (section) {
      section.headingCollapsed = willCollapse;
    }
    const siblingHeadingSelector = ".el-h1,.el-h2,.el-h3,.el-h4,.el-h5,.el-h6";
    let sibling = owner.nextElementSibling;
    while (sibling && !sibling.matches?.(siblingHeadingSelector)) {
      const next = sibling.nextElementSibling;
      if (willCollapse) {
        if (sibling.style.display !== "none") {
          sibling.dataset.noteDrawFoldDisplay = sibling.style.display || "";
          applyElementStyles(sibling, { display: "none" });
        }
      } else {
        applyElementStyles(sibling, { display: sibling.dataset.noteDrawFoldDisplay || "" });
        delete sibling.dataset.noteDrawFoldDisplay;
      }
      sibling = next;
    }
    owner.classList.toggle("is-collapsed", willCollapse);
    arrow.classList.toggle("is-collapsed", willCollapse);
    renderer?.updateVirtualDisplay?.();
  }
  activateMarkdownSelection(block, element) {
    const selectedElement = this.markdownBlockElement(block) || element;
    if (!block?.id || !selectedElement?.isConnected || !this.selectedMarkdownBlockIds.has(block.id)) {
      this.markdownSelectionActivation = null;
      return false;
    }
    const frame = this.captureSelectionFrameSnapshot({ force: true });
    if (!frame) {
      this.markdownSelectionActivation = null;
      return false;
    }
    this.markdownSelectionActivation = {
      id: block.id,
      element: selectedElement,
      selectionKey: this.selectionStateKey()
    };
    return true;
  }
  rebindMarkdownSelectionActivation() {
    const activation = this.markdownSelectionActivation;
    if (!activation?.id || !this.selectedMarkdownBlockIds.has(activation.id)) {
      this.markdownSelectionActivation = null;
      return false;
    }
    const element = this.markdownBlockElements.get(activation.id);
    if (!element?.isConnected) {
      this.markdownSelectionActivation = null;
      return false;
    }
    this.invalidateSelectionFrameSnapshot();
    const frame = this.captureSelectionFrameSnapshot({ force: true });
    if (!frame) {
      this.markdownSelectionActivation = null;
      return false;
    }
    this.markdownSelectionActivation = {
      id: activation.id,
      element,
      selectionKey: this.selectionStateKey()
    };
    return true;
  }
  markdownSelectionCanEditOrDrag(block, element) {
    const activation = this.markdownSelectionActivation;
    const selectedElement = this.markdownBlockElement(block) || element;
    const ready = Boolean(
      block?.id && activation?.id === block.id && activation.element === selectedElement && selectedElement?.isConnected && this.selectedMarkdownBlockIds.has(block.id) && activation.selectionKey === this.selectionStateKey() && this.selectionFrameSnapshot?.key === activation.selectionKey
    );
    if (!ready) {
      this.markdownSelectionActivation = null;
    }
    return ready;
  }
  selectMarkdownBlock(element, { additive = false, toggle = false, skipGroupExpansion = false } = {}) {
    this.markdownSelectionActivation = null;
    const block = this.ensureMarkdownBlockRecord(element);
    if (!block) {
      return null;
    }
    this.syncConnectorElementGroups();
    const next = additive ? new Set(this.selectedMarkdownBlockIds) : /* @__PURE__ */ new Set();
    if (toggle && next.has(block.id)) {
      next.delete(block.id);
    } else {
      next.add(block.id);
    }
    if (!additive) {
      this.selectedStrokeIndexes.clear();
      this.selectedStrokeIndex = -1;
    }
    this.selectedMarkdownBlockIds = next;
    this.invalidateSelectionFrameSnapshot();
    if (!skipGroupExpansion) {
      this.expandSelectedGroups();
    }
    this.hideSelectionMenu();
    this.invalidateStaticCache();
    this.syncPaletteInputs();
    this.updateToolButtons();
    this.syncMarkdownBlockPresentation();
    return block;
  }
  toggleMarkdownBlockSelection(element) {
    return this.selectMarkdownBlock(element, { additive: true, toggle: true });
  }
  expandSelectedGroups() {
    const groupIds = new Set([
      ...this.getSelectedStrokeIndexes().map((index) => this.drawingData.strokes[index]?.groupId),
      ...this.getSelectedMarkdownBlocks().map((block) => block.groupId)
    ].filter((id) => id && !this.enteredElementGroupIds.has(id) && !this.elementGroup(id)?.locked));
    if (!groupIds.size) {
      return;
    }
    for (const [index, stroke] of this.drawingData.strokes.entries()) {
      if (stroke?.groupId && groupIds.has(stroke.groupId)) {
        this.selectedStrokeIndexes.add(index);
      }
    }
    for (const block of this.markdownBlockRecords()) {
      if (block.groupId && groupIds.has(block.groupId)) {
        this.selectedMarkdownBlockIds.add(block.id);
      }
    }
  }
  setSelectedStrokes(indexes, options = {}) {
    this.markdownSelectionActivation = null;
    this.syncConnectorElementGroups();
    const normalized = Array.isArray(indexes) ? indexes : [indexes];
    this.selectedStrokeIndexes = new Set(
      normalized.map((index) => Number(index)).filter((index) => Number.isInteger(index) && index >= 0 && index < this.drawingData.strokes.length)
    );
    if (options.preserveMarkdown !== true) {
      this.selectedMarkdownBlockIds.clear();
    }
    this.invalidateSelectionFrameSnapshot();
    const selected = this.getSelectedStrokeIndexes();
    this.selectedStrokeIndex = selected.length ? selected[selected.length - 1] : -1;
    if (options.skipGroupExpansion !== true) {
      this.expandSelectedGroups();
    }
    this.hideSelectionMenu();
    this.invalidateStaticCache();
    this.syncPaletteInputs();
    this.updateToolButtons();
  }
  toggleStrokeSelection(index) {
    if (!Number.isInteger(index) || index < 0 || index >= this.drawingData.strokes.length) {
      return;
    }
    const next = new Set(this.getSelectedStrokeIndexes());
    if (next.has(index)) {
      next.delete(index);
    } else {
      next.add(index);
    }
    this.setSelectedStrokes(Array.from(next), { preserveMarkdown: true });
  }
  clearSelectedStrokes() {
    this.markdownSelectionActivation = null;
    this.selectedStrokeIndexes.clear();
    this.selectedStrokeIndex = -1;
    this.selectedMarkdownBlockIds.clear();
    this.enteredElementGroupIds.clear();
    this.invalidateSelectionFrameSnapshot();
    this.hideSelectionMenu();
    this.invalidateStaticCache();
    this.syncPaletteInputs();
    if (this.toolMode === TOOL_SELECT && this.paletteOpen) {
      this.setPaletteOpen(false);
    }
    this.updateToolButtons();
    this.syncMarkdownBlockPresentation();
  }
  selectElementsById(ids = []) {
    const wanted = new Set((Array.isArray(ids) ? ids : [ids]).map((id) => String(id || "")).filter(Boolean));
    const indexes = [];
    for (const [index, stroke] of (this.drawingData?.strokes || []).entries()) {
      if (wanted.has(strokeElementId(stroke))) {
        indexes.push(index);
      }
    }
    const markdownIds = new Set(this.markdownBlockRecords().filter((block) => wanted.has(block.id)).map((block) => block.id));
    if (!indexes.length && !markdownIds.size) {
      return false;
    }
    this.setToolFromApi(TOOL_SELECT);
    this.setSelectedStrokes(indexes, { preserveMarkdown: true });
    this.selectedMarkdownBlockIds = markdownIds;
    this.invalidateSelectionFrameSnapshot();
    this.expandSelectedGroups();
    this.syncMarkdownBlockPresentation();
    this.showSelectionMenu();
    this.render();
    return true;
  }
  scrollSelectionIntoView() {
    const frame = this.getSelectedFrameCanvasRect();
    const canvasRect = this.canvas?.getBoundingClientRect?.();
    if (!frame || !canvasRect?.width || !canvasRect?.height) {
      return false;
    }
    const scaleY = canvasRect.height / Math.max(1, this.canvasRenderHeight);
    const targetRect = {
      top: canvasRect.top + (frame.y - this.canvasWindowTop) * scaleY,
      bottom: canvasRect.top + (frame.y + frame.height - this.canvasWindowTop) * scaleY
    };
    const scroller = this.scrollContainer;
    const viewport = scroller?.getBoundingClientRect?.() || {
      top: 0,
      bottom: window.innerHeight || 0
    };
    const margin = Math.min(72, Math.max(24, (viewport.bottom - viewport.top) * 0.12));
    const delta = targetRect.top < viewport.top + margin ? targetRect.top - viewport.top - margin : targetRect.bottom > viewport.bottom - margin ? targetRect.bottom - viewport.bottom + margin : 0;
    if (Math.abs(delta) < 1) {
      return true;
    }
    const root = activeDocument.scrollingElement || activeDocument.documentElement;
    if (!scroller || scroller === root || scroller === activeDocument.body || scroller === activeDocument.documentElement) {
      window.scrollBy?.(0, delta);
    } else {
      scroller.scrollTop += delta;
    }
    return true;
  }
  selectedElementLinkPayload() {
    const indexes = this.getSelectedStrokeIndexes();
    const blockIds = this.getSelectedMarkdownBlocks().map((block) => block.id);
    const ids = [
      ...indexes.map((index) => strokeElementId(this.drawingData?.strokes?.[index])).filter(Boolean),
      ...blockIds
    ];
    const file2 = normalizeVaultPath(this.file?.path || "");
    if (!ids.length || !file2) {
      return null;
    }
    const params = new URLSearchParams({ file: file2, ids: Array.from(new Set(ids)).join(",") });
    const uri = `notedraw://element?${params.toString()}`;
    const markdown = `[${this.plugin.t("elementLinkLabel")}](${uri})`;
    return { uri, markdown, ids: Array.from(new Set(ids)), file: file2 };
  }
  async copySelectedElementLink(options = {}) {
    const payload = this.selectedElementLinkPayload();
    if (!payload) {
      return { ok: false, reason: "selection-not-found" };
    }
    const { uri, markdown, ids, file: file2 } = payload;
    const copied = await writeTextToClipboard(markdown);
    if (!copied) {
      return { ok: false, reason: "clipboard-unavailable", uri, markdown };
    }
    this.hideSelectionMenu();
    if (!options.quiet) {
      new import_obsidian.Notice(this.plugin.t("copiedElementLink"));
    }
    return { ok: true, uri, markdown, ids, file: file2 };
  }
  copySelectedElements(options = {}) {
    const indexes = this.getSelectedStrokeIndexes();
    const bounds = this.getStrokeIndexesBounds(indexes);
    if (!indexes.length || !bounds) {
      return { ok: false, reason: "selection-not-found" };
    }
    const selectedIds = new Set(indexes.map((index) => strokeElementId(this.drawingData.strokes[index])).filter(Boolean));
    const strokes = indexes.map((index) => {
      const stroke = JSON.parse(JSON.stringify(this.drawingData.strokes[index]));
      if (stroke.connector?.fromId && stroke.connector?.toId && (!selectedIds.has(stroke.connector.fromId) || !selectedIds.has(stroke.connector.toId))) {
        delete stroke.connector;
      }
      return stroke;
    });
    this.plugin.elementClipboard = {
      version: 1,
      sourcePath: this.file?.path || "",
      sourceWidth: this.canvasWidth(),
      sourceHeight: this.canvasHeight(),
      bounds,
      strokes,
      copiedAt: Date.now()
    };
    const link = this.selectedElementLinkPayload();
    if (link) {
      void writeTextToClipboard(link.markdown);
    }
    this.hideSelectionMenu();
    if (!options.quiet) {
      new import_obsidian.Notice(this.plugin.t("copiedElements", { count: strokes.length }));
    }
    return { ok: true, count: strokes.length };
  }
  visibleCanvasPastePoint() {
    const frame = this.getResponsiveContentFrame();
    const x = clamp10((frame.left + Math.min(frame.width * 0.16, 72)) / this.canvasWidth(), 0.02, 0.92);
    const y = clamp10((this.canvasWindowTop + Math.min(this.canvasRenderHeight * 0.22, 140)) / this.canvasHeight(), 0.01, 0.96);
    return { x, y, t: Date.now() };
  }
  pasteCopiedElements(options = {}) {
    const clipboard = this.plugin.elementClipboard;
    if (!clipboard?.strokes?.length || !clipboard.bounds) {
      if (!options.quiet) {
        new import_obsidian.Notice(this.plugin.t("nothingToPaste"));
      }
      return { ok: false, reason: "clipboard-empty" };
    }
    const historyBefore = this.captureDrawingHistorySnapshot();
    const targetWidth = this.canvasWidth();
    const targetHeight = this.canvasHeight();
    const sourceWidth = Math.max(1, Number(clipboard.sourceWidth) || 1);
    const sourceHeight = Math.max(1, Number(clipboard.sourceHeight) || 1);
    const sourceBounds = clipboard.bounds;
    const selectedBounds = this.getSelectedStrokeBounds();
    const requested = options.point && Number.isFinite(Number(options.point.x)) && Number.isFinite(Number(options.point.y)) ? { x: clamp10(Number(options.point.x), 0, 1), y: clamp10(Number(options.point.y), 0, 1) } : null;
    const defaultPoint = selectedBounds ? {
      x: clamp10((selectedBounds.minX + 22) / targetWidth, 0, 0.96),
      y: clamp10((selectedBounds.minY + 22) / targetHeight, 0, 0.96)
    } : this.visibleCanvasPastePoint();
    const targetPoint = requested || defaultPoint;
    const groupWidth = Math.max(1, sourceBounds.maxX - sourceBounds.minX);
    const groupHeight = Math.max(1, sourceBounds.maxY - sourceBounds.minY);
    const fitScale = Math.min(
      1,
      Math.max(0.12, (targetWidth - 24) / groupWidth),
      Math.max(0.12, (targetHeight - 24) / groupHeight)
    );
    const targetX = clamp10(targetPoint.x * targetWidth, 12, Math.max(12, targetWidth - groupWidth * fitScale - 12));
    const targetY = clamp10(targetPoint.y * targetHeight, 12, Math.max(12, targetHeight - groupHeight * fitScale - 12));
    const idMap = /* @__PURE__ */ new Map();
    for (const sourceStroke of clipboard.strokes) {
      const sourceId = strokeElementId(sourceStroke);
      if (sourceId) {
        idMap.set(sourceId, createElementLayoutId(this.drawingData.strokes.length + idMap.size));
      }
    }
    const pasted = clipboard.strokes.map((sourceStroke, offset) => {
      const stroke = JSON.parse(JSON.stringify(sourceStroke));
      const sourceId = strokeElementId(stroke);
      stroke.elementId = idMap.get(sourceId) || createElementLayoutId(this.drawingData.strokes.length + offset);
      stroke.layout = null;
      stroke.locked = false;
      stroke.points = (stroke.points || []).map((point) => ({
        x: clamp10((targetX + (Number(point.x) * sourceWidth - sourceBounds.minX) * fitScale) / targetWidth, 0, 1),
        y: clamp10((targetY + (Number(point.y) * sourceHeight - sourceBounds.minY) * fitScale) / targetHeight, 0, 1),
        t: Date.now(),
        anchor: null
      }));
      if (stroke.connector) {
        const fromId = idMap.get(stroke.connector.fromId);
        const toId = idMap.get(stroke.connector.toId);
        stroke.connector = fromId && toId ? { ...stroke.connector, fromId, toId } : null;
      }
      if (Number.isFinite(Number(stroke.width))) {
        stroke.width = clamp10(Number(stroke.width) * fitScale, MIN_BRUSH_WIDTH, MAX_BRUSH_WIDTH);
      }
      if (Number.isFinite(Number(stroke.fontSize))) {
        stroke.fontSize = clamp10(Number(stroke.fontSize) * fitScale, 10, 72);
      }
      for (const key of ["textWidth", "previewWidth", "previewHeight"]) {
        if (Number.isFinite(Number(stroke[key]))) {
          stroke[key] = Number(stroke[key]) * fitScale;
        }
      }
      return normalizeStroke(stroke);
    }).filter((stroke) => stroke.points.length);
    const startIndex = this.drawingData.strokes.length;
    this.drawingData.strokes.push(...pasted);
    const indexes = pasted.map((_, index) => startIndex + index);
    this.captureResponsiveAnchorsForIndexes(indexes.filter((index) => !isConnectorStroke(this.drawingData.strokes[index])));
    this.syncBoundConnectors();
    this.setSelectedStrokes(indexes);
    this.redoStack = [];
    this.invalidateStaticCache();
    this.plugin.scheduleDrawingSave(this.file, this.drawingData, { userOperation: true });
    this.recordDrawingHistory(historyBefore);
    this.scheduleNoteFlowLayout({
      operation: pasted.some((stroke) => stroke?.noteFlow?.enabled)
    });
    this.scheduleLayoutRefresh({ settle: false });
    this.render();
    if (!options.quiet) {
      new import_obsidian.Notice(this.plugin.t("pastedElements", { count: pasted.length }));
    }
    return { ok: true, count: pasted.length, indexes };
  }
  onDocumentKeyDown(event) {
    if (!this.active || this.plugin.interactionController(this) !== this || !(event.ctrlKey || event.metaKey) || event.altKey) {
      return;
    }
    const target = event.target;
    if (target?.closest?.("input, textarea, select, [contenteditable='true']") || this.currentEditor || this.floatingTextInput) {
      return;
    }
    const key = String(event.key || "").toLowerCase();
    if (key === "c" && this.getSelectedStrokeIndexes().length) {
      this.copySelectedElements();
      event.preventDefault();
      event.stopPropagation();
    } else if (key === "v" && this.plugin.elementClipboard?.strokes?.length) {
      this.pasteCopiedElements();
      event.preventDefault();
      event.stopPropagation();
    }
  }
  noteFlowCanvasRect() {
    const rects = [this.canvas, this.staticCanvas, this.underlayCanvas].map((canvas) => canvas?.getBoundingClientRect?.()).filter(Boolean);
    return rects.find((rect) => rect.width > 1 && rect.height > 1) || rects[0] || null;
  }
  captureDraggedNoteFlowRawPreview() {
    if (!this.usesDraggedNoteFlowPlacement()) {
      this.dragNoteFlowRawPreviewPoints = null;
      this.dragNoteFlowRawPreviewNoteFlows = null;
      return;
    }
    this.dragNoteFlowRawPreviewPoints = new Map(Array.from(this.dragStrokeOriginalPoints?.keys?.() || []).flatMap((index) => {
      const stroke = this.drawingData?.strokes?.[index];
      return stroke ? [[index, stroke.points.map((point) => ({ ...point }))]] : [];
    }));
    this.dragNoteFlowRawPreviewNoteFlows = new Map(Array.from(this.dragStrokeOriginalPoints?.keys?.() || []).flatMap((index) => {
      const noteFlow = normalizeNoteFlow(this.drawingData?.strokes?.[index]?.noteFlow);
      return noteFlow ? [[index, { ...noteFlow }]] : [];
    }));
  }
  rememberDraggedNoteFlowLivePreviewState(index) {
    if (this.dragStrokeOriginalPoints?.has?.(index) || this.dragNoteFlowLivePreviewOriginals.has(index)) {
      return;
    }
    const stroke = this.drawingData?.strokes?.[index];
    if (!stroke) {
      return;
    }
    this.dragNoteFlowLivePreviewOriginals.set(index, {
      points: stroke.points.map((point) => ({ ...point })),
      noteFlow: stroke.noteFlow ? { ...stroke.noteFlow } : null
    });
  }
  applyDraggedMarkdownRawPreview() {
    if (!this.dragMarkdownOriginalElements?.size) {
      return;
    }
    const canvasRect = this.dragStrokePointerGeometry?.rect;
    const scaleX = canvasRect?.width > 0 ? canvasRect.width / Math.max(1, this.canvasWidth()) : 1;
    const scaleY = canvasRect?.height > 0 ? canvasRect.height / Math.max(1, this.canvasRenderHeight) : 1;
    const x = (Number(this.dragMarkdownClientDeltaX) || 0) / Math.max(1e-4, scaleX);
    const y = (Number(this.dragMarkdownClientDeltaY) || 0) / Math.max(1e-4, scaleY);
    for (const state of this.dragMarkdownOriginalElements.values()) {
      setNoteDrawCssProps(state.dragElement || state.element, {
        "--notedraw-md-drag-x": `${Math.round(x)}px`,
        "--notedraw-md-drag-y": `${Math.round(y)}px`
      });
    }
  }
  applyDraggedMarkdownPlacementPreview() {
    if (!this.dragMarkdownOriginalElements?.size || !this.dragNoteFlowPlacementClientDelta) {
      return false;
    }
    const canvasRect = this.dragStrokePointerGeometry?.rect;
    const scaleX = canvasRect?.width > 0 ? canvasRect.width / Math.max(1, this.canvasWidth()) : 1;
    const scaleY = canvasRect?.height > 0 ? canvasRect.height / Math.max(1, this.canvasRenderHeight) : 1;
    const clientX = (Number(this.dragMarkdownClientDeltaX) || 0) + Number(this.dragNoteFlowPlacementClientDelta.x || 0);
    const clientY = (Number(this.dragMarkdownClientDeltaY) || 0) + Number(this.dragNoteFlowPlacementClientDelta.y || 0);
    const x = clientX / Math.max(1e-4, scaleX);
    const y = clientY / Math.max(1e-4, scaleY);
    for (const state of this.dragMarkdownOriginalElements.values()) {
      setNoteDrawCssProps(state.dragElement || state.element, {
        "--notedraw-md-drag-x": `${Math.round(x)}px`,
        "--notedraw-md-drag-y": `${Math.round(y)}px`
      });
    }
    return true;
  }
  rememberDraggedNoteFlowDomStyle(element, property) {
    if (!element?.style || !property) {
      return;
    }
    this.dragNoteFlowDomPreview || (this.dragNoteFlowDomPreview = { styles: /* @__PURE__ */ new Map(), classes: /* @__PURE__ */ new Map() });
    let styles = this.dragNoteFlowDomPreview.styles.get(element);
    if (!styles) {
      styles = /* @__PURE__ */ new Map();
      this.dragNoteFlowDomPreview.styles.set(element, styles);
    }
    if (!styles.has(property)) {
      styles.set(property, {
        value: element.style.getPropertyValue(property),
        priority: element.style.getPropertyPriority(property)
      });
    }
  }
  rememberDraggedNoteFlowDomClass(element, className) {
    if (!element?.classList || !className) {
      return;
    }
    this.dragNoteFlowDomPreview || (this.dragNoteFlowDomPreview = { styles: /* @__PURE__ */ new Map(), classes: /* @__PURE__ */ new Map() });
    let classes = this.dragNoteFlowDomPreview.classes.get(element);
    if (!classes) {
      classes = /* @__PURE__ */ new Map();
      this.dragNoteFlowDomPreview.classes.set(element, classes);
    }
    if (!classes.has(className)) {
      classes.set(className, element.classList.contains(className));
    }
  }
  captureNoteFlowPeerRects() {
    const rects = /* @__PURE__ */ new Map();
    const draggedElements = new Set(Array.from(this.dragMarkdownOriginalElements?.values?.() || []).map((state) => state?.dragElement || state?.element).filter(Boolean));
    for (const [id, element] of this.markdownBlockElements || []) {
      const block = this.markdownBlockById?.(id) || this.markdownBlockRecords().find((record) => record.id === id);
      const flowElement = this.markdownBlockFlowElement(element) || element;
      if (!flowElement?.isConnected || block?.floating || draggedElements.has(flowElement)) {
        continue;
      }
      const rect = flowElement.getBoundingClientRect?.();
      if (!rect || rect.width <= 1 || rect.height <= 1) {
        continue;
      }
      rects.set(flowElement, { left: rect.left, top: rect.top });
    }
    for (const state of this.dragMarkdownOriginalElements?.values?.() || []) {
      const flowElement = state.dragElement || state.element;
      if (!flowElement?.isConnected || state.block?.floating || draggedElements.has(flowElement) || rects.has(flowElement)) {
        continue;
      }
      const rect = flowElement.getBoundingClientRect?.();
      if (rect && rect.width > 1 && rect.height > 1) {
        rects.set(flowElement, { left: rect.left, top: rect.top });
      }
    }
    return rects;
  }
  clearNoteFlowPeerAnimations() {
    if (this.dragNoteFlowPeerAnimationFrameId !== null) {
      window.cancelAnimationFrame(this.dragNoteFlowPeerAnimationFrameId);
      this.dragNoteFlowPeerAnimationFrameId = null;
    }
    const animatedElements = /* @__PURE__ */ new Set([
      ...this.dragNoteFlowPeerAnimatingElements || [],
      ...this.dragNoteFlowPeerAnimationTimers?.keys?.() || []
    ]);
    for (const element of animatedElements) {
      const timer = this.dragNoteFlowPeerAnimationTimers?.get?.(element);
      if (timer !== null) {
        window.clearTimeout(timer);
      }
      element.classList?.remove("notedraw-note-flow-peer-animating");
      element.style?.removeProperty("--notedraw-note-flow-peer-x");
      element.style?.removeProperty("--notedraw-note-flow-peer-y");
    }
    this.dragNoteFlowPeerAnimatingElements?.clear?.();
    this.dragNoteFlowPeerAnimationTimers?.clear?.();
  }
  animateNoteFlowPeerShifts(beforeRects) {
    if (!beforeRects?.size) {
      return;
    }
    this.clearNoteFlowPeerAnimations();
    const changed = [];
    for (const [element, before] of beforeRects) {
      if (!element?.isConnected || element.classList?.contains("is-floating")) {
        continue;
      }
      const after = element.getBoundingClientRect?.();
      if (!after || after.width <= 1 || after.height <= 1) {
        continue;
      }
      const x = before.left - after.left;
      const y = before.top - after.top;
      if (Math.hypot(x, y) < 1) {
        continue;
      }
      element.classList.add("notedraw-note-flow-peer-animating");
      setNoteDrawCssProps(element, {
        "--notedraw-note-flow-peer-x": `${Math.round(x)}px`,
        "--notedraw-note-flow-peer-y": `${Math.round(y)}px`
      });
      this.dragNoteFlowPeerAnimatingElements.add(element);
      changed.push(element);
    }
    if (!changed.length) {
      return;
    }
    this.dragNoteFlowPeerAnimationFrameId = window.requestAnimationFrame?.(() => {
      this.dragNoteFlowPeerAnimationFrameId = null;
      for (const element of changed) {
        if (!element?.isConnected) {
          continue;
        }
        setNoteDrawCssProps(element, {
          "--notedraw-note-flow-peer-x": "0px",
          "--notedraw-note-flow-peer-y": "0px"
        });
        const timer = window.setTimeout(() => {
          element.classList.remove("notedraw-note-flow-peer-animating");
          element.style.removeProperty("--notedraw-note-flow-peer-x");
          element.style.removeProperty("--notedraw-note-flow-peer-y");
          this.dragNoteFlowPeerAnimatingElements.delete(element);
          this.dragNoteFlowPeerAnimationTimers.delete(element);
        }, DRAG_PEER_ANIMATION_MS);
        this.dragNoteFlowPeerAnimationTimers.set(element, timer);
      }
    }) ?? null;
  }
  dragStrokeAnimationNow() {
    return Number(window.performance?.now?.()) || Date.now();
  }
  interpolatedDragStrokePoints(entry, now = this.dragStrokeAnimationNow()) {
    const elapsed = Math.max(0, now - Number(this.dragNoteFlowStrokeAnimation?.startedAt || now));
    const progress = clamp10(elapsed / Math.max(1, Number(this.dragNoteFlowStrokeAnimation?.duration) || DRAG_STROKE_ANIMATION_MS), 0, 1);
    const eased = 1 - Math.pow(1 - progress, 3);
    return entry.target.map((point, index) => {
      const from = entry.from[index] || point;
      return {
        ...point,
        x: Number(from.x) + (Number(point.x) - Number(from.x)) * eased,
        y: Number(from.y) + (Number(point.y) - Number(from.y)) * eased
      };
    });
  }
  captureDraggedNoteFlowStrokeVisualPoints(now = this.dragStrokeAnimationNow()) {
    const points = /* @__PURE__ */ new Map();
    for (const [index, stroke] of (this.drawingData?.strokes || []).entries()) {
      if (normalizeNoteFlow(stroke?.noteFlow) && !isConnectorStroke(stroke)) {
        points.set(index, stroke.points.map((point) => ({ ...point })));
      }
    }
    for (const [index, entry] of this.dragNoteFlowStrokeAnimation?.entries || []) {
      points.set(index, this.interpolatedDragStrokePoints(entry, now));
    }
    return points;
  }
  applyDraggedNoteFlowStrokeAnimation(now = this.dragStrokeAnimationNow()) {
    const animation = this.dragNoteFlowStrokeAnimation;
    if (!animation?.entries?.size) {
      return true;
    }
    const elapsed = Math.max(0, now - animation.startedAt);
    const finished = elapsed >= animation.duration;
    const indexes = [];
    const elementIds = /* @__PURE__ */ new Set();
    for (const [index, entry] of animation.entries) {
      const stroke = this.drawingData?.strokes?.[index];
      if (!stroke) {
        continue;
      }
      stroke.points = finished ? entry.target.map((point) => ({ ...point })) : this.interpolatedDragStrokePoints(entry, now);
      indexes.push(index);
      const id = strokeElementId(stroke);
      if (id) {
        elementIds.add(id);
      }
    }
    if (elementIds.size) {
      this.rememberDraggedNoteFlowConnectorStates(elementIds);
      this.syncBoundConnectors({ elementIds });
    }
    this.invalidateSelectionFrameSnapshot();
    this.invalidateStaticCache();
    if (indexes.length) {
      this.updateEmbedLayer({ indexes });
    }
    this.renderCanvas();
    return finished;
  }
  rememberDraggedNoteFlowConnectorStates(elementIds) {
    if (!elementIds?.size) {
      return;
    }
    for (const [index, stroke] of (this.drawingData?.strokes || []).entries()) {
      if (this.dragNoteFlowConnectorOriginalStates.has(index)) {
        continue;
      }
      const connector = normalizeConnector(stroke?.connector);
      if (!connector || !elementIds.has(connector.fromId) && !elementIds.has(connector.toId)) {
        continue;
      }
      this.dragNoteFlowConnectorOriginalStates.set(
        index,
        stroke.points.map((point) => ({ ...point }))
      );
    }
  }
  cancelDraggedNoteFlowStrokeAnimation(applyTarget = false) {
    if (this.dragNoteFlowStrokeAnimationFrameId !== null) {
      window.cancelAnimationFrame(this.dragNoteFlowStrokeAnimationFrameId);
      this.dragNoteFlowStrokeAnimationFrameId = null;
    }
    if (applyTarget && this.dragNoteFlowStrokeAnimation?.entries?.size) {
      const animation = this.dragNoteFlowStrokeAnimation;
      for (const [index, entry] of animation.entries) {
        const stroke = this.drawingData?.strokes?.[index];
        if (stroke) {
          stroke.points = entry.target.map((point) => ({ ...point }));
        }
      }
    }
    this.dragNoteFlowStrokeAnimation = null;
  }
  animateDraggedNoteFlowStrokeShifts(beforePoints, indexes) {
    if (!beforePoints?.size) {
      return;
    }
    const entries = /* @__PURE__ */ new Map();
    for (const index of new Set(indexes || [])) {
      const stroke = this.drawingData?.strokes?.[index];
      const from = beforePoints.get(index);
      const target = stroke?.points;
      if (!stroke || !from?.length || from.length !== target?.length || isConnectorStroke(stroke)) {
        continue;
      }
      const changed = target.some((point, pointIndex) => {
        const previous = from[pointIndex];
        return Math.hypot(Number(point.x) - Number(previous.x), Number(point.y) - Number(previous.y)) > 1e-7;
      });
      if (changed) {
        entries.set(index, {
          from: from.map((point) => ({ ...point })),
          target: target.map((point) => ({ ...point }))
        });
      }
    }
    this.cancelDraggedNoteFlowStrokeAnimation(false);
    if (!entries.size) {
      return;
    }
    this.dragNoteFlowStrokeAnimation = {
      entries,
      startedAt: this.dragStrokeAnimationNow(),
      duration: DRAG_STROKE_ANIMATION_MS
    };
    this.applyDraggedNoteFlowStrokeAnimation(this.dragNoteFlowStrokeAnimation.startedAt);
    const tick = (now) => {
      this.dragNoteFlowStrokeAnimationFrameId = null;
      if (!this.dragNoteFlowStrokeAnimation || this.destroyed) {
        return;
      }
      if (this.applyDraggedNoteFlowStrokeAnimation(Number(now) || this.dragStrokeAnimationNow())) {
        this.cancelDraggedNoteFlowStrokeAnimation(true);
        return;
      }
      this.dragNoteFlowStrokeAnimationFrameId = window.requestAnimationFrame(tick);
    };
    this.dragNoteFlowStrokeAnimationFrameId = window.requestAnimationFrame(tick);
  }
  setDraggedNoteFlowDomStyle(element, property, value, priority = "") {
    if (!element?.style) {
      return;
    }
    this.rememberDraggedNoteFlowDomStyle(element, property);
    if (value === null || value === void 0 || value === "") {
      element.style.removeProperty(property);
    } else {
      element.style.setProperty(property, value, priority);
    }
  }
  setDraggedNoteFlowDomClass(element, className, enabled) {
    if (!element?.classList) {
      return;
    }
    if (className === "notedraw-md-grid" && isNoteFlowCollectionBlock(element)) {
      return;
    }
    if (className === "notedraw-md-grid-item" && isNoteFlowCollectionBlock(element.parentElement)) {
      return;
    }
    this.rememberDraggedNoteFlowDomClass(element, className);
    element.classList.toggle(className, Boolean(enabled));
  }
  restoreDraggedNoteFlowDomPreview(options = {}) {
    const preview = this.dragNoteFlowDomPreview;
    if (preview) {
      for (const [element, styles] of preview.styles || []) {
        for (const [property, state] of styles) {
          if (state.value) {
            element.style?.setProperty?.(property, state.value, state.priority || "");
          } else {
            element.style?.removeProperty?.(property);
          }
        }
      }
      for (const [element, classes] of preview.classes || []) {
        for (const [className, enabled] of classes) {
          element.classList?.toggle?.(className, enabled);
        }
      }
    }
    const restored = /* @__PURE__ */ new Set();
    if (options.keepElements !== true) {
      for (const state of this.dragMarkdownOriginalElements?.values?.() || []) {
        const dragElement = state.dragElement || state.element;
        const marker = state.domMarker;
        if (!dragElement || !marker?.parentNode || restored.has(dragElement)) {
          continue;
        }
        marker.parentNode.insertBefore(dragElement, marker.nextSibling);
        restored.add(dragElement);
      }
    }
    if (options.keepElements !== true && preview?.createdRows?.size) {
      this.unwrapMarkdownGridRows(preview.createdRows);
    }
    this.dragNoteFlowDomPreview = null;
    return Boolean(preview || restored.size);
  }
  clearDraggedMarkdownDomMarkers(options = {}) {
    for (const state of this.dragMarkdownOriginalElements?.values?.() || []) {
      if (options.preserve === true && state.preserveDomUntilCommit) {
        continue;
      }
      state.domMarker?.remove?.();
      state.domMarker = null;
    }
  }
  settleCommittedMarkdownDomPreview(drop, committed) {
    const preview = drop?.domPreview;
    if (!preview) {
      return false;
    }
    if (!committed) {
      for (const [element, styles] of preview.styles || []) {
        for (const [property, state] of styles) {
          if (state.value) {
            element.style?.setProperty?.(property, state.value, state.priority || "");
          } else {
            element.style?.removeProperty?.(property);
          }
        }
      }
      for (const [element, classes] of preview.classes || []) {
        for (const [className, enabled] of classes) {
          element.classList?.toggle?.(className, enabled);
        }
      }
    }
    const restored = /* @__PURE__ */ new Set();
    for (const state of drop.moving || []) {
      const dragElement = state.dragElement || state.element;
      if (!committed && dragElement && state.domMarker?.parentNode && !restored.has(dragElement)) {
        state.domMarker.parentNode.insertBefore(dragElement, state.domMarker.nextSibling);
        restored.add(dragElement);
      }
      state.domMarker?.remove?.();
      state.domMarker = null;
      state.preserveDomUntilCommit = false;
    }
    if (!committed && preview.createdRows?.size) {
      this.unwrapMarkdownGridRows(preview.createdRows);
    }
    drop.domPreview = null;
    if (this.dragNoteFlowDomPreview === preview) {
      this.dragNoteFlowDomPreview = null;
    }
    return true;
  }
  repairMarkdownDomAfterCommit(drop) {
    if (!this.previewEl?.isConnected || !Array.isArray(drop?.moving)) {
      return;
    }
    const missing = drop.moving.some((state) => {
      const dragElement = state.dragElement || state.element;
      return Boolean(dragElement && !dragElement.isConnected);
    });
    if (!missing) {
      return;
    }
    const renderer = this.readingPreviewRenderer();
    if (!renderer) {
      return;
    }
    for (const section of renderer.sections || []) {
      if (section?.el && !section.el.isConnected && section.rendered !== false) {
        section.rendered = false;
      }
    }
    this.repairConnectedReadingSections(renderer);
    this.scheduleResize({ layout: false, measure: true });
    this.repairWidelyDetachedSections(renderer);
  }
  repairWidelyDetachedSections(renderer = null) {
    if (!this.previewEl?.isConnected) {
      return;
    }
    const current = renderer || this.readingPreviewRenderer();
    if (!current) {
      return;
    }
    const sections = current.sections || [];
    const renderedSections = sections.filter((section) => section?.el);
    const connectedCount = renderedSections.filter((section) => section.el.isConnected).length;
    if (renderedSections.length < 4 || connectedCount >= renderedSections.length * 0.6) {
      return;
    }
    let marked = 0;
    for (const section of sections) {
      if (section?.el && !section.el.isConnected && section.rendered !== false) {
        section.rendered = false;
        marked += 1;
      }
    }
    if (marked > 0) {
      this.repairConnectedReadingSections(current);
      this.scheduleResize({ layout: false, measure: true });
      this.scheduleEmbedRepair(600);
    }
  }
  scheduleMarkdownMoveRepair(moving) {
    const states = Array.isArray(moving) ? moving.filter((state) => state?.block) : [];
    if (!states.length || this.destroyed || !this.previewEl?.isConnected) {
      return;
    }
    for (const delay of [250, 900, 2200]) {
      window.setTimeout(() => {
        if (this.destroyed || !this.previewEl?.isConnected || this.pointerDown || this.draggingStroke) {
          return;
        }
        const missingIds = states.map((state) => state.block?.id).filter((id) => {
          return id && !this.markdownBlockElement(id);
        });
        if (!missingIds.length) {
          return;
        }
        const renderer = this.readingPreviewRenderer();
        if (!renderer) {
          return;
        }
        let marked = 0;
        for (const section of renderer.sections || []) {
          if (section?.el && !section.el.isConnected && section.rendered !== false) {
            section.rendered = false;
            marked += 1;
          }
        }
        if (marked > 0) {
          this.repairConnectedReadingSections(renderer);
          this.repairWidelyDetachedSections(renderer);
          this.scheduleResize({ layout: false, measure: true });
          this.scheduleEmbedRepair(400);
        }
        this.scheduleMarkdownAnnotationRefresh({ layout: false, delay: 0, force: true });
      }, delay);
    }
  }
  scheduleEmbedRepair(delay = 1200) {
    if (this.embedRepairTimer !== null) {
      return;
    }
    this.embedRepairTimer = window.setTimeout(() => {
      this.embedRepairTimer = null;
      if (this.destroyed || !this.previewEl?.isConnected || this.pointerDown || this.draggingStroke) {
        return;
      }
      void this.repairUnloadedEmbeds();
      this.repairUnrenderedWikilinks();
    }, delay);
  }
  async repairUnloadedEmbeds() {
    if (this.surfaceType !== "preview" || this.embeddedSurface || !this.previewEl?.isConnected || !this.plugin?.app) {
      return 0;
    }
    const embeds = this.previewEl.querySelectorAll?.("span.internal-embed[src], span.internal-embed[data-src]") || [];
    let repaired = 0;
    const maxPerPass = 6;
    for (const span of embeds) {
      if (repaired >= maxPerPass) {
        break;
      }
      if (span.classList.contains("markdown-embed") || span.querySelector(".markdown-embed-content")) {
        continue;
      }
      const target = span.getAttribute("data-src") || span.getAttribute("src");
      if (!target || target.startsWith("http://") || target.startsWith("https://")) {
        continue;
      }
      if (isAsyncFileEmbedElement(span)) {
        repaired += this.promoteAsyncFileEmbedPlaceholder(span) ? 1 : 0;
        continue;
      }
      const dest = this.plugin.app.metadataCache?.getFirstLinkpathDest?.(target, this.file?.path || "") || null;
      if (dest?.extension?.toLowerCase?.() === "pdf") {
        if (!isPendingPdfEmbedElement(span)) {
          continue;
        }
        repaired += await this.repairNativeFileEmbedPlaceholder(span, target) ? 1 : 0;
        continue;
      }
      if (!dest || dest.extension !== "md") {
        continue;
      }
      try {
        const content = await this.plugin.app.vault.read(dest);
        if (typeof content !== "string") {
          continue;
        }
        span.classList.add("markdown-embed", "inline-embed", "is-loaded");
        const contentEl = span.ownerDocument.createElement("div");
        contentEl.className = "markdown-embed-content";
        const inner = span.ownerDocument.createElement("div");
        inner.className = "markdown-preview-view markdown-rendered";
        contentEl.appendChild(inner);
        span.appendChild(contentEl);
        await import_obsidian.MarkdownRenderer.render(this.plugin.app, content, inner, dest.path, this.plugin);
        this.plugin.syncEmbeddedMarkdownControllers?.();
        repaired += 1;
      } catch (error) {
        console.error(`[${PLUGIN_ID}] Failed to render embed ${target}`, error);
      }
    }
    return repaired;
  }
  async repairNativeFileEmbedPlaceholder(span, target) {
    if (!span?.isConnected || !isPendingPdfEmbedElement(span) || span.dataset.noteDrawNativeEmbedRepair === "true") {
      return false;
    }
    span.dataset.noteDrawNativeEmbedRepair = "true";
    const host = span.ownerDocument.createElement("div");
    host.className = "markdown-rendered notedraw-native-embed-repair";
    span.parentNode?.insertBefore(host, span.nextSibling);
    try {
      const escapedTarget = String(target || "").replaceAll("]", "\\]");
      await import_obsidian.MarkdownRenderer.render(
        this.plugin.app,
        `![[${escapedTarget}]]`,
        host,
        this.file?.path || "",
        this.plugin
      );
      const rendered = Array.from(host.querySelectorAll(".internal-embed[src], .internal-embed[data-src]")).find((element) => isSameEmbedTarget(element, target));
      if (!rendered) {
        host.remove();
        delete span.dataset.noteDrawNativeEmbedRepair;
        return false;
      }
      span.replaceWith(rendered);
      host.remove();
      this.readingAsyncEmbedFilePath = "";
      this.scheduleReadingVirtualSectionSync();
      this.scheduleResize({ layout: false, measure: true });
      return true;
    } catch (error) {
      host.remove();
      delete span.dataset.noteDrawNativeEmbedRepair;
      console.error(`[${PLUGIN_ID}] Failed to render native file embed ${target}`, error);
      return false;
    }
  }
  repairUnrenderedWikilinks() {
    if (this.surfaceType !== "preview" || this.embeddedSurface || !this.previewEl?.isConnected || !this.plugin?.app) {
      return 0;
    }
    const doc = this.previewEl.ownerDocument;
    if (!doc?.createTreeWalker) {
      return 0;
    }
    const walker = doc.createTreeWalker(this.previewEl, NodeFilter.SHOW_TEXT);
    const pattern = /\[\[([^\]|]+)(?:\|([^\]]+))?\]\]/g;
    const candidates = [];
    let node;
    while (node = walker.nextNode()) {
      const text = node.nodeValue || "";
      if (!text.includes("[[") || !text.includes("]]")) {
        continue;
      }
      pattern.lastIndex = 0;
      if (pattern.test(text)) {
        candidates.push(node);
      }
    }
    let repaired = 0;
    for (const node2 of candidates) {
      const text = node2.nodeValue || "";
      if (!node2.parentElement) {
        continue;
      }
      const fragment = doc.createDocumentFragment();
      let lastIndex = 0;
      pattern.lastIndex = 0;
      let match;
      let changed = false;
      while ((match = pattern.exec(text)) !== null) {
        if (match.index > lastIndex) {
          fragment.appendChild(doc.createTextNode(text.slice(lastIndex, match.index)));
        }
        const linkPath = (match[1] || "").trim();
        const alias = (match[2] || "").trim() || linkPath;
        const dest = this.plugin.app.metadataCache?.getFirstLinkpathDest?.(linkPath, this.file?.path || "");
        const link = doc.createElement("a");
        link.className = "internal-link";
        link.textContent = alias;
        if (dest) {
          link.setAttribute("href", dest.path);
          link.setAttribute("data-href", dest.path);
        }
        fragment.appendChild(link);
        lastIndex = match.index + match[0].length;
        changed = true;
      }
      if (!changed) {
        continue;
      }
      if (lastIndex < text.length) {
        fragment.appendChild(doc.createTextNode(text.slice(lastIndex)));
      }
      node2.parentElement.replaceChild(fragment, node2);
      repaired += 1;
    }
    return repaired;
  }
  draggedNoteFlowPreviewHeight(indexes = this.draggedNoteFlowIndexes()) {
    const bounds = this.getStrokeIndexesBounds(indexes);
    const strokeHeight = bounds ? Math.max(0, bounds.maxY - bounds.minY) : 0;
    const markdownHeight = this.draggedNoteFlowMarkdownStates().reduce((height, state) => {
      const canvasBounds = state?.canvasBounds;
      if (canvasBounds) {
        return Math.max(height, canvasBounds.maxY - canvasBounds.minY);
      }
      const rect = state?.clientRect;
      return rect ? Math.max(height, rect.bottom - rect.top) : height;
    }, 0);
    return Math.max(strokeHeight, markdownHeight);
  }
  draggedNoteFlowReservationGap() {
    let gap = 12;
    for (const index of this.dragStrokeOriginalPoints?.keys?.() || []) {
      const stroke = this.drawingData?.strokes?.[index];
      const noteFlow = normalizeNoteFlow(stroke?.noteFlow);
      if (noteFlow && Number.isFinite(Number(noteFlow.gap))) {
        gap = Math.max(gap, Number(noteFlow.gap));
      }
    }
    return gap;
  }
  draggedNoteFlowRatioHeight() {
    const contentFrame = this.getResponsiveContentFrame();
    const contentWidth = Math.max(1, Number(contentFrame?.width) || this.canvasWidth());
    let height = 0;
    for (const index of this.dragStrokeOriginalPoints?.keys?.() || []) {
      const stroke = this.drawingData?.strokes?.[index];
      const noteFlow = normalizeNoteFlow(stroke?.noteFlow);
      if (noteFlow && Number(noteFlow.boxHeightRatio) > 0) {
        height = Math.max(height, Number(noteFlow.boxHeightRatio) * contentWidth);
      }
    }
    return height;
  }
  clearDraggedNoteFlowOriginReservationPreview(indexes) {
    const moved = new Set(indexes || []);
    if (!moved.size) {
      return false;
    }
    let changed = false;
    for (const [element, states] of this.noteFlowStyledElements || []) {
      for (const state of states.values()) {
        if (!moved.has(state.ownerStrokeIndex)) {
          continue;
        }
        this.setDraggedNoteFlowDomStyle(
          element,
          state.styleProperty || state.property,
          state.value || null,
          state.priority || ""
        );
        changed = true;
      }
    }
    return changed;
  }
  draggedNoteFlowRemainsInOriginGap(indexes, placement) {
    const candidate = placement?.candidate;
    const targetKey = noteFlowPlacementGapKey({
      path: placement?.path || candidate?.path || this.file?.path || "",
      line: placement?.line,
      side: placement?.side,
      blockStart: candidate?.blockStart ?? candidate?.start,
      blockEnd: candidate?.blockEnd ?? candidate?.end
    }, this.file?.path || "");
    if (!targetKey) {
      return false;
    }
    const moved = Array.from(indexes || []);
    return moved.length > 0 && moved.every((index) => {
      const original = this.dragStrokeOriginalNoteFlows?.get?.(index);
      return noteFlowPlacementGapKey(original, this.file?.path || "") === targetKey;
    });
  }
  applyDraggedNoteFlowReservationPreview(placement, indexes, reservationHeight = null) {
    if (!placement?.candidate || placement.horizontalSide) {
      return false;
    }
    const target = placement.candidate.element || placement.candidate.sourceElement;
    const height = Number.isFinite(Number(reservationHeight)) ? Math.max(0, Number(reservationHeight)) : Math.max(this.draggedNoteFlowPreviewHeight(indexes), this.draggedNoteFlowRatioHeight());
    if (!target?.style || !(height > 0)) {
      return false;
    }
    const property = placement.side === "after" ? "padding-bottom" : "padding-top";
    const existingState = this.noteFlowStyledElements.get(target)?.get(property);
    const styleProperty = existingState?.styleProperty || this.noteFlowStyleProperty(target, property);
    const applied = Math.ceil(noteFlowRowReservation({
      rowOffset: 0,
      boxHeight: height,
      gap: this.draggedNoteFlowReservationGap()
    }));
    const base = Number.isFinite(Number(existingState?.base)) ? Number(existingState.base) : Number.parseFloat(window.getComputedStyle(target).getPropertyValue(styleProperty)) || 0;
    this.dragNoteFlowReservationStyles || (this.dragNoteFlowReservationStyles = /* @__PURE__ */ new Map());
    let reservationProperties = this.dragNoteFlowReservationStyles.get(target);
    if (!reservationProperties) {
      reservationProperties = /* @__PURE__ */ new Set();
      this.dragNoteFlowReservationStyles.set(target, reservationProperties);
    }
    reservationProperties.add(styleProperty);
    this.setDraggedNoteFlowDomStyle(target, styleProperty, `${Math.ceil(base + applied)}px`, "important");
    this.setDraggedNoteFlowDomClass(target, "notedraw-note-flow-anchor", true);
    return true;
  }
  restoreDraggedNoteFlowReservationStyles() {
    const preview = this.dragNoteFlowDomPreview;
    const reservations = this.dragNoteFlowReservationStyles;
    let changed = false;
    if (preview && reservations?.size) {
      for (const [element, properties] of reservations) {
        const styles = preview.styles.get(element);
        if (!styles) {
          continue;
        }
        for (const property of properties) {
          const state = styles.get(property);
          if (!state) {
            continue;
          }
          if (state.value) {
            element.style?.setProperty?.(property, state.value, state.priority || "");
          } else {
            element.style?.removeProperty?.(property);
          }
          changed = true;
        }
      }
    }
    this.dragNoteFlowReservationStyles?.clear();
    this.dragNoteFlowReservationStyles = null;
    return changed;
  }
  applyDraggedNoteFlowAnchorDomPreview(placement, drop = null) {
    if (!placement?.horizontalSide || !placement?.candidate) {
      return false;
    }
    const candidate = placement.candidate;
    const rawTarget = candidate.inlineElement || candidate.sourceElement || findNoteFlowInlineMeasureElement(candidate.element);
    const target = this.canonicalMarkdownFlowTarget(rawTarget);
    const targetBlock = this.findMarkdownBlockRecordForElement(target);
    const row = drop?.row;
    const moving = this.draggedNoteFlowMarkdownStates();
    const allocation = drop ? this.markdownDropInlineAllocation(drop, targetBlock, moving) : this.markdownInlineRowAllocation(row, targetBlock, moving, placement.horizontalSide);
    const intendedSpan = allocation.canFit ? this.markdownInlineRowSpan(allocation, targetBlock?.id || row?.targetId, targetBlock?.span) : targetBlock?.span;
    if (targetBlock && Number(intendedSpan) < 12) {
      if (!this.applyDraggedMarkdownInlinePresentation(target, intendedSpan)) {
        this.ensureMarkdownBlockGridRow({ ...targetBlock, span: intendedSpan }, target, { preview: true });
      }
    }
    const flowElement = this.markdownBlockFlowElement(target) || target;
    const parent = flowElement?.parentElement;
    const lane = this.dragContentLaneRect || this.layoutMeasureEl?.getBoundingClientRect?.();
    const laneWidth = Number(lane?.right) - Number(lane?.left);
    const boundary = Number(placement.inlineBoundary);
    if (!target?.isConnected || !parent || !(laneWidth > 1) || !Number.isFinite(boundary)) {
      return false;
    }
    this.setDraggedNoteFlowDomClass(parent, "notedraw-md-grid", true);
    const rowMemberIds = new Set([targetBlock?.id, ...row?.memberIds || []].filter(Boolean));
    for (const id of rowMemberIds) {
      const element = this.markdownBlockElements.get(id);
      const memberFlow = this.markdownBlockFlowElement(element) || element;
      if (!memberFlow?.isConnected || memberFlow.parentNode !== parent) {
        continue;
      }
      const span = this.markdownInlineRowSpan(allocation, id, 12);
      if (this.applyDraggedMarkdownInlinePresentation(memberFlow, span)) {
        this.setDraggedNoteFlowDomStyle(element, "width", null);
        continue;
      }
      this.setDraggedNoteFlowDomClass(memberFlow, "notedraw-md-grid-item", true);
      this.setDraggedNoteFlowDomStyle(memberFlow, "grid-column", `span ${span}`);
      this.setDraggedNoteFlowDomStyle(element, "width", null);
    }
    return true;
  }
  applyDraggedMarkdownDomPreview(drop) {
    const moving = Array.from(this.dragMarkdownOriginalElements?.values?.() || []).filter((state) => !state?.block?.floating && (state.dragElement || state.element)?.isConnected);
    const target = drop?.element;
    if (!moving.length || !target?.isConnected || moving.some((state) => state.element === target)) {
      return false;
    }
    const targetBlock = this.findMarkdownBlockRecordForElement(target);
    const allocation = this.markdownDropInlineAllocation(drop, targetBlock, moving);
    const targetSpan = this.markdownInlineRowSpan(allocation, targetBlock?.id || drop?.row?.targetId, targetBlock?.span);
    if ((drop.side === "left" || drop.side === "right") && allocation.canFit && targetBlock) {
      if (!this.applyDraggedMarkdownInlinePresentation(target, targetSpan)) {
        this.ensureMarkdownBlockGridRow({ ...targetBlock, span: targetSpan }, target, { preview: true });
      }
    }
    const targetFlow = this.markdownBlockFlowElement(target) || target;
    const parent = targetFlow?.parentNode;
    if (!parent || moving.some((state) => (state.dragElement || state.element).contains?.(targetFlow))) {
      return false;
    }
    const horizontal = (drop.side === "left" || drop.side === "right") && allocation.canFit;
    const ordered = moving.slice().sort((a, b) => a.clientRect.top - b.clientRect.top || a.clientRect.left - b.clientRect.left);
    let reference = drop.side === "after" || drop.side === "right" ? targetFlow.nextSibling : targetFlow;
    for (const state of ordered) {
      const dragElement = state.dragElement || state.element;
      parent.insertBefore(dragElement, reference);
      if (drop.side === "right" || drop.side === "after") {
        reference = dragElement.nextSibling;
      }
      dragElement.classList?.add?.("is-notedraw-md-dragging");
      this.setDraggedNoteFlowDomStyle(dragElement, "--notedraw-md-drag-x", "0px");
      this.setDraggedNoteFlowDomStyle(dragElement, "--notedraw-md-drag-y", "0px");
      const span = this.markdownInlineRowSpan(allocation, state.block?.id, state.block?.span);
      if (horizontal && this.applyDraggedMarkdownInlinePresentation(dragElement, span)) {
        continue;
      }
      this.setDraggedNoteFlowDomClass(dragElement, "notedraw-md-grid-item", true);
      this.setDraggedNoteFlowDomStyle(dragElement, "grid-column", `span ${horizontal ? span : 12}`);
    }
    if (horizontal) {
      this.setDraggedNoteFlowDomClass(parent, "notedraw-md-grid", true);
      const rowMemberIds = new Set([targetBlock?.id, ...drop.row?.memberIds || []].filter(Boolean));
      for (const id of rowMemberIds) {
        const element = this.markdownBlockElements.get(id);
        const flowElement = this.markdownBlockFlowElement(element) || element;
        if (!flowElement?.isConnected || flowElement.parentNode !== parent) {
          continue;
        }
        const span = this.markdownInlineRowSpan(allocation, id, 12);
        if (this.applyDraggedMarkdownInlinePresentation(flowElement, span)) {
          this.setDraggedNoteFlowDomStyle(element, "width", null);
          continue;
        }
        this.setDraggedNoteFlowDomClass(flowElement, "notedraw-md-grid-item", true);
        this.setDraggedNoteFlowDomStyle(flowElement, "grid-column", `span ${span}`);
        this.setDraggedNoteFlowDomStyle(element, "width", null);
      }
      if (targetBlock?.widthScale && targetBlock.widthScale < 0.999) {
        this.setDraggedNoteFlowDomStyle(target, "width", null);
      }
    }
    return true;
  }
  restoreDraggedNoteFlowLivePreview(options = {}) {
    let changed = false;
    const preserveDom = options.preserveDom === true;
    const domChanged = preserveDom ? false : this.restoreDraggedNoteFlowDomPreview();
    for (const [index, points] of this.dragNoteFlowRawPreviewPoints || []) {
      const stroke = this.drawingData?.strokes?.[index];
      if (!stroke) {
        continue;
      }
      stroke.points = points.map((point) => ({ ...point }));
      const originalNoteFlow = this.dragNoteFlowRawPreviewNoteFlows?.get?.(index) || this.dragStrokeOriginalNoteFlows?.get?.(index);
      stroke.noteFlow = originalNoteFlow ? { ...originalNoteFlow } : null;
      changed = true;
    }
    for (const [index, original] of this.dragNoteFlowLivePreviewOriginals) {
      const stroke = this.drawingData?.strokes?.[index];
      if (!stroke) {
        continue;
      }
      stroke.points = original.points.map((point) => ({ ...point }));
      stroke.noteFlow = original.noteFlow ? { ...original.noteFlow } : null;
      changed = true;
    }
    if (this.dragNoteFlowPreviewElementIds.size) {
      this.syncBoundConnectors({ elementIds: new Set(this.dragNoteFlowPreviewElementIds) });
    }
    this.dragNoteFlowLivePreviewOriginals.clear();
    this.dragNoteFlowPreviewElementIds.clear();
    this.dragNoteFlowPlacementClientDelta = null;
    if (!preserveDom) {
      this.applyDraggedMarkdownRawPreview();
    }
    if (domChanged && !this.draggingStroke) {
      this.resetDragDropGeometry();
    }
    if (changed) {
      this.invalidateStaticCache();
    }
    this.dragNoteFlowReservationStyles?.clear();
    this.dragNoteFlowReservationStyles = null;
    return changed || domChanged;
  }
  applyDraggedNoteFlowLivePreview(placement, options = {}) {
    const previousApplied = this.dragNoteFlowLastAppliedPlacement;
    const previewOrderChanged = Boolean(previousApplied && placement && previousApplied.flowOrder !== placement.flowOrder);
    const previewStructureChanged = Boolean(
      previousApplied?.candidate && placement?.candidate && (previousApplied.candidate.sourceElement || previousApplied.candidate.element) !== (placement.candidate.sourceElement || placement.candidate.element)
    ) || Boolean(
      previousApplied && placement && (previousApplied.horizontalSide !== placement.horizontalSide || previousApplied.side !== placement.side || previousApplied.leftSnap !== placement.leftSnap || previousApplied.line !== placement.line)
    );
    const reuseAppliedPreview = options.skipRestore === true && Boolean(previousApplied) && !previewStructureChanged && !previewOrderChanged;
    const previewTransition = Boolean(placement) && (!previousApplied || previewStructureChanged || previewOrderChanged);
    const peerRects = previewTransition ? this.captureNoteFlowPeerRects() : null;
    const strokePoints = previewTransition ? this.captureDraggedNoteFlowStrokeVisualPoints() : null;
    if (previewStructureChanged && options.skipRestore === true) {
      this.restoreDraggedNoteFlowLivePreview();
    } else if (options.skipRestore !== true) {
      this.restoreDraggedNoteFlowLivePreview();
    }
    const movedIndexes = Array.from(this.dragStrokeOriginalPoints?.keys?.() || []);
    const resolved = this.resolveDraggedNoteFlowPlacement(placement, movedIndexes);
    if (!resolved) {
      return [];
    }
    if (!this.draggedNoteFlowRemainsInOriginGap(movedIndexes, resolved)) {
      this.clearDraggedNoteFlowOriginReservationPreview(movedIndexes);
    }
    if (reuseAppliedPreview) {
      const stableResolved = this.resolveDraggedNoteFlowPlacement(previousApplied, movedIndexes) || resolved;
      this.snapDraggedSelectionToNoteFlowPlacement(stableResolved, movedIndexes);
      for (const index of movedIndexes) {
        const id = strokeElementId(this.drawingData?.strokes?.[index]);
        if (id) {
          this.dragNoteFlowPreviewElementIds.add(id);
        }
      }
      if (this.dragNoteFlowPreviewElementIds.size) {
        this.syncBoundConnectors({ elementIds: new Set(this.dragNoteFlowPreviewElementIds) });
      }
      if (this.dragNoteFlowStrokeAnimation) {
        this.applyDraggedNoteFlowStrokeAnimation();
      }
      this.invalidateSelectionFrameSnapshot();
      this.requestRender(this.selectionHasDomStrokes() ? "interaction" : false);
      this.dragNoteFlowLastAppliedPlacement = previousApplied;
      return movedIndexes;
    }
    const drop = options.drop || this.syncMarkdownDropFromNoteFlowPlacement(resolved);
    this.applyDraggedNoteFlowAnchorDomPreview(resolved, drop);
    const markdownDomPreview = this.applyDraggedMarkdownDomPreview(drop);
    const liveResolved = this.remeasureDraggedNoteFlowPlacement(resolved);
    this.snapDraggedSelectionToNoteFlowPlacement(liveResolved, movedIndexes);
    const candidateBlockKey = liveResolved.candidate?.blockKey || noteFlowBlockKey(liveResolved.candidate, this.file?.path || "");
    const previewCandidates = (this.dragDropGeometrySnapshot()?.noteFlowCandidates || this.noteFlowCandidates()).map((candidate) => {
      const blockKey = candidate.blockKey || noteFlowBlockKey(candidate, this.file?.path || "");
      return candidateBlockKey && blockKey === candidateBlockKey ? liveResolved.candidate : candidate;
    });
    const flowedIndexes = this.reflowNoteFlowElementsAfterDrag(movedIndexes, liveResolved, {
      preview: true,
      candidates: previewCandidates
    });
    const rowKey = noteFlowPlacementRowKey({
      path: liveResolved.path,
      line: liveResolved.line,
      side: liveResolved.side,
      blockStart: liveResolved.candidate?.blockStart ?? liveResolved.candidate?.start,
      blockEnd: liveResolved.candidate?.blockEnd ?? liveResolved.candidate?.end,
      placementMode: liveResolved.horizontalSide ? "inline" : "row"
    });
    const rowExtent = (this.drawingData?.strokes || []).reduce((extent, stroke) => {
      const noteFlow = normalizeNoteFlow(stroke?.noteFlow);
      if (!noteFlow || noteFlowPlacementRowKey(noteFlow, this.file?.path || "") !== rowKey) {
        return extent;
      }
      const bounds = getStrokeBounds(stroke, this.canvasWidth(), this.canvasHeight());
      const rowOffset = Math.max(0, Number(noteFlow.rowOffset) || 0);
      return bounds ? Math.max(extent, rowOffset + bounds.maxY - bounds.minY) : extent;
    }, markdownDomPreview ? 0 : this.draggedNoteFlowPreviewHeight(movedIndexes));
    this.applyDraggedNoteFlowReservationPreview(liveResolved, movedIndexes, rowExtent);
    if (!markdownDomPreview) {
      this.applyDraggedMarkdownPlacementPreview();
    }
    if (peerRects) {
      this.animateNoteFlowPeerShifts(peerRects);
    }
    const affectedIndexes = Array.from(/* @__PURE__ */ new Set([
      ...movedIndexes,
      ...flowedIndexes
    ]));
    for (const index of affectedIndexes) {
      const id = strokeElementId(this.drawingData?.strokes?.[index]);
      if (id) {
        this.dragNoteFlowPreviewElementIds.add(id);
      }
    }
    if (strokePoints) {
      const movedSet = new Set(movedIndexes);
      this.animateDraggedNoteFlowStrokeShifts(
        strokePoints,
        affectedIndexes.filter((index) => !movedSet.has(index))
      );
    }
    if (this.dragNoteFlowPreviewElementIds.size) {
      this.syncBoundConnectors({ elementIds: new Set(this.dragNoteFlowPreviewElementIds) });
    }
    if (markdownDomPreview && (drop?.side === "left" || drop?.side === "right")) {
      this.applyDraggedMarkdownDomPreview(drop);
    }
    this.invalidateSelectionFrameSnapshot();
    this.requestRender(this.selectionHasDomStrokes() ? "interaction" : false);
    this.dragNoteFlowLastAppliedPlacement = {
      ...liveResolved,
      previewCandidates,
      previewRowOffsets: this.captureDraggedNoteFlowPreviewRowOffsets(liveResolved, movedIndexes)
    };
    return affectedIndexes;
  }
  refreshDraggedNoteFlowPreviewCandidate(candidate) {
    const sourceElement = candidate?.sourceElement;
    const element = candidate?.element;
    if (!sourceElement?.isConnected && !element?.isConnected) {
      return null;
    }
    const candidates = this.noteFlowCandidates();
    const blockKey = candidate.blockKey || noteFlowBlockKey(candidate, this.file?.path || "");
    return candidates.find((item) => item.sourceElement === sourceElement && item.start === candidate.start && item.end === candidate.end) || candidates.find((item) => item.element === element && item.blockKey === blockKey) || candidates.find((item) => item.blockKey === blockKey) || null;
  }
  remeasureDraggedNoteFlowPlacement(placement) {
    const candidate = placement?.candidate;
    if (!candidate) {
      return placement;
    }
    const owner = candidate.element || this.noteFlowLayoutElement(candidate.sourceElement);
    const inlineElement = candidate.inlineElement || findNoteFlowInlineMeasureElement(candidate.sourceElement || owner);
    const rowRect = this.markdownElementVisibleClientRect(owner) || this.markdownElementVisibleClientRect(candidate.sourceElement);
    const inlineRect = this.markdownElementVisibleClientRect(inlineElement) || rowRect;
    if (!rowRect || !inlineRect) {
      return placement;
    }
    return {
      ...placement,
      boundary: placement.horizontalSide ? inlineRect.top : placement.side === "after" ? rowRect.bottom : rowRect.top,
      candidate: {
        ...candidate,
        left: rowRect.left,
        right: rowRect.right,
        top: rowRect.top,
        bottom: rowRect.bottom,
        inlineElement,
        inlineLeft: inlineRect.left,
        inlineRight: inlineRect.right,
        inlineTop: inlineRect.top,
        inlineBottom: inlineRect.bottom
      }
    };
  }
  draggedNoteFlowIndexes(indexes = this.dragStrokeOriginalPoints?.keys?.() || []) {
    return Array.from(indexes || []).filter((index) => {
      const stroke = this.drawingData?.strokes?.[index];
      return Boolean(normalizeNoteFlow(stroke?.noteFlow)) && !isConnectorStroke(stroke);
    });
  }
  usesDraggedNoteFlowPlacement() {
    return this.toolMode === TOOL_SELECT && this.supportsNoteFlow() && (this.draggedNoteFlowIndexes().length > 0 || this.draggedNoteFlowMarkdownStates().length > 0);
  }
  ensureNoteFlowDropIndicator() {
    if (this.noteFlowDropIndicator?.isConnected) {
      return this.noteFlowDropIndicator;
    }
    this.noteFlowDropIndicator = createNoteDrawControlElement(activeDocument.body, "notedraw-note-flow-drop-indicator");
    this.noteFlowDropIndicator._noteDrawController = this;
    return this.noteFlowDropIndicator;
  }
  removeDraggedNoteFlowPlacementVisual() {
    this.dragNoteFlowTargetElement?.classList?.remove(
      "notedraw-text-sort-target-before",
      "notedraw-text-sort-target-after",
      "notedraw-text-sort-target-left",
      "notedraw-text-sort-target-right"
    );
    this.dragNoteFlowTargetElement = null;
    this.noteFlowDropIndicator?.classList?.remove("is-visible", "is-notedraw-controls-visible");
    if (this.noteFlowDropIndicator) {
      delete this.noteFlowDropIndicator.dataset.noteDrawDropSide;
      delete this.noteFlowDropIndicator.dataset.noteDrawDropLine;
      delete this.noteFlowDropIndicator.dataset.noteDrawDropMagnet;
    }
  }
  clearDraggedNoteFlowPlacement() {
    if (this.dragNoteFlowDropFrameId !== null) {
      window.cancelAnimationFrame(this.dragNoteFlowDropFrameId);
      this.dragNoteFlowDropFrameId = null;
    }
    this.dragNoteFlowDropClientX = null;
    this.dragNoteFlowDropClientY = null;
    this.dragNoteFlowPlacement = null;
    this.dragNoteFlowRebuildKey = "";
    this.dragNoteFlowRebuildSince = 0;
    this.dragNoteFlowLastAppliedPlacement = null;
    this.removeDraggedNoteFlowPlacementVisual();
  }
  applyDraggedNoteFlowPlacementFrame(clientX, clientY) {
    if (!this.usesDraggedNoteFlowPlacement() || !Number.isFinite(Number(clientX)) || !Number.isFinite(Number(clientY))) {
      this.restoreDraggedNoteFlowLivePreview();
      this.clearDraggedNoteFlowPlacement();
      return null;
    }
    this.dragNoteFlowDropClientX = Number(clientX);
    this.dragNoteFlowDropClientY = Number(clientY);
    if (this.dragNoteFlowDropFrameId !== null) {
      return this.dragNoteFlowPlacement;
    }
    this.dragNoteFlowDropFrameId = window.requestAnimationFrame(() => {
      this.dragNoteFlowDropFrameId = null;
      const pendingX = this.dragNoteFlowDropClientX;
      const pendingY = this.dragNoteFlowDropClientY;
      this.dragNoteFlowDropClientX = null;
      this.dragNoteFlowDropClientY = null;
      if (Number.isFinite(Number(pendingX)) && Number.isFinite(Number(pendingY))) {
        this.updateDraggedNoteFlowPlacement(Number(pendingX), Number(pendingY));
        if (this.syncDraggedTextHighlightAnchors()) {
          this.invalidateStaticCache();
          this.requestRender(this.selectionHasDomStrokes() ? "interaction" : false);
        }
      }
    });
    return this.dragNoteFlowPlacement;
  }
  updateDraggedNoteFlowPlacement(clientX, clientY, options = {}) {
    if (!this.usesDraggedNoteFlowPlacement() || !Number.isFinite(Number(clientX)) || !Number.isFinite(Number(clientY))) {
      this.restoreDraggedNoteFlowLivePreview();
      this.clearDraggedNoteFlowPlacement();
      this.dragMarkdownLastValidDrop = null;
      this.clearMarkdownBlockDropTarget();
      return null;
    }
    const geometry = this.dragDropGeometrySnapshot();
    const previousPlacement = this.dragNoteFlowPlacement;
    let placement = selectNoteFlowDropPlacementFromIndex(geometry?.noteFlowDropIndex, { dropY: Number(clientY) });
    let directMarkdownEntry = null;
    let directMarkdownArea = Number.POSITIVE_INFINITY;
    for (const entry of geometry?.markdownCandidates || []) {
      const rect = entry.rect;
      if (Number(clientX) < rect.left || Number(clientX) > rect.right || Number(clientY) < rect.top || Number(clientY) > rect.bottom) {
        continue;
      }
      const area = rect.width * rect.height;
      if (area < directMarkdownArea) {
        directMarkdownEntry = entry;
        directMarkdownArea = area;
      }
    }
    const directMarkdownBlock = directMarkdownEntry ? this.findMarkdownBlockRecordForElement(directMarkdownEntry.element) : null;
    const directNoteFlowCandidate = directMarkdownBlock?.id ? geometry?.noteFlowCandidateByMarkdownId?.get(directMarkdownBlock.id) : null;
    if (directNoteFlowCandidate) {
      const directRect = this.noteFlowCandidateRect(directNoteFlowCandidate, "inline") || directMarkdownEntry.rect;
      const side = Number(clientY) >= (directRect.top + directRect.bottom) / 2 ? "after" : "before";
      placement = {
        candidate: directNoteFlowCandidate,
        side,
        line: side === "after" ? directNoteFlowCandidate.end : directNoteFlowCandidate.start,
        boundary: side === "after" ? directRect.bottom : directRect.top
      };
    }
    let keptPreviousInline = false;
    if (previousPlacement?.horizontalSide && previousPlacement.candidate) {
      const previousCandidate = this.refreshDraggedNoteFlowPreviewCandidate(previousPlacement.candidate) || previousPlacement.candidate;
      const previousKey = previousPlacement.candidate.blockKey || noteFlowBlockKey(previousPlacement.candidate, this.file?.path || "");
      const freshKey = placement?.candidate ? placement.candidate.blockKey || noteFlowBlockKey(placement.candidate, this.file?.path || "") : "";
      const freshRect = placement?.candidate ? this.noteFlowCandidateRect(placement.candidate, "inline") : null;
      const enteredDifferentCandidate = Boolean(freshRect && freshKey && previousKey && freshKey !== previousKey && Number(clientY) >= freshRect.top && Number(clientY) <= freshRect.bottom);
      const previousRect = this.noteFlowCandidateRect(previousPlacement.candidate, "inline") || (previousCandidate ? this.noteFlowCandidateRect(previousCandidate, "inline") : null);
      const previousHeight = previousRect ? Math.max(1, previousRect.bottom - previousRect.top) : 0;
      const verticalTolerance = Math.max(48, previousHeight * 1.1);
      const lane = geometry?.laneRect;
      const remainsInLane = !lane || Number(clientX) >= Number(lane.left) - 32 && Number(clientX) <= Number(lane.right) + 32;
      if (!enteredDifferentCandidate && previousRect && remainsInLane && Number(clientY) >= previousRect.top - verticalTolerance && Number(clientY) <= previousRect.bottom + verticalTolerance) {
        placement = {
          candidate: previousCandidate,
          side: previousPlacement.side,
          line: previousPlacement.line,
          boundary: previousRect.top,
          flowOrder: previousPlacement.flowOrder,
          noteFlowBoundary: previousPlacement.noteFlowBoundary
        };
        keptPreviousInline = true;
      }
    }
    if (previousPlacement && !previousPlacement.horizontalSide && !previousPlacement.leftSnap && previousPlacement.candidate) {
      const previousCandidate = this.refreshDraggedNoteFlowPreviewCandidate(previousPlacement.candidate) || previousPlacement.candidate;
      if (previousCandidate) {
        const previousRect = this.noteFlowCandidateRect(previousPlacement.candidate, "row") || this.noteFlowCandidateRect(previousCandidate, "row") || previousPlacement.candidate;
        const boundary = previousPlacement.side === "after" ? previousRect.bottom : previousRect.top;
        const previousHeight = Math.max(1, previousRect.bottom - previousRect.top);
        const rowTolerance = clamp10(previousHeight * 0.1, 3, 7);
        if (Number.isFinite(Number(boundary)) && Math.abs(Number(clientY) - Number(boundary)) <= rowTolerance) {
          placement = {
            candidate: previousCandidate,
            side: previousPlacement.side,
            line: previousPlacement.line,
            boundary,
            flowOrder: previousPlacement.flowOrder,
            noteFlowBoundary: previousPlacement.noteFlowBoundary
          };
        }
      }
    }
    const target = placement?.candidate?.sourceElement || placement?.candidate?.element || null;
    const targetRect = placement?.candidate ? this.noteFlowCandidateRect(placement.candidate, "inline") : null;
    if (!placement || !target || !targetRect || targetRect.width <= 1 || targetRect.height <= 1) {
      this.restoreDraggedNoteFlowLivePreview();
      this.clearDraggedNoteFlowPlacement();
      this.dragMarkdownLastValidDrop = null;
      this.clearMarkdownBlockDropTarget();
      return null;
    }
    const laneRect = geometry?.laneRect || targetRect;
    const draggedClientBounds = this.draggedNoteFlowClientBounds();
    const draggedBounds = this.getStrokeIndexesBounds(this.draggedNoteFlowIndexes());
    const canvasRect = this.noteFlowCanvasRect();
    const draggedStrokeClientWidth = draggedBounds && canvasRect?.width > 1 ? (draggedBounds.maxX - draggedBounds.minX) * canvasRect.width / Math.max(1, this.canvasWidth()) : 0;
    const draggedMarkdownCount = this.draggedNoteFlowMarkdownStates().length;
    const movingLaneCount = draggedMarkdownCount + (draggedStrokeClientWidth > 0 ? 1 : 0);
    const laneWidth = Math.max(1, laneRect.right - laneRect.left);
    const inlineTarget = isConcreteMarkdownBlockElement(target) ? target : findNoteFlowInlineMeasureElement(target);
    const movingElements = new Set(this.draggedNoteFlowMarkdownStates().map((state) => state.element));
    const inlineRow = this.markdownDropRowMetrics(inlineTarget, movingElements);
    const equalLaneWidth = Math.max(
      MIN_INLINE_NOTE_FLOW_ITEM_WIDTH_PX,
      (laneWidth - Math.max(0, inlineRow.totalCount - 1) * INLINE_NOTE_FLOW_GAP_PX) / Math.max(1, inlineRow.totalCount)
    );
    const draggedClientWidth = Math.min(
      laneWidth,
      draggedStrokeClientWidth + draggedMarkdownCount * equalLaneWidth + Math.max(0, movingLaneCount - 1) * 10
    );
    const minimumTargetWidth = Math.min(equalLaneWidth, laneWidth * 0.45);
    const projectedTargetRight = Math.max(
      targetRect.left + minimumTargetWidth,
      Math.min(targetRect.right, laneRect.right - draggedClientWidth - 10)
    );
    const targetHeight = Math.max(1, targetRect.bottom - targetRect.top);
    const inlineEdgeBand = clamp10(targetHeight * 0.1, 2, 6);
    const inlineCaptureBand = clamp10(targetHeight * 0.85, 28, 72);
    const sameInlineCandidate = Boolean(previousPlacement?.horizontalSide && previousPlacement.candidate && placement?.candidate && (previousPlacement.candidate.blockKey || noteFlowBlockKey(previousPlacement.candidate, this.file?.path || "")) === (placement.candidate.blockKey || noteFlowBlockKey(placement.candidate, this.file?.path || "")));
    const inlineRowHit = sameInlineCandidate ? Number(clientY) >= targetRect.top - inlineCaptureBand && Number(clientY) <= targetRect.bottom + inlineCaptureBand : Number(clientY) >= targetRect.top + inlineEdgeBand && Number(clientY) <= targetRect.bottom - inlineEdgeBand;
    const horizontalRoom = inlineRowHit && movingLaneCount > 0 && inlineRow.canFit;
    const intent = resolveDragDropHorizontalIntent({
      clientX,
      targetLeft: targetRect.left,
      targetRight: targetRect.right,
      laneLeft: laneRect.left,
      laneRight: laneRect.right,
      draggedLeft: this.draggedSelectionClientLeft(),
      horizontalRoom,
      requireRightIntent: true
    });
    const horizontalSide = intent === "inline-right" ? "right" : intent === "inline-left" ? "left" : keptPreviousInline ? previousPlacement.horizontalSide : null;
    const leftSnap = intent === "line-start";
    const canonicalGap = horizontalSide ? null : canonicalNoteFlowGapPlacement(
      geometry?.noteFlowCandidates,
      { anchor: placement.candidate, side: placement.side }
    );
    const flowCandidate = canonicalGap?.anchor || placement.candidate;
    const flowSide = horizontalSide ? "after" : canonicalGap?.side || placement.side;
    const flowLine = horizontalSide ? placement.candidate.end : canonicalGap?.line ?? placement.line;
    const flowTarget = flowCandidate?.sourceElement || flowCandidate?.element || target;
    const flowTargetRect = this.noteFlowCandidateRect(
      flowCandidate,
      horizontalSide ? "inline" : "row"
    ) || targetRect;
    let flowBoundary = horizontalSide ? flowTargetRect.top : flowSide === "after" ? flowTargetRect.bottom : flowTargetRect.top;
    let flowOrder = null;
    let noteFlowBoundary = false;
    let inlineBoundary = horizontalSide === "left" ? targetRect.left - draggedClientWidth - 10 : horizontalSide ? projectedTargetRight + 10 : null;
    const placementMode = horizontalSide ? "inline" : "row";
    const canonicalRowKey = noteFlowPlacementRowKey({
      path: normalizeVaultPath(flowCandidate?.path || this.file?.path || ""),
      line: Number(flowLine),
      side: flowSide,
      blockStart: flowCandidate?.blockStart ?? flowCandidate?.start,
      blockEnd: flowCandidate?.blockEnd ?? flowCandidate?.end,
      placementMode
    });
    const moved = new Set(this.draggedNoteFlowIndexes());
    const peers = [];
    const liveCanvasRect = this.noteFlowCanvasRect();
    const liveScaleX = liveCanvasRect?.width > 1 ? liveCanvasRect.width / Math.max(1, this.canvasWidth()) : 1;
    const liveScaleY = liveCanvasRect?.height > 1 ? liveCanvasRect.height / Math.max(1, this.canvasRenderHeight) : 1;
    for (const [index, stroke] of (this.drawingData?.strokes || []).entries()) {
      if (moved.has(index) || !normalizeNoteFlow(stroke?.noteFlow) || isConnectorStroke(stroke)) {
        continue;
      }
      const original = this.dragNoteFlowLivePreviewOriginals.get(index);
      const peerStroke = original ? { ...stroke, points: original.points } : stroke;
      const peerNoteFlow = original?.noteFlow || stroke.noteFlow;
      const bounds = getStrokeBounds(peerStroke, this.canvasWidth(), this.canvasHeight());
      const canonical = bounds && this.noteFlowCanonicalGapPlacement(
        peerNoteFlow,
        geometry?.noteFlowCandidates,
        liveCanvasRect?.top + (bounds.minY - this.canvasWindowTop) * liveScaleY
      );
      if (!bounds || !canonical || noteFlowPlacementRowKey(canonical.noteFlow, this.file?.path || "") !== canonicalRowKey) {
        continue;
      }
      peers.push({
        index,
        left: liveCanvasRect.left + bounds.minX * liveScaleX,
        right: liveCanvasRect.left + bounds.maxX * liveScaleX,
        top: liveCanvasRect.top + (bounds.minY - this.canvasWindowTop) * liveScaleY,
        bottom: liveCanvasRect.top + (bounds.maxY - this.canvasWindowTop) * liveScaleY,
        order: Number.isFinite(Number(canonical.noteFlow.flowOrder)) ? Number(canonical.noteFlow.flowOrder) : index
      });
    }
    peers.sort(horizontalSide ? (a, b) => a.left - b.left || a.order - b.order || a.index - b.index : (a, b) => a.top - b.top || a.order - b.order || a.index - b.index);
    const insertionIndex = peers.findIndex((peer) => horizontalSide ? Number(clientX) < (peer.left + peer.right) / 2 : Number(clientY) < (peer.top + peer.bottom) / 2);
    flowOrder = insertionIndex >= 0 ? insertionIndex : peers.length;
    if (peers.length) {
      if (horizontalSide) {
        inlineBoundary = horizontalSide === "left" ? insertionIndex >= 0 ? peers[insertionIndex].left - draggedClientWidth - 8 : targetRect.left - draggedClientWidth - 10 : insertionIndex >= 0 ? peers[insertionIndex].left : peers[peers.length - 1].right + 8;
      } else {
        flowBoundary = insertionIndex >= 0 ? peers[insertionIndex].top : peers[peers.length - 1].bottom + 6;
      }
      noteFlowBoundary = true;
    }
    const indicator = this.ensureNoteFlowDropIndicator();
    const previous = this.dragNoteFlowPlacement;
    const targetChanged = previous?.candidate?.sourceElement !== flowTarget || previous?.candidate?.element !== flowCandidate.element || previous?.side !== flowSide || previous?.line !== Number(flowLine) || previous?.horizontalSide !== horizontalSide || previous?.leftSnap !== leftSnap;
    const boundaryJitter = (Number.isFinite(Number(previous?.inlineBoundary)) && Number.isFinite(Number(inlineBoundary)) ? Math.abs(Number(previous.inlineBoundary) - Number(inlineBoundary)) > 2 : previous?.inlineBoundary !== inlineBoundary) || (Number.isFinite(Number(previous?.boundary)) && Number.isFinite(Number(flowBoundary)) ? Math.abs(Number(previous.boundary) - Number(flowBoundary)) > 2 : previous?.boundary !== flowBoundary);
    const presentationChanged = targetChanged || boundaryJitter || previous?.flowOrder !== flowOrder;
    if (presentationChanged || !indicator.classList.contains("is-visible")) {
      this.removeDraggedNoteFlowPlacementVisual();
      this.dragNoteFlowTargetElement = flowTarget;
      indicator.dataset.noteDrawDropSide = horizontalSide || flowSide;
      indicator.dataset.noteDrawDropLine = String(flowLine);
      if (leftSnap) {
        indicator.dataset.noteDrawDropMagnet = "left";
      } else {
        delete indicator.dataset.noteDrawDropMagnet;
      }
    }
    this.dragNoteFlowPlacement = {
      path: normalizeVaultPath(placement.candidate.path || this.file?.path || ""),
      line: Number(flowLine),
      side: flowSide,
      horizontalSide,
      leftSnap,
      boundary: Number(flowBoundary),
      inlineBoundary: Number.isFinite(Number(inlineBoundary)) ? Number(inlineBoundary) : null,
      flowOrder,
      noteFlowBoundary,
      candidate: { ...flowCandidate }
    };
    const drop = this.syncMarkdownDropFromNoteFlowPlacement(this.dragNoteFlowPlacement);
    this.applyDraggedNoteFlowLivePreview(this.dragNoteFlowPlacement, { skipRestore: true, drop });
    return this.dragNoteFlowPlacement;
  }
  syncMarkdownDropFromNoteFlowPlacement(placement) {
    if (!this.dragMarkdownOriginalElements?.size || !placement?.candidate) {
      return null;
    }
    const candidate = placement.candidate;
    const rawTarget = candidate.inlineElement || candidate.sourceElement || findNoteFlowInlineMeasureElement(candidate.element);
    const target = this.canonicalMarkdownFlowTarget(rawTarget);
    const movingElements = new Set(Array.from(this.dragMarkdownOriginalElements.values()).map((state) => state.element));
    if (!target?.isConnected || movingElements.has(target) || Array.from(movingElements).some((element) => element.contains?.(target) || target.contains?.(element))) {
      this.dragMarkdownLastValidDrop = null;
      this.clearMarkdownBlockDropTarget();
      return null;
    }
    const side = placement.horizontalSide === "left" ? "left" : placement.horizontalSide === "right" ? "right" : placement.side;
    const cachedDrop = this.dragMarkdownLastValidDrop;
    const drop = cachedDrop?.element === target && cachedDrop?.side === side ? cachedDrop : this.captureMarkdownBlockDropTarget(target, side, this.markdownDropRowMetrics(target, movingElements));
    if (!drop) {
      return null;
    }
    this.dragMarkdownDropTarget = target;
    this.dragMarkdownDropSide = side;
    this.dragMarkdownLastValidDrop = drop;
    return drop;
  }
  markdownDropIsNoOp(drop) {
    const side = drop?.side;
    if (side !== "after" && side !== "before") {
      return false;
    }
    const moving = Array.isArray(drop?.moving) ? drop.moving.filter((state) => state?.domMarker) : [];
    if (!moving.length || !drop?.element?.isConnected) {
      return false;
    }
    const ordered = moving.slice().sort((a, b) => (Number(a.clientRect?.top) || 0) - (Number(b.clientRect?.top) || 0) || (Number(a.clientRect?.left) || 0) - (Number(b.clientRect?.left) || 0));
    const after = side === "after";
    const marker = after ? ordered[0]?.domMarker : ordered[ordered.length - 1]?.domMarker;
    if (!marker?.parentNode) {
      return false;
    }
    const originalNeighbour = after ? marker.previousElementSibling : marker.nextElementSibling;
    if (originalNeighbour && originalNeighbour === drop.element) {
      return true;
    }
    const targetEndLine = Number(drop.targetInfo?.lineEnd ?? drop.targetInfo?.lineStart);
    const targetStartLine = Number(drop.targetInfo?.lineStart);
    const firstStartLine = Number(ordered[0]?.sourceInfo?.lineStart);
    const lastEndLine = Number(ordered[ordered.length - 1]?.sourceInfo?.lineEnd);
    if (after) {
      return Number.isFinite(targetEndLine) && Number.isFinite(firstStartLine) && firstStartLine > targetEndLine && firstStartLine - targetEndLine <= 3;
    }
    return Number.isFinite(targetStartLine) && Number.isFinite(lastEndLine) && lastEndLine < targetStartLine && targetStartLine - lastEndLine <= 3;
  }
  prepareMarkdownAnchorForInlineNoteFlow(placement) {
    const candidate = placement?.candidate;
    const rawTarget = candidate?.inlineElement || candidate?.sourceElement || findNoteFlowInlineMeasureElement(candidate?.element);
    const target = this.canonicalMarkdownFlowTarget(rawTarget);
    if (!target?.isConnected) {
      return false;
    }
    const block = this.ensureMarkdownBlockRecord(target);
    const lane = this.dragContentLaneRect || this.layoutMeasureEl?.getBoundingClientRect?.();
    const laneWidth = Number(lane?.right) - Number(lane?.left);
    const boundary = Number(placement?.inlineBoundary);
    if (!block || !(laneWidth > 1) || !Number.isFinite(boundary)) {
      return false;
    }
    const movingStates = this.draggedNoteFlowMarkdownStates();
    const movingElements = new Set(movingStates.map((state) => state.element));
    const row = this.markdownDropRowMetrics(target, movingElements);
    const allocation = this.markdownInlineRowAllocation(row, block, movingStates, placement.horizontalSide);
    const blocks = new Map((row.memberIds || []).map((id) => {
      const item = this.markdownBlockRecords().find((record) => record.id === id);
      return item ? [id, item] : null;
    }).filter(Boolean));
    blocks.set(block.id, block);
    for (const state of movingStates) {
      if (state?.block?.id) {
        blocks.set(state.block.id, state.block);
      }
    }
    let changed = false;
    for (const [id, item] of blocks) {
      const span = this.markdownInlineRowSpan(allocation, id, item.span);
      const widthScale = Number(item.span) === Number(span) ? normalizeMarkdownBlockWidthScale(item.widthScale) : 1;
      changed = changed || item.span !== span || item.widthScale !== widthScale || item.noteFlowAutoSpan !== true;
      item.span = span;
      item.widthScale = widthScale;
      item.floating = false;
      item.floatingExplicit = false;
      item.floatBox = null;
      item.noteFlowAutoSpan = this.draggedNoteFlowMarkdownStates().length === 0;
    }
    return changed;
  }
  reconcileAutoNoteFlowMarkdownSpans() {
    const activeInlineFlows = (this.drawingData?.strokes || []).flatMap((stroke) => {
      const noteFlow = normalizeNoteFlow(stroke?.noteFlow);
      return noteFlow?.placementMode === "inline" ? [noteFlow] : [];
    });
    const activeInlineBlockKeys = new Set(activeInlineFlows.map((noteFlow) => noteFlow.blockKey).filter(Boolean));
    let changed = false;
    for (const block of this.markdownBlockRecords()) {
      if (!block.noteFlowAutoSpan) {
        continue;
      }
      if (Number(block.span) > 0 && Number(block.span) < 12) {
        block.noteFlowAutoSpan = false;
        continue;
      }
      const blockKey = noteFlowBlockKey({
        path: block.path,
        line: block.lineStart,
        blockStart: block.lineStart,
        blockEnd: block.lineEnd
      });
      if (blockKey && activeInlineBlockKeys.has(blockKey)) {
        continue;
      }
      const blockPath = normalizeVaultPath(block.path || this.file?.path || "");
      const blockStart = block.lineStart === null || block.lineStart === void 0 ? Number.NaN : Number(block.lineStart);
      const blockEnd = block.lineEnd === null || block.lineEnd === void 0 ? blockStart : Number(block.lineEnd);
      const hasSemanticInlineMatch = activeInlineFlows.some((noteFlow) => {
        const flowPath = normalizeVaultPath(noteFlow.path || this.file?.path || "");
        const flowStart = noteFlow.blockStart === null || noteFlow.blockStart === void 0 ? Number(noteFlow.line) : Number(noteFlow.blockStart);
        const flowEnd = noteFlow.blockEnd === null || noteFlow.blockEnd === void 0 ? flowStart : Number(noteFlow.blockEnd);
        return flowPath === blockPath && Number.isFinite(blockStart) && Number.isFinite(flowStart) && Math.max(blockStart, flowStart) <= Math.min(blockEnd, flowEnd);
      });
      if (hasSemanticInlineMatch) {
        continue;
      }
      if (!this.noteFlowMarkdownAnnotationComplete || !this.markdownBlockElement(block)?.isConnected) {
        continue;
      }
      block.span = 12;
      block.widthScale = 1;
      block.noteFlowAutoSpan = false;
      changed = true;
    }
    return changed;
  }
  resolveDraggedNoteFlowPlacement(placement, indexes) {
    const line = Number(placement?.line);
    const side = placement?.side;
    const path = normalizeVaultPath(placement?.path || this.file?.path || "");
    if (!Number.isFinite(line) || !["before", "after"].includes(side)) {
      return null;
    }
    const candidate = placement?.candidate;
    const exactCandidate = candidate && (candidate.sourceElement || candidate.element)?.isConnected && normalizeVaultPath(candidate.path || this.file?.path || "") === path && Number.isFinite(Number(candidate.start)) && Number.isFinite(Number(candidate.end)) && line >= Number(candidate.start) && line <= Number(candidate.end) ? candidate : null;
    return exactCandidate ? {
      candidate: exactCandidate,
      path,
      line,
      side,
      horizontalSide: placement?.horizontalSide || null,
      leftSnap: placement?.leftSnap === true,
      boundary: placement?.boundary !== null && placement?.boundary !== void 0 && Number.isFinite(Number(placement.boundary)) ? Number(placement.boundary) : null,
      inlineBoundary: placement?.inlineBoundary !== null && placement?.inlineBoundary !== void 0 && Number.isFinite(Number(placement.inlineBoundary)) ? Number(placement.inlineBoundary) : null,
      flowOrder: Number.isFinite(Number(placement?.flowOrder)) ? Number(placement.flowOrder) : null,
      noteFlowBoundary: placement?.noteFlowBoundary === true,
      previewCandidates: Array.isArray(placement?.previewCandidates) ? placement.previewCandidates : null,
      previewRowOffsets: placement?.previewRowOffsets && typeof placement.previewRowOffsets === "object" ? placement.previewRowOffsets : null
    } : null;
  }
  snapDraggedSelectionToNoteFlowPlacement(placement, indexes) {
    const movedIndexes = Array.from(indexes || []);
    const allBounds = this.getStrokeIndexesBounds(movedIndexes);
    const canvasRect = this.noteFlowCanvasRect();
    const draggedClientBounds = this.draggedNoteFlowClientBounds();
    if (!placement?.candidate || !draggedClientBounds || !canvasRect || canvasRect.width <= 1 || canvasRect.height <= 1) {
      return false;
    }
    const scaleX = canvasRect.width / Math.max(1, this.canvasWidth());
    const scaleY = canvasRect.height / Math.max(1, this.canvasRenderHeight);
    const horizontalSide = placement.horizontalSide;
    const leftSnap = placement.leftSnap === true;
    const targetRect = this.noteFlowCandidateRect(placement.candidate, horizontalSide ? "inline" : "row");
    const hasExplicitBoundary = placement?.boundary !== null && placement?.boundary !== void 0 && Number.isFinite(Number(placement.boundary));
    const boundary = horizontalSide && targetRect ? targetRect.top : hasExplicitBoundary ? Number(placement.boundary) : placement.side === "after" ? placement.candidate.bottom : placement.candidate.top;
    if (!Number.isFinite(Number(boundary))) {
      this.dragNoteFlowPlacementClientDelta = null;
      return false;
    }
    const requestedClientY = Number(boundary) - draggedClientBounds.top;
    const targetClientX = leftSnap ? Number(this.dragContentLaneRect?.left ?? targetRect?.left) : horizontalSide && Number.isFinite(Number(placement.inlineBoundary)) ? Number(placement.inlineBoundary) : Number.NaN;
    const requestedClientX = Number.isFinite(targetClientX) ? targetClientX - draggedClientBounds.left : 0;
    let deltaX = requestedClientX / Math.max(1e-4, scaleX);
    let deltaY = requestedClientY / Math.max(1e-4, scaleY);
    if (allBounds) {
      deltaX = clamp10(deltaX, -allBounds.minX, this.canvasWidth() - allBounds.maxX);
      deltaY = clamp10(deltaY, -allBounds.minY, this.canvasHeight() - allBounds.maxY);
    }
    const placementClientDelta = {
      x: allBounds ? deltaX * scaleX : requestedClientX,
      y: allBounds ? deltaY * scaleY : requestedClientY
    };
    this.dragNoteFlowPlacementClientDelta = placementClientDelta;
    const movedCanvasElements = Boolean(allBounds && movedIndexes.length);
    if (!movedCanvasElements) {
      return Math.abs(requestedClientX) >= 0.25 || Math.abs(requestedClientY) >= 0.25;
    }
    const normalizedDeltaY = deltaY / Math.max(1, this.canvasHeight());
    const normalizedDeltaX = deltaX / Math.max(1, this.canvasWidth());
    for (const index of movedIndexes) {
      const stroke = this.drawingData?.strokes?.[index];
      if (!stroke || stroke.locked) {
        continue;
      }
      stroke.points = stroke.points.map((point) => ({
        ...point,
        x: clamp10(Number(point.x) + normalizedDeltaX, 0, 1),
        y: clamp10(Number(point.y) + normalizedDeltaY, 0, 1)
      }));
    }
    this.invalidateStaticCache();
    return Math.abs(deltaY) >= 0.25 || Math.abs(deltaX) >= 0.25;
  }
  captureDraggedNoteFlowPreviewRowOffsets(placement, indexes) {
    const canvasRect = this.noteFlowCanvasRect();
    if (!placement?.candidate || !canvasRect || canvasRect.height <= 1) {
      return null;
    }
    const targetRect = this.noteFlowCandidateRect(placement.candidate, placement.horizontalSide ? "inline" : "row");
    const boundary = placement.horizontalSide ? targetRect?.top : Number.isFinite(Number(placement.boundary)) ? Number(placement.boundary) : placement.side === "after" ? placement.candidate.bottom : placement.candidate.top;
    if (!Number.isFinite(Number(boundary))) {
      return null;
    }
    const scaleY = canvasRect.height / Math.max(1, this.canvasRenderHeight);
    const anchorCanvasY = this.canvasWindowTop + (Number(boundary) - canvasRect.top) / Math.max(1e-4, scaleY);
    return Object.fromEntries(Array.from(indexes || []).flatMap((index) => {
      const stroke = this.drawingData?.strokes?.[index];
      const bounds = getStrokeBounds(stroke, this.canvasWidth(), this.canvasHeight());
      return bounds ? [[index, Math.max(0, bounds.minY - anchorCanvasY)]] : [];
    }));
  }
  captureNoteFlowAnchor(stroke, options = {}) {
    const previous = normalizeNoteFlow(stroke?.noteFlow);
    const preservedBox = normalizeNoteFlow(options.preserveBoxGeometry);
    const bounds = getStrokeBounds(stroke, this.canvasWidth(), this.canvasHeight());
    const canvasRect = this.noteFlowCanvasRect();
    if (!bounds || !canvasRect || canvasRect.width <= 1 || canvasRect.height <= 1) {
      return previous || {
        enabled: true,
        path: this.file?.path || "",
        line: null,
        side: null,
        positionBasis: null,
        positionPath: this.file?.path || "",
        positionLine: null,
        positionVersion: 0,
        placementVersion: 0,
        placementMode: "row",
        inlineSide: null,
        leftSnap: false,
        gap: 12
      };
    }
    const scaleY = canvasRect.height > 0 ? canvasRect.height / Math.max(1, this.canvasRenderHeight) : 1;
    const strokeTop = canvasRect.top + (bounds.minY - this.canvasWindowTop) * scaleY;
    const strokeBottom = canvasRect.top + (bounds.maxY - this.canvasWindowTop) * scaleY;
    const candidates = this.noteFlowCandidates();
    const explicitPlacement = Boolean(options.placement?.candidate);
    const placement = explicitPlacement ? options.placement : selectNoteFlowInsertionPlacement(candidates, { strokeTop, strokeBottom });
    const previousAnchor = options.preservePlacement === true && Number.isFinite(previous?.line) && ["before", "after"].includes(previous?.side) ? candidates.filter((candidate) => {
      return candidate.path === previous.path && previous.line >= candidate.start && previous.line <= candidate.end;
    }).sort((a, b) => a.bottom - a.top - (b.bottom - b.top) || a.order - b.order)[0] || null : null;
    const preservePlacement = Boolean(previousAnchor);
    let anchor = preservePlacement ? previousAnchor : placement?.candidate;
    let side = preservePlacement ? previous.side : placement?.side || previous?.side || null;
    let line = preservePlacement ? previous.line : Number.isFinite(placement?.line) ? placement.line : previous?.line ?? null;
    const placementMode = options.placement?.horizontalSide ? "inline" : options.placement?.candidate ? "row" : preservePlacement ? previous?.placementMode || "row" : previous?.placementMode || "row";
    if (placementMode === "row" && anchor) {
      const canonical = canonicalNoteFlowGapPlacement(candidates, { anchor, side });
      if (canonical) {
        anchor = canonical.anchor;
        side = canonical.side;
        line = canonical.line;
      }
    }
    const exactPlacement = explicitPlacement || preservePlacement && hasExactNoteFlowPlacement(previous);
    const position = exactPlacement ? selectExactNoteFlowPositionAnchor(candidates, { candidate: anchor, side, line }) : selectNoteFlowPositionAnchor(candidates, {
      strokeTop,
      maxOrderExclusive: side === "before" && anchor ? anchor.order : Number.POSITIVE_INFINITY
    });
    const positionAnchor = position?.candidate;
    const candidatesReady = candidates.length > 0;
    const contentFrame = this.getResponsiveContentFrame();
    const contentWidth = Math.max(1, Number(contentFrame?.width) || this.canvasWidth());
    const anchorRect = anchor ? this.noteFlowCandidateRect(anchor, placementMode) : null;
    const anchorBoundary = anchor ? placementMode === "inline" ? anchorRect?.top : side === "after" ? anchor.bottom : anchor.top : Number.NaN;
    const anchorCanvasY = Number.isFinite(anchorBoundary) ? this.canvasWindowTop + (anchorBoundary - canvasRect.top) / Math.max(1e-4, scaleY) : Number.NaN;
    return {
      enabled: true,
      path: anchor?.path || previous?.path || this.file?.path || "",
      line,
      side,
      blockStart: Number.isFinite(Number(anchor?.blockStart ?? anchor?.start)) ? Number(anchor?.blockStart ?? anchor?.start) : previous?.blockStart ?? line,
      blockEnd: Number.isFinite(Number(anchor?.blockEnd ?? anchor?.end)) ? Number(anchor?.blockEnd ?? anchor?.end) : previous?.blockEnd ?? line,
      blockKey: anchor?.blockKey || previous?.blockKey || "",
      positionBasis: positionAnchor ? "above" : candidatesReady ? "document" : previous?.positionBasis || null,
      positionPath: positionAnchor?.path || previous?.positionPath || this.file?.path || "",
      positionLine: positionAnchor && Number.isFinite(position?.line) ? position.line : candidatesReady ? null : previous?.positionLine ?? null,
      positionVersion: 1,
      placementVersion: anchor && Number.isFinite(Number(line)) && ["before", "after"].includes(side) ? 1 : previous?.placementVersion || 0,
      placementMode,
      inlineSide: placementMode === "inline" ? ["left", "right"].includes(options.placement?.horizontalSide) ? options.placement.horizontalSide : ["left", "right"].includes(previous?.inlineSide) ? previous.inlineSide : "right" : null,
      leftSnap: placementMode === "row" ? options.placement?.leftSnap === true || !options.placement && previous?.leftSnap === true : false,
      flowOrder: Number.isFinite(Number(options.placement?.flowOrder)) ? Number(options.placement.flowOrder) : Number.isFinite(Number(previous?.flowOrder)) ? Number(previous.flowOrder) : null,
      rowOffset: options.rowOffset !== null && options.rowOffset !== void 0 && Number.isFinite(Number(options.rowOffset)) ? Math.max(0, Number(options.rowOffset)) : Number.isFinite(anchorCanvasY) ? Math.max(0, bounds.minY - anchorCanvasY) : Math.max(0, Number(previous?.rowOffset) || 0),
      boxLeftRatio: (bounds.minX - Number(contentFrame?.left || 0)) / contentWidth,
      boxWidthRatio: preservedBox?.boxWidthRatio > 0 ? preservedBox.boxWidthRatio : Math.max(2 / contentWidth, (bounds.maxX - bounds.minX) / contentWidth),
      boxHeightRatio: preservedBox?.boxHeightRatio > 0 ? preservedBox.boxHeightRatio : Math.max(2 / contentWidth, (bounds.maxY - bounds.minY) / contentWidth),
      avoidancePath: "",
      avoidanceLine: null,
      gap: clamp10(Number(stroke?.noteFlow?.gap) || 12, 4, 64)
    };
  }
  toggleSelectedFlowMode() {
    const selection = this.selectedFlowModeElements();
    if (!selection.total || !this.supportsNoteFlow()) {
      return;
    }
    const historyBefore = this.captureDrawingHistorySnapshot();
    const markdownBounds = new Map(selection.markdownBlocks.map((block) => [
      block.id,
      this.markdownElementCanvasBounds(this.markdownBlockElement(block), { forSelection: true })
    ]));
    this.resetNoteFlowSettle();
    this.clearNoteFlowLayout();
    this.noteFlowAvoidanceAnchors.clear();
    for (const index of selection.strokes) {
      const stroke = this.drawingData.strokes[index];
      if (normalizeNoteFlow(stroke.noteFlow)) {
        stroke.noteFlow = null;
        stroke.belowMarkdown = false;
      } else {
        stroke.noteFlow = this.captureNoteFlowAnchor(stroke);
        stroke.belowMarkdown = true;
      }
    }
    for (const block of selection.markdownBlocks) {
      if (block.floating) {
        block.floating = false;
        block.floatingExplicit = false;
        block.floatBox = null;
        continue;
      }
      const bounds = markdownBounds.get(block.id);
      if (!bounds) {
        continue;
      }
      block.floating = true;
      block.floatingExplicit = true;
      block.floatBox = normalizeMarkdownFloatBox({
        x: bounds.minX / this.canvasWidth(),
        y: bounds.minY / this.canvasHeight(),
        width: (bounds.maxX - bounds.minX) / this.canvasWidth(),
        height: (bounds.maxY - bounds.minY) / this.canvasHeight()
      });
    }
    this.captureResponsiveAnchorsForIndexes(selection.strokes);
    this.hideSelectionMenu();
    this.redoStack = [];
    this.plugin.scheduleDrawingSave(this.file, this.drawingData, { userOperation: true });
    this.recordDrawingHistory(historyBefore);
    this.scheduleNoteFlowLayout({ operation: true });
    this.scheduleLayoutRefresh({ settle: false });
    this.syncSelectionMenuButtons();
    this.render();
  }
  noteFlowLayoutElement(element) {
    if (!element || !this.previewEl?.contains?.(element)) {
      return null;
    }
    return findNoteFlowMarkdownBlockElement(element, this.previewEl);
  }
  noteFlowCandidateRect(candidate, placementMode = "row") {
    if (!candidate) {
      return null;
    }
    const inline = placementMode === "inline";
    const left = inline && Number.isFinite(Number(candidate.inlineLeft)) ? Number(candidate.inlineLeft) : Number(candidate.left);
    const right = inline && Number.isFinite(Number(candidate.inlineRight)) ? Number(candidate.inlineRight) : Number(candidate.right);
    const top = inline && Number.isFinite(Number(candidate.inlineTop)) ? Number(candidate.inlineTop) : Number(candidate.top);
    const bottom = inline && Number.isFinite(Number(candidate.inlineBottom)) ? Number(candidate.inlineBottom) : Number(candidate.bottom);
    if (![left, right, top, bottom].every(Number.isFinite)) {
      return null;
    }
    return {
      left,
      right: Math.max(left, right),
      top,
      bottom: Math.max(top, bottom),
      width: Math.max(0, right - left),
      height: Math.max(0, bottom - top)
    };
  }
  noteFlowInlineLineCandidates(sourceElement, path, start, end) {
    if (sourceElement?.tagName !== "P" || !Number.isFinite(start) || !Number.isFinite(end) || end <= start) {
      return [];
    }
    const layoutElement = this.noteFlowLayoutElement(sourceElement);
    if (!layoutElement) {
      return [];
    }
    const childNodes = Array.from(sourceElement.childNodes || []);
    const breakIndexes = childNodes.map((node, index) => node.nodeName === "BR" ? index : -1).filter((index) => index >= 0);
    if (!breakIndexes.length) {
      return [];
    }
    const candidates = [];
    let segmentStart = 0;
    const segmentCount = breakIndexes.length + 1;
    const appendSegment = (segmentEnd, segmentIndex) => {
      const nodes = childNodes.slice(segmentStart, segmentEnd).filter((node) => {
        if (node.nodeType === Node.TEXT_NODE) {
          return Boolean(node.nodeValue?.trim());
        }
        return node.nodeType === Node.ELEMENT_NODE && !node.classList?.contains("notedraw-note-flow-line-spacer");
      });
      const firstNode = nodes[0];
      const lastNode = nodes[nodes.length - 1];
      if (!firstNode || !lastNode) {
        return;
      }
      const line = clamp10(Math.round(start + segmentIndex * (end - start) / Math.max(1, segmentCount - 1)), start, end);
      try {
        const range = sourceElement.ownerDocument.createRange();
        range.setStartBefore(firstNode);
        range.setEndAfter(lastNode);
        const rects = Array.from(range.getClientRects?.() || []).filter((rect) => rect.width > 1 && rect.height > 1);
        if (!rects.length) {
          return;
        }
        candidates.push({
          element: layoutElement,
          sourceElement,
          path,
          start: line,
          end: line,
          left: Math.min(...rects.map((rect) => rect.left)),
          right: Math.max(...rects.map((rect) => rect.right)),
          top: Math.min(...rects.map((rect) => rect.top)),
          bottom: Math.max(...rects.map((rect) => rect.bottom)),
          inlineElement: sourceElement,
          inlineLeft: Math.min(...rects.map((rect) => rect.left)),
          inlineRight: Math.max(...rects.map((rect) => rect.right)),
          inlineTop: Math.min(...rects.map((rect) => rect.top)),
          inlineBottom: Math.max(...rects.map((rect) => rect.bottom)),
          lineSpacer: {
            key: `${path}\0${line}\0${segmentIndex}`,
            parent: sourceElement,
            before: firstNode
          }
        });
      } catch (error) {
        void error;
      }
    };
    for (let segmentIndex = 0; segmentIndex < breakIndexes.length; segmentIndex += 1) {
      const index = breakIndexes[segmentIndex];
      appendSegment(index, segmentIndex);
      segmentStart = index + 1;
    }
    appendSegment(childNodes.length, segmentCount - 1);
    return candidates;
  }
  noteFlowVisualLineCandidates(sourceElement, path, start, end) {
    if (!sourceElement?.ownerDocument?.createRange || !Number.isFinite(start) || !Number.isFinite(end)) {
      return [];
    }
    const layoutElement = this.noteFlowLayoutElement(sourceElement);
    if (!layoutElement) {
      return [];
    }
    const textNodes = [];
    const collectTextNodes = (parent) => {
      for (const node of Array.from(parent?.childNodes || [])) {
        if (node.nodeType === 3) {
          if (String(node.nodeValue || "").trim()) {
            textNodes.push(node);
          }
          continue;
        }
        if (node.nodeType === 1 && !node.classList?.contains("notedraw-note-flow-line-spacer")) {
          collectTextNodes(node);
        }
      }
    };
    collectTextNodes(sourceElement);
    const fragments = [];
    for (const node of textNodes) {
      try {
        const range = sourceElement.ownerDocument.createRange();
        range.selectNodeContents(node);
        for (const rect of Array.from(range.getClientRects?.() || [])) {
          if (rect.width > 1 && rect.height > 1) {
            fragments.push({ top: rect.top, bottom: rect.bottom, left: rect.left, right: rect.right, height: rect.height });
          }
        }
      } catch (error) {
        void error;
      }
    }
    fragments.sort((a, b) => a.top - b.top || a.left - b.left);
    const lines = [];
    for (const fragment of fragments) {
      const line = lines.find((candidate) => {
        const tolerance = Math.max(2, Math.min(candidate.height, fragment.height) * 0.25);
        return Math.abs(candidate.top - fragment.top) <= tolerance;
      });
      if (line) {
        line.top = Math.min(line.top, fragment.top);
        line.bottom = Math.max(line.bottom, fragment.bottom);
        line.left = Math.min(line.left, fragment.left);
        line.right = Math.max(line.right, fragment.right);
        line.height = Math.max(line.height, fragment.height);
      } else {
        lines.push({ ...fragment });
      }
    }
    if (lines.length <= 1 && start === end) {
      return [];
    }
    const lineStart = Math.min(start, end);
    const lineEnd = Math.max(start, end);
    return lines.map((line, index) => {
      const sourceLine = Math.round(lineStart + index * (lineEnd - lineStart) / Math.max(1, lines.length - 1));
      return {
        element: layoutElement,
        sourceElement,
        path,
        start: sourceLine,
        end: sourceLine,
        left: line.left,
        right: line.right,
        top: line.top,
        bottom: line.bottom,
        inlineElement: sourceElement,
        inlineLeft: line.left,
        inlineRight: line.right,
        inlineTop: line.top,
        inlineBottom: line.bottom,
        visualLine: true
      };
    });
  }
  noteFlowCandidates() {
    const cacheKey = [
      this.layoutRefreshGeneration,
      this.dragDropScrollKey(),
      this.noteFlowMarkdownAnnotationComplete ? 1 : 0
    ].join(":");
    if (!this.draggingStroke && this.noteFlowCandidateCache?.key === cacheKey) {
      return this.noteFlowCandidateCache.candidates;
    }
    const grouped = /* @__PURE__ */ new Map();
    let order = 0;
    const sourceElements = /* @__PURE__ */ new Set();
    for (const sourceElement of [
      ...Array.from(this.previewEl.querySelectorAll?.("[data-note-draw-line-start]") || []),
      ...Array.from(this.previewEl.querySelectorAll?.("[data-line]") || []),
      ...Array.from(this.previewEl.querySelectorAll?.(NOTE_FLOW_RENDERED_OWNER_SELECTOR) || [])
    ]) {
      const owner = findNoteFlowMarkdownBlockElement(sourceElement, this.previewEl);
      if (owner && !isNoteFlowCollectionBlock(owner)) {
        sourceElements.add(owner);
      }
    }
    for (const element of sourceElements) {
      if (element.closest?.(".notedraw-body-control, .notedraw-embed-layer, .notedraw-underlay-embed-layer")) {
        continue;
      }
      if (!element || isNoteFlowCollectionBlock(element)) {
        continue;
      }
      const sourceElement = element;
      const sourceOwnLine = parseDataLine(sourceElement.getAttribute?.("data-line"));
      const blockOwnLine = parseDataLine(element.getAttribute?.("data-line"));
      const inherited = sourceElement.dataset.noteDrawDataLineInherited === "true";
      const canonical = sourceElement.dataset.noteDrawLineMapped === "true";
      const exactOwnDataLine = Number.isFinite(sourceOwnLine) && sourceElement.dataset.noteDrawDataLineScope !== "ancestor";
      const mappedStart = parseInteger(sourceElement.dataset.noteDrawLineStart);
      const mappedEnd = parseInteger(sourceElement.dataset.noteDrawLineEnd) ?? mappedStart;
      const mappedConfidence = Number(sourceElement.dataset.noteDrawLineConfidence);
      const reliableOwnerRange = Number.isFinite(mappedStart) && Number.isFinite(mappedEnd) && mappedEnd >= mappedStart && Number.isFinite(mappedConfidence) && mappedConfidence > 0;
      if (!canonical && !exactOwnDataLine && !reliableOwnerRange) {
        continue;
      }
      let start = mappedStart;
      let end = mappedEnd;
      if (inherited) {
        if (!Number.isFinite(blockOwnLine) || isNoteFlowCollectionBlock(element)) {
          continue;
        }
        start = blockOwnLine;
        end = blockOwnLine;
      } else if (!Number.isFinite(start)) {
        start = sourceOwnLine ?? blockOwnLine;
        end = start;
      }
      const rect = this.markdownElementVisibleClientRect(element) || this.noteFlowEmptyOwnerClientRect(element);
      if (!element || !Number.isFinite(start) || !rect || rect.width <= 1 || rect.height <= 1) {
        continue;
      }
      const inlineElement = findNoteFlowInlineMeasureElement(element);
      const inlineRect = this.markdownElementVisibleClientRect(inlineElement) || this.noteFlowEmptyOwnerClientRect(inlineElement) || rect;
      const annotatedPathElement = sourceElement.dataset.noteDrawSourcePath ? sourceElement : element.querySelector?.("[data-note-draw-source-path]");
      const identityQuality = canonical ? 3 : exactOwnDataLine ? 2 : reliableOwnerRange ? 1.5 : 1;
      const path = normalizeVaultPath(
        annotatedPathElement?.dataset?.noteDrawSourcePath || resolveRenderedSourcePath(this.plugin.app, element, this.file?.path || "") || this.file?.path || ""
      );
      let byPath = grouped.get(element);
      if (!byPath) {
        byPath = /* @__PURE__ */ new Map();
        grouped.set(element, byPath);
      }
      const existing = byPath.get(path);
      if (existing) {
        if (identityQuality > existing.identityQuality) {
          existing.start = start;
          existing.end = end;
          existing.sourceElement = sourceElement;
          existing.identityQuality = identityQuality;
        } else if (identityQuality === existing.identityQuality) {
          existing.start = Math.min(existing.start, start);
          existing.end = Math.max(existing.end, end);
        }
        existing.blockStart = existing.start;
        existing.blockEnd = existing.end;
        existing.blockKey = noteFlowBlockKey(existing);
        existing.inlineElement = inlineElement;
        existing.inlineLeft = inlineRect.left;
        existing.inlineRight = inlineRect.right;
        existing.inlineTop = inlineRect.top;
        existing.inlineBottom = inlineRect.bottom;
        if (!inherited && sourceElement.dataset.noteDrawLineMapped === "true") {
          existing.sourceElement = sourceElement;
        }
        continue;
      }
      byPath.set(path, {
        element,
        sourceElement,
        path,
        start,
        end,
        blockStart: start,
        blockEnd: end,
        blockKey: noteFlowBlockKey({ path, line: start, blockStart: start, blockEnd: end }),
        inheritedStart: parseInteger(sourceElement.dataset.noteDrawInheritedLineStart),
        inheritedEnd: parseInteger(sourceElement.dataset.noteDrawInheritedLineEnd),
        left: rect.left,
        right: rect.right,
        top: rect.top,
        bottom: rect.bottom,
        inlineElement,
        inlineLeft: inlineRect.left,
        inlineRight: inlineRect.right,
        inlineTop: inlineRect.top,
        inlineBottom: inlineRect.bottom,
        order: order++,
        identityQuality
      });
    }
    const blockCandidates = Array.from(grouped.values()).flatMap((byPath) => Array.from(byPath.values()));
    const candidates = blockCandidates.flatMap((candidate) => {
      const paragraph = candidate.inlineElement?.tagName === "P" ? candidate.inlineElement : null;
      if (!paragraph || !Number.isFinite(candidate.start) || !Number.isFinite(candidate.end)) {
        return [candidate];
      }
      const explicitLines = this.noteFlowInlineLineCandidates(
        paragraph,
        candidate.path,
        candidate.start,
        candidate.end
      );
      if (explicitLines.length <= 1) {
        return [candidate];
      }
      return explicitLines.map((line, lineIndex) => ({
        ...candidate,
        ...line,
        blockStart: line.start,
        blockEnd: line.end,
        blockKey: noteFlowBlockKey({
          path: candidate.path,
          line: line.start,
          blockStart: line.start,
          blockEnd: line.end
        }),
        order: candidate.order + lineIndex / Math.max(2, explicitLines.length + 1)
      }));
    });
    const result = candidates.filter((candidate) => {
      return !candidates.some((other) => other !== candidate && other.path === candidate.path && candidate.element !== other.element && !candidate.element.matches?.("li") && candidate.element.contains?.(other.element));
    }).sort((a, b) => a.top - b.top || a.bottom - b.bottom || a.order - b.order);
    if (!this.draggingStroke) {
      this.noteFlowCandidateCache = { key: cacheKey, candidates: result };
    }
    return result;
  }
  noteFlowTargetElement(anchor, side = anchor?.side, placementMode = "row") {
    const descriptor = anchor?.lineSpacer;
    if (descriptor?.parent?.isConnected && descriptor.before?.isConnected) {
      const existing = this.noteFlowLineSpacers.get(descriptor.key);
      if (existing?.isConnected && existing.parentElement === descriptor.parent) {
        return existing;
      }
      existing?.remove?.();
      this.markNoteFlowLayoutMutation();
      const spacer2 = descriptor.parent.createSpan({
        cls: "notedraw-note-flow-line-spacer",
        attr: { "aria-hidden": "true" }
      });
      descriptor.parent.insertBefore(spacer2, descriptor.before);
      this.noteFlowLineSpacers.set(descriptor.key, spacer2);
      return spacer2;
    }
    const owner = anchor?.element || null;
    if (!owner?.isConnected || placementMode === "inline" || !["before", "after"].includes(side)) {
      return owner;
    }
    const blockKey = anchor.blockKey || noteFlowBlockKey(anchor, this.file?.path || "");
    if (!blockKey || !owner.parentElement) {
      return owner;
    }
    const key = `${blockKey}\0${side}`;
    let spacer = this.noteFlowBlockSpacers.get(key);
    if (!spacer?.isConnected) {
      spacer?.remove?.();
      spacer = owner.ownerDocument.createElement("div");
      spacer.className = "notedraw-note-flow-block-spacer";
      spacer.setAttribute("aria-hidden", "true");
      spacer.dataset.noteDrawNoteFlowBlockKey = blockKey;
      spacer.dataset.noteDrawNoteFlowSide = side;
      this.noteFlowBlockSpacers.set(key, spacer);
      this.markNoteFlowLayoutMutation();
    }
    spacer._noteDrawMeasureElement = owner;
    owner._noteDrawExternalFlowSpacing = true;
    const expectedChild = side === "after" ? owner.lastElementChild : owner.firstElementChild;
    if (spacer.parentElement !== owner || expectedChild !== spacer) {
      this.markNoteFlowLayoutMutation();
      if (side === "after") {
        owner.appendChild(spacer);
      } else {
        owner.insertBefore(spacer, owner.firstChild);
      }
    }
    return owner;
  }
  noteFlowAnchorElement(noteFlow, allCandidates = null, strokeTop = Number.NaN) {
    const path = normalizeVaultPath(noteFlow?.path || this.file?.path || "");
    const line = Number(noteFlow?.line);
    if (!Number.isFinite(line)) {
      return null;
    }
    const allMatchingCandidates = (allCandidates || this.noteFlowCandidates()).filter((candidate) => {
      const semanticMatch = line >= candidate.start && line <= candidate.end;
      const inheritedMatch = Number.isFinite(candidate.inheritedStart) && line >= candidate.inheritedStart && line <= (Number.isFinite(candidate.inheritedEnd) ? candidate.inheritedEnd : candidate.inheritedStart);
      return candidate.path === path && (semanticMatch || inheritedMatch);
    });
    const storedBlockKey = noteFlowBlockKey(noteFlow, this.file?.path || "");
    const exactBlockCandidates = storedBlockKey ? allMatchingCandidates.filter((candidate) => candidate.blockKey === storedBlockKey) : [];
    const candidates = exactBlockCandidates.length ? exactBlockCandidates : allMatchingCandidates;
    return selectStoredNoteFlowAnchorCandidate(candidates, {
      side: noteFlow?.side,
      strokeTop
    });
  }
  noteFlowCanonicalGapPlacement(noteFlow, allCandidates = null, strokeTop = Number.NaN) {
    const normalized = normalizeNoteFlow(noteFlow);
    if (!normalized) {
      return null;
    }
    const candidates = allCandidates || this.noteFlowCandidates();
    const anchor = this.noteFlowAnchorElement(normalized, candidates, strokeTop);
    const canonical = normalized.placementMode === "row" ? canonicalNoteFlowGapPlacement(candidates, { anchor, side: normalized.side }) : anchor ? {
      anchor,
      path: normalized.path,
      line: normalized.line,
      side: normalized.side,
      blockStart: normalized.blockStart,
      blockEnd: normalized.blockEnd,
      blockKey: normalized.blockKey
    } : null;
    return canonical ? {
      anchor: canonical.anchor,
      noteFlow: {
        ...normalized,
        path: canonical.path,
        line: canonical.line,
        side: canonical.side,
        blockStart: canonical.blockStart,
        blockEnd: canonical.blockEnd,
        blockKey: canonical.blockKey
      }
    } : null;
  }
  noteFlowStrokeClientTop(stroke) {
    const bounds = getStrokeBounds(stroke, this.canvasWidth(), this.canvasHeight());
    const canvasRect = this.noteFlowCanvasRect();
    if (!bounds || !canvasRect || canvasRect.height <= 1) {
      return Number.NaN;
    }
    const scaleY = canvasRect.height / Math.max(1, this.canvasRenderHeight);
    return canvasRect.top + (bounds.minY - this.canvasWindowTop) * scaleY;
  }
  noteFlowStoredRowCanvasY(noteFlow, allCandidates = null, strokeTop = Number.NaN) {
    const candidates = allCandidates || this.noteFlowCandidates();
    const canonical = this.noteFlowCanonicalGapPlacement(noteFlow, candidates, strokeTop);
    const normalized = canonical?.noteFlow || normalizeNoteFlow(noteFlow);
    const canvasRect = this.noteFlowCanvasRect();
    if (!normalized || !canvasRect || canvasRect.height <= 1) {
      return null;
    }
    const scaleY = canvasRect.height / Math.max(1, this.canvasRenderHeight);
    const anchor = canonical?.anchor || this.noteFlowAnchorElement(normalized, candidates, strokeTop);
    if (!anchor) {
      return null;
    }
    if (normalized.placementMode === "inline") {
      const anchorRect = this.noteFlowCandidateRect(anchor, "inline");
      if (!anchorRect) {
        return null;
      }
      return this.canvasWindowTop + (anchorRect.top - canvasRect.top) / Math.max(1e-4, scaleY) + Math.max(0, Number(normalized.rowOffset) || 0);
    }
    const side = normalized.side;
    const target = this.noteFlowTargetElement(anchor, side, normalized.placementMode || "row");
    const property = side === "after" ? "padding-bottom" : "padding-top";
    const state = this.noteFlowStyledElements.get(target)?.get(property);
    const applied = state?.applied || 0;
    const spacerRect = target?.classList?.contains("notedraw-note-flow-block-spacer") ? target.getBoundingClientRect?.() : null;
    const targetRect = state?.styleProperty === "margin-top" || state?.styleProperty === "margin-bottom" ? target?.getBoundingClientRect?.() : null;
    const rowTop = targetRect && Number.isFinite(targetRect.top) && Number.isFinite(targetRect.bottom) ? side === "after" ? targetRect.bottom : targetRect.top - applied * scaleY : spacerRect && Number.isFinite(spacerRect.top) ? spacerRect.top : noteFlowReservedRowTop({
      side,
      anchorTop: anchor.top,
      anchorBottom: anchor.bottom,
      applied,
      scale: scaleY
    });
    return this.canvasWindowTop + (rowTop - canvasRect.top) / Math.max(1e-4, scaleY) + Math.max(0, Number(normalized.rowOffset) || 0);
  }
  packInlineNoteFlowItems(items, candidates, options = {}) {
    const canvasRect = options.canvasRect || this.noteFlowCanvasRect();
    const contentFrame = options.contentFrame || this.getResponsiveContentFrame();
    const gap = Math.max(0, Number(options.gap) || 8);
    const sourceItems = Array.isArray(items) ? items : [];
    if (!sourceItems.length || !canvasRect || canvasRect.width <= 1 || canvasRect.height <= 1) {
      return { placements: [], anchorBoundsByRow: /* @__PURE__ */ new Map() };
    }
    const scaleX = canvasRect.width / Math.max(1, this.canvasWidth());
    const scaleY = canvasRect.height / Math.max(1, this.canvasRenderHeight);
    const candidateCanvasBounds = (candidate) => {
      const rect = this.noteFlowCandidateRect(candidate, "inline");
      const minX = (Number(rect?.left) - canvasRect.left) / Math.max(1e-4, scaleX);
      const maxX = (Number(rect?.right) - canvasRect.left) / Math.max(1e-4, scaleX);
      const minY = this.canvasWindowTop + (Number(rect?.top) - canvasRect.top) / Math.max(1e-4, scaleY);
      const maxY = this.canvasWindowTop + (Number(rect?.bottom) - canvasRect.top) / Math.max(1e-4, scaleY);
      return [minX, maxX, minY, maxY].every(Number.isFinite) ? { minX, maxX, minY, maxY } : null;
    };
    const groups = /* @__PURE__ */ new Map();
    for (const item of sourceItems) {
      const key = item.rowKey || `index:${item.index}`;
      const group = groups.get(key) || [];
      group.push(item);
      groups.set(key, group);
    }
    const placements = [];
    const anchorBoundsByRow = /* @__PURE__ */ new Map();
    for (const [rowKey, group] of groups) {
      const anchor = group[0]?.anchor;
      const anchorBounds = candidateCanvasBounds(anchor);
      if (!anchorBounds) {
        placements.push(...group.map((item) => ({
          id: item.id,
          index: item.index,
          minX: item.minX,
          maxX: item.maxX,
          minY: item.minY,
          maxY: item.maxY,
          rowKey,
          deltaX: 0,
          deltaY: 0
        })));
        continue;
      }
      anchorBoundsByRow.set(rowKey, anchorBounds);
      const anchorBlockKey = anchor.blockKey || noteFlowBlockKey(anchor, this.file?.path || "");
      const blockerKeys = /* @__PURE__ */ new Set();
      const blockers = (Array.isArray(candidates) ? candidates : []).flatMap((candidate) => {
        const blockKey = candidate.blockKey || noteFlowBlockKey(candidate, this.file?.path || "");
        const candidateRect = this.noteFlowCandidateRect(candidate, "inline");
        const anchorRect = this.noteFlowCandidateRect(anchor, "inline");
        const sharesRow = candidate !== anchor && blockKey && blockKey !== anchorBlockKey && !blockerKeys.has(blockKey) && candidateRect && anchorRect && Math.min(candidateRect.bottom, anchorRect.bottom) - Math.max(candidateRect.top, anchorRect.top) > 0.5;
        if (!sharesRow) {
          return [];
        }
        const blocker = candidateCanvasBounds(candidate);
        if (!blocker) {
          return [];
        }
        blockerKeys.add(blockKey);
        return [blocker];
      });
      const packed = packNoteFlowInlineRectangles(group, {
        anchor: anchorBounds,
        blockers,
        laneLeft: Number(contentFrame?.left) || 0,
        laneRight: (Number(contentFrame?.left) || 0) + Math.max(1, Number(contentFrame?.width) || this.canvasWidth()),
        gap
      });
      placements.push(...packed.map((placement) => ({ ...placement, rowKey })));
    }
    return { placements, anchorBoundsByRow };
  }
  alignNoteFlowStrokesToReservedRows(allCandidates = null, options = {}) {
    const measureOnly = options.measureOnly === true;
    const interaction = options.interaction === true;
    if (!measureOnly && !interaction && (this.draggingStroke || this.resizingSelection) || !this.supportsNoteFlow() || !this.previewEl?.isConnected) {
      return false;
    }
    const canvasWidth = this.canvasWidth();
    const canvasHeight = this.canvasHeight();
    const contentFrame = this.getResponsiveContentFrame();
    if (!isStableResponsiveCaptureFrame(canvasWidth, contentFrame)) {
      return false;
    }
    const canvasRect = this.noteFlowCanvasRect();
    if (!canvasRect || canvasRect.height <= 1) {
      return false;
    }
    const scaleY = canvasRect.height / Math.max(1, this.canvasRenderHeight);
    const candidates = allCandidates || this.noteFlowCandidates();
    const targets = [];
    const stableFallbackWidth = (layout, bounds) => {
      const sourceWidth = Number(layout?.sourceFrame?.contentWidth) || 0;
      const savedWidth = Number(layout?.box?.width) || 0;
      if (sourceWidth >= 24 && savedWidth > 0) {
        return savedWidth * contentFrame.width / sourceWidth;
      }
      return Math.max(0, bounds.maxX - bounds.minX);
    };
    let changed = false;
    for (const [index, stroke] of (this.drawingData?.strokes || []).entries()) {
      const storedNoteFlow = normalizeNoteFlow(stroke?.noteFlow);
      const layout = normalizeElementLayout(stroke?.layout);
      const bounds = getStrokeBounds(stroke, canvasWidth, canvasHeight);
      if (!storedNoteFlow || !bounds || !(storedNoteFlow.boxWidthRatio > 0) || !(storedNoteFlow.boxHeightRatio > 0) || isConnectorStroke(stroke)) {
        continue;
      }
      const strokeTop = canvasRect.top + (bounds.minY - this.canvasWindowTop) * scaleY;
      const canonical = this.noteFlowCanonicalGapPlacement(storedNoteFlow, candidates, strokeTop);
      const noteFlow = canonical?.noteFlow || storedNoteFlow;
      const anchor = canonical?.anchor || this.noteFlowAnchorElement(noteFlow, candidates, strokeTop);
      const rowY = this.noteFlowStoredRowCanvasY(noteFlow, candidates, strokeTop);
      if (!Number.isFinite(rowY) || !anchor) {
        continue;
      }
      const projectedTargetBox = projectStableNoteFlowBox({
        boxLeftRatio: noteFlow.boxLeftRatio,
        boxWidthRatio: noteFlow.boxWidthRatio,
        boxHeightRatio: noteFlow.boxHeightRatio,
        contentLeft: contentFrame?.left,
        contentWidth: contentFrame?.width,
        canvasWidth,
        fallbackWidth: stableFallbackWidth(layout, bounds),
        y: rowY
      });
      const targetBox = this.noteFlowOperationPending ? {
        x: bounds.minX,
        y: rowY,
        width: Math.max(1e-3, bounds.maxX - bounds.minX),
        height: Math.max(1e-3, bounds.maxY - bounds.minY)
      } : projectedTargetBox;
      targets.push({
        index,
        stroke,
        noteFlow,
        layout,
        bounds,
        targetBox,
        anchor,
        placementMode: noteFlow.placementMode === "inline" ? "inline" : "row",
        order: Number.isFinite(Number(noteFlow.flowOrder)) ? Number(noteFlow.flowOrder) : index,
        rowKey: noteFlowPlacementRowKey(noteFlow, this.file?.path || "")
      });
    }
    const rowTargets = targets.filter((target) => target.placementMode !== "inline");
    const placements = reflowNoteFlowRectangles(rowTargets.map((target) => ({
      id: strokeElementId(target.stroke) || String(target.index),
      index: target.index,
      order: target.order,
      minX: target.targetBox.x,
      maxX: target.targetBox.x + target.targetBox.width,
      minY: target.targetBox.y,
      maxY: target.targetBox.y + target.targetBox.height,
      baseMinY: target.targetBox.y,
      originalMinY: target.targetBox.y,
      rowKey: target.rowKey,
      overlapGroup: isNoteFlowInkStroke(target.stroke) ? `ink:${target.rowKey}` : "",
      gap: Math.min(8, target.noteFlow.gap)
    })), { gap: 6 });
    const inlineItems = targets.filter((target) => target.placementMode === "inline").map((target) => ({
      id: strokeElementId(target.stroke) || String(target.index),
      index: target.index,
      order: target.order,
      minX: target.targetBox.x,
      maxX: target.targetBox.x + target.targetBox.width,
      minY: target.targetBox.y,
      maxY: target.targetBox.y + target.targetBox.height,
      rowKey: target.rowKey,
      overlapGroup: isNoteFlowInkStroke(target.stroke) ? `ink:${target.rowKey}` : "",
      anchor: target.anchor
    }));
    const inlinePacking = this.packInlineNoteFlowItems(inlineItems, candidates, {
      canvasRect,
      contentFrame,
      gap: 8
    });
    placements.push(...inlinePacking.placements);
    for (const target of targets.filter((item) => item.placementMode === "inline")) {
      target.anchorCanvasBounds = inlinePacking.anchorBoundsByRow.get(target.rowKey) || null;
    }
    const settledByIndex = new Map(placements.map((placement) => [placement.index, placement]));
    const settledRowExtents = /* @__PURE__ */ new Map();
    for (const target of targets) {
      const settled = settledByIndex.get(target.index);
      const targetY = settled?.minY ?? target.targetBox.y;
      if (target.rowKey) {
        const extent = target.placementMode === "inline" ? Math.max(0, targetY + target.targetBox.height - Number(target.anchorCanvasBounds?.maxY ?? targetY)) : noteFlowSettledRowExtent({
          rowOffset: target.noteFlow.rowOffset,
          boxHeight: target.targetBox.height,
          displacement: targetY - target.targetBox.y
        });
        settledRowExtents.set(target.rowKey, Math.max(
          settledRowExtents.get(target.rowKey) || 0,
          extent
        ));
      }
    }
    const previousExtentSignature = Array.from(this.noteFlowSettledRowExtents || []).map(([key, value]) => `${key}:${Math.round(value * 100)}`).join("|");
    const nextExtentSignature = Array.from(settledRowExtents).map(([key, value]) => `${key}:${Math.round(value * 100)}`).join("|");
    const extentsChanged = previousExtentSignature !== nextExtentSignature;
    this.noteFlowSettledRowExtents = settledRowExtents;
    if (measureOnly) {
      return extentsChanged;
    }
    for (const target of targets) {
      const { stroke, noteFlow, layout, bounds } = target;
      const settled = settledByIndex.get(target.index);
      const targetBox = settled ? {
        ...target.targetBox,
        x: settled.minX,
        y: settled.minY,
        width: Math.max(1e-3, settled.maxX - settled.minX)
      } : target.targetBox;
      const nextPoints = this.noteFlowOperationPending ? translateNoteFlowPointsToRow(stroke.points, bounds, targetBox.y, {
        canvasWidth,
        canvasHeight
      }) : projectNoteFlowPointsToBox(stroke.points, bounds, targetBox, {
        canvasWidth,
        canvasHeight
      });
      const moved = nextPoints.some((point, index) => {
        const previous = stroke.points?.[index];
        return !previous || Math.abs(Number(point.x) - Number(previous.x)) * canvasWidth > 0.25 || Math.abs(Number(point.y) - Number(previous.y)) * canvasHeight > 0.25;
      });
      const layoutBox = layout?.box;
      const staleLayout = !layoutBox || Math.abs(layoutBox.x - targetBox.x) > 0.25 || Math.abs(layoutBox.y - targetBox.y) > 0.25 || Math.abs(layoutBox.width - targetBox.width) > 0.25 || Math.abs(layoutBox.height - targetBox.height) > 0.25;
      const staleAnchors = (stroke.points || []).some((point) => {
        const anchor = normalizeResponsiveAnchor(point?.anchor);
        return !anchor || Number.isFinite(anchor.line) || Math.abs(Number(anchor.offsetY) || 0) > 0.25;
      });
      if (!moved && !staleLayout && !staleAnchors) {
        continue;
      }
      stroke.points = nextPoints.map((point) => ({
        ...point,
        ...createResponsivePoint({
          canvasX: Number(point.x) * canvasWidth,
          canvasY: Number(point.y) * canvasHeight,
          canvasWidth,
          canvasHeight,
          frame: contentFrame,
          sourcePath: noteFlow.path || this.file?.path || "",
          linePosition: null,
          lineConfidence: null,
          lineOffsetY: 0,
          time: point?.t
        })
      }));
      stroke.layout = createElementLayout({
        id: layout?.id || strokeElementId(stroke),
        bounds: {
          minX: targetBox.x,
          minY: targetBox.y,
          maxX: targetBox.x + targetBox.width,
          maxY: targetBox.y + targetBox.height
        },
        canvasWidth,
        canvasHeight,
        frame: contentFrame,
        viewportHeight: this.previewEl?.clientHeight || canvasHeight,
        sourcePath: noteFlow.path || this.file?.path || "",
        metrics: layout?.metrics || {
          width: stroke.width,
          fontSize: stroke.fontSize,
          textWidth: stroke.textWidth,
          previewWidth: stroke.previewWidth,
          previewHeight: stroke.previewHeight
        },
        relations: []
      });
      const metrics = this.noteFlowOperationPending ? null : scaleElementMetrics(stroke.layout?.metrics, targetBox);
      if (metrics?.width) {
        stroke.width = metrics.width;
      }
      changed = true;
    }
    if (changed) {
      this.invalidateStaticCache();
    }
    return changed || extentsChanged;
  }
  frozenNoteFlowAnchorsReady(frozen = normalizeFrozenNoteFlowLayout(this.drawingData?.noteFlowLayout)) {
    if (!frozen.offsets.length) {
      return true;
    }
    const candidates = this.noteFlowCandidates();
    return frozen.offsets.every((record) => Boolean(this.noteFlowAnchorElement({
      path: record.path,
      line: record.line,
      side: record.side,
      blockStart: record.blockStart,
      blockEnd: record.blockEnd
    }, candidates)));
  }
  prepareFrozenNoteFlowLayout(options = {}) {
    if (this.destroyed || this.surfaceType !== "preview" || !this.drawingsLoaded || !this.previewEl?.isConnected) {
      return Promise.resolve(false);
    }
    if (this.noteFlowOperationPending || this.draggingStroke || this.resizingSelection || this.currentStroke?.noteFlow?.enabled) {
      return Promise.resolve(false);
    }
    const frozen = normalizeFrozenNoteFlowLayout(this.drawingData?.noteFlowLayout);
    annotateVisibleMarkdownElements(this.plugin.app, this.previewEl, this.file?.path || "");
    const restore = () => {
      if (this.destroyed || !this.drawingsLoaded || !this.previewEl?.isConnected) {
        return false;
      }
      const changed = this.restoreFrozenNoteFlowLayout();
      if (changed) {
        this.scheduleResize({ layout: false, measure: false });
      }
      return changed;
    };
    if (!frozen.offsets.length) {
      return Promise.resolve(restore());
    }
    const requiresCanonicalAnnotation = !this.noteFlowMarkdownAnnotationComplete || !this.frozenNoteFlowAnchorsReady(frozen);
    if (!requiresCanonicalAnnotation) {
      return Promise.resolve(restore());
    }
    if (this.frozenNoteFlowPreparation) {
      return this.frozenNoteFlowPreparation;
    }
    const filePath = this.file?.path || "";
    const generation = this.drawingLoadGeneration;
    const preparation = annotateRenderedMarkdownLines(this.plugin.app, this.previewEl, filePath, { force: true }).then(() => {
      if (this.destroyed || generation !== this.drawingLoadGeneration || this.file?.path !== filePath) {
        return false;
      }
      this.noteFlowMarkdownAnnotationComplete = true;
      const changed = restore();
      if (options.retry !== false && !this.frozenNoteFlowAnchorsReady(frozen) && this.frozenNoteFlowRetryFrameId === null) {
        this.frozenNoteFlowRetryFrameId = window.requestAnimationFrame(() => {
          this.frozenNoteFlowRetryFrameId = null;
          this.prepareFrozenNoteFlowLayout({ retry: false }).catch((error) => {
            void error;
          });
        });
      }
      return changed;
    }).finally(() => {
      if (this.frozenNoteFlowPreparation === preparation) {
        this.frozenNoteFlowPreparation = null;
      }
    });
    this.frozenNoteFlowPreparation = preparation;
    return preparation;
  }
  scheduleFrozenNoteFlowLayoutRestore() {
    if (this.destroyed || !this.drawingsLoaded || this.frozenNoteFlowRestoreFrameId !== null || this.frozenNoteFlowRestoreTimer !== null) {
      return;
    }
    if (this.surfaceType === "preview" && !this.active && !this.initialReadingLayoutSettled) {
      return;
    }
    if (this.noteFlowOperationPending || this.draggingStroke || this.resizingSelection || this.currentStroke?.noteFlow?.enabled) {
      return;
    }
    const run = () => {
      if (this.frozenNoteFlowRestoreFrameId !== null) {
        window.cancelAnimationFrame(this.frozenNoteFlowRestoreFrameId);
        this.frozenNoteFlowRestoreFrameId = null;
      }
      if (this.frozenNoteFlowRestoreTimer !== null) {
        window.clearTimeout(this.frozenNoteFlowRestoreTimer);
        this.frozenNoteFlowRestoreTimer = null;
      }
      if (this.destroyed || this.noteFlowOperationPending || this.draggingStroke || this.resizingSelection) {
        return;
      }
      this.prepareFrozenNoteFlowLayout().catch((error) => {
        void error;
      });
    };
    this.frozenNoteFlowRestoreFrameId = window.requestAnimationFrame(run);
    this.frozenNoteFlowRestoreTimer = window.setTimeout(run, 120);
  }
  scheduleFrozenNoteFlowLayoutRestoreAfterMeasurement() {
    if (this.destroyed || this.frozenNoteFlowPostMeasurePending) {
      return;
    }
    const schedule = () => {
      this.frozenNoteFlowPostMeasurePending = false;
      if (this.destroyed || !this.previewEl?.isConnected) {
        return;
      }
      this.scheduleFrozenNoteFlowLayoutRestore();
    };
    const preparation = this.frozenNoteFlowPreparation;
    if (preparation) {
      this.frozenNoteFlowPostMeasurePending = true;
      preparation.then(schedule, schedule);
      return;
    }
    schedule();
  }
  cancelFrozenNoteFlowLayoutRestore() {
    if (this.frozenNoteFlowRestoreFrameId !== null) {
      window.cancelAnimationFrame(this.frozenNoteFlowRestoreFrameId);
      this.frozenNoteFlowRestoreFrameId = null;
    }
    if (this.frozenNoteFlowRestoreTimer !== null) {
      window.clearTimeout(this.frozenNoteFlowRestoreTimer);
      this.frozenNoteFlowRestoreTimer = null;
    }
    if (this.frozenNoteFlowRetryFrameId !== null) {
      window.cancelAnimationFrame(this.frozenNoteFlowRetryFrameId);
      this.frozenNoteFlowRetryFrameId = null;
    }
  }
  noteFlowStyleProperty(element, property) {
    if (element?.classList?.contains("notedraw-note-flow-block-spacer")) {
      return "height";
    }
    if (element?._noteDrawExternalFlowSpacing) {
      if (property === "padding-top") {
        return "margin-top";
      }
      if (property === "padding-bottom") {
        return "margin-bottom";
      }
    }
    return property;
  }
  removeNoteFlowBlockSpacer(element) {
    const directSpacer = element?.classList?.contains("notedraw-note-flow-block-spacer") ? element : null;
    const owner = directSpacer?._noteDrawMeasureElement || (element?._noteDrawExternalFlowSpacing ? element : null);
    if (!directSpacer && !owner) {
      return;
    }
    for (const [key, spacer] of this.noteFlowBlockSpacers || []) {
      if (spacer === directSpacer || spacer?._noteDrawMeasureElement === owner) {
        this.noteFlowBlockSpacers.delete(key);
        spacer._noteDrawMeasureElement = null;
        spacer.remove?.();
      }
    }
    if (owner) {
      owner._noteDrawExternalFlowSpacing = false;
    }
  }
  pruneDisconnectedNoteFlowLayout() {
    for (const [element, states] of Array.from(this.noteFlowStyledElements || [])) {
      if (!element?.isConnected) {
        for (const state of states.values()) {
          if (state.value) {
            element.style?.setProperty?.(state.styleProperty || state.property, state.value, state.priority || "");
          } else {
            element.style?.removeProperty?.(state.styleProperty || state.property);
          }
        }
        element.classList?.remove?.("notedraw-note-flow-anchor");
        this.noteFlowStyledElements.delete(element);
      }
    }
    for (const map of [this.noteFlowLineSpacers, this.noteFlowBlockSpacers]) {
      for (const [key, element] of Array.from(map || [])) {
        if (!element?.isConnected) {
          map.delete(key);
        }
      }
    }
  }
  updateFrozenNoteFlowLayout(offsets) {
    if (!this.drawingData) {
      return false;
    }
    const previous = frozenNoteFlowLayoutSignature(this.drawingData.noteFlowLayout);
    const next = normalizeFrozenNoteFlowLayout({ version: 1, offsets });
    const signature = frozenNoteFlowLayoutSignature(next);
    if (signature === previous) {
      return false;
    }
    this.drawingData.noteFlowLayout = next;
    return true;
  }
  restoreFrozenNoteFlowLayout() {
    if (this.surfaceType !== "preview" || !this.supportsNoteFlow() || !this.previewEl?.isConnected) {
      return false;
    }
    this.pruneDisconnectedNoteFlowLayout();
    const frozen = normalizeFrozenNoteFlowLayout(this.drawingData?.noteFlowLayout);
    if (!this.drawingsVisible) {
      const changed2 = Boolean(
        this.noteFlowStyledElements?.size || this.noteFlowLineSpacers?.size || this.noteFlowBlockSpacers?.size
      );
      if (changed2) {
        this.clearNoteFlowLayout();
      }
      this.noteFlowLayoutSignature = "";
      return changed2;
    }
    if (!frozen.offsets.length) {
      const cleared = Boolean(
        this.noteFlowStyledElements?.size || this.noteFlowLineSpacers?.size || this.noteFlowBlockSpacers?.size
      );
      if (cleared) {
        this.clearNoteFlowLayout();
      }
      const aligned2 = this.alignNoteFlowStrokesToReservedRows();
      this.noteFlowLayoutSignature = "frozen:empty";
      return cleared || aligned2;
    }
    const candidates = this.noteFlowCandidates();
    const settledRowExtentsChanged = this.alignNoteFlowStrokesToReservedRows(candidates, { measureOnly: true });
    const wanted = /* @__PURE__ */ new Map();
    const wantedOwners = /* @__PURE__ */ new Map();
    let missingAnchor = false;
    for (const record of frozen.offsets) {
      const ownerStrokeIndex = this.findNoteFlowOwnerStrokeIndex(record);
      const ownerStroke = this.drawingData?.strokes?.[ownerStrokeIndex];
      const storedOwnerNoteFlow = normalizeNoteFlow(ownerStroke?.noteFlow);
      const canonical = this.noteFlowCanonicalGapPlacement(
        storedOwnerNoteFlow || record,
        candidates,
        this.noteFlowStrokeClientTop(ownerStroke)
      );
      const ownerNoteFlow = canonical?.noteFlow || storedOwnerNoteFlow;
      const anchor = canonical?.anchor || this.noteFlowAnchorElement({
        path: record.path,
        line: record.line,
        side: record.side,
        blockStart: ownerNoteFlow?.blockStart ?? record.blockStart,
        blockEnd: ownerNoteFlow?.blockEnd ?? record.blockEnd
      }, candidates, this.noteFlowStrokeClientTop(ownerStroke));
      const side = ownerNoteFlow?.side || record.side;
      const property = side === "after" ? "padding-bottom" : "padding-top";
      const element = this.noteFlowTargetElement(anchor, side, ownerNoteFlow?.placementMode || "row");
      if (!element) {
        missingAnchor = true;
        continue;
      }
      let offsets = wanted.get(element);
      if (!offsets) {
        offsets = /* @__PURE__ */ new Map();
        wanted.set(element, offsets);
      }
      const rowKey = noteFlowPlacementRowKey(ownerNoteFlow || record, record.path);
      const ownerGap = Math.max(0, Number(ownerNoteFlow?.gap) || 0);
      const settledExtent = this.noteFlowSettledRowExtents.get(rowKey) || 0;
      const settledOffset = ownerNoteFlow?.placementMode === "inline" ? settledExtent > 0 ? settledExtent + ownerGap : 0 : noteFlowRowReservation({
        rowOffset: 0,
        boxHeight: settledExtent,
        gap: ownerGap
      });
      const hasSettledMeasurement = this.noteFlowSettledRowExtents.has(rowKey);
      const effectiveOffset = hasSettledMeasurement ? settledOffset : record.offset;
      const previousOffset = offsets.get(property) || 0;
      if (effectiveOffset >= previousOffset) {
        offsets.set(property, effectiveOffset);
        let owners = wantedOwners.get(element);
        if (!owners) {
          owners = /* @__PURE__ */ new Map();
          wantedOwners.set(element, owners);
        }
        owners.set(property, {
          ownerStrokeIndex,
          ownerRecord: {
            ...record,
            path: ownerNoteFlow?.path || record.path,
            line: ownerNoteFlow?.line ?? record.line,
            blockStart: ownerNoteFlow?.blockStart ?? record.blockStart,
            blockEnd: ownerNoteFlow?.blockEnd ?? record.blockEnd,
            blockKey: ownerNoteFlow?.blockKey || record.blockKey,
            side,
            property
          }
        });
      }
    }
    let changed = false;
    for (const [element, states] of Array.from(this.noteFlowStyledElements.entries())) {
      const elementOffsets = wanted.get(element);
      for (const [property, state] of Array.from(states.entries())) {
        if (elementOffsets?.has(property) || missingAnchor && element?.isConnected) {
          continue;
        }
        if (state.value) {
          element.style.setProperty(state.styleProperty || property, state.value, state.priority || "");
        } else {
          element.style.removeProperty(state.styleProperty || property);
        }
        states.delete(property);
        changed = true;
      }
      if (!states.size) {
        element.classList.remove("notedraw-note-flow-anchor");
        this.noteFlowStyledElements.delete(element);
        this.removeNoteFlowBlockSpacer(element);
      }
    }
    for (const [element, offsets] of wanted) {
      let states = this.noteFlowStyledElements.get(element);
      if (!states) {
        states = /* @__PURE__ */ new Map();
        this.noteFlowStyledElements.set(element, states);
      }
      for (const [property, offset] of offsets) {
        if (!states.has(property)) {
          const styleProperty = this.noteFlowStyleProperty(element, property);
          const value = element.style.getPropertyValue(styleProperty);
          const priority = element.style.getPropertyPriority(styleProperty);
          states.set(property, {
            property,
            styleProperty,
            value,
            priority,
            base: Number.parseFloat(window.getComputedStyle(element).getPropertyValue(styleProperty)) || 0,
            applied: 0,
            ownerStrokeIndex: -1,
            ownerRecord: null
          });
        }
        const state = states.get(property);
        const appliedValue = Math.ceil(state.base + offset);
        const nextValue = `${appliedValue}px`;
        if (element.style.getPropertyValue(state.styleProperty) !== nextValue || element.style.getPropertyPriority(state.styleProperty) !== "important") {
          this.markNoteFlowLayoutMutation();
          element.style.setProperty(state.styleProperty, nextValue, "important");
          changed = true;
        }
        state.applied = Math.max(0, appliedValue - state.base);
        const owner = wantedOwners.get(element)?.get(property);
        state.ownerStrokeIndex = owner?.ownerStrokeIndex ?? -1;
        state.ownerRecord = owner?.ownerRecord || null;
      }
      element.classList.add("notedraw-note-flow-anchor");
    }
    const aligned = this.alignNoteFlowStrokesToReservedRows();
    this.noteFlowLayoutSignature = `frozen:${frozenNoteFlowLayoutSignature(frozen)}`;
    return changed || aligned || settledRowExtentsChanged;
  }
  clearNoteFlowLayout() {
    if (this.noteFlowStyledElements?.size) {
      this.markNoteFlowLayoutMutation();
    }
    for (const [element, states] of this.noteFlowStyledElements || []) {
      if (!element?.style) {
        continue;
      }
      for (const state of states.values()) {
        if (state.value) {
          this.markNoteFlowLayoutMutation();
          element.style.setProperty(state.styleProperty || state.property, state.value, state.priority || "");
        } else {
          this.markNoteFlowLayoutMutation();
          element.style.removeProperty(state.styleProperty || state.property);
        }
      }
      element.classList.remove("notedraw-note-flow-anchor");
    }
    this.noteFlowStyledElements?.clear();
    for (const spacer of this.noteFlowLineSpacers?.values?.() || []) {
      spacer?.remove?.();
    }
    this.noteFlowLineSpacers?.clear();
    this.noteFlowSettledRowExtents?.clear();
    for (const spacer of this.noteFlowBlockSpacers?.values?.() || []) {
      if (spacer) {
        if (spacer._noteDrawMeasureElement) {
          spacer._noteDrawMeasureElement._noteDrawExternalFlowSpacing = false;
        }
        spacer._noteDrawMeasureElement = null;
      }
      spacer?.remove?.();
    }
    this.noteFlowBlockSpacers?.clear();
  }
  repairRunawayNoteFlowSurface(referenceHeight) {
    const viewportHeight = Math.max(1, this.previewEl?.clientHeight || this.canvasRenderHeight || 1);
    const { stableHeight, runawayThreshold } = noteFlowSurfaceRepairLimits(referenceHeight, viewportHeight);
    let repaired = false;
    for (const element of this.previewEl.querySelectorAll?.(".markdown-preview-sizer, .markdown-preview-section") || []) {
      const minHeight = Number.parseFloat(element.style?.minHeight) || 0;
      if (minHeight > runawayThreshold) {
        element.style.removeProperty("min-height");
        repaired = true;
      }
    }
    for (const element of this.previewEl.querySelectorAll?.(".notedraw-note-flow-anchor") || []) {
      const properties = ["padding-top", "padding-bottom", "margin-top", "margin-bottom"];
      if (element.classList?.contains("notedraw-note-flow-block-spacer")) {
        properties.push("height");
      }
      for (const property of properties) {
        if ((Number.parseFloat(element.style.getPropertyValue(property)) || 0) > runawayThreshold) {
          element.style.removeProperty(property);
          repaired = true;
        }
      }
    }
    if (this.canvasCssHeight > runawayThreshold) {
      this.canvasCssHeight = stableHeight;
      this.canvasWindowTop = clamp10(this.canvasWindowTop, 0, Math.max(0, stableHeight - Math.min(stableHeight, this.canvasRenderHeight)));
      repaired = true;
    }
    for (const element of [this.underlayEmbedLayer, this.embedLayer, this.readingZoomExtent]) {
      if ((Number.parseFloat(element?.style?.height) || 0) > runawayThreshold) {
        element.style.setProperty("height", `${Math.ceil(stableHeight * (element === this.readingZoomExtent ? this.readingZoomScale() : 1))}px`);
        repaired = true;
      }
    }
    if (repaired) {
      this.responsivePointsInitialized = false;
      this.responsiveLayoutContext = null;
      this.responsiveLayoutSignature = "";
    }
    return repaired;
  }
  applyNoteFlowLayout() {
    this.noteFlowLayoutIncomplete = false;
    const editingNoteFlow = this.active && (this.noteFlowOperationPending || this.draggingStroke || this.resizingSelection || this.currentStroke?.noteFlow?.enabled) || this.noteFlowReadingRepairPending;
    if (!editingNoteFlow || this.isReadingZoomInteractionActive()) {
      return false;
    }
    this.pruneDisconnectedNoteFlowLayout();
    if (!this.supportsNoteFlow() || !this.drawingsVisible || !this.canvas?.isConnected || !isElementVisibleEnough(this.previewEl)) {
      this.noteFlowLayoutIncomplete = this.hasNoteFlowElements();
      return false;
    }
    const contentFrame = this.getResponsiveContentFrame();
    const viewportHeight = Math.max(1, this.previewEl?.clientHeight || this.canvasRenderHeight || 1);
    const flows = (this.drawingData?.strokes || []).map((stroke, index) => ({ stroke, index })).filter(({ stroke }) => stroke?.noteFlow?.enabled && !isConnectorStroke(stroke)).map(({ stroke, index }) => {
      const bounds = getStrokeBounds(stroke, this.canvasWidth(), this.canvasHeight());
      if (!bounds) {
        return null;
      }
      const stabilized = stabilizeNoteFlowBounds({
        bounds,
        layout: normalizeElementLayout(stroke.layout),
        contentWidth: contentFrame.width,
        viewportHeight,
        preferCurrent: Boolean(normalizeNoteFlow(stroke.noteFlow)?.positionBasis) || this.resizingSelection
      });
      return { stroke, index, ...stabilized };
    }).filter(Boolean).sort((a, b) => a.bounds.minY - b.bounds.minY);
    const canLayoutDuringResize = this.resizingSelection && flows.some((item) => {
      const noteFlow = normalizeNoteFlow(item.stroke?.noteFlow);
      return hasStableNoteFlowAnchor(noteFlow) || Number.isFinite(Number(noteFlow?.line));
    });
    if (this.noteFlowOperationPending && flows.length && !this.noteFlowMarkdownAnnotationComplete && !canLayoutDuringResize) {
      this.noteFlowLayoutIncomplete = true;
      this.scheduleMarkdownAnnotationRefresh({ layout: false, delay: 0, force: true });
      return false;
    }
    if (!flows.length) {
      this.noteFlowLayoutIncomplete = false;
      const frozenLayoutChanged2 = this.updateFrozenNoteFlowLayout([]);
      const changed2 = Boolean(this.noteFlowLayoutSignature) || frozenLayoutChanged2;
      this.clearNoteFlowLayout();
      this.noteFlowLayoutSignature = "";
      if (frozenLayoutChanged2 && this.noteFlowPersistencePending && this.file && this.drawingData) {
        this.plugin.scheduleDrawingSave(this.file, this.drawingData, { userOperation: this.noteFlowPersistencePending });
      }
      return changed2;
    }
    const runawayReferenceHeight = flows.filter((item) => item.runaway).reduce((height, item) => Math.max(height, item.referenceHeight), 0);
    if (runawayReferenceHeight > 0) {
      this.repairRunawayNoteFlowSurface(runawayReferenceHeight);
    }
    const canvasRect = this.noteFlowCanvasRect();
    if (!canvasRect || canvasRect.width <= 1 || canvasRect.height <= 1) {
      this.noteFlowLayoutIncomplete = true;
      return false;
    }
    const scaleY = canvasRect.height > 0 ? canvasRect.height / Math.max(1, this.canvasRenderHeight) : 1;
    const candidates = this.noteFlowCandidates();
    if (this.resizingSelection) {
      this.noteFlowSettledRowExtents = /* @__PURE__ */ new Map();
    }
    const settledRowExtentsChanged = this.alignNoteFlowStrokesToReservedRows(candidates, { measureOnly: true });
    let missingStableAnchor = candidates.length === 0;
    const canRepairStoredAnchors = this.noteFlowAnchorRepairReady && !this.noteFlowAnchorRepairComplete && Date.now() - this.lastScrollAt > 480;
    const offsets = /* @__PURE__ */ new Map();
    const offsetDescriptors = /* @__PURE__ */ new Map();
    let migratedAnchor = false;
    let updatedNoteFlowMetadata = false;
    for (const item of flows) {
      let currentNoteFlow = normalizeNoteFlow(item.stroke.noteFlow);
      const hasStoredAnchor = hasStableNoteFlowAnchor(currentNoteFlow);
      if (!hasStoredAnchor) {
        const recaptured = this.captureNoteFlowAnchor(item.stroke, { preservePlacement: true });
        const resolved = Number.isFinite(recaptured?.line) && ["before", "after"].includes(recaptured?.side) && Boolean(recaptured?.positionBasis);
        if (resolved) {
          item.stroke.noteFlow = recaptured;
          currentNoteFlow = recaptured;
          this.captureNoteFlowResponsiveAnchors(item.stroke, this.getResponsiveLayoutContext(true));
          migratedAnchor = true;
        } else {
          item.stroke.noteFlow = currentNoteFlow;
        }
      }
      const exactPlacement = hasExactNoteFlowPlacement(currentNoteFlow);
      const strokeTop = canvasRect.top + (item.bounds.minY - this.canvasWindowTop) * scaleY;
      const strokeBottom = canvasRect.top + (item.bounds.maxY - this.canvasWindowTop) * scaleY;
      let anchor = this.noteFlowAnchorElement(currentNoteFlow, candidates, strokeTop);
      const previewRect = this.previewEl.getBoundingClientRect();
      const strokeNearViewport = strokeTop >= previewRect.top - 64 && strokeTop <= previewRect.bottom + 64;
      if (!hasStoredAnchor && anchor && currentNoteFlow?.side === "before" && anchor.top < strokeTop - 4) {
        const recaptured = this.captureNoteFlowAnchor(item.stroke);
        const correctedAnchor = this.noteFlowAnchorElement(recaptured, candidates, strokeTop);
        if (correctedAnchor) {
          item.stroke.noteFlow = recaptured;
          currentNoteFlow = recaptured;
          anchor = correctedAnchor;
          this.captureNoteFlowResponsiveAnchors(item.stroke, this.getResponsiveLayoutContext(true));
          migratedAnchor = true;
        }
      }
      const samePathCandidates = candidates.filter((candidate) => candidate.path === normalizeVaultPath(currentNoteFlow?.path || this.file?.path || ""));
      const storedLine = Number(currentNoteFlow?.line);
      const renderedStart = Math.min(...samePathCandidates.map((candidate) => candidate.start));
      const renderedEnd = Math.max(...samePathCandidates.map((candidate) => candidate.end));
      const staleStoredAnchor = canRepairStoredAnchors && hasStoredAnchor && samePathCandidates.length > 1 && Number.isFinite(storedLine) && (storedLine < renderedStart || storedLine > renderedEnd) && strokeNearViewport;
      if (!anchor && !exactPlacement && (!hasStoredAnchor || staleStoredAnchor)) {
        const recaptured = this.captureNoteFlowAnchor(item.stroke);
        anchor = this.noteFlowAnchorElement(recaptured, candidates, strokeTop);
        if (anchor && recaptured?.positionBasis) {
          item.stroke.noteFlow = recaptured;
          currentNoteFlow = recaptured;
          this.captureNoteFlowResponsiveAnchors(item.stroke, this.getResponsiveLayoutContext(true));
          migratedAnchor = true;
        }
      }
      if (exactPlacement) {
        const canonical = this.noteFlowCanonicalGapPlacement(currentNoteFlow, candidates, strokeTop);
        if (canonical) {
          const changedGap = canonical.noteFlow.path !== currentNoteFlow.path || canonical.noteFlow.line !== currentNoteFlow.line || canonical.noteFlow.side !== currentNoteFlow.side || canonical.noteFlow.blockStart !== currentNoteFlow.blockStart || canonical.noteFlow.blockEnd !== currentNoteFlow.blockEnd;
          currentNoteFlow = canonical.noteFlow;
          anchor = canonical.anchor;
          if (changedGap && this.noteFlowPersistencePending) {
            item.stroke.noteFlow = { ...item.stroke.noteFlow, ...currentNoteFlow };
            updatedNoteFlowMetadata = true;
          }
        }
      }
      const avoidanceKey = strokeElementId(item.stroke) || String(item.index);
      if (exactPlacement) {
        this.noteFlowAvoidanceAnchors.delete(avoidanceKey);
        if (currentNoteFlow.avoidancePath || Number.isFinite(currentNoteFlow.avoidanceLine)) {
          item.stroke.noteFlow = {
            ...currentNoteFlow,
            avoidancePath: "",
            avoidanceLine: null
          };
          currentNoteFlow = item.stroke.noteFlow;
          updatedNoteFlowMetadata = true;
        }
      }
      let avoidanceReference = exactPlacement ? null : noteFlowAvoidanceReference(currentNoteFlow, this.file?.path) || this.noteFlowAvoidanceAnchors.get(avoidanceKey) || null;
      let avoidanceAnchor = avoidanceReference ? this.noteFlowAnchorElement({
        path: avoidanceReference.path,
        line: avoidanceReference.line,
        side: "before"
      }, candidates, strokeTop) : null;
      if (avoidanceAnchor && !selectNoteFlowAvoidanceCandidate([avoidanceAnchor], { strokeTop, strokeBottom })) {
        avoidanceReference = null;
        avoidanceAnchor = null;
        this.noteFlowAvoidanceAnchors.delete(avoidanceKey);
        item.stroke.noteFlow = {
          ...currentNoteFlow,
          avoidancePath: "",
          avoidanceLine: null
        };
        currentNoteFlow = item.stroke.noteFlow;
        updatedNoteFlowMetadata = true;
      }
      if (!exactPlacement && !avoidanceReference) {
        avoidanceAnchor = selectNoteFlowAvoidanceCandidate(candidates, { strokeTop, strokeBottom });
        if (avoidanceAnchor) {
          avoidanceReference = { path: avoidanceAnchor.path, line: avoidanceAnchor.start };
          this.noteFlowAvoidanceAnchors.set(avoidanceKey, avoidanceReference);
          item.stroke.noteFlow = {
            ...currentNoteFlow,
            avoidancePath: avoidanceReference.path,
            avoidanceLine: avoidanceReference.line
          };
          currentNoteFlow = item.stroke.noteFlow;
          updatedNoteFlowMetadata = true;
        }
      }
      if (avoidanceReference && !avoidanceAnchor) {
        missingStableAnchor = true;
        continue;
      }
      const selectedAnchor = exactPlacement ? anchor : avoidanceAnchor || anchor;
      const side = exactPlacement ? currentNoteFlow.side : avoidanceAnchor ? "before" : currentNoteFlow.side;
      const element = this.noteFlowTargetElement(selectedAnchor, side, currentNoteFlow?.placementMode || "row");
      if (!element) {
        missingStableAnchor = true;
        continue;
      }
      const property = side === "after" ? "padding-bottom" : "padding-top";
      let states = this.noteFlowStyledElements.get(element);
      if (!states) {
        states = /* @__PURE__ */ new Map();
        this.noteFlowStyledElements.set(element, states);
      }
      if (!states.has(property)) {
        const styleProperty = this.noteFlowStyleProperty(element, property);
        const value = element.style.getPropertyValue(styleProperty);
        const priority = element.style.getPropertyPriority(styleProperty);
        states.set(property, {
          property,
          styleProperty,
          value,
          priority,
          base: Number.parseFloat(window.getComputedStyle(element).getPropertyValue(styleProperty)) || 0,
          applied: 0,
          ownerStrokeIndex: -1,
          ownerRecord: null
        });
      }
      const liveBounds = this.resizingSelection ? getStrokeBounds(item.stroke, this.canvasWidth(), this.canvasHeight()) : null;
      const liveHeight = Math.max(
        0,
        (liveBounds || item.bounds).maxY - (liveBounds || item.bounds).minY
      );
      const stableHeight = this.resizingSelection ? liveHeight : currentNoteFlow.boxHeightRatio > 0 ? currentNoteFlow.boxHeightRatio * Math.max(1, contentFrame.width) : liveHeight;
      const settledRowKey = noteFlowPlacementRowKey(currentNoteFlow, this.file?.path || "");
      const measuredExtent = this.noteFlowSettledRowExtents.get(settledRowKey);
      const settledExtent = Number.isFinite(measuredExtent) ? measuredExtent : Math.max(0, Number(currentNoteFlow.rowOffset) || 0) + stableHeight;
      const required = currentNoteFlow.placementMode === "inline" ? settledExtent > 0 ? settledExtent + currentNoteFlow.gap : 0 : noteFlowRowReservation({
        rowOffset: 0,
        boxHeight: settledExtent,
        gap: currentNoteFlow.gap
      });
      if (!(required > 0)) {
        continue;
      }
      let elementOffsets = offsets.get(element);
      if (!elementOffsets) {
        elementOffsets = /* @__PURE__ */ new Map();
        offsets.set(element, elementOffsets);
      }
      const previousOffset = elementOffsets.get(property) || 0;
      if (required >= previousOffset) {
        elementOffsets.set(property, required);
        let descriptors = offsetDescriptors.get(element);
        if (!descriptors) {
          descriptors = /* @__PURE__ */ new Map();
          offsetDescriptors.set(element, descriptors);
        }
        descriptors.set(property, {
          path: normalizeVaultPath(selectedAnchor?.path || currentNoteFlow?.path || this.file?.path || ""),
          line: exactPlacement ? currentNoteFlow.line : side === "after" ? selectedAnchor?.end : selectedAnchor?.start,
          blockStart: selectedAnchor?.blockStart ?? selectedAnchor?.start,
          blockEnd: selectedAnchor?.blockEnd ?? selectedAnchor?.end,
          side,
          property,
          ownerId: strokeElementId(item.stroke),
          ownerStrokeIndex: item.index
        });
      }
    }
    let layoutStyleChanged = false;
    for (const [element, states] of Array.from(this.noteFlowStyledElements.entries())) {
      const elementOffsets = offsets.get(element);
      for (const [property, state] of Array.from(states.entries())) {
        if (elementOffsets?.has(property)) {
          continue;
        }
        if (missingStableAnchor && this.noteFlowLayoutSignature) {
          continue;
        }
        if (state.value) {
          element.style.setProperty(state.styleProperty || state.property, state.value, state.priority || "");
        } else {
          element.style.removeProperty(state.styleProperty || state.property);
        }
        states.delete(property);
        layoutStyleChanged = true;
      }
      if (!states.size) {
        element.classList.remove("notedraw-note-flow-anchor");
        this.noteFlowStyledElements.delete(element);
        this.removeNoteFlowBlockSpacer(element);
      }
    }
    for (const [element, elementOffsets] of offsets) {
      const states = this.noteFlowStyledElements.get(element);
      for (const [property, offset] of elementOffsets) {
        const state = states.get(property);
        const appliedValue = Math.ceil(state.base + offset);
        const nextValue = `${appliedValue}px`;
        if (element.style.getPropertyValue(state.styleProperty) !== nextValue || element.style.getPropertyPriority(state.styleProperty) !== "important") {
          this.markNoteFlowLayoutMutation();
          element.style.setProperty(state.styleProperty, nextValue, "important");
          layoutStyleChanged = true;
        }
        state.applied = Math.max(0, appliedValue - state.base);
        const descriptor = offsetDescriptors.get(element)?.get(property);
        state.ownerStrokeIndex = descriptor?.ownerStrokeIndex ?? -1;
        state.ownerRecord = descriptor || null;
      }
      element.classList.add("notedraw-note-flow-anchor");
    }
    const signature = Array.from(offsets, ([element, elementOffsets]) => {
      return Array.from(elementOffsets, ([property, offset]) => `${property}:${Math.round(offset)}`).join(",");
    }).join("|");
    const frozenOffsets = [];
    for (const [element, elementOffsets] of offsets) {
      const descriptors = offsetDescriptors.get(element);
      for (const [property, offset] of elementOffsets) {
        const descriptor = descriptors?.get(property);
        if (descriptor && Number.isFinite(Number(descriptor.line))) {
          frozenOffsets.push({ ...descriptor, line: Number(descriptor.line), offset });
        }
      }
    }
    const aligned = this.alignNoteFlowStrokesToReservedRows();
    const frozenLayoutChanged = !missingStableAnchor && this.updateFrozenNoteFlowLayout(frozenOffsets);
    this.captureReadingLogicalSizerHeight(void 0, { allowGrowth: true });
    this.noteFlowLayoutIncomplete = missingStableAnchor;
    if (canRepairStoredAnchors) {
      this.noteFlowAnchorRepairComplete = true;
      if (missingStableAnchor) {
        this.noteFlowAnchorRepairComplete = false;
      }
    }
    if (this.noteFlowPersistencePending && (aligned || migratedAnchor || updatedNoteFlowMetadata || frozenLayoutChanged) && this.file && this.drawingData) {
      this.plugin.scheduleDrawingSave(this.file, this.drawingData, { userOperation: this.noteFlowPersistencePending });
    }
    const changed = settledRowExtentsChanged || aligned || migratedAnchor || updatedNoteFlowMetadata || frozenLayoutChanged || layoutStyleChanged || signature !== this.noteFlowLayoutSignature;
    this.noteFlowLayoutSignature = signature;
    return changed;
  }
  markNoteFlowLayoutMutation() {
    this.noteFlowCandidateCache = null;
    this.noteFlowSuppressResizeUntil = Math.max(this.noteFlowSuppressResizeUntil, Date.now() + 180);
    if (!this.destroyed && this.resizeFrameId !== null && this.resizeNeedsLayout) {
      this.cancelResizeFrame();
      this.scheduleResize({ layout: false, measure: false });
    }
  }
  noteFlowSettleEpochSignature() {
    const frame = this.getResponsiveContentFrame();
    return [
      this.file?.path || "",
      this.surfaceType,
      Math.round(Number(this.previewEl?.clientWidth) || 0),
      Math.round(Number(frame?.left) || 0),
      Math.round(Number(frame?.width) || 0),
      Math.round(this.readingZoomScale() * 1e3),
      this.layoutRefreshGeneration
    ].join(":");
  }
  resetNoteFlowSettle() {
    this.noteFlowSettleEpoch = "";
    this.noteFlowSettlePasses = 0;
  }
  scheduleNoteFlowAnchorRepair() {
    if (this.destroyed || !this.active || !this.noteFlowOperationPending || this.noteFlowAnchorRepairComplete || this.noteFlowAnchorRepairTimer !== null) {
      return;
    }
    this.noteFlowAnchorRepairTimer = window.setTimeout(() => {
      this.noteFlowAnchorRepairTimer = null;
      if (this.destroyed || !this.active || !this.noteFlowOperationPending || this.noteFlowAnchorRepairComplete || !this.previewEl?.isConnected) {
        return;
      }
      const layoutBusy = this.markdownAnnotationTimer !== null || Date.now() < this.noteFlowSuppressResizeUntil;
      if (layoutBusy || Date.now() - this.lastScrollAt <= 700) {
        this.scheduleNoteFlowAnchorRepair();
        return;
      }
      this.noteFlowAnchorRepairReady = true;
      this.scheduleMarkdownAnnotationRefresh({ layout: false, delay: 0 });
      this.scheduleNoteFlowLayout();
    }, 700);
  }
  scheduleNoteFlowSettleResize() {
    if (this.destroyed || this.noteFlowResizeTimer !== null) {
      return;
    }
    const epoch = this.noteFlowSettleEpochSignature();
    if (epoch !== this.noteFlowSettleEpoch) {
      this.noteFlowSettleEpoch = epoch;
      this.noteFlowSettlePasses = 0;
    }
    if (this.noteFlowSettlePasses >= 2) {
      return;
    }
    const delay = Math.max(34, this.noteFlowSuppressResizeUntil - Date.now() + 20);
    this.noteFlowResizeTimer = window.setTimeout(() => {
      this.noteFlowResizeTimer = null;
      if (this.destroyed) {
        return;
      }
      if (this.draggingStroke || this.isReadingZoomInteractionActive() || Date.now() < this.noteFlowSuppressResizeUntil) {
        this.scheduleNoteFlowSettleResize();
        return;
      }
      const currentEpoch = this.noteFlowSettleEpochSignature();
      if (currentEpoch !== this.noteFlowSettleEpoch) {
        this.noteFlowSettleEpoch = currentEpoch;
        this.noteFlowSettlePasses = 0;
      }
      if (this.noteFlowSettlePasses >= 2) {
        return;
      }
      this.noteFlowSettlePasses += 1;
      this.scheduleResize({ layout: false, measure: true, preserveAbsolutePlacement: true });
    }, delay);
  }
  reflowNoteFlowElementsAfterDrag(movedIndexes, placement = null, options = {}) {
    if (!this.supportsNoteFlow() || !this.dragStrokeOriginalPoints?.size) {
      return [];
    }
    const moved = new Set((movedIndexes || []).filter((index) => {
      const stroke = this.drawingData?.strokes?.[index];
      return normalizeNoteFlow(stroke?.noteFlow) && !isConnectorStroke(stroke);
    }));
    if (!moved.size) {
      return [];
    }
    const canvasWidth = this.canvasWidth();
    const canvasHeight = this.canvasHeight();
    const canvasRect = this.noteFlowCanvasRect();
    const scaleY = canvasRect?.height > 1 ? canvasRect.height / Math.max(1, this.canvasRenderHeight) : 1;
    const preview = options.preview === true;
    const candidates = Array.isArray(options.candidates) ? options.candidates : canvasRect?.height > 1 ? this.noteFlowCandidates() : [];
    const items = [];
    for (const [index, stroke] of (this.drawingData?.strokes || []).entries()) {
      const noteFlow = normalizeNoteFlow(stroke?.noteFlow);
      if (!noteFlow || isConnectorStroke(stroke)) {
        continue;
      }
      const bounds = getStrokeBounds(stroke, canvasWidth, canvasHeight);
      if (!bounds) {
        continue;
      }
      const movedItem = moved.has(index);
      const placementMode = movedItem ? placement?.horizontalSide ? "inline" : "row" : noteFlow.placementMode;
      const rawAnchor = movedItem && placement?.candidate ? placement.candidate : this.noteFlowAnchorElement(noteFlow, candidates, this.noteFlowStrokeClientTop(stroke));
      const rawSide = movedItem ? placement?.side : noteFlow.side;
      const canonical = placementMode === "row" ? canonicalNoteFlowGapPlacement(candidates, { anchor: rawAnchor, side: rawSide }) : rawAnchor ? {
        anchor: rawAnchor,
        path: normalizeVaultPath(movedItem ? placement?.path : noteFlow.path || this.file?.path || ""),
        line: Number(movedItem ? placement?.line : noteFlow.line),
        side: rawSide,
        blockStart: movedItem ? placement?.candidate?.blockStart ?? placement?.candidate?.start : noteFlow.blockStart,
        blockEnd: movedItem ? placement?.candidate?.blockEnd ?? placement?.candidate?.end : noteFlow.blockEnd
      } : null;
      const anchor = canonical?.anchor || rawAnchor;
      const path = normalizeVaultPath(canonical?.path || (movedItem ? placement?.path : noteFlow.path) || this.file?.path || "");
      const line = Number(canonical?.line ?? (movedItem ? placement?.line : noteFlow.line));
      const side = canonical?.side || rawSide;
      const blockStart = canonical?.blockStart ?? (movedItem ? placement?.candidate?.blockStart ?? placement?.candidate?.start : noteFlow.blockStart);
      const blockEnd = canonical?.blockEnd ?? (movedItem ? placement?.candidate?.blockEnd ?? placement?.candidate?.end : noteFlow.blockEnd);
      const anchorRect = anchor ? this.noteFlowCandidateRect(anchor, placementMode) : null;
      const anchorBoundary = anchor ? placementMode === "inline" ? anchorRect?.top : side === "after" ? anchor.bottom : anchor.top : Number.NaN;
      const baseMinY = Number.isFinite(anchorBoundary) && canvasRect ? this.canvasWindowTop + (anchorBoundary - canvasRect.top) / Math.max(1e-4, scaleY) : bounds.minY;
      items.push({
        id: strokeElementId(stroke) || String(index),
        index,
        order: Number.isFinite(Number(noteFlow.flowOrder)) ? Number(noteFlow.flowOrder) : bounds.minY,
        storedOrder: Number.isFinite(Number(noteFlow.flowOrder)) ? Number(noteFlow.flowOrder) : null,
        minX: bounds.minX,
        maxX: bounds.maxX,
        minY: bounds.minY,
        maxY: bounds.maxY,
        baseMinY,
        originalMinY: bounds.minY,
        moved: movedItem,
        shrinkToFit: movedItem,
        placementMode,
        rowKey: noteFlowPlacementRowKey({ path, line, side, blockStart, blockEnd, placementMode }),
        overlapGroup: isNoteFlowInkStroke(stroke) ? `ink:${noteFlowPlacementRowKey({ path, line, side, blockStart, blockEnd, placementMode })}` : "",
        anchor,
        align: "top",
        gap: Math.min(8, noteFlow.gap)
      });
    }
    const dragRows = /* @__PURE__ */ new Map();
    for (const item of items) {
      const key = item.rowKey || `index:${item.index}`;
      const row = dragRows.get(key) || [];
      row.push(item);
      dragRows.set(key, row);
    }
    for (const rowItems2 of dragRows.values()) {
      const inlineRow = rowItems2.some((item) => item.moved && item.placementMode === "inline");
      const movedItems = rowItems2.filter((item) => item.moved).sort(inlineRow ? (a, b) => a.minX - b.minX || a.index - b.index : (a, b) => a.minY - b.minY || a.index - b.index);
      if (!movedItems.length) {
        continue;
      }
      const peers = rowItems2.filter((item) => !item.moved).sort((a, b) => (inlineRow ? a.minX - b.minX : a.minY - b.minY) || (a.storedOrder ?? a.index) - (b.storedOrder ?? b.index) || a.index - b.index);
      const insertionIndex = clamp10(Number(placement?.flowOrder) || 0, 0, peers.length);
      const orderedItems = peers.slice();
      orderedItems.splice(insertionIndex, 0, ...movedItems);
      orderedItems.forEach((item, order) => {
        item.order = order;
        item.orderAssigned = true;
      });
    }
    const changed = [];
    for (const item of items) {
      if (!item.orderAssigned) {
        continue;
      }
      const stroke = this.drawingData.strokes[item.index];
      const current = normalizeNoteFlow(stroke?.noteFlow);
      if (!stroke || !current || current.flowOrder === item.order) {
        continue;
      }
      if (preview) {
        this.rememberDraggedNoteFlowLivePreviewState(item.index);
      }
      stroke.noteFlow = { ...stroke.noteFlow, flowOrder: item.order };
      changed.push(item.index);
    }
    const rowItems = items.filter((item) => item.placementMode !== "inline");
    const inlineItems = items.filter((item) => item.placementMode === "inline");
    const inlinePacking = this.packInlineNoteFlowItems(inlineItems, candidates, {
      canvasRect,
      contentFrame: this.getResponsiveContentFrame(),
      gap: 8
    });
    const placements = [
      ...reflowNoteFlowRectangles(rowItems),
      ...inlinePacking.placements
    ];
    for (const placement2 of placements) {
      const horizontalScale = Number.isFinite(Number(placement2.scaleX)) ? Number(placement2.scaleX) : 1;
      if (Math.abs(placement2.deltaX || 0) < 0.5 && Math.abs(placement2.deltaY || 0) < 0.5 && Math.abs(horizontalScale - 1) < 1e-4) {
        continue;
      }
      const stroke = this.drawingData.strokes[placement2.index];
      if (!stroke || stroke.locked) {
        continue;
      }
      if (preview) {
        this.rememberDraggedNoteFlowLivePreviewState(placement2.index);
      }
      const deltaY = placement2.deltaY / Math.max(1, canvasHeight);
      const bounds = getStrokeBounds(stroke, canvasWidth, canvasHeight);
      stroke.points = stroke.points.map((point) => ({
        ...point,
        x: bounds ? clamp10((placement2.minX + (Number(point.x) * canvasWidth - bounds.minX) * horizontalScale) / Math.max(1, canvasWidth), 0, 1) : clamp10(Number(point.x) + (Number(placement2.deltaX) || 0) / Math.max(1, canvasWidth), 0, 1),
        y: clamp10(Number(point.y) + deltaY, 0, 1)
      }));
      changed.push(placement2.index);
    }
    const settledRows = /* @__PURE__ */ new Map();
    for (const rowPlacement of placements.filter((placement2) => items.find((item) => item.index === placement2.index)?.placementMode !== "inline")) {
      const key = rowPlacement.rowKey || `index:${rowPlacement.index}`;
      const row = settledRows.get(key) || [];
      row.push(rowPlacement);
      settledRows.set(key, row);
    }
    for (const rowPlacements of settledRows.values()) {
      rowPlacements.sort((a, b) => a.minY - b.minY || a.minX - b.minX || a.index - b.index);
      let rowOrder = -1;
      let previousTop = Number.NEGATIVE_INFINITY;
      for (const rowPlacement of rowPlacements) {
        if (Math.abs(rowPlacement.minY - previousTop) > 0.5) {
          rowOrder += 1;
          previousTop = rowPlacement.minY;
        }
        const stroke = this.drawingData.strokes[rowPlacement.index];
        const current = normalizeNoteFlow(stroke?.noteFlow);
        if (!stroke || !current || current.flowOrder === rowOrder) {
          continue;
        }
        if (preview) {
          this.rememberDraggedNoteFlowLivePreviewState(rowPlacement.index);
        }
        stroke.noteFlow = { ...stroke.noteFlow, flowOrder: rowOrder };
        if (!changed.includes(rowPlacement.index)) {
          changed.push(rowPlacement.index);
        }
      }
    }
    if (changed.length) {
      this.invalidateStaticCache();
    }
    return changed;
  }
  queueDraggedNoteFlowRefresh(indexes) {
    let queued = false;
    for (const index of indexes || []) {
      const stroke = this.drawingData?.strokes?.[index];
      if (stroke?.noteFlow?.enabled && !isConnectorStroke(stroke)) {
        this.pendingDraggedNoteFlowIndexes.add(index);
        queued = true;
      }
    }
    if (queued && !this.draggingStroke) {
      this.scheduleNoteFlowLayout();
    }
  }
  refreshDraggedNoteFlowAnchors() {
    if (!this.draggingStroke || !this.pendingDraggedNoteFlowIndexes.size) {
      this.pendingDraggedNoteFlowIndexes.clear();
      return false;
    }
    const indexes = Array.from(this.pendingDraggedNoteFlowIndexes);
    this.pendingDraggedNoteFlowIndexes.clear();
    this.clearNoteFlowLayout();
    let refreshed = false;
    for (const index of indexes) {
      const stroke = this.drawingData?.strokes?.[index];
      const current = normalizeNoteFlow(stroke?.noteFlow);
      if (!stroke || !current || isConnectorStroke(stroke)) {
        continue;
      }
      const captured = this.captureNoteFlowAnchor(stroke);
      stroke.noteFlow = {
        ...current,
        enabled: true,
        path: captured.path,
        line: captured.line,
        side: captured.side,
        blockStart: captured.blockStart,
        blockEnd: captured.blockEnd,
        blockKey: captured.blockKey,
        gap: captured.gap
      };
      refreshed = true;
    }
    return refreshed;
  }
  scheduleNoteFlowLayout(options = {}) {
    if (options.operation === true && this.active) {
      this.cancelFrozenNoteFlowLayoutRestore();
      this.noteFlowOperationPending = true;
      this.noteFlowPersistencePending = true;
    }
    const editingNoteFlow = this.active && (this.noteFlowOperationPending || this.draggingStroke || this.resizingSelection || this.currentStroke?.noteFlow?.enabled);
    if (!editingNoteFlow) {
      return;
    }
    if (options.defer === true) {
      return;
    }
    const run = () => {
      if (this.noteFlowFrameId !== null) {
        window.cancelAnimationFrame(this.noteFlowFrameId);
        this.noteFlowFrameId = null;
      }
      if (this.noteFlowFallbackTimer !== null) {
        window.clearTimeout(this.noteFlowFallbackTimer);
        this.noteFlowFallbackTimer = null;
      }
      if (this.destroyed) {
        return;
      }
      const refreshedDraggedFlow = this.refreshDraggedNoteFlowAnchors();
      const layoutChanged = this.applyNoteFlowLayout() || refreshedDraggedFlow;
      if (!this.draggingStroke && !this.resizingSelection) {
        if (this.noteFlowLayoutIncomplete) {
          this.scheduleNoteFlowAnchorRepair();
        } else {
          this.noteFlowOperationPending = false;
          this.noteFlowPersistencePending = false;
        }
      }
      const selectedNoteFlowChanged = layoutChanged && this.getSelectedStrokeIndexes().some((index) => normalizeNoteFlow(this.drawingData?.strokes?.[index]?.noteFlow));
      if (selectedNoteFlowChanged && !this.draggingStroke && !this.resizingSelection) {
        this.invalidateSelectionFrameSnapshot();
        if (!this.selectionFrameAwaitingMarkdownSync) {
          this.captureSelectionFrameSnapshot({ force: true });
        }
        this.requestRender(this.selectionHasDomStrokes() ? "interaction" : false);
      }
      if (layoutChanged && !this.draggingStroke) {
        this.scheduleNoteFlowSettleResize();
      }
    };
    if (options.immediate === true) {
      run();
      if (this.noteFlowOperationPending && !this.destroyed) {
        this.scheduleNoteFlowLayout();
      }
      return;
    }
    if (this.noteFlowFrameId !== null || this.noteFlowFallbackTimer !== null) {
      return;
    }
    this.noteFlowFrameId = window.requestAnimationFrame(run);
    this.noteFlowFallbackTimer = window.setTimeout(run, 120);
  }
  cancelNoteFlowLayout() {
    this.noteFlowOperationPending = false;
    this.noteFlowPersistencePending = false;
    this.noteFlowLayoutIncomplete = false;
    if (this.noteFlowFrameId !== null) {
      window.cancelAnimationFrame(this.noteFlowFrameId);
      this.noteFlowFrameId = null;
    }
    if (this.noteFlowFallbackTimer !== null) {
      window.clearTimeout(this.noteFlowFallbackTimer);
      this.noteFlowFallbackTimer = null;
    }
    if (this.noteFlowResizeTimer !== null) {
      window.clearTimeout(this.noteFlowResizeTimer);
      this.noteFlowResizeTimer = null;
    }
    if (this.noteFlowAnchorRepairTimer !== null) {
      window.clearTimeout(this.noteFlowAnchorRepairTimer);
      this.noteFlowAnchorRepairTimer = null;
    }
  }
  getSelectedStrokeIndexes() {
    if (this.selectedStrokeIndexes?.size) {
      return Array.from(this.selectedStrokeIndexes).filter((index) => index >= 0 && index < this.drawingData.strokes.length).sort((a, b) => a - b);
    }
    if (this.selectedStrokeIndex >= 0 && this.selectedStrokeIndex < this.drawingData.strokes.length) {
      return [this.selectedStrokeIndex];
    }
    return [];
  }
  isStrokeSelected(index) {
    return Boolean(this.selectedStrokeIndexes?.has(index)) || !this.selectedStrokeIndexes?.size && this.selectedStrokeIndex === index;
  }
  selectionHasDomStrokes() {
    return this.getSelectedMarkdownBlocks().length > 0 || this.getSelectedStrokeIndexes().some((index) => {
      const stroke = this.drawingData.strokes[index];
      return isEmbedStroke(stroke) || isRichTextStroke(stroke);
    });
  }
  getSelectedStrokeBounds() {
    const indexes = this.getSelectedStrokeIndexes();
    let result = this.getStrokeIndexesBounds(indexes);
    for (const block of this.getSelectedMarkdownBlocks()) {
      const bounds = this.markdownElementCanvasBounds(this.markdownBlockElementOrFallback(block), { forSelection: true });
      if (!bounds) {
        continue;
      }
      result = result ? {
        minX: Math.min(result.minX, bounds.minX),
        maxX: Math.max(result.maxX, bounds.maxX),
        minY: Math.min(result.minY, bounds.minY),
        maxY: Math.max(result.maxY, bounds.maxY)
      } : { ...bounds };
    }
    return result;
  }
  markdownBlockElementOrFallback(block) {
    const mapped = this.markdownBlockElement(block);
    if (mapped) {
      return mapped;
    }
    if (block?.id && this.previewEl?.isConnected) {
      const id = String(block.id).replace(/["\\]/g, "");
      const live = this.previewEl.querySelector?.(`[data-note-draw-markdown-block-id="${id}"]`);
      if (live?.isConnected) {
        return live;
      }
    }
    return null;
  }
  selectionStateKey() {
    const strokeIds = this.getSelectedStrokeIndexes().map((index) => strokeElementId(this.drawingData?.strokes?.[index]) || `index:${index}`);
    const markdownIds = this.getSelectedMarkdownBlocks().map((block) => block.id);
    return `${strokeIds.join(",")}|${markdownIds.join(",")}`;
  }
  invalidateSelectionFrameSnapshot() {
    this.selectionFrameSnapshot = null;
  }
  captureSelectionFrameSnapshot({ force = false } = {}) {
    if (this.selectionFrameAwaitingMarkdownSync && !this.draggingStroke) {
      this.selectionFrameSnapshot = null;
      return null;
    }
    if (!this.hasHybridSelection()) {
      this.selectionFrameSnapshot = null;
      return null;
    }
    const key = this.selectionStateKey();
    if (!force && this.selectionFrameSnapshot?.key === key) {
      return this.selectionFrameSnapshot.rect;
    }
    const bounds = this.getSelectedStrokeBounds();
    if (!bounds) {
      this.selectionFrameSnapshot = null;
      return null;
    }
    const padding = this.effectiveSelectionFramePaddingPx();
    const rect = {
      x: bounds.minX - padding,
      y: bounds.minY - padding,
      width: bounds.maxX - bounds.minX + padding * 2,
      height: bounds.maxY - bounds.minY + padding * 2
    };
    const inlineLimits = this.selectedMarkdownInlineFrameLimitsCanvas();
    if (Number.isFinite(inlineLimits.minX) && rect.x < inlineLimits.minX) {
      rect.width -= inlineLimits.minX - rect.x;
      rect.x = inlineLimits.minX;
    }
    if (Number.isFinite(inlineLimits.maxX) && rect.x + rect.width > inlineLimits.maxX) {
      rect.width = inlineLimits.maxX - rect.x;
    }
    rect.width = Math.max(1, rect.width);
    this.selectionFrameSnapshot = { key, rect };
    return rect;
  }
  effectiveSelectionFramePaddingPx() {
    const padding = this.selectionFramePaddingPx();
    const neighborGap = this.minSelectedNeighborGapCanvasPx();
    const shrunk = Number.isFinite(neighborGap) && neighborGap >= 0 ? Math.min(padding, Math.max(0, neighborGap / 2)) : padding;
    return Math.max(3, shrunk);
  }
  minSelectedNeighborGapCanvasPx() {
    const canvasRect = this.canvas?.getBoundingClientRect?.();
    if (!canvasRect || canvasRect.width <= 0 || canvasRect.height <= 0) {
      return Number.POSITIVE_INFINITY;
    }
    const scaleX = this.canvasWidth() / canvasRect.width;
    const scaleY = this.canvasRenderHeight / canvasRect.height;
    const pixelScale = Math.min(scaleX, scaleY);
    let minGap = Number.POSITIVE_INFINITY;
    for (const block of this.getSelectedMarkdownBlocks()) {
      const element = this.markdownBlockElement(block);
      if (!element?.isConnected || !element.parentElement) {
        continue;
      }
      const rect = element.getBoundingClientRect();
      if (rect.width <= 0 || rect.height <= 0) {
        continue;
      }
      for (const sibling of Array.from(element.parentElement.children || [])) {
        if (sibling === element || !sibling?.isConnected) {
          continue;
        }
        const siblingRect = sibling.getBoundingClientRect?.();
        if (!siblingRect || siblingRect.width <= 0 || siblingRect.height <= 0) {
          continue;
        }
        const overlapX = Math.min(rect.right, siblingRect.right) - Math.max(rect.left, siblingRect.left);
        const overlapY = Math.min(rect.bottom, siblingRect.bottom) - Math.max(rect.top, siblingRect.top);
        let gap = Number.POSITIVE_INFINITY;
        if (overlapX > 0) {
          if (overlapY > 0) {
            gap = 0;
          } else if (siblingRect.bottom <= rect.top) {
            gap = rect.top - siblingRect.bottom;
          } else {
            gap = siblingRect.top - rect.bottom;
          }
        } else if (overlapY > 0) {
          gap = siblingRect.right <= rect.left ? rect.left - siblingRect.right : siblingRect.left - rect.right;
        }
        if (Number.isFinite(gap) && gap >= 0 && gap < minGap) {
          minGap = gap;
        }
      }
    }
    return Number.isFinite(minGap) ? minGap * pixelScale : Number.POSITIVE_INFINITY;
  }
  markdownInlineSelectionClientLimits(element, elementRect = null, { includeSelectedPeers = false } = {}) {
    const currentRect = elementRect || this.markdownElementVisibleClientRect(element) || element?.getBoundingClientRect?.();
    const lane = this.markdownBlockLayoutLane(element);
    if (!lane || !currentRect || currentRect.width <= 0 || currentRect.height <= 0) {
      return { left: Number.NEGATIVE_INFINITY, right: Number.POSITIVE_INFINITY };
    }
    const currentBlock = this.findMarkdownBlockRecordForElement(element);
    const selectedIds = includeSelectedPeers ? /* @__PURE__ */ new Set() : this.selectedMarkdownBlockIds || /* @__PURE__ */ new Set();
    const currentCenter = (currentRect.left + currentRect.right) / 2;
    let left = Number.NEGATIVE_INFINITY;
    let right = Number.POSITIVE_INFINITY;
    for (const [id, peerElement] of this.markdownBlockElements.entries()) {
      if (id === currentBlock?.id || selectedIds.has(id) || !peerElement?.isConnected || this.markdownBlockLayoutLane(peerElement) !== lane) {
        continue;
      }
      const peerBlock = this.markdownBlockRecords().find((block) => block.id === id);
      if (!peerBlock || peerBlock.floating || Number(peerBlock.span || 12) >= 12) {
        continue;
      }
      const peerRect = this.markdownElementVisibleClientRect(peerElement) || peerElement.getBoundingClientRect?.();
      if (!peerRect || peerRect.width <= 0 || peerRect.height <= 0 || Math.min(currentRect.bottom, peerRect.bottom) - Math.max(currentRect.top, peerRect.top) <= 1) {
        continue;
      }
      const peerCenter = (peerRect.left + peerRect.right) / 2;
      if (peerCenter < currentCenter) {
        const midpoint = peerRect.right <= currentRect.left ? (peerRect.right + currentRect.left) / 2 : (Math.max(peerRect.left, currentRect.left) + Math.min(peerRect.right, currentRect.right)) / 2;
        const boundary = midpoint - INLINE_SELECTION_BOUNDARY_BIAS_PX;
        left = Math.max(left, boundary);
      } else if (peerCenter > currentCenter) {
        const midpoint = currentRect.right <= peerRect.left ? (currentRect.right + peerRect.left) / 2 : (Math.max(peerRect.left, currentRect.left) + Math.min(peerRect.right, currentRect.right)) / 2;
        const boundary = midpoint - INLINE_SELECTION_BOUNDARY_BIAS_PX;
        right = Math.min(right, boundary);
      }
    }
    return { left, right };
  }
  selectedMarkdownInlineFrameLimitsCanvas() {
    const canvasRect = this.canvas?.getBoundingClientRect?.();
    if (!canvasRect || canvasRect.width <= 0) {
      return { minX: Number.NEGATIVE_INFINITY, maxX: Number.POSITIVE_INFINITY };
    }
    let clientLeft = Number.NEGATIVE_INFINITY;
    let clientRight = Number.POSITIVE_INFINITY;
    for (const block of this.getSelectedMarkdownBlocks()) {
      const element = this.markdownBlockElementOrFallback(block);
      if (!element?.isConnected || block.floating || Number(block.span || 12) >= 12) {
        continue;
      }
      const rect = this.markdownElementVisibleClientRect(element) || element.getBoundingClientRect?.();
      const limits = this.markdownInlineSelectionClientLimits(element, rect);
      clientLeft = Math.max(clientLeft, limits.left);
      clientRight = Math.min(clientRight, limits.right);
    }
    const scaleX = this.canvasWidth() / canvasRect.width;
    return {
      minX: Number.isFinite(clientLeft) ? (clientLeft - canvasRect.left) * scaleX : Number.NEGATIVE_INFINITY,
      maxX: Number.isFinite(clientRight) ? (clientRight - canvasRect.left) * scaleX : Number.POSITIVE_INFINITY
    };
  }
  noteFlowAppliedVerticalInsets(element) {
    const insets = { top: 0, bottom: 0 };
    const states = this.noteFlowStyledElements?.get(element);
    for (const state of states?.values?.() || []) {
      const applied = Math.max(0, Number(state.applied) || 0);
      if (state.styleProperty === "padding-top") {
        insets.top += applied;
      } else if (state.styleProperty === "padding-bottom") {
        insets.bottom += applied;
      }
    }
    for (const spacer of Array.from(element?.children || [])) {
      if (!spacer.classList?.contains("notedraw-note-flow-block-spacer")) {
        continue;
      }
      const spacerStates = this.noteFlowStyledElements?.get(spacer);
      let applied = 0;
      for (const state of spacerStates?.values?.() || []) {
        if (state.styleProperty === "height") {
          applied = Math.max(applied, Math.max(0, Number(state.applied) || 0));
        }
      }
      if (spacer.dataset.noteDrawNoteFlowSide === "after") {
        insets.bottom += applied;
      } else {
        insets.top += applied;
      }
    }
    return insets;
  }
  markdownElementVisibleClientRect(element) {
    const rect = element?.getBoundingClientRect?.();
    if (!rect || rect.width <= 0 || rect.height <= 0) {
      return null;
    }
    const localHeight = Number(element.offsetHeight) || 0;
    const visualScale = localHeight > 0 ? rect.height / localHeight : this.readingZoomScale();
    const flowInsets = this.noteFlowAppliedVerticalInsets(element);
    return trimMarkdownClientRect(rect, {
      insetTop: flowInsets.top,
      insetBottom: flowInsets.bottom,
      scale: visualScale
    });
  }
  noteFlowEmptyOwnerClientRect(element) {
    const rect = element?.getBoundingClientRect?.();
    if (!rect || rect.width <= 1) {
      return null;
    }
    const hasMedia = element.matches?.(".el-img,.internal-embed,.markdown-embed,.markdown-embed-content,figure,video,audio,iframe") || element.querySelector?.(".el-img,.internal-embed,.markdown-embed,.markdown-embed-content,figure,img,video,audio,iframe");
    if (!hasMedia) {
      return null;
    }
    const localHeight = Number(element.offsetHeight) || 0;
    const visualScale = localHeight > 0 ? rect.height / localHeight : this.readingZoomScale();
    const flowInsets = this.noteFlowAppliedVerticalInsets(element);
    const contentTop = rect.top + flowInsets.top * visualScale;
    const contentBottom = rect.bottom - flowInsets.bottom * visualScale;
    if (contentBottom - contentTop > 1) {
      return null;
    }
    const computed = window.getComputedStyle?.(element);
    const fontSize = Number.parseFloat(computed?.fontSize) || 16;
    const lineHeight = Number.parseFloat(computed?.lineHeight) || fontSize * 1.5;
    const nextBlockTop = Array.from(element.parentElement?.children || []).map((sibling) => sibling === element ? null : sibling.getBoundingClientRect?.().top).filter((top) => Number.isFinite(top) && top > contentTop + 0.5).sort((a, b) => a - b)[0];
    const availableHeight = Number.isFinite(nextBlockTop) ? nextBlockTop - contentTop : lineHeight;
    const height = Math.max(1, Math.min(96, lineHeight, availableHeight));
    return {
      left: rect.left,
      right: rect.right,
      top: contentTop,
      bottom: contentTop + height,
      width: rect.width,
      height
    };
  }
  markdownElementContainsClientPoint(element, clientPoint = null) {
    if (!clientPoint || !Number.isFinite(Number(clientPoint.x)) || !Number.isFinite(Number(clientPoint.y))) {
      return true;
    }
    const visibleRect = this.markdownElementVisibleClientRect(element);
    if (clientPointInRect(visibleRect, clientPoint)) {
      return true;
    }
    return clientPointInRect(this.markdownTaskCheckboxRect(element, visibleRect), clientPoint);
  }
  markdownTaskCheckboxRect(element, elementRect = null) {
    const listItem = element?.closest?.("li");
    if (!listItem) {
      return null;
    }
    let hasTaskCheckbox = false;
    for (const checkbox of listItem.querySelectorAll?.("input.task-list-item-checkbox, input[type='checkbox']") || []) {
      if (checkbox.closest?.("li") !== listItem) {
        continue;
      }
      hasTaskCheckbox = true;
      const rect = checkbox.getBoundingClientRect?.();
      if (rect?.width > 0 && rect?.height > 0) {
        return rect;
      }
    }
    const taskLike = hasTaskCheckbox || listItem.matches?.(".task-list-item, [data-task], [data-task-status]") || element?.matches?.(".task-list-item, [data-task], [data-task-status]");
    if (!taskLike) {
      return null;
    }
    const base = elementRect || element?.getBoundingClientRect?.();
    const listRect = listItem.getBoundingClientRect?.();
    const left = Number.isFinite(listRect?.left) ? listRect.left : base?.left;
    const top = Number.isFinite(base?.top) ? base.top : listRect?.top;
    const bottom = Number.isFinite(base?.bottom) ? base.bottom : listRect?.bottom;
    if (![left, top, bottom].every(Number.isFinite) || bottom <= top) {
      return null;
    }
    const markerWidth = Math.max(14, Math.min(24, bottom - top));
    return {
      left,
      right: left + markerWidth,
      top,
      bottom,
      width: markerWidth,
      height: bottom - top
    };
  }
  markdownElementTextClientRect(element, limitRect = null) {
    if (!element?.ownerDocument?.createRange) {
      return null;
    }
    try {
      const range = element.ownerDocument.createRange();
      range.selectNodeContents(element);
      const rects = Array.from(range.getClientRects?.() || []).filter((rect) => {
        if (!(rect.width > 0.5 && rect.height > 0.5)) {
          return false;
        }
        return !limitRect || Math.min(rect.bottom, limitRect.bottom) - Math.max(rect.top, limitRect.top) > 0.5;
      });
      if (!rects.length) {
        return null;
      }
      const left = Math.min(...rects.map((rect) => rect.left));
      const right = Math.max(...rects.map((rect) => rect.right));
      const top = Math.min(...rects.map((rect) => rect.top));
      const bottom = Math.max(...rects.map((rect) => rect.bottom));
      return { left, right, top, bottom, width: right - left, height: bottom - top };
    } catch (error) {
      void error;
      return null;
    }
  }
  markdownElementCanvasBounds(element, { forSelection = false } = {}) {
    if (!element?.isConnected || !this.canvas) {
      return null;
    }
    const elementRect = element.getBoundingClientRect();
    const canvasRect = this.canvas.getBoundingClientRect();
    if (elementRect.width <= 0 || elementRect.height <= 0 || canvasRect.width <= 0 || canvasRect.height <= 0) {
      return null;
    }
    let left = elementRect.left;
    let right = elementRect.right;
    let top = elementRect.top;
    let bottom = elementRect.bottom;
    if (forSelection) {
      const visibleRect = this.markdownElementVisibleClientRect(element) || elementRect;
      const includesReservedHeight = element.hasAttribute("data-note-draw-resized-height");
      const contentElement = findNoteFlowInlineMeasureElement(element);
      const contentRect = contentElement && contentElement !== element ? contentElement.getBoundingClientRect?.() : null;
      const textRect = this.markdownElementTextClientRect(contentElement || element, visibleRect);
      if (includesReservedHeight) {
        left = visibleRect.left;
        right = visibleRect.right;
        top = visibleRect.top;
        bottom = visibleRect.bottom;
      } else if (contentRect?.width > 0 && contentRect?.height > 0) {
        left = contentRect.left;
        right = contentRect.right;
        top = Math.max(visibleRect.top, textRect?.top ?? contentRect.top);
        bottom = Math.min(visibleRect.bottom, textRect?.bottom ?? contentRect.bottom);
      } else {
        left = visibleRect.left;
        right = visibleRect.right;
        top = textRect ? Math.max(visibleRect.top, textRect.top) : visibleRect.top;
        bottom = textRect ? Math.min(visibleRect.bottom, textRect.bottom) : visibleRect.bottom;
      }
      left = Math.min(left, visibleRect.left);
      left -= Math.max(7, Math.min(14, Math.max(1, bottom - top) * 0.22));
      const checkboxRect = this.markdownTaskCheckboxRect(element, visibleRect);
      if (checkboxRect) {
        left = Math.min(left, checkboxRect.left);
        right = Math.max(right, checkboxRect.right);
        top = Math.min(top, checkboxRect.top);
        bottom = Math.max(bottom, checkboxRect.bottom);
      }
      const inlineLimits = this.markdownInlineSelectionClientLimits(element, { left, right, top, bottom, width: right - left, height: bottom - top });
      left = Math.max(left, inlineLimits.left);
      right = Math.min(right, inlineLimits.right);
    }
    if (right <= left || bottom <= top) {
      return null;
    }
    const scaleX = this.canvasWidth() / canvasRect.width;
    const scaleY = this.canvasRenderHeight / canvasRect.height;
    return {
      minX: (left - canvasRect.left) * scaleX,
      maxX: (right - canvasRect.left) * scaleX,
      minY: this.canvasWindowTop + (top - canvasRect.top) * scaleY,
      maxY: this.canvasWindowTop + (bottom - canvasRect.top) * scaleY
    };
  }
  getStrokeIndexesBounds(indexes) {
    let result = null;
    for (const index of indexes) {
      const bounds = getStrokeBounds(this.drawingData.strokes[index], this.canvasWidth(), this.canvasHeight());
      if (!bounds) {
        continue;
      }
      result = result ? {
        minX: Math.min(result.minX, bounds.minX),
        maxX: Math.max(result.maxX, bounds.maxX),
        minY: Math.min(result.minY, bounds.minY),
        maxY: Math.max(result.maxY, bounds.maxY)
      } : { ...bounds };
    }
    return result;
  }
  getStrokeIndexesNormalizedBounds(indexes) {
    const bounds = this.getStrokeIndexesBounds(indexes);
    const width = this.canvasWidth();
    const height = this.canvasHeight();
    if (!bounds || width <= 0 || height <= 0) {
      return null;
    }
    return {
      minX: clamp10(bounds.minX / width, 0, 1),
      minY: clamp10(bounds.minY / height, 0, 1),
      maxX: clamp10(bounds.maxX / width, 0, 1),
      maxY: clamp10(bounds.maxY / height, 0, 1)
    };
  }
  getSelectedStrokeNormalizedBounds() {
    const bounds = this.getSelectedStrokeBounds();
    if (!bounds) {
      return null;
    }
    return {
      minX: clamp10(bounds.minX / this.canvasWidth(), 0, 1),
      minY: clamp10(bounds.minY / this.canvasHeight(), 0, 1),
      maxX: clamp10(bounds.maxX / this.canvasWidth(), 0, 1),
      maxY: clamp10(bounds.maxY / this.canvasHeight(), 0, 1)
    };
  }
  shouldSnapStrokeIndexes(indexes) {
    return indexes.some((index) => isSnapStroke(this.drawingData.strokes[index]));
  }
  prepareDragSnapBounds(excludeIndexes = []) {
    if (this.dragSnapOtherBounds) {
      return this.dragSnapOtherBounds;
    }
    const exclude = new Set(excludeIndexes);
    const width = this.canvasWidth();
    const height = this.canvasHeight();
    this.dragSnapOtherBounds = [];
    for (let index = 0; index < this.drawingData.strokes.length; index += 1) {
      if (exclude.has(index)) {
        continue;
      }
      const itemBounds = getStrokeBounds(this.drawingData.strokes[index], width, height);
      if (itemBounds) {
        this.dragSnapOtherBounds.push(itemBounds);
      }
    }
    this.dragSnapOtherXValues = this.dragSnapOtherBounds.flatMap((item) => [item.minX, (item.minX + item.maxX) / 2, item.maxX]);
    this.dragSnapOtherYValues = this.dragSnapOtherBounds.flatMap((item) => [item.minY, (item.minY + item.maxY) / 2, item.maxY]);
    return this.dragSnapOtherBounds;
  }
  computeSnapDeltaForNormalizedBounds(bounds, excludeIndexes = []) {
    if (!bounds) {
      return { dx: 0, dy: 0 };
    }
    const width = this.canvasWidth();
    const height = this.canvasHeight();
    const otherBounds = this.draggingStroke && this.dragSnapOtherBounds ? this.dragSnapOtherBounds : [];
    let xCandidates = this.draggingStroke && this.dragSnapOtherXValues;
    let yCandidates = this.draggingStroke && this.dragSnapOtherYValues;
    if (!otherBounds.length && !(this.draggingStroke && this.dragSnapOtherBounds)) {
      const exclude = new Set(excludeIndexes);
      const fallbackBounds = [];
      for (let index = 0; index < this.drawingData.strokes.length; index += 1) {
        if (exclude.has(index)) {
          continue;
        }
        const itemBounds = getStrokeBounds(this.drawingData.strokes[index], width, height);
        if (itemBounds) {
          fallbackBounds.push(itemBounds);
        }
      }
      xCandidates = fallbackBounds.flatMap((item) => [item.minX, (item.minX + item.maxX) / 2, item.maxX]);
      yCandidates = fallbackBounds.flatMap((item) => [item.minY, (item.minY + item.maxY) / 2, item.maxY]);
    }
    const xValues = [bounds.minX * width, (bounds.minX + bounds.maxX) * width / 2, bounds.maxX * width];
    const yValues = [bounds.minY * height, (bounds.minY + bounds.maxY) * height / 2, bounds.maxY * height];
    return {
      dx: nearestSnapDelta(xValues, xCandidates || [], SNAP_GRID_PX, SNAP_THRESHOLD_PX) / width,
      dy: nearestSnapDelta(yValues, yCandidates || [], SNAP_GRID_PX, SNAP_THRESHOLD_PX) / height
    };
  }
  snapPointForPreset(point, preset) {
    if (!isSnapPreset(preset)) {
      return point;
    }
    const width = this.canvasWidth();
    const height = this.canvasHeight();
    const x = point.x * width;
    const y = point.y * height;
    const bounds = { minX: point.x, minY: point.y, maxX: point.x, maxY: point.y };
    const delta = this.computeSnapDeltaForNormalizedBounds(bounds, []);
    return {
      ...point,
      x: clamp10((x + delta.dx * width) / width, 0, 1),
      y: clamp10((y + delta.dy * height) / height, 0, 1)
    };
  }
  reorderSelectedStrokes(direction) {
    const selectedIndexes = this.getSelectedStrokeIndexes();
    if (!selectedIndexes.length) {
      return;
    }
    const historyBefore = this.captureDrawingHistorySnapshot();
    const strokes = this.drawingData.strokes;
    const selectedStrokes = selectedIndexes.map((index) => strokes[index]).filter(Boolean);
    const selectedSet = new Set(selectedStrokes);
    if (direction === "front" || direction === "forward") {
      selectedStrokes.forEach((stroke) => {
        stroke.belowMarkdown = false;
      });
    } else if (direction === "back") {
      selectedStrokes.forEach((stroke) => {
        stroke.belowMarkdown = true;
      });
    }
    if (direction === "front" || direction === "back") {
      const rest = strokes.filter((stroke) => !selectedSet.has(stroke));
      this.drawingData.strokes = direction === "front" ? [...rest, ...selectedStrokes] : [...selectedStrokes, ...rest];
    } else if (direction === "forward") {
      for (let index = strokes.length - 2; index >= 0; index -= 1) {
        if (selectedSet.has(strokes[index]) && !selectedSet.has(strokes[index + 1])) {
          [strokes[index], strokes[index + 1]] = [strokes[index + 1], strokes[index]];
        }
      }
    } else if (direction === "backward") {
      for (let index = 1; index < strokes.length; index += 1) {
        if (selectedSet.has(strokes[index]) && !selectedSet.has(strokes[index - 1])) {
          [strokes[index], strokes[index - 1]] = [strokes[index - 1], strokes[index]];
        }
      }
    }
    this.setSelectedStrokes(selectedStrokes.map((stroke) => this.drawingData.strokes.indexOf(stroke)).filter((index) => index >= 0));
    this.hideSelectionMenu();
    this.redoStack = [];
    this.invalidateStaticCache();
    this.plugin.scheduleDrawingSave(this.file, this.drawingData, { userOperation: true });
    this.recordDrawingHistory(historyBefore);
    this.scheduleNoteFlowLayout();
    this.render();
  }
  toggleSelectedStrokeLock() {
    const indexes = this.getSelectedStrokeIndexes();
    const blocks = this.getSelectedMarkdownBlocks();
    if (!indexes.length && !blocks.length) {
      return;
    }
    const historyBefore = this.captureDrawingHistorySnapshot();
    const shouldUnlock = indexes.every((index) => this.drawingData.strokes[index]?.locked) && blocks.every((block) => block.locked);
    const itemCount = indexes.length + blocks.length;
    const existingGroup = [
      ...indexes.map((index) => this.drawingData.strokes[index]?.groupId),
      ...blocks.map((block) => block.groupId)
    ].find(Boolean);
    const groupId = shouldUnlock || itemCount < 2 ? "" : existingGroup || `group-${Date.now().toString(36)}-${hashString(`${this.file?.path || ""}:${itemCount}`)}`;
    for (const index of indexes) {
      const stroke = this.drawingData.strokes[index];
      if (stroke) {
        stroke.locked = !shouldUnlock;
        stroke.groupId = groupId;
      }
    }
    for (const block of blocks) {
      block.locked = !shouldUnlock;
      block.groupId = groupId;
    }
    if (groupId) {
      const group = this.elementGroup(groupId);
      if (group) {
        group.locked = !shouldUnlock;
        group.boxOwnedLock = false;
      } else {
        this.elementGroupRecords().push({ id: groupId, boxed: false, locked: true, boxOwnedLock: false, borderColor: "#64748b", backgroundColor: "" });
      }
    }
    if (shouldUnlock && existingGroup) {
      this.enteredElementGroupIds.delete(existingGroup);
    }
    this.pruneElementGroups();
    this.hideSelectionMenu();
    this.redoStack = [];
    this.invalidateStaticCache();
    this.plugin.scheduleDrawingSave(this.file, this.drawingData, { userOperation: true });
    this.recordDrawingHistory(historyBefore);
    this.render();
  }
  pruneElementGroups() {
    const groups = this.elementGroupRecords();
    for (const group of groups) {
      const strokeIndexes = this.groupMemberStrokeIndexes(group.id);
      const blocks = this.groupMemberMarkdownBlocks(group.id);
      if (!group.boxed && strokeIndexes.length + blocks.length < 2) {
        for (const index of strokeIndexes) {
          this.drawingData.strokes[index].groupId = "";
        }
        for (const block of blocks) {
          block.groupId = "";
        }
      }
    }
    const liveIds = new Set([
      ...this.drawingData.strokes.map((stroke) => stroke?.groupId),
      ...this.markdownBlockRecords().map((block) => block.groupId)
    ].filter(Boolean));
    this.drawingData.elementGroups = groups.filter((group) => liveIds.has(group.id));
  }
  toggleSelectedElementBox() {
    const indexes = this.getSelectedStrokeIndexes();
    const blocks = this.getSelectedMarkdownBlocks();
    if (!indexes.length && !blocks.length) {
      return;
    }
    const historyBefore = this.captureDrawingHistorySnapshot();
    const boxedGroups = this.selectedWholeElementGroups().filter((group) => group.boxed);
    if (boxedGroups.length === 1) {
      const group = boxedGroups[0];
      if (!group.backgroundColor) {
        group.backgroundColor = isCssColor(group.borderColor) ? group.borderColor : SELECTION_FRAME_COLOR;
      } else {
        this.clearElementGroupCommands(group.id);
        group.boxed = false;
        group.backgroundColor = "";
        if (group.boxOwnedLock) {
          for (const index of this.groupMemberStrokeIndexes(group.id)) {
            this.drawingData.strokes[index].groupId = "";
            this.drawingData.strokes[index].locked = false;
          }
          for (const block of this.groupMemberMarkdownBlocks(group.id)) {
            block.groupId = "";
            block.locked = false;
          }
        }
      }
    } else {
      const existingGroupId = [
        ...indexes.map((index) => this.drawingData.strokes[index]?.groupId),
        ...blocks.map((block) => block.groupId)
      ].filter(Boolean).find((id) => this.isElementGroupFullySelected(id));
      const groupId = existingGroupId || `group-${Date.now().toString(36)}-${hashString(`${this.file?.path || ""}:box:${indexes.length + blocks.length}`)}`;
      for (const index of indexes) {
        const stroke = this.drawingData.strokes[index];
        stroke.groupId = groupId;
        stroke.locked = true;
      }
      for (const block of blocks) {
        block.groupId = groupId;
        block.locked = true;
      }
      let group = this.elementGroup(groupId);
      if (!group) {
        group = { id: groupId, boxed: true, locked: true, boxOwnedLock: true, borderColor: "#64748b", backgroundColor: "" };
        this.elementGroupRecords().push(group);
      }
      group.boxed = true;
      group.locked = true;
      group.boxOwnedLock = group.boxOwnedLock !== false;
    }
    this.pruneElementGroups();
    this.redoStack = [];
    this.invalidateStaticCache();
    this.plugin.scheduleDrawingSave(this.file, this.drawingData, { userOperation: true });
    this.recordDrawingHistory(historyBefore);
    this.syncSelectionMenuButtons();
    this.render();
  }
  elementFramePaddingPx(strokeIndexes, markdownCount = 0) {
    const widths = Array.from(strokeIndexes || []).map((index) => {
      return Number(this.drawingData.strokes[index]?.width) || this.penWidth;
    });
    const strokePadding = widths.length ? Math.max(...widths) / 2 + 2 : 0;
    return Math.max(3, markdownCount > 0 ? 8 : 0, strokePadding);
  }
  selectionFramePaddingPx() {
    return this.elementFramePaddingPx(
      this.getSelectedStrokeIndexes(),
      this.getSelectedMarkdownBlocks().length
    );
  }
  getSelectedFrameCanvasRect() {
    if (!this.hasHybridSelection()) {
      return null;
    }
    if (this.resizingSelection && this.resizeSelectionPreviewBounds) {
      const bounds = this.resizeSelectionPreviewBounds;
      const width = Math.max(1, Number(this.resizeSelectionPointerGeometry?.canvasWidth) || this.canvasWidth());
      const height = Math.max(1, Number(this.resizeSelectionPointerGeometry?.canvasHeight) || this.canvasHeight());
      const padding = this.effectiveSelectionFramePaddingPx();
      return {
        x: bounds.minX * width - padding,
        y: bounds.minY * height - padding,
        width: (bounds.maxX - bounds.minX) * width + padding * 2,
        height: (bounds.maxY - bounds.minY) * height + padding * 2
      };
    }
    if (this.draggingStroke || this.resizingSelection) {
      const bounds = this.getSelectedStrokeBounds();
      if (!bounds) {
        return null;
      }
      const padding = this.effectiveSelectionFramePaddingPx();
      return {
        x: bounds.minX - padding,
        y: bounds.minY - padding,
        width: bounds.maxX - bounds.minX + padding * 2,
        height: bounds.maxY - bounds.minY + padding * 2
      };
    }
    return this.captureSelectionFrameSnapshot() || null;
  }
  getVisibleSelectionFrameCanvasRect() {
    const frame = this.getSelectedFrameCanvasRect();
    if (!frame) {
      return null;
    }
    const inset = SELECT_RESIZE_HANDLE_SIZE / 2 + SELECTION_FRAME_LINE_WIDTH / 2;
    const fitted = fitSelectionFrameToCanvas(frame, {
      canvasWidth: this.canvasWidth(),
      canvasHeight: this.canvasHeight(),
      inset
    });
    if (!fitted) {
      return null;
    }
    const outlineLeft = this.selectedOutlineFrameLeft();
    const visibleLeft = Math.max(1, Number.isFinite(outlineLeft) ? outlineLeft : Number(frame.x) || 0);
    const right = fitted.x + fitted.width;
    return {
      ...fitted,
      x: visibleLeft,
      width: Math.max(1, right - visibleLeft)
    };
  }
  selectedOutlineFrameLeft() {
    const boxedGroups = this.selectedWholeElementGroups().filter((group) => group.boxed);
    if (boxedGroups.length === 1) {
      return this.elementGroupFrameRect(boxedGroups[0].id)?.x;
    }
    const indexes = this.getSelectedStrokeIndexes();
    const blocks = this.getSelectedMarkdownBlocks();
    if (indexes.length === 1 && blocks.length === 0 && this.drawingData?.strokes?.[indexes[0]]?.boxed) {
      return getStrokeBounds(this.drawingData.strokes[indexes[0]], this.canvasWidth(), this.canvasHeight())?.minX;
    }
    return null;
  }
  findSelectionHandleAt(point) {
    const canResize = (item) => this.canManipulateGroupedElement(item);
    const hasUnlocked = this.getSelectedStrokeIndexes().some((index) => canResize(this.drawingData.strokes[index])) || this.getSelectedMarkdownBlocks().some(canResize);
    if (!hasUnlocked) {
      return null;
    }
    const frame = this.getVisibleSelectionFrameCanvasRect();
    if (!frame) {
      return null;
    }
    const hitPoint = this.pointToCanvas(point);
    const rects = [frame];
    const canvasRect = this.canvas?.getBoundingClientRect?.();
    const scaleX = canvasRect?.width > 0 ? canvasRect.width / Math.max(1, this.canvasWidth()) : 1;
    const scaleY = canvasRect?.height > 0 ? canvasRect.height / Math.max(1, this.canvasRenderHeight) : 1;
    const screenHitRadius = Math.max(SELECT_RESIZE_HANDLE_HIT_RADIUS, this.selectionHitPaddingPx() + 6, 28);
    const hitRadiusX = screenHitRadius / Math.max(0.01, Math.abs(scaleX));
    const hitRadiusY = screenHitRadius / Math.max(0.01, Math.abs(scaleY));
    let best = null;
    for (const [rectIndex, rect] of rects.entries()) {
      if (rect.width <= hitRadiusX * 2 && hitPoint.x >= rect.x - hitRadiusX && hitPoint.x <= rect.x + rect.width + hitRadiusX) {
        const topDistance = Math.abs(hitPoint.y - rect.y);
        const bottomDistance = Math.abs(hitPoint.y - (rect.y + rect.height));
        if (Math.min(topDistance, bottomDistance) <= hitRadiusY) {
          const top = topDistance <= bottomDistance;
          const right = hitPoint.x >= rect.x + rect.width / 2;
          const handle = top ? right ? "ne" : "nw" : right ? "se" : "sw";
          const handleX = right ? rect.x + rect.width : rect.x;
          const handleY = top ? rect.y : rect.y + rect.height;
          const distance = ((hitPoint.x - handleX) / hitRadiusX) ** 2 + ((hitPoint.y - handleY) / hitRadiusY) ** 2;
          if (!best || distance < best.distance || distance === best.distance && rectIndex < best.rectIndex) {
            best = { handle, distance, rectIndex };
          }
          continue;
        }
      }
      for (const handle of getSelectionHandlePointsFromRect(rect)) {
        const dx = hitPoint.x - handle.x;
        const dy = hitPoint.y - handle.y;
        if (Math.abs(dx) > hitRadiusX || Math.abs(dy) > hitRadiusY) {
          continue;
        }
        const distance = (dx / hitRadiusX) ** 2 + (dy / hitRadiusY) ** 2;
        if (!best || distance < best.distance || distance === best.distance && rectIndex < best.rectIndex) {
          best = { handle: handle.handle, distance, rectIndex };
        }
      }
    }
    return best?.handle || null;
  }
  selectedStrokeFrameContains(point) {
    const rect = this.getSelectedFrameCanvasRect();
    if (!rect) {
      return false;
    }
    const hitPoint = this.pointToCanvas(point);
    const hitPadding = this.selectionHitPaddingPx();
    return hitPoint.x >= rect.x - hitPadding && hitPoint.x <= rect.x + rect.width + hitPadding && hitPoint.y >= rect.y - hitPadding && hitPoint.y <= rect.y + rect.height + hitPadding;
  }
  applyTextEditingFlowClip(element) {
    const insets = this.noteFlowAppliedVerticalInsets(element);
    const top = Math.max(0, Number(insets.top) || 0);
    const bottom = Math.max(0, Number(insets.bottom) || 0);
    if (top <= 0 && bottom <= 0) {
      this.clearTextEditingFlowClip(element);
      return;
    }
    element.addClass("notedraw-editing-flow-clipped");
    setNoteDrawCssProps(element, {
      "--notedraw-editing-flow-top": `${top}px`,
      "--notedraw-editing-flow-bottom": `${bottom}px`
    });
  }
  clearTextEditingFlowClip(element) {
    element?.removeClass?.("notedraw-editing-flow-clipped");
    element?.style?.removeProperty("--notedraw-editing-flow-top");
    element?.style?.removeProperty("--notedraw-editing-flow-bottom");
  }
  captureEmbeddedEditorLayout(element) {
    if (!this.currentEditorEmbedded || !element?.isConnected) {
      return null;
    }
    const rect = element.getBoundingClientRect?.();
    if (!rect?.width || !rect?.height) {
      return null;
    }
    const scrollers = [];
    const seen = /* @__PURE__ */ new Set();
    let current = element.parentElement;
    while (current) {
      if (!seen.has(current) && (current === this.scrollContainer || current.scrollHeight > current.clientHeight + 1 || current.scrollWidth > current.clientWidth + 1)) {
        seen.add(current);
        scrollers.push({ element: current, top: Number(current.scrollTop) || 0, left: Number(current.scrollLeft) || 0 });
      }
      current = current.parentElement;
    }
    return {
      element,
      scrollers,
      width: Math.max(1, Number(element.offsetWidth) || rect.width),
      height: Math.max(1, Number(element.offsetHeight) || rect.height),
      frameIds: []
    };
  }
  restoreEmbeddedEditorViewport(lock = this.currentEditorLayoutLock) {
    if (!lock || lock !== this.currentEditorLayoutLock) {
      return;
    }
    for (const scroll of lock.scrollers) {
      if (!scroll.element?.isConnected) {
        continue;
      }
      if (Math.abs((Number(scroll.element.scrollTop) || 0) - scroll.top) > 0.25) {
        scroll.element.scrollTop = scroll.top;
      }
      if (Math.abs((Number(scroll.element.scrollLeft) || 0) - scroll.left) > 0.25) {
        scroll.element.scrollLeft = scroll.left;
      }
    }
  }
  stabilizeEmbeddedEditor(element, capturedLock = null) {
    const lock = capturedLock || this.captureEmbeddedEditorLayout(element);
    this.currentEditorLayoutLock = lock;
    if (!lock) {
      return;
    }
    element.addClass("notedraw-editing-embedded-stable");
    setNoteDrawCssProps(element, {
      "--notedraw-editing-stable-width": `${lock.width}px`,
      "--notedraw-editing-stable-height": `${lock.height}px`
    });
    this.restoreEmbeddedEditorViewport(lock);
    const restoreFrame = () => {
      if (lock !== this.currentEditorLayoutLock) {
        return;
      }
      this.restoreEmbeddedEditorViewport(lock);
      this.positionFormatToolbar();
    };
    if (typeof window.requestAnimationFrame === "function") {
      lock.frameIds.push(window.requestAnimationFrame(() => {
        restoreFrame();
        lock.frameIds.push(window.requestAnimationFrame(restoreFrame));
      }));
    }
  }
  clearEmbeddedEditorStability(element) {
    const lock = this.currentEditorLayoutLock;
    for (const frameId of lock?.frameIds || []) {
      window.cancelAnimationFrame?.(frameId);
    }
    element?.removeClass?.("notedraw-editing-embedded-stable");
    element?.style?.removeProperty("--notedraw-editing-stable-width");
    element?.style?.removeProperty("--notedraw-editing-stable-height");
    this.currentEditorLayoutLock = null;
  }
  startTextEdit(element, clientPoint = null) {
    if (!element?.isConnected || this.surfaceType !== "webview" && !this.previewEl?.contains?.(element)) {
      const block = this.findMarkdownBlockRecordForElement(element);
      const replacement = block ? this.markdownBlockElement(block) : null;
      if (!replacement?.isConnected || !this.previewEl?.contains?.(replacement)) {
        return false;
      }
      element = replacement;
    }
    const metadataProperty = element.closest?.(METADATA_PROPERTY_SELECTOR);
    if (metadataProperty) {
      return this.activateMetadataPropertyEditor(metadataProperty, clientPoint);
    }
    if (this.currentEditor === element) {
      this.applyTextEditingFlowClip(element);
      element.focus();
      placeCaretInEditable(element, clientPoint);
      this.currentTextRange = window.getSelection?.()?.rangeCount ? window.getSelection().getRangeAt(0).cloneRange() : null;
      this.positionFormatToolbar();
      return true;
    }
    this.endTextEdit();
    this.currentEditor = element;
    this.currentEditorFile = this.plugin.resolveEditableFile(element, this.file);
    this.currentEditorEmbedded = this.embeddedSurface || isEmbeddedEditableElement(element) || normalizeVaultPath(this.currentEditorFile?.path) !== normalizeVaultPath(this.file?.path);
    this.formatToolbarManualPosition = null;
    element.dataset.noteDrawOriginal = element.innerText;
    const saveToVault = this.surfaceType !== "webview";
    if (saveToVault) {
      this.plugin.prepareTextEditState(this.currentEditorFile, element.innerText, element, this);
    }
    const editorLayoutLock = this.captureEmbeddedEditorLayout(element);
    this.currentEditorLayoutLock = editorLayoutLock;
    element.contentEditable = "true";
    element.spellcheck = true;
    element.addClass("notedraw-editing");
    this.applyTextEditingFlowClip(element);
    if (editorLayoutLock) {
      this.stabilizeEmbeddedEditor(element, editorLayoutLock);
    }
    this.previewEl.addClass("is-native-text-editing");
    this.formatToolbar?.addClass("is-visible");
    element.focus();
    placeCaretInEditable(element, clientPoint);
    this.currentTextRange = window.getSelection?.()?.rangeCount ? window.getSelection().getRangeAt(0).cloneRange() : null;
    this.positionFormatToolbar();
    const onInput = () => {
      if (!saveToVault) {
        return;
      }
      const original = element.dataset.noteDrawOriginal || "";
      const edited = serializeControllerEditableSource(element, this.currentEditorEmbedded);
      if (this.currentEditorEmbedded) {
        this.plugin.stageTextSave(this.currentEditorFile, original, edited, element, this);
      } else {
        this.plugin.stageTextSave(this.currentEditorFile, original, edited, element, this);
      }
      this.positionFormatToolbar();
    };
    const onKeyDown = (event) => {
      if (event.key === "Escape") {
        this.endTextEdit();
      }
      if ((event.metaKey || event.ctrlKey) && event.key === "Enter") {
        event.preventDefault();
        this.endTextEdit();
      }
      if (event.metaKey || event.ctrlKey) {
        const key = event.key.toLowerCase();
        if (key === "b") {
          event.preventDefault();
          this.applyTextInlineFormat("strong");
        } else if (key === "i") {
          event.preventDefault();
          this.applyTextInlineFormat("em");
        } else if (key === "u") {
          event.preventDefault();
          this.applyTextInlineFormat("u");
        }
      }
    };
    const onBlur = (event) => {
      const nextTarget = event.relatedTarget;
      window.setTimeout(() => {
        const active = activeDocument.activeElement;
        if (this.currentEditor !== element || element.dataset.noteDrawSortDragging === "true" || element.contains(active) || this.isTextEditingControlTarget(active) || this.isTextEditingControlTarget(nextTarget)) {
          return;
        }
        this.endTextEdit();
      }, 120);
    };
    element._noteDrawCleanup = () => {
      element.removeEventListener("input", onInput);
      element.removeEventListener("keydown", onKeyDown);
      element.removeEventListener("blur", onBlur);
    };
    element.addEventListener("input", onInput);
    element.addEventListener("keydown", onKeyDown);
    element.addEventListener("blur", onBlur);
    return true;
  }
  isTextEditingControlTarget(target) {
    return Boolean(target && [
      this.toolbar,
      this.formatToolbar,
      this.palettePanel,
      this.brushPanel,
      this.textPanel,
      this.selectionMenu
    ].some((element) => element?.contains?.(target)));
  }
  activateMetadataPropertyEditor(property, clientPoint = null) {
    if (!property?.isConnected || !this.previewEl?.contains?.(property)) {
      return false;
    }
    const value = property.querySelector?.(".metadata-property-value, .metadata-input-longtext, input, textarea, select") || property;
    const rect = value.getBoundingClientRect?.() || property.getBoundingClientRect?.();
    const point = clientPoint || (rect ? {
      x: rect.left + Math.min(Math.max(8, rect.width * 0.5), Math.max(8, rect.width - 8)),
      y: rect.top + Math.max(8, rect.height * 0.5)
    } : null);
    this.hideSelectionMenu();
    return dispatchMouseClickThroughOverlay(this.canvas, point);
  }
  focusSourceEditorAt(clientPoint) {
    if (!clientPoint || !Number.isFinite(clientPoint.x) || !Number.isFinite(clientPoint.y)) {
      return false;
    }
    const cmView = getCodeMirrorView(this.view, this.previewEl);
    if (cmView && typeof cmView.posAtCoords === "function") {
      const pos = cmView.posAtCoords({ x: clientPoint.x, y: clientPoint.y }, false) ?? cmView.posAtCoords({ x: clientPoint.x, y: clientPoint.y });
      if (Number.isFinite(pos)) {
        cmView.focus?.();
        cmView.dispatch?.({
          selection: { anchor: pos },
          scrollIntoView: false
        });
        return true;
      }
    }
    const editor = this.view?.editor;
    if (editor && typeof editor.focus === "function") {
      editor.focus();
    }
    return dispatchMouseClickThroughOverlay(this.canvas, clientPoint);
  }
  endTextEdit(options = {}) {
    const element = this.currentEditor;
    if (!element) {
      return Promise.resolve(true);
    }
    const original = element.dataset.noteDrawOriginal || "";
    const edited = this.surfaceType === "webview" ? element.innerText : serializeControllerEditableSource(element, this.currentEditorEmbedded, true);
    let commit = Promise.resolve(true);
    if (options.save === false) {
    } else if (this.surfaceType === "webview") {
      commit = Promise.resolve(this.commitWebviewTextEdit(element, original, edited));
    } else if (normalizeEditableSourceText(original) !== normalizeEditableSourceText(edited)) {
      commit = this.queueTextSaveAndWait(this.currentEditorFile || this.file, original, edited, element);
    }
    const markdownBlock = this.findMarkdownBlockRecordForElement(element);
    let markdownMetadataChanged = false;
    if (markdownBlock) {
      const nextHint = normalizeRenderedText2(element.innerText || element.textContent || "").slice(0, 240);
      const info = getSourceInfo(element);
      markdownMetadataChanged = markdownBlock.textHint !== nextHint || markdownBlock.lineStart !== (Number.isFinite(info.lineStart) ? info.lineStart : markdownBlock.lineStart) || markdownBlock.lineEnd !== (Number.isFinite(info.lineEnd) ? info.lineEnd : markdownBlock.lineEnd);
      markdownBlock.textHint = nextHint;
      if (Number.isFinite(info.lineStart)) {
        markdownBlock.lineStart = info.lineStart;
        markdownBlock.lineEnd = info.lineEnd ?? info.lineStart;
      }
    }
    element._noteDrawCleanup?.();
    delete element._noteDrawCleanup;
    delete element.dataset.noteDrawOriginal;
    element.querySelector(":scope > .notedraw-text-sort-handle")?.remove();
    element.contentEditable = "false";
    this.clearTextEditingFlowClip(element);
    this.clearEmbeddedEditorStability(element);
    element.removeClass("notedraw-editing");
    this.previewEl.removeClass("is-native-text-editing");
    this.formatToolbar?.removeClass("is-visible");
    this.stopFormatToolbarDrag();
    this.currentTextRange = null;
    this.formatToolbarManualPosition = null;
    this.currentEditor = null;
    this.currentEditorFile = null;
    this.currentEditorEmbedded = false;
    if (markdownMetadataChanged && this.surfaceType === "preview") {
      this.plugin.scheduleDrawingSave(this.file, this.drawingData, { userOperation: true });
    }
    return commit;
  }
  commitWebviewTextEdit(element, originalText, editedText) {
    const normalizedOriginal = normalizeRenderedText2(originalText);
    const normalizedEdited = normalizeRenderedText2(editedText);
    if (!normalizedOriginal || normalizedOriginal === normalizedEdited) {
      return;
    }
    const edit = {
      kind: "text",
      path: domPathForElement(element, this.previewEl),
      originalText: String(originalText || ""),
      editedText: String(editedText || ""),
      updatedAt: (/* @__PURE__ */ new Date()).toISOString()
    };
    if (!edit.path) {
      return;
    }
    const historyBefore = this.captureDrawingHistorySnapshot();
    const edits = Array.isArray(this.drawingData.webEdits) ? this.drawingData.webEdits : [];
    const existingIndex = edits.findIndex((item) => item?.kind === "text" && item.path === edit.path && normalizeRenderedText2(item.originalText) === normalizedOriginal);
    if (existingIndex >= 0) {
      edits[existingIndex] = edit;
    } else {
      edits.push(edit);
    }
    this.drawingData.webEdits = edits;
    this.redoStack = [];
    this.recordDrawingHistory(historyBefore);
    this.plugin.scheduleDrawingSave(this.file, this.drawingData, { userOperation: true });
    this.render();
    return true;
  }
  applyWebEdits() {
    if (this.surfaceType !== "webview" || this.currentEditor || !Array.isArray(this.drawingData?.webEdits)) {
      return;
    }
    const used = /* @__PURE__ */ new Set();
    for (const edit of this.drawingData.webEdits) {
      if (edit?.kind !== "text" || !edit.path || typeof edit.editedText !== "string") {
        continue;
      }
      const original = normalizeRenderedText2(edit.originalText);
      const edited = normalizeRenderedText2(edit.editedText);
      if (!original || !edited) {
        continue;
      }
      const element = findWebEditElement(this.previewEl, edit, used);
      if (!element) {
        continue;
      }
      const current = normalizeRenderedText2(element.innerText);
      if (current !== original && current !== edited) {
        continue;
      }
      if (current !== edited) {
        element.innerText = edit.editedText;
      }
      used.add(element);
    }
  }
  recordMarkdownHistory(file2, before, after, coalesceKey = null) {
    if (String(before ?? "") === String(after ?? "")) {
      return;
    }
    this.plugin.recordControllerHistory(this, { kind: "markdown", file: file2, before, after }, { coalesceKey });
  }
  recordCompoundHistory(markdownFile, markdownBefore, markdownAfter, drawingBefore) {
    const drawingAfter = this.captureDrawingHistorySnapshot();
    if (String(markdownBefore ?? "") === String(markdownAfter ?? "") && JSON.stringify(drawingBefore) === JSON.stringify(drawingAfter)) {
      return;
    }
    this.plugin.recordControllerHistory(this, {
      kind: "compound",
      drawingFile: this.file,
      drawingBefore,
      drawingAfter,
      markdownFile,
      markdownBefore,
      markdownAfter
    });
  }
  captureDrawingHistorySnapshot() {
    return cloneDrawingData(this.drawingData, this.file);
  }
  recordDrawingHistory(before, options = {}) {
    const after = this.captureDrawingHistorySnapshot();
    if (options.knownChanged !== true && JSON.stringify(before) === JSON.stringify(after)) {
      return;
    }
    this.plugin.recordControllerHistory(this, { kind: "drawing", file: this.file, before, after });
  }
  async commitActiveTextEditForHistory() {
    if (this.currentEditor) {
      await this.endTextEdit();
    }
    await this.textCommitBarrier.wait();
  }
  async undoLastStroke() {
    if (this.applyActiveTextHistory("undo")) {
      return;
    }
    await this.commitActiveTextEditForHistory();
    await this.plugin.undoControllerHistory(this);
    this.clearSelectedStrokes();
    this.render();
  }
  async redoLastStroke() {
    if (this.applyActiveTextHistory("redo")) {
      return;
    }
    await this.commitActiveTextEditForHistory();
    await this.plugin.redoControllerHistory(this);
    this.clearSelectedStrokes();
    this.render();
  }
  applyActiveTextHistory(direction) {
    if (this.currentEditor?.isConnected) {
      this.currentEditor.focus?.();
      const applied = activeDocument.execCommand?.(direction, false, null);
      if (applied !== false) {
        this.currentEditor.dispatchEvent?.(new Event("input", { bubbles: true }));
        this.positionFormatToolbar();
        return true;
      }
    }
    if (this.surfaceType === "source" && this.toolMode === TOOL_EDIT_MD) {
      const editor = this.view?.editor;
      const action = direction === "redo" ? editor?.redo : editor?.undo;
      if (typeof action === "function") {
        action.call(editor);
        return true;
      }
    }
    return false;
  }
  deleteSelectedStroke() {
    const indexes = this.getSelectedStrokeIndexes().filter((index) => !this.drawingData.strokes[index]?.locked || Boolean(this.drawingData.strokes[index]?.groupId));
    if (!indexes.length) {
      return;
    }
    const removedNoteFlow = indexes.some((index) => this.drawingData.strokes[index]?.noteFlow?.enabled);
    const historyBefore = this.captureDrawingHistorySnapshot();
    const deletedIds = new Set(indexes.map((index) => strokeElementId(this.drawingData.strokes[index])).filter(Boolean));
    for (const index of indexes.slice().sort((a, b) => b - a)) {
      this.drawingData.strokes.splice(index, 1);
    }
    this.drawingData.strokes = this.drawingData.strokes.filter((stroke) => {
      const connector = normalizeConnector(stroke.connector);
      return !connector || !deletedIds.has(connector.fromId) && !deletedIds.has(connector.toId);
    });
    this.clearSelectedStrokes();
    this.redoStack = [];
    this.rebuildElementRelations();
    this.plugin.scheduleDrawingSave(this.file, this.drawingData, { userOperation: true, replace: true });
    this.recordDrawingHistory(historyBefore);
    this.scheduleNoteFlowLayout({ operation: removedNoteFlow });
    this.render();
  }
};
var notedraw_plugin_default = NoteDrawPlugin;
var NoteDrawSettingTab = class extends import_obsidian.PluginSettingTab {
  constructor(app, plugin) {
    super(app, plugin);
    this.plugin = plugin;
  }
  display() {
    const { containerEl } = this;
    containerEl.empty();
    for (const definition of this.getSettingDefinitions()) {
      const setting = new import_obsidian.Setting(containerEl);
      definition.render(setting);
    }
  }
  refreshSettingsView() {
    this.display();
  }
  getSettingDefinitions() {
    const settings = sanitizeSettings(this.plugin.noteDrawSettings);
    return [
      this.createSectionDefinition("settingsSectionInterface"),
      this.createSettingDefinition("settingsLanguage", "settingsLanguageDesc", (setting) => {
        setting.addDropdown((component) => {
          for (const option of LANGUAGE_OPTIONS) {
            component.addOption(option.value, option.value === LANGUAGE_AUTO ? `${this.plugin.t("languageAuto")} (${languageNativeName(detectNoteDrawLanguage(this.plugin.app))})` : option.label);
          }
          component.setValue(settings.language).onChange(async (value) => {
            this.plugin.noteDrawSettings.language = value;
            await this.plugin.saveSettings();
            this.refreshSettingsView();
          });
        });
      }),
      this.createSectionDefinition("settingsSectionStorage"),
      this.createSettingDefinition("drawingStorageMode", "drawingStorageModeDesc", (setting) => {
        setting.addDropdown((component) => {
          component.addOption(DRAWING_STORAGE_CONFIG, this.plugin.t("drawingStorageConfig")).addOption(DRAWING_STORAGE_NOTE_SUBFOLDER, this.plugin.t("drawingStorageNoteSubfolder")).addOption(DRAWING_STORAGE_NOTE_FOLDER, this.plugin.t("drawingStorageNoteFolder")).addOption(DRAWING_STORAGE_EMBEDDED, this.plugin.t("drawingStorageEmbedded")).setValue(settings.drawingStorageMode).onChange(async (value) => {
            await this.plugin.changeDrawingStorageMode(value);
            this.refreshSettingsView();
          });
        });
      }),
      this.createSectionDefinition("settingsSectionPen"),
      this.createSettingDefinition("defaultPenColor", "defaultPenColorDesc", (setting) => {
        setting.addColorPicker((component) => component.setValue(settings.defaultPenColor).onChange(async (value) => {
          this.plugin.noteDrawSettings.defaultPenColor = value;
          await this.plugin.saveSettings();
        }));
      }),
      this.createSettingDefinition("defaultPenWidth", "defaultPenWidthDesc", (setting) => {
        this.addSliderWithValue(setting, {
          value: settings.defaultPenWidth,
          min: MIN_BRUSH_WIDTH,
          max: MAX_BRUSH_WIDTH,
          step: 0.5,
          format: (value) => `${formatSettingNumber(value)} px`,
          onChange: async (value) => {
            this.plugin.noteDrawSettings.defaultPenWidth = value;
            await this.plugin.saveSettings();
          }
        });
      }),
      this.createSettingDefinition("defaultPenOpacity", "defaultPenOpacityDesc", (setting) => {
        this.addSliderWithValue(setting, {
          value: settings.defaultPenOpacity,
          min: 0,
          max: 1,
          step: 0.02,
          format: (value) => `${Math.round(value * 100)}%`,
          onChange: async (value) => {
            this.plugin.noteDrawSettings.defaultPenOpacity = value;
            await this.plugin.saveSettings();
          }
        });
      }),
      this.createSectionDefinition("settingsSectionWatercolor"),
      this.createSettingDefinition("defaultWatercolorColor", "defaultWatercolorColorDesc", (setting) => {
        setting.addColorPicker((component) => component.setValue(settings.defaultWatercolorColor).onChange(async (value) => {
          this.plugin.noteDrawSettings.defaultWatercolorColor = value;
          await this.plugin.saveSettings();
        }));
      }),
      this.createSettingDefinition("defaultWatercolorWidth", "defaultWatercolorWidthDesc", (setting) => {
        this.addSliderWithValue(setting, {
          value: settings.defaultWatercolorWidth,
          min: MIN_BRUSH_WIDTH,
          max: MAX_BRUSH_WIDTH,
          step: 0.5,
          format: (value) => `${formatSettingNumber(value)} px`,
          onChange: async (value) => {
            this.plugin.noteDrawSettings.defaultWatercolorWidth = value;
            await this.plugin.saveSettings();
          }
        });
      }),
      this.createSettingDefinition("defaultWatercolorOpacity", "defaultWatercolorOpacityDesc", (setting) => {
        this.addSliderWithValue(setting, {
          value: settings.defaultWatercolorOpacity,
          min: 0,
          max: 1,
          step: 0.02,
          format: (value) => `${Math.round(value * 100)}%`,
          onChange: async (value) => {
            this.plugin.noteDrawSettings.defaultWatercolorOpacity = value;
            await this.plugin.saveSettings();
          }
        });
      }),
      this.createSettingDefinition("resetBrushDefaults", "resetBrushDefaultsDesc", (setting) => {
        setting.addButton((component) => component.setButtonText(this.plugin.t("reset")).onClick(async () => {
          Object.assign(this.plugin.noteDrawSettings, {
            defaultPenColor: DEFAULT_SETTINGS.defaultPenColor,
            defaultPenWidth: DEFAULT_SETTINGS.defaultPenWidth,
            defaultPenOpacity: DEFAULT_SETTINGS.defaultPenOpacity,
            defaultWatercolorColor: DEFAULT_SETTINGS.defaultWatercolorColor,
            defaultWatercolorWidth: DEFAULT_SETTINGS.defaultWatercolorWidth,
            defaultWatercolorOpacity: DEFAULT_SETTINGS.defaultWatercolorOpacity,
            brushVariantSettings: {}
          });
          await this.plugin.saveSettings();
          this.refreshSettingsView();
        }));
      }),
      this.createSectionDefinition("settingsSectionInteraction"),
      this.createSettingDefinition("defaultSourceEditMarkdown", "defaultSourceEditMarkdownDesc", (setting) => {
        setting.addToggle((component) => component.setValue(settings.defaultSourceEditMarkdown).onChange(async (value) => {
          this.plugin.noteDrawSettings.defaultSourceEditMarkdown = value;
          await this.plugin.saveSettings();
        }));
      }),
      this.createSettingDefinition("longPressMs", "longPressMsDesc", (setting) => {
        this.addSliderWithValue(setting, {
          value: settings.longPressMs,
          min: MIN_LONG_PRESS_MS,
          max: MAX_LONG_PRESS_MS,
          step: 25,
          format: (value) => `${Math.round(value)} ms`,
          onChange: async (value) => {
            this.plugin.noteDrawSettings.longPressMs = value;
            await this.plugin.saveSettings();
          }
        });
      }),
      this.createSettingDefinition("selectTapDistance", "selectTapDistanceDesc", (setting) => {
        this.addSliderWithValue(setting, {
          value: settings.selectTapDistance,
          min: MIN_SELECT_TAP_DISTANCE,
          max: MAX_SELECT_TAP_DISTANCE,
          step: 1,
          format: (value) => `${Math.round(value)} px`,
          onChange: async (value) => {
            this.plugin.noteDrawSettings.selectTapDistance = value;
            await this.plugin.saveSettings();
          }
        });
      }),
      this.createSettingDefinition("selectStrokePadding", "selectStrokePaddingDesc", (setting) => {
        this.addSliderWithValue(setting, {
          value: settings.selectStrokePadding,
          min: MIN_SELECT_STROKE_PADDING,
          max: MAX_SELECT_STROKE_PADDING,
          step: 1,
          format: (value) => `${Math.round(value)} px`,
          onChange: async (value) => {
            this.plugin.noteDrawSettings.selectStrokePadding = value;
            await this.plugin.saveSettings();
          }
        });
      }),
      this.createSettingDefinition("selectedStrokeAlpha", "selectedStrokeAlphaDesc", (setting) => {
        this.addSliderWithValue(setting, {
          value: settings.selectedStrokeAlpha,
          min: MIN_SELECTED_STROKE_ALPHA,
          max: MAX_SELECTED_STROKE_ALPHA,
          step: 0.02,
          format: (value) => `${Math.round(value * 100)}%`,
          onChange: async (value) => {
            this.plugin.noteDrawSettings.selectedStrokeAlpha = value;
            await this.plugin.saveSettings();
          }
        });
      }),
      this.createSectionDefinition("settingsSectionPerformance"),
      this.createSettingDefinition("drawingInterpolationStep", "drawingInterpolationStepDesc", (setting) => {
        this.addSliderWithValue(setting, {
          value: settings.drawingInterpolationStep,
          min: MIN_DRAWING_INTERPOLATION_STEP_PX,
          max: MAX_DRAWING_INTERPOLATION_STEP_PX,
          step: 0.25,
          format: (value) => `${formatSettingNumber(value)} px`,
          onChange: async (value) => {
            this.plugin.noteDrawSettings.drawingInterpolationStep = value;
            await this.plugin.saveSettings();
          }
        });
      }),
      this.createSettingDefinition("drawingMinPointDistance", "drawingMinPointDistanceDesc", (setting) => {
        this.addSliderWithValue(setting, {
          value: settings.drawingMinPointDistance,
          min: MIN_DRAWING_MIN_POINT_DISTANCE_PX,
          max: MAX_DRAWING_MIN_POINT_DISTANCE_PX,
          step: 0.05,
          format: (value) => `${formatSettingNumber(value)} px`,
          onChange: async (value) => {
            this.plugin.noteDrawSettings.drawingMinPointDistance = value;
            await this.plugin.saveSettings();
          }
        });
      }),
      this.createSettingDefinition("drawingCompactDistance", "drawingCompactDistanceDesc", (setting) => {
        this.addSliderWithValue(setting, {
          value: settings.drawingCompactDistance,
          min: MIN_DRAWING_COMPACT_DISTANCE_PX,
          max: MAX_DRAWING_COMPACT_DISTANCE_PX,
          step: 0.1,
          format: (value) => `${formatSettingNumber(value)} px`,
          onChange: async (value) => {
            this.plugin.noteDrawSettings.drawingCompactDistance = value;
            await this.plugin.saveSettings();
          }
        });
      }),
      this.createSettingDefinition("autoSaveDelayMs", "autoSaveDelayMsDesc", (setting) => {
        this.addSliderWithValue(setting, {
          value: settings.autoSaveDelayMs,
          min: MIN_AUTO_SAVE_DELAY_MS,
          max: MAX_AUTO_SAVE_DELAY_MS,
          step: 20,
          format: (value) => `${Math.round(value)} ms`,
          onChange: async (value) => {
            this.plugin.noteDrawSettings.autoSaveDelayMs = value;
            await this.plugin.saveSettings();
          }
        });
      }),
      this.createSectionDefinition("settingsSectionLayout"),
      this.createSettingDefinition("frameCornerRadius", "frameCornerRadiusDesc", (setting) => {
        this.addSliderWithValue(setting, {
          value: settings.frameCornerRadius,
          min: MIN_FRAME_CORNER_RADIUS,
          max: MAX_FRAME_CORNER_RADIUS,
          step: 1,
          format: (value) => `${Math.round(value)} px`,
          onChange: async (value) => {
            this.plugin.noteDrawSettings.frameCornerRadius = value;
            await this.plugin.saveSettings();
          }
        });
      }),
      this.createSettingDefinition("toolbarTopOffset", "toolbarTopOffsetDesc", (setting) => {
        this.addSliderWithValue(setting, {
          value: settings.toolbarTopOffset,
          min: 0,
          max: 48,
          step: 1,
          format: (value) => `${Math.round(value)} px`,
          onChange: async (value) => {
            this.plugin.noteDrawSettings.toolbarTopOffset = value;
            await this.plugin.saveSettings();
          }
        });
      }),
      this.createSettingDefinition("resetLayoutDefaults", "resetLayoutDefaultsDesc", (setting) => {
        setting.addButton((component) => component.setButtonText(this.plugin.t("reset")).onClick(async () => {
          Object.assign(this.plugin.noteDrawSettings, {
            frameCornerRadius: DEFAULT_SETTINGS.frameCornerRadius,
            toolbarTopOffset: DEFAULT_SETTINGS.toolbarTopOffset,
            longPressMs: DEFAULT_SETTINGS.longPressMs,
            selectTapDistance: DEFAULT_SETTINGS.selectTapDistance,
            selectStrokePadding: DEFAULT_SETTINGS.selectStrokePadding,
            selectedStrokeAlpha: DEFAULT_SETTINGS.selectedStrokeAlpha,
            drawingInterpolationStep: DEFAULT_SETTINGS.drawingInterpolationStep,
            drawingMinPointDistance: DEFAULT_SETTINGS.drawingMinPointDistance,
            drawingCompactDistance: DEFAULT_SETTINGS.drawingCompactDistance,
            autoSaveDelayMs: DEFAULT_SETTINGS.autoSaveDelayMs
          });
          await this.plugin.saveSettings();
          this.refreshSettingsView();
        }));
      }),
      this.createSectionDefinition("settingsSectionDiagnostics"),
      this.createSettingDefinition("debugLog", "debugLogDesc", (setting) => {
        setting.addToggle((component) => component.setValue(settings.enableDebugLog).onChange(async (value) => {
          this.plugin.noteDrawSettings.enableDebugLog = value;
          await this.plugin.saveSettings();
        }));
      }),
      this.createSectionDefinition("settingsSectionSupport"),
      this.createCodesDefinition()
    ];
  }
  addSliderWithValue(setting, options) {
    let valueEl = null;
    setting.addSlider((component) => {
      component.setLimits(options.min, options.max, options.step).setValue(options.value).onChange(async (value) => {
        if (valueEl) {
          valueEl.setText(options.format(value));
        }
        await options.onChange(value);
      });
    });
    valueEl = setting.controlEl.createSpan({
      cls: "notedraw-setting-value",
      text: options.format(options.value)
    });
  }
  createSectionDefinition(key) {
    return {
      name: this.plugin.t(key),
      searchable: false,
      render: (setting) => {
        setting.setName(this.plugin.t(key)).setHeading();
        setting.settingEl.addClass("notedraw-settings-section-title");
      }
    };
  }
  createSettingDefinition(nameKey, descKey, renderControl) {
    return {
      name: this.plugin.t(nameKey),
      desc: this.plugin.t(descKey),
      render: (setting) => {
        setting.setName(this.plugin.t(nameKey)).setDesc(this.plugin.t(descKey));
        renderControl(setting);
      }
    };
  }
  createCodesDefinition() {
    return {
      name: this.plugin.t("supportTitle"),
      desc: this.plugin.t("supportSubtitle"),
      render: (setting) => {
        setting.settingEl.empty();
        const codesContainer = setting.settingEl.createDiv({ cls: "notedraw-settings-codes" });
        void this.renderExtraCodes(codesContainer);
      }
    };
  }
  async renderExtraCodes(containerEl) {
    const codeItems = (await Promise.all(
      SETTINGS_EXTRA_CODE_ASSETS.map(async (asset) => {
        const src = await this.plugin.getOptionalAssetResourcePath(asset.path) || asset.dataUrl;
        return src ? { ...asset, src } : null;
      })
    )).filter(Boolean);
    if (!codeItems.length) {
      containerEl.remove();
      return;
    }
    containerEl.createDiv({
      cls: "notedraw-settings-codes-title",
      text: this.plugin.t("supportTitle")
    });
    containerEl.createDiv({
      cls: "notedraw-settings-codes-subtitle",
      text: this.plugin.t("supportSubtitle")
    });
    const gridEl = containerEl.createDiv({ cls: "notedraw-settings-codes-grid" });
    for (const item of codeItems) {
      const label = this.plugin.t(item.labelKey);
      const codeEl = gridEl.createDiv({ cls: "notedraw-settings-code" });
      const imageEl = codeEl.createEl("img", {
        cls: "notedraw-settings-code-image",
        attr: {
          alt: label,
          loading: "lazy",
          src: item.src
        }
      });
      imageEl.src = item.src;
      codeEl.createDiv({
        cls: "notedraw-settings-code-label",
        text: label
      });
    }
  }
};
function isConcreteMarkdownBlockElement(element) {
  if (element?.matches?.(METADATA_PROPERTY_SELECTOR)) {
    return true;
  }
  if (element?.matches?.(".notedraw-md-line-block")) {
    return true;
  }
  if (isMarkdownEmbedBlockElement(element)) {
    return true;
  }
  if (!element?.matches?.(MARKDOWN_TEXT_SELECTOR)) {
    return false;
  }
  if (element.matches(".callout-content")) {
    return false;
  }
  if (element.querySelector?.(".internal-embed, .markdown-embed, .markdown-embed-content")) {
    return false;
  }
  return !element.querySelector?.(MARKDOWN_TEXT_SELECTOR);
}
function isMarkdownBlockCandidateElement(element) {
  if (!element || element.closest?.(NOTEDRAW_OWNED_MUTATION_SELECTOR) || element.closest?.(".frontmatter")) {
    return false;
  }
  if (element.matches?.(METADATA_PROPERTY_SELECTOR)) {
    return true;
  }
  if (isConcreteMarkdownBlockElement(element)) {
    return true;
  }
  return Boolean(
    element.matches?.(NOTE_FLOW_RENDERED_OWNER_SELECTOR) || element.matches?.(NOTE_FLOW_RENDERED_BLOCK_SELECTOR) || element.matches?.("img,video,audio,iframe,hr,figure,table,pre,blockquote,.math-block,.el-img,.el-hr")
  );
}
function markdownBlockCandidateElements(root) {
  if (!root) {
    return [];
  }
  const candidates = [];
  const seen = /* @__PURE__ */ new Set();
  for (const element of root.querySelectorAll?.(".notedraw-md-line-block") || []) {
    if (!isMarkdownBlockCandidateElement(element) || seen.has(element)) {
      continue;
    }
    seen.add(element);
    candidates.push(element);
  }
  for (const element of collectNoteFlowMarkdownOwners(root)) {
    if (!isMarkdownBlockCandidateElement(element) || seen.has(element)) {
      continue;
    }
    if (element.querySelector?.(".notedraw-md-line-block")) {
      continue;
    }
    seen.add(element);
    candidates.push(element);
  }
  for (const element of [
    ...Array.from(root.querySelectorAll?.(EDITABLE_SELECTOR) || []),
    ...Array.from(root.querySelectorAll?.(MARKDOWN_EMBED_SELECTOR) || []),
    ...Array.from(root.querySelectorAll?.(NOTE_FLOW_RENDERED_BLOCK_SELECTOR) || [])
  ]) {
    const owner = findNoteFlowMarkdownBlockElement(element, root) || element;
    if (!isMarkdownBlockCandidateElement(owner) || seen.has(owner)) {
      continue;
    }
    seen.add(owner);
    candidates.push(owner);
  }
  return candidates;
}
function markdownBlockCandidateElementForTarget(target, root) {
  if (!target || !root?.contains?.(target)) {
    return null;
  }
  const metadataProperty = target.closest?.(METADATA_PROPERTY_SELECTOR);
  const explicitLine = target.closest?.(".notedraw-md-line-block");
  const marked = target.closest?.("[data-note-draw-markdown-block-id]");
  const owner = findNoteFlowMarkdownBlockElement(target, root);
  const preciselyMapped = target.closest?.("[data-note-draw-line-mapped='true']");
  const mappedChild = Array.from(target.querySelectorAll?.("[data-note-draw-line-mapped='true']") || []).find((element) => {
    return isMarkdownBlockCandidateElement(element);
  });
  for (const candidate of [metadataProperty, explicitLine, marked, owner, preciselyMapped, mappedChild]) {
    if (candidate && root.contains(candidate) && isMarkdownBlockCandidateElement(candidate)) {
      return candidate;
    }
  }
  return isMarkdownBlockCandidateElement(target) ? target : null;
}
function isMarkdownEmbedBlockElement(element) {
  if (!element?.matches?.(MARKDOWN_EMBED_SELECTOR)) {
    return false;
  }
  const nestedEmbed = element.parentElement?.closest?.(MARKDOWN_EMBED_SELECTOR);
  if (nestedEmbed && nestedEmbed.matches?.(".internal-embed, .markdown-embed")) {
    return false;
  }
  return Boolean(
    element.matches?.(".internal-embed, .markdown-embed") || !element.parentElement?.closest?.(".internal-embed, .markdown-embed")
  );
}
function findMarkdownEmbedBlockElement(target, previewEl = null) {
  const embed = target?.matches?.(MARKDOWN_EMBED_SELECTOR) ? target : target?.closest?.(MARKDOWN_EMBED_SELECTOR) || target?.querySelector?.(MARKDOWN_EMBED_SELECTOR);
  if (!embed || previewEl && !previewEl.contains?.(embed)) {
    return null;
  }
  const owner = embed.matches?.(".internal-embed") ? embed : embed.closest?.(".internal-embed") || (embed.matches?.(".markdown-embed") ? embed : embed.closest?.(".markdown-embed")) || embed;
  return !previewEl || previewEl.contains?.(owner) ? owner : null;
}
function findNoteFlowInlineMeasureElement(element) {
  if (!element) {
    return null;
  }
  if (isConcreteMarkdownBlockElement(element)) {
    return element;
  }
  const inlineOwner = element.matches?.(".el-p,.el-h1,.el-h2,.el-h3,.el-h4,.el-h5,.el-h6,li") ? element : null;
  if (!inlineOwner) {
    return element;
  }
  return Array.from(inlineOwner.querySelectorAll?.(MARKDOWN_TEXT_SELECTOR) || []).find((candidate) => isConcreteMarkdownBlockElement(candidate)) || element;
}
function isNoteFlowCollectionBlock(element) {
  return Boolean(element?.matches?.("ul,ol,.el-ul,.el-ol,.markdown-preview-section,.markdown-preview-sizer,.markdown-rendered,.callout-content"));
}
function findNoteFlowMarkdownBlockElement(element, previewEl = null) {
  if (!element || previewEl && !previewEl.contains?.(element)) {
    return null;
  }
  const explicitLine = element.matches?.(".notedraw-md-line-block") ? element : element.closest?.(".notedraw-md-line-block");
  if (explicitLine && (!previewEl || previewEl.contains(explicitLine))) {
    return explicitLine;
  }
  let listItem = element.closest?.("li");
  while (listItem?.parentElement) {
    const parentItem = listItem.parentElement.closest?.("li");
    if (!parentItem || previewEl && !previewEl.contains(parentItem)) {
      break;
    }
    listItem = parentItem;
  }
  if (listItem && (!previewEl || previewEl.contains(listItem))) {
    return listItem;
  }
  const embedBlock = findMarkdownEmbedBlockElement(element, previewEl);
  if (embedBlock) {
    return embedBlock;
  }
  const calloutBlock = element.closest?.(".callout");
  if (calloutBlock && (!previewEl || previewEl.contains(calloutBlock))) {
    return calloutBlock;
  }
  const quoteBlock = element.closest?.(".el-blockquote") || element.closest?.("blockquote");
  if (quoteBlock && (!previewEl || previewEl.contains(quoteBlock))) {
    return quoteBlock;
  }
  const tableBlock = element.closest?.(".el-table") || element.closest?.("table");
  if (tableBlock && (!previewEl || previewEl.contains(tableBlock))) {
    return tableBlock;
  }
  const renderedOwner = element.matches?.(NOTE_FLOW_RENDERED_OWNER_SELECTOR) ? element : element.closest?.(NOTE_FLOW_RENDERED_OWNER_SELECTOR);
  if (renderedOwner && (!previewEl || previewEl.contains(renderedOwner))) {
    return renderedOwner;
  }
  const renderedBlock = element.matches?.(NOTE_FLOW_RENDERED_BLOCK_SELECTOR) ? element : element.closest?.(NOTE_FLOW_RENDERED_BLOCK_SELECTOR);
  if (renderedBlock && (!previewEl || previewEl.contains(renderedBlock))) {
    return renderedBlock;
  }
  const markdownBlock = element.matches?.(MARKDOWN_TEXT_SELECTOR) ? element : element.closest?.(MARKDOWN_TEXT_SELECTOR);
  return markdownBlock && (!previewEl || previewEl.contains(markdownBlock)) ? markdownBlock : element;
}
function findEditableTarget(target, previewEl, clientPoint = null) {
  if (!target || !previewEl.contains(target)) {
    return null;
  }
  const controller = previewEl?._noteDrawController;
  if (controller?.surfaceType === "webview") {
    return findWebviewEditableTarget(target, previewEl);
  }
  const metadataProperty = target.closest?.(METADATA_PROPERTY_SELECTOR);
  if (metadataProperty && previewEl.contains(metadataProperty)) {
    return metadataProperty;
  }
  if (target.closest(BLOCKED_EDIT_SELECTOR)) {
    return null;
  }
  const embeddedBlock = findMarkdownEmbedBlockElement(target, previewEl);
  if (embeddedBlock) {
    const embeddedEditable = target.closest?.(MARKDOWN_TEXT_SELECTOR) || embeddedBlock.querySelector?.(MARKDOWN_TEXT_SELECTOR);
    if (!embeddedEditable || !normalizeRenderedText2(embeddedEditable.innerText)) {
      return null;
    }
    if (clientPoint && controller?.markdownElementContainsClientPoint && !controller.markdownElementContainsClientPoint(embeddedBlock, clientPoint)) {
      return null;
    }
    return embeddedEditable;
  }
  const editable = target.closest(EDITABLE_SELECTOR);
  if (!editable || !previewEl.contains(editable)) {
    return null;
  }
  if (!isConcreteMarkdownBlockElement(editable)) {
    return null;
  }
  if (!normalizeRenderedText2(editable.innerText)) {
    return null;
  }
  if (clientPoint && controller?.markdownElementContainsClientPoint && !controller.markdownElementContainsClientPoint(editable, clientPoint)) {
    return null;
  }
  return editable;
}
function findWebviewEditableTarget(target, previewEl) {
  if (!target || !previewEl.contains(target) || target.closest(WEBVIEW_BLOCKED_EDIT_SELECTOR)) {
    return null;
  }
  let current = target.closest(WEBVIEW_EDITABLE_SELECTOR);
  while (current && current !== previewEl) {
    if (!current.closest(WEBVIEW_BLOCKED_EDIT_SELECTOR) && normalizeRenderedText2(current.innerText)) {
      return current;
    }
    current = current.parentElement?.closest?.(WEBVIEW_EDITABLE_SELECTOR);
  }
  return null;
}
function sanitizeSettings(settings) {
  const input = settings || {};
  return {
    language: normalizeLanguageCode(input.language ?? DEFAULT_SETTINGS.language),
    drawingStorageMode: normalizeDrawingStorageMode(input.drawingStorageMode ?? DEFAULT_SETTINGS.drawingStorageMode),
    defaultPenColor: isCssColor(input.defaultPenColor) ? input.defaultPenColor : DEFAULT_SETTINGS.defaultPenColor,
    defaultPenWidth: clamp10(Number(input.defaultPenWidth ?? DEFAULT_SETTINGS.defaultPenWidth), MIN_BRUSH_WIDTH, MAX_BRUSH_WIDTH),
    defaultPenOpacity: clamp10(Number(input.defaultPenOpacity ?? DEFAULT_SETTINGS.defaultPenOpacity), 0, 1),
    defaultWatercolorColor: isCssColor(input.defaultWatercolorColor) ? input.defaultWatercolorColor : DEFAULT_SETTINGS.defaultWatercolorColor,
    defaultWatercolorWidth: clamp10(Number(input.defaultWatercolorWidth ?? DEFAULT_SETTINGS.defaultWatercolorWidth), MIN_BRUSH_WIDTH, MAX_BRUSH_WIDTH),
    defaultWatercolorOpacity: clamp10(Number(input.defaultWatercolorOpacity ?? DEFAULT_SETTINGS.defaultWatercolorOpacity), 0, 1),
    brushVariantSettings: createBrushVariantSettings(input.brushVariantSettings, {
      [BRUSH_PEN]: {
        color: isCssColor(input.defaultPenColor) ? input.defaultPenColor : DEFAULT_SETTINGS.defaultPenColor,
        width: clamp10(Number(input.defaultPenWidth ?? DEFAULT_SETTINGS.defaultPenWidth), MIN_BRUSH_WIDTH, MAX_BRUSH_WIDTH),
        opacity: clamp10(Number(input.defaultPenOpacity ?? DEFAULT_SETTINGS.defaultPenOpacity), 0, 1),
        count: 1
      },
      [BRUSH_WATERCOLOR]: {
        color: isCssColor(input.defaultWatercolorColor) ? input.defaultWatercolorColor : DEFAULT_SETTINGS.defaultWatercolorColor,
        width: clamp10(Number(input.defaultWatercolorWidth ?? DEFAULT_SETTINGS.defaultWatercolorWidth), MIN_BRUSH_WIDTH, MAX_BRUSH_WIDTH),
        opacity: clamp10(Number(input.defaultWatercolorOpacity ?? DEFAULT_SETTINGS.defaultWatercolorOpacity), 0, 1),
        count: 1
      }
    }),
    lastToolMode: normalizeToolMode(input.lastToolMode),
    lastBrushMode: input.lastBrushMode === BRUSH_WATERCOLOR ? BRUSH_WATERCOLOR : BRUSH_PEN,
    lastPenVariant: normalizeBrushVariant(BRUSH_PEN, input.lastPenVariant),
    lastWatercolorVariant: normalizeBrushVariant(BRUSH_WATERCOLOR, input.lastWatercolorVariant),
    lastTextPreset: normalizeTextPreset(input.lastTextPreset),
    lastReadingZoom: clamp10(Number(input.lastReadingZoom ?? DEFAULT_SETTINGS.lastReadingZoom), MIN_READING_ZOOM, MAX_READING_ZOOM),
    mindMapAffectsSource: input.mindMapAffectsSource !== false,
    defaultSourceEditMarkdown: input.defaultSourceEditMarkdown !== false,
    frameCornerRadius: clamp10(Number(input.frameCornerRadius ?? DEFAULT_SETTINGS.frameCornerRadius), MIN_FRAME_CORNER_RADIUS, MAX_FRAME_CORNER_RADIUS),
    toolbarTopOffset: clamp10(Number(input.toolbarTopOffset ?? DEFAULT_SETTINGS.toolbarTopOffset), 0, 48),
    toolbarPosition: normalizeToolbarPosition(input.toolbarPosition),
    longPressMs: clamp10(Number(input.longPressMs ?? DEFAULT_SETTINGS.longPressMs), MIN_LONG_PRESS_MS, MAX_LONG_PRESS_MS),
    selectTapDistance: clamp10(Number(input.selectTapDistance ?? DEFAULT_SETTINGS.selectTapDistance), MIN_SELECT_TAP_DISTANCE, MAX_SELECT_TAP_DISTANCE),
    selectStrokePadding: clamp10(Number(input.selectStrokePadding ?? DEFAULT_SETTINGS.selectStrokePadding), MIN_SELECT_STROKE_PADDING, MAX_SELECT_STROKE_PADDING),
    selectedStrokeAlpha: clamp10(Number(input.selectedStrokeAlpha ?? DEFAULT_SETTINGS.selectedStrokeAlpha), MIN_SELECTED_STROKE_ALPHA, MAX_SELECTED_STROKE_ALPHA),
    drawingInterpolationStep: clamp10(Number(input.drawingInterpolationStep ?? DEFAULT_SETTINGS.drawingInterpolationStep), MIN_DRAWING_INTERPOLATION_STEP_PX, MAX_DRAWING_INTERPOLATION_STEP_PX),
    drawingMinPointDistance: clamp10(Number(input.drawingMinPointDistance ?? DEFAULT_SETTINGS.drawingMinPointDistance), MIN_DRAWING_MIN_POINT_DISTANCE_PX, MAX_DRAWING_MIN_POINT_DISTANCE_PX),
    drawingCompactDistance: clamp10(Number(input.drawingCompactDistance ?? DEFAULT_SETTINGS.drawingCompactDistance), MIN_DRAWING_COMPACT_DISTANCE_PX, MAX_DRAWING_COMPACT_DISTANCE_PX),
    autoSaveDelayMs: clamp10(Number(input.autoSaveDelayMs ?? DEFAULT_SETTINGS.autoSaveDelayMs), MIN_AUTO_SAVE_DELAY_MS, MAX_AUTO_SAVE_DELAY_MS),
    enableDebugLog: Boolean(input.enableDebugLog)
  };
}
function normalizeToolbarPosition(value) {
  if (!value || !Number.isFinite(Number(value.xRatio)) || !Number.isFinite(Number(value.yRatio))) {
    return null;
  }
  return {
    xRatio: clamp10(Number(value.xRatio), 0, 1),
    yRatio: clamp10(Number(value.yRatio), 0, 1)
  };
}
function translateNoteDraw(plugin, key, vars = {}) {
  const language = resolveNoteDrawLanguage(plugin);
  const template = I18N[language]?.[key] ?? I18N.en[key] ?? key;
  return String(template).replace(/\{(\w+)\}/g, (_, name) => vars?.[name] ?? "");
}
function formatSettingNumber(value) {
  return String(Math.round(Number(value || 0) * 100) / 100);
}
function resolveNoteDrawLanguage(plugin) {
  const language = normalizeLanguageCode(plugin?.noteDrawSettings?.language ?? DEFAULT_SETTINGS.language);
  if (language !== LANGUAGE_AUTO) {
    return language;
  }
  return detectNoteDrawLanguage(plugin?.app);
}
function detectNoteDrawLanguage(app) {
  const navigatorLanguage = typeof navigator !== "undefined" ? navigator.language : "";
  const navigatorLanguages = typeof navigator !== "undefined" ? Array.from(navigator.languages ?? []) : [];
  const candidates = [
    app?.vault?.getConfig?.("language"),
    app?.vault?.getConfig?.("locale"),
    activeDocument.documentElement.lang,
    navigatorLanguage,
    ...navigatorLanguages,
    app?.appId
  ];
  for (const candidate of candidates) {
    const language = normalizeLanguageCode(candidate, false);
    if (language && language !== LANGUAGE_AUTO) {
      return language;
    }
  }
  return "en";
}
function normalizeLanguageCode(value, allowAuto = true) {
  const text = String(value || "").trim();
  if (!text) {
    return allowAuto ? LANGUAGE_AUTO : "";
  }
  const lower = text.replace("_", "-").toLowerCase();
  if (allowAuto && lower === LANGUAGE_AUTO) {
    return LANGUAGE_AUTO;
  }
  if (lower.startsWith("zh-tw") || lower.startsWith("zh-hk") || lower.includes("hant")) {
    return "zh-TW";
  }
  if (lower.startsWith("zh")) {
    return "zh";
  }
  if (lower.startsWith("ug") || lower.startsWith("uig")) {
    return "ug";
  }
  const primary = lower.split("-")[0];
  return I18N[primary] ? primary : allowAuto ? LANGUAGE_AUTO : "";
}
function languageNativeName(language) {
  return LANGUAGE_OPTIONS.find((option) => option.value === language)?.label || LANGUAGE_OPTIONS.find((option) => option.value === "en")?.label || "English";
}
function setNoteDrawCssProps(element, props) {
  if (typeof element?.setCssProps === "function") {
    element.setCssProps(props);
  }
}
function createNoteDrawControlElement(parent, cls) {
  const element = parent.createDiv({ cls });
  if (parent === activeDocument.body) {
    element.addClass("notedraw-body-control");
  }
  return element;
}
function bindNoteDrawControlTap(element, action) {
  let touchPointer = null;
  let suppressClickUntil = 0;
  element.addEventListener("pointerdown", (event) => {
    if (event.button !== void 0 && event.button !== 0) {
      return;
    }
    event.stopPropagation();
    if (event.pointerType && event.pointerType !== "mouse") {
      touchPointer = {
        id: event.pointerId,
        x: event.clientX,
        y: event.clientY
      };
      try {
        element.setPointerCapture?.(event.pointerId);
      } catch (error) {
        void error;
      }
    }
  });
  element.addEventListener("pointerup", (event) => {
    if (!touchPointer || event.pointerId !== touchPointer.id) {
      return;
    }
    const moved = pointerDistance(touchPointer, { x: event.clientX, y: event.clientY });
    touchPointer = null;
    if (moved > 12) {
      return;
    }
    suppressClickUntil = Date.now() + 700;
    try {
      element.releasePointerCapture?.(event.pointerId);
    } catch (error) {
      void error;
    }
    event.preventDefault();
    event.stopPropagation();
    action(event);
  });
  element.addEventListener("pointercancel", (event) => {
    touchPointer = null;
    try {
      element.releasePointerCapture?.(event.pointerId);
    } catch (error) {
      void error;
    }
  });
  element.addEventListener("click", (event) => {
    event.preventDefault();
    event.stopPropagation();
    if (Date.now() <= suppressClickUntil) {
      suppressClickUntil = 0;
      return;
    }
    action(event);
  });
}
function shouldUseBodyFloatingControls(previewEl, surfaceType) {
  if (surfaceType !== "preview") {
    return false;
  }
  return isMobileRuntime() && Boolean(previewEl.closest?.(".markdown-preview-view"));
}
function isMobileRuntime() {
  return Boolean(import_obsidian.Platform.isMobileApp || activeDocument?.body?.classList?.contains("is-mobile"));
}
function isAppleMobileRuntime() {
  return Boolean(import_obsidian.Platform.isIosApp);
}
function isAppleTouchEvent(event) {
  if (!isAppleMobileRuntime()) {
    return false;
  }
  return event?.type?.startsWith?.("touch") || event?.pointerType === "touch";
}
function setAccessibleLabel(element, label) {
  if (!element || !label) {
    return;
  }
  element.setAttribute("title", label);
  element.setAttribute("aria-label", label);
}
function isCssColor(value) {
  return typeof value === "string" && /^#[0-9a-fA-F]{6}$/.test(value);
}
function isPaletteColor(value) {
  return value === NO_COLOR || isCssColor(value);
}
function strokePaletteColor(stroke, fallback) {
  return stroke?.color === NO_COLOR ? NO_COLOR : stroke?.color || fallback;
}
function contrastTextColor(hexColor) {
  if (!isCssColor(hexColor)) {
    return "#111827";
  }
  const red = parseInt(hexColor.slice(1, 3), 16);
  const green = parseInt(hexColor.slice(3, 5), 16);
  const blue = parseInt(hexColor.slice(5, 7), 16);
  const luminance = (0.2126 * red + 0.7152 * green + 0.0722 * blue) / 255;
  return luminance > 0.58 ? "#111827" : "#ffffff";
}
function brushWidthToPaletteSlider(value) {
  const width = clamp10(Number(value) || MIN_BRUSH_WIDTH, MIN_BRUSH_WIDTH, MAX_BRUSH_WIDTH);
  const ratio = Math.log(width / MIN_BRUSH_WIDTH) / Math.log(MAX_BRUSH_WIDTH / MIN_BRUSH_WIDTH);
  return Math.round(clamp10(ratio, 0, 1) * 1e3);
}
function paletteSliderToBrushWidth(value) {
  const ratio = clamp10(Number(value) / 1e3, 0, 1);
  const width = MIN_BRUSH_WIDTH * Math.pow(MAX_BRUSH_WIDTH / MIN_BRUSH_WIDTH, ratio);
  return Math.round(width * 100) / 100;
}
function paletteBrushWidthToElementScale(width) {
  const ratio = brushWidthToPaletteSlider(width) / 1e3;
  const defaultRatio = brushWidthToPaletteSlider(DEFAULT_SETTINGS.defaultPenWidth) / 1e3;
  return ratio <= defaultRatio ? clamp10(0.5 + 0.5 * ratio / Math.max(1e-3, defaultRatio), 0.5, 1) : clamp10(1 + 2 * (ratio - defaultRatio) / Math.max(1e-3, 1 - defaultRatio), 1, 3);
}
function elementScaleToPaletteBrushWidth(scale) {
  const nextScale = clamp10(Number(scale), 0.5, 3);
  const defaultRatio = brushWidthToPaletteSlider(DEFAULT_SETTINGS.defaultPenWidth) / 1e3;
  const ratio = nextScale <= 1 ? (nextScale - 0.5) / 0.5 * defaultRatio : defaultRatio + (nextScale - 1) / 2 * (1 - defaultRatio);
  return paletteSliderToBrushWidth(clamp10(ratio, 0, 1) * 1e3);
}
function opacityToPaletteSlider(value) {
  return Math.round(Math.sqrt(clamp10(Number(value), 0, 1)) * 1e3);
}
function paletteSliderToOpacity(value) {
  const normalized = clamp10(Number(value) / 1e3, 0, 1);
  return Math.round(normalized * normalized * 1e4) / 1e4;
}
function isSourceTextTarget(target, previewEl) {
  if (!target || !previewEl?.contains(target)) {
    return false;
  }
  if (target.closest(BLOCKED_EDIT_SELECTOR)) {
    return false;
  }
  return Boolean(
    target.closest?.(".cm-line, .cm-content, .cm-activeLine") || target.classList?.contains("cm-line") || target.classList?.contains("cm-content")
  );
}
function normalizeRenderedText2(value) {
  return String(value || "").replace(/\u00a0/g, " ").replace(/\r\n/g, "\n").split("\n").map((line) => line.trim()).filter(Boolean).join("\n").trim();
}
function normalizeEditableSourceText(value) {
  return String(value || "").replace(/\u00a0/g, " ").replace(/\r\n/g, "\n").replace(/[ \t]+\n/g, "\n").replace(/\n{3,}/g, "\n\n").trim();
}
function placeCaretInEditable(element, clientPoint) {
  const selection = window.getSelection?.();
  if (!selection) {
    return;
  }
  const range = rangeFromClientPoint(element, clientPoint) || nearestTextRangeFromPoint(element, clientPoint) || rangeAtEditableEnd(element);
  if (!range) {
    return;
  }
  selection.removeAllRanges();
  selection.addRange(range);
}
function rangeFromClientPoint(element, clientPoint) {
  if (!clientPoint || !Number.isFinite(clientPoint.x) || !Number.isFinite(clientPoint.y)) {
    return null;
  }
  let range = null;
  const overlay = element.closest(".notedraw-shell")?.querySelector(".notedraw-canvas");
  const previousPointerEvents = overlay?.style.pointerEvents;
  try {
    if (overlay) {
      applyElementStyles(overlay, { pointerEvents: "none" });
    }
    if (typeof activeDocument.caretPositionFromPoint === "function") {
      const position = activeDocument.caretPositionFromPoint(clientPoint.x, clientPoint.y);
      if (position?.offsetNode) {
        range = activeDocument.createRange();
        range.setStart(position.offsetNode, position.offset);
        range.collapse(true);
      }
    } else {
      const legacyCaretRangeFromPoint = activeDocument[["caret", "Range", "From", "Point"].join("")];
      if (typeof legacyCaretRangeFromPoint === "function") {
        range = legacyCaretRangeFromPoint.call(activeDocument, clientPoint.x, clientPoint.y);
      }
    }
  } finally {
    if (overlay) {
      applyElementStyles(overlay, { pointerEvents: previousPointerEvents || "" });
    }
  }
  if (!range || !element.contains(range.startContainer)) {
    return null;
  }
  range.collapse(true);
  return range;
}
function nearestTextRangeFromPoint(element, clientPoint) {
  const walker = activeDocument.createTreeWalker(
    element,
    NodeFilter.SHOW_TEXT,
    {
      acceptNode(node2) {
        return node2.nodeValue && node2.nodeValue.trim() ? NodeFilter.FILTER_ACCEPT : NodeFilter.FILTER_REJECT;
      }
    }
  );
  let best = null;
  let bestScore = Number.POSITIVE_INFINITY;
  let node = walker.nextNode();
  while (node) {
    const value = node.nodeValue || "";
    for (let offset = 0; offset < value.length; offset += 1) {
      const charRange = activeDocument.createRange();
      charRange.setStart(node, offset);
      charRange.setEnd(node, offset + 1);
      for (const rect of Array.from(charRange.getClientRects())) {
        if (!isUsableRect(rect)) {
          continue;
        }
        const score = scoreRectDistance(rect, clientPoint);
        if (score < bestScore) {
          const caretOffset = clientPoint.x > rect.left + rect.width / 2 ? offset + 1 : offset;
          const caretRange = activeDocument.createRange();
          caretRange.setStart(node, caretOffset);
          caretRange.collapse(true);
          best = caretRange;
          bestScore = score;
        }
      }
    }
    node = walker.nextNode();
  }
  return best;
}
function isUsableRect(rect) {
  return rect && Number.isFinite(rect.left) && Number.isFinite(rect.top) && rect.width > 0 && rect.height > 0;
}
function rangeLineRect(range) {
  if (!range) {
    return null;
  }
  const rects = Array.from(range.getClientRects?.() || []).filter(isUsableRect);
  if (rects.length) {
    return rects[rects.length - 1];
  }
  const rect = range.getBoundingClientRect?.();
  if (rect && Number.isFinite(rect.left) && Number.isFinite(rect.top) && rect.height > 0) {
    return rect;
  }
  return null;
}
function scoreRectDistance(rect, point) {
  const xDistance = point.x < rect.left ? rect.left - point.x : point.x > rect.right ? point.x - rect.right : 0;
  const centerY = rect.top + rect.height / 2;
  const sameLineBonus = point.y >= rect.top - 2 && point.y <= rect.bottom + 2 ? 0 : 1e5;
  const linePenalty = Math.abs(point.y - centerY) * 200;
  const inlinePenalty = xDistance;
  return sameLineBonus + linePenalty + inlinePenalty;
}
function rangeAtEditableEnd(element) {
  const range = activeDocument.createRange();
  range.selectNodeContents(element);
  range.collapse(false);
  return range;
}
function applyInlineFormatStyles(element, styles = {}) {
  const safeStyles = {};
  if (styles.color && isCssColor(styles.color)) {
    safeStyles.color = styles.color;
  }
  if (styles.backgroundColor && isCssColor(styles.backgroundColor)) {
    safeStyles.backgroundColor = styles.backgroundColor;
  }
  if (styles.fontSize && isSafeCssSize(styles.fontSize)) {
    safeStyles.fontSize = styles.fontSize;
  }
  if (Object.keys(safeStyles).length) {
    applyElementStyles(element, safeStyles);
  }
}
function selectNodeContents(node) {
  const selection = window.getSelection?.();
  if (!selection) {
    return;
  }
  const range = activeDocument.createRange();
  range.selectNodeContents(node);
  selection.removeAllRanges();
  selection.addRange(range);
}
function serializeEditableSource(element) {
  return stripGeneratedTerminalBreaks(serializeEditableChildren(element)).replace(/\u00a0/g, " ").replace(/[ \t]+\n/g, "\n").replace(/\n{3,}/g, "\n\n").trim();
}
function isClearableInlineFormattingElement(element) {
  const tag = String(element?.tagName || "").toUpperCase();
  return ["A", "B", "CODE", "DEL", "EM", "I", "KBD", "MARK", "S", "SMALL", "SPAN", "STRONG", "SUB", "SUP", "U"].includes(tag);
}
function serializeControllerEditableSource(element, embeddedSurface = false, cleanupTerminalBreak = false) {
  const source = String(element?.tagName || "").toLowerCase() === "pre" ? String(element.querySelector?.("code")?.textContent ?? element.textContent ?? "").replace(/\n+$/g, "") : serializeEditableSource(element);
  return cleanupTerminalBreak ? stripOneTerminalBreakPerLine(source) : source;
}
function stripOneTerminalBreakPerLine(value) {
  return String(value || "").replace(/<br\s*\/?>[ \t]*(?=\n|$)/gim, "").trim();
}
function stripGeneratedTerminalBreaks(value) {
  return String(value || "").replace(/(?:<br>\s*)+$/gi, "");
}
function serializeEditableChildren(node) {
  return Array.from(node.childNodes || []).map((child) => serializeEditableNode(child)).join("");
}
function serializeEditableNode(node) {
  if (node.nodeType === Node.TEXT_NODE) {
    return node.nodeValue || "";
  }
  if (node.nodeType !== Node.ELEMENT_NODE) {
    return "";
  }
  const element = node;
  const tag = element.tagName.toLowerCase();
  if (tag === "br") {
    return "<br>";
  }
  if (tag === "pre") {
    const code = element.querySelector("code");
    const text = code ? code.textContent || "" : element.textContent || "";
    return `
\`\`\`
${text.replace(/\n+$/g, "")}
\`\`\`
`;
  }
  let inner = serializeEditableChildren(element);
  if (["div", "p", "h1", "h2", "h3", "h4", "h5", "h6", "li", "blockquote", "td", "th", "figcaption", "caption"].includes(tag)) {
    inner = stripGeneratedTerminalBreaks(inner);
  }
  if (!inner && !["span", "mark"].includes(tag)) {
    return "";
  }
  if (tag === "strong" || tag === "b") {
    return wrapInlineMarkdown(inner, "**");
  }
  if (tag === "em" || tag === "i") {
    return wrapInlineMarkdown(inner, "*");
  }
  if (tag === "u") {
    return `<u>${inner}</u>`;
  }
  if (tag === "code") {
    return inlineCodeMarkdown(element.textContent || inner);
  }
  if (tag === "mark") {
    const background = normalizeCssColor(element.style.backgroundColor);
    return background ? `<mark style="background-color: ${background};">${inner}</mark>` : `==${inner}==`;
  }
  if (tag === "span") {
    const styleText = serializeInlineStyle(element);
    return styleText ? `<span style="${styleText}">${inner}</span>` : inner;
  }
  if (tag === "kbd" || tag === "sup" || tag === "sub" || tag === "small") {
    return `<${tag}>${inner}</${tag}>`;
  }
  if (tag === "div" || tag === "p") {
    return `${inner.replace(/<br>$/, "")}
`;
  }
  if (tag === "li") {
    return inner;
  }
  return inner;
}
function wrapInlineMarkdown(text, marker) {
  const value = String(text || "");
  return value.trim() ? `${marker}${value}${marker}` : value;
}
function inlineCodeMarkdown(text) {
  const value = String(text || "");
  const longest = Math.max(0, ...Array.from(value.matchAll(/`+/g)).map((match) => match[0].length));
  const fence = "`".repeat(longest + 1 || 1);
  return `${fence}${value}${fence}`;
}
function serializeInlineStyle(element) {
  const styles = [];
  const color = normalizeCssColor(element.style.color);
  const background = normalizeCssColor(element.style.backgroundColor);
  const fontSize = isSafeCssSize(element.style.fontSize) ? element.style.fontSize : "";
  const fontFamily = /monospace/i.test(element.style.fontFamily || "") ? "monospace" : "";
  if (color) {
    styles.push(`color: ${color}`);
  }
  if (background) {
    styles.push(`background-color: ${background}`);
  }
  if (fontSize) {
    styles.push(`font-size: ${fontSize}`);
  }
  if (fontFamily) {
    styles.push(`font-family: ${fontFamily}`);
  }
  return styles.join("; ");
}
function normalizeCssColor(value) {
  const text = String(value || "").trim();
  if (isCssColor(text)) {
    return text;
  }
  const rgb = text.match(/^rgba?\((\d+),\s*(\d+),\s*(\d+)/i);
  if (!rgb) {
    return "";
  }
  const toHex = (part) => clamp10(Math.round(Number(part)), 0, 255).toString(16).padStart(2, "0");
  return `#${toHex(rgb[1])}${toHex(rgb[2])}${toHex(rgb[3])}`;
}
function isSafeCssSize(value) {
  return /^(0?\.?\d+|\d+(?:\.\d+)?)(em|rem|px|%)$/i.test(String(value || "").trim());
}
function normalizeMarkdownBlock(value) {
  let text = String(value || "").trim();
  text = text.replace(/^\s*(`{3,}|~{3,})[^\r\n]*\r?\n([\s\S]*?)\r?\n\s*\1\s*$/, "$2");
  text = text.replace(/<br\s*\/?>/gi, "\n").replace(/<\/(p|div|li|h[1-6])>/gi, "\n").replace(/<pre[^>]*><code[^>]*>([\s\S]*?)<\/code><\/pre>/gi, "$1").replace(/<\/?(span|u|mark|kbd|sup|sub|small|strong|b|em|i|code)[^>]*>/gi, "").replace(/<[^>]+>/g, "");
  text = text.replace(/^\s*[-*+]\s+\[[ xX]\]\s+/gm, "").replace(/^#{1,6}\s+/gm, "").replace(/^\s{0,3}>\s?/gm, "").replace(/^\s*[-*+]\s+/gm, "").replace(/^\s*\d+[.)]\s+/gm, "").replace(/\*\*([^*]+)\*\*/g, "$1").replace(/\*([^*]+)\*/g, "$1").replace(/__([^_]+)__/g, "$1").replace(/_([^_]+)_/g, "$1").replace(/==([^=]+)==/g, "$1").replace(/`([^`]+)`/g, "$1").replace(/\[\[([^\]|]+)\|([^\]]+)\]\]/g, "$2").replace(/\[\[([^\]]+)\]\]/g, "$1").replace(/\[([^\]]+)\]\([^)]+\)/g, "$1");
  return normalizeRenderedText2(text);
}
function collapseRunawayBlankLines(source, trackPosition = -1) {
  let out = "";
  let position = trackPosition;
  let inFence = false;
  let i = 0;
  const n = source.length;
  let lineStart = 0;
  while (i < n) {
    const ch = source[i];
    const beforeIsBlank = source.slice(lineStart, i).trim() === "";
    if (beforeIsBlank && (ch === "`" || ch === "~")) {
      let j = i;
      while (j < n && source[j] === ch) j += 1;
      if (j - i >= 3) {
        inFence = !inFence;
        out += source.slice(i, j);
        i = j;
        continue;
      }
    }
    if (!inFence && ch === "\n") {
      let j = i;
      while (j < n && source[j] === "\n") j += 1;
      const count = j - i;
      if (count > 2) {
        const removed = count - 2;
        if (position >= j) {
          position -= removed;
        } else if (position > i) {
          position = Math.min(i + 2, position);
        }
        out += "\n\n";
      } else {
        out += "\n".repeat(count);
      }
      i = j;
      lineStart = i;
      continue;
    }
    out += ch;
    if (ch === "\n") {
      lineStart = i + 1;
    }
    i += 1;
  }
  return { text: out, position };
}
function collectMarkdownBlocks(source) {
  const blocks = [];
  const lines = source.split(/(\r?\n)/);
  let offset = 0;
  let lineNumber = 0;
  let start = 0;
  let startLine = 0;
  let buffer = "";
  let inFence = false;
  for (let i = 0; i < lines.length; i += 2) {
    const line = lines[i] || "";
    const newline = lines[i + 1] || "";
    const fullLine = line + newline;
    const trimmed = line.trim();
    const lineStart = offset;
    const currentLine = lineNumber;
    offset += fullLine.length;
    lineNumber += 1;
    if (/^```|^~~~/.test(trimmed)) {
      if (!inFence) {
        if (buffer.trim()) {
          blocks.push({
            start,
            end: lineStart,
            line: startLine,
            endLine: Math.max(startLine, currentLine - 1),
            text: buffer.replace(/\s+$/, "")
          });
        }
        start = lineStart;
        startLine = currentLine;
        buffer = fullLine;
        inFence = true;
      } else {
        buffer += fullLine;
        blocks.push({
          start,
          end: offset,
          line: startLine,
          endLine: currentLine,
          text: buffer.replace(/\s+$/, "")
        });
        buffer = "";
        start = offset;
        startLine = lineNumber;
        inFence = false;
      }
      continue;
    }
    if (inFence) {
      buffer += fullLine;
      continue;
    }
    if (!trimmed) {
      if (buffer.trim()) {
        blocks.push({
          start,
          end: lineStart,
          line: startLine,
          endLine: Math.max(startLine, currentLine - 1),
          text: buffer.replace(/\s+$/, "")
        });
        buffer = "";
      }
      start = offset;
      startLine = lineNumber;
      continue;
    }
    if (!buffer) {
      start = lineStart;
      startLine = currentLine;
    }
    buffer += fullLine;
  }
  if (buffer.trim()) {
    blocks.push({
      start,
      end: source.length,
      line: startLine,
      endLine: Math.max(startLine, lineNumber - 1),
      text: buffer.replace(/\s+$/, "")
    });
  }
  return blocks;
}
function resolveRenderedSourcePath(app, root, fallbackPath) {
  const embed = root?.closest?.(".internal-embed, .markdown-embed, .markdown-embed-content");
  if (!embed) {
    return normalizeVaultPath(fallbackPath);
  }
  if (embed.matches?.(".internal-embed") && !embed.querySelector?.(".markdown-embed-content, .markdown-preview-view") && !normalizeRenderedText2(embed.innerText || embed.textContent || "")) {
    return normalizeVaultPath(fallbackPath);
  }
  const owner = embed.matches?.(".internal-embed") ? embed : embed.closest?.(".internal-embed") || embed.querySelector?.(".internal-embed") || embed;
  const rawLink = owner?.getAttribute?.("data-src") || owner?.getAttribute?.("data-path") || owner?.getAttribute?.("src") || "";
  const link = unwrapWikiLink(String(rawLink || "").replace(/^!/, "")).split("|")[0].split("#")[0].trim();
  if (!link) {
    return normalizeVaultPath(fallbackPath);
  }
  const file2 = app.metadataCache.getFirstLinkpathDest?.(link, fallbackPath || "") || getVaultFileByPath(app.vault, link);
  return normalizeVaultPath(file2?.path || fallbackPath);
}
function collectNoteFlowMarkdownOwners(root) {
  if (!root) {
    return [];
  }
  const raw = [];
  const selector = `${NOTE_FLOW_RENDERED_OWNER_SELECTOR},${MARKDOWN_TEXT_SELECTOR},[data-line],.internal-embed,.markdown-embed,.markdown-embed-content`;
  if (root.matches?.(selector)) {
    raw.push(root);
  }
  raw.push(...Array.from(root.querySelectorAll?.(selector) || []));
  const owners = [];
  const seen = /* @__PURE__ */ new Set();
  for (const element of raw) {
    const owner = findNoteFlowMarkdownBlockElement(element, root);
    if (!owner || seen.has(owner) || isNoteFlowCollectionBlock(owner) || owner.closest?.(NOTEDRAW_OWNED_MUTATION_SELECTOR) || owner.closest?.(".frontmatter,.metadata-container")) {
      continue;
    }
    seen.add(owner);
    owners.push(owner);
  }
  return owners;
}
function renderedMarkdownIdentityText(element) {
  let text = element?.innerText || element?.textContent || element?._noteDrawSourceText || "";
  const injectedIdentityDecorations = element?.querySelectorAll?.(
    ".multiple-files-indicator,.multiple-files-references"
  );
  if (injectedIdentityDecorations?.length) {
    const identityClone = element.cloneNode(true);
    for (const decoration of identityClone.querySelectorAll(
      ".multiple-files-indicator,.multiple-files-references"
    )) {
      decoration.remove();
    }
    text = identityClone.textContent || text;
  }
  if (normalizeRenderedText2(text)) {
    return text;
  }
  const media = element?.matches?.("[alt], [data-href], [src]") ? element : element?.querySelector?.("[alt], [data-href], [src]");
  const mediaText = media?.getAttribute?.("alt") || media?.getAttribute?.("data-href") || media?.getAttribute?.("src") || "";
  if (mediaText) {
    return mediaText;
  }
  return element?.matches?.("hr,.el-hr") || element?.querySelector?.("hr") ? "---" : "";
}
function applyRenderedMarkdownLineMetadata(element, match) {
  const members = [element, ...Array.from(element?.querySelectorAll?.(MARKDOWN_TEXT_SELECTOR) || [])].filter((member) => {
    return member === element || findNoteFlowMarkdownBlockElement(member, element.parentElement) === element;
  });
  for (const member of members) {
    const previousStart = parseInteger(member.dataset.noteDrawLineStart);
    const previousEnd = parseInteger(member.dataset.noteDrawLineEnd) ?? previousStart;
    if (Number.isFinite(previousStart) && (previousStart !== match.lineStart || previousEnd !== match.lineEnd)) {
      member.dataset.noteDrawInheritedLineStart = String(previousStart);
      member.dataset.noteDrawInheritedLineEnd = String(previousEnd);
    } else {
      delete member.dataset.noteDrawInheritedLineStart;
      delete member.dataset.noteDrawInheritedLineEnd;
    }
    member.dataset.noteDrawLineStart = String(match.lineStart);
    member.dataset.noteDrawLineEnd = String(match.lineEnd);
    member.dataset.noteDrawLineConfidence = String(match.confidence);
    member.dataset.noteDrawLineMapped = "true";
    delete member.dataset.noteDrawDataLineInherited;
  }
}
function annotateVisibleMarkdownElements(app, root, fallbackPath) {
  for (const element of collectNoteFlowMarkdownOwners(root)) {
    const sourcePath = isMarkdownEmbedBlockElement(element) ? normalizeVaultPath(fallbackPath) : resolveRenderedSourcePath(app, element, fallbackPath);
    if (element.dataset.noteDrawSourcePath !== sourcePath) {
      delete element.dataset.noteDrawLineMapped;
      delete element.dataset.noteDrawInheritedLineStart;
      delete element.dataset.noteDrawInheritedLineEnd;
    }
    element.dataset.noteDrawSourcePath = sourcePath;
    if (element.dataset.noteDrawLineMapped === "true") {
      continue;
    }
    const ownDataLine = parseDataLine(element.getAttribute("data-line"));
    const inheritedDataLine = Number.isFinite(ownDataLine) ? null : parseDataLine(element.parentElement?.closest?.("[data-line]")?.getAttribute("data-line"));
    const dataLine = ownDataLine ?? inheritedDataLine;
    if (Number.isFinite(dataLine)) {
      element.dataset.noteDrawLineStart = String(dataLine);
      element.dataset.noteDrawLineEnd = String(dataLine);
      element.dataset.noteDrawDataLine = String(dataLine);
      if (Number.isFinite(inheritedDataLine)) {
        element.dataset.noteDrawDataLineInherited = "true";
        element.dataset.noteDrawInheritedLineStart = String(inheritedDataLine);
        element.dataset.noteDrawInheritedLineEnd = String(inheritedDataLine);
      } else {
        delete element.dataset.noteDrawDataLineInherited;
        delete element.dataset.noteDrawInheritedLineStart;
        delete element.dataset.noteDrawInheritedLineEnd;
      }
    }
  }
}
async function annotateRenderedMarkdownLines(app, root, fallbackPath, options = {}) {
  annotateVisibleMarkdownElements(app, root, fallbackPath);
  const annotated = collectNoteFlowMarkdownOwners(root).filter((element) => element.dataset.noteDrawSourcePath);
  const elements = annotated.filter((element) => {
    if (options.force === true) {
      return true;
    }
    const line = parseInteger(element.dataset.noteDrawLineStart);
    if (!Number.isFinite(line)) {
      return true;
    }
    return line === 0 || element.dataset.noteDrawDataLineInherited === "true";
  });
  const sources = /* @__PURE__ */ new Map();
  for (const path of new Set(elements.map((element) => normalizeVaultPath(element.dataset.noteDrawSourcePath || fallbackPath)))) {
    const file2 = getVaultFileByPath(app.vault, path);
    if (!file2) {
      continue;
    }
    try {
      sources.set(path, await app.vault.cachedRead(file2));
    } catch (error) {
      void error;
    }
  }
  const usedTargets = /* @__PURE__ */ new Map();
  const sourceIndexes = new Map(Array.from(sources.entries()).map(([path, source]) => {
    return [path, createMarkdownSourceIndex(source)];
  }));
  for (const element of elements) {
    const path = normalizeVaultPath(element.dataset.noteDrawSourcePath || fallbackPath);
    const source = sources.get(path);
    const sourceIndex = sourceIndexes.get(path);
    if (typeof source !== "string") {
      continue;
    }
    const renderedText = renderedMarkdownIdentityText(element);
    const sourceInfo = getSourceInfo(element);
    const used = usedTargets.get(path) || /* @__PURE__ */ new Set();
    const embedIdentity = markdownDragSourceIdentity(element);
    const embedTarget = embedIdentity.embedDestination ? resolveMarkdownEmbedSourceTarget(source, embedIdentity.embedDestination, sourceInfo, sourceIndex) : null;
    const sourceTargets = embedTarget ? [embedTarget] : findRenderedMarkdownSourceTargets(source, renderedText, sourceIndex);
    let target = embedTarget || resolveRenderedMarkdownSourceTarget(source, renderedText, sourceInfo, sourceIndex);
    if (!target || used.has(`${target.start}:${target.end}`)) {
      target = sourceTargets.find((candidate) => {
        return !used.has(`${candidate.start}:${candidate.end}`);
      }) || null;
    }
    const fallback = target || sourceTargets.length ? null : matchRenderedTextToMarkdown(source, renderedText, sourceIndex);
    const match = target ? {
      lineStart: target.line,
      lineEnd: target.endLine,
      confidence: 1
    } : fallback;
    if (!match) {
      continue;
    }
    if (target) {
      used.add(`${target.start}:${target.end}`);
      usedTargets.set(path, used);
    }
    applyRenderedMarkdownLineMetadata(element, match);
  }
}
function annotateEditableElements(root, ctx, sourcePath = ctx?.sourcePath || "") {
  const elements = [];
  if (root.matches?.(EDITABLE_SELECTOR)) {
    elements.push(root);
  }
  elements.push(...root.querySelectorAll(EDITABLE_SELECTOR));
  elements.push(...collectNoteFlowMarkdownOwners(root));
  const seen = /* @__PURE__ */ new Set();
  for (const element of elements) {
    if (seen.has(element)) {
      continue;
    }
    seen.add(element);
    element.dataset.noteDrawSourcePath = normalizeVaultPath(sourcePath);
    const owner = findNoteFlowMarkdownBlockElement(element, root) || element;
    const semanticChild = owner === element ? owner.querySelector?.(MARKDOWN_TEXT_SELECTOR) : element;
    const info = safeGetSectionInfo(ctx, element) || safeGetSectionInfo(ctx, owner) || safeGetSectionInfo(ctx, semanticChild);
    const ownDataLine = parseDataLine(element.getAttribute("data-line"));
    const dataLineEl = element.closest("[data-line]");
    const closestDataLine = parseDataLine(dataLineEl?.getAttribute("data-line"));
    const dataLine = Number.isFinite(ownDataLine) ? ownDataLine : closestDataLine;
    if (Number.isFinite(dataLine)) {
      element.dataset.noteDrawDataLine = String(dataLine);
      element.dataset.noteDrawDataLineScope = Number.isFinite(ownDataLine) ? "self" : "ancestor";
    }
    if (!info) {
      continue;
    }
    if (typeof info.text === "string" && info.text.trim()) {
      element._noteDrawSourceText = info.text;
    }
    if (Number.isFinite(info.lineStart)) {
      element.dataset.noteDrawLineStart = String(info.lineStart);
      element.dataset.noteDrawLineConfidence = "1";
    }
    if (Number.isFinite(info.lineEnd)) {
      element.dataset.noteDrawLineEnd = String(info.lineEnd);
    }
  }
}
function safeGetSectionInfo(ctx, element) {
  try {
    return ctx.getSectionInfo?.(element) || null;
  } catch (error) {
    void error;
    return null;
  }
}
function findOwningMarkdownView(app, element, sourcePath) {
  const leaves = app.workspace.getLeavesOfType?.("markdown") || [];
  for (const leaf of leaves) {
    const view = leaf.view;
    if (!(view instanceof import_obsidian.MarkdownView)) {
      continue;
    }
    if (sourcePath && view.file?.path !== sourcePath) {
      continue;
    }
    if (view.containerEl?.contains(element)) {
      return view;
    }
  }
  return null;
}
function findOwningWorkspaceView(app, element) {
  const leaves = typeof app.workspace?.iterateAllLeaves === "function" ? collectWorkspaceLeaves(app) : [];
  for (const leaf of leaves) {
    const view = leaf?.view;
    if (view?.containerEl?.contains(element)) {
      return view;
    }
  }
  const leafContent = element.closest?.(".workspace-leaf-content");
  return leafContent?._notedrawFallbackView || {
    containerEl: leafContent || element,
    addAction: null,
    getViewType: () => leafContent?.dataset?.type || "webview"
  };
}
function findOwningLeaf(app, element) {
  if (!element) {
    return null;
  }
  return collectWorkspaceLeaves(app).find((leaf) => leaf?.view?.containerEl?.contains(element)) || null;
}
function findActiveWorkspaceLeaf(app) {
  try {
    const recent = app.workspace?.getMostRecentLeaf?.();
    if (recent?.view?.containerEl?.isConnected) {
      return recent;
    }
  } catch (error) {
    void error;
  }
  return collectWorkspaceLeaves(app).find((leaf) => {
    const container = leaf?.view?.containerEl;
    return Boolean(container?.closest?.(".workspace-leaf.mod-active, .workspace-tab-header.is-active")) || container?.classList?.contains("mod-active");
  }) || null;
}
function collectWorkspaceLeaves(app) {
  const leaves = [];
  try {
    app.workspace.iterateAllLeaves((leaf) => leaves.push(leaf));
  } catch (error) {
    void error;
    return [];
  }
  return leaves;
}
function collectImageWorkspaceLeaves(app) {
  const leaves = /* @__PURE__ */ new Set(app.workspace.getLeavesOfType?.("image") || []);
  for (const leaf of collectWorkspaceLeaves(app)) {
    if (isImageWorkspaceView(leaf?.view, workspaceViewType(leaf?.view))) {
      leaves.add(leaf);
    }
  }
  return Array.from(leaves);
}
function isMainWorkspaceView(view) {
  const container = view?.containerEl;
  if (!container?.isConnected || container.closest?.(".workspace-drawer, .mod-sidedock")) {
    return false;
  }
  return Boolean(container.closest?.(".workspace-split.mod-root"));
}
function workspaceViewType(view) {
  return String(view?.getViewType?.() || view?.containerEl?.querySelector?.(".workspace-leaf-content")?.dataset?.type || view?.containerEl?.dataset?.type || "").toLowerCase();
}
function isNativeGraphWorkspaceType(viewType) {
  return viewType === "graph" || viewType === "localgraph";
}
function isWebviewWorkspaceType(viewType) {
  return /(webview|web-view|browser|iframe)/i.test(String(viewType || ""));
}
var IMAGE_WORKSPACE_EXTENSIONS = /* @__PURE__ */ new Set([
  "png",
  "jpg",
  "jpeg",
  "gif",
  "webp",
  "bmp",
  "svg",
  "avif",
  "apng",
  "ico",
  "tif",
  "tiff",
  "heic",
  "heif",
  "jxl"
]);
function isImageWorkspaceView(view, viewType = workspaceViewType(view)) {
  const normalizedType = String(viewType || "").trim().toLowerCase();
  if (normalizedType === "image" || normalizedType === "image-view" || normalizedType === "imageview") {
    return true;
  }
  const extension = String(view?.file?.extension || view?.file?.path?.split(".").pop() || "").trim().toLowerCase();
  return IMAGE_WORKSPACE_EXTENSIONS.has(extension);
}
function createWorkspaceDrawingFile(view, viewType) {
  const sourcePath = workspaceSurfaceStoragePath(view, viewType);
  return {
    path: sourcePath,
    name: sourcePath.split("/").pop() || "workspace.md",
    extension: "md"
  };
}
function normalizeRegisteredSurfaceOptions(app, options = {}) {
  const host = options.host || options.container || options.element;
  if (!isHtmlElementLike(host)) {
    throw new Error("NoteDraw registerSurface requires a host HTMLElement");
  }
  const owner = String(options.owner || "external").trim() || "external";
  const id = String(options.id || options.surfaceId || options.key || options.name || "").trim() || `${safeStorageName(owner)}-${hashString(Date.now())}`;
  const key = registeredSurfaceKey(owner, id);
  const normalizedSource2 = normalizeRegisteredSurfaceSource(options.source || {
    kind: options.kind,
    path: options.path || options.file?.path,
    url: options.url,
    key: options.sourceKey,
    title: options.title,
    format: options.format,
    mediaType: options.mediaType
  }, { owner, id });
  const source = normalizedSource2.kind ? normalizedSource2 : {
    kind: "virtual",
    key: id,
    title: String(options.title || id)
  };
  const capabilities = normalizeRegisteredSurfaceCapabilities(options.capabilities);
  const file2 = createRegisteredSurfaceDrawingFile(app, { owner, id, key, source });
  return {
    owner,
    id,
    key,
    host,
    view: options.view || findOwningWorkspaceView(app, host) || { containerEl: host, getViewType: () => "notedraw-registered-surface" },
    source,
    capabilities,
    viewport: options.viewport || {},
    file: file2
  };
}
function normalizeRegisteredSurfaceSource(source = {}, fallback = {}) {
  if (!source || typeof source !== "object") {
    return {};
  }
  const hasSource = Boolean(source.kind || source.path || source.file?.path || source.url || source.href || source.key || source.title || source.format || source.mediaType);
  if (!hasSource) {
    return {};
  }
  const kind = String(source.kind || (source.url || source.href ? "url" : source.path || source.file?.path ? "vault" : "virtual")).trim().toLowerCase();
  const normalizedKind = ["vault", "url", "virtual"].includes(kind) ? kind : "virtual";
  const result = {
    kind: normalizedKind,
    title: String(source.title || source.name || fallback.id || "").trim(),
    format: normalizeRegisteredSurfaceFormat(source.format || source.type || source.extension || source.mediaType),
    mediaType: String(source.mediaType || source.mime || "").trim().toLowerCase()
  };
  if (normalizedKind === "vault") {
    result.path = normalizeVaultPath(source.path || source.file?.path || source.key || "");
    result.key = result.path || String(source.key || fallback.id || "").trim();
  } else if (normalizedKind === "url") {
    result.url = canonicalizeWebviewUrl(source.url || source.href || source.key || "");
    result.key = result.url || String(source.key || fallback.id || "").trim();
  } else {
    result.key = String(source.key || source.path || source.url || fallback.id || "").trim();
  }
  return result;
}
function normalizeRegisteredSurfaceFormat(value) {
  const format = String(value || "").trim().toLowerCase().replace(/^\./, "");
  const aliases = {
    htm: "html",
    webpage: "web",
    website: "web",
    msword: "docx",
    spreadsheet: "xlsx",
    presentation: "pptx"
  };
  return aliases[format] || (["html", "docx", "xlsx", "pptx", "pdf", "web"].includes(format) ? format : "");
}
function normalizeSurfaceRegion(value = {}, surface = {}) {
  if (!value || typeof value !== "object") {
    return null;
  }
  const basis = String(value.basis || value.coordinateBasis || "surface-normalized").trim().toLowerCase();
  const pixelBasis = ["pixel", "pixels", "surface-pixel", "surface-pixels"].includes(basis);
  const surfaceWidth = Math.max(1, Number(surface.width) || 1);
  const surfaceHeight = Math.max(1, Number(surface.height) || 1);
  let x = Number(value.x ?? value.left);
  let y = Number(value.y ?? value.top);
  let width = Number(value.width);
  let height = Number(value.height);
  if (!Number.isFinite(width) && Number.isFinite(Number(value.right)) && Number.isFinite(x)) {
    width = Number(value.right) - x;
  }
  if (!Number.isFinite(height) && Number.isFinite(Number(value.bottom)) && Number.isFinite(y)) {
    height = Number(value.bottom) - y;
  }
  if (![x, y, width, height].every(Number.isFinite) || width <= 0 || height <= 0) {
    return null;
  }
  if (pixelBasis) {
    x /= surfaceWidth;
    width /= surfaceWidth;
    y /= surfaceHeight;
    height /= surfaceHeight;
  }
  const left = clamp10(x, 0, 1);
  const top = clamp10(y, 0, 1);
  const right = clamp10(x + width, left, 1);
  const bottom = clamp10(y + height, top, 1);
  if (right - left <= 0 || bottom - top <= 0) {
    return null;
  }
  const region = {
    basis: "surface-normalized",
    x: left,
    y: top,
    width: right - left,
    height: bottom - top
  };
  for (const key of ["page", "slide", "sheet", "frame", "layer", "range", "sourceId"]) {
    if (value[key] !== void 0 && value[key] !== null && value[key] !== "") {
      region[key] = value[key];
    }
  }
  return region;
}
function normalizeSurfaceInkPoints(points, region, pointBasis = "surface-normalized") {
  const basis = String(pointBasis || "surface-normalized").trim().toLowerCase();
  return (Array.isArray(points) ? points : []).map((point) => {
    let x = Number(point?.x);
    let y = Number(point?.y);
    if (!Number.isFinite(x) || !Number.isFinite(y)) {
      return null;
    }
    if (["region", "region-normalized", "local"].includes(basis)) {
      x = region.x + clamp10(x, 0, 1) * region.width;
      y = region.y + clamp10(y, 0, 1) * region.height;
    }
    return { x: clamp10(x, 0, 1), y: clamp10(y, 0, 1) };
  }).filter(Boolean);
}
function normalizeRegisteredSurfaceCapabilities(capabilities = {}) {
  return {
    drawing: capabilities.drawing !== false,
    textEditing: capabilities.textEditing !== false,
    elements: capabilities.elements !== false,
    attachments: capabilities.attachments !== false,
    regionEditing: capabilities.regionEditing !== false,
    sourceUpdates: capabilities.sourceUpdates !== false
  };
}
function createRegisteredSurfaceDrawingFile(app, record) {
  const source = record.source || {};
  if (source.kind === "vault" && source.path) {
    const file2 = getVaultFileByPath(app?.vault, source.path);
    if (file2) {
      return file2;
    }
    return {
      path: source.path,
      name: source.path.split("/").pop() || "registered-surface.md",
      extension: source.path.split(".").pop() || "md"
    };
  }
  const sourcePath = registeredSurfaceStoragePath(record);
  return {
    path: sourcePath,
    name: sourcePath.split("/").pop() || "registered-surface.md",
    extension: "md"
  };
}
function registeredSurfaceStoragePath(record) {
  const source = record.source || {};
  const identity = [record.owner, record.id, source.kind, source.path, source.url, source.key, source.title, source.format, source.mediaType].filter(Boolean).join("|") || `${record.owner}|${record.id}`;
  const owner = safeStorageName(record.owner || "external");
  const label = safeStorageName(source.title || source.url || source.path || source.key || record.id || "surface");
  return `${REGISTERED_SURFACE_PREFIX}/${owner}/${label}__${hashString(identity)}.md`;
}
function registeredSurfaceKey(owner, id) {
  return `${String(owner || "external").trim() || "external"}::${String(id || "surface").trim() || "surface"}`;
}
function isHtmlElementLike(value) {
  return Boolean(value && value.nodeType === 1 && typeof value.querySelector === "function" && typeof value.appendChild === "function");
}
function workspaceSurfaceStoragePath(view, viewType) {
  let state = {};
  try {
    state = view?.getState?.() || {};
  } catch (error) {
    void error;
  }
  const title = viewTitle(view) || viewType || "workspace";
  let serializedState = "";
  try {
    serializedState = JSON.stringify(state);
  } catch (error) {
    void error;
  }
  const identity = `${viewType || "workspace"}|${title}|${serializedState}`;
  return `${WORKSPACE_DRAWING_PREFIX}/${safeStorageName(title)}__${hashString(identity)}.md`;
}
function findWorkspaceDrawingSurface(view) {
  const candidates = [
    view?.contentEl,
    view?.containerEl?.querySelector?.(":scope > .view-content"),
    view?.containerEl?.querySelector?.(".view-content")
  ].filter((element) => element?.isConnected && !element.closest?.(".notedraw-body-control"));
  return candidates.find((element) => element.getBoundingClientRect?.().width > 8 && element.getBoundingClientRect?.().height > 8) || candidates[0] || null;
}
function findRootPreviewsForView(view) {
  const rendererPreview = view?.previewMode?.renderer?.previewEl;
  const candidates = [rendererPreview, ...Array.from(view?.containerEl?.querySelectorAll(".markdown-preview-view") || [])];
  return Array.from(new Set(candidates)).filter((preview) => preview?.isConnected && !isEmbeddedPreview(preview));
}
function findRootPreviewForView(view) {
  const previews = findRootPreviewsForView(view);
  const rendererPreview = view?.previewMode?.renderer?.previewEl;
  return pickRootPreview(previews, rendererPreview, isElementVisibleEnough, isElementLaidOut);
}
function rootPreviewSizer(preview) {
  return preview?.querySelector?.(":scope > .markdown-preview-sizer") || preview?.querySelector?.(".markdown-preview-sizer") || null;
}
function isAsyncFileEmbedElement(element) {
  if (!element) {
    return false;
  }
  if (element.matches?.(".obcc-inline-workbench-embed")) {
    return true;
  }
  const source = String(element.getAttribute?.("data-src") || element.getAttribute?.("src") || "").split("#", 1)[0].split("?", 1)[0].toLowerCase();
  return /\.(?:docx?|pptx?|xlsx?|odt|ods|odp|rtf)$/.test(source);
}
function isPendingPdfEmbedElement(element) {
  if (!isPdfEmbedSourceElement(element)) {
    return false;
  }
  return !element.matches(".pdf-embed, .is-loaded") && !element.querySelector?.(".pdf-container, .pdf-viewer-container");
}
function isPdfEmbedSourceElement(element) {
  if (!element?.matches?.(".internal-embed[src], .internal-embed[data-src]")) {
    return false;
  }
  const source = String(element.getAttribute("data-src") || element.getAttribute("src") || "").split("#", 1)[0].split("?", 1)[0].toLowerCase();
  return source.endsWith(".pdf");
}
function isSameEmbedTarget(element, target) {
  if (!element?.matches?.(".internal-embed[src], .internal-embed[data-src]")) {
    return false;
  }
  const normalize = (value) => decodeURIComponent(String(value || "").replace(/^!/, "").split("#", 1)[0].split("?", 1)[0].trim()).toLowerCase();
  const expected = normalize(target);
  const actual = normalize(element.getAttribute("data-src") || element.getAttribute("src"));
  return Boolean(expected && actual && (actual === expected || normalizeVaultPath(actual) === normalizeVaultPath(expected)));
}
function rootPreviewHasRenderedContent(preview) {
  const sizer = rootPreviewSizer(preview);
  if (!sizer) {
    return false;
  }
  const ignored = ".mod-ui, .mod-header, .mod-footer, .markdown-preview-pusher, .notedraw-reading-zoom-extent, .notedraw-reading-bottom-spacer, .notedraw-embed-layer, .notedraw-underlay-embed-layer, .notedraw-underlay-canvas, .notedraw-static-canvas, .notedraw-canvas";
  for (const section of sizer.querySelectorAll?.(".markdown-preview-section") || []) {
    if (Array.from(section.children || []).some((element) => !element.matches?.(ignored))) {
      return true;
    }
  }
  return Array.from(sizer.children || []).some((element) => {
    return !element.matches?.(`${ignored}, .markdown-preview-section`);
  });
}
function markdownViewHasSourceContent(view) {
  const candidates = [view?.data];
  try {
    candidates.push(view?.editor?.getValue?.());
  } catch (error) {
    void error;
  }
  if (candidates.some((value) => typeof value === "string" && value.trim().length > 0)) {
    return true;
  }
  return Number(view?.file?.stat?.size) > 0;
}
function rootPreviewLifecycleState(view, preview) {
  return {
    sourceMode: isSourceMode(view) && !isElementVisibleEnough(preview),
    visible: isMarkdownPreviewVisible(view, preview),
    hasSurface: Boolean(preview && rootPreviewSizer(preview)),
    sourceHasContent: markdownViewHasSourceContent(view),
    renderedContent: rootPreviewHasRenderedContent(preview)
  };
}
function isRootPreviewReady(view, preview) {
  return shouldMountRootPreview(rootPreviewLifecycleState(view, preview));
}
function resetDormantRootPreview(view, preview) {
  if (!preview || !shouldResetDormantRootPreview(rootPreviewLifecycleState(view, preview))) {
    return false;
  }
  cleanupDrawingUi(preview);
  const sizer = rootPreviewSizer(preview);
  for (const property of ["min-height", "padding-bottom"]) {
    sizer?.style?.removeProperty(property);
  }
  cleanupOrphanedNoteFlowLayout(preview);
  return true;
}
function findPrimaryMarkdownSurface(view) {
  const source = findSourceSurfaceForView(view);
  const preview = findRootPreviewForView(view);
  if (isMarkdownPreviewVisible(view, preview)) {
    return preview;
  }
  if (isMarkdownSourceVisible(view, source)) {
    return source;
  }
  return isSourceMode(view) ? source || preview : preview || source;
}
function currentMarkdownSurfaceType(view) {
  const preview = findRootPreviewForView(view);
  if (isMarkdownPreviewVisible(view, preview)) {
    return "preview";
  }
  const source = findSourceSurfaceForView(view);
  if (isMarkdownSourceVisible(view, source)) {
    return "source";
  }
  return isSourceMode(view) ? "source" : "preview";
}
function isReadingSurfaceVisible(view) {
  const preview = findRootPreviewForView(view);
  return isMarkdownPreviewVisible(view, preview);
}
function isElementVisibleEnough(element) {
  if (!isElementLaidOut(element)) {
    return false;
  }
  const rect = element.getBoundingClientRect();
  return Boolean(rect.bottom > 0 && rect.right > 0 && rect.top < window.innerHeight && rect.left < window.innerWidth);
}
function isMarkdownPreviewVisible(view, preview) {
  if (!view || !preview?.isConnected || !isElementDisplayVisible(preview)) {
    return false;
  }
  if (hasVisibleAlternateWorkspaceSurface(view, preview)) {
    return false;
  }
  const viewContent = view.containerEl?.querySelector?.(":scope > .view-content") || view.containerEl?.querySelector?.(".view-content") || view.containerEl;
  if (!isElementVisibleEnough(viewContent)) {
    return false;
  }
  return Boolean(rootPreviewSizer(preview) && isElementLaidOut(preview));
}
function hasVisibleAlternateWorkspaceSurface(view, preview) {
  if (!view?.containerEl || !preview) {
    return false;
  }
  return findWebviewSurfaces(view.containerEl).some((surface) => isElementVisibleEnough(surface) && (!preview.contains?.(surface) || isDominantEmbeddedWebviewSurface(preview, surface)));
}
function isDominantEmbeddedWebviewSurface(preview, surface) {
  if (!preview?.contains?.(surface) || !surface?.matches?.(".mwv-embed[data-url]")) {
    return false;
  }
  const previewWidth = Math.max(1, Number(preview.clientWidth) || Number(preview.offsetWidth) || 1);
  const previewHeight = Math.max(1, Number(preview.clientHeight) || Number(preview.offsetHeight) || 1);
  const surfaceWidth = Math.max(0, Number(surface.offsetWidth) || 0);
  const surfaceHeight = Math.max(0, Number(surface.offsetHeight) || 0);
  const surfaceBlock = surface.closest?.(".el-div, .el-p, .el-pre, .el-table, .el-blockquote, .el-ul, .el-ol") || surface;
  const sizer = rootPreviewSizer(preview);
  const otherBodyBlocks = Array.from(sizer?.children || []).filter((element) => {
    return element !== surfaceBlock && element.matches?.("[class*='el-']") && !element.matches?.(".el-h1, .el-h2, .el-h3, .el-h4, .el-h5, .el-h6");
  });
  const primaryDocumentSurface = preview.classList?.contains("mwv-note-browser-document") && otherBodyBlocks.length === 0;
  return surfaceWidth >= previewWidth * 0.8 && (surfaceHeight >= previewHeight * 0.8 || primaryDocumentSurface);
}
function isElementDisplayVisible(element) {
  if (!element?.isConnected) {
    return false;
  }
  const style = element.ownerDocument?.defaultView?.getComputedStyle?.(element);
  return style?.display !== "none" && style?.visibility !== "hidden" && Number(style?.opacity ?? 1) !== 0;
}
function isMarkdownSourceVisible(view, source = findSourceSurfaceForView(view)) {
  if (!view || !source?.isConnected || !isElementDisplayVisible(source) || !isElementLaidOut(source)) {
    return false;
  }
  const sourceRoot = source.closest?.(".markdown-source-view") || source;
  return isElementDisplayVisible(sourceRoot) && isElementLaidOut(sourceRoot);
}
function isElementLaidOut(element) {
  if (!element?.isConnected) {
    return false;
  }
  const rect = element.getBoundingClientRect?.();
  return Boolean(rect && rect.width > 8 && rect.height > 8);
}
function isElementNearViewport(element, margin = 320) {
  if (!isElementLaidOut(element)) {
    return false;
  }
  const rect = element.getBoundingClientRect();
  return rect.bottom > -margin && rect.right > -margin && rect.top < window.innerHeight + margin && rect.left < window.innerWidth + margin;
}
function isBlockingObsidianOverlayOpen(document2) {
  const candidates = document2?.querySelectorAll?.(".modal-container .modal, .modal-container.mod-dim, .modal-bg") || [];
  return Array.from(candidates).some((element) => {
    if (!element?.isConnected) {
      return false;
    }
    const style = element.ownerDocument?.defaultView?.getComputedStyle?.(element);
    const rect = element.getBoundingClientRect?.();
    return style?.display !== "none" && style?.visibility !== "hidden" && Number(style?.opacity ?? 1) !== 0 && Boolean(rect && rect.width > 1 && rect.height > 1);
  });
}
function findWebviewSurfaces(root) {
  if (!root) {
    return [];
  }
  const selectors = [
    ".mwv-embed[data-url]",
    ".workspace-leaf-content[data-type='mobile-webviewer-view'] .view-content",
    ".workspace-leaf-content[data-type*='webview'] .view-content",
    ".workspace-leaf-content[data-type*='web-view'] .view-content",
    ".workspace-leaf-content[data-type*='browser'] .view-content",
    ".workspace-leaf-content[data-type*='iframe'] .view-content"
  ];
  const candidates = selectors.flatMap((selector) => Array.from(root.querySelectorAll(selector)));
  const iframeHosts = Array.from(root.querySelectorAll("webview, iframe")).map((element) => element.closest(".mwv-embed[data-url], .view-content, .workspace-leaf-content") || element);
  return uniqueConnectedElements([...candidates, ...iframeHosts]).filter((element) => {
    const viewType = String(element.closest?.(".workspace-leaf-content")?.dataset?.type || "").toLowerCase();
    return !isNativeGraphWorkspaceType(viewType) && !element.closest(".notedraw-toolbar, .notedraw-palette-panel, .notedraw-brush-panel, .notedraw-text-panel");
  });
}
function findWebviewButtonHost(previewEl, view) {
  const candidates = [
    previewEl?.querySelector?.(".mwv-toolbar"),
    previewEl?.querySelector?.(".mwv-note-actions"),
    previewEl?.querySelector?.(".mwv-note-source"),
    previewEl?.querySelector?.(".mwv-address-row"),
    previewEl?.querySelector?.(".mwv-header"),
    view?.containerEl?.querySelector?.(".view-actions")
  ].filter(Boolean);
  for (const candidate of candidates) {
    if (candidate?.isConnected) {
      return candidate;
    }
  }
  return null;
}
function uniqueConnectedElements(elements) {
  const seen = /* @__PURE__ */ new Set();
  const result = [];
  for (const element of elements) {
    if (!element?.isConnected || seen.has(element)) {
      continue;
    }
    seen.add(element);
    result.push(element);
  }
  return result.filter((element) => !result.some((other) => other !== element && element.contains(other)));
}
function createWebviewDrawingFile(surface, view) {
  const sourcePath = webviewSurfaceStoragePath(surface, view);
  return {
    path: sourcePath,
    name: sourcePath.split("/").pop() || "webview.md",
    extension: "md"
  };
}
function webviewSurfaceStoragePath(surface, view) {
  const explicitUrl = surface?.dataset?.url || surface?.querySelector?.("[data-url]")?.dataset?.url || webviewCurrentUrl(view) || surface?.querySelector?.("webview, iframe")?.getAttribute?.("src") || "";
  const identity = canonicalizeWebviewUrl(explicitUrl) || viewTitle(view) || surface?.closest?.(".workspace-leaf-content")?.getAttribute?.("data-type") || "webview";
  const label = safeStorageName(webviewSurfaceLabel(identity, view));
  return `${WEBVIEW_DRAWING_PREFIX}/${label}__${hashString(identity)}.md`;
}
function webviewCurrentUrl(view) {
  const candidates = [
    view?.currentUrl,
    view?.iframeEl?.getAttribute?.("src"),
    view?.iframeEl?.src,
    view?.addressEl?.value
  ];
  return candidates.map((value) => String(value || "").trim()).find(Boolean) || "";
}
function canonicalizeWebviewUrl(url) {
  const text = String(url || "").trim();
  if (!text) {
    return "";
  }
  try {
    return new URL(text).toString();
  } catch (error) {
    void error;
    return text;
  }
}
function webviewSurfaceLabel(identity, view) {
  try {
    const parsed = new URL(identity);
    return parsed.hostname.replace(/^www\./, "") || viewTitle(view) || "webview";
  } catch (error) {
    void error;
    return viewTitle(view) || "webview";
  }
}
function viewTitle(view) {
  try {
    if (typeof view?.getDisplayText === "function") {
      return view.getDisplayText();
    }
  } catch (error) {
    void error;
  }
  return view?.containerEl?.querySelector?.(".view-header-title")?.textContent?.trim() || view?.containerEl?.getAttribute?.("data-type") || view?.getViewType?.() || "webview";
}
function safeStorageName(value) {
  return String(value || "webview").replace(/\\/g, "/").split("/").pop().replace(/[^a-zA-Z0-9._-]+/g, "_").replace(/^_+|_+$/g, "").slice(0, 80) || "webview";
}
function applyElementStyles(element, styles) {
  if (!element || !styles) {
    return;
  }
  if (typeof element.setCssStyles === "function") {
    element.setCssStyles(styles);
    return;
  }
  for (const [key, value] of Object.entries(styles)) {
    element.style[key] = value;
  }
}
function findNoteDrawExportHost(container) {
  const closestShell = container.closest?.(".notedraw-shell");
  if (closestShell instanceof HTMLElement) {
    return closestShell;
  }
  const nestedShell = container.querySelector?.(".notedraw-shell");
  if (nestedShell instanceof HTMLElement) {
    return nestedShell;
  }
  return container;
}
async function prepareExportImages(root) {
  const images = root instanceof HTMLImageElement ? [root] : Array.from(root?.querySelectorAll?.("img") || []);
  for (const image of images) {
    await prepareExportImage(image);
  }
}
async function prepareExportImage(image) {
  if (!(image instanceof HTMLImageElement)) {
    return;
  }
  image.removeAttribute("srcset");
  image.setAttribute("decoding", "sync");
  image.setAttribute("loading", "eager");
  await waitForImage(image, 1800).catch(() => null);
  if (!image.naturalWidth || !image.naturalHeight) {
    return;
  }
  flattenImageOnWhite(image);
}
function flattenImageOnWhite(image) {
  try {
    const width = Math.max(1, image.naturalWidth || image.width);
    const height = Math.max(1, image.naturalHeight || image.height);
    const canvas = activeDocument.createElement("canvas");
    canvas.width = width;
    canvas.height = height;
    const context = canvas.getContext("2d");
    if (!context) {
      return false;
    }
    context.fillStyle = "#fff";
    context.fillRect(0, 0, width, height);
    context.drawImage(image, 0, 0, width, height);
    image.src = canvas.toDataURL("image/png");
    return true;
  } catch (error) {
    void error;
    return false;
  }
}
function hashString(value) {
  let hash = 2166136261;
  const text = String(value || "");
  for (let index = 0; index < text.length; index += 1) {
    hash ^= text.charCodeAt(index);
    hash = Math.imul(hash, 16777619);
  }
  return (hash >>> 0).toString(16).padStart(8, "0");
}
function isSourceMode(view) {
  try {
    if (typeof view?.getMode === "function") {
      return view.getMode() === "source";
    }
  } catch (error) {
    void error;
  }
  const stateMode = view?.getState?.()?.mode;
  if (stateMode) {
    return stateMode === "source";
  }
  return Boolean(findSourceSurfaceForView(view));
}
function findSourceSurfaceForView(view) {
  const container = view?.containerEl;
  if (!container) {
    return null;
  }
  const candidates = [
    container.querySelector(".markdown-source-view .cm-scroller"),
    container.querySelector(".markdown-source-view .cm-editor"),
    container.querySelector(".markdown-source-view")
  ].filter(Boolean);
  return candidates.find((element) => isElementDisplayVisible(element) && isElementLaidOut(element)) || candidates.find((element) => isElementLaidOut(element)) || candidates[0] || null;
}
function getCodeMirrorView(markdownView, sourceEl) {
  const candidates = [
    markdownView?.editor?.cm,
    markdownView?.editor?.editor?.cm,
    markdownView?.editor?.editor,
    markdownView?.cm,
    markdownView?.cmEditor,
    sourceEl?.cmView
  ];
  return candidates.find((candidate) => candidate && typeof candidate.posAtCoords === "function" && typeof candidate.dispatch === "function") || null;
}
function dispatchMouseClickThroughOverlay(canvas, clientPoint) {
  if (!canvas || !clientPoint) {
    return false;
  }
  const previousPointerEvents = canvas.style.pointerEvents;
  applyElementStyles(canvas, { pointerEvents: "none" });
  const target = activeDocument.elementFromPoint(clientPoint.x, clientPoint.y);
  applyElementStyles(canvas, { pointerEvents: previousPointerEvents || "" });
  if (!target) {
    return false;
  }
  const eventInit = {
    bubbles: true,
    cancelable: true,
    view: window,
    clientX: clientPoint.x,
    clientY: clientPoint.y,
    button: 0,
    buttons: 1
  };
  target.dispatchEvent(new MouseEvent("mousedown", eventInit));
  target.dispatchEvent(new MouseEvent("mouseup", { ...eventInit, buttons: 0 }));
  target.dispatchEvent(new MouseEvent("click", { ...eventInit, buttons: 0 }));
  return true;
}
function collectTextLineRectsBelowCanvas(canvas, root, ownerIds = null) {
  const document2 = canvas?.ownerDocument || activeDocument;
  const canvasRect = canvas?.getBoundingClientRect?.();
  if (!canvas || !root || !canvasRect || !document2?.createTreeWalker) {
    return [];
  }
  const nodeFilter = document2.defaultView?.NodeFilter || window.NodeFilter;
  if (!nodeFilter) {
    return [];
  }
  const seenNodes = /* @__PURE__ */ new Set();
  const seenRects = /* @__PURE__ */ new Set();
  const lineBucketsByOwner = /* @__PURE__ */ new Map();
  const ownerKeys = /* @__PURE__ */ new WeakMap();
  let nextOwnerKey = 1;
  let lineBucketCount = 0;
  const ownerKeyFor = (element, sourcePath, lineStart, blockId) => {
    if (blockId) {
      return `block:${blockId}`;
    }
    if (element && typeof element === "object") {
      if (!ownerKeys.has(element)) {
        ownerKeys.set(element, `element:${nextOwnerKey++}`);
      }
      return ownerKeys.get(element);
    }
    return `line:${sourcePath}:${lineStart}`;
  };
  const addRect = (rect, element) => {
    if (!rect || rect.width <= 1 || rect.height <= 1 || rect.bottom < canvasRect.top - 48 || rect.top > canvasRect.bottom + 48 || rect.right < canvasRect.left - 180 || rect.left > canvasRect.right + 180) {
      return;
    }
    const sourcePath = element.dataset.noteDrawSourcePath || element.closest?.("[data-note-draw-source-path]")?.dataset?.noteDrawSourcePath || "";
    const lineStart = parseInteger(element.dataset.noteDrawLineStart);
    const lineEnd = parseInteger(element.dataset.noteDrawLineEnd);
    const blockId = String(element.dataset.noteDrawMarkdownBlockId || element.closest?.("[data-note-draw-markdown-block-id]")?.dataset?.noteDrawMarkdownBlockId || "");
    const ownerKey = ownerKeyFor(element, sourcePath, lineStart, blockId);
    const key = [ownerKey, rect.left, rect.top, rect.right, rect.bottom].map((value) => Math.round(Number(value) * 2) || value).join(":");
    if (seenRects.has(key)) {
      return;
    }
    seenRects.add(key);
    const ownerBuckets = lineBucketsByOwner.get(ownerKey) || [];
    const tolerance = Math.max(2, Math.min(rect.height, 28) * 0.45);
    const bucket = ownerBuckets.find((candidate) => Math.abs(candidate.top - rect.top) <= tolerance);
    const next = {
      left: rect.left,
      top: rect.top,
      right: rect.right,
      bottom: rect.bottom,
      width: rect.width,
      height: rect.height,
      element,
      sourcePath,
      lineStart,
      lineEnd,
      blockId,
      ownerKey
    };
    if (bucket) {
      bucket.left = Math.min(bucket.left, next.left);
      bucket.top = Math.min(bucket.top, next.top);
      bucket.right = Math.max(bucket.right, next.right);
      bucket.bottom = Math.max(bucket.bottom, next.bottom);
      bucket.width = bucket.right - bucket.left;
      bucket.height = bucket.bottom - bucket.top;
      return;
    }
    ownerBuckets.push(next);
    lineBucketsByOwner.set(ownerKey, ownerBuckets);
    lineBucketCount += 1;
  };
  for (const textElement of root.querySelectorAll?.(MARKDOWN_TEXT_SELECTOR) || []) {
    const ownerId = String(textElement.dataset.noteDrawMarkdownBlockId || textElement.closest?.("[data-note-draw-markdown-block-id]")?.dataset?.noteDrawMarkdownBlockId || "");
    if (ownerIds && (!ownerId || !ownerIds.has(ownerId)) || textElement.closest?.(WEBVIEW_BLOCKED_EDIT_SELECTOR) || textElement.closest?.(NOTEDRAW_OWNED_MUTATION_SELECTOR) || textElement.querySelector?.(MARKDOWN_TEXT_SELECTOR)) {
      continue;
    }
    const beforeCount = lineBucketCount;
    const walker = document2.createTreeWalker(textElement, nodeFilter.SHOW_TEXT);
    let node = walker.nextNode();
    while (node && lineBucketCount < 1200) {
      if (!seenNodes.has(node) && String(node.nodeValue || "").trim()) {
        seenNodes.add(node);
        const range = document2.createRange();
        range.selectNodeContents(node);
        for (const rect of Array.from(range.getClientRects())) {
          addRect(rect, textElement);
        }
      }
      node = walker.nextNode();
    }
    if (beforeCount === lineBucketCount) {
      addRect(textElement.getBoundingClientRect?.(), textElement);
    }
    if (lineBucketCount >= 1200) {
      break;
    }
  }
  for (const textElement of root.querySelectorAll?.(".cm-line") || []) {
    const ownerId = String(textElement.dataset.noteDrawMarkdownBlockId || textElement.closest?.("[data-note-draw-markdown-block-id]")?.dataset?.noteDrawMarkdownBlockId || "");
    if (lineBucketCount >= 1200 || ownerIds && (!ownerId || !ownerIds.has(ownerId)) || textElement.closest?.(WEBVIEW_BLOCKED_EDIT_SELECTOR) || textElement.closest?.(NOTEDRAW_OWNED_MUTATION_SELECTOR)) {
      continue;
    }
    const beforeCount = lineBucketCount;
    const walker = document2.createTreeWalker(textElement, nodeFilter.SHOW_TEXT);
    let node = walker.nextNode();
    while (node && lineBucketCount < 1200) {
      if (!seenNodes.has(node) && String(node.nodeValue || "").trim()) {
        seenNodes.add(node);
        const range = document2.createRange();
        range.selectNodeContents(node);
        for (const rect of Array.from(range.getClientRects())) {
          addRect(rect, textElement);
        }
      }
      node = walker.nextNode();
    }
    if (beforeCount === lineBucketCount) {
      addRect(textElement.getBoundingClientRect?.(), textElement);
    }
  }
  const rects = Array.from(lineBucketsByOwner.values()).flat();
  const grouped = /* @__PURE__ */ new Map();
  for (const rect of rects) {
    const key = rect.ownerKey || rect.blockId || `${rect.sourcePath}:${rect.lineStart ?? ""}`;
    const group = grouped.get(key) || [];
    group.push(rect);
    grouped.set(key, group);
  }
  for (const group of grouped.values()) {
    group.sort((left, right) => left.top - right.top || left.left - right.left);
    group.forEach((rect, index) => {
      rect.lineIndex = index;
      rect.lineKey = `${Math.round(rect.top * 2)}:${Math.round(rect.bottom * 2)}`;
    });
  }
  return rects.sort((left, right) => left.top - right.top || left.left - right.left);
}
function domPathForElement(element, root) {
  if (!element || !root?.contains(element) || element === root) {
    return "";
  }
  const parts = [];
  let current = element;
  while (current && current !== root && current.nodeType === Node.ELEMENT_NODE) {
    const parent = current.parentElement;
    if (!parent) {
      return "";
    }
    const tag = current.tagName.toLowerCase();
    const siblings = Array.from(parent.children).filter((child) => child.tagName === current.tagName);
    const index = Math.max(0, siblings.indexOf(current));
    parts.unshift(`${tag}:${index}`);
    current = parent;
  }
  return parts.join("/");
}
function elementForDomPath(root, path) {
  if (!root || !path) {
    return null;
  }
  let current = root;
  for (const part of String(path).split("/")) {
    const [tag, indexText] = part.split(":");
    const index = Number(indexText);
    if (!tag || !Number.isInteger(index) || index < 0) {
      return null;
    }
    const matches = Array.from(current.children || []).filter((child) => child.tagName.toLowerCase() === tag);
    current = matches[index] || null;
    if (!current) {
      return null;
    }
  }
  return current;
}
function findWebEditElement(root, edit, used = /* @__PURE__ */ new Set()) {
  const direct = elementForDomPath(root, edit.path);
  if (direct && !used.has(direct)) {
    return direct;
  }
  const original = normalizeRenderedText2(edit.originalText);
  if (!original) {
    return null;
  }
  const candidates = Array.from(root.querySelectorAll(WEBVIEW_EDITABLE_SELECTOR)).filter((element) => !used.has(element) && !element.closest(WEBVIEW_BLOCKED_EDIT_SELECTOR));
  return candidates.find((element) => normalizeRenderedText2(element.innerText) === original) || null;
}
function isEmbeddedPreview(preview) {
  return Boolean(preview.closest(".markdown-embed, .markdown-embed-content, .internal-embed, .external-embed"));
}
function isEmbeddedEditableElement(element) {
  return Boolean(element?.closest?.(".markdown-embed, .markdown-embed-content, .internal-embed"));
}
function cleanupAllDrawingHeaderButtons() {
  activeDocument.querySelectorAll(".notedraw-header-button, .notedraw-webview-button").forEach((button) => button.remove());
  activeDocument.body?.querySelectorAll?.(".notedraw-body-control, .notedraw-file-input").forEach((element) => element.remove());
}
function cleanupOrphanedNoteFlowLayout(preview) {
  preview?.querySelectorAll?.(".notedraw-note-flow-line-spacer, .notedraw-note-flow-block-spacer").forEach((spacer) => spacer.remove());
  for (const anchor of preview?.querySelectorAll?.(".notedraw-note-flow-anchor") || []) {
    for (const property of ["padding-top", "padding-bottom", "margin-top", "margin-bottom"]) {
      if (anchor.style?.getPropertyPriority(property) === "important") {
        anchor.style.removeProperty(property);
      }
    }
    anchor.classList?.remove("notedraw-note-flow-anchor");
  }
}
function cleanupDrawingUi(preview) {
  cleanupOrphanedNoteFlowLayout(preview);
  for (const row of preview.querySelectorAll?.(".notedraw-md-grid-row") || []) {
    const parent = row.parentNode;
    if (!parent) {
      continue;
    }
    for (const child of Array.from(row.children || [])) {
      delete child.dataset.noteDrawGridSpan;
      parent.insertBefore(child, row);
    }
    row.remove();
  }
  preview.querySelectorAll(".notedraw-button, .notedraw-fallback-button, .notedraw-webview-button, .notedraw-toolbar, .notedraw-palette-panel, .notedraw-brush-panel, .notedraw-text-panel, .notedraw-selection-menu, .notedraw-format-toolbar, .notedraw-reading-zoom-extent, .notedraw-reading-bottom-spacer, .notedraw-embed-layer, .notedraw-underlay-embed-layer, .notedraw-note-flow-line-spacer, .notedraw-note-flow-block-spacer, .notedraw-file-input, .notedraw-underlay-canvas, .notedraw-static-canvas, .notedraw-canvas").forEach((element) => element.remove());
  preview.classList.remove("notedraw-shell", "is-drawing-active", "is-drawing-hidden", "is-select-mode", "is-palette-open", "is-brush-panel-open", "is-text-panel-open", "is-selection-menu-open", "is-watercolor-mode", "is-edit-md-mode", "is-selecting-strokes", "is-resizing-selection", "is-native-text-editing", "is-reading-zoomed", "is-editing-layout-zoomed", "is-notedraw-webview-shell", "is-notedraw-workspace-shell", "is-notedraw-embedded-shell", "is-notedraw-registered-shell", "is-notedraw-responsive-layout", "is-notedraw-controls-visible", "has-notedraw-body-controls", "has-notedraw-canvas");
  for (const property of [
    "--notedraw-toolbar-right",
    "--notedraw-toolbar-left",
    "--notedraw-toolbar-top",
    "--notedraw-palette-top",
    "--notedraw-palette-left",
    "--notedraw-palette-right",
    "--notedraw-brush-panel-top",
    "--notedraw-brush-panel-left",
    "--notedraw-brush-panel-right",
    "--notedraw-text-panel-top",
    "--notedraw-text-panel-left",
    "--notedraw-text-panel-right"
  ]) {
    preview.style?.removeProperty(property);
  }
}
function isNoteDrawOwnedMutation(mutation) {
  if (!mutation) {
    return false;
  }
  const target = mutation.target;
  if (target?.nodeType === Node.ELEMENT_NODE && (target.matches?.(NOTEDRAW_OWNED_MUTATION_SELECTOR) || target.closest?.(NOTEDRAW_OWNED_MUTATION_SELECTOR))) {
    return true;
  }
  const nodes = [...Array.from(mutation.addedNodes || []), ...Array.from(mutation.removedNodes || [])];
  return nodes.length > 0 && nodes.every((node) => node?.nodeType !== Node.ELEMENT_NODE || node.matches?.(NOTEDRAW_OWNED_MUTATION_SELECTOR) || node.closest?.(NOTEDRAW_OWNED_MUTATION_SELECTOR));
}
function isMarkdownContentMutation(mutation) {
  if (mutation?.type !== "childList" || isNoteDrawOwnedMutation(mutation)) {
    return false;
  }
  if (mutation.target?.closest?.(".internal-embed, .markdown-embed, .markdown-embed-content")) {
    return false;
  }
  const changedElements = [...Array.from(mutation.addedNodes || []), ...Array.from(mutation.removedNodes || [])].filter((node) => node?.nodeType === Node.ELEMENT_NODE);
  const embedSelector = ".internal-embed, .file-embed, .pdf-embed, .pdf-container, .pdf-viewer-container, .obcc-inline-workbench-embed";
  if (changedElements.length && changedElements.every((node) => node.matches?.(embedSelector) || node.closest?.(embedSelector) || node.querySelector?.(embedSelector))) {
    return false;
  }
  const markdownRootSelector = ".markdown-preview-view, .markdown-embed-content, .internal-embed";
  return Array.from(mutation.addedNodes || []).some((node) => {
    if (!node || node.nodeType !== Node.ELEMENT_NODE) {
      return false;
    }
    const insideMarkdown = Boolean(node.closest?.(markdownRootSelector));
    return node.matches?.(markdownRootSelector) || node.querySelector?.(markdownRootSelector) || insideMarkdown && (node.matches?.(MARKDOWN_TEXT_SELECTOR) || node.querySelector?.(MARKDOWN_TEXT_SELECTOR));
  });
}
function isEmbeddedSurfaceSyncMutation(mutation) {
  if (!mutation || isNoteDrawOwnedMutation(mutation)) {
    return false;
  }
  const selector = ".markdown-embed, .markdown-embed-content, .internal-embed";
  if (mutation.type === "attributes") {
    return mutation.attributeName === "class" && Boolean(
      mutation.target?.matches?.(selector) || mutation.target?.closest?.(selector) || mutation.target?.querySelector?.(selector)
    );
  }
  if (mutation.type !== "childList") {
    return false;
  }
  return [...Array.from(mutation.addedNodes || []), ...Array.from(mutation.removedNodes || [])].some((node) => node?.nodeType === Node.ELEMENT_NODE && (node.matches?.(selector) || node.querySelector?.(selector) || node.closest?.(selector)));
}
function isWebviewSyncMutation(mutation) {
  if (!mutation || isNoteDrawOwnedMutation(mutation)) {
    return false;
  }
  if (mutation.type === "attributes") {
    return mutation.attributeName === "data-url" || mutation.attributeName === "src";
  }
  if (mutation.type !== "childList") {
    return false;
  }
  return [...Array.from(mutation.addedNodes || []), ...Array.from(mutation.removedNodes || [])].some((node) => isWebviewRelatedNode(node));
}
function isWorkspaceSurfaceMutation(mutation) {
  if (!mutation || isNoteDrawOwnedMutation(mutation)) {
    return false;
  }
  if (mutation.type === "attributes") {
    return Boolean(mutation.target?.matches?.(".workspace-leaf, .workspace-leaf-content, .view-content, .canvas-wrapper"));
  }
  if (mutation.type !== "childList") {
    return false;
  }
  const nodes = [...Array.from(mutation.addedNodes || []), ...Array.from(mutation.removedNodes || [])].filter((node) => {
    return node?.nodeType === Node.ELEMENT_NODE && !node.matches?.(".notedraw-reading-bottom-spacer, .notedraw-underlay-canvas, .notedraw-static-canvas, .notedraw-canvas, .notedraw-embed-layer, .notedraw-underlay-embed-layer, .notedraw-body-control");
  });
  if (!nodes.length) {
    return false;
  }
  const leafContent = mutation.target?.closest?.(".workspace-leaf-content");
  const viewType = leafContent?.dataset?.type || "";
  return Boolean(leafContent) && viewType !== "markdown" && !isWebviewWorkspaceType(viewType) && !isNativeGraphWorkspaceType(viewType);
}
function isFloatingControlsVisibilityMutation(mutation) {
  if (!mutation || isNoteDrawOwnedMutation(mutation)) {
    return false;
  }
  if (mutation.type === "attributes") {
    return Boolean(mutation.target?.matches?.(".workspace-leaf, .workspace-leaf-content, .workspace-tabs, .modal-container, .modal, .app-container, body"));
  }
  if (mutation.type !== "childList") {
    return false;
  }
  const selector = ".modal-container, .modal, .modal-bg, .workspace-leaf, .workspace-leaf-content";
  return [...Array.from(mutation.addedNodes || []), ...Array.from(mutation.removedNodes || [])].some((node) => node?.nodeType === Node.ELEMENT_NODE && (node.matches?.(selector) || node.querySelector?.(selector)));
}
function isWebviewRelatedNode(node) {
  if (!node || node.nodeType !== Node.ELEMENT_NODE) {
    return false;
  }
  return node.matches?.(".mwv-embed, webview, iframe, .workspace-leaf-content[data-type*='webview'], .workspace-leaf-content[data-type*='web-view'], .workspace-leaf-content[data-type*='browser'], .workspace-leaf-content[data-type*='iframe']") || Boolean(node.querySelector?.(".mwv-embed, webview, iframe, .workspace-leaf-content[data-type*='webview'], .workspace-leaf-content[data-type*='web-view'], .workspace-leaf-content[data-type*='browser'], .workspace-leaf-content[data-type*='iframe']"));
}
function getVaultFileByPath(vault, path) {
  const normalized = normalizeVaultPath(path);
  if (!vault || !normalized) {
    return null;
  }
  const file2 = vault.getAbstractFileByPath((0, import_obsidian.normalizePath)(normalized));
  return file2 && typeof file2.extension === "string" ? file2 : null;
}
function normalizeVaultPath(path) {
  return String(path || "").replace(/\\/g, "/").replace(/^\/+/, "").replace(/\/+$/, "");
}
function sanitizeAssetFileName(name) {
  const cleaned = String(name || "attachment.bin").replace(/\\/g, "/").split("/").pop().split("").map((character) => {
    const code = character.charCodeAt(0);
    if (character === "<" || character === ">" || character === ":" || character === '"' || character === "|" || character === "?" || character === "*" || code <= 31) {
      return "_";
    }
    return character;
  }).join("").replace(/\s+/g, " ").trim();
  return cleaned || "attachment.bin";
}
function normalizeStrokeKind(kind) {
  if (kind === TOOL_TEXT || kind === TOOL_EMBED) {
    return kind;
  }
  return void 0;
}
function normalizeBrushVariant(brush, variant) {
  const value = String(variant || BRUSH_VARIANT_DEFAULT).trim().toLowerCase();
  if (brush === BRUSH_WATERCOLOR) {
    return [WATERCOLOR_VARIANT_TEXT, WATERCOLOR_VARIANT_STRAIGHT].includes(value) ? value : BRUSH_VARIANT_DEFAULT;
  }
  return [PEN_VARIANT_FOUNTAIN, PEN_VARIANT_NOTE, PEN_VARIANT_LASER].includes(value) ? value : BRUSH_VARIANT_DEFAULT;
}
function brushVariantsForMode(mode) {
  return mode === BRUSH_WATERCOLOR ? [BRUSH_VARIANT_DEFAULT, WATERCOLOR_VARIANT_TEXT, WATERCOLOR_VARIANT_STRAIGHT] : [BRUSH_VARIANT_DEFAULT, PEN_VARIANT_FOUNTAIN, PEN_VARIANT_NOTE, PEN_VARIANT_LASER];
}
function normalizeBrushToolSettings(value, fallback = {}) {
  const input = value || {};
  return {
    color: isCssColor(input.color) ? input.color : isCssColor(fallback.color) ? fallback.color : "#e53935",
    width: clamp10(Number(input.width ?? fallback.width ?? DEFAULT_SETTINGS.defaultPenWidth), MIN_BRUSH_WIDTH, MAX_BRUSH_WIDTH),
    opacity: clamp10(Number(input.opacity ?? fallback.opacity ?? DEFAULT_PEN_OPACITY), 0, 1),
    count: Math.max(1, Math.round(Number(input.count ?? fallback.count ?? 1)))
  };
}
function createBrushVariantSettings(value, fallbacks = {}) {
  const input = value || {};
  const result = {};
  for (const mode of [BRUSH_PEN, BRUSH_WATERCOLOR]) {
    result[mode] = {};
    for (const variant of brushVariantsForMode(mode)) {
      result[mode][variant] = normalizeBrushToolSettings(input?.[mode]?.[variant], fallbacks?.[mode]);
    }
  }
  return result;
}
function cloneBrushVariantSettings(value) {
  const result = {};
  for (const mode of [BRUSH_PEN, BRUSH_WATERCOLOR]) {
    result[mode] = {};
    for (const variant of brushVariantsForMode(mode)) {
      if (value?.[mode]?.[variant]) {
        result[mode][variant] = { ...value[mode][variant] };
      }
    }
  }
  return result;
}
function normalizeToolMode(value) {
  return [TOOL_DRAW, TOOL_SELECT, TOOL_EDIT_MD, TOOL_TEXT].includes(value) ? value : TOOL_DRAW;
}
function normalizeTextPreset(value) {
  const preset = String(value || "plain");
  const migrated = {
    title: "plain",
    code: "plain",
    file: "plain",
    button: "plain",
    buttonPrimary: "plain",
    buttonOutline: "rectangle",
    buttonPill: "circle",
    arrowUp: "arrow",
    arrowDown: "arrow",
    arrowLeft: "arrow",
    arrowRight: "arrow"
  }[preset] || preset;
  return [
    "plain",
    "rectangle",
    "circle",
    "arrow",
    "image",
    "video",
    "attachment",
    "markdown",
    "html",
    "note",
    "mindMap"
  ].includes(migrated) ? migrated : "plain";
}
function normalizeConnector(value) {
  if (!value || typeof value !== "object") {
    return null;
  }
  const fromId = typeof value.fromId === "string" ? value.fromId : "";
  const toId = typeof value.toId === "string" ? value.toId : "";
  const isFreeConnector = value.kind === "connector";
  if (!isFreeConnector && (!fromId || !toId || fromId === toId)) {
    return null;
  }
  return {
    kind: "connector",
    fromId,
    toId,
    style: value.style === "mindmap" ? "mindmap" : "curve",
    arrow: value.arrow !== false
  };
}
function normalizeNoteFlow(value) {
  if (!value?.enabled) {
    return null;
  }
  const line = value.line === null || value.line === void 0 || value.line === "" ? NaN : Number(value.line);
  const blockStartValue = value.blockStart === null || value.blockStart === void 0 || value.blockStart === "" ? NaN : Number(value.blockStart);
  const blockEndValue = value.blockEnd === null || value.blockEnd === void 0 || value.blockEnd === "" ? NaN : Number(value.blockEnd);
  const positionLine = value.positionLine === null || value.positionLine === void 0 || value.positionLine === "" ? NaN : Number(value.positionLine);
  const avoidanceLine = value.avoidanceLine === null || value.avoidanceLine === void 0 || value.avoidanceLine === "" ? NaN : Number(value.avoidanceLine);
  const positionBasis = value.positionBasis === "document" ? "document" : value.positionBasis === "above" && Number.isFinite(positionLine) && positionLine >= 0 ? "above" : null;
  const placementMode = value.placementMode === "inline" ? "inline" : "row";
  const hasStoredPlacement = Number.isFinite(line) && line >= 0 && ["before", "after"].includes(value.side) && Number(value.positionVersion) >= 1;
  const path = normalizeVaultPath(value.path || "");
  const blockStart = Number.isFinite(blockStartValue) && blockStartValue >= 0 ? Math.floor(blockStartValue) : Number.isFinite(line) && line >= 0 ? Math.floor(line) : null;
  const blockEnd = Number.isFinite(blockEndValue) && blockEndValue >= 0 ? Math.floor(blockEndValue) : blockStart;
  return {
    enabled: true,
    path,
    line: Number.isFinite(line) && line >= 0 ? line : null,
    side: ["before", "after"].includes(value.side) ? value.side : null,
    blockStart,
    blockEnd,
    blockKey: noteFlowBlockKey({ path, line, blockStart, blockEnd }),
    positionBasis,
    positionPath: normalizeVaultPath(value.positionPath || value.path || ""),
    positionLine: positionBasis === "above" ? positionLine : null,
    positionVersion: Number(value.positionVersion) >= 1 ? 1 : 0,
    placementVersion: Number(value.placementVersion) >= 1 || hasStoredPlacement ? 1 : 0,
    placementMode,
    inlineSide: placementMode === "inline" && ["left", "right"].includes(value.inlineSide) ? value.inlineSide : null,
    leftSnap: placementMode === "row" && value.leftSnap === true,
    flowOrder: Number.isFinite(Number(value.flowOrder)) ? Number(value.flowOrder) : null,
    rowOffset: clamp10(Number(value.rowOffset) || 0, 0, 2e5),
    boxLeftRatio: clamp10(Number(value.boxLeftRatio) || 0, -2, 3),
    boxWidthRatio: clamp10(Number(value.boxWidthRatio) || 0, 0, 5),
    boxHeightRatio: clamp10(Number(value.boxHeightRatio) || 0, 0, 5),
    avoidancePath: Number.isFinite(avoidanceLine) && avoidanceLine >= 0 ? normalizeVaultPath(value.avoidancePath || value.path || "") : "",
    avoidanceLine: Number.isFinite(avoidanceLine) && avoidanceLine >= 0 ? avoidanceLine : null,
    gap: clamp10(Number(value.gap) || 12, 4, 64)
  };
}
function normalizeMindMapNode(value) {
  if (!value || typeof value.mapId !== "string" || typeof value.nodeId !== "string") {
    return null;
  }
  const sourceLine = Number(value.sourceLine);
  const sourceEndLine = Number(value.sourceEndLine);
  const type = ["root", "heading", "paragraph", "list", "task", "quote", "table", "code"].includes(value.type) ? value.type : value.role === "root" ? "root" : value.role === "heading" ? "heading" : "paragraph";
  return {
    mapId: value.mapId,
    nodeId: value.nodeId,
    parentId: typeof value.parentId === "string" ? value.parentId : "",
    depth: Math.max(0, Math.round(Number(value.depth) || 0)),
    role: value.role === "root" ? "root" : value.role === "heading" ? "heading" : "node",
    type,
    sourcePath: normalizeVaultPath(value.sourcePath || ""),
    sourceLine: Number.isFinite(sourceLine) && sourceLine >= 0 ? Math.round(sourceLine) : -1,
    sourceEndLine: Number.isFinite(sourceEndLine) && sourceEndLine >= 0 ? Math.round(sourceEndLine) : Number.isFinite(sourceLine) && sourceLine >= 0 ? Math.round(sourceLine) : -1,
    sourceText: typeof value.sourceText === "string" ? value.sourceText : "",
    affectsSource: value.affectsSource !== false
  };
}
function normalizeTextRenderMode(mode) {
  if ([TEXT_RENDER_MARKDOWN, TEXT_RENDER_HTML, TEXT_RENDER_NOTE].includes(mode)) {
    return mode;
  }
  return TEXT_RENDER_PLAIN;
}
function normalizeEmbedType(type) {
  if ([EMBED_IMAGE, EMBED_VIDEO, EMBED_FILE].includes(type)) {
    return type;
  }
  return EMBED_FILE;
}
function isAssetTextPreset(preset) {
  return ["image", "video", "attachment"].includes(preset);
}
function filePickerAcceptForPreset(preset) {
  if (preset === "image") {
    return "image/*";
  }
  if (preset === "video") {
    return "video/*";
  }
  return "image/*,video/*,.pdf,.md,.markdown,.txt,.csv,.json,.html,.htm";
}
function classifyImportedAsset(asset) {
  const mime = String(asset?.mime || "").toLowerCase();
  const name = String(asset?.name || "").toLowerCase();
  if (mime.startsWith("image/") || /\.(png|jpe?g|gif|webp|bmp|svg)$/.test(name)) {
    return EMBED_IMAGE;
  }
  if (mime.startsWith("video/") || /\.(mp4|webm|mov|m4v|ogv)$/.test(name)) {
    return EMBED_VIDEO;
  }
  return EMBED_FILE;
}
function classifyImportedPreviewRender(asset) {
  const mime = String(asset?.mime || "").toLowerCase();
  const name = String(asset?.name || "").toLowerCase();
  if (mime === "text/markdown" || /\.(md|markdown)$/.test(name)) {
    return TEXT_RENDER_MARKDOWN;
  }
  if (mime === "text/html" || /\.(html|htm)$/.test(name)) {
    return TEXT_RENDER_HTML;
  }
  return null;
}
function isTextAssetMime(name, mime) {
  const lowerName = String(name || "").toLowerCase();
  const lowerMime = String(mime || "").toLowerCase();
  return lowerMime.startsWith("text/") || lowerMime === "application/json" || /\.(md|markdown|txt|csv|json|html|htm)$/.test(lowerName);
}
function guessMimeType(name) {
  const lower = String(name || "").toLowerCase();
  if (/\.(png|jpg|jpeg|gif|webp|bmp|svg)$/.test(lower)) {
    return lower.endsWith(".svg") ? "image/svg+xml" : "image/*";
  }
  if (/\.(mp4|webm|mov|m4v|ogv)$/.test(lower)) {
    return "video/*";
  }
  if (lower.endsWith(".pdf")) {
    return "application/pdf";
  }
  if (lower.endsWith(".md") || lower.endsWith(".markdown")) {
    return "text/markdown";
  }
  if (/\.(html|htm)$/.test(lower)) {
    return "text/html";
  }
  if (/\.(txt|csv)$/.test(lower)) {
    return "text/plain";
  }
  if (lower.endsWith(".json")) {
    return "application/json";
  }
  return "application/octet-stream";
}
function formatBytes(value) {
  const size = Number(value || 0);
  if (!Number.isFinite(size) || size <= 0) {
    return "Attachment";
  }
  if (size < 1024) {
    return `${Math.round(size)} B`;
  }
  if (size < 1024 * 1024) {
    return `${(size / 1024).toFixed(1)} KB`;
  }
  return `${(size / (1024 * 1024)).toFixed(1)} MB`;
}
function unwrapWikiLink(value) {
  const text = String(value || "").trim();
  const wiki = text.match(/^\[\[([^\]|#]+)(?:#[^\]|]+)?(?:\|[^\]]+)?\]\]$/);
  if (wiki) {
    return wiki[1].trim();
  }
  return text.replace(/^!?\[\[/, "").replace(/\]\]$/, "").trim();
}
function getEmbedRenderToken(stroke) {
  return [
    stroke.kind,
    stroke.embedType,
    normalizeTextRenderMode(stroke.render),
    stroke.text || "",
    stroke.assetPath || "",
    stroke.assetName || "",
    stroke.assetSize || 0,
    stroke.commandId || "",
    stroke.commandName || "",
    stroke.exportImageDataUrl ? String(stroke.exportImageDataUrl).length : 0
  ].join("|");
}
function sanitizeHTMLToDomSafe(content) {
  const parsed = new DOMParser().parseFromString(String(content || ""), "text/html");
  const fragment = activeDocument.createDocumentFragment();
  Array.from(parsed.body.childNodes).forEach((node) => {
    fragment.appendChild(activeDocument.importNode(node, true));
  });
  fragment.querySelectorAll("script, iframe, object, embed, link[rel='import']").forEach((node) => node.remove());
  fragment.querySelectorAll("*").forEach((element) => {
    for (const attribute of Array.from(element.attributes || [])) {
      const name = attribute.name.toLowerCase();
      const value = attribute.value || "";
      if (name.startsWith("on") || /^\s*javascript:/i.test(value)) {
        element.removeAttribute(attribute.name);
      }
    }
  });
  return fragment;
}
function portableTimestamp(value) {
  const timestamp = Date.parse(String(value || ""));
  return Number.isFinite(timestamp) ? timestamp : 0;
}
function normalizePortableBundle(value, file2) {
  if (!value || typeof value !== "object" || !value.drawing || typeof value.drawing !== "object") {
    return null;
  }
  return {
    format: "notedraw-portable",
    version: Math.max(1, Number(value.version) || 1),
    purpose: value.purpose === "share" ? "share" : "storage",
    sourcePath: normalizeVaultPath(value.sourcePath || file2?.path || ""),
    updatedAt: typeof value.updatedAt === "string" ? value.updatedAt : value.drawing?.updatedAt || null,
    drawing: normalizeDrawingData(value.drawing, file2),
    resources: normalizePortableResources(value.resources),
    skippedResources: Array.isArray(value.skippedResources) ? value.skippedResources.map(String).filter(Boolean) : []
  };
}
function normalizePortableResources(value) {
  return Array.isArray(value) ? value.map(normalizePortableResource).filter(Boolean) : [];
}
function normalizePortableResource(value) {
  const dataBase64 = typeof value?.dataBase64 === "string" ? value.dataBase64.replace(/\s+/g, "") : "";
  if (!dataBase64 || !/^[A-Za-z0-9+/]*={0,2}$/.test(dataBase64)) {
    return null;
  }
  const source = typeof value?.source === "string" ? value.source : "";
  const resolvedPath = typeof value?.resolvedPath === "string" ? value.resolvedPath : "";
  const name = sanitizeAssetFileName(value?.name || portableUrlName(resolvedPath || source) || "attachment.bin");
  const aliases = Array.from(new Set([
    source,
    resolvedPath,
    name,
    ...Array.from(Array.isArray(value?.aliases) ? value.aliases : [])
  ].map((item) => String(item || "").trim()).filter(Boolean)));
  return {
    id: typeof value?.id === "string" && value.id ? value.id : portableResourceId(resolvedPath || source || name),
    source,
    resolvedPath,
    aliases,
    name,
    mime: String(value?.mime || portableMimeType(name) || "application/octet-stream").split(";")[0].trim().toLowerCase(),
    size: Number.isFinite(Number(value?.size)) ? Math.max(0, Number(value.size)) : Math.floor(dataBase64.length * 0.75),
    dataBase64
  };
}
function findPortableResource(resources, reference, aliases = []) {
  const targets = portableReferenceKeys([reference, ...aliases]);
  if (!targets.exact.size && !targets.basename.size) {
    return null;
  }
  let basenameMatch = null;
  for (const resource of normalizePortableResources(resources)) {
    const keys = portableReferenceKeys([resource.source, resource.resolvedPath, resource.name, ...resource.aliases]);
    if (Array.from(targets.exact).some((key) => keys.exact.has(key))) {
      return resource;
    }
    if (!basenameMatch && Array.from(targets.basename).some((key) => keys.basename.has(key))) {
      basenameMatch = resource;
    }
  }
  return basenameMatch;
}
function portableReferenceKeys(values) {
  const exact = /* @__PURE__ */ new Set();
  const basename = /* @__PURE__ */ new Set();
  for (const value of values.flatMap((item) => Array.isArray(item) ? item : [item])) {
    let text = String(value || "").trim();
    if (!text) {
      continue;
    }
    try {
      text = decodeURIComponent(text);
    } catch (error) {
      void error;
    }
    text = unwrapWikiLink(text.replace(/^!/, "")).split("|")[0].trim();
    const withoutFragment = text.replace(/[?#].*$/, "").replace(/\\/g, "/").replace(/^\/+/, "");
    for (const candidate of [text, withoutFragment]) {
      if (candidate) {
        exact.add(candidate.toLowerCase());
      }
    }
    let name = withoutFragment.split("/").pop() || "";
    if (/^[a-z]+:\/\//i.test(text)) {
      try {
        name = decodeURIComponent(new URL(text).pathname.split("/").pop() || name);
      } catch (error) {
        void error;
      }
    }
    if (name) {
      basename.add(name.toLowerCase());
    }
  }
  return { exact, basename };
}
function portableResourceIdentity(resource) {
  return String(resource?.resolvedPath || resource?.source || resource?.id || "");
}
function portableResourceId(value) {
  return `${PORTABLE_RESOURCE_PREFIX}-${hashString(String(value || "resource"))}`;
}
function portableResourceDataUrl(resource) {
  const normalized = normalizePortableResource(resource);
  return normalized ? `data:${normalized.mime || "application/octet-stream"};base64,${normalized.dataBase64}` : "";
}
function portableResourceText(resource) {
  const normalized = normalizePortableResource(resource);
  if (!normalized) {
    return "";
  }
  const binary = atob(normalized.dataBase64);
  const bytes = new Uint8Array(binary.length);
  for (let index = 0; index < binary.length; index += 1) {
    bytes[index] = binary.charCodeAt(index);
  }
  return new TextDecoder().decode(bytes);
}
function portableMimeType(name) {
  const lower = String(name || "").split(/[?#]/)[0].toLowerCase();
  const types = {
    ".png": "image/png",
    ".jpg": "image/jpeg",
    ".jpeg": "image/jpeg",
    ".gif": "image/gif",
    ".webp": "image/webp",
    ".bmp": "image/bmp",
    ".svg": "image/svg+xml",
    ".mp4": "video/mp4",
    ".webm": "video/webm",
    ".mov": "video/quicktime",
    ".m4v": "video/x-m4v",
    ".mp3": "audio/mpeg",
    ".wav": "audio/wav",
    ".m4a": "audio/mp4",
    ".ogg": "audio/ogg",
    ".pdf": "application/pdf",
    ".md": "text/markdown",
    ".markdown": "text/markdown",
    ".txt": "text/plain",
    ".csv": "text/csv",
    ".json": "application/json",
    ".html": "text/html",
    ".htm": "text/html",
    ".docx": "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
    ".pptx": "application/vnd.openxmlformats-officedocument.presentationml.presentation",
    ".xlsx": "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"
  };
  const extension = Object.keys(types).find((candidate) => lower.endsWith(candidate));
  return extension ? types[extension] : "application/octet-stream";
}
function portableResponseMime(headers) {
  if (!headers) {
    return "";
  }
  if (typeof headers.get === "function") {
    return String(headers.get("content-type") || "").split(";")[0].trim();
  }
  const entry = Object.entries(headers).find(([key]) => key.toLowerCase() === "content-type");
  return String(entry?.[1] || "").split(";")[0].trim();
}
function portableUrlName(value) {
  try {
    const url = new URL(String(value || ""));
    return decodeURIComponent(url.pathname.split("/").pop() || url.hostname || "resource.bin");
  } catch {
    return String(value || "").replace(/\\/g, "/").split("/").pop() || "resource.bin";
  }
}
function extractPortableMarkdownLinks(markdown) {
  const source = stripNotedrawDataBlocks(markdown);
  const links = [];
  for (const match of source.matchAll(/!?\[\[([^\]]+)\]\]/g)) {
    links.push(match[1]);
  }
  for (const match of source.matchAll(/!?\[[^\]]*\]\(\s*(?:<([^>]+)>|([^\s)]+))/g)) {
    links.push(match[1] || match[2]);
  }
  for (const match of source.matchAll(/<((?:https?):\/\/[^>]+)>/gi)) {
    links.push(match[1]);
  }
  return Array.from(new Set(links.map((link) => String(link || "").trim()).filter(Boolean)));
}
function extractPortableHtmlLinks(html) {
  const links = [];
  for (const match of String(html || "").matchAll(/\b(?:src|href)\s*=\s*(?:"([^"]+)"|'([^']+)'|([^\s>]+))/gi)) {
    links.push(match[1] || match[2] || match[3]);
  }
  return Array.from(new Set(links.map((link) => String(link || "").trim()).filter(Boolean)));
}
function portableElementReferences(element) {
  const values = [
    element.getAttribute?.("src"),
    element.getAttribute?.("href"),
    element.getAttribute?.("data-src"),
    element.getAttribute?.("data-href"),
    element.getAttribute?.("data-path"),
    element.getAttribute?.("alt"),
    element.dataset?.href,
    element.dataset?.src,
    element.dataset?.path
  ];
  const text = String(element.textContent || "").trim();
  if (text && text.length <= 240) {
    values.push(text);
  }
  return Array.from(new Set(values.map((value) => String(value || "").trim()).filter(Boolean)));
}
function createEmptyDrawingData(file2) {
  return {
    version: 3,
    sourcePath: file2.path,
    sourceRevision: sourceRevisionForFile(file2),
    visible: true,
    strokes: [],
    markdownBlocks: [],
    elementGroups: [],
    noteFlowLayout: normalizeFrozenNoteFlowLayout(null),
    webEdits: [],
    updatedAt: null
  };
}
function normalizeSourceRevision(value) {
  return typeof value === "string" ? value.trim().slice(0, 160) : "";
}
function sourceRevisionForFile(file2) {
  const stat = file2?.stat || {};
  const ctime = Number.isFinite(Number(stat.ctime)) ? Math.max(0, Math.round(Number(stat.ctime))) : 0;
  const mtime = Number.isFinite(Number(stat.mtime)) ? Math.max(0, Math.round(Number(stat.mtime))) : 0;
  const size = Number.isFinite(Number(stat.size)) ? Math.max(0, Math.round(Number(stat.size))) : 0;
  return `${ctime}:${mtime}:${size}`;
}
function createDrawingDataFile(path) {
  const normalized = normalizeVaultPath(path) || "NoteDraw.md";
  const name = normalized.split("/").pop() || "NoteDraw.md";
  const dot = name.lastIndexOf(".");
  return {
    path: normalized,
    name,
    basename: dot > 0 ? name.slice(0, dot) : name,
    extension: dot > 0 ? name.slice(dot + 1) : "md"
  };
}
function clonePortableValue(value) {
  return JSON.parse(JSON.stringify(value));
}
function waitForNextFrame() {
  return new Promise((resolve) => {
    if (typeof window.requestAnimationFrame === "function") {
      window.requestAnimationFrame(resolve);
    } else {
      window.setTimeout(resolve, 16);
    }
  });
}
function parseNoteDrawElementLink(rawLink) {
  const raw = String(rawLink || "").trim();
  if (!raw.toLowerCase().startsWith("notedraw://element")) {
    return null;
  }
  try {
    const url = new URL(raw);
    if (url.protocol !== "notedraw:" || url.hostname.toLowerCase() !== "element") {
      return null;
    }
    const file2 = normalizeVaultPath(url.searchParams.get("file") || "");
    const ids = Array.from(new Set(
      [
        ...url.searchParams.getAll("id"),
        ...url.searchParams.getAll("ids").flatMap((value) => value.split(","))
      ].map((id) => String(id || "").trim()).filter(Boolean)
    ));
    return file2 && ids.length ? { file: file2, ids } : null;
  } catch {
    return null;
  }
}
async function writeTextToClipboard(text) {
  const value = String(text ?? "");
  try {
    if (typeof navigator !== "undefined" && typeof navigator.clipboard?.writeText === "function") {
      await navigator.clipboard.writeText(value);
      return true;
    }
  } catch (error) {
    void error;
  }
  const textarea = activeDocument.createElement("textarea");
  textarea.value = value;
  textarea.setAttribute("readonly", "true");
  applyElementStyles(textarea, { position: "fixed", opacity: "0" });
  activeDocument.body?.appendChild(textarea);
  textarea.select();
  let copied = false;
  try {
    copied = Boolean(activeDocument.execCommand?.("copy"));
  } catch {
    copied = false;
  }
  textarea.remove();
  return copied;
}
function normalizeDrawingData(data, file2) {
  const strokes = Array.isArray(data?.strokes) ? data.strokes : [];
  return {
    version: Math.max(1, Number.isFinite(data?.version) ? data.version : 1),
    sourcePath: file2.path,
    sourceRevision: normalizeSourceRevision(data?.sourceRevision) || sourceRevisionForFile(file2),
    visible: data?.visible !== false,
    strokes: strokes.map(normalizeStroke).map((stroke) => ({
      ...stroke,
      points: compactStrokePoints(stroke.points)
    })).filter((stroke) => stroke.points.length),
    markdownBlocks: normalizeMarkdownBlocks(data?.markdownBlocks, file2),
    elementGroups: normalizeElementGroups(data?.elementGroups),
    noteFlowLayout: normalizeFrozenNoteFlowLayout(data?.noteFlowLayout),
    webEdits: normalizeWebEdits(data?.webEdits),
    updatedAt: data?.updatedAt || null
  };
}
function parseExplicitMarkdownLineGroup(value) {
  const parts = String(value || "").split("\0");
  if (parts.length < 4) {
    return null;
  }
  const path = normalizeVaultPath(parts[0]);
  const start = Number(parts[1]);
  const end = Number(parts[2]);
  if (!path || !Number.isFinite(start) || !Number.isFinite(end) || end < start) {
    return null;
  }
  return { path, start: Math.round(start), end: Math.round(end) };
}
function normalizeMarkdownBlocks(value, file2) {
  if (!Array.isArray(value)) {
    return [];
  }
  const seen = /* @__PURE__ */ new Set();
  const normalized = value.map((block, index) => {
    const path = normalizeVaultPath(block?.path || file2?.path || "");
    const textHint = normalizeRenderedText2(block?.textHint || "").slice(0, 240);
    const lineStart = Number.isFinite(Number(block?.lineStart)) ? Math.max(0, Math.round(Number(block.lineStart))) : null;
    const id = typeof block?.id === "string" && block.id ? block.id : `md-${hashString(`${path}:${lineStart ?? "x"}:${textHint}:${index}`)}`;
    const floatBox = normalizeMarkdownFloatBox(block?.floatBox);
    const normalized2 = {
      id,
      path,
      lineStart,
      lineEnd: Number.isFinite(Number(block?.lineEnd)) ? Math.max(lineStart ?? 0, Math.round(Number(block.lineEnd))) : lineStart,
      textHint,
      renderKind: block?.renderKind === "task" ? "task" : "",
      span: clamp10(Math.round(Number(block?.span) || 12), 1, 12),
      noteFlowAutoSpan: Boolean(block?.noteFlowAutoSpan),
      widthScale: normalizeMarkdownBlockWidthScale(block?.widthScale),
      inlineWidthMode: normalizeMarkdownInlineWidthMode(
        block?.inlineWidthMode,
        block?.span,
        block?.widthScale
      ),
      minHeight: normalizeMarkdownBlockMinHeight(block?.minHeight),
      borderColor: isPaletteColor(block?.borderColor) ? block.borderColor : "",
      backgroundColor: isPaletteColor(block?.backgroundColor) ? block.backgroundColor : "",
      contentColor: isPaletteColor(block?.contentColor) ? block.contentColor : "",
      contentOpacity: clamp10(Number(block?.contentOpacity ?? 1), 0, 1),
      contentScale: clamp10(Number(block?.contentScale ?? 1), 0.5, 3),
      floating: Boolean(block?.floating && floatBox),
      floatingExplicit: Boolean(block?.floatingExplicit),
      floatBox,
      locked: Boolean(block?.locked),
      groupId: typeof block?.groupId === "string" ? block.groupId : "",
      commandId: typeof block?.commandId === "string" ? block.commandId.trim() : "",
      commandName: typeof block?.commandName === "string" ? block.commandName.trim() : "",
      explicitLineGroup: typeof block?.explicitLineGroup === "string" ? block.explicitLineGroup : ""
    };
    const explicit = parseExplicitMarkdownLineGroup(normalized2.explicitLineGroup);
    if (explicit && (explicit.path !== path || !Number.isFinite(lineStart) || lineStart < explicit.start || lineStart > explicit.end || normalized2.lineEnd < explicit.start || normalized2.lineEnd > explicit.end)) {
      return null;
    }
    return normalized2;
  }).filter((block) => block && block.path && block.id && !seen.has(block.id) && seen.add(block.id));
  return dedupeMarkdownBlockRecords(normalized);
}
function normalizeMarkdownBlockWidthScale(value) {
  const widthScale = Number(value);
  return Number.isFinite(widthScale) ? clamp10(widthScale, 0.2, 1) : 1;
}
function normalizeMarkdownInlineWidthMode(value, span = 12, widthScale = 1) {
  if (value === "fixed" || value === "fit") {
    return value;
  }
  return Number(span) < 12 || normalizeMarkdownBlockWidthScale(widthScale) < 0.999 ? "fixed" : "fit";
}
function taskSourceRangeLooksRelative(block, element, info) {
  if (!element?.matches?.("li.task-list-item") || !Number.isFinite(Number(block?.lineStart)) || !Number.isFinite(Number(info?.lineStart))) {
    return false;
  }
  const list = element.closest?.("ul.contains-task-list,ol.contains-task-list");
  const renderedLines = Array.from(list?.querySelectorAll?.(":scope > li.task-list-item") || []).map((item) => Number(getSourceInfo(item).lineStart)).filter(Number.isFinite);
  if (!renderedLines.length) {
    return false;
  }
  const renderedMax = Math.max(...renderedLines);
  return Math.min(...renderedLines) <= 2 && renderedMax <= renderedLines.length * 4 && Number(block.lineStart) > renderedMax + 2;
}
function markdownElementRenderKind(element) {
  return element?.matches?.("li.task-list-item") ? "task" : "";
}
function markdownInlineWidthCss(span, widthScale = 1, element = null) {
  const columns = clamp10(Math.round(Number(span) || 12), 1, 12);
  const percent = Math.round(columns / 12 * normalizeMarkdownBlockWidthScale(widthScale) * 1e4) / 100;
  const gap = element?.matches?.("li") ? INLINE_NOTE_FLOW_LIST_GAP_PX : INLINE_NOTE_FLOW_GAP_PX;
  return `max(${MIN_INLINE_NOTE_FLOW_ITEM_WIDTH_PX}px, calc(${percent}% - ${gap}px))`;
}
function collectDeletedVaultFiles(file2, output = []) {
  if (!file2?.path) {
    return output;
  }
  if (Array.isArray(file2.children)) {
    for (const child of file2.children) {
      collectDeletedVaultFiles(child, output);
    }
    return output;
  }
  output.push(file2);
  return output;
}
function normalizeElementGroups(value) {
  if (!Array.isArray(value)) {
    return [];
  }
  const seen = /* @__PURE__ */ new Set();
  return value.map((group) => ({
    id: typeof group?.id === "string" ? group.id : "",
    boxed: Boolean(group?.boxed),
    locked: group?.locked !== false,
    boxOwnedLock: group?.boxOwnedLock === void 0 ? Boolean(group?.boxed) : Boolean(group.boxOwnedLock),
    relationOwned: Boolean(group?.relationOwned),
    relationKey: typeof group?.relationKey === "string" ? group.relationKey : "",
    borderColor: isPaletteColor(group?.borderColor) ? group.borderColor : "#64748b",
    backgroundColor: isPaletteColor(group?.backgroundColor) ? group.backgroundColor : "",
    opacity: clamp10(Number(group?.opacity ?? 1), 0, 1)
  })).filter((group) => group.id && !seen.has(group.id) && seen.add(group.id));
}
function normalizeDrawingDataForStorage(data, file2) {
  const normalized = normalizeDrawingData(data, file2);
  normalized.strokes = normalized.strokes.map((stroke) => {
    const layout = normalizeElementLayout(stroke.layout);
    if (!layout || elementLayoutNeedsRepair(layout) || !stroke.points.some((point) => point.anchor)) {
      return stroke;
    }
    return {
      ...stroke,
      points: projectElementPoints(stroke.points, layout, {
        id: layout.id,
        x: layout.box.x,
        y: layout.box.y,
        width: layout.box.width,
        height: layout.box.height,
        scale: 1
      }, {
        canvasWidth: layout.sourceFrame.surfaceWidth,
        canvasHeight: layout.sourceFrame.documentHeight
      })
    };
  });
  return normalized;
}
function cloneDrawingData(data, file2) {
  return JSON.parse(JSON.stringify(normalizeDrawingDataForStorage(data, file2)));
}
function normalizeWebEdits(value) {
  if (!Array.isArray(value)) {
    return [];
  }
  return value.map((edit) => ({
    kind: edit?.kind === "text" ? "text" : "",
    path: typeof edit?.path === "string" ? edit.path : "",
    originalText: typeof edit?.originalText === "string" ? edit.originalText : "",
    editedText: typeof edit?.editedText === "string" ? edit.editedText : "",
    updatedAt: typeof edit?.updatedAt === "string" ? edit.updatedAt : null
  })).filter((edit) => edit.kind === "text" && edit.path && normalizeRenderedText2(edit.editedText));
}
function normalizeStroke(stroke) {
  const points = Array.isArray(stroke?.points) ? stroke.points : [];
  const kind = normalizeStrokeKind(stroke?.kind);
  const brush = stroke?.brush === BRUSH_WATERCOLOR ? BRUSH_WATERCOLOR : BRUSH_PEN;
  const layout = normalizeElementLayout(stroke?.layout);
  const connector = normalizeConnector(stroke?.connector);
  const noteFlow = normalizeNoteFlow(stroke?.noteFlow) || (!connector && stroke?.belowMarkdown ? normalizeNoteFlow({ enabled: true, placementMode: "row" }) : null);
  return {
    elementId: typeof stroke?.elementId === "string" && stroke.elementId ? stroke.elementId : layout?.id || "",
    kind,
    embedType: normalizeEmbedType(stroke?.embedType),
    brush,
    variant: normalizeBrushVariant(brush, stroke?.variant),
    color: typeof stroke?.color === "string" ? stroke.color : "#e53935",
    width: Number.isFinite(Number(stroke?.width)) ? clamp10(Number(stroke.width), MIN_BRUSH_WIDTH, MAX_BRUSH_WIDTH) : 3,
    opacity: clamp10(Number(stroke?.opacity ?? DEFAULT_PEN_OPACITY), 0, 1),
    count: clamp10(Math.round(Number(stroke?.count) || 1), 1, MAX_PEN_COUNT),
    text: typeof stroke?.text === "string" ? stroke.text : "",
    render: normalizeTextRenderMode(stroke?.render),
    assetPath: normalizeVaultPath(stroke?.assetPath || ""),
    assetName: typeof stroke?.assetName === "string" ? stroke.assetName : "",
    assetMime: typeof stroke?.assetMime === "string" ? stroke.assetMime : "",
    assetSize: Number.isFinite(Number(stroke?.assetSize)) ? Math.max(0, Number(stroke.assetSize)) : 0,
    exportImageDataUrl: normalizeImageDataUrl(stroke?.exportImageDataUrl),
    previewWidth: Number.isFinite(Number(stroke?.previewWidth)) ? clamp10(Number(stroke.previewWidth), 80, 900) : 260,
    previewHeight: Number.isFinite(Number(stroke?.previewHeight)) ? clamp10(Number(stroke.previewHeight), 40, 700) : 160,
    textWidth: Number.isFinite(Number(stroke?.textWidth)) && Number(stroke.textWidth) > 0 ? clamp10(Number(stroke.textWidth), 24, 900) : null,
    fontSize: Number.isFinite(Number(stroke?.fontSize)) ? clamp10(Number(stroke.fontSize), 10, 72) : 18,
    bold: Boolean(stroke?.bold),
    code: Boolean(stroke?.code),
    boxed: Boolean(stroke?.boxed),
    file: Boolean(stroke?.file),
    uiRole: normalizeUiRole(stroke?.uiRole),
    buttonStyle: normalizeButtonStyle(stroke?.buttonStyle),
    commandId: typeof stroke?.commandId === "string" ? stroke.commandId.trim() : "",
    commandName: typeof stroke?.commandName === "string" ? stroke.commandName.trim() : "",
    snap: Boolean(stroke?.snap),
    locked: Boolean(stroke?.locked),
    groupId: typeof stroke?.groupId === "string" ? stroke.groupId : "",
    belowMarkdown: Boolean(noteFlow),
    connector,
    noteFlow,
    mindMapNode: normalizeMindMapNode(stroke?.mindMapNode),
    textAnchor: normalizeTextHighlightAnchor(stroke?.textAnchor),
    layout,
    points: points.map((point) => ({
      x: clamp10(Number(point?.x), 0, 1),
      y: clamp10(Number(point?.y), 0, 1),
      t: Number.isFinite(Number(point?.t)) ? Number(point.t) : Date.now(),
      anchor: normalizeResponsiveAnchor(point?.anchor)
    })).filter((point) => Number.isFinite(point.x) && Number.isFinite(point.y))
  };
}
function normalizeTextHighlightAnchor(value) {
  const path = normalizeVaultPath(value?.path || "");
  const lineStart = Number(value?.lineStart);
  const lineEnd = Number(value?.lineEnd);
  const u = Array.isArray(value?.u) ? value.u.map((item) => clamp10(Number(item), 0, 1)).filter(Number.isFinite) : [];
  if (!path || !Number.isFinite(lineStart) || !u.length) {
    return null;
  }
  return {
    mode: value?.mode === "detached" ? "detached" : "hard",
    ownerType: "markdown",
    ownerId: typeof value?.ownerId === "string" && value.ownerId ? value.ownerId : typeof value?.blockId === "string" ? value.blockId : "",
    path,
    lineStart,
    lineEnd: Number.isFinite(lineEnd) ? lineEnd : lineStart,
    blockId: typeof value?.blockId === "string" ? value.blockId : "",
    textHint: typeof value?.textHint === "string" ? value.textHint.slice(0, 240) : "",
    baseline: clamp10(Number(value?.baseline ?? 0.58), 0, 1),
    lineIndex: Number.isInteger(Number(value?.lineIndex)) && Number(value.lineIndex) >= 0 ? Math.round(Number(value.lineIndex)) : null,
    lineKey: typeof value?.lineKey === "string" ? value.lineKey.slice(0, 80) : "",
    u
  };
}
function textBindingEvidenceTokens(value) {
  const text = normalizeRenderedText2(value).trim().toLocaleLowerCase();
  if (!text) {
    return [];
  }
  const words = text.split(/\s+/).map((word) => word.replace(/^\W+|\W+$/g, "")).filter((word) => word.length >= 2);
  if (words.length > 1) {
    return Array.from(new Set(words));
  }
  const compact = text.replace(/\s+/g, "");
  if (compact.length <= 4) {
    return [compact];
  }
  const size = compact.length >= 12 ? 3 : 2;
  const tokens = [];
  for (let index = 0; index <= compact.length - size; index += size) {
    tokens.push(compact.slice(index, index + size));
  }
  return Array.from(new Set(tokens));
}
function isTextStroke(stroke) {
  return stroke?.kind === TOOL_TEXT && normalizeTextRenderMode(stroke.render) === TEXT_RENDER_PLAIN && typeof stroke.text === "string" && stroke.text.trim().length > 0;
}
function isRichTextStroke(stroke) {
  return stroke?.kind === TOOL_TEXT && normalizeTextRenderMode(stroke.render) !== TEXT_RENDER_PLAIN && typeof stroke.text === "string" && stroke.text.trim().length > 0;
}
function isTextLikeStroke(stroke) {
  return (isTextStroke(stroke) || isRichTextStroke(stroke)) && typeof stroke.text === "string" && stroke.text.trim().length > 0;
}
function isEmbedStroke(stroke) {
  return stroke?.kind === TOOL_EMBED && Boolean(stroke.assetPath || stroke.text || stroke.assetName);
}
function isImageEmbedStroke(stroke) {
  return stroke?.kind === TOOL_EMBED && normalizeEmbedType(stroke.embedType) === EMBED_IMAGE && Boolean(stroke.assetPath || stroke.exportImageDataUrl);
}
function strokeElementId(stroke) {
  return typeof stroke?.elementId === "string" && stroke.elementId ? stroke.elementId : normalizeElementLayout(stroke?.layout)?.id || "";
}
function isConnectorStroke(stroke) {
  return Boolean(normalizeConnector(stroke?.connector));
}
function isNoteFlowInkStroke(stroke) {
  return stroke?.brush === BRUSH_PEN && normalizeBrushVariant(BRUSH_PEN, stroke?.variant) === PEN_VARIANT_NOTE && Boolean(normalizeNoteFlow(stroke?.noteFlow));
}
function isButtonLikeStroke(stroke) {
  return stroke?.uiRole === "button" || Boolean(normalizeButtonStyle(stroke?.buttonStyle));
}
function isSnapStroke(stroke) {
  return Boolean(stroke?.snap) || Boolean(stroke?.boxed) || stroke?.uiRole === "button" || stroke?.uiRole === "arrow" || isConnectorStroke(stroke);
}
function isSnapPreset(preset) {
  return ["button", "rectangle", "circle", "arrow", "buttonPrimary", "buttonOutline", "buttonPill", "arrowUp", "arrowDown", "arrowLeft", "arrowRight"].includes(preset);
}
function instantTextForPreset(preset) {
  return {
    arrowUp: "\u2191",
    arrowDown: "\u2193",
    arrowLeft: "\u2190",
    arrowRight: "\u2192"
  }[preset] || "";
}
function normalizeUiRole(value) {
  return ["button", "arrow"].includes(value) ? value : "";
}
function normalizeButtonStyle(value) {
  return ["solid", "outline", "pill", "circle"].includes(value) ? value : "";
}
function createTextPreset(preset, text, color) {
  const normalized = String(text || "").trim();
  if (preset === "title") {
    return { kind: TOOL_TEXT, text: normalized, render: TEXT_RENDER_PLAIN, color, fontSize: 26, bold: true, code: false, boxed: false, file: false };
  }
  if (preset === "code") {
    return { kind: TOOL_TEXT, text: normalized, render: TEXT_RENDER_PLAIN, color: "#374151", fontSize: 16, bold: false, code: true, boxed: true, file: false };
  }
  if (preset === "button") {
    return { kind: TOOL_TEXT, text: normalized, render: TEXT_RENDER_PLAIN, color, fontSize: 17, bold: true, code: false, boxed: true, file: false, uiRole: "button", buttonStyle: "", snap: true };
  }
  if (preset === "buttonPrimary") {
    return { kind: TOOL_TEXT, text: normalized, render: TEXT_RENDER_PLAIN, color: "#2563eb", fontSize: 17, bold: true, code: false, boxed: true, file: false, uiRole: "button", buttonStyle: "solid", snap: true };
  }
  if (["rectangle", "buttonOutline"].includes(preset)) {
    return { kind: TOOL_TEXT, text: normalized, render: TEXT_RENDER_PLAIN, color: "#2563eb", fontSize: 17, bold: true, code: false, boxed: true, file: false, uiRole: "button", buttonStyle: "outline", snap: true };
  }
  if (["circle", "buttonPill"].includes(preset)) {
    return { kind: TOOL_TEXT, text: normalized, render: TEXT_RENDER_PLAIN, color: "#7c3aed", fontSize: 17, bold: true, code: false, boxed: true, file: false, uiRole: "button", buttonStyle: preset === "circle" ? "solid" : "pill", snap: true };
  }
  if (["arrowUp", "arrowDown", "arrowLeft", "arrowRight"].includes(preset)) {
    return { kind: TOOL_TEXT, text: normalized, render: TEXT_RENDER_PLAIN, color: "#111827", fontSize: 28, bold: true, code: false, boxed: false, file: false, uiRole: "arrow", buttonStyle: "", snap: true };
  }
  if (preset === "file") {
    return { kind: TOOL_TEXT, text: normalized.startsWith("@") ? normalized : `@${normalized}`, render: TEXT_RENDER_PLAIN, color, fontSize: 17, bold: false, code: false, boxed: true, file: true };
  }
  if (preset === "markdown") {
    return { kind: TOOL_TEXT, text: normalized, render: TEXT_RENDER_MARKDOWN, color: "#1f2937", fontSize: 16, bold: false, code: false, boxed: true, file: false, previewWidth: 300, previewHeight: 180 };
  }
  if (preset === "html") {
    return { kind: TOOL_TEXT, text: normalized, render: TEXT_RENDER_HTML, color: "#1f2937", fontSize: 16, bold: false, code: true, boxed: true, file: false, previewWidth: 300, previewHeight: 180 };
  }
  if (preset === "note") {
    return { kind: TOOL_TEXT, text: normalized, render: TEXT_RENDER_NOTE, color: "#1f2937", fontSize: 16, bold: false, code: false, boxed: true, file: true, previewWidth: 320, previewHeight: 220 };
  }
  return { kind: TOOL_TEXT, text: normalized, render: TEXT_RENDER_PLAIN, color, fontSize: 18, bold: false, code: false, boxed: false, file: false };
}
function compactDrawingData(data, compactDistance = DEFAULT_SETTINGS.drawingCompactDistance) {
  if (!Array.isArray(data?.strokes)) {
    return data;
  }
  data.strokes = data.strokes.map((stroke) => ({
    ...stroke,
    points: compactStrokePoints(stroke.points, compactDistance)
  }));
  return data;
}
function compactStrokePoints(points, compactDistance = DEFAULT_SETTINGS.drawingCompactDistance) {
  if (!Array.isArray(points) || points.length <= 2) {
    return points || [];
  }
  const compacted = [points[0]];
  for (let index = 1; index < points.length - 1; index += 1) {
    const point = points[index];
    const last = compacted[compacted.length - 1];
    const distance = pointDistanceOnCanvas(last, point, 1, 1) * 1e3;
    if (distance >= compactDistance) {
      compacted.push(point);
    }
  }
  compacted.push(points[points.length - 1]);
  return compacted;
}
function getSourceInfo(element) {
  const lineStart = parseInteger(element.dataset.noteDrawLineStart);
  const lineEnd = parseInteger(element.dataset.noteDrawLineEnd);
  const dataLine = parseInteger(element.dataset.noteDrawDataLine) ?? parseDataLine(element.closest("[data-line]")?.getAttribute("data-line"));
  const dataLineScope = element.dataset.noteDrawDataLineScope || (Number.isFinite(parseDataLine(element.getAttribute("data-line"))) ? "self" : "ancestor");
  const exactDataLine = dataLineScope === "self" && !Number.isFinite(lineStart) ? dataLine : null;
  const resolvedStart = lineStart ?? exactDataLine ?? dataLine ?? null;
  const resolvedEnd = lineEnd ?? exactDataLine ?? resolvedStart;
  return {
    lineStart: resolvedStart,
    lineEnd: resolvedEnd,
    dataLine,
    dataLineScope,
    sourceText: typeof element._noteDrawSourceText === "string" ? element._noteDrawSourceText : null
  };
}
function markdownDragSourceIdentity(element) {
  if (element?.matches?.("hr,.el-hr") || element?.querySelector?.(":scope > hr")) {
    return {
      sourceInfo: getSourceInfo(element),
      sourceText: "---",
      embedDestination: ""
    };
  }
  const embed = findMarkdownEmbedBlockElement(element);
  const owner = embed?.matches?.(".internal-embed") ? embed : embed?.closest?.(".internal-embed") || embed?.querySelector?.(".internal-embed");
  const linkedFileSelector = "[data-cancip-inline-path],[data-file-path],[data-embed-path]";
  const linkedFile = element?.matches?.(linkedFileSelector) ? element : element?.closest?.(linkedFileSelector) || element?.querySelector?.(linkedFileSelector);
  const embedDestination = owner?.getAttribute?.("data-src") || owner?.getAttribute?.("data-path") || owner?.getAttribute?.("src") || linkedFile?.getAttribute?.("data-cancip-inline-path") || linkedFile?.getAttribute?.("data-file-path") || linkedFile?.getAttribute?.("data-embed-path") || "";
  if (!embedDestination) {
    return {
      sourceInfo: getSourceInfo(element),
      sourceText: String(renderedMarkdownIdentityText(element) || ""),
      embedDestination: ""
    };
  }
  const hostLineElement = (owner || linkedFile || element).closest?.("[data-note-draw-line-start],[data-line]");
  const hostLine = parseInteger(hostLineElement?.dataset?.noteDrawLineStart) ?? parseDataLine(hostLineElement?.getAttribute?.("data-line"));
  return {
    sourceInfo: {
      lineStart: hostLine,
      lineEnd: hostLine,
      dataLine: hostLine,
      dataLineScope: "self",
      sourceText: null
    },
    sourceText: `![[${embedDestination}]]`,
    embedDestination
  };
}
function resolveSourceEditTarget(source, sourceInfo, originalText) {
  const normalizedOriginal = normalizeRenderedText2(originalText);
  if (!normalizedOriginal) {
    return null;
  }
  const blocks = collectMarkdownBlocks(source);
  const sourceLine = sourceInfo?.lineStart ?? (sourceInfo?.dataLineScope === "self" ? sourceInfo?.dataLine : null);
  const fencedMatch = blocks.find((block) => Number.isFinite(sourceLine) && sourceLine >= block.line && sourceLine <= block.endLine && /^\s*(`{3,}|~{3,})/.test(block.text) && normalizeMarkdownBlock(block.text) === normalizedOriginal);
  let match = fencedMatch || pickFromSectionText(source, sourceInfo, normalizedOriginal) || collectSourceLineBlock(source, sourceInfo, normalizedOriginal) || pickLineInSourceRange(source, sourceInfo, normalizedOriginal) || pickBlockInSourceRange(blocks, sourceInfo, normalizedOriginal) || pickBlockBySourceInfo(blocks, sourceInfo, normalizedOriginal);
  if (!match) {
    const candidates = blocks.filter((block) => normalizeMarkdownBlock(block.text) === normalizedOriginal);
    match = pickNearestBlock(candidates, sourceLine);
  }
  if (!match) {
    const partialCandidates = blocks.filter((block) => {
      const normalized = normalizeMarkdownBlock(block.text);
      return isReasonablePartialMatch(block, normalized, normalizedOriginal);
    });
    match = pickNearestBlock(partialCandidates, sourceLine);
  }
  if (!match) {
    match = pickNearestPlainLine(source, normalizedOriginal, sourceLine);
  }
  return match ? createTextEditTarget(match, sourceInfo, originalText) : null;
}
function resolveSourceDropTarget(source, sourceInfo, originalText, sourceIndex = null, embedDestination = "") {
  const stableTarget = embedDestination ? resolveMarkdownEmbedSourceTarget(source, embedDestination, sourceInfo, sourceIndex) : resolveRenderedMarkdownSourceTarget(source, originalText, sourceInfo, sourceIndex);
  return stableTarget ? createTextEditTarget(stableTarget, sourceInfo, originalText) : null;
}
function resolveLockedTarget(source, target, baselineText) {
  if (!target) {
    return null;
  }
  const normalizedBaseline = normalizeRenderedText2(baselineText);
  const start = Number(target.start);
  const end = Number(target.end);
  if (isValidSourceRange(source, start, end)) {
    const currentText = source.slice(start, end);
    const normalizedCurrent = normalizeMarkdownBlock(currentText);
    if (currentText === target.text || normalizedCurrent === normalizedBaseline || normalizedCurrent === target.normalizedText) {
      return {
        ...target,
        text: currentText
      };
    }
  }
  const exactIndex = findNearestTextIndex(source, target.text, target.start);
  if (exactIndex >= 0) {
    return {
      ...target,
      start: exactIndex,
      end: exactIndex + target.text.length,
      text: target.text
    };
  }
  return null;
}
function createTextEditTarget(match, sourceInfo, renderedText) {
  if (!match || !Number.isFinite(match.start) || !Number.isFinite(match.end)) {
    return null;
  }
  const text = String(match.text ?? "");
  return {
    start: match.start,
    end: match.end,
    line: Number.isFinite(match.line) ? match.line : null,
    endLine: Number.isFinite(match.endLine) ? match.endLine : match.line ?? null,
    text,
    normalizedText: normalizeRenderedText2(renderedText),
    normalizedMarkdown: normalizeMarkdownBlock(text),
    sourceInfo: {
      lineStart: sourceInfo?.lineStart ?? null,
      lineEnd: sourceInfo?.lineEnd ?? null,
      dataLine: sourceInfo?.dataLine ?? null
    }
  };
}
function pickFromSectionText(source, sourceInfo, normalizedOriginal) {
  const sectionText = typeof sourceInfo?.sourceText === "string" ? sourceInfo.sourceText : "";
  if (!sectionText.trim()) {
    return null;
  }
  const section = locateSectionRange(source, sourceInfo, sectionText);
  if (!section) {
    return null;
  }
  const lineMatch = pickNearestPlainLine(section.text, normalizedOriginal, null);
  if (lineMatch) {
    return shiftMatch(lineMatch, section.start);
  }
  const blocks = collectMarkdownBlocks(section.text);
  let match = blocks.find((block) => normalizeMarkdownBlock(block.text) === normalizedOriginal);
  if (!match) {
    match = blocks.find((block) => {
      const normalized = normalizeMarkdownBlock(block.text);
      return isReasonablePartialMatch(block, normalized, normalizedOriginal);
    });
  }
  if (match) {
    return shiftMatch(match, section.start);
  }
  const normalizedSection = normalizeMarkdownBlock(section.text);
  const sectionLines = section.text.split(/\r?\n/).filter((line) => line.trim()).length;
  if (sectionLines <= 3 && isReasonableLineMatch(normalizedSection, normalizedOriginal)) {
    return {
      start: section.start,
      end: section.end,
      line: sourceInfo?.lineStart ?? null,
      endLine: sourceInfo?.lineEnd ?? sourceInfo?.lineStart ?? null,
      text: section.text
    };
  }
  return null;
}
function locateSectionRange(source, sourceInfo, sectionText) {
  const byLines = collectSourceLineRange(source, sourceInfo?.lineStart, sourceInfo?.lineEnd);
  if (byLines && (normalizeMarkdownBlock(byLines.text) === normalizeMarkdownBlock(sectionText) || normalizeMarkdownBlock(byLines.text).includes(normalizeMarkdownBlock(sectionText)) || normalizeMarkdownBlock(sectionText).includes(normalizeMarkdownBlock(byLines.text)))) {
    return byLines;
  }
  const preferredStart = getLineStartOffset(source, sourceInfo?.lineStart) ?? 0;
  const exactIndex = findNearestTextIndex(source, sectionText, preferredStart);
  if (exactIndex >= 0) {
    return {
      start: exactIndex,
      end: exactIndex + sectionText.length,
      line: sourceInfo?.lineStart ?? null,
      endLine: sourceInfo?.lineEnd ?? sourceInfo?.lineStart ?? null,
      text: source.slice(exactIndex, exactIndex + sectionText.length)
    };
  }
  return byLines;
}
function pickLineInSourceRange(source, sourceInfo, normalizedOriginal) {
  const lineStart = sourceInfo?.lineStart;
  const lineEnd = sourceInfo?.lineEnd ?? lineStart;
  if (!Number.isFinite(lineStart) || !Number.isFinite(lineEnd)) {
    return null;
  }
  const start = Math.max(0, Math.min(lineStart, lineEnd) - 1);
  const end = Math.max(lineStart, lineEnd) + 1;
  const candidates = collectLineMatches(source, normalizedOriginal).filter((match) => match.line >= start && match.line <= end);
  return pickNearestBlock(candidates, lineStart);
}
function pickNearestPlainLine(source, normalizedOriginal, sourceLine) {
  return pickNearestBlock(collectLineMatches(source, normalizedOriginal), sourceLine);
}
function collectLineMatches(source, normalizedOriginal) {
  const matches = [];
  const lines = source.split(/(\r?\n)/);
  let offset = 0;
  let inFence = false;
  for (let index = 0, currentLine = 0; index < lines.length; index += 2, currentLine += 1) {
    const line = lines[index] || "";
    const newline = lines[index + 1] || "";
    const trimmed = line.trim();
    const start = offset;
    const end = start + line.length;
    offset += line.length + newline.length;
    if (/^```|^~~~/.test(trimmed)) {
      inFence = !inFence;
      continue;
    }
    if (inFence || !trimmed) {
      continue;
    }
    const normalizedLine = normalizeMarkdownBlock(line);
    if (normalizedLine === normalizedOriginal || normalizedLine && normalizedOriginal && normalizedLine.includes(normalizedOriginal) || isReasonableLineMatch(normalizedLine, normalizedOriginal)) {
      matches.push({
        start,
        end,
        line: currentLine,
        endLine: currentLine,
        text: line
      });
    }
  }
  return matches;
}
function collectSourceLineRange(source, lineStart, lineEnd) {
  if (!Number.isFinite(lineStart)) {
    return null;
  }
  const startLine = Math.max(0, Math.min(lineStart, Number.isFinite(lineEnd) ? lineEnd : lineStart));
  const endLine = Math.max(startLine, Number.isFinite(lineEnd) ? Math.max(lineStart, lineEnd) : lineStart);
  const lines = source.split(/(\r?\n)/);
  let offset = 0;
  let start = null;
  let end = null;
  for (let index = 0, currentLine = 0; index < lines.length; index += 2, currentLine += 1) {
    const line = lines[index] || "";
    const newline = lines[index + 1] || "";
    const lineStartOffset = offset;
    const lineEndOffset = lineStartOffset + line.length;
    offset += line.length + newline.length;
    if (currentLine === startLine) {
      start = lineStartOffset;
    }
    if (currentLine === endLine) {
      end = lineEndOffset;
      break;
    }
  }
  if (!Number.isFinite(start)) {
    return null;
  }
  if (!Number.isFinite(end)) {
    end = source.length;
  }
  return {
    start,
    end,
    line: startLine,
    endLine,
    text: source.slice(start, end)
  };
}
function getLineStartOffset(source, wantedLine) {
  if (!Number.isFinite(wantedLine) || wantedLine < 0) {
    return null;
  }
  const lines = source.split(/(\r?\n)/);
  let offset = 0;
  for (let index = 0, currentLine = 0; index < lines.length; index += 2, currentLine += 1) {
    if (currentLine === wantedLine) {
      return offset;
    }
    offset += (lines[index] || "").length + (lines[index + 1] || "").length;
  }
  return null;
}
function shiftMatch(match, offset) {
  return {
    ...match,
    start: match.start + offset,
    end: match.end + offset
  };
}
function pickBlockInSourceRange(blocks, sourceInfo, normalizedOriginal) {
  const lineStart = sourceInfo?.lineStart;
  const lineEnd = sourceInfo?.lineEnd ?? lineStart;
  if (!Number.isFinite(lineStart) || !Number.isFinite(lineEnd)) {
    return null;
  }
  const start = Math.min(lineStart, lineEnd);
  const end = Math.max(lineStart, lineEnd);
  const candidates = blocks.filter((block) => block.line <= end && (block.endLine ?? block.line) >= start);
  const exact = candidates.find((block) => normalizeMarkdownBlock(block.text) === normalizedOriginal);
  if (exact) {
    return exact;
  }
  return candidates.find((block) => {
    const normalized = normalizeMarkdownBlock(block.text);
    return isReasonablePartialMatch(block, normalized, normalizedOriginal);
  }) || null;
}
function pickBlockBySourceInfo(blocks, sourceInfo, normalizedOriginal) {
  const lineStart = sourceInfo?.lineStart ?? (sourceInfo?.dataLineScope === "self" ? sourceInfo?.dataLine : null);
  if (!Number.isFinite(lineStart)) {
    return null;
  }
  const lineMatches = blocks.filter((block) => block.line <= lineStart && lineStart <= (block.endLine ?? block.line));
  const exact = lineMatches.find((block) => normalizeMarkdownBlock(block.text) === normalizedOriginal);
  if (exact) {
    return exact;
  }
  return lineMatches.find((block) => {
    const normalized = normalizeMarkdownBlock(block.text);
    return isReasonablePartialMatch(block, normalized, normalizedOriginal);
  }) || null;
}
function collectSourceLineBlock(source, sourceInfo, normalizedOriginal) {
  const primaryLine = sourceInfo?.dataLineScope === "self" ? sourceInfo?.dataLine : sourceInfo?.lineStart;
  if (!Number.isFinite(primaryLine)) {
    return null;
  }
  const candidateLines = [
    primaryLine,
    primaryLine - 1,
    primaryLine + 1
  ].filter((line, index, list) => Number.isFinite(line) && line >= 0 && list.indexOf(line) === index);
  const lines = source.split(/(\r?\n)/);
  let offset = 0;
  for (let index = 0, currentLine = 0; index < lines.length; index += 2, currentLine += 1) {
    const line = lines[index] || "";
    const newline = lines[index + 1] || "";
    const start = offset;
    const end = start + line.length;
    offset += line.length + newline.length;
    if (!candidateLines.includes(currentLine) || !line.trim()) {
      continue;
    }
    const normalizedLine = normalizeMarkdownBlock(line);
    if (normalizedLine === normalizedOriginal || normalizedLine && normalizedOriginal && normalizedLine.includes(normalizedOriginal) || isReasonableLineMatch(normalizedLine, normalizedOriginal)) {
      return {
        start,
        end,
        line: currentLine,
        endLine: currentLine,
        text: line
      };
    }
    return null;
  }
  return null;
}
function isReasonableLineMatch(normalizedLine, normalizedOriginal) {
  if (!normalizedLine || !normalizedOriginal) {
    return false;
  }
  if (normalizedLine.includes(normalizedOriginal) || normalizedOriginal.includes(normalizedLine)) {
    const longer = Math.max(normalizedLine.length, normalizedOriginal.length);
    const shorter = Math.min(normalizedLine.length, normalizedOriginal.length);
    return longer > 0 && shorter / longer > 0.75;
  }
  return false;
}
function isReasonablePartialMatch(block, normalized, normalizedOriginal) {
  if (!normalized || !normalizedOriginal) {
    return false;
  }
  const isPartial = normalized.includes(normalizedOriginal) || normalizedOriginal.includes(normalized);
  if (!isPartial) {
    return false;
  }
  const lineSpan = (block.endLine ?? block.line) - block.line;
  if (lineSpan <= 0) {
    return true;
  }
  const longer = Math.max(normalized.length, normalizedOriginal.length);
  const shorter = Math.min(normalized.length, normalizedOriginal.length);
  return longer > 0 && shorter / longer > 0.75;
}
function isValidSourceRange(source, start, end) {
  return Number.isFinite(start) && Number.isFinite(end) && start >= 0 && end >= start && end <= source.length;
}
function findNearestTextIndex(source, text, preferredStart) {
  const needle = String(text || "");
  if (!needle) {
    return -1;
  }
  let bestIndex = -1;
  let bestDistance = Number.POSITIVE_INFINITY;
  let index = source.indexOf(needle);
  while (index >= 0) {
    const distance = Math.abs(index - (Number(preferredStart) || 0));
    if (distance < bestDistance) {
      bestDistance = distance;
      bestIndex = index;
    }
    index = source.indexOf(needle, index + Math.max(1, needle.length));
  }
  return bestIndex;
}
function summarizeSourceInfo(sourceInfo) {
  if (!sourceInfo) {
    return null;
  }
  return {
    lineStart: sourceInfo.lineStart ?? null,
    lineEnd: sourceInfo.lineEnd ?? null,
    dataLine: sourceInfo.dataLine ?? null,
    dataLineScope: sourceInfo.dataLineScope ?? null,
    hasSourceText: typeof sourceInfo.sourceText === "string" && sourceInfo.sourceText.length > 0,
    sourceTextLength: typeof sourceInfo.sourceText === "string" ? sourceInfo.sourceText.length : 0,
    sourceTextSample: shortText(sourceInfo.sourceText)
  };
}
function summarizeTarget(target) {
  if (!target) {
    return null;
  }
  return {
    start: Number.isFinite(target.start) ? target.start : null,
    end: Number.isFinite(target.end) ? target.end : null,
    line: Number.isFinite(target.line) ? target.line : null,
    endLine: Number.isFinite(target.endLine) ? target.endLine : null,
    textLength: typeof target.text === "string" ? target.text.length : 0,
    textSample: shortText(target.text)
  };
}
function shortText(value) {
  const text = String(value || "").replace(/\s+/g, " ").trim();
  if (text.length <= 120) {
    return text;
  }
  return `${text.slice(0, 120)}...`;
}
function parseDataLine(value) {
  if (!value) {
    return null;
  }
  const matches = String(value).match(/\d+/g);
  if (!matches?.length) {
    return null;
  }
  return Number.parseInt(matches[0], 10);
}
function parseInteger(value) {
  const parsed = Number.parseInt(value || "", 10);
  return Number.isFinite(parsed) ? parsed : null;
}
function pointerDistance(a, b) {
  return Math.hypot((a.x || 0) - (b.x || 0), (a.y || 0) - (b.y || 0));
}
function findResponsiveContentElement(previewEl, surfaceType) {
  if (!previewEl) {
    return null;
  }
  if (surfaceType === "source") {
    return previewEl.querySelector?.(".cm-content") || previewEl.querySelector?.(".cm-sizer") || previewEl;
  }
  return previewEl.querySelector?.(":scope > .markdown-preview-sizer") || previewEl.querySelector?.(".markdown-preview-sizer") || previewEl;
}
function measureResponsiveContentFrame(previewEl, surfaceType, surfaceWidth, canvas, visualScale = 1) {
  const content = findResponsiveContentElement(previewEl, surfaceType);
  const contentRect = content?.getBoundingClientRect?.();
  const surfaceRect = previewEl?.getBoundingClientRect?.() || canvas?.getBoundingClientRect?.();
  const scale = Math.max(0.01, Number(visualScale) || 1);
  if (!contentRect || !surfaceRect || contentRect.width <= 1) {
    return constrainWideContentFrame({
      surfaceWidth,
      contentLeft: 0,
      contentWidth: surfaceWidth
    }, { isMobile: isMobileRuntime() });
  }
  return constrainWideContentFrame({
    surfaceWidth,
    contentLeft: (contentRect.left - surfaceRect.left + (Number(previewEl?.scrollLeft) || 0)) / scale,
    contentWidth: contentRect.width / scale
  }, { isMobile: isMobileRuntime() });
}
function measureResponsiveViewportHeight(previewEl, scrollContainer, visualScale = 1) {
  const scrollRect = scrollContainer?.getBoundingClientRect?.();
  const scale = Math.max(0.01, Number(visualScale) || 1);
  const candidates = [
    scrollContainer === previewEl ? previewEl?.clientHeight : scrollContainer?.clientHeight,
    scrollRect?.height,
    window.visualViewport?.height,
    window.innerHeight,
    previewEl?.clientHeight
  ];
  return Math.max(1, (Number(candidates.find((value) => Number(value) > 1)) || 1) / scale);
}
function createElementLayoutId(index = -1) {
  return `el-${Date.now().toString(36)}-${Math.max(0, Number(index) || 0).toString(36)}-${Math.random().toString(36).slice(2, 9)}`;
}
function needsElementLayoutMigration(strokes) {
  const ids = /* @__PURE__ */ new Set();
  return (Array.isArray(strokes) ? strokes : []).some((stroke) => {
    if (isConnectorStroke(stroke)) {
      return false;
    }
    const id = normalizeElementLayout(stroke?.layout)?.id;
    if (!id || ids.has(id)) {
      return true;
    }
    ids.add(id);
    return false;
  });
}
function isStableResponsiveCaptureFrame(surfaceWidth, frame) {
  const width = Number(surfaceWidth) || 0;
  const contentWidth = Number(frame?.width) || 0;
  const stableWideLane = width >= 900 && contentWidth >= 720;
  return width >= 180 && contentWidth >= 140 && (contentWidth / width >= 0.42 || stableWideLane);
}
function responsiveLayoutSignature(width, height, frame, surfaceType, viewportHeight) {
  return [
    surfaceType,
    Math.round(Number(width) || 0),
    Math.round(Number(height) || 0),
    Math.round(Number(frame?.left) || 0),
    Math.round(Number(frame?.width) || 0),
    Math.round(Number(viewportHeight) || 0)
  ].join(":");
}
function collectRenderedLineAnchors(root, canvas, canvasWindowTop, visualScale = 1) {
  if (!root || !canvas) {
    return [];
  }
  const canvasRect = canvas.getBoundingClientRect();
  const scale = Math.max(0.01, Number(visualScale) || 1);
  const anchors = [];
  for (const element of root.querySelectorAll?.("[data-note-draw-line-start]") || []) {
    const start = parseInteger(element.dataset.noteDrawLineStart);
    const end = parseInteger(element.dataset.noteDrawLineEnd) ?? start;
    const path = normalizeVaultPath(element.dataset.noteDrawSourcePath || "");
    const rect = element.getBoundingClientRect?.();
    if (!Number.isFinite(start) || !Number.isFinite(end) || !path || !rect || rect.width <= 1 || rect.height <= 1) {
      continue;
    }
    anchors.push({
      path,
      start: Math.min(start, end),
      end: Math.max(start, end),
      left: (rect.left - canvasRect.left) / scale,
      right: (rect.right - canvasRect.left) / scale,
      top: (rect.top - canvasRect.top) / scale + canvasWindowTop,
      bottom: (rect.bottom - canvasRect.top) / scale + canvasWindowTop,
      height: rect.height / scale,
      area: rect.width * rect.height / (scale * scale),
      confidence: clamp10(Number(element.dataset.noteDrawLineConfidence ?? 1), 0, 1)
    });
  }
  return anchors;
}
function collectVirtualMarkdownLineAnchors(view, root, canvas, canvasWindowTop, fallbackPath, visualScale = 1) {
  const renderer = view?.previewMode?.renderer;
  const sections = Array.isArray(renderer?.sections) ? renderer.sections : [];
  if (!root || !canvas || !sections.length || renderer?.previewEl !== root) {
    return [];
  }
  const canvasRect = canvas.getBoundingClientRect();
  const scale = Math.max(0.01, Number(visualScale) || 1);
  const sizer = renderer.sizerEl || root.querySelector?.(":scope > .markdown-preview-sizer") || root.querySelector?.(".markdown-preview-sizer");
  const sizerRect = sizer?.getBoundingClientRect?.();
  const baseTop = sizerRect ? (sizerRect.top - canvasRect.top) / scale + canvasWindowTop : 0;
  const frame = measureResponsiveContentFrame(root, "preview", Math.max(1, Number(root.clientWidth) || Number(root.scrollWidth) / scale || 1), canvas, scale);
  return buildVirtualMarkdownSectionAnchors(sections.map((section) => {
    const element = section?.el;
    const measured = Number(element?.offsetHeight) > 0 && Number.isFinite(Number(element?.offsetTop));
    return {
      startLine: section?.start?.line,
      endLine: section?.end?.line,
      height: Math.max(Number(section?.height) || 0, Number(element?.offsetHeight) || 0, 1),
      measuredTop: measured ? Number(element.offsetTop) : null,
      excluded: Boolean(element?.matches?.(".mod-ui, .mod-header, .mod-footer"))
    };
  }), {
    baseTop,
    left: frame.left,
    right: frame.left + frame.width,
    path: normalizeVaultPath(fallbackPath),
    confidence: 0.96
  });
}
function captureRenderedLineLocation(anchors, canvasX, canvasY, { maxDistance = 64 } = {}) {
  const horizontal = anchors.filter((anchor2) => canvasX >= anchor2.left - 28 && canvasX <= anchor2.right + 28);
  const containing = horizontal.filter((anchor2) => canvasY >= anchor2.top && canvasY <= anchor2.bottom);
  const candidates = containing.length ? containing : horizontal.map((anchor2) => ({
    ...anchor2,
    distance: canvasY < anchor2.top ? anchor2.top - canvasY : canvasY > anchor2.bottom ? canvasY - anchor2.bottom : 0
  })).filter((anchor2) => anchor2.distance <= maxDistance);
  const anchor = candidates.sort((a, b) => (a.distance || 0) - (b.distance || 0) || a.area - b.area)[0];
  if (!anchor) {
    return null;
  }
  const ratio = clamp10((canvasY - anchor.top) / Math.max(1, anchor.height), 0, 0.999999);
  const distance = Number(anchor.distance || 0);
  const inside = canvasY >= anchor.top && canvasY <= anchor.bottom;
  return {
    path: anchor.path,
    line: anchor.start + ratio * Math.max(1, anchor.end - anchor.start + 1),
    lineConfidence: inside ? anchor.confidence : Math.min(anchor.confidence, clamp10(1 - distance / Math.max(1, maxDistance), 0, 0.55))
  };
}
function projectRenderedLineLocation(anchors, path, line) {
  const normalizedPath = normalizeVaultPath(path);
  const lineNumber = Number(line);
  if (!normalizedPath || !Number.isFinite(lineNumber)) {
    return NaN;
  }
  const integerLine = Math.floor(lineNumber);
  const candidates = anchors.filter((anchor2) => anchor2.path === normalizedPath && integerLine >= anchor2.start && integerLine <= anchor2.end);
  const anchor = candidates.sort((a, b) => (b.confidence || 0) - (a.confidence || 0) || a.end - a.start - (b.end - b.start) || a.area - b.area)[0];
  if (!anchor) {
    return NaN;
  }
  const ratio = clamp10((lineNumber - anchor.start) / Math.max(1, anchor.end - anchor.start + 1), 0, 1);
  return anchor.top + ratio * anchor.height;
}
function captureCodeMirrorLineLocation(codeMirror, clientX, clientY, sourcePath) {
  try {
    const position = codeMirror.posAtCoords?.({ x: clientX, y: clientY }, false) ?? codeMirror.posAtCoords?.({ x: clientX, y: clientY });
    const doc = codeMirror.state?.doc;
    if (!Number.isFinite(position) || !doc?.lineAt) {
      return null;
    }
    const line = doc.lineAt(position);
    const startRect = codeMirror.coordsAtPos?.(line.from);
    const endRect = codeMirror.coordsAtPos?.(line.to);
    const top = Math.min(startRect?.top ?? clientY, endRect?.top ?? startRect?.top ?? clientY);
    const bottom = Math.max(startRect?.bottom ?? clientY + 1, endRect?.bottom ?? startRect?.bottom ?? clientY + 1);
    const ratio = clamp10((clientY - top) / Math.max(1, bottom - top), 0, 0.999999);
    return {
      path: normalizeVaultPath(sourcePath),
      line: Math.max(0, line.number - 1) + ratio,
      lineConfidence: clientY >= top && clientY <= bottom ? 1 : 0.45
    };
  } catch (error) {
    void error;
    return null;
  }
}
function projectCodeMirrorLineLocation(codeMirror, linePosition) {
  try {
    const doc = codeMirror.state?.doc;
    const lineNumber = Number(linePosition);
    if (!doc?.line || !Number.isFinite(lineNumber)) {
      return NaN;
    }
    const wantedLine = Math.floor(lineNumber) + 1;
    if (wantedLine < 1 || wantedLine > (doc.lines || 1)) {
      return NaN;
    }
    const line = doc.line(wantedLine);
    const startRect = codeMirror.coordsAtPos?.(line.from);
    const endRect = codeMirror.coordsAtPos?.(line.to);
    if (!startRect && !endRect) {
      return NaN;
    }
    const top = Math.min(startRect?.top ?? endRect.top, endRect?.top ?? startRect.top);
    const bottom = Math.max(startRect?.bottom ?? endRect.bottom, endRect?.bottom ?? startRect.bottom);
    return top + clamp10(lineNumber - Math.floor(lineNumber), 0, 1) * Math.max(1, bottom - top);
  } catch (error) {
    void error;
    return NaN;
  }
}
function findLayoutMeasureElement(previewEl) {
  return previewEl?.querySelector?.(".markdown-preview-sizer") || previewEl?.querySelector?.(".cm-sizer") || previewEl?.querySelector?.(".cm-content") || previewEl;
}
function measureVisibleSurfaceWindow(previewEl, scrollContainer, documentHeight, visualScale = 1) {
  const height = Math.max(1, Number(documentHeight) || 1);
  const scale = Math.max(0.01, Number(visualScale) || 1);
  const previewRect = previewEl?.getBoundingClientRect?.();
  if (scrollContainer === previewEl) {
    const viewportHeight = Math.max(1, (previewEl.clientHeight || previewRect?.height || window.innerHeight || 1) / scale);
    return {
      top: clamp10((Number(previewEl.scrollTop) || 0) / scale, 0, Math.max(0, height - viewportHeight)),
      height: Math.min(height, viewportHeight)
    };
  }
  const viewportRect = scrollContainer?.getBoundingClientRect?.() || {
    top: 0,
    bottom: window.innerHeight || height,
    height: window.innerHeight || height
  };
  const surfaceTop = previewRect?.top || 0;
  const viewportTop = Math.max(0, (viewportRect.top - surfaceTop) / scale);
  const viewportBottom = Math.min(height, ((viewportRect.bottom || viewportRect.top + viewportRect.height) - surfaceTop) / scale);
  const fallbackHeight = Math.max(1, (viewportRect.height || window.innerHeight || 1) / scale);
  const visibleHeight = Math.min(height, Math.max(1, viewportBottom - viewportTop || fallbackHeight));
  const scrollTop = Number(scrollContainer?.scrollTop) || 0;
  const scrollHeight = Number(scrollContainer?.scrollHeight) || 0;
  const clientHeight = Number(scrollContainer?.clientHeight) || Number(viewportRect.height) || 0;
  const atScrollEnd = scrollHeight > clientHeight && scrollTop + clientHeight >= scrollHeight - 3;
  return {
    top: atScrollEnd ? Math.max(0, height - visibleHeight) : clamp10(viewportTop, 0, Math.max(0, height - visibleHeight)),
    height: visibleHeight
  };
}
function clearCanvasContext(context, canvas) {
  if (!context || !canvas) {
    return;
  }
  context.save();
  context.setTransform(1, 0, 0, 1, 0, 0);
  context.clearRect(0, 0, canvas.width, canvas.height);
  context.restore();
}
function getScrollEventTarget(scroller) {
  if (!scroller) {
    return window;
  }
  return scroller === activeDocument.documentElement || scroller === activeDocument.body ? window : scroller;
}
function findScrollableAncestor(element) {
  let current = element;
  while (current && current !== activeDocument.body && current !== activeDocument.documentElement) {
    const style = window.getComputedStyle(current);
    const canScrollY = /(auto|scroll|overlay)/.test(style.overflowY) && current.scrollHeight > current.clientHeight;
    const canScrollX = /(auto|scroll|overlay)/.test(style.overflowX) && current.scrollWidth > current.clientWidth;
    if (canScrollY || canScrollX) {
      return current;
    }
    current = current.parentElement;
  }
  return activeDocument.scrollingElement || activeDocument.documentElement;
}
function loadExportImage(src, timeoutMs) {
  if (!src) {
    return Promise.resolve(null);
  }
  return new Promise((resolve) => {
    const image = new Image();
    let timer = 0;
    const cleanup = () => {
      window.clearTimeout(timer);
      image.removeEventListener("load", done);
      image.removeEventListener("error", fail);
    };
    const done = () => {
      cleanup();
      resolve(image);
    };
    const fail = () => {
      cleanup();
      resolve(null);
    };
    image.decoding = "sync";
    image.loading = "eager";
    image.addEventListener("load", done, { once: true });
    image.addEventListener("error", fail, { once: true });
    timer = window.setTimeout(fail, timeoutMs);
    image.src = src;
    if (image.complete && image.naturalWidth > 0 && image.naturalHeight > 0) {
      done();
    }
  });
}
function arrayBufferToDataUrl(buffer, mime) {
  return `data:${mime || "image/png"};base64,${arrayBufferToBase64(buffer)}`;
}
function arrayBufferToBase64(buffer) {
  const bytes = new Uint8Array(buffer);
  const chunkSize = 32768;
  let binary = "";
  for (let index = 0; index < bytes.length; index += chunkSize) {
    const chunk = bytes.subarray(index, index + chunkSize);
    binary += String.fromCharCode(...chunk);
  }
  return btoa(binary);
}
function normalizeImageDataUrl(value) {
  const text = typeof value === "string" ? value.trim() : "";
  return /^data:image\/[a-z0-9.+-]+;base64,/i.test(text) ? text : "";
}
function getImageStrokeCacheKey(stroke) {
  if (!isImageEmbedStroke(stroke)) {
    return "";
  }
  const dataLength = stroke.exportImageDataUrl ? String(stroke.exportImageDataUrl).length : 0;
  return [stroke.assetPath || "", stroke.assetName || "", stroke.assetSize || 0, dataLength].join("|");
}
function objectFitContain(sourceWidth, sourceHeight, boxWidth, boxHeight) {
  const scale = Math.min(boxWidth / Math.max(1, sourceWidth), boxHeight / Math.max(1, sourceHeight));
  const width = Math.max(1, sourceWidth * scale);
  const height = Math.max(1, sourceHeight * scale);
  return {
    x: (boxWidth - width) / 2,
    y: (boxHeight - height) / 2,
    width,
    height
  };
}
function waitForImage(image, timeoutMs) {
  if (image.complete && image.naturalWidth > 0 && image.naturalHeight > 0) {
    return Promise.resolve();
  }
  return new Promise((resolve) => {
    let timer = 0;
    const cleanup = () => {
      window.clearTimeout(timer);
      image.removeEventListener("load", done);
      image.removeEventListener("error", done);
    };
    const done = () => {
      cleanup();
      resolve();
    };
    image.addEventListener("load", done, { once: true });
    image.addEventListener("error", done, { once: true });
    timer = window.setTimeout(done, timeoutMs);
  });
}
function pointDistanceOnCanvas(a, b, width, height) {
  return Math.hypot(
    ((a?.x || 0) - (b?.x || 0)) * Math.max(1, width || 1),
    ((a?.y || 0) - (b?.y || 0)) * Math.max(1, height || 1)
  );
}
function buildFreeConnectorPoints(fromPoint, toPoint, canvasWidth, canvasHeight) {
  const width = Math.max(1, Number(canvasWidth) || 1);
  const height = Math.max(1, Number(canvasHeight) || 1);
  const from = {
    x: clamp10(Number(fromPoint?.x) || 0, 0, 1) * width,
    y: clamp10(Number(fromPoint?.y) || 0, 0, 1) * height
  };
  const to = {
    x: clamp10(Number(toPoint?.x) || 0, 0, 1) * width,
    y: clamp10(Number(toPoint?.y) || 0, 0, 1) * height
  };
  const horizontal = Math.abs(to.x - from.x) >= Math.abs(to.y - from.y);
  const control = horizontal ? { x: (from.x + to.x) / 2, y: from.y } : { x: from.x, y: (from.y + to.y) / 2 };
  return [from, control, to].map((point) => ({
    x: clamp10(point.x / width, 0, 1),
    y: clamp10(point.y / height, 0, 1),
    t: Date.now(),
    anchor: null
  }));
}
function sameStrokePointPath(left, right, tolerance = 1e-6) {
  if (!Array.isArray(left) || left.length !== right.length) {
    return false;
  }
  return left.every((point, index) => Math.abs(Number(point.x) - Number(right[index].x)) <= tolerance && Math.abs(Number(point.y) - Number(right[index].y)) <= tolerance);
}
function getTextStrokeLayout(stroke, width, measureText = null) {
  const point = stroke?.points?.[0] || { x: 0, y: 0 };
  const fontSize = clamp10(Number(stroke?.fontSize || 18), 10, 72);
  const uiArrow = stroke?.uiRole === "arrow";
  const padded = !uiArrow && (stroke?.boxed || stroke?.code || stroke?.file || isButtonLikeStroke(stroke));
  const canvasX = clamp10(Number(point.x || 0), 0, 1) * Math.max(1, Number(width) || 1);
  const maxWidth = Math.max(fontSize, Math.max(1, Number(width) || 1) - canvasX - Math.max(8, fontSize * 0.45) - 8);
  const layout = computeTextLayout({
    text: String(stroke?.text || "").trim(),
    fontSize,
    textWidth: stroke?.textWidth,
    maxWidth,
    padded,
    measureText
  });
  if (normalizeButtonStyle(stroke?.buttonStyle) !== "circle") {
    return layout;
  }
  const size = Math.max(44, layout.width, layout.height);
  return {
    ...layout,
    paddingX: (size - layout.contentWidth) / 2,
    paddingY: (size - layout.contentHeight) / 2,
    width: size,
    height: size
  };
}
function getStrokeBounds(stroke, width, height) {
  if (!stroke?.points?.length) {
    return null;
  }
  if (isEmbedStroke(stroke) || isRichTextStroke(stroke)) {
    const point = stroke.points[0];
    const previewWidth = clamp10(Number(stroke.previewWidth || 260), 80, 900);
    const previewHeight = clamp10(Number(stroke.previewHeight || 160), 40, 700);
    const x = point.x * width;
    const y = point.y * height;
    return {
      minX: x,
      minY: y,
      maxX: x + previewWidth,
      maxY: y + previewHeight
    };
  }
  if (isTextStroke(stroke)) {
    const point = stroke.points[0];
    const layout = getTextStrokeLayout(stroke, width);
    const x = point.x * width;
    const y = point.y * height;
    return {
      minX: x - layout.paddingX,
      minY: y - layout.paddingY,
      maxX: x + layout.contentWidth + layout.paddingX,
      maxY: y + layout.contentHeight + layout.paddingY
    };
  }
  let minX = Infinity;
  let minY = Infinity;
  let maxX = -Infinity;
  let maxY = -Infinity;
  for (const point of stroke.points) {
    const x = point.x * width;
    const y = point.y * height;
    minX = Math.min(minX, x);
    minY = Math.min(minY, y);
    maxX = Math.max(maxX, x);
    maxY = Math.max(maxY, y);
  }
  return {
    minX,
    minY,
    maxX,
    maxY
  };
}
function normalizeCanvasRect(a, b) {
  return {
    minX: Math.min(a.x, b.x),
    minY: Math.min(a.y, b.y),
    maxX: Math.max(a.x, b.x),
    maxY: Math.max(a.y, b.y)
  };
}
function rectsIntersect(a, b) {
  return a.minX <= b.maxX && a.maxX >= b.minX && a.minY <= b.maxY && a.maxY >= b.minY;
}
function translateNormalizedBounds(bounds, dx, dy) {
  return {
    minX: bounds.minX + dx,
    minY: bounds.minY + dy,
    maxX: bounds.maxX + dx,
    maxY: bounds.maxY + dy
  };
}
function nearestSnapDelta(values, candidates, gridSize, threshold) {
  let best = 0;
  let bestDistance = Number.POSITIVE_INFINITY;
  for (const value of values) {
    if (!Number.isFinite(value)) {
      continue;
    }
    if (gridSize > 0) {
      const gridTarget = Math.round(value / gridSize) * gridSize;
      const gridDelta = gridTarget - value;
      const distance = Math.abs(gridDelta);
      if (distance <= threshold && distance < bestDistance) {
        best = gridDelta;
        bestDistance = distance;
      }
    }
    for (const candidate of candidates) {
      if (!Number.isFinite(candidate)) {
        continue;
      }
      const delta = candidate - value;
      const distance = Math.abs(delta);
      if (distance <= threshold && distance < bestDistance) {
        best = delta;
        bestDistance = distance;
      }
    }
  }
  return best;
}
function getSelectionHandlePointsFromRect(rect) {
  return [
    { handle: "nw", x: rect.x, y: rect.y },
    { handle: "ne", x: rect.x + rect.width, y: rect.y },
    { handle: "sw", x: rect.x, y: rect.y + rect.height },
    { handle: "se", x: rect.x + rect.width, y: rect.y + rect.height }
  ];
}
function getSelectionResizeAnchor(bounds, handle) {
  if (handle === "nw") {
    return { x: bounds.maxX, y: bounds.maxY };
  }
  if (handle === "ne") {
    return { x: bounds.minX, y: bounds.maxY };
  }
  if (handle === "sw") {
    return { x: bounds.maxX, y: bounds.minY };
  }
  return { x: bounds.minX, y: bounds.minY };
}
function getSelectionResizeCorner(bounds, handle) {
  if (handle === "nw") {
    return { x: bounds.minX, y: bounds.minY };
  }
  if (handle === "ne") {
    return { x: bounds.maxX, y: bounds.minY };
  }
  if (handle === "sw") {
    return { x: bounds.minX, y: bounds.maxY };
  }
  return { x: bounds.maxX, y: bounds.maxY };
}
function scaleNormalizedBoundsFromAnchor(bounds, anchor, scaleX, scaleY) {
  const firstX = anchor.x + (bounds.minX - anchor.x) * scaleX;
  const secondX = anchor.x + (bounds.maxX - anchor.x) * scaleX;
  const firstY = anchor.y + (bounds.minY - anchor.y) * scaleY;
  const secondY = anchor.y + (bounds.maxY - anchor.y) * scaleY;
  return {
    minX: Math.min(firstX, secondX),
    minY: Math.min(firstY, secondY),
    maxX: Math.max(firstX, secondX),
    maxY: Math.max(firstY, secondY)
  };
}
function limitSelectionResizeScaleToCanvas(scale, bounds, anchor, axis) {
  const minimum = 0.04;
  const value = Math.max(minimum, Number(scale) || 1);
  const min = axis === "x" ? Number(bounds?.minX) : Number(bounds?.minY);
  const max = axis === "x" ? Number(bounds?.maxX) : Number(bounds?.maxY);
  const origin = axis === "x" ? Number(anchor?.x) : Number(anchor?.y);
  if (![min, max, origin].every(Number.isFinite)) {
    return value;
  }
  let maximum = Number.POSITIVE_INFINITY;
  const negativeDistance = min - origin;
  const positiveDistance = max - origin;
  if (negativeDistance < 0) {
    maximum = Math.min(maximum, origin / -negativeDistance);
  }
  if (positiveDistance > 0) {
    maximum = Math.min(maximum, (1 - origin) / positiveDistance);
  }
  return Math.min(value, Math.max(minimum, maximum));
}
function getPenOffsets(count, width) {
  if (count <= 1) {
    return [{ x: 0, y: 0 }];
  }
  const radius = Math.max(2, Number(width || 3) * 1.15);
  const offsets = [{ x: 0, y: 0 }];
  for (let index = 1; index < count; index += 1) {
    const angle = (index - 1) / Math.max(1, count - 1) * Math.PI * 2;
    offsets.push({
      x: Math.cos(angle) * radius,
      y: Math.sin(angle) * radius
    });
  }
  return offsets;
}
function strokeHitTest(stroke, hitPoint, width, height, threshold) {
  const bounds = getStrokeBounds(stroke, width, height);
  const insideBounds = Boolean(bounds) && hitPoint.x >= bounds.minX - threshold && hitPoint.x <= bounds.maxX + threshold && hitPoint.y >= bounds.minY - threshold && hitPoint.y <= bounds.maxY + threshold;
  if (!insideBounds) {
    return false;
  }
  if (isEmbedStroke(stroke) || isRichTextStroke(stroke) || isTextStroke(stroke)) {
    return true;
  }
  if (stroke.points.length === 1) {
    return pointerDistance({
      x: stroke.points[0].x * width,
      y: stroke.points[0].y * height
    }, hitPoint) <= threshold;
  }
  let previous = {
    x: stroke.points[0].x * width,
    y: stroke.points[0].y * height
  };
  for (let index = 1; index < stroke.points.length; index += 1) {
    const current = {
      x: stroke.points[index].x * width,
      y: stroke.points[index].y * height
    };
    if (distanceToSegment(hitPoint, previous, current) <= threshold) {
      return true;
    }
    previous = current;
  }
  return false;
}
function distanceToSegment(point, start, end) {
  const dx = end.x - start.x;
  const dy = end.y - start.y;
  if (dx === 0 && dy === 0) {
    return pointerDistance(point, start);
  }
  const t = clamp10(((point.x - start.x) * dx + (point.y - start.y) * dy) / (dx * dx + dy * dy), 0, 1);
  const projection = {
    x: start.x + t * dx,
    y: start.y + t * dy
  };
  return pointerDistance(point, projection);
}
function clampCanvasFrameForDisplay(frame, canvasWidth, canvasHeight) {
  if (!frame || !Number.isFinite(frame.x) || !Number.isFinite(frame.y) || !Number.isFinite(frame.width) || !Number.isFinite(frame.height) || frame.width <= 0 || frame.height <= 0) {
    return null;
  }
  const left = clamp10(frame.x, 1, Math.max(1, canvasWidth - 1));
  const top = clamp10(frame.y, 1, Math.max(1, canvasHeight - 1));
  const right = clamp10(frame.x + frame.width, left + 1, Math.max(left + 1, canvasWidth - 1));
  const bottom = clamp10(frame.y + frame.height, top + 1, Math.max(top + 1, canvasHeight - 1));
  return { x: left, y: top, width: right - left, height: bottom - top };
}
function roundRect(ctx, x, y, width, height, radius) {
  const r = Math.max(0, Math.min(radius, width / 2, height / 2));
  ctx.beginPath();
  ctx.moveTo(x + r, y);
  ctx.lineTo(x + width - r, y);
  ctx.quadraticCurveTo(x + width, y, x + width, y + r);
  ctx.lineTo(x + width, y + height - r);
  ctx.quadraticCurveTo(x + width, y + height, x + width - r, y + height);
  ctx.lineTo(x + r, y + height);
  ctx.quadraticCurveTo(x, y + height, x, y + height - r);
  ctx.lineTo(x, y + r);
  ctx.quadraticCurveTo(x, y, x + r, y);
  ctx.closePath();
}
function pickNearestBlock(candidates, sourceLine) {
  if (!candidates.length) {
    return null;
  }
  if (sourceLine === null || sourceLine === void 0) {
    return candidates[0];
  }
  return candidates.slice().sort((a, b) => Math.abs(a.line - sourceLine) - Math.abs(b.line - sourceLine))[0];
}
function formatReplacementBlock(originalBlock, editedText) {
  const original = String(originalBlock || "");
  const edited = normalizeEditableSourceText(editedText);
  const firstLine = original.split(/\r?\n/)[0] || "";
  const fencedCode = firstLine.match(/^(\s*)(`{3,}|~{3,})([^\r\n]*)$/);
  if (fencedCode) {
    return `${fencedCode[1]}${fencedCode[2]}${fencedCode[3]}
${edited}
${fencedCode[1]}${fencedCode[2]}`;
  }
  const heading = firstLine.match(/^(#{1,6}\s+)/);
  if (heading) {
    return `${heading[1]}${edited}`;
  }
  const quote = firstLine.match(/^(\s{0,3}>\s?)/);
  if (quote) {
    return edited.split("\n").map((line) => `${quote[1]}${line}`).join("\n");
  }
  const task = firstLine.match(/^(\s*[-*+]\s+\[[ xX]\]\s+)/);
  if (task) {
    const lines = edited.split("\n");
    return lines.map((line, index) => index === 0 ? `${task[1]}${line}` : line).join("\n");
  }
  const unordered = firstLine.match(/^(\s*[-*+]\s+)/);
  if (unordered) {
    const lines = edited.split("\n");
    return lines.map((line, index) => index === 0 ? `${unordered[1]}${line}` : line).join("\n");
  }
  const ordered = firstLine.match(/^(\s*\d+[.)]\s+)/);
  if (ordered) {
    const lines = edited.split("\n");
    return lines.map((line, index) => index === 0 ? `${ordered[1]}${line}` : line).join("\n");
  }
  return edited;
}
function clamp10(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

// src/main.js
var main_default = notedraw_plugin_default;

/* nosourcemap */